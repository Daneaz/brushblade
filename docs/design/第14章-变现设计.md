# 《字·斗》章节设计文档 · 第 14 章:变现设计

> 版本 v0.6 · 设计阶段
> 配套总文档:《字·斗》GDD v0.4
> **v0.6 重写(2026-07-12)**:按 v0.5 转向(第 19 章)完整重写为 **F2P + 奖励式广告 + 单一月订阅** 变现总章。v0.3 的 Premium 买断制正文废止(决策记录保留于 14.1.1)。变现细节以本章为唯一来源;第 19 章 19.8 缩为指针。

---

## 14.1 模型定位

**F2P(免费下载)+ 奖励式广告(主)+ 单一月订阅(轻内购)**。首发平台:海外移动(iOS + Google Play),仅简体中文。

参照系(F2P 养成卡牌 / 单机 F2P):

| 参照游戏 | 借鉴点 | 备注 |
|---|---|---|
| Clash Royale | 宝箱计时开启 + 箱位队列经济 | 本作 19.5 的原型 |
| Archero | 奖励式广告位设计(拒绝插屏,广告=玩家主动换收益) | 单机手感 F2P 的口碑做法 |
| AFK Arena | 单一订阅「月卡」形态(时间型权益,不卖数值) | 14.3 订阅参照 |

**为什么适合个人开发者**:下载零门槛做大漏斗;奖励式广告不需要运营买量也有底线收入;单一订阅实现简单(一个 SKU),无礼包/多档位的运营负担。

### 14.1.1 决策记录:Premium → F2P

- v0.3(2026-05-26)拍板 Premium 买断,核心理由是「个人主体、零服务端、零运营」。
- v0.5(2026-07-05)项目转向养成 RPG(第 19 章):买断制与「宝箱/集卡/等级」养成层根本冲突;首发平台改海外移动,移动端买断转化率远低于 Steam。
- 代价接受:需要极轻量服务端(校时/存档校验,19.9)、广告 SDK 与订阅接入、软启动调优周期。

## 14.2 广告变现细目

**铁律:全部奖励式(rewarded video),无强制插屏。** 广告永远是玩家主动发起的交换,不打断游玩。

| 广告位 | 触发场景 | 频次上限 | 奖励 |
|---|---|---|---|
| 开箱加速 | 箱位内计时中的宝箱 | 每个宝箱 1 次 | 按档位缩短(素纸/竹简即开,青瓷 40m,紫檀 60m,鎏金 90m,朱漆 105m,赤霄 120m;19.5.2) |
| 商城免费刷新 | 每日商城 | 1 次/日 | 货架整体重刷(19.6) |
| 商城墨锭位 | 每日商城固定货架位 | 1 次/日 | 墨锭 +30(首版基准) |
| 局内扩容·字库 | 战斗内字库满 | 每关 1 次 | 字库上限 +2(本关有效) |
| 局内扩容·部件池 | 战斗内部件池满 | 每关 1 次 | 部件池上限 +2(本关有效) |
| 复活位(Q18,待定) | 关内败北 | 软启动前拍板 | 原地复活续打(肉鸽纯粹性 vs 养成挫败缓冲) |

**节奏原则**:广告位总曝光跟随玩家自然行为(开箱/逛商城/打关),不做「看广告弹窗轰炸」;单日自然游玩预期触达 3~6 次观看机会,上不封顶但不主动推送。

## 14.3 订阅设计(单一月订阅)

- **定价**:首版基准 **$4.99/月**(Q20,软启动 A/B 定价测试后定稿)。
- **权益**:
  1. **永久去广告体验**——所有奖励式广告位免观看直领(权益期内);
  2. **通行证**(订阅期内):箱位 +1(4→5)、每日墨锭礼包、开箱时长 −25%。
- **设计红线**:不卖数值、不卖卡、不卖抽数;订阅玩家与免费玩家**只差时间,不差强度**。任何新权益入列前用这条红线过筛。

### 14.3.1 实现现状(2026-09-22)

**Platform 层骨架已就位**,`Platform/` 不再是空 asmdef:

| 件 | 状态 |
|---|---|
| `IAdService` / `AdPlacement`(6 位)| 接口就绪;`DirectGrantAdService` 为占位实现(不播广告,直接发奖) |
| `AdMobAdService` | AdMob 适配器,在 `BRUSHBLADE_ADMOB` define 之后;默认走官方测试单元 |
| `DeviceWhitelist` / `WhitelistBypassAdService` | 测试机白名单后门:名单内不发广告请求、直接发奖 |
| `IBillingService` / 订阅态 | 接口就绪;`StubBillingService` 永远未订阅(订阅整体后置) |
| `Monetization` | 服务定位器 + `InstallDefault()` 装配链,`GameRoot.Boot` 调一次 |
| `AdGate`(Presentation)| 「看完才发奖」的唯一实现处;**7 个调用点**全部改走这里(6 个位,字库扩容有两个入口)|

六个广告位**全部接线完毕**(`DrawPoolAdSlot` 那个部件池位一直都在,2026-09-22 的稿子
误记为「没有 UI 入口」,是查的时候用错了方法名 —— Core 里的真名是 `TryExpandPool`)。

### 14.3.2 AdMob 接入(2026-09-23)

适配器 `Platform/AdMobAdService.cs` 已写好,**整份在 `BRUSHBLADE_ADMOB` define 之后** ——
SDK 不在工程里,不加 define 时它等于空文件,工程照常编译。启用步骤:

1. 装 Google Mobile Ads Unity SDK(GitHub releases 的 `.unitypackage` 或 UPM `.tgz`);
2. Player Settings → Scripting Define Symbols 加 `BRUSHBLADE_ADMOB`;
3. AdMob 后台建六个奖励式单元,ID 填进 `AdUnitIds.Production`;
4. Android 的 App ID 写进 `AndroidManifest.xml`
   (`com.google.android.gms.ads.APPLICATION_ID`),iOS 写进 `Info.plist`
   (`GADApplicationIdentifier`);
5. 出包前把 `AdUnitIds.UseTestUnits` 置 **false**。

**默认全部走 Google 官方测试单元**(公开常量,可以进仓库,不是密钥)。
⚠️ **拿真单元做开发自测会被判无效流量,严重的会封号** —— 所以默认必须是测试单元,
`test_测试单元_ID_是_Google_官方那几个` 守着这条。

**测试机白名单后门**(`DeviceWhitelist`):名单内的设备不触发广告直接发奖。
iOS 认 IDFV,其他平台认 `SystemInfo.deviceUniqueIdentifier`;名单留空即关闭。
装配链是 `白名单装饰器 → (AdMob 或 占位)`,白名单在**最外层**,
所以名单内的机器连广告请求都不发 —— 不产生任何假曝光。这是底线:
绕开请求没问题,伪造曝光是广告欺诈。
取本机 ID:跑开发构建看启动日志的「本机设备 ID = ...」。
⚠️ IDFV 在同厂商 App 全部卸载后会变,名单项会失效,要重取。

**仍未做**:

1. **订阅四条权益在 Core 里一条都没有**(广告位免看直领 / 箱位 4→5 / 每日墨锭礼包 /
   开箱时长 −25%)。所以商城那条订阅栏**刻意没接购买** —— 计费通了也不能卖,
   卖出去就是收了钱不给货。正确顺序:**先做权益进 Core,再接购买**。
   (2026-09-23 用户拍板:订阅整体后置,先不做。)
2. **`AdMobAdService` 从未编译过** —— 开发环境没装 SDK。用的是 GMA Unity v9/v10 的
   API 形状(`RewardedAd.Load` 静态方法、`new AdRequest()` 无 Builder);
   装的版本不同要对着官方样例改。
3. 接线对账测试在 `tools/monetization/tests/`,守的是「发奖有没有过闸门」;
   **它抓不到异步回调类 bug** —— 占位实现是同步回调,接真 SDK 后变异步,
   「点完立刻读发奖后状态」这类问题只有真机能暴露。

## 14.4 明确不做清单

| 不做 | 理由 |
|---|---|
| 宝箱/货币直购(首发) | 「开箱概率 × 直购」组合触发多地区合规重负;先用广告+订阅验证盘子 |
| 体力系统 | 节奏阀已由「箱位 + 计时开箱」承担(19.5.3);叠体力=双重惩罚,单机 F2P 差评雷区 |
| 强制插屏广告 | 口碑优先;打断式广告是单机 F2P 差评第一来源 |
| 多档订阅/礼包轰炸 | 个人开发者运营带宽有限;单 SKU 保持商店页与心智简洁 |
| 开局付费墙/内容锁 | 全部章节内容免费可达,付费只买时间与便利 |

## 14.5 合规

- **掉率公示(Q21)**:宝箱各档位掉率表需在游戏内可查(Apple 4.7 / Google Play 及部分地区法规);随 ChestRules 权重表定稿产出。
- **广告 SDK 隐私**:iOS ATT 弹窗、GDPR/CCPA 同意管理(聚合 SDK 自带 CMP);儿童定向豁免——本作不申报儿童类目,商店年龄分级按 9+/Everyone 10+ 走。
- **订阅条款**:自动续费披露、取消入口、恢复购买(StoreKit/Play Billing 标准流程)。

## 14.6 现金流预期(保守/中位)

变量:DAU、广告 ARPDAU(奖励式,休闲卡牌海外 $0.02~0.06)、订阅渗透率(0.5%~2%)。

| 场景 | DAU | 广告 ARPDAU | 订阅渗透 | 月毛收入 |
|---|---|---|---|---|
| 底线 | 2,000 | $0.02 | 0.5% | ~$1.5K |
| 中位 | 20,000 | $0.04 | 1% | ~$34K |
| 顺利 | 100,000 | $0.05 | 1.5% | ~$225K |

> 与 Premium 的关键差异:收入是**月流水**而非首发一波;中位场景年化 ~$400K 毛收入,但依赖留存(第 15 章重写时对齐)。财务目标基线:中位场景跑 12 个月。

## 14.7 与其他系统的关系

- **宝箱/商城/货币/扩容位规则参数**:第 19 章 19.5/19.6/19.7 为准,本章只管「哪些位收钱、收多少、红线」。
- **留存(第 15 章)**:F2P 模型下留存=收入,第 15 章待重写(头部已挂声明)。
- **技术(第 16 章)**:轻服务端(校时/存档校验)、广告 SDK、订阅接入(19.9)。
- **MVP(第 17 章)**:软启动范围含广告位全量 + 订阅,直购不入首发。

---

*第 14 章 完*
