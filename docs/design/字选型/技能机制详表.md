# 技能机制详表 · 139 字实现规格

配套 `技能四类判定-评分版.md`(判类 + 评分 + 稀有度),本表补**每个字的可实现机制**:
效果类型、数值、目标、以及**当前引擎能不能直接跑**。

目标读者是实现者 —— 每一行都能直接翻成 `chars.json` 条目或一条引擎扩展需求。

---

## 一 · 实现约定

### 1.1 引擎现状(截至 2026-08-03)

`Core/EffectDef.cs` 的 `EffectKind` **只有 8 种**,这是本表的硬边界:

| EffectKind | 语义 | **吃相生 ×3?** | 附加字段 |
|---|---|---|---|
| `DamageSingle` | 单体伤害 | ✅ 吃 | `DoubleVsBurning`(为「灼」设计) |
| `DamageAll` | 全体伤害 | ✅ 吃 | 同上 |
| `Shield` | 自身护盾 | ✅ 吃 | `PersistOnce`(为「堡」设计) |
| `HealSelf` | 治疗自身 | ✅ 吃 | — |
| `BurnSingle` | 单体灼烧叠层 | ❌ **平值** | — |
| `BurnAll` | 全体灼烧叠层 | ❌ **平值** | — |
| `BurnPotency` | 每层灼烧伤害 +Value | ❌ **平值** | (为「炽」设计) |
| `Summon` | 召唤前排单位 | ❌ **平值**(血量/攻击都不过 Resolver) | `SummonCount` / `SummonAttack` / `SummonChar` |

> ⭐ **「吃相生」这一列是全部数值设计的前提。** 只有伤害/护盾/治疗三类过
> `WuxingResolver.ResolveEffect`;灼烧层数、灼烧系数、召唤物血量与攻击力都是**平值**。
> 故 DOT 流与召唤流**天然不受 ×3 冲击**,数值直接用锚点;伤害/护盾/治疗字必须走 1.5 节换算。

`BattleEngine` 配套规则:召唤物**自动替玩家承伤**(`SummonHit`)并在回合末**反击**(`SummonAttack`);
护盾分 `_shieldNormal` / `_shieldPersist` 两池;灼烧 X 层 → 回合末 X×2 伤害后 −1 层。

### 1.1.1 灼烧属火 / 流血无属性

| 状态 | 属性 | 生克 | 对金系 |
|---|---|---|---|
| **灼烧** | **火** | 走生克结算 | **×1.5**(与 `wuxing-reference` 一致) |
| **流血** | **无属性** | **不走生克** | ×1 |

⚠ 两处引擎改动:(1) `EndTurn()` 的 `tick = Burn * _burnPerStack` 没有任何属性判定,
要让灼烧属火须接入 `WuxingResolver`(**只结算克制,不结算相生** —— 层数是平值,相生已在施加时体现);
(2) `Bleed` 是引擎第一个「无属性伤害」,须**绕过** `DamageEnemy` 的强制生克通道。

### 1.2 稀有度与 AP

`CardRarity` 新增 `Gold = 7`;**AP 一律 1**(拆 0 AP、合 1 AP):

```csharp
public enum CardRarity { White=1, Green=2, Blue=3, Purple=4, Orange=5, Red=6, Gold=7 }
public static int ApCostFor(CardRarity rarity) => 1;
```

> 📌 2026-08-05 更新:上面的枚举名已作废。强度序不变(1~7),但枚举名改为与皮肤色对齐——
> `White=1, Green=2, Blue=3, Purple=4, Gold=5, Orange=6, Red=7`。故本节下文所说的
> 「`Gold`(第 7 档)」现在叫 **`Red`**、「`Orange`(第 5 档)」现在叫 **`Gold`**。
> 唯一来源见 `Core/CharDef.cs` 的 `CardRarity`。

⚠ `第10章 10.1` 的 AP 表作废(同一真相,须同步改)。每回合 3 AP → **一回合能出 3 张金卡**。

**稀有度迁移**:燚/㵘/㙓/𨰻/𣛧 `Purple`→**`Gold`**;焱/淼/鑫/森/垚 `Blue`→`Orange`;
炎/林/沝/圭/鍂 `Green`→`Purple`。AP 统一后此迁移**不影响手感**。

### 1.3 战场与掉落规则(2026-08-03)

| 项 | 旧 | **新** | 落点 |
|---|---|---|---|
| 召唤物上限 | 4 | **6** | `BattleEngine.SummonCap` |
| 敌人同屏上限 | 无硬上限 | **6** | 需新增 `EnemyCap`,关卡生成与分裂怪都要守 |
| 回合掉落 | +2 随机**部件** | **+1 随机字** | `StartTurn()` |
| **战斗战利品** | 掉部件 | **随机 5 个字,任选 2** | `RunEngine` 战利品流程 |
| **广告复活补给** | 字/部件混合 | **随机 5 选 2,做两次 → 共 4 字** | `RunEngine.Revive` 流程 |

**三处掉落共用同一个池**:该字必须 **① 已拥有/已解锁** 且 **② 已在本局出战牌组**。

> 掉落池 = 玩家自配的出战牌组,设计意图是**强化既定 build** 而非塞无用字。
> 实现上 `BattleConfig` / `RunConfig` 需要新增「出战字表」字段并贯穿到这三处
> —— 现有 `RewardPool`(见 `enemies.json` 的 endless.bands)是**按层段写死的固定池**
> (如「字林」段固定 鍂/林/沝/炎/圭),与新规则冲突,需改为读出战牌组。
>
> ⚠ **副作用**:(a) 出战牌组若只有 1~2 个字,5 选 2 会重复到无意义 —— 建议**牌组下限 5 字**,
> 否则 5 选 2 无从选起;(b) 掉落全是成品字,拆合的输入来源消失 —— **拆免费(1.2)是这条的对冲**,
> 玩家把掉落的字拆成部件再重组。

**召唤上限 6**:`𣛧` 仍召 4 只,但可与其它召唤字叠到 6,木系铺场天花板打开。
**敌人上限 6**:`DamageAll` 期望收益随之提高,平衡时按**平均 3~4 个敌人**估算,不要按 6 个。

### 1.4 数值定标:以第 30 层为锚 ⭐

> **本节 2026-08-13 由 E-b4+E-b5 的 T8 重做**(旧稿是 ×10 之前的量级 + 无 DEF 的世界)。
> 单位一律是**现行量级**,可直接与 `chars.json` / `enemies.json` 的数字对读。
> 定标原则一个字没变 —— 「红档 = 打得动第 30 层」;变的是单位,以及**多了一条点数护甲轴**。

#### 先看怪物实际长什么样(现行量级)

无尽缩放(`Endless.cs`):杂兵 `1 + 0.1×(depth−1)`;Boss 层 `1 + 0.1×(depth−5)`;
**护甲半速** `1 + (scale−1)/2`(E-b4 裁定 11)。同屏敌数 `1 + min(5, (depth−1)/4)`,**21 层起满 6 只**。

⚠ **第 20 章 20.4 写的「1 + min(3, (depth−1)/4),渐进到 4 敌上限」已过期** ——
`Endless.BuildFloor` 在 2026-08-03 随「敌人同屏上限 4 → 6」(本表 1.3)改成了 min(5, …),
`EnemyCap` 也是 6。**以代码为准**;20.4 那行待同步。这条差别不小:深度 30 的一轮总伤
按 4 只算是 624,按 6 只算是 936。
基准杂兵「错字鬼」140 血 / 攻 40;带甲杂兵「墨渍」140 血 / 攻 30 / **护甲 20**;
Boss 最强阶段「钧」攻 110,坚壁阶段「山」**护甲 60**。

⚠ 缩放取整走 `Math.Round`(**不是 `Ceiling`**,E-b4 T1):敌人基础值全是 10 的倍数、
scale 全是 0.1 的整倍数,故 `基础值 × scale` 数学上恒为整数,`Round` 只负责夹掉 float 噪声。
**新配敌人时基础值请继续保持 10 的倍数**,否则这条不变量破掉,`Round` 会开始承担真实取整语义。

| 层 | 敌数 | scale | 杂兵 HP | 杂兵单击 | 一轮总伤 | 墨渍护甲 | Boss scale | Boss 单击 | 山护甲 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 1 | 1.0 | 140 | 40 | 40 | 20 | 0.6 | 66 | 48 |
| 5 | 2 | 1.4 | 196 | 56 | 112 | 24 | 1.0 | 110 | 60 |
| 10 | 3 | 1.9 | 266 | 76 | 228 | 29 | 1.5 | 165 | 75 |
| 15 | 4 | 2.4 | 336 | 96 | 384 | 34 | 2.0 | 220 | 90 |
| 20 | 5 | 2.9 | 406 | 116 | 580 | 39 | 2.5 | 275 | 105 |
| **30** | **6** | **3.9** | **546** | **156** | **936** | **49** | **3.5** | **385** | **135** |
| 40 | 6 | 4.9 | 686 | 196 | 1176 | 59 | 4.5 | 495 | 165 |
| 50 | 6 | 5.9 | 826 | 236 | 1416 | 69 | 5.5 | 605 | 195 |

#### 锚点(最终值,对中立目标)

**定标原则:红档 = 打得动第 30 层。** 单攻能一击秒杂兵、护盾能挡 Boss 一击、
召唤物能挨住一击不死、治疗能回满一次 Boss 击。

| 稀有度 | 部件 | 单攻 | 全体 | 护盾 | 治疗 | 召唤(只×血/攻) | 灼烧层 | 流血 |
|---|---|---|---|---|---|---|---|---|
| 🔴 红 | 40(叠) | **600** | **300** | **450** | **350** | 4 × 300/120 | **5** | 60/回合 |
| 🟠 橙 | 40 | 480 | 240 | 360 | 280 | 3 × 260/100 | **4** | 50/回合 |
| 🟡 金 | 30 | 400 | 200 | 300 | 240 | 2 × 220/90 | 3 | 40/回合 |
| 🟣 紫 | 20 | 200 | 100 | 150 | 120 | 2 × 110/50 | 2 | 20/回合 |
| 🔵 蓝 | 10 | 130 | 70 | 100 | 80 | 1 × 80/40 | 2 | 20/回合 |
| 🟢 绿 | 10 | 90 | 50 | 70 | 60 | 1 × 60/30 | 1 | 10/回合 |
| ⚪ 白 | 10 | 60 | 30 | 40 | 40 | 1 × 40/20 | 1 | 10/回合 |

> **2026-08-14 灼烧层阶梯订正**:橙 3 → 4、红 4 → 5。原表白/绿(1)、蓝/紫(2)、金/橙(3)
> 三对档位的层数两两相同 —— 层数是小整数,7 档挤不下,于是高一档的字在 DOT 轴上拿不到
> 任何区分度。燊(橙)与 焱/焚(金)同为 3 层就是这么来的:「橙 > 金」全靠伤害那 20% 撑着,
> 而伤害差还被相生 ×3 的填值方式藏在卡面之外。本次先修用户点名的金/橙/红三档;
> ⚠ **白/绿(均 1 层)与紫(2 层)的重叠仍在** —— 蓝档现已无灼烧字,绿档提到 2 不会撞车,
> 但那要连着重排整条阶梯与 5 个紫档字,留待专项。

> 2026-08-13 T8 对全表的实测核对:86 条伤害 / 护盾 / 治疗效果里,**超出本表 15% 的只有 5 条**
> —— 錰 / 刺 / 锥 是 E-b4 T3 把旧「穿甲无条件 +15%」固化进基础值的结果(精确等价,见 §6.4),
> 塔(护盾 1.20)/ 剿(全体 1.20)/ 棒(单攻 1.17)是历史小幅上沿。**其余全部落在锚点上或以下**,
> 低于锚点的一律是复合字的副效果(本节末「复合字预算」允许)。**字表本身无需重定值。**

#### 定标产出了一条清晰的设计语言:稀有度 ↔ 有效深度

⚠ **点数护甲把这张表变成了两列** —— 同一张字打无甲怪和打带甲怪的有效深度不一样,
而这正是「AOE 清杂兵、单体破装甲」那条战术分工的具体形状。

| 档 | 单攻 | 秒杀无甲杂兵可至 | 秒杀**带甲**杂兵(墨渍)可至 | 挡 30 层 Boss 一击(385) | 定位 |
|---|---|---|---|---|---|
| ⚪白 | 60 | 第 1 层 | 第 1 层 | ✗ | 开局与合成材料 |
| 🟢绿 | 90 | 第 1 层 | 第 1 层 | ✗ | 同上 |
| 🔵蓝 | 130 | 第 1 层 | 第 1 层 | ✗ | 同上 |
| 🟣紫 | 200 | 第 5 层 | 第 3 层 | ✗(能挡杂兵一击) | 中期主力 |
| 🟡金 | 400 | 第 19 层 | 第 17 层 | ✗(差 85 点) | 深层主力 |
| 🟠橙 | 480 | 第 25 层 | 第 22 层 | ✅ | 深层核心 |
| 🔴红 | 600 | **第 33 层** | **第 30 层** | ✅ 有余量 | 压箱底 |

**每档对应一个深度区间的「一击秒杀线」** —— 稀有度越高,有效深度越深。
低稀有度字在深层自然退场、转为合成材料,这正是拆合玩法想要的循环。

#### ⚠ 点数护甲对低档字是**开关**,不是削减

这条是点数制自带的性质(与 §4.2 论证「DOT 不吃 DEF」用的是同一条),配数值时必须知道:

| 档 | 单攻 | 深度 20 的墨渍护甲 39 吃掉 | 深度 50 的护甲 69 吃掉 |
|---|---|---|---|
| ⚪白 | 60 | 65% | **115%(归零)** |
| 🟢绿 | 90 | 43% | 77% |
| 🔵蓝 | 130 | 30% | 53% |
| 🟣紫 | 200 | 20% | 34% |
| 🟡金 | 400 | 10% | 17% |
| 🔴红 | 600 | 6% | 12% |

同一件护甲对低档字是**十倍于**高档字的惩罚。守卫测试
`LowestTierChar_StillDentsArmoredMobAtDepth20`(spec §6.3.2 的 T3-V4)把这条钉在深度 20 上,
它靠的是**半速缩放**;同速缩放会让最低档在深度 20 就归零。

⚠ **T3-V4 的作用域只到它标定的那个玩家画像**(12 级 / 卡 4 级 → 最低档打出 47)。
再往深处走,最低档终究会被护甲追上 —— 该画像在**深度 28** 归零,而 26 级 / 卡 10 级的画像
要到**深度 66**。这是「低档字自然退场」的设计意图,不是缺陷;但**它意味着 T3-V4 不是一条
全深度成立的保证**,日后调墨渍护甲或字表最低档时要重新标定,不能只看那一条测试绿不绿。

#### ⚠ 一个结构性风险,光调字表解决不了

**玩家 HP 硬封顶 1000**(`MetaRules.MaxHpFor => Math.Min(1000, 500 + 20×(level−1))`,26 级到顶),
而怪物 scale **无上限线性增长**。E-b4 给玩家补了两条防御轴(护甲 / 闪避),它们同样封顶:

| 等级 | HP | ATK | 护甲 | 闪避 |
|---|---|---|---|---|
| 1 | 500 | 100 | 0 | 0% |
| 11 | 700 | 120 | 5 | 10% |
| 20 | 880 | 138 | 9 | 19% |
| **26(封顶)** | **1000** | **150** | **12** | **25%** |

| 层 | 一轮总伤(6 只) | 满级裸抗后(`(单击−12) × 只数 × 0.75`) | 满血 1000 能撑 |
|---|---|---|---|
| 30 | 936 | 648 | 1.5 轮 |
| 40 | 1176 | 828 | 1.2 轮 |
| 50 | 1416 | 1008 | **1.0 轮** |
| 60 | 1656 | 1188 | **0.8 轮** |

**性质与旧稿完全一样,连交点都几乎没挪**:玩家侧全部封顶、怪物侧线性无上限,
**50 层往后满级玩家满血也活不过一轮**。生存在深层仍然要靠护盾 / 召唤物 / 治疗顶伤。
⚠ 旧稿是按 4 只算的(那时同屏上限就是 4),改成 6 只之后 E-b4 补的两条防御轴
(护甲 12 + 闪避 25%)刚好被多出来的两只吃干净 —— **E-b4 没有推后这个交点。**
⚠ 护甲这条轴在深层**衰减得最快** —— 它是减法,占单击的比例随深度趋近 0
(封顶 12 点对深度 30 的 156 一击是 7.7%,对深度 60 的 276 一击只剩 4.3%),
而闪避 25% 是乘性的、不随深度衰减 —— **深层的防御轴其实只剩闪避与护盾**。

**三个可选方向(需拍板,本表未擅自采用):**
- **A. 解除封顶** —— 让 `MaxHpFor` / `DefenseFor` / `DodgeFor` 随 meta 等级继续增长。改动最小。
- **B. 护盾/召唤按深度缩放** —— 玩家侧数值也吃 `ScaleFor(depth)`,与怪物并跑。
- **C. 接受「深层靠盾不靠血」** —— 明确把护盾/召唤定为深层唯一生存手段,
  那么土系与木系在 30 层后就是**强制配置**,而非流派选择。这会压缩 build 多样性。

#### 相生 ×3 的换算规则

> ⚠ **2026-09-02:本节规则已取消**(全局拍板,见 `wuxing-reference.md`)。全表 74 字里
> 只有下面这 5 字吃得到,是条空转规则。焚/蒸/刲 已按等值改写把「基础值」直接填成
> ×3 后的实战值(战斗结果不变);沏 待 Task 10 按水系锚点重配。以下公式与推导保留作
> 历史记录,**不再是当前的填表规则** —— 现在「效果配置(基础值)」列即最终值,不再心算。

`wuxing-reference`(2026-08-04 收紧为**「他生我」**):配方原料里含**生本字属性**的那个 → 该字效果 **×3**
(固有,永久生效)。本字去生原料(**我生他**)**不算** —— 如 灶(火系,火+土)的火生土、淋(水系,水+木)的水生木。

⚠ 这条收紧的后果:同系部件合成的字(城=土+成、沝=水+水)原料属性 = 自身属性,**永远吃不到相生**。
只有跨系配方才可能,且必须是母属性。全表最终只有 **5 字**吃到 ×3:燊 焚 蒸(木生火)、刲(土生金)、沏(金生水)。
**护盾与治疗两类目前 0 字吃到**(护盾字全土系缺火部件、治疗字全水系缺金部件)—— 内容缺口,待补字时填。

> 2026-08-09 补:`炑`(火 + 木)的配方也命中木生火,但它的效果只有 `BurnSingle` 与 `BurnNoDecay`,
> 两者都不走 `WuxingResolver`(灼烧层数是平值,施加时不过生克),所以 ×3 对它**无任何效果**,
> 数值表里不体现。⚠ 若日后给 炑 加伤害成分,×3 会立刻生效,数值要重算。

**锚点表定义的是最终值,配置表填基础值**:

```
无相生字:  基础值 = 锚点值
有相生字:  基础值 = ⌈锚点值 ÷ 3⌉        ← 配置表填这个
```

本表每个字都给 `效果 基础值 → 最终值`,实现者填前者、验收看后者。
**有相生的字在本档取上沿**(约 +10%,体现评分表的「相生档内加分」),但不得触及下一档。

**灼烧层 / 流血 / 召唤三列是平值**,直接填,不做 ÷3。

**复合字预算**:一字带两效果时总量守恒,**副效果可低于本档锚点**(参照实装
`圭 = 盾+单攻`、`沝 = 治疗+单攻`)。**唯一硬约束:任何字的最终值不得触及上一档锚点。**

**多段字补偿**(2026-08-12 E-b4/E-b5 spec §4.4(b),2026-08-13 T8 落地):点数护甲对
「把总量摊成多份」有天然惩罚 —— `HitCount N` 的字每段各扣一次 DEF,面对 DEF 30 的敌人,
`100×2` 只打出 140 而同档单段 200 打出 170。补偿规则:

> **多段字的总基础值 = 同档单段字 × (1 + 0.1 × (段数 − 1))**

全表目前只有 `剁` 一个多段字:紫档单段锚点 200 → 剁 = **110 × 2 = 220 总**。
不追求完全拉平(补偿后对 DEF 30 打 160,单段 220 打 190,仍差 30)——
多段在「两次过斩杀阈值」「两次触发受击后效」上有独立收益。

> ⚠ **2026-08-13 订正**:E-b4/E-b5 spec §4.4(b) 的**正文**曾写 `120 × 2 = 240`,与它自己
> 上面那行公式(`200 × 1.1 = 220`)矛盾。**以公式为准。**
> 理由:补偿存在是因为 N 段字比单段多吃 `(N−1)` 次 DEF,系数**必须**正比于 `段数 − 1` ——
> 单段字代入才得 ×1.0(不需要补偿)。240 相当于 `1 + 0.1 × 段数`,那会让单段字也白拿 +10%。

**相克 ×1.5 不影响阶梯**(各档同乘)。但两个交互要知道:`DamageEnemy` 里**被克时敌人承伤减免失效**;
相生 ×3 与相克 ×1.5 **相乘**,最高 ×4.5 —— 故金卡 刲 打金系敌人 ≈ 67,会超过红卡打中立的 60,
**这是设计意图**(打对属性该有回报),跨条件比较不算破坏阶梯。

### 1.4.1 副效果价目表与攻击性下限(2026-08-14 T9 拍板)⭐

T8 校准完时,全表的**主数值**已经贴着 §1.4 的锚点,但**副效果是白送的** —— 同一个
「冻结 1 回合」,埋/坑 只花了 0.33 档预算(90 → 60),冻/溺/藤 却花掉整档(130 → 0)。
后果是低档字严格压制高档字:绿档的 埋(60 伤 + 冻结)在任何局面都优于蓝档的 冻(0 伤 + 冻结)。
全表当时有 **10 对**这样的严格劣化。

**两条新规矩:**

1. **副效果按价目表计价**,价格 = 它占该档主预算的比例。主数值 = 锚点 ×(1 − 副效果价格合计)。
2. **攻击性下限 35%** —— 任何字的伤害不得低于本档锚点的 35%。召唤字豁免(输出在召唤物身上),
   DOT 字视灼烧/流血为攻击性。**纯功能牌(0 伤)自此不存在。**

| 副效果 | 价格 | 副效果 | 价格 |
|---|---|---|---|
| 冻结 1 回合 | 0.35 | 驱散 1 条 | 0.15 |
| 减速 2 回合 | 0.30 | 驱散全部 | 0.35 |
| 沉默 1 回合 | 0.35 | 净化 | 0.25 |
| 致盲(任意档) | 0.30 | 免疫 1 次 / 2 次 | 0.35 / 0.60 |
| 反弹 50%×2 回合 | 0.35 | 复活 1 名 | 0.40 |
| 破甲 | 0.33 | 斩杀(加倍 / 处决) | 0.15 / 0.20 |
| 战意 1 层 / 3 层 | 0.15 / 0.45 | 灼烧威力 +10 / +20 | 0.30 / 0.45 |
| 暴击 +20% | 0.40 | 穿透 +20 | 0.35 |
| 攻击 +50 | 0.45 | **AP 上限 +1** | **0.50** |

> **破甲 0.33 是这张表的锚** —— 碎/破/溶/熔/锤/溃 六字在 T9 之前就已经是一致的 0.31~0.33,
> 是全表唯一自洽的一组,其余价格都按它的量级定。
>
> **AP 最贵(0.50)**:在 3 AP 的体系里 +1 是 +33% 行动力,构筑级效果,白档字尤其要压。

**数值型功能(护盾 / 治疗 / 护甲 / HoT)**不按价目表,直接乘 **0.65** —— 即让出 35% 给攻击。
护甲点数因此整体 ×0.65:巍 3→2、磐 6→4、崟 9→6、崊 12→8、漜 15→10。
⚠ 这让 §6.2 的「旧减伤% × 0.6」折算表**只对 T9 之前有效**,现行点数以本节为准。

**一条被判据挡回来的取值**:白档 35% = 21 点伤害,而深度 20 的墨渍护甲是 39 —— 21 会被
整口吃掉,`LowestTierChar_StillDentsArmoredMobAtDepth20`(E-b4 的 T3-V4)当场变红。
故**白档功能字的攻击取地板 30**,不取 35%。这正是 §1.4「点数护甲对低档字是开关」那条的
第一次实战撞击。


### 1.4.2 DOT 当量与战意新定价(2026-08-15 批量改造)⭐

**火系:攻击的同时挂 DOT。金系:攻击的同时叠战意。** 两条批量策略各自需要一个当量。

#### DOT 当量 —— 从引擎公式直接读出来,不是拍的

灼烧每层每回合结算 `层数 × _burnPerStack`,回合末层数 −1,故 N 层的**总伤** =
`N(N+1)/2 × 20`(引擎里 `Detonate` 的引爆公式就是这条,见 `BattleEngine.DetonateBurn`):

| 层数 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 总伤当量 | 20 | 60 | 120 | 200 | 300 |
| 约等于哪档单攻锚点 | — | 白 60 | 蓝 130 | 紫 200 | — |

**不折价也不溢价**:DOT 的延迟劣势(要 N 回合才打完、目标提前死就浪费)与它
**不吃 DEF**(§4.2 的硬约束)在深层的优势对冲。复合字按 `直伤 + DOT当量 = 档位锚点` 守恒。

⚠ **当量随层数二次增长,而档位锚点线性增长** —— 所以高层数会挤占直伤:
橙档 4 层已吃掉 200/240,红档 5 层(300)本身就等于红档全体锚点。这不是配错,
是层数轴与伤害轴的量纲差异;四叠字(燊/燚)按压箱底容许上沿。

⚠ **`DoubleVsBurning` 的字(灼/烬)DOT 必须写在伤害之后** —— 数组顺序即结算顺序,
写反了它们会自己点火再吃自己的翻倍,从「消费灼烧的字」变成自给自足。

#### 战意改为每回合末衰减一层

原为本场持久,2026-08-15 改。计价随之从 0.15 降到 **0.10** —— 一层战意 = 当回合 +10 攻,
按一回合出 3 张牌算,收益约是同档伤害的十分之一。金系除四张 BUFF 字(战/利/锋/锐)、
铠(护甲)、铸(反弹)、剡(Empower)外**全部挂 1 层**,主数值一律 ×0.90。

实现在 `BattleEngine.TickPlayerStatuses` 末尾单独一段:战意是**计数器式**状态
(`TurnsLeft = -1`,层数记在 `Magnitude`),`TickTurns` 只认 `TurnsLeft`,碰不到它 ——
同理 ApBoost / CritBuff / PierceBuff / Empower 仍是本场持久。守卫见
`BuffCharTests.Morale_DecaysOneStackPerTurnEnd` 与 `Morale_OtherBuffsStayPermanent`。

**首回合宽限(2026-08-18 补)**:身上**没有**战意时新挂的那条,当回合末不递减,
从第二回合起才开始掉;身上已有战意时再叠,则照常当回合递减。没有这条的话,批量挂的
那 1 层等于白给 —— 当回合生效、同一个回合末就归零,而金系「攻击的同时叠战意」这条
批量策略正是靠层数攒起来才成立。标记 `_moraleGraceTurn` 由 `AddPlayerCounter` 的
「新建」那一支置起,随 `BattleSnapshot.MoraleGraceTurn` 进存档。守卫见
`Morale_FromZero_SkipsFirstDecay` / `Morale_OnTopOfExisting_DecaysSameTurn` /
`Morale_Regained_AfterFullDecay_GetsGraceAgain` / `MoraleGrace_SurvivesSnapshotRoundTrip`。
计价按 0.10 不动:宽限抬高的是**下限**(1 层字从「白给」变成「真给一回合」),
上限(战 3 层、满层 5)一个字没动。

### 1.5 五系叠字链配方(标准口径,2026-08-03 拍板)

配方一律**部件在前、低阶字在后** —— 读作「往低阶字上再加一个部件」,与「叠字」的语义一致。

| 系 | 二叠(🟣紫) | 三叠(🟡金) | 四叠(🔴红) |
|---|---|---|---|
| 木 | 林 = **木 + 木** | 森 = **木 + 林** | 𣛧 = **木 + 森** |
| 水 | 沝 = **水 + 水** | 淼 = **水 + 沝** | 㵘 = **水 + 淼** |
| 火 | 炎 = **火 + 火** | 焱 = **火 + 炎** | 燚 = **火 + 焱** |
| 金 | 鍂 = **金 + 金** | 鑫 = **金 + 鍂** | 𨰻 = **金 + 鑫** |
| 土 | 圭 = **土 + 土** | 垚 = **土 + 圭** | 㙓 = **土 + 垚** |

⚠ **实装 `chars.json` 有 10 个字顺序是反的**,需在落地时一并修正:

| 字 | 实装现状 | 应改为 |
|---|---|---|
| 森 / 淼 / 焱 / 鑫 / 垚 | 林+木 / 沝+水 / 炎+火 / 鍂+金 / 圭+土 | 木+林 / 水+沝 / 火+炎 / 金+鍂 / 土+圭 |
| 𣛧 / 㵘 / 燚 / 𨰻 / 㙓 | 森+木 / 淼+水 / 焱+火 / 鑫+金 / 垚+土 | 木+森 / 水+淼 / 火+焱 / 金+鑫 / 土+垚 |

五个二叠字(林/沝/炎/圭/鍂)本就是「部件+部件」,无需改动。

> **这 15 条配方是人工口径,不走 IDS 拆解。** IDS 会把 燚 拆成 `炏+炏`、焱 拆成 `火+炏`
> (炏 = 二火异体字),与叠字链冲突。落地脚本必须**先写死这 15 条,再用 IDS 补其余字**,
> 否则叠字链会被 炏 之类的中间字污染,配方 DAG 也可能成环。

---

## 二 · 火系 · 31 字

> **2026-09-02:相生 ×3 已取消**(全局拍板,见 `wuxing-reference.md`)。此前受影响的 3 字里
> 燊 已移出字表,焚/蒸 的「效果配置」列已改填 ×3 后的实战值(等值改写,战斗结果不变)。
> 灿(火系,火+山)、熣(火系)的配方无木部件,本就没吃到 ×3,数值不受影响。

### 攻 · AOE

| 字 | 稀 | 效果配置(基础值) | 相生 | **最终值** | 实现 |
|---|---|---|---|---|---|
| 燚 | 🔴红 | `DamageAll 180` + `BurnAll 5` | — | 全体 180 + 全体 5 层 | ✅ 数值需改(原 22/3);**2026-08-14**:灼烧 4 → 5,随橙档上调保持红 > 橙;**2026-08-15**:300 → 100。5 层当量 300 本身就是红档全体锚点,四叠字按压箱底允许上沿至 400;**2026-09-02**:修档位倒挂 —— 100 曾低于橙档 焱/焚 的 120,红档反而更弱。改回 180 = 红档 AOE 锚点 250 ×0.7(带灼烧附加),与 㵘/㙓 的红档全体 250 自洽 |
| 燊 | 🟠橙 | `DamageAll 15` + `BurnAll 4` | ×3 | **全体 45** + 全体 4 层 | ⚠ **2026-08-25 字表重构移出**:橙档换血为纯三部件字。移除前:;**2026-08-14**:灼烧 3 → 4 —— 原先与金档的 焱/焚 同为 3 层,橙档在 DOT 轴上毫无区分度,「橙 > 金」只靠伤害那 20% 撑着;**2026-08-15**:基础 80 → 15(×3 = 45);4 层当量 200 已吃掉橙档 240 的大半,DOT 之王的定位 |
| 焚 | 🟠橙 | `DamageAll 120` + `BurnAll 4` | — | 全体 120 + 全体 4 层 | ✅ 金上沿;**2026-08-15**:基础 70 → 30(×3 = 90)。原先 210 已满金档锚点,3 层全体是白送的;**2026-08-25 字表重构**;**2026-09-02**:相生 ×3 取消,基础值改填等值改写后的实战值(40 → 120);**2026-09-02**:修档位倒挂 —— 取消相生后与 焱 数值完全相同,而 焚 需要 林+火 跨系配方、严格更难合,灼烧 3 → 4 做区分 |
| 焱 | 🟠橙 | `DamageAll 120` + `BurnAll 3` | — | 全体 120 + 全体 3 层 | ✅ 数值需改(原 12/2);**2026-08-15**:200 → 80;80 + 120 = 200 = 金档全体锚点;**2026-08-25 字表重构** |
| 炎 | 🟡金 | `DamageSingle 340` + `BurnSingle 2` | — | 单体 340 + 2 层 | ✅ 数值需改(原 12/2);**2026-08-15**:200 + 2 层 = 260 是全表最超载的复合字之一,压到 140 + 60 = 200;**2026-08-25 字表重构** |
| 爆 | 🔵蓝 | `DamageAll 50` + `BurnAll 1` | — | 全体 50 + 全体 1 层 | ✅ 全体低倍表达「溅射」;**2026-08-15**:70 → 50 + 全体 1 层 |
| 燎 | 🔵蓝 | `BurnAll 2` | — | 全体 2 层 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。蓝档唯一的纯全体灼烧;移除后蓝档无灼烧字;移除前状态:全体上灼烧即「燎原」 |
| 炸 | 🔵蓝 | `DamageAll 50` + `DetonateAll` | — | 全体 50 + 引爆全部剩余灼烧 | ✅;**2026-08-15**:130 → 110 + 1 层;**2026-08-25 字表重构**;**2026-08-25**:改 AOE —— 与 爆(全体 50 + 全体灼烧 1)成对:爆铺、炸收 |

### 攻 · 单攻

| 字 | 稀 | 效果配置(基础值) | 相生 | **最终值** | 实现 |
|---|---|---|---|---|---|
| 烈 | 🟣紫 | `DamageAll 70` + `BurnAll 2` | — | 全体 70 + 全体 2 层 | ✅ 对金 ×1.5 自动结算;**2026-08-15**:单体 200 → 全体 40 + 全体 2 层(= 紫档全体锚点 100)。改走 AOE 与 炎 分开——「烈火燎原」本就是铺开的语义;**2026-08-25**:全体 40 → 70 |
| 熣 | 🟣紫 | `DamageSingle 80` + `BurnSingle 2` + `Blind 50`(turns 2) | — | 单体 80 + 2 层 + 致盲 2 回合 | ✅ 数值由 21 降为 16:带了致盲还拿 21 会压死同档的 灿(18);**2026-08-14 T9**:单体 160→140:致盲按价目表计 0.30;**2026-08-15**:140 → 80 + 2 层;致盲仍计 0.30,合计 80+60+60 = 200 |
| 灿 | 🟡金 | `DamageSingle 380` + `BurnSingle 1` | — | 单体 380 + 1 层 | ✅ 熣(16 + 致盲)是 灿(18)的机制升阶;**2026-08-15**:180 → 180 + 1 层 = 200 补齐紫档锚点;与 炎(140+2 层)同预算不同配比;**2026-08-25 字表重构** |
| 熔 | 🔵蓝 | `DamageSingle 90` + `ArmorBreak 20` | — | 单体 90 + 破甲 20(本场) | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。与 锤(金蓝)数值效果全同(90 伤 + 破甲 20),同质对,两张一并移除 |
| 烬 | 🔵蓝 | `DamageSingle 110` + `DoubleVsBurning` + `BurnSingle 1` | — | 单体 110(带灼烧 220) + 1 层 | ⚠ **2026-08-25 字表重构移出**:词组归零。移除前:用现成字段表达「烧残补刀」;**2026-08-15**:130 → 110 + 1 层;DOT 同 灼 排在伤害之后 |
| 灭 | ⚪白 | `DispelAll` + `DamageAll 30` | — | 清空全部增益 + 全体 30 | ✅ 唯一解:标点加攻与焦痕自燃都是段内持久且累计;**2026-08-14 T9**:补攻;**2026-08-15**:火系批量挂 DOT 的**语义例外** —— 「灭」是熄灭,给它点火与字义相反,保持纯驱散;**2026-08-25 字表重构** |
| 焰 | 🟢绿 | `DamageSingle 70` + `BurnSingle 1` | — | 单体 70 + 1 层 | ⚠ **2026-08-25 字表重构移出**:与 热 同为单体+灼烧 1,去重。移除前:;**2026-08-15**:90 → 70 + 1 层 |

### 攻 · DOT(层数平值)

| 字 | 稀 | 效果配置(基础值) | 相生 | **最终值** | 实现 |
|---|---|---|---|---|---|
| 燃 | 🟣紫 | `BurnSingle 4` | — | 4 层 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。紫档纯灼烧 4 层,是锚点(2 层)的两倍,长期超标;移除前状态:点燃源头 |
| 燥 | 🟣紫 | `BurnSingle 3` + `BurnPotency 10` + `BurnSettleNow` | — | 3 层 + 系数 +10 + 立即兑现一次 | ✅ 加速:系数 +10 是本场全局,是 炼(绿)的紫档升阶;**2026-08-15**:2→3 层。DOT 操作字(纯 DOT 例外),补足档位价值 |
| 蒸 | 🟣紫 | `BurnSingle 2` + `DamageSingle 135` | — | 2 层 + **单体 135** | ✅ 主 DOT 副伤害;**2026-08-15**:基础 30 → 45(×3 = 135),135 + 60 = 195 ≈ 紫档 200;**2026-09-02**:相生 ×3 取消,基础值改填等值改写后的实战值(45 → 135) |
| 炑 | 🟣紫 | `BurnSingle 3` + `BurnNoDecay` + `DamageSingle 10` | — | 3 层(本场不衰减) + **单体 30** | ⚠ **2026-08-25 字表重构移出**:不灭机制移交 焦。移除前:延长:配方 火+木 虽吃 ×3,但灼烧不走生克故无影响(见 §169 注);**2026-08-15**:2→3 层并补攻(基础 10 ×3 = 30);120 + 不灭 50 + 30 = 200 |
| 灱 | 🟣紫 | `BurnSingle 3` + `Detonate` | — | 3 层 → 引爆 120 | ⚠ **2026-08-25 字表重构移出**:引爆机制移交 炸。移除前:兑现:引爆 = N(N+1)/2 × 系数,层数清零;与 燃 的区别是「收状态」而非「铺状态」;**2026-08-15**:4→3 层 —— 4 层当量 200 已占满紫档,再挂引爆就白送;3 层 120 + 引爆 60 = 180 |
| 烫 | 🟣紫 | `DamageSingle 120` + `BurnSingle 2` | — | 单体 120 + 2 层 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。与 炎 同为紫档「单攻 + 灼烧 2」 |
| 烧 | 🟢绿 | `DamageSingle 30` + `BurnSingle 2` | — | 单体 30 + 2 层 | ✅;**2026-08-15**:1 层 → 攻 30 + 2 层 = 30 + 60 = 90 = 绿档锚点 |
| 炼 | 🟢绿 | `BurnSingle 1` + `BurnPotency 10` | — | 1 层 + 系数 +10 | ⚠ **配方缺口**:IDS ⿰火𫠣 的 𫠣(U+2B823)在增补平面,UGUI Text 显示不出代理对 → 整字退化为叶子,而叶子不进奖励池(`RunEngine.RollRewardOptions` 跳过 `IsLeaf`),玩家永远拿不到。2026-08-10 移出字表,补 `MANUAL_RECIPES` 换部件即可复活,部件待拍板 |
| 焦 | ⚪白 | `BurnSingle 1` + `BurnNoDecay` | — | 1 层(本场不衰减) | ✅;**2026-08-15**:1→2 层 —— 纯 DOT 字要自己撑满档位,1 层当量仅 20,白档锚点是 60。语义纯粹故保留纯 DOT;**2026-08-25 字表重构** |
| 灼 | ⚪白 | `DamageSingle 40` + `DoubleVsBurning` + `BurnSingle 1` | — | 单体 40(带灼烧 80) + 1 层 | ✅ **引擎为此字设计的字段**;**2026-08-15**:60 → 40 + 1 层。**DOT 写在伤害之后**——顺序反了会自我翻倍,灼 是消费灼烧的字不是自给自足的 |
| 炽 | ⚪白 | `BurnPotency 10` + `DamageSingle 30` | — | 系数 +10 + 单体 30 | ⚠ **2026-08-25 字表重构移出**:白档火系瘦身。移除前:**引擎为此字设计的 Kind**;**2026-08-14 T9**:补攻;**2026-08-15**:攻 40 → 30(白档地板,不再低;20 会被深度 20 的墨渍护甲 39 整口吃掉 —— T3-V4 判据)。系数 +10 的收益依赖场上层数,难精确计价,本字按工具字容许上沿 |
| 热 | 🟢绿 | `DamageSingle 70` + `BurnSingle 1` | — | 单体 70 + 1 层 | ✅;**2026-08-15**:改「攻击 + DOT」,40 + 20 = 60 = 白档锚点;顺带与 焦 去同质;**2026-08-25 字表重构** |

### 攻 · 控制 / 召 · 器物 / 不沾

| 字 | 稀 | 效果配置(基础值) | **最终值** | 实现 |
|---|---|---|---|---|
| 烟 | 🟢绿 | `Blind 30`(turns 1, targetAll) + `DamageAll 35` | 全体致盲 1 回合 + 全体 35 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。绿档全体致盲;T9 刚补攻 35;移除前状态:30% 而非 50%:全体 50%/1 回合在 2 怪场就追平蓝档的 藤,绿卡不该盖蓝卡 |
| 烓 | 🟡金 | `Summon 1`(220 血/攻 30)+ `OnHitBurn 3` + `OnHitBurnAll` + `Ranged` | 1 只 220 血 + 每回合全体 3 层 | ⚠ **2026-08-25 字表重构移出**:火系召唤退场,召唤定位收归木/土/金三系。移除前:光环载体;**2026-08-20**:转远程(无视敌方前排,优先打后排)并补基础攻 30 —— 同档紫 40~70、金 90,给到明显低于同档是因为它还带 3 层灼烧 |
| 灶 | 🟣紫 | `Summon 1`(110 血/攻 20)+ `OnHitBurn 2` + `Ranged` | 1 只 110 血 + 每回合单体 2 层 | ⚠ **2026-08-25 字表重构移出**:同 烓。移除前:光环载体;**2026-08-20**:转远程(无视敌方前排,优先打后排)并补基础攻 20 —— 同档紫 40~70、金 90,给到明显低于同档是因为它还带 2 层灼烧 |
| 煤 | 🟣紫 | `BurnPotency 20` + `DamageSingle 80` | 系数 +20 + 单体 80 | ⚠ **2026-08-25 字表重构移出**:紫档火系瘦身。移除前:燃料 = 烧得更旺;**2026-08-14 T9**:补攻;**2026-08-15**:110 → 80;系数 +20 计 120,合计 200 |

---

## 三 · 木系 · 20 字

> **木系完全不受 ×3 影响** —— `Summon` 血量与攻击都是平值。召唤上限已放宽到 **6**。
> 单体召唤取「同档双召的总量集中到一只」,故单只血量高于双召的每只。

### 召 · 多体

| 字 | 稀 | 效果配置 | **最终值** | 实现 |
|---|---|---|---|---|
| 𣛧 | 🔴红 | `Summon 4`(300 血/攻 120) | 4 只,共 1200 血 | ✅ 数值需改(原 10/4)。可与其它召唤叠到 6 |
| 森 | 🟠橙 | `Summon 3`(260 血/攻 100) | 3 只 · 平衡型 | ✅ 数值需改(原 12/4);**2026-08-25 字表重构** |
| 林 | 🟡金 | `Summon 2`(220 血/攻 90) | 2 只 · 平衡型 | ✅ 数值需改(原 6/2);**2026-08-25 字表重构** |

### 召 · 单体 · 带被动

| 字 | 稀 | 效果配置 | **最终值** | 实现 |
|---|---|---|---|---|
| 楸 | 🟣紫 | `Summon 2`(88 血/攻 40)+ `OnHitBurn 1` + `Ranged` | 2 只 · 灼烧型,远程,出手附 1 层 | ✅;**2026-08-25 字表重构**;**2026-08-25**:接手 荆 让出的 `Ranged` —— 楸木高大、火星飘远,与旧载体 灶/烓 的「远程挂灼烧」同一个定位 |
| 蕉 | 🟣紫 | `Summon 2`(110 血/攻 50)+ `OnHitSlow 50` + `OnHitSlowTurns 2` | 2 只 · 控制型,出手减速 50 / 2 回合(幅度与回合数均吃卡等级) | ✅ 引擎召唤物本就替玩家承伤,高血低攻即天然肉盾;**2026-08-25 字表重构**;**2026-08-25**:由灼烧型改控制型 —— ⚠ 这是「第二五行部件定型」的**例外**(艹+焦 的焦属火,按规则该是灼烧型),用户指定 |
| 荆 | 🟣紫 | `Summon 2`(330 血/攻 0)+ `Thorns 50` + `Taunt` | 2 只 · 纯反伤肉盾,攻 0,反弹 50%,**嘲讽** | ✅ 反伤为平值,不走生克;**2026-08-25 字表重构**;**2026-08-25**:由攻击型改前排肉盾反伤型,去掉 `Ranged`;同日再调:血 ×2 至 330、攻归 0、反伤 30 → **50%** —— 反伤是它唯一的输出手段,`Thorns` 的单位同时从固定点数改成**受到伤害的百分比**(与玩家侧 Reflect 同口径),固定值在深层会被怪的线性成长甩开;**2026-08-25**:补嘲讽 —— 没有它,攻 0 的反伤肉盾挨不挨打全看引擎的均匀随机,输出量不受玩家控制 |
| 桃 | 🟢绿 | `Summon 1`(120 血/攻 30)+ `HealAlly 30` | 1 只,每回合己方各回 30 | ⚠ **用户裁定移出字表**(2026-08-14)。移出时是绿档三张同血召唤(桃/槐/松,同为 120 血)之一;`HealAlly` 随之无字使用,字段保留待复活 |
| 槐 | 🟢绿 | `Summon 1`(120 血/攻 40)+ `OnHitCurse 25` | 1 只,出手挂减攻 25%(2 回合) | ⚠ **用户裁定移出字表**(2026-08-14)。`OnHitCurse` 随之无字使用,字段保留待复活 |
| 柳 | 🟢绿 | `Summon 1`(80 血/攻 30)+ `Dodge 50` | 1 只,50% 闪避 | ⚠ **2026-08-25 字表重构移出**:词组归零(柳林属树种+林,不计)。移除前:数值由 12/4 降为 8/3:闪避把期望有效血翻倍,拿原值会让同档的 松(12 血/攻 3)没有出场理由 |

### 召 · 单体 · 肉盾 / 控制 / 常规

| 字 | 稀 | 效果配置 | **最终值** | 实现 |
|---|---|---|---|---|
| 桂 | 🟠橙 | `Summon 3`(390 血/攻 50)+ `SummonShield 60` | 3 只 · 肉盾型,全场召唤物 +60 盾 | ✅ 一次性额外血条,吸完即无;**2026-08-25 字表重构** |
| 柘 | 🟡金 | `Summon 2`(330 血/攻 45) | 2 只 · 肉盾型 | ✅ 石部件 = 全场最硬召唤物;**2026-08-25 字表重构** |
| 桤 | 🟣紫 | `Summon 2`(165 血/攻 25)+ `SummonSpeed 150` | 2 只 · 肉盾型,速度 150 | ✅ 详表原写「当回合即可反击」是引擎既有默认行为,改用速度差异化;**2026-08-25 字表重构** |
| 藤 | 🔵蓝 | `Summon 1`(80 血/攻 40)+ `OnHitFreeze 10` + `OnHitFreezeTurns 1` | 1 只 · 控制型,出手 10% 冻结 1 回合(概率吃卡等级) | ✅;**2026-08-14 T9**:补攻;**2026-08-25 字表重构**;**2026-08-25**:入场冻结改出手概率冻结,并优先打未冻结目标 |
| 柏 | ⚪白 | `Summon 1`(40 血/攻 20) | 1 只 · 平衡型 | ✅ 长青 = 高耐久;**2026-08-25 字表重构** |
| 松 | 🟢绿 | `Summon 1`(60 血/攻 30) | 1 只 · 平衡型 | ✅;**2026-08-25 字表重构** |
| 杨 | 🟢绿 | `Summon 1`(80 血/攻 50) | 1 只 | ⚠ **配方缺口**:IDS ⿰木𠃓 的 𠃓(U+200D3)在增补平面 → 退化为叶子,拿不到(同 炼,见 §二·炼)。2026-08-10 移出字表,部件待拍板 |
| 桑 | ⚪白 | `Summon 1`(60 血/攻 20) | 1 只 | ⚠ **用户裁定移出字表**(2026-08-14)。与同档 梅(60 血/攻 20)数值完全相同,是白档召唤的同质对之一 |
| 梅 | ⚪白 | `Summon 1`(60 血/攻 20) | 1 只 | ⚠ **2026-08-25 字表重构移出**:词组归零(梅林属树种+林,不计)。移除前: |
| 杖 | ⚪白 | `Summon 1`(40 血/攻 10)+ `HealAlly 10` | 1 只 · 治疗型,每回合己方各回 10 | ✅ **2026-08-25 新增**:木系治疗召唤的唯一载体。配方 木+丈,丈 是中性部件,按「第二五行部件定型」该是平衡型 —— **本字是特例**,由用户指定为辅助/治疗 |
| 枪 | 🟢绿 | `Summon 1`(36 血/攻 48)+ `Skewer` + `ShapePercent 70` | 1 只 · 攻击型,贯穿前后排同列(非主目标 70%) | ✅ **2026-08-25 新增**:配方 木+仓。低血高攻 = 后排定位;贯穿是长枪的兵器语义,不给 `Ranged`(越前排只留给 荆) |

### 攻 · 单攻

| 字 | 稀 | 效果配置 | **最终值** | 实现 |
|---|---|---|---|---|
| 棒 | ⚪白 | `DamageSingle 70` | 单体 70 | ⚠ **2026-08-25 字表重构移出**:木系直伤与 棍 同质,木系转纯召唤。移除前: |
| 棍 | ⚪白 | `DamageSingle 60` | 单体 60 | ⚠ **2026-08-25 字表重构移出**:与 碾 数值全同,去重。移除前: |

---

## 四 · 水系 · 29 字

> **2026-09-02(水土双方向 Task 10)**:水系 15 张可出牌字改「纯二选一」的双方向 ——
> 「效果配置(基础值,治疗面)」是双击选「护」走的那一套,新增的「攻击效果配置(基础值)」
> 是双击选「攻」/拖到敌人身上走的那一套,两者互斥,不再像旧表那样两段效果一次全打出。
> 相生 ×3 已于同批 Task 6 取消,下表不再有 ×3 换算(沏 是最后一个残留案例,已在此并入重配)。

### 攻 · AOE / 单攻

| 字 | 稀 | 效果配置(基础值,治疗面) | 攻击效果配置(基础值) | 相生 | **最终值** | 实现 |
|---|---|---|---|---|---|---|
| 㵘 | 🔴红 | `SpendWaterPower 80` + `HealSelf 540` | `DamageAll 250` | — | 治疗面:泻(耗水势,层数×80 全体)+ 治疗 540;攻击面:全体 250 | ✅;**2026-09-02 双方向重配**:治疗面接「泻」(SpendWaterPower),与 淼 同型;攻击面纯 AOE;**2026-09-02 终审修复**:泻要在治疗前结算,否则本回合刚攒的层数被自己当场引爆 |
| 溎 | 🟠橙 | `DamageAll 240` + `HealSelf 240` | — | — | 全体 240 + 治疗 240 | ⚠ **2026-08-25 字表重构移出**:橙档换血为纯三部件字。移除前: |
| 淼 | 🟠橙 | `SpendWaterPower 50` + `HealSelf 430` | `DamageAll 190` | — | 治疗面:泻(耗水势,层数×50 全体)+ 治疗 430;攻击面:全体 190 | ✅;**2026-09-02 双方向重配**:治疗面接「泻」,攒与泄落在同一张字上;攻击面纯 AOE;**2026-09-02 终审修复**:泻要在治疗前结算,否则本回合刚攒的层数被自己当场引爆 |
| 淡 | 🟠橙 | `HealSelf 430` | `DamageAll 133` + `DispelEach 1` | — | 治疗面:治疗 430;攻击面:全体 133 + 全体各驱散 1 条 | ✅;**2026-09-02 双方向重配**:驱散移到攻击面,治疗面回归纯治疗 |
| 沝 | 🟡金 | `HealSelf 340` | `DamageSingle 340` | — | 治疗面:治疗 340;攻击面:单体 340 | ✅;**2026-09-02 双方向重配**:金档满值,护盾/治疗与单体伤害 1:1 |
| 湮 | 🟣紫 | `HealSelf 150` | `DamageSingle 105` + `DispelAll` | — | 治疗面:治疗 150;攻击面:单体 105 + 清空增益 | ✅;**2026-09-02 双方向重配**:纯效果满值,驱散仍是附加特性(×0.7) |
| 沏 | 🟣紫 | `HealSelf 180` | `DamageSingle 180` | — | 治疗面:治疗 180;攻击面:单体 180 | ✅;**2026-09-02 双方向重配**:紫档锚点 150 × 1.2 = 180,相生取消后用户拍板给它保留一点独特性;两面均为纯效果,不再有 ×3 换算 |
| 海 | 🟢绿 | `HealSelf 70` | `DamageAll 50` | — | 治疗面:治疗 70;攻击面:全体 50 | ✅;**2026-09-02 双方向重配** |
| 溃 | ⚪白 | `HealSelf 45` | `DamageSingle 32` + `Chain 3` + `ShapePercent 50` + `ArmorBreak 10` | — | 治疗面:治疗 45;攻击面:单体 32,弹射 3 跳(每跳 ×50%,不重复目标) + 破甲 10 | ✅;**2026-09-02 双方向重配**:攻击面带附加特性(弹射+破甲)按 ×0.7 折算;`LowestTierChar_StillDentsArmoredMobAtDepth20` 的判据基准现由 利/烧(仍是纯效果单攻 30)顶着,与 溃 无关 |
| 洪 | 🔵蓝 | `DamageAll 70` | — | — | 全体 70 | ⚠ **2026-08-25 字表重构移出**:收紧收词规则后词组归零(洪水/山洪均为本体部件词)。移除前: |
| 涛 | 🟢绿 | `DamageAll 50` | — | — | 全体 50 | ⚠ **2026-08-25 字表重构移出**:与 海 同为水系纯 AOE,去重。移除前: |
| 淹 | 🟢绿 | `DamageAll 50` | — | — | 全体 50 | ⚠ **用户裁定移出字表**(2026-08-14 第三批)。与 涛(绿·水,全体 50)数值效果全同,同质对 |
| 淬 | 🟢绿 | `DamageSingle 90` | — | — | 单体 90 | ⚠ **2026-08-25 字表重构移出**:词组归零(淬火为本体部件词)。移除前:对金 ×1.5 自动结算 |
| 沸 | ⚪白 | `DamageSingle 60` | — | — | 单体 60 | ⚠ **用户裁定移出字表**(2026-08-14 第三批)。白档水系单攻 60,与同档 割/剖/碾 同为 60 伤的纯单攻 |

### 攻 · 控制

| 字 | 稀 | 效果配置(治疗面) | 攻击效果配置 | **最终值** | 实现 |
|---|---|---|---|---|---|
| 洼 | 🟡金 | `DamageAll 140` + `Slow 2` | — | 全体 140 + 减速 2 回合 | ⚠ **2026-08-25 字表重构移出**:金档瘦身;减速仍有 冷 承载。移除前:;**2026-08-14 T9**:全体 200→140:减速按价目表计 0.30,不再白送 |
| 冰 | 🟡金 | `HealSelf 340` | `DamageSingle 238` + `DoubleVsControlled` + `Freeze 1` | 治疗面:治疗 340;攻击面:单体 238(对冻结/减速目标翻倍) + 冻结 1 回合 | ✅;**2026-09-02 双方向重配**:攻击面带控制翻倍(附加特性 ×0.7) |
| 冻 | ⚪白 | `HealSelf 45` | `Freeze 1` + `DamageSingle 32` | 治疗面:治疗 45;攻击面:冻结 1 回合 + 单体 32 | ✅;**2026-09-02 双方向重配** |
| 溺 | 🔵蓝 | `Slow 2` + `DamageSingle 90` | — | — | ⚠ **用户裁定移出字表**(2026-08-14)。T9 刚把它从「冻结 1」改为「减速 2」以与同档 冻 去同质,当日即裁定移除 |
| 溶 | 🟢绿 | `DamageSingle 60` + `ArmorBreak 15` | — | 单体 60 + 破甲 15(本场) | ⚠ **2026-08-25 字表重构移出**:词组归零。移除前:数值偏离原表:原为纯破甲无伤害,本轮补 `DamageSingle 6`(与「破」同值,靠五行区分:溶属水、破属土) |
| 凝 | 🟢绿 | `Slow 2` + `DamageSingle 65` | — | — | ⚠ **2026-08-25 字表重构移出**:与 冷 同为减速+单体,去重。移除前:;**2026-08-14 T9**:补攻 |
| 冷 | 🔵蓝 | `HealSelf 110` | `Slow 2` + `DamageSingle 77` | 治疗面:治疗 110;攻击面:减速 2 回合 + 单体 77 | ✅;**2026-09-02 双方向重配** |

> ⚠ **水系控制组受阻最重**:冻/溺/凝/冷 降级后全部退化成普通单攻。冰/冻/冷 的控制现在只在攻击面出现,治疗面纯治疗。

### 疗(单体 / 群体 × 即时 / 持续 四格全覆盖)

| 字 | 稀 | 二级 | 效果配置(基础值,治疗面) | 攻击效果配置(基础值) | 相生 | **最终值** | 实现 |
|---|---|---|---|---|---|---|---|
| 淋 | 🟠橙 | **群体即时** | `HealAll 430` | `DamageAll 190` | — | 治疗面:群体治疗 430;攻击面:全体 190 | ✅;**2026-09-02 双方向重配**:纯效果满值 |
| 澡 | 🟣紫 | **单体即时+净化** | `HealSelf 105` + `Cleanse` | `DamageSingle 105` + `Cleanse` | — | 治疗面:治疗 105 + 净化自身;攻击面:单体 105 + 净化自身 | ✅;**2026-09-02 双方向重配**:两面各自带净化(附加特性 ×0.7),不再是纯 buff |
| 沐 | 🟡金 | **单体持续** | `HealOverTime 113`(turns 3) | `DamageSingle 340` | — | 治疗面:持续治疗 113×3 回合;攻击面:单体 340 | ✅;**2026-09-02 双方向重配** |
| 润 | 🟢绿 | **群体持续** | `HealOverTime 20`(turns 2, targetAll) + `DamageSingle 35` | — | — | 玩家+全召唤物每回合 20,共 40;单体 35 | ⚠ **用户裁定移出字表**(2026-08-14 第三批)。群体持续治疗;与 滋 仅差单体/群体 |
| 滋 | 🟢绿 | 单体持续(可叠) | `HealOverTime 20`(turns 2) + `DamageSingle 35` | — | — | 每回合 20,共 40,可叠;单体 35 | ⚠ **用户裁定移出字表**(2026-08-14 第三批)。单体持续治疗(可叠);与 润 同组 |
| 活 | 🔵蓝 | **复活** | `Revive 1` + `DamageSingle 80` | — | — | — | ⚠ **2026-08-25 字表重构移出**:词组归零(活水为本体部件词);复活机制移交 浴。移除前:半血救回,保留被动;**2026-08-14 T9**:补攻 |
| 治 | 🔵蓝 | 单体即时 | `HealSelf 50` + `DamageSingle 45` | — | — | 治疗 50 + 单体 45 | ⚠ **用户裁定移出字表**(2026-08-14 第三批)。蓝档单体即时治疗 |
| 浴 | 🔵蓝 | **净化** | `HealSelf 77` + `Cleanse` | `Revive 1` + `DamageSingle 77` | — | 治疗面:治疗 77 + 净化自身;攻击面:复活 1 名召唤物(回半血) + 单体 77 | ✅;**2026-09-02 双方向重配**:复活移到攻击面(与净化分家),治疗面回归纯治疗+净化;蓝档 110 × 0.7(带净化)= 77 |

> `HealOverTime` 建议做成**一个 Kind + `TargetAll` 布尔**,与 `Shield.PersistOnce` 同构,
> 一次改动同时喂饱 沐/润/滋 三字。

---

## 五 · 金系 · 32 字

> **2026-09-02:相生 ×3 已取消**(全局拍板,见 `wuxing-reference.md`)。此前唯一受影响的
> **刲**(圭土+刂金 → 土生金)已改填 ×3 后的实战值(等值改写)。錰 原标「木生火」有误
> (錰属金,母属性是土),配方无土部件 → 从未吃到相生,不受影响。

### 攻 · AOE / 单攻

| 字 | 稀 | 效果配置(基础值) | 相生 | **最终值** | 实现 |
|---|---|---|---|---|---|
| 𨰻 | 🔴红 | `DamageSingle 540` + `Morale 1` | — | 单体 540,战意 +1 | ✅ 数值需改(原 45);**2026-08-15**:600 → 540,批量挂战意 |
| 鑫 | 🟠橙 | `DamageSingle 430` + `Morale 1` | — | 单体 430 + 战意 1 | ✅ 数值需改(原 30);**2026-08-15**:400 → 360,批量挂战意;**2026-08-25 字表重构** |
| 刲 | 🟠橙 | `DamageSingle 450` + `Morale 1` | — | **单体 450** + 战意 1 | ✅ 金上沿,低于橙 48;**2026-08-15**:基础 150 → 135(×3 = 405),批量挂战意;**2026-08-25 字表重构**;**2026-09-02**:相生 ×3 取消,基础值改填等值改写后的实战值(150 → 450) |
| 錰 | 🟡金 | `DamageSingle 415`,`Pierce 30` + `Morale 1` | — | 单体 415,穿透 30,战意 +1 | ⚠ **2026-08-25 字表重构移出**:金档单体扎堆,瘦身。移除前:数值偏离原表:原 45 → 40(45×1.15 穿甲加成 = 51 会触及上一档橙锚点 48);2026-08-12 E-b4 T3 把旧「穿甲无条件 +15%」固化进基础值,400 → 460,精确等价;**2026-08-15**:460 → 415,批量挂战意 |
| 锬 | 🟡金 | `DamageAll 180` + `Morale 1` | — | 全体 180,战意 +1 | ⚠ **2026-08-25 字表重构移出**:金档 AOE 与 淡 同质。移除前:;**2026-08-15**:200 → 180,批量挂战意 |
| 鍂 | 🟡金 | `DamageSingle 180` + `Morale 1` | — | 单体 180,战意 +1 | ✅ 数值需改(原 14);**2026-08-15**:200 → 180,批量挂战意;**2026-08-25 字表重构** |
| 铡 | 🟣紫 | `DamageSingle 110` + `ExecuteKill 25` + `DoubleVsBleeding` + `Morale 1` | — | 单体 110(对流血目标翻倍;斩杀 25%→直杀,Boss 双倍) + 战意 1 | ✅ Boss 免疫抹杀但吃双倍;2026-08-07 拍板降伤(原 20)—— 同档同系 鍂/锹 都是纯 20 伤,铡带处决 rider 却与它们同伤会让后两者严格劣化,与刮为「驱散全部」让伤(9→6)同待遇;**2026-08-15**:160 → 145,批量挂战意;**2026-08-23**:斩杀线统一 25%(Boss 改吃双倍而非无收益);**2026-08-25**:装配 `DoubleVsBleeding`,与 劈 组成金系的「铺流血 → 收割」链;伤害 145 → 110 让出预算 |
| 剁 | 🟣紫 | `DamageSingle 70` + `HitCount 2` + `Bleed 20` + `Morale 1` | — | 单体 70(2 段独立结算) + 流血 20/回合(3 回合) + 战意 1 | ✅ 每段独立过生克/破甲/斩杀阈值;2026-08-13 E-b4/E-b5 T8 按多段补偿规则 100→110(见 §1.4 的补偿规则与订正);**2026-08-15**:110 → 100,批量挂战意;**2026-08-25**:补流血 —— 剁 是全表唯一多段字,两段各触发一次受击后效,与流血最对味;140 + 60 + 战意 ≈ 紫档 200×多段补偿 1.1 |
| 锹 | 🟣紫 | `DamageSingle 180` + `Morale 1` | — | 单体 180,战意 +1 | ⚠ **2026-08-25 字表重构移出**:与 鍂 数值全同,去重。移除前:;**2026-08-15**:200 → 180,批量挂战意 |
| 劈 | ⚪白 | `DamageSingle 30` + `Bleed 10` | — | 单体 30 + 流血 10/回合(3 回合) | ✅;**2026-08-15**:130 → 115,批量挂战意;**2026-08-25 字表重构**;**2026-08-25**:接手全表无载体的 `Bleed`。流血是**无属性**伤害不走生克,正好补上金系「低档铺状态」的位子,给 铡 收割 |
| 锤 | 🔵蓝 | `DamageSingle 90` + `ArmorBreak 20` | — | 单体 90 + 破甲 20(本场) | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。与 熔(火蓝)同质,见上 |
| 刺 | 🔵蓝 | `DamageSingle 100`,`Pierce 15` + `Skewer` + `ShapePercent 60` + `Morale 1` | — | 单体 100(穿透 15),贯穿同列前后排(非主目标 60%) + 战意 1 | ✅ 2026-08-12 E-b4 T3:旧「穿甲 +15%」固化进基础值 130 → 150;**2026-08-15**:150 → 135,批量挂战意;**2026-08-20**:加偷袭 —— 全字表唯一能点敌方后排的直伤字;**2026-08-25 字表重构**;**2026-08-25**:偷袭换成贯穿 —— 两者都是「够到后排」,贯穿是其中有几何的那个。⚠ 它是教程演示字:首层单怪时贯穿只中 1 只,100 × 金克木 1.5 = 150 ≥ 140,一击清场仍成立 |
| 戟 | 🔵蓝 | `DamageAll 70` | — | 全体 70 | ⚠ **配方缺口**:IDS ⿰𠦝戈 的 𠦝(U+2099D)在增补平面 → 退化为叶子,拿不到(同 炼,见 §二·炼)。2026-08-10 移出字表,部件待拍板(候选 `钅+戈`,要新增 戈 部件) |
| 剑 | 🔵蓝 | `Summon 1`(48 血/攻 64)+ `Sweep` + `ShapePercent 50` | — | 1 只 · 攻击型(横扫,非主目标 50%) | ✅;**2026-08-15**:130 → 115,批量挂战意;**2026-08-25 字表重构** |
| 镰 | 🟢绿 | `DamageSingle 65` + `ExecuteBonus 25` + `Morale 1` | — | 单体 65,残血 ×2,战意 +1 | ⚠ **2026-08-25 字表重构移出**:词组归零(镰刀为本体部件词);斩杀仍有 剿/铡 两个载体。移除前:与剿靠单体/全体分开;**2026-08-14 T9**:单体 90→75:斩杀计 0.15;**2026-08-15**:75 → 65,批量挂战意;**2026-08-23**:斩杀线统一 25%(Boss 改吃双倍而非无收益) |
| 戮 | 🟢绿 | `DamageSingle 75` + `Morale 1` | — | 单体 75 + 战意 1 层 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。T9 刚按战意计价降到 75;移除前状态:2026-08-12 落地(E-b3-a);战意与 战 共用同一条计数器,上限 5 层 |
| 锥 | ⚪白 | `Summon 1`(24 血/攻 32)+ `Shots 2` | — | 1 只 · 攻击型,连发 2 发(后排优先) | ✅ 2026-08-12 E-b4 T3:旧「穿甲 +15%」固化进基础值 90 → 105;**2026-08-15**:105 → 95,批量挂战意;**2026-08-25 字表重构**;**2026-08-25**:装配连发 —— `Volley` 此前全表无载体 |
| 剿 | 🔵蓝 | `DamageSingle 100` + `ExecuteBonus 25` + `Morale 1` | — | 单体 100(斩杀 25%→双倍) + 战意 1 | ✅ 剿灭一群;**2026-08-14 T9**:全体 60→40:斩杀计 0.15,原值本就超锚 1.20;**2026-08-15**:40 → 35,批量挂战意;**2026-08-23**:绿 → 蓝,全体 35 → 55,斩杀线统一 25%;**2026-08-25 字表重构**;**2026-08-25**:由全体改**单体**斩杀并升蓝档,数值按蓝档单攻锚点 130 配(100 + 斩杀 0.15 + 战意 0.10) |
| 削 | 🟢绿 | `DamageSingle 75` + `Dispel 1` | — | 单体 75 + 削 1 层 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。与 刮 同为绿档「单攻 + 驱散」,靠「削一条 / 刮干净」区分;移除前状态:与刮靠「削一条 / 刮干净」分开 |
| 刮 | 🟢绿 | `DamageSingle 60` + `DispelAll` | — | 单体 60 + 清空增益 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。与 削 同组,见上;移除前状态:刮得干净,打得轻 |
| 割 | ⚪白 | `DamageSingle 60` | — | 单体 60 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。与 剖 数值完全相同(白档单攻 60),同质对,两张一并移除 |
| 剖 | ⚪白 | `DamageSingle 60` | — | 单体 60 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。与 割 同质,见上 |

### 攻 · 控制 / DOT / 防

| 字 | 稀 | 效果配置(基础值) | 相生 | **最终值** | 实现 |
|---|---|---|---|---|---|
| 锁 | 🔵蓝 | `Silence 0`(turns 1) + `DamageSingle 85` | — | 沉默 1 回合 + 单体 85 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。蓝档沉默;T9 刚补攻 85;移除前状态:Boss 蓄力取消 + 五种字怪能力哑火 |
| 钩 | 🟢绿 | 把后排拉到前排 | — | — | ⚠ **模型缺口**:引擎里敌人是平表、无排位概念,当前战斗模型下无从实现 |
| 锯 | 🟢绿 | `Bleed 10`(每回合 1,**无属性**) | — | 每回合 10 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。全表唯一的 Bleed 载体 —— 移除后流血机制无字使用 |
| 铸 | 🟢绿 | `Reflect 40`(turns 2) + `DamageSingle 65` | — | 反弹 40%,2 回合 + 单体 65 | ⚠ **2026-08-25 字表重构移出**:反弹机制移交 壁。移除前:**2026-08-14 第三批新增**:接手 镜 移出后无载体的 `Reflect`。绿档口径比旧 镜(蓝,50%/2 回合)低一档;反弹按价目表计 0.30(低于蓝档 50% 的 0.35),余下 0.70 给单攻 = 90×0.70 = 63 → 65。配方 `钅 + 寿` 走 IDS ⿰钅寿,两个部件都已在表 |
| 铠 | 🟣紫 | `DefenseBuff 5` | — | 护甲 +5 | ✅;**2026-08-14 T9**:补攻;护甲 12→5 —— 金系不该在防御轴压过同档土系 崟;**2026-08-29 用户拍板**:去伤害,回归纯 buff —— 增益已可单体指定(含加给召唤物),带一发伤害会逼玩家先选敌人再选友方、拖拽也没法直接拖到友方。撤销 T9 那次「补攻」 |
| 镜 | 🔵蓝 | `Reflect 50`(turns 2) + `DamageSingle 85` | — | 反弹 50%,2 回合 + 单体 85 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。蓝档反弹;T9 刚补攻 85;移除前状态:按总伤害反,护盾吸掉的也反 |

### 不沾四类(BUFF)

| 字 | 稀 | 效果配置 | 实现 |
|---|---|---|---|
| 剡 | 🟡金 | `Empower 50` + `DamageSingle 220` | ⚠ **2026-08-25 字表重构移出**:金档瘦身;攻击力增益机制移交 战。移除前:2026-08-12 落地(E-b3-a)。本场攻击 +50(基准 100 → 150,即 +50%);可叠加、本场持久;**2026-08-14 T9**:补攻 |
| 战 | 🟢绿 | `Empower 30` | ✅ 2026-08-12 落地(E-b3-a)。战意 +3 层,每层 +10 攻击、上限 5 层(满层 +50 = 剡 单张的量);本场持久;**2026-08-14 T9**:补攻;**2026-08-25 字表重构**;**2026-08-29 用户拍板**:去伤害,回归纯 buff —— 增益已可单体指定(含加给召唤物),带一发伤害会逼玩家先选敌人再选友方、拖拽也没法直接拖到友方。撤销 T9 那次「补攻」 |
| 利 | 🟢绿 | `ApBoost 1` + `DamageSingle 30` | ✅ 2026-08-12 落地(E-b3-a)。本场每回合 AP 上限 +1;可叠加,**不吃卡等级**(AP 是节奏/经济不是资源,与驱散条数同口径);**2026-08-14 T9**:补攻;AP 计 0.50(构筑级效果,白档最贵的一张);**2026-08-25 字表重构** |
| 锋 | 🔵蓝 | `CritBuff 20` | ✅ 2026-08-12 落地(E-b2)。本场暴击率 +20 个百分点,暴击伤害 ×1.5;可叠加、本场持久,上限由 `EffectiveCrit` 钳到 100。只有单体/群体**直接伤害**吃暴击,灼烧/引爆/流血/召唤物/护盾/治疗一律不吃;**2026-08-14 T9**:补攻;**2026-08-25 字表重构**;**2026-08-25**:补流血 —— 「锋」是刃口,割伤流血语义正;伤害 50→30 让出预算(暴击计 0.42 + 伤 30 + 流血 45 ≈ 蓝档 130);**2026-08-29 用户拍板**:去伤害,回归纯 buff —— 增益已可单体指定(含加给召唤物),带一发伤害会逼玩家先选敌人再选友方、拖拽也没法直接拖到友方。撤销 T9 那次「补攻」 |
| 锐 | 🟢绿 | `PierceBuff 20` | ✅ 2026-08-12 落地(E-b4 T5)。本场穿透 +20 点,进 `EffectiveEnemyDefense` 的减数;可叠加、本场持久。定位:一张正好穿光墨渍(T3 落地的 DEF 20;spec §12 写的 25 是拟定值)、抵江/钧阶段 30 的 2/3,两张叠满 40 配 錰 的本体穿透 30 才打得穿山阶段的 60 —— 详表原写「穿透 +3」是 6 伤量级下的数字,×10 得 30,再按「一张白字不应单独穿光坚壁 Boss」下调到 20;**2026-08-14 T9**:补攻;**2026-08-25 字表重构**;**2026-08-29 用户拍板**:去伤害,回归纯 buff —— 增益已可单体指定(含加给召唤物),带一发伤害会逼玩家先选敌人再选友方、拖拽也没法直接拖到友方。撤销 T9 那次「补攻」 |

> 2026-08-12(E-b3-a):BUFF 组 6 字**不再是同一个 `Buff` 扩展**。只依赖 E-b1 攻击力载体与
> 既有 AP 系统的 4 字(剡 / 战 / 戮 / 利)已落地为 `Empower` / `Morale` / `ApBoost` 三个
> `EffectKind`,可入池;剩下 锋(暴击)/ 锐(穿透)各自等一条独立的属性轴,继续暂缓。
>
> 2026-08-12(E-b4 T5):锐 随穿透轴落地,**BUFF 组 ⚠ 清零**(6 字全部可入池)。
> 穿透口径:`PierceBuff` 是本场持久的 Buff,`SourceId` 铸唯一序号故可叠;它与效果自带的
> 一次性 `Pierce`(錰/刺/锥)进的是同一个减数,与目标身上的破甲从同一份基础护甲里合并相减,
> 外层 `max(0, …)` 保证穿过头只是归零、绝不倒贴增伤。配方 `钅 + 兑`,新部件 兑 随本轮入表。
>
> 2026-08-12(E-b2):锋 随暴击轴落地,BUFF 组只剩 锐 一个 ⚠(等 E-b4 穿透)。
> 暴击口径:暴击率**不随角色等级成长**(用户裁定),基准恒 0 —— 锋 与将来的养成技能
> 是它唯一的来源;倍率 ×1.5 是常量(`BattleConfig.CritMultiplierPercent`),E-b5 重调。
> 已知代价:火系(灼烧/引爆)与木系(召唤)吃不到暴击,锋 在这两个流派里是废牌 ——
> 与 E-b1 「护盾/治疗吃不到 ATK」同一个立场(不同流派堆不同属性),平衡问题留给 E-b5。
>
> 战意口径:`StatusKind.Morale` 的 `Magnitude` 是**层数**不是攻击加成值;战与戮往同一条
> 计数器上累加并钳到 5 层(3 + 3 = 5,不是 6)。已知代价:抬攻击力会**回溯**放大已经挂在
> 场上的灼烧 —— 沿用 E-b1 「灼烧/引爆结算时读 ATK」的既有口径,是设计不是 bug。

---

## 六 · 土系 · 27 字

> 受 ×3 影响 **0 字**。嶘/漜/崟 都是土系,母属性是火,而配方里全无火部件 —— 土系护盾的相生缺口。

> **2026-09-02(水土双方向 Task 11)**:土系 13 张可出牌字改「纯二选一」的双方向 ——
> 「效果配置(基础值,护盾面)」是双击选「护」走的那一套,新增的「攻击效果配置(基础值)」
> 是双击选「攻」/拖到敌人身上走的那一套,两者互斥,不再像旧表那样两段效果一次全打出。
> **碉/堡/塔 三张召唤字例外**:攻击面保留原有召唤效果(血/攻/被动)不动,只新增护盾面 ——
> 召唤的数值体系是另一套(血/攻/被动),不套伤害锚点折算。相生 ×3 已于同批 Task 6 取消,
> 下表不再有 ×3 换算。

### 防 · 护盾

| 字 | 稀 | 效果配置(基础值,护盾面) | 攻击效果配置(基础值) | 相生 | **最终值** | 实现 |
|---|---|---|---|---|---|---|
| 㙓 | 🔴红 | `Shield 540`(`PersistOnce`) | `DamageAll 250` + `SpendMomentum 60` | — | 护盾面:盾 540(豁免一次清盾);攻击面:全体 250 + 发势(层数×60) | ✅;**2026-09-02 双方向重配**:红档满值;攻击面挂发势,是引爆机制的红档载体(与绿档 崩 配对) |
| 嶘 | 🟠橙 | `Shield 360`(`PersistOnce`)+ `DamageAll 180` | — | — | 盾 360 + 全体 180 | ⚠ **2026-08-25 字表重构移出**:橙档换血为纯三部件字。移除前: |
| 垚 | 🟠橙 | `Shield 430` | `DamageSingle 301` + `DoubleVsArmorBroken` | — | 护盾面:盾 430(满值);攻击面:单体 301(对破甲目标翻倍,附加特性 ×0.7) | ✅;**2026-09-02 双方向重配**:护盾满值 430,攻击面带翻倍特性按 0.7 折(430×0.7=301);原「盾 240+单攻 100」合并式拆分 |
| 圭 | 🟡金 | `Shield 340` | `DamageSingle 340` | — | 护盾面:盾 340;攻击面:单体 340 | ✅;**2026-09-02 双方向重配**:金档满值,护盾/单体伤害两面各自 1:1;原「盾 200+单攻 135」合并式拆分 |
| 塔 | 🟣紫 | `Shield 150` | `Summon 2`(110 血/攻 50) | — | 护盾面:盾 150;攻击面:2 只 · 平衡型(不变) | ✅;**2026-09-02 双方向重配**:新增护盾面(紫档满值 150);攻击面数值不变 —— 召唤走血/攻/被动体系,不套伤害锚点 |
| 城 | 🔵蓝 | `Shield 65` + `DamageSingle 45` | — | — | 盾 65 + 单体 45 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。与 垒 同为蓝档护盾,T9 后两张配比一致(盾 65 + 攻 45) |
| 碉 | ⚪白 | `Shield 45` | `Summon 1`(120 血/攻 0)+ `Thorns 50` | — | 护盾面:盾 45;攻击面:1 只 · 纯反伤肉盾,攻 0,被打反弹 50%(不变) | ✅;**2026-09-02 双方向重配**:新增护盾面(白档满值 45);攻击面数值不变 —— 召唤走血/攻/被动体系,不套伤害锚点 |
| 垒 | 🟢绿 | `Shield 70` | `DamageSingle 70` | — | 护盾面:盾 70;攻击面:单体 70 | ✅;**2026-09-02 双方向重配**:绿档满值,两面各自 70;原「盾 50+单攻 30」合并式拆分 |
| 堡 | 🔵蓝 | `Shield 110` | `Summon 1`(240 血/攻 0)+ `Thorns 50` + `Taunt` | — | 护盾面:盾 110;攻击面:1 只 · 纯反伤肉盾,攻 0,反弹 50%,**嘲讽**(不变) | ✅;**2026-09-02 双方向重配**:新增护盾面(蓝档满值 110);攻击面数值不变 —— 召唤走血/攻/被动体系,不套伤害锚点 |
| 墙 | 🟢绿 | `Shield 45` + `DamageSingle 35` | — | — | 盾 45 + 单体 35 | ⚠ **2026-08-25 字表重构移出**:与 壁 同为护盾+单体,去重。移除前:;**2026-08-14 T9**:补攻;盾 70→45,单攻 30 守住 35% 下限 |
| 壁 | 🟢绿 | `Shield 49` + `Reflect 30`(turns 2) | `DamageSingle 49` + `Reflect 30`(turns 2) | — | 护盾面:护盾 49 + 反弹 30%×2 回合;攻击面:单体 49 + 反弹 30%×2 回合 | ✅;**2026-09-02 双方向重配**:反弹是防御机制,两面都保留;绿档 70×0.7(带反弹)= 49,原盾 40 → 49 |

### 防 · 减伤 / 免伤

| 字 | 稀 | 效果配置(基础值,护盾面) | 攻击效果配置(基础值) | 相生 | **最终值** | 实现 |
|---|---|---|---|---|---|---|
| 漜 | 🟠橙 | `DefenseBuff 10` + `DamageSingle 170` | — | — | 护甲 +10 + 单体 170 | ⚠ **2026-08-25 字表重构移出**:橙档换血为纯三部件字。移除前:;**2026-08-14 T9**:补攻;护甲 15→10,去掉 Slow 2(橙档护甲王不再兼控制) |
| 崊 | 🟡金 | `DefenseBuff 8` + `DamageSingle 140` | — | — | 护甲 +8 + 单体 140 | ⚠ **2026-08-25 字表重构移出**:金档护甲与 铠 同质。移除前:;**2026-08-14 T9**:补攻;护甲 12→8 |
| 崟 | 🟣紫 | `DefenseBuff 6` + `DamageSingle 70` | — | — | 护甲 +6 + 单体 70 | ⚠ **2026-08-25 字表重构移出**:紫档护甲与 铠 同质。移除前:;**2026-08-14 T9**:补攻;护甲 9→6 |
| 磐 | 🔵蓝 | `DefenseBuff 4` + `DamageSingle 45` | — | — | 护甲 +4 + 单体 45 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。土系蓝档护甲 4 点 |
| 巍 | ⚪白 | `DefenseBuff 2` + `DamageSingle 30` | — | — | 护甲 +2 + 单体 30 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。土系白档护甲 2 点,是全表最低伤害档(T3-V4 判据的基准字) |
| 杜 | 🟡金 | `Shield 238` + `Immunity 2` | `DamageSingle 238` + `Immunity 1` | — | 护盾面:护盾 238(带免疫 ×0.7)+ 免疫 2 次;攻击面:单体 238(带免疫 ×0.7)+ 免疫 1 次 | ✅;**2026-09-02 双方向重配**:金档 340×0.7(带免疫)= 238,两面各自是「主效果+免疫」;双方向让玩家可选纯护(不选敌)或纯攻(选敌),不再是同一次施放里既要选敌又要选友——撤销 2026-08-29「去伤害回归纯 buff」的裁定理由(那次是因为单一效果列表逼玩家先选敌人再选友方,双方向拆分后这个顾虑不复存在) |
| 岿 | 🔵蓝 | `Immunity 1` + `Cleanse` + `DamageSingle 50` | — | — | 免疫 1 次 + 净化 + 单体 50 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。蓝档免疫 + 净化;移除前状态:岿然不动:稳如山,顺手抖掉杂七杂八 |
| 塞 | 🟢绿 | `Immunity 1` + `DamageSingle 60` | — | — | 免疫 1 次 + 单体 60 | ⚠ **用户裁定移出字表**(2026-08-14 第二批)。绿档免疫 1 次 |

> 减伤组降级成护盾后**仍各有数值梯度**,不像水系控制组那样退化成同质普攻。

### 攻

| 字 | 稀 | 效果配置(基础值,护盾面) | 攻击效果配置(基础值) | **最终值** | 实现 |
|---|---|---|---|---|---|
| 崩 | 🟢绿 | `Shield 70` | `DamageAll 50` + `SpendMomentum 30` | 护盾面:盾 70;攻击面:全体 50 + 发势(层数×30) | ✅;**2026-09-02 双方向重配**:新增护盾面(绿档满值 70);攻击面挂发势,是前期就能拿到的引爆载体(中档,与红档 㙓 配对) |
| 塌 | 🔵蓝 | — | `DamageAll 70` | 全体 70 | ⚠ **配方缺口**:IDS ⿰土𦐇 的 𦐇(U+26407)在增补平面 → 退化为叶子,拿不到(同 炼,见 §二·炼)。2026-08-10 移出字表;同表 崩 已是同数值同稀有度的 `DamageAll 7`,本字纯属重复,复活优先级最低 |
| 破 | 🟢绿 | — | `DamageSingle 60` + `ArmorBreak 15` | 单体 60 + 破甲 15(本场) | ⚠ **2026-08-25 字表重构移出**:与 碎/溃 同为单体+破甲,去重。移除前: |
| 砸 | ⚪白 | `Shield 45` | `DamageSingle 45` + `Cleave` + `ShapePercent 50` | 护盾面:盾 45;攻击面:单体 45,溅射相邻(非主目标 50%) | ✅;**2026-09-02 双方向重配**:新增护盾面(白档满值 45);攻击面数值不变 |
| 埋 | 🟢绿 | — | `DamageSingle 60` + `Freeze 1` | 单体 60 | ⚠ **用户裁定移出字表**(2026-08-14)。与 坑 数值效果完全相同,是绿档土系的同质对;两张一并移除 |
| 坑 | 🟢绿 | — | `DamageSingle 60` + `Freeze 1` | 单体 60 | ⚠ **用户裁定移出字表**(2026-08-14)。与 埋 同质,见上 |
| 碾 | ⚪白 | `Shield 45` | `DamageSingle 45` + `Sweep` + `ShapePercent 60` | 护盾面:盾 45;攻击面:单体 45,横扫整排(非主目标 60%) | ✅;**2026-09-02 双方向重配**:新增护盾面(白档满值 45);攻击面 40→45,与白档满值对齐(原值系旧单方向定价遗留) |
| 碎 | 🔵蓝 | `Shield 110` | `DamageSingle 77` + `ArmorBreak 20` | 护盾面:盾 110;攻击面:单体 77 + 破甲 20(带破甲 ×0.7) | ✅;**2026-09-02 双方向重配**:蓝档满值 110(护盾面);攻击面 110×0.7(带破甲)= 77,原单方向值 90 → 77 |

---

## 七 · 引擎扩展需求清单

### 7.1 破甲 = 削减护甲点数

⚠ **本节 2026-08-12(E-b4 T3)整段重写。** 原文的前提是「引擎**没有「护甲」概念**,
但 `EnemyDef` 有 `DamageTaken` 承伤系数」,所以把破甲实现成「承伤 +25%、不叠层、限时」的
乘法代偿。**那个前提消失了** —— 点数制护甲上线,承伤系数字段删除,守方侧从此没有任何乘数。
破甲回到第 10 章 `:56` 的原始设计:**削目标护甲 N 点,本场持久、可叠加**。

```csharp
// 施加(BattleEngine.cs):Value 就是**削减的护甲点数**(不再是回合数)。
// TurnsLeft = -1 本场持久(依据第 10 章 :56「破甲永久降护甲」);
// SourceId 铸唯一序号 → **可叠加**(不叠只刷新的话六个破甲字互相排斥,
// 先出削 20 的再出削 10 的会变弱,而「三张接力削光坚壁 Boss」是它的设计玩法)。
case EffectKind.ArmorBreak:
    enemy.Statuses.Apply(new StatusEffect {
        Kind = StatusKind.ArmorBreak, Polarity = StatusPolarity.Debuff,
        Magnitude = value, TurnsLeft = -1, SourceId = $"{def.Id}#{_statusSerial++}",
    });
    break;

// 结算(EffectiveEnemyDefense):破甲与穿透从**同一个基础护甲**里减,
// 一个 max(0,…) —— 不嵌套、不重复扣,削过头只归零、绝不倒贴成增伤。
private int EffectiveEnemyDefense(EnemyState enemy, int pierce) => Math.Max(0,
    enemy.Defense
    - enemy.Statuses.TotalMagnitude(StatusKind.ArmorBreak)
    - (pierce + _playerStatuses.TotalMagnitude(StatusKind.PierceBuff)));
```

| 目标 | 护甲 | 一张 锤(破甲 20)后 | 效果 |
|---|---|---|---|
| 坚壁「山」阶段 | 60 | 40 | 要**三张蓝档**才削光(战例二的标定依据) |
| 墨渍(小怪) | 20 | 0 | 一张即削光 |
| 无甲敌人 | 0 | 0 | 破甲部分是纯浪费 —— 但 6 个字**全部带伤害**,退化成普通伤害字,不是废卡 |

**破甲与穿透的一句话区分**(文案要能让玩家一眼分开):

| | 破甲 | 穿透 |
|---|---|---|
| 削的是 | 目标的甲,**削掉就一直是削掉的** | 这一击的**视角**,下一击不算数 |
| 挂在谁身上 | **目标** | **攻击者**(或这张字自带) |
| 持续 | 本场 | 本次 |
| 队友能不能蹭到 | 能 —— 甲是真的薄了,召唤物打它也吃这个便宜 | 不能 |

⚠ 原文那条「被克(×1.5)时承伤减免直接失效,但破甲加成不受此限」的补丁**已删除,且不需要
替代品**:它当年存在是因为乘法层会按比例抽走克制的收益(100×1.5×0.5 = 75,而无甲时是 150)。
减法对乘法是透明的 —— 基础 100 对护甲 30 的敌人,不克制打 70、克制 ×1.5 打 120,净收益 +50,
**与无甲时完全相同**。打对属性的奖励天然不被护甲侵蚀。

涉及字统一为 `ArmorBreak`(原「破甲/破盾」合并):**碎 10 / 溶 15 / 破 15 / 熔 20 / 溃 20 / 锤 20**(6 字)。
「穿甲」这个词**本批之后不再存在**:錰 / 刺 / 锥 改走 `EffectDef.Pierce` **点数**,
旧的 `IgnoreArmor` 布尔标记删除,它那份无条件 +15% 已**固化进基础值**(400→460 / 130→150 / 90→105)
—— 对无甲目标一分不差,模型少一个常量。

### 7.2 汇总

| 优先级 | 新 EffectKind / 字段 | 字数 | 涉及字 |
|---|---|---|---|
| **P0** | **灼烧接入生克**(改 `BurnTick`) | 18 | 全部火系 DOT 与 AOE |
| **P0** | **`Bleed`(无属性 DOT)** | 1 | 锯 —— 无降级方案 |
| **P0** | **`HealAll` + `HealOverTime`(带 `TargetAll`)** | 4 | 淋 润 沐 滋 |
| **P0** | `Freeze` / `Stun` / `Slow` / `Root` | 11 | 冰 冻 溺 凝 冷 埋 坑 藤 洼 漜 |
| **P0** | `DamageReduction` | 6 | 铠 崊 崟 磐 巍 漜 |
| ~~P1~~ ✅ | **`ArmorBreak`(承伤系数,见 7.1)** | 6 | 熔 溃 溶 锤 破 碎 —— 已实现(2026-08-05) |
| ~~P1~~ ✅ | `Dispel` | 5 | 灭 湮 淡 削 刮 —— 已实现(2026-08-06) |
| ~~P1~~ ✅ | `Immunity` | 3 | 杜 岿 塞 —— 已实现(2026-08-06) |
| ~~P1~~ ✅ | `Execute` | 3 | 铡 镰 剿 —— 已实现(2026-08-06;烬 早已用 DoubleVsBurning 落地,不在此列) |
| **P2** | `Buff`(攻/暴击/穿透/AP) | 5 | 剡 锋 锐 战 利 |
| ~~P2~~ ✅ | 召唤物被动族 | 9 | 蕉 荆 烓 灶 桃 楸 桂 槐 桤 —— 已实现(2026-08-06) |
| ~~P2~~ ✅ | `Cleanse` / `Revive` | 3 | 澡 浴 活 —— 已实现(2026-08-06) |
| ~~P3~~ ✅ | `Blind` / `Silence` / `Reflect` | 4 | 熣 烟 锁 镜 —— 已实现(2026-08-07;钩 为模型缺口,移出字表) |
| ~~P3~~ ✅ | `MultiHit` | 1 | 剁 —— 已实现(2026-08-07) |
| ~~P3~~ ✅ | `IgnoreArmor` | 3 | 錰 刺 锥 —— 已实现(2026-08-05) |
| ~~P3~~ ✅ | `BurnNoDecay` / `BurnSettleNow` / `Detonate` | 3 | 炑 燥 灱 —— 已实现(2026-08-09,火系 DOT 三分化;燊 早已落地) |

### 7.3 实现覆盖率

| 状态 | 火 | 木 | 水 | 金 | 土 | 合计 | 占比 |
|---|---|---|---|---|---|---|---|
| ✅ 可直接实现 | 30 | 16 | 28 | 30 | 24 | **128** | 92.1% |
| ⚠ 需扩展 | 1 | 4 | 1 | 2 | 3 | **11** | 7.9% |

> 2026-08-08:计数订正——原表(22/11/12/11/15 = 71;9/9/17/21/12 = 68)是设计初期快照,
> 此后灼烧生克、P0 控制族/减伤、P1 破甲/驱散/免疫/斩杀、P2 召唤物被动/净化复活、
> P3 伤害变体六波陆续落地,原「⚠ 需扩展」里对应的字逐波转为「✅」,表格没跟着改。
> 现按 7.2 的实现状态自数一遍:全表 139 字里,只剩 BUFF 组(剡/锋/锐/战/利/戮,6 字,金)、
> `BurnRefresh`/`HealReduction`(炑/燥/灱,3 字,火)、`钩`(模型缺口,金)共 10 字仍需扩展,
> 其余 129 字已可直接落地。
>
> 2026-08-09:计数再订正——火系 DOT 三分化落地,`BurnNoDecay`/`BurnSettleNow`/`Detonate`
> 三个 Kind 接入引擎,炑/燥/灱转为「✅」,火系「⚠ 需扩展」清零。全表 139 字里现只剩
> BUFF 组(剡/锋/锐/战/利/戮,6 字,金)、`钩`(模型缺口,金)共 7 字仍需扩展,
> 其余 132 字已可直接落地。
>
> 2026-08-10:计数三订正——**配方缺口**这一类首次被识别并计入「⚠ 需扩展」。
> 炼(火)/杨(木)/戟(金)/塌(土)的 IDS 各有一侧部件落在增补平面,UGUI Text 显示不出
> 代理对,`_blocked_smp_part` 跳过整条配方 → 整字退化为叶子;而叶子不进奖励池
> (`RunEngine.RollRewardOptions` 跳过 `IsLeaf`),既不能合成也发不出来,躺在字表里纯占位。
> 此前它们一直标 ✅ 并进了 `chars.json`,是**四个玩家永远拿不到的死条目**。
> 本日移出字表(132 → 128),各补一条 `MANUAL_RECIPES` 换部件即可复活,部件待拍板。
>
> 2026-08-14:计数五订正——**用户裁定移出 6 字**:埋 / 坑(绿·土,同质对,数值效果全同)、
> 溺(蓝·水)、桑(白·木,与 梅 全同)、桃 / 槐(绿·木)。与「配方缺口」那类不同,这批**配方完好**,
> 是主动精简。木 19 → 16、水 29 → 28、土 26 → 24,✅ 134 → 128。
> 管线随之自动清掉 6 个只被它们用的孤儿部件(亢 / 里 / 弱 / 叒 / 兆 / 鬼),
> `chars.json` 235 → 223 条。
> ⚠ 两个召唤被动 `HealAlly`(桃)/ `OnHitCurse`(槐)**自此无字使用** ——
> `SummonPassive` 的字段与引擎实现都保留,复活或新字接手即可用。
>
> 2026-08-12:计数四订正——E-b3-a 落地 BUFF 组的 剡 / 战 / 戮 / 利 四字
> (`Empower` / `Morale` / `ApBoost` 三个新 `EffectKind`,只依赖 E-b1 攻击力载体与既有
> AP 系统)。戮 顺带补了一条 `MANUAL_RECIPES`(⿰翏戈 的 翏 太生僻 → 羽 + 戈),
> 是配方缺口四字之外第一个被换部件救回来的。金系 ✅ 24 → 28、⚠ 8 → 4,全表 128 → 132。
>
> 2026-08-12:计数五订正——E-b2 落地暴击轴,锋 转为「✅」。锋 也补了一条
> `MANUAL_RECIPES`(⿰钅夆 的 夆 太生僻 → 钅 + 丰,夆 = 夂 + 丰)。
> 金系 ✅ 28 → 29、⚠ 4 → 3,全表 132 → 133。
>
> 2026-08-12:计数六订正——E-b4 T5 落地穿透轴,锐 转为「✅」(`PierceBuff 20`)。
> 锐 的配方 `钅 + 兑` 直接取 IDS 一级拆解,新部件 兑 随之入表(金,八卦之兑属金;
> 它同时是 锐 的唯一新部件,获取路径见下)。金系 ✅ 29 → 30、⚠ 3 → 2,全表 133 → 134。

**金系仍受阻最重(2/32)** —— 模型缺口(钩)、配方缺口(戟);其余四系各剩 0~1 字。

### 7.4 ⚠ 建议暂缓入池的 4 字(4 组)

> 2026-08-12:标题计数四订正——E-b4 T5 落地后 锐 有了真实机制(本场穿透 +20),
> 从名单移除。下表实际 3 组、4 字(溺凝、锯、钩),自数为准。

> 2026-08-12:标题计数三订正——E-b2 落地后 锋 有了真实机制(本场暴击率),
> 从名单移除。下表实际 4 组、5 字(溺凝、锐、锯、钩),自数为准。

> 2026-08-12:标题计数再订正——E-b3-a 落地后 剡/战/戮/利 各有真实机制
> (本场攻击 / 战意层数 / 每回合 AP 上限),「降级后同质」的前提消除,从名单移除。
> 下表实际 4 组、6 字(溺凝、锋锐、锯、钩),自数为准。

> 2026-08-08:标题计数订正——`Blind`/`Silence`/`Reflect`/`MultiHit` 落地后连续两轮从名单
> 删字(先删镰戮剿削刮等驱散/斩杀撞车字,后删熣烟锁镜),标题的「19」没跟着往下改。
> 下表实际 5 组、10 字(溺凝、戮、剡锋锐战利、锯、钩),自数为准。

> 2026-08-06:`Dispel`/`Execute` 落地后,灭·削·刮(驱散)与 镰·剿(斩杀)已用
> 「清一条 / 刮干净」「单体 / 全体」区分开,撞车前提消除,从名单移除。
>
> 2026-08-05:`ArmorBreak`/`IgnoreArmor` 落地后,熔/溶/破/碎(破甲)与锥(穿甲)已用
> 承伤系数/无视减伤与同组其他字区分开,撞车前提消除,从名单移除。冻/冷现为**初始收集字符**,
> 已在局内使用,同样不适用「暂缓入池」,一并移除。
>
> 2026-08-07:`Blind`/`Silence`/`Reflect` 落地后,熣·烟(致盲)与 锁·镜(沉默/反弹)已用
> 真实机制与同组其他字区分开,撞车前提消除,从名单移除。
>
> 2026-08-07:`钩` 与名单里其余「降级后同质」的字性质不同 —— 它不是数值撞车而是**模型缺口**
> (引擎里敌人是一个平表,「前排」只对召唤物存在)。补数值放不进来,要动战斗模型才行。

| 系 | 字 | 降级后 | 与谁撞车 |
|---|---|---|---|
| 水 | 溺 凝 | 单攻 13/9 | 与 沸 淬 无区别 |
| 金 | **锯** | **无降级方案** | 流血不可用灼烧替代 |
| 金 | 钩 | 无降级方案 | 模型缺口:敌人无排位概念,「拉到前排」无从实现 |

### 7.5 落地顺序

0. **先改灼烧生克** —— 修既有实现与规格的偏差(现在灼烧完全不吃属性),影响 18 字。
1. **`Bleed` 与 `HealAll` / `HealOverTime`** —— 各只需一个 Kind,但**都无降级方案**。
2. **P0 控制族** —— 解锁水系控制轴 7 字 + 土系 2 字,收字最多。
3. **P0 减伤 + P1 破甲** —— 前者给土系第二种防御资源,后者只改一个 float 收 6 字。
4. **P1 三族(驱散/免伤/斩杀)** 各只需一个布尔或数值字段。
   —— 2026-08-06 落地。驱散/净化/免疫/复活做成四个新 `EffectKind`(而非修饰字段:
   灭·浴·塞·岿·杜·活 六字没有伤害效果可挂),斩杀是 `EffectDef` 的两个修饰字段。
   为了让净化与免疫有靶子,顺带补上了引擎从未有过的**敌方施加给玩家的减益**:
   Boss 倾覆的 AP 惩罚状态化为 `Seal`(零新内容,顺带修掉它从不进存档的老 bug),
   新字怪「灯花」带 `Sear` 每次攻击给玩家挂 1 层灼烧(复用既有 `StatusKind.Burn`)。
   岿 的「免疫位移与控制」改为「免疫 1 次伤害 + 立即净化」—— 位移机制不存在,
   冻结/减速是玩家打敌人的方向,原口径没有靶子。
5. **P2 召唤物被动**做成 `EffectDef` 扩展字段而非新 `Kind`,与 `SummonCount` 同构。
   —— 2026-08-06 落地为 `SummonPassive` 值对象;详表原列的 `SummonAura`×2 与
   `SummonOnHitBurn` 合并成同一个字段对(攻 0 的召唤物照常出手,输出全靠 `OnHitBurn`),
   `SummonTaunt` 无需机制(高血低攻即天然肉盾),`SummonHaste` 改用速度 150
   (「当回合即可反击」本就是引擎默认行为)。
6. **P3 伤害变体** —— 2026-08-07 落地。致盲/沉默/反弹做成三个新 `EffectKind` + 三个新
   `StatusKind`,闪避是 `SummonPassive` 加一个字段,多段是 `EffectDef.HitCount`。
   为此给引擎补上了**命中判定**:插在 `DamagePlayerDirect` 与 `DamageSummon` 最开头,
   只判敌人打过来的那一侧(玩家出字必中)。**命中率满格时短路不摇随机数** ——
   `_random` 的唯一既有消费方是回合掉字,无条件摇会平移掉落序列。
   `钩` 因模型缺口(敌人无排位概念)移出字表,见 §7.4。
7. **火系 DOT 三分化** —— 2026-08-09 落地。炑 / 燥 / 灱 按「延长 / 加速 / 兑现」三个方向分化:
   `BurnNoDecay`(层数不衰减)、`BurnSettleNow`(玩家回合内立即兑现一次)、`Detonate`
   (按 N(N+1)/2 全额兑现剩余层数并清空)。为此把 `EndTurn` 里内联的灼烧结算抽成
   `SettleBurnOn(int)`,回合末与立即结算共用一份实现。
   原案的 `BurnRefresh` 被 `BurnNoDecay` 取代;`HealReduction` 因引擎里唯一会回血的敌人
   只有缺笔妖(且第 3 次补全是直接回满)而废止。

---

## 八 · 现在就能落地的 134 字

不需要引擎改动,可直接写进 `Brushblade/Assets/StreamingAssets/config/chars.json`。

> 2026-08-08:标题计数订正,口径与 7.3 同步——现在「✅ 可直接实现」总量是 129(见 7.3),
> 不再是设计初期的 71。下方**清单仍是最初那批 71 字的建议名单**,未再改动;另外 58 字
> (P0~P3 六波陆续落地的控制/减伤/破甲/驱散/免疫/斩杀/召唤物被动/净化复活/伤害变体等)
> 已有真实实现,规格分散在本文档第二~六节的逐字表格里,不在此处重复列出。
>
> 2026-08-09:标题计数再订正,口径与 7.3 同步——现在「✅ 可直接实现」总量是 132(见 7.3)。
> 炑/燥/灱三字随本轮落地一并计入,规格在第二节「攻 · DOT(层数平值)」表,同样不在此处
> 重复列出。
>
> 2026-08-10:标题计数三订正,口径与 7.3 同步——炼/杨/戟/塌 因配方缺口移出,总量 132 → 128。
>
> 2026-08-12:标题计数四订正,口径与 7.3 同步——E-b3-a 落地 剡/战/戮/利,总量 128 → 132。
> 规格在第五节「不沾四类(BUFF)」与「攻 · 直接伤害」两表,同样不在此处重复列出。
>
> 2026-08-12:标题计数五订正,口径与 7.3 同步——E-b2 落地 锋(132 → 133)、
> E-b4 T5 落地 锐(133 → 134),BUFF 组至此全员可入池。E-b2 那次漏改本标题,一并补上。

⚠ **2026-09-02:相生 ×3 已取消**,以下这条「填基础值,引擎自动 ×3」的规则与下方 焚 的
JSON 示例（`"value": 70`）**已作废** —— 现在一律直接填最终值,焚 的真实配置见第二节
（`DamageAll 120`)。本段其余内容(134 字清单)是本节早期规划的历史记录,不再更新。

```json
{
  "id": "焚",
  "element": "Fire",
  "pinyin": "fén",
  "gloss": "树林燃起大火",
  "recipe": ["火", "林"],
  "rarity": "Orange",
  "effects": [
    { "kind": "DamageAll", "value": 70 },
    { "kind": "BurnAll",   "value": 3 }
  ]
}
```

**清单**(`*` = 含相生,必须填换算后的基础值):

- **火(22)**:燚 燊\* 焚\* 焱 炎 爆 燎 炸 烈 灿\* 烬 焰 燃 蒸\* 烫 烧 炼 焦 灼 炽 热 煤
- **木(11)**:𣛧 森 林 柘 柏 松 杨 桑 梅 棒 棍
- **水(12)**:㵘 溎\* 淼 沝 沏\* 海 洪 涛 淹 淬 沸 治
- **金(11)**:𨰻 鑫 刲\* 锬 鍂 锹 劈 戟 剑 割 剖
- **土(15)**:㙓 嶘\* 垚 圭 塔 城 碉 垒 堡 墙 壁 崩 塌 砸 碾

其中 **15 字已在 `chars.json` 里跑着**(燚 焱 炎 𣛧 森 林 㵘 淼 沝 𨰻 鑫 鍂 㙓 垚 圭)
—— 但按 1.4 新锚点,**这 15 字的数值全部要改**,不只是改 `rarity`。**50 字是新增**。

> ⚠ 落地前先做:**1.2** 两处(`CardRarity` 加 `Gold`、`ApCostFor` 改常量 1)、
> **1.3** 五处(召唤上限 6、敌人上限 6、回合掉落改随机字、战利品 5 选 2、复活 5 选 2 ×2),
> 并就 **1.4 的玩家 HP 封顶问题**给出方向(A 解除封顶 / B 玩家数值随深度缩放 / C 接受靠盾生存)。
