# 字表平衡重做 · 设计稿(2026-09-05)

> 状态:**待实现**。本稿定的是**规则**,§6 那张 60 字表是规则的产物 ——
> 改规则要回到 §1~§4,不要直接手改 §6 的数字。
> 表由 `tools/design/rebalance_2026_09_05.py` 从锚点 + 价目表推出,改完重跑。

## 0 · 为什么要做

2026-09-05 读表查出六处硬伤:

1. **终极技配错面**:`溃` / `㵘` 的 `SpendWellspring` 与 `HealSelf` **写在同一个数组(护面)**,
   `EffectsOf()` 一次取整个数组顺序执行 —— 清空泉后立刻回满。土系两张(`崩`/`㙓`)把
   `SpendHeft` 放在攻面、护盾放护面,攒放二选一,是对的;水系配反了。
2. **充电太快**:阈值 `MaxHp/10`(1 级 = 50),而 `㵘` 一张治 540 = 10.8 层,一张顶满,
   「攒」这个过程根本不存在。
3. **放电太狠**:满 10 层 `㵘` 打 800 全体,红档全体锚点 300。
4. **锚点断层**:`鍂` 金档单攻 180,锚点 400;它比紫档 `剁`(140)只高 40,而橙档 `鑫` 是 430。
5. **攻击性下限被破**:`锐` / `锋` 伤害 0,而 §1.4.1 明写「纯功能牌自此不存在」。
   (本稿处置:`锐` 移出字表,`锋` 改绿档限时暴击并补足伤害。)
6. **系特色缺口**:木系 4 张召唤字无被动;火系「引爆」只有 `炸` 一张;土系召唤字拿不到厚;
   `Empower` / `DefenseBuff` 两个 `EffectKind` 字表 0 载体。

外加一条口径纠正:**目标形状(贯穿 / 横扫 / 溅射 / 连发 / 弹射)算特性技能,不算攻击模式**
(2026-09-05 用户裁定)。形状因此必须计价,见 §2.3。

## 1 · 定标口径

§1.4 的锚点表**不动**(2026-08-13 T8 实测校准过)。变的是「一张字能拿几条特性、每条花多少预算」。

### 1.1 特性配额

| 档 | ⚪白 | 🟢绿 | 🔵蓝 | 🟣紫 | 🟡金 | 🟠橙 | 🔴红 |
|---|---|---|---|---|---|---|---|
| 本体特性条数 | 0~1 | 1 | 1~2 | 2 | 2 | 2~3 | 3 |

主数值 = 档位锚点 ×(1 − 特性价格合计)。**攻击性下限 35% 保留**,纯功能牌不存在。
**预算上限 60%** —— 超过就得砍特性,不许靠压主数值硬塞。

### 1.2 系印记:免配额、免计价,但只免最低单位

| 系 | 印记 | 载体 | 免费额度 | 超出部分 |
|---|---|---|---|---|
| 🟡金 | 战意 +1 层 | 所有攻击字 | 1 层 | 第 2 层起 0.10/层 |
| 🟢木 | 召唤物被动 | 所有召唤字 | 第 1 条**免计价**(仍占配额) | 第 2 条起按价目表 |
| 🔴火 | 灼烧 ≥1 层 | 所有攻击字 | 1 层(当量 20) | 超出按 §1.4.2 当量扣主数值 |
| 🔵水 | 治疗即攒泉 | 引擎自动 | 全免,不占字面 | — |
| 🟤土 | 护盾即攒厚 | 引擎自动 | 全免,不占字面 | — |

木系的印记只免计价、不免配额 —— 召唤被动的表达空间有限,紫档「2 条本体 + 1 条印记 = 3 条被动」
读不过来。

**语义例外两张**:`灭`(熄灭,点火与字义相反)、`炸`(引爆是收方,自带点火会破坏「爆铺炸收」的配合)。

### 1.3 双方向字(水 / 土)两面的口径

| 面 | 口径 |
|---|---|
| 攻面 | 该档 单攻 / 全体 锚点 ×(1 − 价格合计) |
| 护面 · 护盾 | 该档护盾锚点 × **0.65** ×(1 − 价格合计) |
| 护面 · 治疗 | 该档治疗锚点 × **1.00** ×(1 − 价格合计) |
| 群体版(`HealAll` / `ShieldAll`) | 上述基础上再 ×0.6 |

**护盾 0.65 而治疗 1.00 的不对称是刻意的**:护盾是可累积资源(`_shieldNormal += shield`,
无上限无衰减、跨战斗延续),需要压制;治疗被 `MaxHp` 当场封顶,自限。

**治疗的两条硬判据**(为第 7 章「治疗溢出转伤害」的法宝预留):
1. 治疗锚点 ≤ 同档单攻锚点 × 0.7 —— §1.4 现行值是 0.58~0.67,已满足;
2. 法宝的溢出转换率上限 50%。

### 1.4 中层刮痧的解法:护盾战斗结束衰减

现行护盾无上限、无衰减、跨战斗累积。玩家一旦把每回合 2~3 张牌投给护盾,盾的增长追平敌人输出,
而且**只要有一个回合转正就永久留在身上** —— 战斗不会输也打不死怪,变成刮痧。压单张数值只能
把转正点往后挪几层,挪不掉这个性质(已经砍过两次,没解决)。

**新规:每场战斗结束时,`_shieldNormal` / `_shieldPersist` 各减半**(整数除,向下取整)。
`_shieldAccum` / `_healAccum` 两个攒层余数不动 —— 它们不是护盾。

⚠ 已知残留:战斗结束衰减管的是**跨场累积**,单场内的净增长它碰不到。选它而不选回合末衰减
是用户 2026-09-05 的裁定(理由:回合末衰减让护盾读起来像没加)。故护盾数值维持 ×0.65,
不上调 —— 上调会让单场内的转正点比现在更早。

### 1.5 双方向字:效果放在哪一面 —— 归属规则

现行字表**没有这条规则**,于是每张字各配各的,已经出了三个 bug:

| 字 | 现状 | 问题 |
|---|---|---|
| `溃` / `㵘` | `SpendWellspring` 与 `HealSelf` 都在护面 | 清空泉后立刻回满(§0 第 1 条) |
| `壁` | 攻面与护面**都挂** `Reflect 30` | 「攻/护二选一」白设 —— 攻面附带反伤且多一次伤害,严格优于护面,护面永远没人选 |
| `壁` 攻面 | `Reflect 30` **没有 `turns`** | `TurnsLeft = 0` 会被 `TickTurns` 当场清掉 —— 卡面印着,实际大概率不生效 |

规则:

| 效果性质 | 放哪一面 | 例 |
|---|---|---|
| 打在**敌人**身上 | **攻面** | 伤害、破甲、封禁、冻结/减速、流血、灼烧、终极技放电 |
| 挂在**自己**身上 | **护面** | 护盾、治疗、反伤、免疫、护甲、复活 |
| 两面都挂 | **不允许** | 会让二选一失效 |

⚠ 带 `turns` 的效果**必须显式填 turns** —— 缺省 0 等于当场失效。建议在 `extract_values.py`
加一条校验:`Reflect` / `Blind` / `Silence`(封禁)/ `HealOverTime` 缺 `turns` 直接报错,
别让它再静默漏一次。

### 1.6 反伤的总量钳位

反伤做两级(`壁` 绿 30% / `圭` 金 50%)就踩上引擎里那条已有的警告:

```
BattleEngine.cs:3335  // 刻意不钳位:眼下字表只有「映」一个 Reflect 字……
                      // 日后加第二张反弹字之前,先想清楚上限 —— 两张 60% 同在身会反弹 120%,比挨的还多。
```

**新规:反伤总量上限 60%。** 60 不是拍的 —— 30 层一轮敌方总伤 936,`936 × 60% = 562`,
恰好约等于红档单攻锚点 600。也就是「站着挨满一整轮」的反伤收益 ≈ 一张红档输出字,
定价自洽;再高就会让站桩流的输出超过专职输出系。

**2026-09-06 补充:召唤物的荆棘(`Thorns`,`荆`)一并纳入同一份 60%。** 首版钳位只钳了
`Reflect`(壁/圭),漏了召唤物的荆棘 —— `DamageSummon` 里荆棘与反弹是两次各自独立的弹射,
「玩家壁(30%)+ 召唤物壁(50%,钳到 60)+ 荆棘 50%」这条打召唤物的管道当时仍能反弹
> 100%,本节开头那条引擎警告描述的危险状态没被完全消灭。

分配规则:**荆棘先扣满,反弹拿剩余**——

```
thornsEffective  = min(60, thorns)
reflectBudget    = 60 - thornsEffective
reflectEffective = min(reflectBudget, 玩家 Reflect + 召唤物 Reflect)
```

不按比例缩放,而是「本体机制优先,外挂 buff 拿剩余」:`荆` 这类字的设计定位就是「攻 0,
反伤是它唯一的输出手段」,按比例缩放会让后挂的 `壁`/`圭` 把荆的本体机制挤掉一部分 ——
一张字的固有能力不该被另一张字的 buff 稀释;反过来(反伤先扣)又会让荆在有 buff 时几乎
打不出东西。另外两次弹射本来就是分开结算的,顺序分配比按比例缩放算出来的零碎数字更好
解释,实现也更直白。

## 2 · 价目表

### 2.1 档位系数 K

§1.4.1 的价格是「占该档主预算的比例」,当初只服务 1~2 条特性的旧字表。配额提到 3 条后,
红档 3 条 ×0.35 就吃掉 105% 预算 —— 模型本身破了。

依据:**副效果的绝对价值不随稀有度线性增长**。「冻结 1 回合」在白档和红档都只是跳过一回合,
而红档主预算是白档的 10 倍;灼烧尤甚 —— `_burnPerStack` 是常量 20 且**不吃攻击力**,
深层相对价值断崖下跌。

| 档 | 白 | 绿 | 蓝 | 紫 | 金 | 橙 | 红 |
|---|---|---|---|---|---|---|---|
| K | 1.00 | 1.00 | 0.90 | 0.80 | 0.65 | 0.55 | 0.45 |

实付价格 = 价目表价 × K。DOT 当量同样乘 K。

### 2.2 §1.4.1 原表(不变)

冻结 1 回合 0.35 / 减速 2 回合 0.30 / 致盲 0.30 / 反弹 50%×2 0.35 / **破甲 0.33** /
免疫 1 次 0.35 · 2 次 0.60 / 复活 1 名 0.40 / 斩杀(加倍 0.15 · 处决 0.20)/
**破甲 2026-09-08 起限时 3 回合、降价 0.33 → 0.25**(持久 × 0.75,见 §11.2)/
灼烧威力 +10 0.30 · +20 0.45 / 暴击 +20% 0.40 / 攻击 +50 0.45 / AP 上限 +1 0.50 /
战意(印记外)0.10 每层

**本次休眠清单**(0 载体,留在引擎里待复活,处置同 `BurnNoDecay` 那批):

| 机制 | 为什么没了 |
|---|---|
| 穿透 `PierceBuff` / `EffectDef.Pierce` | 整体并入破甲(改为给目标上 debuff) |
| 净化 `Cleanse` | 并入封禁;且用户裁定不保留解控手段 |
| 驱散 `Dispel` | 并入封禁 |
| AP 上限 `ApBoost` | 唯一载体 `利` 改成限时增攻 |
| 连发形状 `Volley` | 唯一载体 `锥` 不再是召唤字 |
| 随行治疗 `healAlly` | 唯一载体 `桤` 移出 |
| 本场持久的 `Empower` +50 / `CritBuff` +20% | 两个载体都改成**限时**版(利 2 回合 / 锋 3 回合),持久变体 0 载体 |
| 召唤被动 `onHitMorale` | **本稿新增又立即作废** —— `锥` / `剑` 双双改成攻击字后,金系已无召唤字,这条印记补丁失去对象 |

### 2.3 本次新增

| 项 | 价格 | 依据 |
|---|---|---|
| 溅射(相邻 50%) | 0.20 | 期望多命中 ~1 个目标的半伤 |
| 贯穿(同列前后排 70%) | 0.20 | 同上,能越排 |
| 横扫(整排最多 3,50%) | 0.30 | 期望多命中 2 个的半伤 |
| 连发 N 发 | 0.15 ×(N−1) | |
| 弹射 N 跳(每跳 ×50%) | 0.25 | 3 跳总伤 1.75 倍 |
| **终极技**(`SpendHeft` / `SpendWellspring`) | **0.00** | 2026-09-08 由 0.50 归零,见 §11.1 —— 载体只有蓝、红各一张,红卡太难获取,发挥厚/泉基本必须靠那两张蓝卡;它是补强机制,不该自己付补强的钱 |
| 偷袭 | 0.25 | |
| 分 2 段 | 0.20 | |
| 条件翻倍(对灼烧 / 流血 / 控制 / 破甲) | 0.25 | |
| 免一次清盾 | 0.20 | |
| **封禁 1 回合 / 2 回合** | **0.38 / 0.52** | 取代净化 + 驱散两条轴。**对 Boss 降级**为「护甲减半 N 回合」,故略低于全效价 0.40 |
| 流血 | 0.25 | |
| 护甲(`DefenseBuff`,**限时 4 回合**) | **0.28** | 2026-09-08 限时化后由 0.33 降价(持久 × 0.85),见 §11.2 |
| 召唤被动:护甲(`SummonDefense`,不限时) | **0.25** | 2026-09-08 新增(塔)。低于玩家侧:只护这一只召唤物 |
| 召唤被动:嘲讽 / 荆棘 / 光环攻 | 0.30 / 0.30 / 0.35 | |
| 召唤被动:光环盾 | **0.20** | 2026-09-08 由 0.35 降价,见 §10.3(引擎实现是一次性护盾,不是持续光环);与「免一次清盾 0.20」同量级 |
| 召唤被动:随行治疗 / 命中挂灼烧 / 自愈 / 闪避 / 远程 / 入场护盾 | 0.30 / 0.25 / 0.25 / 0.25 / 0.25 / 0.25 | |
| 召唤被动:迅捷 / 命中冻结 / 叠战意 | 0.20 / 0.20 / 0.20 | |

### 2.4 终极技的放电当量

`value` = 同档全体锚点 ÷ 5 → **满 10 层 = 该档全体锚点 ×2**(蓝 140 / 金 400 / 红 600)。
攒满要三张以上同系高档字、跨几层楼,Boss 层倒出来。

## 3 · 引擎与管线改动

| # | 位置 | 改动 |
|---|---|---|
| 1 | `BattleEngine.ResourceThreshold` | `MaxHp/10` → `MaxHp/5` |
| 2 | `BattleEngine.WellspringPercentPerStack` | `10` → `5`(与厚对齐) |
| 3 | `RunEngine` 携带态 | 战斗结束 `_carriedNormalShield` / `_carriedPersistShield` 各减半 |
| 5 | `SummonPassive` | 新字段 `auraAttack`(给场上**全部**召唤物 +N 攻,**含自己**) |
| 6 | `SummonState.EffectiveAttack` | 接入玩家战意 + 厚的百分比乘区,由 `BattleEngine` 注入 `PlayerAttackPercent` |
| 7 | 《技能机制详表》 | `SpendWellspring` 从护面移到攻面(溃 / 㵘) |
| 8 | 字表 | **移出 4 字**:`浴`(复活载体先由 `活` 顶替)、`葬`(无字依赖,配方 艹+死)、`锐`、`桤`、以及 `活` 本身 —— 2026-09-05 用户两批裁定叠加的净结果是 **复活载体只剩 `沐`(金)一张**。**新增 1 字**:`花`(木绿,魅惑)。净 60 → 57 字 |
| 9 | `CardTraits.cs` | 形状从 `Modes()` 移到 `Of()`;补 `onHitMorale` / `auraAttack` 两条分支 |
| 11 | `BattleEngine` + `EffectDef` | **封禁**:复用 0 载体的 `EffectKind.Silence`,语义从「主动机制哑火」扩到「特殊能力全部失效」—— `EffectiveEnemyDefense` 遇封禁归零、召唤/敌人被动不触发、Boss 大招哑火。玩家可见名定为**「封禁」**。**对 Boss 降级**:只削「护甲减半,持续 N 回合」,被动与大招不受影响 —— 与斩杀对 Boss 从「直杀」降为「吃双倍」是同一条设计纪律。需一条 `IsBoss` 分支 + 一条降级事件(不发事件玩家会当成 bug),卡面文案两句都要写(参考 `char.effect.execute.kill` 的写法) |
| 14 | `BattleEngine` + `EffectDef` | **魅惑**(新 `EffectKind.Charm`):目标本回合攻击**自己阵营**。要动敌人行动分支的目标选择,并处理「魅惑致死自己人」的事件与掉落归属 |
| 15 | `EffectDef` + `BattleEngine` | **限时增益**:`Empower` / `CritBuff` 现在是本场持久,要支持 `Turns`(利 2 回合、锋 3 回合) |
| 16 | `MetaRules.ScaleByCardLevel` | **卡等级加回合数**:现在只缩放 `Value`,要新增一条 turns 缩放(利 / 锋 的「升级可增加回合数」) |
| 17 | 字表 + 管线 + 字体 | **新字 `花` 入表**(艹 + 化,木绿):详表条目 + 配方 + **字体子集必跑**(新字不在子集里会在真机上显示成豆腐块,离线编译与 Core 单测都发现不了) |
| 18 | `BattleEngine.DamagePlayerDirect` | **反伤总量钳位 60%**(§1.6)—— 两级反伤同在身会反弹 100%,引擎注释早已警告 |
| 19 | 双方向字全表 | 按 §1.5 的归属规则重排:打敌人的进攻面、挂自己的进护面、不许两面都挂。顺带修 `壁` 的双挂 + `turns` 丢失、`溃` / `㵘` 的终极技配错面 |
| 20 | `extract_values.py` | 带 `turns` 的效果缺 `turns` 直接报错(`Reflect` / `Blind` / 封禁 / `HealOverTime`)—— `壁` 攻面那条漏了 turns,静默失效至今 |
| 13 | 字表 | **不保留净化**(2026-09-05 用户裁定)—— 玩家从此没有解控手段,冻结 / 减速 / 流血只能挨过去。这是刻意的:控制在这套设计里是真威胁,不给对冲 |
| 12 | 字表 | 穿透并入破甲(`PierceBuff` / `Pierce` 休眠);净化 + 驱散并入封禁(`Cleanse` / `Dispel` 休眠) |
| 10 | `gen_char_doc.py` | 特性列收进形状;补新被动;口径说明改写 |

⚠ **第 6 项的陷阱**:`ScaleByBaseAttack`(护盾 / 治疗)**绝不能**跟着改成读 `EffectiveAttack` ——
那条注释里写明了会造出「堆盾涨厚 → 厚放大护盾 → 涨更多厚」的正反馈环。召唤物攻击接厚是安全的
(召唤物攻击不产生护盾),护盾 / 治疗接厚不安全。

⚠ 第 6 项**推翻 2026-08-28 的拍板**(`EnemyDef.cs:214` 注释:「战意是玩家专属……用户明确留在玩家侧」)。
本次用户重新裁定:召唤物攻击跟随战意与厚变动(基础 100 → 战意 1 层 110 → 5 层 150 → 归零回 100)。
满 buff(战意 5 层 +50% + 厚 10 层 +50%)= ×2,故 §6 表里召唤物基础攻按 **§1.4 锚点 × 0.70** 重定标。

## 4 · 梯队

### 4.1 核心特性 · 三级(全部用现有 60 字换载体,不召回)

| 系 | 核心特性 | 低 | 中 | 高 |
|---|---|---|---|---|
| 🟡金 | 斩杀 | 剿(蓝,残血加伤) | 铡(紫,直杀) | 刲(橙,直杀 + 偷袭 + 分段) |
| 🟡金 | **横扫(只两级)** | 剑(蓝) | — | 鑫(橙) |
| 🟤土 | **反伤(两级)** | 壁(绿,30%) | — | 圭(金,50%) | 总量钳位 60%(§1.6) |
| 🟡金🟤土 | **破甲(跨系四级)** | 锥(蓝,削 20)· 碎(土蓝,削 20) | 鍂(金,削 30) | 垚(土橙,削 35)· 𨰻(红,削 40) |
| 🟢木 | 单只体量(**判据从只数改为体量**) | 枪(40 血)· 藤(60 血) | 楸 / 荆(176~430 血) | 柘(980)· 森(587)· 藻(694)· 𣛧(2 只 ×465) |
| 🟢木🟤土 | 嘲讽肉盾(跨系四级) | 碉(土白,100 血)· 堡(土蓝,200 血) | 荆(紫,430 血) | 柘(金,980 血)· 桂(土橙,790 血) |
| 🔵水 | 涌泉相报(**只两级**) | 溃(蓝,14/层) | — | 㵘(红,60/层) |
| 🔵水 | 控制 | 冷(白,减速 1) | 冻(绿,减速 2)· 冰(金,冻结 1) | 淼(橙,冻结 2)· 㵘(红,冻结 2) |
| 🔴火 | 引爆 | 炸(蓝,全体引爆) | 燥(紫,引爆) | 燚(红,全体引爆) |
| 🔴火 | 灼烧层数 | 热(绿,1) | 烈 / 燥 / 蒸(紫,2)· 炎(金,3) | 焚(橙,4)· 燚(红,5) |
| 🟤土 | 厚积薄发(**只两级**) | 崩(蓝,14/层) | — | 㙓(红,60/层) |
| 🟤土 | 护甲(点数) | 垒(绿,+4) | 杜(金,+9) | 垚(橙,+11)· 㙓(红,+13) |

土系那条护甲链是关键补丁:`DefenseBuff` 此前字表 0 载体,而护甲是**点数减伤、不累积、不滚雪球**
的防御轴 —— 让玩家能变硬但硬度有天花板,正是「中层刮痧」的正解。土系从此两条腿:
护盾(可消耗缓冲,攒厚)+ 护甲(恒定减伤,不攒厚)。

### 4.2 边缘特性 · 单级,且必须辅助本系核心

| 系 | 边缘特性 | 载体 | 辅助谁 |
|---|---|---|---|
| 🟡金 | 限时增攻 / 限时暴击 | 利(白,+30 × 2 回合)· 锋(绿,+20% × 3 回合) | 抬高战意乘区之前的加点;**回合数随卡等级成长**,是养成侧唯一吃 turns 的两张字 |
| 🟡金 | 流血 | 剁(紫) | 给 铡 的「对流血翻倍」喂状态 |
| 🟢木 | 攻击光环 `auraAttack` | 𣛧(红) | 放大全场召唤物(含自己) |
| 🟢木 | **魅惑** | 花(绿,新字) | 把敌人的一次出手转成对敌方自己的伤害 —— 控制与输出两头都占,故 0.40 定价高于冻结 |
| 🟢木 | 自愈 | 藻(橙)· 林(金)· 森(橙) | 召唤物每回合给**自己**回血,拖住场面 |
| 🟤土 | 护盾光环 | 塔(紫)· 桂(橙) | 召唤物给全场加盾,兼作攒厚来源(`Shield` 加给召唤物同样走 `GainHeft`) |
| 🔵水🔴火 | **封禁** | 灭(火白)· 海(绿)· 澡(紫)· 湮(紫)· 沐(金)· 淋(橙,2 回合) | 关掉对手的护甲 / 被动 / 大招,给自己的治疗与终极技争取回合 |
| 🔵水 | 复活 | 沐(金) | 救召唤物,水木联动 |
| 🔴火 | 灼烧增威 | 蒸(紫)· 焱(橙) | 抬高引爆当量 |
| 🟤土 | 免疫 / 反伤 | 杜(金)· 壁(绿) | 拖时间给护盾攒厚 |

## 5 · 批次划分

| 批 | 内容 | 验证 |
|---|---|---|
| **P0** | §3 的 1~8(引擎 + 管线 + 浴→活),不动数值 | coretests 全绿;新增测试覆盖阈值 / 泉倍率 / 护盾衰减 / 两个新被动 / 召唤物吃 buff |
| **P1** | §3 的 9~10(`CardTraits` 形状迁移 + 文档生成器) | prescompile 通过;`pytest tools/design/` 全绿;人工看一遍卡面详情 |
| **P2** | §6 全表数值与特性落地(走《详表》→ `extract_values` → `export_chars`) | 管线 pytest;`chars.json` regenerable 测试;字体子集重跑;仿真读数 |

每批各自跑完 `pytest tools/pipeline tools/fonts tools/icons tools/design` + coretests + prescompile 再进下一批。

## 6 · 全表 57 字(规则的产物)

| 字 | 系 | 档 | 攻击 | 护盾 | 治疗 | 护甲 | 召唤 | 终极技/层 | 灼烧 | 特性技能 | 预算 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 利 | 金 | 白 | 45 | — | — | — | — | — | — | 叠战意(印) / 增攻30限时 | 0.25 |
| 锋 | 金 | 绿 | 63 | — | — | — | — | — | — | 叠战意(印) / 暴击20限时 | 0.30 |
| 锥 | 金 | 蓝 | 101 | — | — | — | — | — | — | 叠战意(印) / 破甲 | 0.23 |
| 剑 | 金 | 蓝 | 95 | — | — | — | — | — | — | 叠战意(印) / 横扫 | 0.27 |
| 剿 | 金 | 蓝 | 112 | — | — | — | — | — | — | 叠战意(印) / 残血加伤 | 0.14 |
| 铡 | 金 | 紫 | 128 | — | — | — | — | — | — | 叠战意(印) / 斩杀 / 对流血 | 0.36 |
| 剁 | 金 | 紫 | 128 | — | — | — | — | — | — | 叠战意(印) / 分2段 / 流血 | 0.36 |
| 鍂 | 金 | 金 | 283 | — | — | — | — | — | — | 叠战意(印) / 破甲 / 分2段 | 0.29 |
| 鑫 | 金 | 橙 | 348 | — | — | — | — | — | — | 叠战意(印) / 横扫 / 战意+2 | 0.28 |
| 刲 | 金 | 橙 | 308 | — | — | — | — | — | — | 叠战意(印) / 斩杀 / 偷袭 / 分2段 | 0.36 |
| 𨰻 | 金 | 红 | 384 | — | — | — | — | — | — | 叠战意(印) / 破甲 / 战意+3 / 对破甲 | 0.36 |
| 枪 | 木 | 白 | — | — | — | — | 1 只 · 40 血 / 14 攻 | — | — | 贯穿 | 0.00 |
| 藤 | 木 | 绿 | — | — | — | — | 1 只 · 60 血 / 21 攻 | — | — | 命中冻结 | 0.00 |
| 花 | 木 | 绿 | 54 | — | — | — | — | — | — | 魅惑1 | 0.40 |
| 箭 | 木 | 蓝 | — | — | — | — | 1 只 · 80 血 / 28 攻 | — | — | 远程 | 0.00 |
| 楸 | 木 | 紫 | — | — | — | — | 1 只 · 176 血 / 39 攻 | — | — | 远程 / 命中挂灼烧 | 0.20 |
| 荆 | 木 | 紫 | — | — | — | — | 1 只 · 430 血 / 0 攻 | — | — | 荆棘 / 嘲讽 | 0.24 |
| 林 | 木 | 金 | — | — | — | — | 1 只 · 368 血 / 106 攻 | — | — | 迅捷 / 自愈 | 0.16 |
| 柘 | 木 | 金 | — | — | — | — | 1 只 · 980 血 / 0 攻 | — | — | 嘲讽 / 荆棘 | 0.20 |
| 森 | 木 | 橙 | — | — | — | — | 1 只 · 587 血 / 158 攻 | — | — | 荆棘 / 迅捷 / 自愈 | 0.25 |
| 藻 | 木 | 橙 | — | — | — | — | 1 只 · 694 血 / 187 攻 | — | — | 自愈 / 迅捷 | 0.11 |
| 𣛧 | 木 | 红 | — | — | — | — | 2 只 · 465 血 / 130 攻 | — | — | 光环攻 / 迅捷 / 荆棘 | 0.23 |
| 冷 | 水 | 白 | 49 | — | 33 | — | — | — | — | 减速1 | 0.18 |
| 冻 | 水 | 绿 | 63 | — | 42 | — | — | — | — | 减速2 | 0.30 |
| 海 | 水 | 蓝 | 101 | — | 62 | — | — | — | — | 弹射3 | 0.23 |
| 溃 | 水 | 蓝 | 70 | — | 48 | — | — | 14 | — | 终极技 | 0.00 |
| 澡 | 水 | 紫 | 99 | — | 60 | — | — | — | — | 封禁 / 弹射3 | 0.50 |
| 湮 | 水 | 紫 | 99 | — | 60 | — | — | — | — | 封禁 / 对控制 | 0.50 |
| 冰 | 水 | 金 | 244 | — | 146 | — | — | — | — | 冻结1 / 对控制 | 0.39 |
| 沐 | 水 | 金 | 197 | — | 39×3 | — | — | — | — | 封禁 / 复活1 | 0.51 |
| 淼 | 水 | 橙 | 269 | — | 157 | — | — | — | — | 冻结2 / 对控制 | 0.44 |
| 淋 | 水 | 橙 | 132 | — | 92 | — | — | — | — | 封禁2 / 减速2 | 0.45 |
| 㵘 | 水 | 红 | 192 | — | 134 | — | — | 60 | — | 终极技 / 冻结2 / 对控制 | 0.36 |
| 灭 | 火 | 白 | 19 | — | — | — | — | — | — | 封禁 | 0.38 |
| 热 | 火 | 绿 | 90 | — | — | — | — | — | 灼烧 1 | 灼烧1 | 0.00 |
| 爆 | 火 | 蓝 | 34 | — | — | — | — | — | 全体灼烧 2 | 全体灼烧2 | 0.00+D36 |
| 炸 | 火 | 蓝 | 45 | — | — | — | — | — | — | 全体引爆 | 0.36 |
| 烈 | 火 | 紫 | 48 | — | — | — | — | — | 全体灼烧 2 | 全体灼烧2 / 对灼烧 | 0.20+D32 |
| 燥 | 火 | 紫 | 120 | — | — | — | — | — | 灼烧 2 | 灼烧2 / 引爆 | 0.24+D32 |
| 蒸 | 火 | 紫 | 120 | — | — | — | — | — | 灼烧 2 | 灼烧2 / 灼烧增威10 | 0.24+D32 |
| 炎 | 火 | 金 | 270 | — | — | — | — | — | 灼烧 3 | 灼烧3 / 对灼烧 | 0.16+D65 |
| 灿 | 火 | 金 | 309 | — | — | — | — | — | 灼烧 2 | 灼烧2 / 偷袭 | 0.16+D26 |
| 焚 | 火 | 橙 | 108 | — | — | — | — | — | 全体灼烧 4 | 全体灼烧4 / 对灼烧 | 0.14+D99 |
| 焱 | 火 | 橙 | 126 | — | — | — | — | — | 全体灼烧 3 | 全体灼烧3 / 灼烧增威20 | 0.25+D55 |
| 燚 | 火 | 红 | 86 | — | — | — | — | — | 全体灼烧 5 | 全体灼烧5 / 全体引爆 / 对灼烧 | 0.29+D126 |
| 碉 | 土 | 白 | — | — | — | — | 1 只 · 100 血 / 0 攻 · 盾 20 | — | — | 入场护盾(印) / 荆棘 | 0.30 |
| 垒 | 土 | 绿 | 65 | 33 | — | 4 | — | — | — | 护甲 | 0.28 |
| 壁 | 土 | 绿 | 70 | 35 | — | — | — | — | — | 反伤30 | 0.22 |
| 堡 | 土 | 蓝 | — | — | — | — | 1 只 · 200 血 / 0 攻 · 盾 20 | — | — | 入场护盾(印) / 荆棘 / 嘲讽 | 0.54 |
| 崩 | 土 | 蓝 | 70 | 39 | — | — | — | 14 | — | 终极技 | 0.00 |
| 碎 | 土 | 蓝 | 101 | 50 | — | — | — | — | — | 破甲 | 0.23 |
| 塔 | 土 | 紫 | — | — | — | 7 | 1 只 · 153 血 / 21 攻 · 盾 20 | — | — | 入场护盾(印) / 荆棘 / 护甲(召) | 0.44 |
| 圭 | 土 | 金 | 244 | 119 | — | — | — | — | — | 反伤50 / 对破甲 | 0.39 |
| 杜 | 土 | 金 | 236 | 115 | — | 9 | — | — | — | 免疫1 / 护甲 | 0.41 |
| 桂 | 土 | 橙 | — | — | — | — | 1 只 · 892 血 / 76 攻 · 盾 150 | — | — | 入场护盾(印) / 光环盾 / 荆棘 | 0.28 |
| 垚 | 土 | 橙 | 340 | 166 | — | 11 | — | — | — | 破甲 / 护甲 | 0.29 |
| 㙓 | 土 | 红 | 235 | 138 | — | 13 | — | 60 | — | 终极技 / 免一次清盾 / 护甲 | 0.22 |

共 57 字

## 6.0 攻击模式口径(2026-09-07 补,原为空白)

§6 的「攻击」列**只是数值,不表达单体 / 全体** —— 而两者的锚点不同(`A['全体']` < `A['单攻']`),
所以模式必须另有出处。规则就是 `rebalance_2026_09_05.py` 里 `dual_s` / `dual_h` 两个分支
已经在用的那条,现在把它写明:

| 形态 | 攻面落成 | 走哪个锚点 |
|---|---|---|
| `atk` | `DamageSingle` | 单攻 |
| `aoe` | `DamageAll` | 全体 |
| `dual_s` / `dual_h` **带**「群盾」/「群疗」 | `DamageAll` | 全体 ×0.60 |
| `dual_s` / `dual_h` **不带**群盾 / 群疗 | `DamageSingle` | 单攻 |

即:**双面字的攻面默认是单体,只有同时做群体护盾 / 群体治疗的字,攻面才一并变群体。**
群体那一档的数值已按 `GROUP_F = 0.60` 折过,所以「群疗 + 群攻」不是白拿。

⚠ 这条口径落地时会改三个字的攻击模式(P2 Task 4):

| 字 | 形态 | 当前 | 改为 | 为什么 |
|---|---|---|---|---|
| `㙓` | dual_s(无群盾) | `DamageAll` | `DamageSingle` | 拿单攻锚点 322 配成全体攻击 = 隐形超标 |
| `㵘` | dual_h(无群疗) | `DamageAll` | `DamageSingle` | 同上,249 |
| `淼` | dual_h(无群疗) | `DamageAll` | `DamageSingle` | 同上,269 |

`崩`(群盾)· `海` / `淋`(群疗)三个走全体锚点,当前配的也确实是群体 —— 口径本来就一致,不动。

这不是「顺手改玩法」:这三个字是拿**单体**的价钱配**全体**的打击面,正是本次重做要修的
那类不平衡。三者恰好都是高档字(红 / 红 / 橙),超标幅度最大的地方。

反向的三处**明文**改动另有出处,不受本条支配 —— `海`「攻面走全体」(§6 roster note)、
`剑` 横扫单排、`锥` 单体破甲(均见 §4 梯队与用户 2026-09-05 第二批裁定)。

## 6.1 召唤只数与光环(2026-09-05 定)

**只数**:**除 `𣛧`(2 只)外,全系召唤字一律 1 只**(2026-09-05 两步裁定,第二步收紧)。只数变了按**总量守恒**
折算单只血/攻 —— 沿用 2026-09-04「只数 2→1,血/攻 ×2」那次的口径,§1.4 的召唤锚点是
「只数 × 单只」的总预算,只数改了就把总量摊到新的只数上。

于是高档召唤物变成「少而巨大」:`柘` 1 只 980 血、`桂` 1 只 1061 血、`森` 1 只 587 血 / 158 攻。参照 30 层杂兵
546 血 / 一轮总伤 936 —— 980 血的 `柘` 恰好扛住一整轮,是肉盾该有的样子。

**光环含自己**:`auraAttack` 原本只加**其它**召唤物,只数收归 1 只后会**空转**
(场上没有别人)。故改为含自己 —— `𣛧` / `藻` 从此是「自带攻的巨型单体」,
与别的召唤字同场时光环照常外溢。价格维持 0.35(收益变化不大:多数局面本就只有它一只)。

⚠ **订正(2026-09-07,P2 Task 2 查实)**:本节初稿写的是「`auraAttack` / 光环盾……」,
把光环盾也算作既有机制 —— **那是错的**。引擎侧只有 `AuraAttack`
(`EnemyDef.cs` 的 `AuraAttackBonus` + `BattleEngine.RefreshSummonAura`,P0 为 `𣛧` 新加),
**`AuraShield` 从来不存在**,Core 全文零命中。初稿还把 `桂` 列为「自带盾」的例子,
同属这个错误前提。

于是 §6 里 `塔`(土紫)· `桂`(土橙)两个字的「光环盾」特性**在 P2 落不了地**。
P2 的裁定:**本批留空,标记 P3** —— P2 的批次定义是「数值落地」,补一条新引擎机制
是 P0 性质的工作;且光环盾属「特性技能」列,不在 P2 对账覆盖的七个数值列里,
留空不影响本批的自动化判据。两个字各自还有另一条特性(迅捷 / 荆棘),不会变成空字。

P3 的三条路(⚠ **已于 2026-09-08 拍板走下面的第 4 条,并把定价改到 0.20 —— 见 §10**。
本段与下一段保留为决策过程,数值一律以 §10 为准):
1. 补 `AuraShield` 引擎机制 —— 但「护盾是消耗性资源」与 `AuraAttack` 的「持续属性加成」
   模型不同:入场发一次、还是每回合回补、提供者死了已发的盾要不要收回,语义都要先定;
2. 改用已存在的召唤被动(`HealAlly` / `Dodge` / `Regen`)顶替 —— 会弱化土系「主护盾」的系特色;
3. 让这两个字只保留单条特性 —— 会动到 §2 的配额与预算(光环盾定价 0.35)。
4. ⭐ **用既有的 `EffectDef.SummonShield` 顶替(2026-09-07 追加,推荐)** —— 见下。

### 第 4 条:`SummonShield` 其实就够了(2026-09-07 追加)

进一步查实:引擎**早就有** `EffectDef.SummonShield`,语义是「**召唤当时**发一次性护盾」
(`BattleEngine.cs:2635`,吃卡等级)。**`桂` 本来就是用它实现的 —— 旧配置是
`SummonShield 60`**,当时有一条测试守着这个 60。

⚠ 那条测试在 P2 落地后已随断言反转而改名(`RealConfig_GuiGrantsSummonShield` →
`RealConfig_GuiCarriesThornsNotSummonShield`),现在断言的是 `SummonShield == 0` /
`SummonCount == 1` / `Passive.Thorns == 50` —— 即「桂 的一次性护盾**已被移除**」这个新事实。
所以第 4 条方案落地时,那条测试要再改回来。

而 §6.1 上半段说的「只数收归 1 只后,`桂` / `藻` 是自带盾 / 自带攻的巨型单体」——
**只数 = 1 时,`SummonShield` 就正好是那个效果**。所谓「光环盾」根本不需要新机制。

与 `AuraAttack` 的两处差别(取舍点):

| | `AuraAttack`(持续) | `SummonShield`(一次性) |
|---|---|---|
| 生效时机 | `EffectiveAttack` 每次出手现读 | 召唤落位时发一次 |
| 后来入场的召唤物 | 吃得到(会外溢) | 吃不到 |
| 被消耗后 | 不适用(属性加成) | 不恢复 |

对**只数 = 1** 的高档土系召唤字,这两处差别基本不体现 —— 场上就它一只,没有外溢对象;
「护盾被打掉不恢复」本来就是护盾该有的样子(护盾是消耗性资源,这也正是
2026-09-05 讨论「护盾不能变成第二条无上限血条」时的结论)。

⚠ **唯一缺的是数值来源。** `rebalance_2026_09_05.py` 里「光环盾」只作为 `PRICE` 条目
**扣预算**(0.35),不产出点数 —— 所以 `塔` / `桂` 各该发多少盾,现在没有推导规则。
两个可选口径(⚠ **2026-09-08 已拍第一条「按血量 15%」,并入 §10.3;脚本自此产出点数,
对账测试的召唤列也跟着比盾量了**):

- **按召唤物血量比例**:旧桂是 3 只 390 血 + 盾 60,盾/血 ≈ 15%。新桂 1 只 790 血
  → 盾 ≈ 120;新塔 1 只 182 血 → 盾 ≈ 27。口径简单、与「盾护着这只召唤物」的语义贴合。
- **按 §1.4 护盾锚点**:土紫 / 土橙的护盾锚点 × 某系数。与全表其余护盾同源,但那条锚点
  是给**玩家**的盾定的,套到召唤物身上要另定系数。

**T4a 已按「留空」落地并全绿**,所以这条不阻塞 P2 —— 改成第 4 条方案只需在详表里给
`塔` / `桂` 各加一个 `SummonShield N`,重跑管线即可,不动引擎。
(2026-09-08 P3 就是这么落的,一行引擎代码没动;顺带给 `碉` / `堡` 补上土系印记的
20 点,四个字共用同一个字段。)

## 7 · 召唤字的攻血转换(2026-09-05 定)

召唤字可把**锚点攻击**按 `1 攻 = 3 血` 转成血量,比例只取三档 **{0, 50%, 100%}**(便于读牌)。
`r = 100%`(纯肉盾)时**被动免计价** —— 它已经把全部攻击预算付出去了,再收特性费是双重收费。

1:3 是从现行肉盾字反推的中点:碉 放弃 20 攻 → +80 血(1:4)、堡 放弃 40 → +160(1:4)、
柘 放弃 45 → +110(1:2.4)、桂 放弃 50 → +130(1:2.6)。

| r | 载体 | 结果 |
|---|---|---|
| 100% | 藤(绿)· 荆(紫)· 柘(金)· 碉(土白)· 堡(土蓝) | 嘲讽肉盾梯队:150 → 430 → 490 血;土系 100 → 200 血 |
| 50% | 桂(土橙)· 塔(土紫) | 半肉光环位 |
| 0% | 其余召唤字 | 常规 |

## 8 · 召唤物基础攻系数 = ×0.70(2026-09-05 实测定标)

`tools/balance` 自 2026-09-04 的 势/水势 → 厚/泉 改名起**编译不过**,无人可跑;且它
**没有木系画像**,召唤流一个观测点都没有。本次修好并补齐后的第一组读数(300 种子):

| 画像 | 均卒层 | P50 | P90 | 最深 | 达文山(26) |
|---|---|---|---|---|---|
| 火系(养成) | 12.0 | 12 | 14 | 17 | 0% |
| 水系双方向 | 15.6 | 16 | 20 | 22 | 0% |
| 土系双方向 | 17.4 | 16 | 21 | 22 | 0% |
| **木系召唤(×1.00,现行)** | **22.4** | 23 | 27 | 31 | **19%** |

木系是全表最强、且是唯一摸到文山的画像 —— **而这还是召唤物没接战意/厚之前**。
系数对照实验:

| 召唤攻系数 | 木系均卒层 | 判定 |
|---|---|---|
| ×1.00 | 22.4 | 远超其它系 ❌ |
| **×0.70** | **15.9** | 落在水 15.6 / 土 17.4 之间 ✅ |
| ×0.55 | 12.8 | 掉到火系一档,过头 ❌ |

⚠ **实验没覆盖的一条**:纯木系卡组里既没有战意来源(金系攻击字)也没有厚来源(土系护盾字),
所以接 buff 后纯召唤流几乎不涨 —— 涨的是 **木+金 / 木+土 混色卡组**。这正是设计要的联动奖励,
但 P0 落地后**必须补一档混色画像重测**,否则混色流派的上限没有观测点。

## 8.1 · 第三组读数(2026-09-08,T5,字表重做 P2 收尾)

⚠ **画像口径已扩**:上面第一组只有 4 档(火/水/土/木各一)。T4b 把画像扩到 9 档
(补了新手/小成长/探针×2/木土混色,并把水土两档换成真实的双方向起手字),下面第三组
按现在的 9 档跑,与上面 4 档不是逐行同名对照——四个可对照的系别画像单独在文字里点出来。

### 8.1.1 两种口径(Power() 补分前 / 后)

`tools/balance/Program.cs` 的 `Power()` 在这批之前只给 15 个 `EffectKind` 记分,其余
19 个(含全部控制类:`Silence`/`Freeze`/`Slow`/`ArmorBreak`/`Morale`/`CritBuff`/`Empower`/
`Immunity`/`Reflect`/`Detonate`/`Blind`/`Dispel`/`Cleanse`/`Revive`/`Bleed`/`HealOverTime`/
`BurnNoDecay`/`BurnSettleNow`/`ApBoost`)一律 0 分。本任务已按 design §2.2/§1.4.1 的
价目表补齐(换算口径:借用已有的 `Charm` 案例反推,定价 0.40 → 60 分,即 150 分/1.0
价格单位;详见 `Program.cs` 里那段新增注释)。**两组读数如实记录,不作取舍**:

**口径 A(补分前,与 T4b 报告一致)**:

| 画像 | 均卒层 | P50 | P90 | 最深 | 达词渊(11) | 达文山(26) | 达墨海(51) | 带甲战/次 | 带甲多怪战/次 |
|---|---|---|---|---|---|---|---|---|---|
| 新手(灭,1级,HP500,ATK100,DEF0,闪0) | 11.9 | 12 | 14 | 16 | 80% | 0% | 0% | 4.6 | 4.0 |
| 小成长(灭炎爆热,卡3级,3级,HP540,ATK104,DEF1,闪2) | 13.8 | 14 | 16 | 19 | 98% | 0% | 0% | 5.8 | 5.1 |
| 养成(焚炎灿燚,卡5级,10级,HP680,ATK118,DEF4,闪9) | 15.9 | 16 | 19 | 25 | 100% | 0% | 0% | 7.0 | 6.3 |
| 探针·对照(火系,深启11) | 17.9 | 18 | 20 | 25 | 100% | 0% | 0% | 4.8 | 4.8 |
| 探针·AOE专精(全 DamageAll,深启11) | 13.7 | 14 | 15 | 16 | 100% | 0% | 0% | 2.4 | 2.4 |
| **水系双方向(冻冰淼㵘,卡5级,10级)** | **5.5** | 6 | 6 | 6 | 0% | 0% | 0% | 0.6 | 0.0 |
| 土系双方向(垒圭垚㙓,卡5级,10级) | 21.3 | 21 | 26 | 27 | 100% | 11% | 0% | 10.8 | 10.1 |
| 木系召唤(林森藻𣛧,卡5级,10级) | 33.1 | 33 | 40 | 44 | 100% | 97% | 0% | 22.9 | 20.7 |
| 木土混色(林森圭垚,卡5级,10级) | 29.8 | 31 | 32 | 36 | 100% | 95% | 0% | 20.1 | 17.7 |

**口径 B(补分后)**:

| 画像 | 均卒层 | P50 | P90 | 最深 | 达词渊(11) | 达文山(26) | 达墨海(51) | 带甲战/次 | 带甲多怪战/次 |
|---|---|---|---|---|---|---|---|---|---|
| 新手(灭,1级,HP500,ATK100,DEF0,闪0) | 11.7 | 11 | 14 | 16 | 71% | 0% | 0% | 4.5 | 3.8 |
| 小成长(灭炎爆热,卡3级,3级,HP540,ATK104,DEF1,闪2) | 13.7 | 14 | 16 | 19 | 97% | 0% | 0% | 5.7 | 5.0 |
| 养成(焚炎灿燚,卡5级,10级,HP680,ATK118,DEF4,闪9) | 15.7 | 16 | 18 | 25 | 100% | 0% | 0% | 6.9 | 6.2 |
| 探针·对照(火系,深启11) | 17.9 | 18 | 20 | 25 | 100% | 0% | 0% | 4.9 | 4.9 |
| 探针·AOE专精(全 DamageAll,深启11) | 13.7 | 14 | 15 | 16 | 100% | 0% | 0% | 2.4 | 2.4 |
| **水系双方向(冻冰淼㵘,卡5级,10级)** | **5.5** | 6 | 6 | 6 | 0% | 0% | 0% | 0.6 | 0.0 |
| 土系双方向(垒圭垚㙓,卡5级,10级) | 21.3 | 21 | 26 | 26 | 100% | 13% | 0% | 10.8 | 10.1 |
| 木系召唤(林森藻𣛧,卡5级,10级) | 33.1 | 33 | 40 | 44 | 100% | 97% | 0% | 22.9 | 20.7 |
| 木土混色(林森圭垚,卡5级,10级) | 29.0 | 31 | 31 | 39 | 100% | 90% | 0% | 19.3 | 16.9 |

**读数逐条归因**:

- **水系:补分前后逐位不差(5.5/6/6/6/0%/0%/0%/0.6/0.0 完全一样)。** 这不是巧合——
  `Power()` 的 `foreach` 只扫 `CharDef.Effects`,而水系 11 张双方向字的 `Effects` 里
  **只有** `HealSelf`/`HealAll`/`HealOverTime`(此前就已 100% 记分),它们的 `Silence`/
  `Freeze`/`Slow`/`ArmorBreak`/`SpendWellspring` 全部装在 `AttackEffects` 里 ——
  `Power()` 从来没读过这个字段一次,不管里面的 kind 记不记分都一样。**结论见下面
  「头号任务」独立小节**,不在这里重复。
- 其余 8 档画像里,新手/养成/木土混色三档出现个位数到小数点后一位的漂移(如
  养成 15.9→15.7、木土混色 29.8→29.0),来自 `灭`(封禁 Silence 现在计分)、
  `杜`/`壁`/`圭`(免疫/反弹现在计分)等字在弃牌/合成决策里的优先级微调,方向不一致
  (有涨有跌),量级都在噪声带内(300 种子下 1 层以内属于正常抖动,参见 T4b 报告
  E 类关于「探针的绝对值不是判据」的既有说明)——**没有一档因为补分而发生方向性改善
  或恶化**,这本身也是口径 B 的价值:证明这批计分修正没有引入新的系统性偏差。

### 8.1.2 头号任务:水系归因结论 —— 工装看不见,而且比 brief 假设的更彻底

**归因结论:工装看不见(不是数值问题,不是形态问题)。**

补齐 `Power()` 计分前后水系读数逐位相同,已经排除「候选 4:控制类不计分」是根因
——因为水系的控制/伤害效果根本不在 `Power()` 会读到的字段里,分数高低无关紧要。
往下深挖一层,找到了比 brief 设想更硬的证据:

**`tools/balance` 的贪心机器人从未以 `attackMode: true` 调用过 `battle.Cast`**
(`command grep -rn attackMode tools/balance/` 命中 0 次)。`Cast` 的 `attackMode`
参数默认 `false`,`EffectsOf(def, attackMode)` 在 `false` 时恒定返回 `def.Effects`
(`BattleEngine.cs:1163`)——`PlayTurn` 里唯一的出字调用 `battle.Cast(pick, target)`
永远不传这个参数。design §1.5(2026-09-05 定稿)明文规定双方向字「打在敌人身上的效果
放攻面(`AttackEffects`),挂在自己身上的效果放护面(`Effects`),两面都挂不允许」——
这是**刻意的设计**,服务真人玩家「拖到敌人身上 = 攻,拖到自己身上 = 护」的操作。
但 `tools/balance` 的机器人只会拖自己(`target` 只在 `NeedsTarget(pickDef)` 为真时
才选,而 `NeedsTarget` 同样默认 `attackMode=false`,只检查 `Effects`)——**它结构性地
没有能力选择攻面**。

水系 11 张字**全部**是这种双方向字,且 `WaterCards` 里**没有一张召唤字**——攻面
(伤害/控制/终极技)全部沉在机器人摸不到的地方,水系因此在这个仿真里**只会治疗,
从来不会攻击**。实测证据(临时探针,验证完已从 `Program.cs` 移除,不入库):

| 指标 | 水系双方向 | 其余 8 档 |
|---|---|---|
| 300 局里"僵局判死"(60 回合分不出胜负,`ClimbUntilDeath` 的 `turns > StallTurns` 分支) | **259** | 0(全部 8 档均为 0) |
| 300 局里"真被打死"(HP 归零) | 41 | 300 |

**其余全部 8 档画像僵局判死恒为 0,唯独水系 259/300。** 这不是"水系打不过怪",是
"水系的机器人从头到尾没有对怪造成过一次真正的攻击面伤害"——每回合只会治疗,66 回合
打不死任何怪,判定超时死亡。土系双方向也有 8/12 张字是同款双方向结构(`垒`/`垚`/`圭`/
`壁`/`杜`/`碎`/`㙓`/`崩`,攻面同样摸不到),但土系画像的 `OwnedCards` 里另有 4 张
**纯召唤字**(`塔`/`桂`/`碉`/`堡`,只有 `Effects=[Summon]`,没有 `AttackEffects`)——
这批召唤物入场后**独立于玩家出字自动反击**(`SummonState` 的回合末攻击,不经过
`Cast`/`attackMode` 这条链路),给了土系一条完全绕开这个坑的输出通道。土系画像实测
`塔`(3262 次)/`桂`(3038 次)/`堡`(2227 次)/`碉`(1175 次)出场次数全部远超它自己
库里的双方向字,印证了"土系没死"的真正原因是召唤物在打,不是双方向字的攻面在打。
水系没有对应的召唤字可以顶上——这是水系与土系"同样都是双方向字扎堆"却一个 5.5
一个 21.3 的完整解释。

**三者判据**:
- 数值问题?排除——水系的 `AttackEffects`(`冻`Slow2+Dmg63、`冰`Freeze1+Dmg244…)
  数值本身与其他系同档位对照并不离谱(§6 表已核过锚点),它们只是从未被执行。
- 形态问题(双方向字每回合二选一的固有劣势,brief 候选 1)?**部分成立但不是主因**——
  真实游戏里双方向确实要牺牲一部分输出去换治疗,这是设计承认的权衡(§1.3 的护盾/
  治疗定价已经把这个权衡计入价目表)。但当前的 5.5 不是这个权衡的产物,是「二选一」
  变成了「只能选一边,另一边工装选不到」的极端情况,与真实玩家能自由二选一的体验
  相差一个数量级。
- **工装看不见:确认成立,而且范围比 brief 原先定位的"`Power()` 漏计分"更大** ——
  即使把 `Power()` 修得再精确,只要 `PlayTurn` 不会传 `attackMode: true`,水系的
  攻面就永远不会被执行。**这一条修复不在本任务授权范围内**(brief 只放行"补齐
  `Power()` 的计分",没有放行改 `PlayTurn` 的选字/施法逻辑),已写入 P3 交接。

**不建议动数值或 §1.3/§1.5 的形态定价** ——问题不在那里,改了也不会让这个仿真的水系
读数动一分。真正要做的是修 `tools/balance` 的机器人,让它学会在攻面/护面之间选择
(例如:分别算一次 `Power(def.Effects)` 与 `Power(def.AttackEffects)`,取更高者对应
的 `attackMode`),这是一次工装改动,不是数值改动,留给 P3。

### 8.1.3 护盾"中层无聊"验收(用户原始动机,本批验收项)

**明确答复:跨战场景的"滚雪球式无限堆盾"已经解决,但单场战斗内"堆盾抢跑领先伤害"
这件事本身没有被这批改动消除——这是设计文档自己承认并刻意保留的残留,不是本批的
验收标准,不算本次遗留问题。**

依据:

1. **本批实际落地的两条修复**(design §1.4):护盾定价系数 `SHIELD_F` 从原值下调到
   **0.65**(治疗维持 1.00,刻意不对称——护盾可累积无衰减、治疗被 `MaxHp` 自然封顶);
   **每场战斗结束时 `_shieldNormal`/`_shieldPersist` 各减半**。这两条对症的是 §0 第
   2/3 条描述的病灶:玩家把每回合的牌都砸在护盾上,盾滚雪球式跨战斗累积,一路刮到
   游戏结束都打不死也死不了。
2. **design §1.4 自己写明的已知残留**(不是本次疏漏,是权衡后的刻意选择):
   「已知残留:战斗结束衰减管的是跨场累积,单场内的净增长它碰不到……选它而不选
   回合末衰减是用户 2026-09-05 的裁定(理由:回合末衰减让护盾读起来像没加)」,
   §9 第 3 条重申同一件事并给出下一步方案(「若上线后中层仍闷,下一步是把衰减改到
   回合末」)。**这本身就是把「回合末衰减」这个更彻底的方案主动搁置到下一批**,
   不是本批漏做。
3. **本次读数支持"跨战雪球已解，单场内仍可能刮痧"这个判断**:土系双方向(护盾密度
   最高的画像,`EarthShieldCards` 8 字全部带 `Shield`/`ShieldAll`,数值见下)均卒层
   21.3,落在 9 档画像的中段(11.9~33.1 区间内,不是异常拔高的最高值)——如果护盾
   还在无限雪球,土系应该顶到全表最高甚至撞 `DepthCap`(木系召唤那种)。同时
   300 局里"僵局判死"恒为 0(见 8.1.2 的探针表)——每一局最终都真被打死或打赢,
   没有出现"60 回合打不出胜负"的僵局,说明战斗本身仍会在合理时间内分出胜负。
   但这两条只能证明"没有彻底失控",证不了"单场内不闷"——那是一个战斗节奏/手感
   问题,这份仿真的粒度(只记卒层,不记单场内每回合的盾/血曲线)量不出来,只能
   引用 design 文档自己的判断。
4. **`EarthShieldCards` 现读护盾数值**(2026-09-07 随字表重做 P2 标定,T4b 已核对):
   垒 30 / 壁 35(+ 反弹 30)/ 崩 `ShieldAll` 21 / 碎 46 / 圭 119(+ 反弹 50)/
   杜 109(+ 免疫 2 次)/ 垚 149 / 㙓 157(持久一次)——均已按 `SHIELD_F=0.65` 折算,
   高档护盾从原锚点砍了超过三分之一。

### 8.1.4 梯队是否成立

**成立,没有查到任何"出牌 0 次"的梯队字。** 用临时探针(见 8.1.2,已从 `Program.cs`
移除不入库)统计了每张字在 300 局里的实际出牌次数,§4 列出的每一条梯队(核心特性
三级 + 边缘特性)在其对应画像里全部命中非零次数,包括:

- 水系涌泉相报:溃(蓝,低档)2366 次、㵘(红,高档)2114 次;
- 土系厚积薄发:崩(蓝,低档)962 次、㙓(红,高档)2586 次;
- 土系反伤:壁(绿,低档)1885 次、圭(金,高档)2132 次;
- 土系破甲:碎(蓝,低档)1065 次(经合成/战利品补入库,不在起手 4 张里);
- 木系单只体量:枪(白,低档)111 次、藤(绿,低档)133 次——数量级明显低于高档
  (柘 2070/藻 1949),符合"持续带但用得越来越少"的梯队设计意图,不是"用完就弃"。

⚠ **一条重要限定,别读过头**:「出牌次数不为 0」只证明这张字**被拖上场过**,不证明
它标榜的那个终极机制**真的触发过**——涌泉相报(`SpendWellspring`)、厚积薄发
(`SpendHeft`)都挂在 `AttackEffects` 里,与 8.1.2 揭出的坑是同一个坑:这两个"终极技"
的实际放电,在这份仿真里同样从未发生过一次,不管溃/㵘/崩/㙓 被"出牌"多少次。
梯队本身(有没有低档字陪跑)成立,但梯队里两条"终极技"轴(涌泉相报/厚积薄发)的
真实强度,这份仿真完全没有观测力——这一条已经写进 P3 交接第 11 条的姊妹项。

### 8.1.5 召唤系数 ×0.70 是否仍然合适

仍合适,读数没有给出需要重新校准的信号。第三组的木系召唤(33.1)与木土混色(29.0/29.8)
两档都在合理区间内(不是全表最低也不是撞 `DepthCap`),且木系召唤仍是唯一稳定摸到
墨海段附近深度、达文山比例最高(97%)的画像——与 §8 原文早前指出的"木系全表最强"
结论一致,没有因为本批的召唤只数收归 1 只 + 血攻按总量守恒折算而掉出合理区间。
✓ 没有发现需要调整 ×0.70 的证据;若后续要精细校准,应先修 8.1.2 指出的
`attackMode` 缺口再重跑,否则土系/水系这两档的读数底噪太大,校准会建立在错误的
基线上。

## 10 · P3 落地参数(2026-09-08 用户拍板)

用户对 P2 交出的三件待拍板给了裁定:**① 修 attackMode 缺口;② 金系战意 1 层、
土系入场护盾 20 点;③ 光环盾「听你的」**。第 ③ 项授权下我改了本稿原先的定价,
并顺带订正了本稿的一处机制误解 —— 见 §10.2。

### 10.1 系印记的数值(拍板)

| 系 | 印记 | 载体 | 数值 |
|---|---|---|---|
| 🟡金 | 叠战意 | 所有 `atk` 形态字(11 张) | **`Morale 1`**(战意 1 层) |
| 🟤土 | 入场护盾 | 所有 `sum` 形态字(4 张) | **20 点** |

两者都**免配额免计价**(§1.2),所以加它们**不改动任何既有数值**。

⚠ 金系的 `Morale 1` 不落在 §6 表的任何数值列里,故不影响对账;
**土系的 20 点会进护盾侧**,`rebalance` 脚本必须一并产出它,否则对账测试会红。

### 10.2 订正:「入场护盾」与「光环盾」是**同一个机制**

本稿 §2.3 把两者列成两条独立的召唤被动(各 0.25 / 0.35),**那是错的**。
2026-09-08 读引擎查实:

```csharp
// BattleEngine.cs,Summon 分支
if (effect.SummonShield > 0)
{
    int shieldGrant = MetaRules.ScaleByCardLevel(effect.SummonShield, cardLevel);
    foreach (var summon in _summons)
        if (summon != null && summon.Alive) summon.Shield += shieldGrant;
    // 桂 的全场加盾同样攒厚(2026-09-02)
    GainHeft(shieldGrant);
}
```

- 两者都只能落到 **`EffectDef.SummonShield` 这一个字段**上,而一个 `Summon` 效果
  只有一个该字段 —— **不可能同时存在两份**,必须合并成一个数值。
- 它**给召唤物护盾,同时让玩家攒厚**(`GainHeft`)。

⚠ **连带订正 §0 第 6 条**:那里把「土系召唤字拿不到厚」列为待修硬伤 —— 其实
**2026-09-02 那行 `GainHeft(shieldGrant)` 就已经修好了**,一直缺的只是载体。
而 P2 还把 `桂` 唯一的 `SummonShield 60` 按「不在新表里」删掉了(P2 Task 4a G 类),
于是全表零载体。本节把载体补回来。

### 10.3 光环盾重新定价:0.35 → 0.20

**依据**:0.35 是按「**持续**光环」定的(§6.1 上半段的措辞),而引擎实现是
「**召唤当时发一次性护盾**」—— 不恢复、不外溢给后来入场的召唤物。
一次性效果的价值远低于持续加成,0.35 是**付持续的钱、拿一次性的货**。

0.20 与 §2.3 里「**免一次清盾 0.20**」对齐 —— 两者都是一次性、条件触发的防御效果,同量级。

**加成量 = 召唤物血量 × 15%**,取整到 10 的倍数。15% 有历史锚:
旧 `桂` 是 3 只 390 血 + 盾 60,盾/血 = 15.4%。

### 10.4 最终数值

| 字 | 档 | `SummonShield` 构成 | 值 | 血 / 攻 |
|---|---|---|---|---|
| `碉` | 白 | 印记 20 | **20** | 100 / 0(不变) |
| `堡` | 蓝 | 印记 20 | **20** | 200 / 0(不变) |
| `塔` | 紫 | 印记 20 + 光环盾 30 | **50** | 182/14 → **221 / 17** |
| `桂` | 橙 | 印记 20 + 光环盾 130 | **150** | 790/67 → **892 / 76** |

`塔` / `桂` 的血攻上调是光环盾降价的直接结果(ratio 塔 0.44→0.32、桂 0.36→0.28)。
实测:**其余 55 字一个都不受影响**,无字超预算上限 0.60,脚本三项自检(配额 /
预算 / 同系同形态单调性)全过。

⚠ 盾量依赖血量、血量依赖 ratio、ratio 依赖光环盾**价格**(常量)—— 无循环依赖,
但**重算顺序是硬的**:先定价 → 出血攻 → 再按血量算盾量。

### 10.5 attackMode 缺口(拍板要修)

见 `docs/superpowers/plans/2026-09-08-字表平衡重做-P3-交接.md` §0。
`tools/balance` 的贪心机器人调 `Cast` 时从未传 `attackMode: true`,双方向字的
攻面从未被执行过 —— 300 局里水系 259 局是「60 回合僵局判死」。

⚠ 修完之后 **§8 / §8.1 的三组读数全部作废**(它们都建立在「机器人只会用一半机制」
之上),要跑第四组并在 §8.2 明确标注「前三组不可比」。

## 8.2 · 第四组读数(2026-09-08,P3 收尾)

⚠⚠ **§8 与 §8.1 的全部三组读数(第一组 4 档 + 第三组口径 A/口径 B)已确定作废,
不得再拿它们与下表做趋势对比。** 三组读数全部建立在「机器人只会用一半机制」之上——
`tools/balance` 的贪心机器人调 `Cast` 从未传 `attackMode: true`,双方向字的攻面
（水/土两系合计 20 张字里的伤害/控制效果)从未被实际执行过一次;修复见 `c888ace`
(T1,`tools/balance/Program.cs` 一个文件)。下表是修复后跑的**第一组**、也是**唯一**
一组「机器人两面都会用」的读数,此前所有组次相对它而言不是「同一把尺子的不同刻度」,
是「坏尺子」与「尺子」的区别,不可比。

```
scalePerDepth=0.1 bossBonus=1 × 300 种子

| 画像 | 均卒层 | P50 | P90 | 最深 | 达词渊(11) | 达文山(26) | 达墨海(51) | 带甲战/次 | 带甲多怪战/次 | 攻面出字/局 | 僵局判死/300 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 新手(灭,1级,HP500,ATK100,DEF0,闪0) | 11.7 | 11 | 14 | 16 | 71% | 0% | 0% | 4.5 | 3.8 | 0.0 | 0 |
| 小成长(灭炎爆热,卡3级,3级,HP540,ATK104,DEF1,闪2) | 13.7 | 14 | 16 | 19 | 97% | 0% | 0% | 5.7 | 5.0 | 0.0 | 0 |
| 养成(焚炎灿燚,卡5级,10级,HP680,ATK118,DEF4,闪9) | 15.7 | 16 | 18 | 25 | 100% | 0% | 0% | 6.9 | 6.2 | 0.0 | 0 |
| 探针·对照(火系,深启11) | 17.9 | 18 | 20 | 25 | 100% | 0% | 0% | 4.9 | 4.9 | 0.0 | 0 |
| 探针·AOE专精(全 DamageAll,深启11) | 13.2 | 13 | 14 | 15 | 100% | 0% | 0% | 2.1 | 2.1 | 5.8 | 0 |
| 水系双方向(冻冰淼㵘,卡5级,10级) | 14.2 | 14 | 16 | 21 | 100% | 0% | 0% | 6.0 | 5.3 | 43.5 | 1 |
| 土系双方向(垒圭垚㙓,卡5级,10级) | 21.3 | 21 | 26 | 31 | 99% | 19% | 0% | 10.8 | 10.1 | 25.1 | 0 |
| 木系召唤(林森藻𣛧,卡5级,10级) | 33.1 | 33 | 40 | 44 | 100% | 97% | 0% | 22.9 | 20.7 | 0.0 | 0 |
| 木土混色(林森圭垚,卡5级,10级) | 27.2 | 27 | 31 | 36 | 100% | 78% | 0% | 17.8 | 15.4 | 14.6 | 0 |
```

`chars.json` md5:`c82a00c95ab8ca137434608640a42f1f`(含 `21fda03` 落地的金/土印记 +
光环盾重定价)。命令与验证命令一节完全一致(`cd tools/balance && dotnet run`),300 种子
的仿真本身是确定性的(无外部随机源),两次独立跑输出逐字节相同。

### 8.2.1 唯一有效的历史对照点:T1(`c888ace`)的「修完 attackMode、T2 尚未落地」读数

`c888ace` 落地后、`21fda03` 落地前,`tools/balance` 曾单独跑过一组读数(记在
`.superpowers/sdd/2026-09-08-字表平衡重做-P3/task-1-report.md`)。它与上表用的是
**同一把尺子**(机器人两面都会用),只差 T2 这一次改动,是唯一能拿来做「T2 改了什么」
差分的基线,摘录如下(与上表逐行对齐,列全部相同):

| 画像 | 均卒层 | P50 | P90 | 最深 | 达文山 | 带甲战/次 | 带甲多怪战/次 | 攻面出字/局 |
|---|---|---|---|---|---|---|---|---|
| 新手 / 小成长 / 养成 / 探针·对照 / 探针·AOE / 水系双方向 / 木系召唤 | **与上表逐位相同** | | | | | | | |
| 土系双方向 | 18.9 | 20 | 21 | 26 | 4% | 9.0 | 8.3 | 36.3 |
| 木土混色 | 27.4 | 27 | 31 | 36 | 81% | 17.9 | 15.5 | 15.5 |

**七档画像(新手/小成长/养成/探针×2/水系/木系召唤)在 T2 前后逐字节不变**——这是一条
干净的阴性对照:T2 只改了金系战意标记(载体是攻击形态字,不在这七档任何一份卡池里)、
土系入场护盾(载体是 `碉`/`堡`/`塔`/`桂`,只在土系/木土混色两档卡池里)、光环盾重定价
(同样只影响 `塔`/`桂`)。这七档没有任何一张牌能碰到这次改动,读数不动,说明改动没有
产生任何范围外的副作用。

### 8.2.2 五系是否平衡了?—— 只能回答 4/5,金系本身在这份仿真里从未有过观测点

**先说一个此前所有报告都没点破的缺口**:`tools/balance/Program.cs` 的九档画像覆盖
火/水/木/土四系(含两档双方向、一档混色、两档探针),**从建立以来就没有一档金系画像**
——`grep -n "Metal\|金系\|GoldCards" Program.cs` 只命中注释里提「金系是别人的战意来源」,
没有任何一处以金系字为主角的 `Profile`。本批(T2)刚落地的「金系战意 1 层」印记,载体是
`利`/`锋`/`剿`/`鑫`/`刲`/`鍂`/`铡`/`剁`/`剑`/`锥` 等 11 张攻击形态字,**这 11 张字
一张都不在上面九档任何一个卡池表里**——`FireCards`/`WaterCards`/`EarthCards`/
`WoodCards`/`AoeCards`/`WoodSummonCards`/`EarthShieldCards` 逐张核对,均无命中。
**结论:金系战意这条印记,本批落地之后,在这份仿真里的实际强度仍然是零观测**——
不是「测了说没影响」,是「从来没有一次被这份仿真的任何一局摸到过」。这不是本任务
引入的缺口(七个卡池表在 T2 之前就没有金系),但过去几批报告在回答「五系平衡了吗」
时都没有点出这一条,这次一并交代清楚。

**剩下 4/5 系(火/水/木/土)的结论**:均卒层落在 **11.7 ~ 33.1** 区间,与 T1 单独
读数的区间(11.9~33.1,brief 引用)基本一致,**没有任何一档跌出这个区间或撞
`DepthCap`(300)**——不再有"水系工装看不见"那种结构性归零。但区间内部的差距本身
没有缩小:**木系召唤(33.1)仍是全表最强,是水系双方向(14.2)的 2.3 倍、土系双方向
(21.3)的 1.55 倍**——这个差距在 T1(木系 33.1 / 水系 14.2 / 土系 18.9)、以及更早的
§8 原文("木系是全表最强")里都是同一个结论,本批(T2)只字未改木系任何数值,自然
不可能缩小它。**明确结论:四系(火水木土)不再有工装缺陷导致的假性失衡,但木系相对
水/土两系的领先幅度这批完全没有处理(不在 T2 授权范围内:T2 只动了金系印记/土系印记/
光环盾定价三件事)——「五系平衡」目前只能对 4/5 系给出「没有结构性失衡」的结论,
金系没有观测点,木系对水/土的领先幅度是已知但本批未处理的残留。**

### 8.2.3 土系读数确实回升了,原因是 T2 给 `塔`/`桂`/`碉`/`堡` 补上了护盾/攒厚

**实测:土系双方向 18.9(T1)→ 21.3(T2,本组),+2.4 层(+12.7%)。** 归因链路,
每一环都有独立证据,不是"看起来合理就写上":

1. **改动本身**:`git diff c888ace 21fda03 -- Brushblade/Assets/StreamingAssets/config/chars.json`
   显示 `碉`/`堡` 各新增 `summonShield: 20`,`塔` 新增 `summonShield: 50`(同时
   `value`182→221、`attack`14→17),`桂` 新增 `summonShield: 150`(`value`790→892、
   `attack`67→76)——与 §10.4 的最终数值表逐字吻合。
2. **引擎语义**(§10.2 已引用,此处复用):`SummonShield > 0` 时,`BattleEngine` 既
   给场上召唤物加盾,也 `GainHeft(shieldGrant)`——即打这四张字会让玩家**攒厚**,
   而厚会放大召唤物攻击(T1 报告 §6「顾虑」一节已指出这条链路)。
3. **卡池覆盖面**:「土系双方向」画像的 `ownedCards` 用的是 `EarthCards`(12 字全表,
   `Program.cs:65`),**包含** `塔`/`桂`/`碉`/`堡`——这四张字此前(T2 之前)是
   `EarthCards` 里唯一「不产生任何护盾/不攒厚」的四个,现在补上了。
4. **负对照**:「木土混色」画像的卡池用的是 `EarthShieldCards`(`Program.cs:74`,
   **显式排除** `塔`/`碉`/`堡`/`桂`,类文档写明理由是"不产生护盾,不会攒厚,混进来会
   稀释观测")——这四张字这次改动**摸不到**这一档画像。实测木土混色 27.4→27.2,
   在 8.1.1 已确认的噪声带内(300 种子、1 层以内),没有方向性变化。**四字只出现在
   土系画像的卡池里、不出现在木土混色的卡池里,而只有土系画像的读数动了、木土混色
   没动——这是最直接的因果证据,不是巧合。**
5. **出牌频次实测**(方法论说明:为了确认"是不是这四张字被机器人重仓了",在
   `PlayTurn`/`SimulateProfile` 里临时加了一个逐字出牌计数器,跑完这一条归因后
   **已用 `git checkout -- Program.cs` 撤销,未提交,不影响任何委交的代码或后续
   读数**——纯粹是本次归因用的一次性诊断)。300 局合计:`塔` 3199 次、`桂` 2495 次,
   是**全库出牌次数最高的两张字**,远超四张双方向字本身(`垒`565/`圭`1756/`垚`2084/
   `㙓`2204)。T2 给 `塔`/`桂` 加的血攻(§10.4)推高了它们的 `CardValue`,贪心机器人
   的合成/留牌逻辑因此更愿意把它们留在库里反复出——这也解释了另一项变化:
   **攻面出字/局从 36.3 降到 25.1**——`塔`/`桂` 挤占了出字机会与合成槽位,双方向字
   (会用到攻面的那四张)相对被出得更少了,即使整体卒层变深了。

**结论:土系回升,原因明确,不是随机噪声,也不是"重新校准巧合对齐了旧基线"**
(虽然巧合的是 21.3 与更早、attackMode 还没修时的读数数值上重合,但那组读数本身已经
作废,且这次最深/达文山/攻面出字这几列都不同,不是同一套底层分布,纯属数值巧合,
已在 8.2.1 的表里用真实差异列打破这个巧合观感)。

### 8.2.4 护盾「中层无聊」验收 —— 本轮 P3 没有解决,而且把关键链路的覆盖面扩大了

**明确结论,不含糊**:

1. **跨战雪球式堆盾(用户原始诉求的一半)—— 已解决,本批未受影响**。P2 T5 落地的
   `SHIELD_F=0.65` + 战斗结束护盾各减半这两条,本批(T2)没有改动任何相关代码或系数,
   仍然有效。
2. **单场战斗内"堆盾抢跑"(用户原始诉求的另一半)—— 仍未解决,design §1.4 主动
   搁置,本批授权范围内也没有处理**。这不是本批遗漏,是 P2 就已写明、本批延续的
   已知残留(见 §8.1.3、§9 第 3 条)。
3. **T1 揭示的第三个维度:护盾经由"厚"转化为召唤物输出,不是纯防御** —— **本批
   (T2)没有缓解这条链路,反而把它的覆盖面从"8 张土系护盾字"扩大到了"12 张土系字
   全员"**(新增 `塔`/`桂`/`碉`/`堡` 四张召唤字的护盾/攒厚,详见 8.2.3)。这是执行
   用户拍板(金系战意/土系入场护盾/光环盾)的直接、必然后果——三条拍板本身是在给
   召唤物系统"补机制",没有一条是冲着"削弱护盾"去的,但它们落地的载体恰好是
   `EffectDef.SummonShield` 这同一个字段,于是客观上让"护盾即输出"这条链路变得
   更普遍,而不是更少见。
4. **量化这条变化**:土系双方向画像的均卒层因此从 18.9 升到 21.3,而带动它上升的
   两张字(`塔`/`桂`)恰恰是全库出牌次数最高的两张(8.2.3)——说明"打召唤物、靠厚
   放大它的攻击"在这份仿真里现在是土系**更优**的打法,门槛(需要摸到 `塔`/`桂` 这类
   护盾/召唤字)比"用双方向字的攻面正面输出"更低、回报更高。
5. **这份仿真量不出"无聊"本身**(与 8.1.3 的既有限定一致):它只记卒层/深度/带甲战
   次数,不记单场内每回合的盾层/血量/伤害曲线,无法直接回答"玩家实战会不会觉得
   闷"。能确认的只是"护盾→厚→输出"这条链路的覆盖面与吸引力这批之后变大了,不是
   变小了。

**给用户的验收结论(不含糊)**:「护盾中层无聊」这一条,本轮 P3 **没有解决**,也
**没有朝解决的方向推进**——已解决的只是这个诉求里"跨战雪球"那一半(P2 完成,本批
未动),原本就搁置的"单场内堆盾抢跑"那一半仍然搁置,而本批新落地的三条数值改动
恰好又都长在"护盾"这同一个字段上,客观效果是让"护盾不只是防御、还是输出"这条
此前只影响 8 张字的链路,现在覆盖了全部 12 张土系字。**如果这一条要继续往前推,
唯一被明确点过名的候选方案仍是 design §1.4 早就写下、一直没做的"回合末护盾衰减"**
——这超出本批授权(不许改数值/配置),留给下一批拍板。

## 9 · 已知风险 / 待验证

### 9.x 光环类效果按「一份」计价,而收益是「N 份」(2026-09-08,P3 落地时发现)

价目表给 **光环攻 0.35 / 光环盾 0.20** 定的都是**一份**的价,而引擎两者都是
`foreach (var summon in _summons)` —— **给场上全部存活召唤物各发一份**,召唤槽上限 **6**。
所以一张字的实际收益随同场召唤物数量**线性放大**,这个形态价目表没有计价。

实测量级(`ScaleByCardLevel` = ×(1 + 0.1×(级−1)),满级 10 级 = **×1.9**):

| 字 | 基础 | 满级单只 | 铺满 6 只 |
|---|---|---|---|
| `桂`(土橙,光环盾 150) | 150 | 285 | **1710** 点护盾 |
| `𣛧`(木红,光环攻 20) | 20 | 38 | **228** 点攻击 |

**为什么本批不改**(2026-09-08 裁定):

1. **单只量与同档同轴的字完全同量级** —— 桂 满级 285,对比土系同档护盾字
   `垚` 149→284 / `㙓` 157→299。定价在「一份」这个口径上是自洽的。
2. **「×6」的投入与收益匹配** —— 要铺满 6 只得出 6 张召唤字(各占一次出字机会 + AP),
   而召唤物是消耗品、会死。
3. **攒厚按单只量**(`GainHeft(shieldGrant)`,不是总量)—— 厚循环**没有**被放大,
   这一点引擎里有专门的注释守着。

**风险在哪**:上面三条都是「当前字表下」的判断。一旦将来出现「低成本铺场」的手段
(便宜的召唤字、召唤物复制、或提高召唤槽上限),光环类的实际价值会脱离它的定价 ——
而价目表里看不出这个杠杆。

**若要修**,两个方向(未定):
- 给光环类加一条「按 N 份计价」的规则(例如价格 × 期望同场只数),但期望只数依赖字表构成,
  会让定价随字表变动;
- 或在引擎侧钳位(例如光环只加最多 K 只),与 §1.6 反伤钳位同一条思路。

⚠ 与 §1.6(反伤总量钳位 60%)是**同型问题** —— 那一条当年也是「一张字的收益被另一张字
线性放大」,处理方式是引擎侧钳位。光环这条目前**没有**钳位。


1. **火系高档直伤仍低** —— `燚` 86、`焚` 108。根因是 §1.4.2 承认的量纲问题:DOT 当量按
   N(N+1)/2 二次增长,而档位锚点线性增长。已用档位系数 K 缓解(红档 5 层从吃掉 280 降到 126),
   但没根治。**待验证**:实战里 DOT 在深层是否真如 K 假设的那样贬值。
2. **`鑫` 与 `刲` 同档差 52 点**(256 vs 308)—— 同档内特性组合不同导致,不算倒挂,但读牌时
   `鑫` 会显得弱。可考虑把 `暴击 +20%`(0.40,全表最贵的副效果之一)换成便宜的。
3. **单场内堆盾仍可转正** —— §1.4 已说明,是本次刻意保留的残留。若上线后中层仍闷,
   下一步是把衰减改到回合末。
4. **P0 落地后要重跑仿真** —— 见 §8 的混色画像那条。

## 11 · P4:终极技不计价与 buff 限时化(2026-09-08 用户裁定)

### 11.1 终极技不计价,且带终极技的字一律群体形态

**裁定原文**:「当初设计终结技的时候就是因为体感上水系和土系较弱。而且终极技只有蓝卡一张和
最终极的一张红卡,本质上不是通用技能。因为红卡太难获取,且要发挥厚和泉的作用基本必须依赖
这两张蓝卡。所以终极技不计价。」

价目表 0.50 → **0.00**。理由是它与其余特性不同类:其余特性是「这张字额外能做的事」,
终极技是「厚 / 泉这条构筑的入口」——载体只有 溃 / 崩(蓝)与 㵘 / 㙓(红)四张,
而红卡的获取率决定了实战中几乎只能靠那两张蓝卡。按构筑级收 0.50,等于让补强水/土两系的
机制自己付掉补强的钱。

**形态规则(同批新增)**:带终极技的字**一律群体形态** —— 攻面 `DamageAll`、护面
`HealAll` / `ShieldAll`。依据是放电本身:`SpendHeft` / `SpendWellspring` 的引擎语义就是
「清空资源,对**全体**造成 层数 × Value 伤害」,两面跟着对称才自洽。
这条同时挡住了归零带来的副作用 —— 溃 若留在单体形态会是 130 攻 / 80 疗,数值上越过同系
紫档的 澡 / 湮(99 / 60),一张蓝卡强过紫卡;走群体后是 70 / 48,回到档位序列里。

### 11.2 所有 buff 类必须附带回合数

**裁定原文**:「所有 buff 类技能必须附带回合数,不存在本场生效。护盾类的可以持续久一点,
比如 4/5 回合。」后续三条补充裁定:**护盾不做时限**、**免疫按次数算**、**破甲也要限时**。

落到全表只剩两条载体轴:

| 效果 | 原 | 新 | 价格 |
|---|---|---|---|
| 护甲 `DefenseBuff` | 本场持久(`TurnsLeft = -1`) | **4 回合** | 0.33 → **0.28**(持久 × 0.85) |
| 破甲 `ArmorBreak` | 本场持久 | **3 回合** | 0.33 → **0.25**(持久 × 0.75) |

破甲跟着限时是因为它与护甲是同一条轴的正负两半,只改一边会不对称;第 10 章 :56
「破甲永久降护甲」随之作废。折扣口径沿用既有的「持久 → 限时」先例(增攻 +50 持久 0.45 →
+30 限时 0.25;暴击 +20% 持久 0.40 → 3 回合 0.30)。

**三条显式例外**:

1. **护盾**(`Shield` / `ShieldAll`)不做时限 —— 用户裁定。引擎里护盾是两个整数桶
   (`_shieldNormal` / `_shieldPersist`),没有回合概念,加时限是结构性改动且要重新定义
   跨战斗携带规则;
2. **免疫**(`Immunity`)按次数算 —— 挡完即消,本来就不是持续量;
3. **召唤物自带护甲**(新增 `EffectDef.SummonDefense`,塔)不限时 —— 它随单位存在、
   单位死了就没了,是那只单位的属性,不是场上飘着的玩家 buff。

**连带改动:护甲不再跨战斗。** `RunEngine` 的携带筛选从「按 Kind 挑 DefenseBuff」改成
「按存续挑 `TurnsLeft < 0`」——「4 回合内有效」的东西跨过一整场战斗还剩几回合,没有一个
说得通的答案。厚 / 泉是层数(恒 -1),照旧跨场。

### 11.3 归属修正:攻面不得带自身增益

**裁定原文**:「给锋和利也加上攻护两面,不能即加攻又能攻击,还能给我方 +buff」「同类型的
还有杜,要么攻击,要么护。攻击的话就不能加免疫 buff」。

这是 §1.5「打敌人的进攻面、挂自己的进护面、不许两面都挂」的漏网,全表扫下来共三处:

| 字 | 问题 | 处置 |
|---|---|---|
| 利 | 单面字,一次触发同时吃到伤害 + 增攻 30 | 拆两面:攻面 `DamageSingle 45` + `Morale 1`,护面 `Empower 30`(2 回合) |
| 锋 | 同上(暴击 20%) | 拆两面:攻面 `DamageSingle 63` + `Morale 1`,护面 `CritBuff 20`(3 回合) |
| 杜 | **攻面**挂着 `Immunity 1` —— 全表唯一一个「攻面带自身增益」 | 删掉;另护面的免疫次数 2 → 1(spec §6 与预算都按「免疫1」0.35 算的,那第 2 次从没付过钱) |

利 / 锋 的数值**不动**:价目表里 buff 的价格与它落在哪一面无关,拆面本身就是净削弱
(同 壁 的双挂修复)。战意印记留在攻面 —— 依「金系攻击字出手叠战意」的设定,挪到护面会
变成「不打人反而涨战意」。

管线侧配套:金系加进 `DUAL_DIRECTION_ELEMENTS`,同时 `_parse_row` 把「实现」列排除在
反引号候选之外(那列的备注文字里常年散落着引用 token 名的反引号,正是这道元素闸原先要防的
东西,按格排除比按系排除更直接)。

### 11.4 塔:荆棘 + 自带护甲

**裁定原文**:「塔的特技改为荆棘+护甲,移除迅捷。同时调整下攻击力,虽然主防御,但是攻击与
本档差距太大。」「召唤物当前就应该有护甲这个属性,我可以给召唤物上护甲。」

- 被动:迅捷 + 光环盾 → **荆棘 50% + 护甲 7**(光环盾一并卸掉,盾量回落到土系印记的 20);
- 「护甲」用新增的 `EffectDef.SummonDefense` 落地:召唤落位时往那只召唤物自己的状态袋挂一条
  `DefenseBuff`,由**现成的** `SummonState.EffectiveDefense` 读走 —— 不新建减伤路径
  (那条通道 2026-08-28 就通了,只是此前没有任何字往召唤物身上挂过护甲);
- 定价 0.25,低于玩家侧的 0.28:它只护这一只召唤物;
- 攻击力:肉盾换血比例 50% → **25%**,17 攻 → **21 攻**、221 血 → **153 血**。
  取 25% 而不是 0%(那是 123 血 / 27 攻)是因为紫档的塔不该比蓝档的堡(200 血)还脆。

### 11.5 数值变动汇总

| 字 | 原 | 新 | 动因 |
|---|---|---|---|
| 溃 水蓝 | 72 单攻 / 44 单疗 + 弹射3 + 破甲20 | **70 群攻 / 48 群疗**,只留终极技 | §11.1;弹射与破甲从未计价,卸掉 |
| 崩 土蓝 | 38 / 21 | **70 / 39** | §11.1 |
| 㵘 水红 | 249 / 145 | **192 群攻 / 134 群疗** | §11.1 |
| 㙓 土红 | 322 / 157 | **235 群攻 / 138 群盾** | §11.1 |
| 锥 金蓝 | 91 | **101** | 破甲降价 |
| 鍂 金金 | 262(131 × 2 段) | **282**(141 × 2 段) | 破甲降价;283 是奇数、分不成两段,差 1 计入取整 |
| 𨰻 金红 | 362 | **384** | 破甲降价 |
| 碎 土蓝 | 91 / 46 | **101 / 50** | 破甲降价 |
| 垒 土绿 | 60 / 30 | **65 / 33** | 护甲降价 |
| 杜 土金 | 223 / 109 | **236 / 115** | 护甲降价;另见 §11.3 |
| 垚 土橙 | 306 / 149 | **340 / 166** | 破甲 + 护甲双降价 |
| 塔 土紫 | 221 血 / 17 攻 / 盾 50 | **153 血 / 21 攻 / 甲 7 / 盾 20** | §11.4 |
| 利 / 锋 | 45 / 63 | **不变**,拆两面 | §11.3 |

### 11.6 海:封禁 → 弹射(并从群体退回单体)

**裁定原文**:「海的特技替换为弹射,替代溃」「海改成单体弹射」。

弹射是 `TargetShape`,引擎里**只对 `DamageSingle` 生效** —— 挂在 `DamageAll` 上会静默失效
(形状字段照读,`Targeting.ExpandTargets` 对全体伤害不走形状那条路)。海 原本是群疗字、
攻面走 `DamageAll`,所以「换弹射」必然连带「退回单体」:按 §6.0 的口径,dual_h 不带群疗时
攻面本来就该走单攻锚点。

| | 原 | 新 |
|---|---|---|
| 护面 | `HealAll 32` | `HealSelf 62` |
| 攻面 | `DamageAll 46` + 封禁 1 回合 | `DamageSingle 101` + 弹射 3 跳(每跳 ×50%) |
| 预算 | 0.34(封禁) | 0.23(弹射3 0.25 × K蓝 0.90) |

**两处已知的遗留问题**(留待用户裁定,不自行改动):

1. **档位倒挂 2 点**:海(蓝)101 / 62 高于同系紫档的 澡 / 湮(99 / 60)。
   `rebalance_2026_09_05.py` 的单调性检查会持续报这一条。根因是计价体系的正常现象 ——
   海 只买了一条 0.25 的特性(预算利用率 0.23),而紫档两张各买满两条(0.50);
   蓝档 K=0.90 与紫档 K=0.80 的差距填不平这个缺口。可动的旋钮:给 海 配第二条特性把预算
   顶到 0.30 以上,或让紫档那两张少买一条。
2. **弹射与 澡 重复**:澡(水紫)是「封禁 + 弹射3」,现在两张水系字带同一条特技。
   若要保持特技不重样,澡 的弹射需要换成别的(数值不变,只换特性)。

**封禁载体因此从六张减到五张**:灭 / 湮 / 淋 / 沐 / 澡。
