# 轮三 · 局内流程串联 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `SafeLayer` / `Settle` / `Reward` / `Replace` / `Event` / `RunEnd` / `Dialogs` / `Popups` 八张稿落到实现，让「一趟塔从打完到结算」的每个决策点都有版面依据。

**Architecture:** 三条线并行推进但按序提交 ——（a）`Ui` 层抽一个统一外壳 `Ui.Sheet`，`UnitSheet` 与 `Ui.ModalShell` 都改坐在它上面；（b）`BattleView` 五个阶段各自换版面，其中四个「换字」入口合并成一个 `DrawReplaceSheet`；（c）`GameRoot` 两个整屏从朴素居中卡改成稿上的整屏版面。绘制方法**原地不动**，只换布局与文案 —— 与轮一同一条落地方式。

**Tech Stack:** Unity 6000.5.2f1 / uGUI 纯代码搭建 / `Presentation` asmdef（可引用 UnityEngine）/ 文案走 `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json` / 字体子集 `tools/fonts/subset_fonts.py`

**Spec:** `docs/superpowers/specs/2026-08-30-战斗屏与局内流程接稿-design.md`（轮三小节）

---

## Global Constraints

以下每条对**每个任务**都成立，任务里不再重复：

- **`Core` 与 `Data` 禁止引用 UnityEngine**。本轮全部改动在 `Presentation`，不碰 Core/Data。
- **玩家可见 UI 文案一律走字符串表**，禁止硬编码中文字面量。key 必须以**字面量**形式出现在 `Strings.T("…")` 里 —— `StringsTableTests` 只认字面量，key 传变量会被判成孤儿。三元表达式要写成两个完整的 `Strings.T("字面量")` 调用。
- **新增/改动任何玩家可见中文后必须重跑字体子集**：`python3 tools/fonts/subset_fonts.py`，然后 `python3 -m pytest tools/fonts/tests/ -q`。**判定变没变要比 cmap，不能看 diff 大小**（重新生成必然产生几百 KB 时间戳 churn）。
- **改完 `Presentation` 必跑离线编译**：`cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q`。只看 `error CS`，`warning MSB3245` 忽略。Presentation 一行自动化测试都没有，离线编译是唯一的门。
- **单位换算 1pt = 2.093 逻辑单位**（iPhone 16 Pro Max 横屏 932×430pt；CanvasScaler 1600×900 按高匹配 → 实际逻辑画布 1950×900）。**换算结果写进代码时要在常量旁注明稿上的 pt 值**。
- **稿是唯一权威**，但本计划 Task 7 列出的三处「稿比代码旧」是例外 —— 那三处**改稿不改码**，理由见该任务。
- **凡是不可逆的，都要在按下去之前说清楚**（稿上定死的贯穿规矩）：安全层把「深入 / 撤退」的取舍写在钮下面而不是一行小字，换字把「永久失去」做成标红告警条。
- 提交信息用 conventional commits（`feat`/`fix`/`docs`/`chore` + 范围），中文正文。
- 每个任务结束**交付一次可试玩的提交**，不攒到最后（稿上写明的风险缓解：版面回归没有自动化兜底）。

## 两条已定裁定（执行时不要再纠结）

**裁定 A — `Dialogs.dc.html` 与 `Popups.dc.html` 是规范板，不是设备屏，不做 pt 换算。**
两张板的 `.sheet` 都是 1180px 宽的说明页，不是 932×430pt 的设备画布；`Dialogs` 自己那行注释也承认「.dlg 268px 代表 296pt」—— 板上的 px 与 pt 不是同一把尺。所以从这两张板取的是**结构、文案、规矩**（描边卡 / `dismissable: false` 的适用面 / 同屏只留一个弹窗 / 不可逆标红 / 飘字配色口径 / 全屏闪只有两处），**不取字号与尺寸**。`Ui.Modal` 现有的 24 / 17 / 18 三个字号原样保留。
代价若判错：弹窗字号与稿的观感差一档，人工试玩时一眼能看出来，改三个常量即可。

**裁定 B — 三张稿比代码旧，Task 7 改稿不改码。**
`SafeLayer` / `Settle` / `RunEnd` 三张写着「墨锭减半」「这 340 墨还不是你的」，而**半额 2026-08-30 已取消**（见 `GameRoot.SettleTower` 的注释：「半额取消后每笔收入都是赚到即结」）；`Popups` 写着直伤一律朱砂 + 18 种 + 两个下角相生相克环图，而 8-30/8-31 已改成**按五行分色**、实际 19 种（多一条 `enemy_mend`）、相生环图轮一已撤；`Reward` 的候选里画了「刂」部件，而 **2026-08-04 起战利品不再给部件**（`RunEngine` 注释）。这三处都是后来的用户拍板推翻了稿，不是实现走偏。
代价若判错：稿被改成了错的，将来照稿实现会走偏 —— 所以 Task 7 每一处回填都要写明日期与推翻它的那条决策出处。

---

## 文件结构

| 文件 | 本轮责任 | 任务 |
| --- | --- | --- |
| `Brushblade/Assets/_Project/Presentation/UI/Ui.cs` | 新增 `Ui.Sheet` 统一外壳；`ModalShell` 改坐在它上面并换成描边卡 | T1 |
| `Brushblade/Assets/_Project/Presentation/UI/UnitSheet.cs` | `Show` 的外壳段改调 `Ui.Sheet`，行为不变 | T1 |
| `Brushblade/Assets/_Project/Presentation/UI/Theme.cs` | 新增 `ScrimSoft`（局内浮层用的浅遮罩）与 `ScrimPaper`（段末横幅的纸色罩） | T1 |
| `Brushblade/Assets/_Project/Presentation/BattleView.cs` | 四个换字入口合并成 `DrawReplaceSheet`；战利品/复活/奇遇/段末横幅四处换版面 | T2–T5 |
| `Brushblade/Assets/_Project/Presentation/GameRoot.cs` | `ShowSafeLayer` / `ShowTowerSettle` 两个整屏换版面 | T6 |
| `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json` | 新增/改写各屏文案 key | T2–T6 |
| `docs/design/ui/scenes/{SafeLayer,Settle,Reward,Popups}.dc.html` | 三处过期记载回填 | T7 |
| `docs/design/ui/scenes/README.md` | 8 块从「稿子先行」转「已接线」 | T7 |
| `Brushblade/Assets/_Project/Presentation/Fonts/Resources/*.ttf` + `tools/fonts/charset.txt` | 字体子集重跑 | T2–T6 每任务、T8 复核 |

## 换算表（照抄，不要重算）

| 稿上 pt | 逻辑单位 | 用在 |
| --- | --- | --- |
| 62 × 78（`.tile`） | **130 × 163** | 战利品/换字的字牌 |
| 46 × 58（`.tile.sm`） | **96 × 121** | 奇遇部件池的小牌 |
| 640 | **1340** | SafeLayer `.panel` 宽 |
| 560 | **1172** | Settle `.panel` 宽 |
| 620 | **1298** | Reward `.sheet` 宽 |
| 780 | **1633** | Replace `.sheet` 宽 |
| 124 × 44（`.opt`） | **260 × 92** | 奇遇选项钮 |
| 40（`.pill` 高） | **84** | 通用胶囊钮 |
| 48（RunEnd `.pill` 高） | **100** | 段末大钮 |
| 30（小钮高） | **63** | 「不要了，开拔」「算了，不换」 |

字号：`9 → 19`、`9.5 → 20`、`10 → 21`、`10.5 → 22`、`11 → 23`、`12 → 25`、`12.5 → 26`、`13 → 27`、`14 → 29`、`15 → 31`、`16 → 33`、`17 → 36`、`19 → 40`、`21 → 44`、`22 → 46`、`34 → 71`、`40 → 84`、`44 → 92`。

---

### Task 1: `Ui.Sheet` 统一外壳

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/UI/Theme.cs`（`Scrim` 那一行附近，约 43 行）
- Modify: `Brushblade/Assets/_Project/Presentation/UI/Ui.cs:167-216`（`ModalShell` / `Modal` / `Alert`）
- Modify: `Brushblade/Assets/_Project/Presentation/UI/UnitSheet.cs:57-125`（`Show` 的外壳段）

**Interfaces:**
- Produces（后续任务全部依赖）：
  ```csharp
  // Ui.cs
  public static GameObject Sheet(Transform root, string name, float width, float height,
      bool dismissable, Color scrim, out Transform content);
  public static GameObject Sheet(Transform root, string name, float width, float height,
      bool dismissable, out Transform content);   // scrim 默认 Theme.Scrim
  // Theme.cs
  public static readonly Color ScrimSoft;    // 局内浮层：底下战场留半张脸
  public static readonly Color ScrimPaper;   // 段末横幅：纸色罩不是墨色罩
  ```
- Consumes：无（本轮第一个任务）。

**背景（实现者必读）：** 现在有两套外壳各写各的 —— `Ui.ModalShell`（墨遮罩 + **无边**的 `CardPanel` + 12 间距 VStack + 24 号标题）和 `UnitSheet.Show` 里手写的一份（`Theme.Scrim` + `Ui.OutlinedPanel(PanelPaper, PanelBorder, 18, 1.5f)` + 吃点击的 Button + 带 padding 的 VStack）。`Dialogs.dc.html` 要求两者是同一套：**宣纸圆角卡带 1pt 描边**。`UnitSheet` 那一份已经是对的，所以做法是把它抽出来，让 `ModalShell` 也用。

- [ ] **Step 1: Theme 加两个遮罩色**

在 `Theme.cs` 的 `public static readonly Color Scrim = new(0.088f, 0.105f, 0.132f, 0.55f);` 这一行**紧接着**加：

```csharp
        /// <summary>局内浮层的浅遮罩(稿 rgba(22,27,34,.42))。战利品/换字这几张浮在战斗屏上,
        /// 底下那半张脸要留着 —— 玩家才知道自己还在第几层、字库里有什么。
        /// 全遮死就成了「不知从哪冒出来的窗」(Reward.dc.html / Replace.dc.html 的原话)。</summary>
        public static readonly Color ScrimSoft = new(0.086f, 0.106f, 0.133f, 0.42f);
        /// <summary>段末横幅的**纸色**罩(稿 rgba(246,241,231,.72))。胜负横幅压的是自家宣纸底,
        /// 不是墨色 —— 墨罩会把战场压成深色,与「本段告捷」的明快读感相反(RunEnd.dc.html)。</summary>
        public static readonly Color ScrimPaper = new(0.965f, 0.945f, 0.906f, 0.72f);
```

- [ ] **Step 2: Ui.cs 加 `Sheet`**

在 `Ui.cs` 的 `ModalShell` 方法**之前**插入：

```csharp
        /// <summary>统一浮层外壳(2026-09-01,轮三 Task 1):遮罩 + 宣纸描边卡 + 带内边距的竖排容器。
        ///
        /// `Dialogs.dc.html` 定死了一条:全站弹窗是**同一套**外壳 —— 半透遮罩 + 宣纸圆角卡
        /// **带 1pt 描边**(#DED7C9)+ 内容。没有那条描边,浅色卡会直接融进同样浅色的宣纸底。
        /// 此前 <see cref="ModalShell"/> 用的是无边 <see cref="CardPanel"/>,而 UnitSheet 自己
        /// 手写了一份带描边的 —— 两套外壳各写各的,正是这次要收掉的东西。
        ///
        /// **同屏只留一个**:建之前先销毁 <paramref name="root"/> 下的同名节点。稿上的原话是
        /// 「新的弹出前先销毁旧的,否则叠成一摞、点不到底下那层」。调用方因此不必自己 Destroy,
        /// 传同一个 <paramref name="name"/> 反复调即可。
        ///
        /// ⚠ 吃点击的 Button 必须挂在**外层**、targetGraphic 指向 <c>face</c>:
        /// <see cref="OutlinedPanel"/> 对 face 无条件设了 raycastTarget = false,
        /// 挂在 face 身上的 Button 永远吃不到点击,点击会穿透下去命中遮罩的关闭按钮。</summary>
        /// <param name="dismissable">点遮罩是否关闭。必须做出选择的流程(战利品、换字)传 false。</param>
        public static GameObject Sheet(Transform root, string name, float width, float height,
            bool dismissable, Color scrim, out Transform content)
        {
            var stale = root.Find(name);
            if (stale != null) UnityEngine.Object.Destroy(stale.gameObject);

            var overlay = new GameObject(name, typeof(RectTransform), typeof(Image));
            overlay.transform.SetParent(root, false);
            var mask = overlay.GetComponent<Image>();
            mask.color = scrim;
            Stretch((RectTransform)overlay.transform);
            var maskButton = overlay.AddComponent<Button>();
            maskButton.targetGraphic = mask;
            if (dismissable)
                maskButton.onClick.AddListener(() => UnityEngine.Object.Destroy(overlay));

            var outer = OutlinedPanel(overlay.transform, "Card", Theme.PanelPaper, Theme.PanelBorder,
                SheetRadius, SheetBorder, out var face);
            Anchor((RectTransform)outer.transform, new Vector2(0.5f, 0.5f), new Vector2(0.5f, 0.5f),
                new Vector2(-width / 2f, -height / 2f), new Vector2(width / 2f, height / 2f));
            outer.gameObject.AddComponent<Button>().targetGraphic = face;

            var stack = VStack(face.transform, "Stack", SheetSpacing);
            Stretch((RectTransform)stack.transform);
            var layout = stack.GetComponent<VerticalLayoutGroup>();
            layout.childAlignment = TextAnchor.UpperCenter;
            layout.padding = new RectOffset(SheetPad, SheetPad, SheetPad, SheetPad);
            content = stack.transform;
            return overlay;
        }

        public static GameObject Sheet(Transform root, string name, float width, float height,
            bool dismissable, out Transform content) =>
            Sheet(root, name, width, height, dismissable, Theme.Scrim, out content);

        private const int SheetRadius = 18;    // 稿 9pt 圆角
        private const float SheetBorder = 1.5f; // 稿 1pt 描边
        private const float SheetSpacing = 14f;
        private const int SheetPad = 24;
```

- [ ] **Step 3: `ModalShell` 改坐在 `Sheet` 上**

把 `Ui.cs` 里 `ModalShell` 的**整个方法体**替换成：

```csharp
        public static GameObject ModalShell(Transform root, string title, Vector2 halfSize,
            bool dismissable, out Transform content)
        {
            var overlay = Sheet(root, "Modal", halfSize.x * 2f, halfSize.y * 2f,
                dismissable, out content);
            ThemedLabel(content, title, 24, Theme.TextMain, Theme.TitleFont);
            return overlay;
        }
```

⚠ 三处行为差异，都是**故意的**，别当 bug 修回去：
1. 卡片从无边 `CardPanel` 变成 `OutlinedPanel`（稿要求）。
2. 内容多了 24 的四边内边距（此前是 `Stretch` 顶满卡边）。
3. 同屏只留一个 `"Modal"` —— `BattleView` 里那些 `if (_modal != null) Object.Destroy(_modal);` 现在是冗余的，但**这个任务不要删它们**（`_modal` 字段还被别处读，删是 Task 2 的事）。

- [ ] **Step 4: `UnitSheet.Show` 改调 `Ui.Sheet`**

`UnitSheet.cs` 里，把从 `var overlay = new GameObject(SheetName, typeof(RectTransform), typeof(Image));` 起到 `stackLayout.padding = new RectOffset(...)` 止的整段（约 76–105 行）替换成：

```csharp
            var overlay = Ui.Sheet(root, SheetName, SheetWidth, SheetHeight,
                dismissable: true, out var stackTransform);
```

并把随后三行 `BuildHeader(stack.transform, …)` / `BuildBody(stack.transform, …)` / `BuildFoot(stack.transform, …)` 的 `stack.transform` 全部换成 `stackTransform`。

⚠ `Show` 顶部那段「记下旧实例的滚动位置再销毁它」的代码**要保留**，但销毁那一行 `Object.Destroy(previous.gameObject);` 要**删掉** —— `Ui.Sheet` 现在自己会销毁同名节点。改成：

```csharp
            Vector2[] savedScroll = null;
            var previous = root.Find(SheetName);
            if (previous != null)
            {
                var previousScrolls = previous.GetComponentsInChildren<ScrollRect>();
                savedScroll = new Vector2[previousScrolls.Length];
                for (int i = 0; i < previousScrolls.Length; i++)
                    savedScroll[i] = previousScrolls[i].normalizedPosition;
                // 销毁交给 Ui.Sheet(它对同名节点做同一件事),这里只负责把滚动位置抄下来
            }
```

`SheetPadding` / `RootSpacing` 两个常量在 `UnitSheet` 里如果没有别的引用点了就删掉（`Ui.Sheet` 的 24 / 14 与它们同值，是刻意对齐的）；若还有引用就留着。

- [ ] **Step 5: 编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```
Expected：`Build succeeded`，0 个 `error CS`。

- [ ] **Step 6: 全量回归（本任务没动 Core，但外壳是全站共用的，跑一遍兜底）**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```
Expected：`Failed: 0`。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation/UI/Ui.cs Brushblade/Assets/_Project/Presentation/UI/UnitSheet.cs Brushblade/Assets/_Project/Presentation/UI/Theme.cs
git commit -m "refactor(ui): 抽出 Ui.Sheet 统一浮层外壳,弹窗卡按稿补上 1pt 描边"
```

---

### Task 2: 换字四合一 —— `DrawReplaceSheet`

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`（`DrawDropChoiceStep` 2975、`DrawRewardReplaceStep` 3202、`DrawReviveReplaceStep` 3308、`DrawEventReplaceStep` 3496）
- Modify: `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`

**Interfaces:**
- Consumes：`Ui.Sheet(Transform, string, float, float, bool, Color, out Transform)`、`Theme.ScrimSoft`（Task 1）
- Produces：
  ```csharp
  private void DrawReplaceSheet(string title, string incoming,
      System.Collections.Generic.IReadOnlyList<string> library,
      System.Action<int> onPick, System.Action onCancel);
  private const float ReplaceSheetW = 1633f;
  private const float TileW = 130f;   // 后续任务复用
  private const float TileH = 163f;
  ```

**背景：** `Replace.dc.html` 的 `.sources` 那一行把话挑明了 —— 四个入口（战利品 / 回合掉字 / 奇遇 / 广告复活）**共用这一版**。实现侧现在是四个近乎逐字重复的方法：都建一个 `ModalShell(…, new Vector2(360, 240), dismissable: false, …)`，都画一行 `battle.common.replace_warning`，都每 4 个一行铺字牌，都挂一个「算了不换」。差异只有三处：标题 key、字库来自 `Battle.Library` 还是 `_run.CarriedLibrary`、点中之后调哪个 Core 方法。

稿上的关键差异（这是这一版要做出来的东西）：
- **不换行**。稿的 780pt 是算出来的：9 张牌 ×62 + 8 道 7pt 间隙 = 614，加上来牌 62 + 箭头 22 + 两道 9pt 间隙 = 102，再加左右各 20 内边距 → 756，取 780。稿的原话：「窄一档就会折行，最后一张孤零零掉到第二排，读起来像『还有别的选项』」。所以**不要保留 `i % 4 == 0` 的换行**。
- **来牌在左，箭头，字库在右**，一横排读完。
- 警告条是**标红胶囊**（`.warn`：底 #FBE3E2、字 #9B1E22、加粗），不是一行灰色小字。
- 遮罩用 `Theme.ScrimSoft`，底下战场留半张脸。

- [ ] **Step 1: 加常量与警告条色**

在 `BattleView.cs` 里现有的尺寸常量区（`FieldContentWidth` 附近）加：

```csharp
        // 换字面板(稿 Replace.dc.html)。780pt 是算出来的不是拍的:字库满员 9 张 ×62
        // + 8 道 7pt 间隙 = 614,加来牌 62 + 箭头 22 + 两道 9pt 间隙 = 102,再加左右各 20
        // 内边距 → 756,取 780。窄一档就会折行,最后一张孤零零掉到第二排,读起来像
        // 「还有别的选项」。
        private const float ReplaceSheetW = 1633f;   // 稿 780pt
        private const float ReplaceSheetH = 460f;
        private const float TileW = 130f;            // 稿 .tile 62pt
        private const float TileH = 163f;            // 稿 .tile 78pt
        private const float TileGap = 15f;           // 稿 7pt
```

在 `Theme.cs` 加两个色（`Cinnabar` 附近）：

```csharp
        public static readonly Color WarnBg = new(0.984f, 0.890f, 0.886f);   // 稿 #FBE3E2:不可逆告警条底
        public static readonly Color WarnText = new(0.607f, 0.117f, 0.133f); // 稿 #9B1E22:告警条字色
```

- [ ] **Step 2: 写 `DrawReplaceSheet`**

在 `DrawDropChoiceStep` **之前**插入：

```csharp
        /// <summary>「字库已满,换掉哪一张」的唯一一版(2026-09-01,轮三 Task 2)。
        /// 稿 <c>Replace.dc.html</c> 的 <c>.sources</c> 那行写明四个入口共用这一版:
        /// 战利品 / 回合掉字 / 奇遇 / 广告复活。此前是四个近乎逐字重复的方法。
        ///
        /// **一横排不折行**:字牌铺在一行里,来牌在左、箭头、字库在右。稿的宽度就是按
        /// 「满员 9 张不折行」算出来的 —— 折行之后最后一张孤零零掉到第二排,读起来像
        /// 「还有别的选项」。
        ///
        /// **警告条标红**:这是全局内唯一一处不可逆的删除,按贯穿全轮的那条规矩
        /// (凡是不可逆的都要在按下去之前说清楚),做成标红胶囊而不是一行灰色小字。</summary>
        /// <param name="onPick">玩家点了字库里第 N 张。调用方负责调 Core 并善后。</param>
        private void DrawReplaceSheet(string title, string incoming,
            System.Collections.Generic.IReadOnlyList<string> library,
            System.Action<int> onPick, System.Action onCancel)
        {
            _modal = Ui.Sheet(transform, "ReplaceSheet", ReplaceSheetW, ReplaceSheetH,
                dismissable: false, Theme.ScrimSoft, out var content);
            Ui.ThemedLabel(content, title, 33, Theme.TextMain, Theme.TitleFont);

            var warn = Ui.Chip(content, Strings.T("battle.reward.replace_hint",
                ("count", library.Count), ("capacity", Battle.LibraryCapacity)),
                Theme.WarnBg, Theme.WarnText, 21, padX: 23, padY: 12);
            Ui.Sized(warn, height: 46);   // 稿 .warn 22pt

            var row = Ui.Row(content, "Incoming", 19);   // 稿 .incoming gap 9pt
            row.GetComponent<HorizontalLayoutGroup>().childAlignment = TextAnchor.MiddleCenter;
            Ui.GlyphTile(row.transform, _graph.Get(incoming), true, null, new Vector2(TileW, TileH));
            var arrow = Ui.ThemedLabel(row.transform, "→", 40, Theme.LockGray);
            Ui.Sized(arrow.gameObject, width: 46, height: 29);   // 稿 .arrow 22×14pt

            var lib = Ui.Row(row.transform, "Library", TileGap);
            lib.GetComponent<HorizontalLayoutGroup>().childAlignment = TextAnchor.MiddleCenter;
            for (int i = 0; i < library.Count; i++)
            {
                int replaceIndex = i;
                Ui.GlyphTile(lib.transform, _graph.Get(library[i]), false,
                    () => onPick(replaceIndex), new Vector2(TileW, TileH));
            }

            Ui.PillButton(content, Strings.T("battle.btn.replace_cancel"), () => onCancel(),
                Theme.LockedBg, Theme.TextMain, 25, new Vector2(300, 63));
        }
```

⚠ `Ui.GlyphTile` 的 `onClick` 传 `null` 是可以的（来牌不可点，只是展示）—— 若 `GlyphTile` 内部对 null 有断言，改成传 `() => { }`。

- [ ] **Step 3: 四个入口改调它**

`DrawDropChoiceStep` 整个方法体换成：

```csharp
        private void DrawDropChoiceStep()
        {
            string incoming = Battle.PendingDrop;
            DrawReplaceSheet(
                Strings.T("battle.drop.replace_title", ("charId", incoming)), incoming, Battle.Library,
                replaceIndex =>
                {
                    string dropped = Battle.Library[replaceIndex];
                    if (Battle.ResolveDrop(replaceIndex) == BattleError.None)
                    {
                        _message = Strings.T("battle.common.replaced_msg", ("incoming", incoming), ("dropped", dropped));
                        if (_modal != null) Object.Destroy(_modal);
                    }
                    Refresh();
                },
                () =>
                {
                    Battle.SkipDrop();
                    if (_modal != null) Object.Destroy(_modal);
                    _message = Strings.T("battle.drop.skip_msg", ("charId", incoming));
                    Refresh();
                });
        }
```

⚠ 掉字这一路的取消钮文案原本是 `battle.btn.drop_skip`（「不要,跳过」）而不是 `battle.btn.replace_cancel`（「算了,不换」）—— 语义不同（弃掉掉落的字 vs 放弃这次替换）。`DrawReplaceSheet` 因此要多一个参数：

```csharp
        private void DrawReplaceSheet(string title, string incoming,
            System.Collections.Generic.IReadOnlyList<string> library,
            System.Action<int> onPick, System.Action onCancel, string cancelLabel)
```
末行改成 `Ui.PillButton(content, cancelLabel, …)`。四个入口分别传 `Strings.T("battle.btn.drop_skip")`、`Strings.T("battle.btn.replace_cancel")`、`Strings.T("battle.btn.revive_replace_cancel")`、`Strings.T("battle.btn.replace_cancel")`。

`DrawRewardReplaceStep`：

```csharp
        private void DrawRewardReplaceStep()
        {
            var incoming = _run.RewardOptions[_pendingRewardIndex];
            DrawReplaceSheet(
                Strings.T("battle.reward.replace_title", ("charId", incoming)), incoming, _run.CarriedLibrary,
                replaceIndex =>
                {
                    string dropped = _run.CarriedLibrary[replaceIndex];
                    if (_run.PickRewardReplacing(_pendingRewardIndex, replaceIndex))
                    {
                        _pendingRewardIndex = -1;
                        _tutorial?.Notify(TutorialAction.PickReward);
                        _message = Strings.T("battle.reward.replaced_in_msg", ("incoming", incoming), ("dropped", dropped));
                        MarkFresh(incoming);
                        CancelSelection();
                    }
                },
                () => { _pendingRewardIndex = -1; Refresh(); },
                Strings.T("battle.btn.replace_cancel"));
            DrawRewardAdBadge(_modal.transform.Find("Card/Face/Stack"));
        }
```

⚠ `DrawRewardAdBadge` 必须画进弹窗内容里（它现在收 `Transform content`）。为了不靠 `Find` 路径字符串，把 `DrawReplaceSheet` 的返回值改成 `Transform`（内容容器），四处按需接住：

```csharp
        private Transform DrawReplaceSheet(…)   // 末尾 return content;
```
然后这里写 `var content = DrawReplaceSheet(…); DrawRewardAdBadge(content);`。

`DrawReviveReplaceStep`：

```csharp
        private void DrawReviveReplaceStep()
        {
            string incoming = _run.RewardOptions[_pendingReviveIndex];
            DrawReplaceSheet(
                Strings.T("battle.revive.replace_title", ("charId", incoming)), incoming, Battle.Library,
                replaceIndex =>
                {
                    string dropped = Battle.Library[replaceIndex];
                    if (_run.PickReviveCharReplacing(_pendingReviveIndex, replaceIndex))
                        _message = Strings.T("battle.common.replaced_msg", ("incoming", incoming), ("dropped", dropped));
                    _pendingReviveIndex = -1;
                    Refresh();
                },
                () => { _pendingReviveIndex = -1; Refresh(); },
                Strings.T("battle.btn.revive_replace_cancel"));
        }
```

`DrawEventReplaceStep`：

```csharp
        private void DrawEventReplaceStep(EventOption option)
        {
            string incoming = _pendingCharChoice >= 0
                ? option.GainCharChoices[_pendingCharChoice] : option.GainChar;
            DrawReplaceSheet(
                Strings.T("battle.event.replace_title", ("charId", incoming)), incoming, _run.CarriedLibrary,
                replaceIndex =>
                {
                    string dropped = _run.CarriedLibrary[replaceIndex];
                    var picks = _eventPicks.Count > 0 ? _eventPicks.ToArray() : null;
                    var beforeHoldings = SnapshotHoldings();
                    if (_run.ChooseEventOption(_pendingEventOption, picks, _pendingCharChoice, replaceIndex))
                    {
                        _message = Strings.T("battle.event.trade_replaced_msg", ("incoming", incoming), ("dropped", dropped));
                        MarkFreshSince(beforeHoldings);
                        if (_modal != null) Object.Destroy(_modal);
                        ResetEventSelection();
                        CancelSelection();
                        return;
                    }
                    Refresh();
                },
                () =>
                {
                    if (_modal != null) Object.Destroy(_modal);
                    ResetEventSelection();
                    _message = Strings.T("battle.event.trade_cancel_msg");
                    Refresh();
                },
                Strings.T("battle.btn.replace_cancel"));
        }
```

⚠ 复活那一路此前赋的是 `_rewardModal` 而非 `_modal`。`DrawReplaceSheet` 统一写 `_modal`，所以 `DrawReviveCharStep` 里那句 `if (_rewardModal != null) Destroy(_rewardModal);` 在转入替换子步之前要保留（它清的是选字页那张）。

- [ ] **Step 4: 删掉 `battle.common.replace_warning`（若已无引用）**

```bash
grep -rn "replace_warning" Brushblade/Assets/_Project/
```
若结果为空，从 `strings.zh-CN.json` 删掉该 key —— `StringsTableTests` 会把没有调用点的 key 判成孤儿。

- [ ] **Step 5: 编译 + 字符串表测试**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```
```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter StringsTable
```
Expected：两者都绿。

- [ ] **Step 6: 字体子集**

```bash
python3 tools/fonts/subset_fonts.py && python3 -m pytest tools/fonts/tests/ -q
```
本任务只删文案不加新字，cmap 预期**不变**或只减。用下面这条核对，别看 diff 大小：
```bash
python3 -c "from fontTools.ttLib import TTFont; import glob; [print(f, len(TTFont(f).getBestCmap())) for f in sorted(glob.glob('Brushblade/Assets/_Project/Presentation/Fonts/Resources/*.ttf'))]"
```

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation/BattleView.cs Brushblade/Assets/_Project/Presentation/UI/Theme.cs Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json Brushblade/Assets/_Project/Presentation/Fonts/Resources tools/fonts/charset.txt
git commit -m "feat(ui): 四个换字入口合并成一版,按稿改横排不折行 + 标红告警条"
```

---

### Task 3: 战利品与复活补给选字页

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`（`DrawRewardCharStep` 3120、`DrawReviveCharStep` 3263、`DrawRewardAdBadge` 3245）
- Modify: `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`

**Interfaces:**
- Consumes：`Ui.Sheet`、`Theme.ScrimSoft`（T1）；`TileW` / `TileH`（T2）
- Produces：`private Transform DrawPickSheet(string title, string hint, out Transform picksRow, out Transform detailBar, out Transform footRow);`

**背景：** `Reward.dc.html` 的关键改动是**效果说明的位置** —— 稿上刻意把「效果」放在牌**下面**的一条 `.detail` 横条里，而不是牌上、也不是标题那一行。原话：「牌面只有 62px 宽，塞进一句效果必然截断」。实现侧现在把 `Brief(...)` 拼进标题下的那行 `ThemedLabel`，一长就顶宽。

版面（自上而下）：标题 → hint 行 → 一排候选牌（每张牌**下面**一行小字「火 · 蓝」）→ detail 条（选中时显示效果，右上角浮「再点一次收下」）→ foot 行（左：广告徽章；右：「不要了，开拔」）。

- [ ] **Step 1: 加常量**

```csharp
        private const float PickSheetW = 1298f;   // 稿 Reward.dc.html .sheet 620pt
        private const float PickSheetH = 520f;
        private const float PickDetailH = 63f;    // 稿 .detail min-height 30pt
```

- [ ] **Step 2: 加共用骨架 `DrawPickSheet`**

在 `DrawRewardCharStep` 之前插入：

```csharp
        /// <summary>战利品 / 复活补给的共用版面(2026-09-01,轮三 Task 3,稿 Reward.dc.html)。
        /// 两页只差标题与回调,版面一模一样。
        ///
        /// **效果说明在牌下面,不在牌上、也不在标题行**:稿上的原话是「牌面只有 62px 宽,
        /// 塞进一句效果必然截断」。所以留一条定高的 detail 横条,未选中时是 hint、
        /// 选中时换成这张牌的效果 + 右浮「再点一次收下」。定高是为了**选中前后不跳版**。</summary>
        private Transform DrawPickSheet(string title, string hint,
            out Transform picksRow, out Transform detailBar, out Transform footRow)
        {
            _rewardModal = Ui.Sheet(transform, "PickSheet", PickSheetW, PickSheetH,
                dismissable: false, Theme.ScrimSoft, out var content);
            Ui.ThemedLabel(content, title, 33, Theme.TextMain, Theme.TitleFont);
            Ui.ThemedLabel(content, hint, 21, Theme.TextDim);

            var picks = Ui.Row(content, "Picks", 21);   // 稿 .picks gap 10pt
            picks.GetComponent<HorizontalLayoutGroup>().childAlignment = TextAnchor.MiddleCenter;
            picksRow = picks.transform;

            var detail = Ui.OutlinedPanel(content, "Detail", Theme.PaperDim, Theme.PanelBorder, 17, 2f, out var face);
            Ui.Sized(detail.gameObject, width: PickSheetW - 48f, height: PickDetailH).minHeight = PickDetailH;
            var detailStack = Ui.Row(face.transform, "DetailRow", 12);
            detailStack.GetComponent<HorizontalLayoutGroup>().childAlignment = TextAnchor.MiddleLeft;
            detailStack.GetComponent<HorizontalLayoutGroup>().padding = new RectOffset(23, 23, 0, 0);
            Ui.Stretch((RectTransform)detailStack.transform);
            detailBar = detailStack.transform;

            var foot = Ui.Row(content, "Foot", 21);
            foot.GetComponent<HorizontalLayoutGroup>().childAlignment = TextAnchor.MiddleLeft;
            Ui.Sized(foot, width: PickSheetW - 48f);
            footRow = foot.transform;
            return content;
        }

        /// <summary>候选牌 + 牌下那行「火 · 蓝」(稿 .pick .name)。</summary>
        private void PickTile(Transform parent, string charId, bool selected, System.Action onTap)
        {
            var def = _graph.Get(charId);
            var column = Ui.VStack(parent, $"Pick_{charId}", 8);
            var button = Ui.GlyphTile(column.transform, def, selected, onTap, new Vector2(TileW, TileH));
            HoldToPreview.Attach(button.gameObject, () => ShowCharPreview(charId));
            Ui.ThemedLabel(column.transform,
                Strings.T("battle.reward.pick_name",
                    ("element", CharInfo.ElementName(def.Element)),
                    ("rarity", CharInfo.RarityName(def.Rarity))),
                19, Theme.TextDim);
        }
```

⚠ `CharDef` 的稀有度字段若不叫 `Rarity`，用 `grep -n "CardRarity" Brushblade/Assets/_Project/Core/CharDef.cs` 查出真名再写。

- [ ] **Step 3: 新增两条 strings**

在 `strings.zh-CN.json` 按**字母序**插入：

```json
  "battle.reward.detail_line": "{charId} ｜ {brief}",
  "battle.reward.pick_name": "{element} · {rarity}",
```

- [ ] **Step 4: `DrawRewardCharStep` 换版面**

方法体替换成：

```csharp
        private void DrawRewardCharStep()
        {
            var content = DrawPickSheet(
                Strings.T("battle.reward.pick_title", ("left", _run.CharPicksLeft)),
                Strings.T("battle.reward.pick_hint",
                    ("count", _run.CarriedLibrary.Count), ("capacity", Battle.LibraryCapacity)),
                out var picksRow, out var detailBar, out var footRow);

            for (int i = 0; i < _run.RewardOptions.Count; i++)
            {
                int index = i;
                var id = _run.RewardOptions[i];
                GameObject tile = null;
                System.Action tap = () =>
                {
                    if (_previewRewardIndex != index) { _previewRewardIndex = index; Refresh(); return; }
                    _previewRewardIndex = -1;
                    bool hasFrom = tile != null;
                    Vector3 fromPos = hasFrom ? tile.transform.position : default;
                    if (_run.PickReward(index))
                    {
                        _tutorial?.Notify(TutorialAction.PickReward);
                        _message = Strings.T("battle.reward.added_msg", ("charId", id));
                        MarkFresh(id);
                        CancelSelection();
                        FlyIntoLibrary(id, hasFrom, fromPos);
                        return;
                    }
                    bool libraryFull = _run.CarriedLibrary.Count >= Battle.LibraryCapacity;
                    Debug.Log($"[战利品诊断] Phase={_run.Phase} 额度={_run.CharPicksLeft} " +
                              $"携带字数={_run.CarriedLibrary.Count} 显示容量={Battle.LibraryCapacity} " +
                              $"已扩容={_run.LibraryExpanded} 判定满库={libraryFull}");
                    if (libraryFull) _pendingRewardIndex = index;
                    else
                        _message = Strings.T("battle.reward.rejected_detail",
                            ("charId", id), ("count", _run.CarriedLibrary.Count),
                            ("capacity", Battle.LibraryCapacity), ("left", _run.CharPicksLeft), ("phase", _run.Phase));
                    Refresh();
                };
                PickTile(picksRow, id, index == _previewRewardIndex, tap);
                tile = picksRow.GetChild(picksRow.childCount - 1).GetChild(0).gameObject;
            }

            if (_previewRewardIndex >= 0)
            {
                var picked = _run.RewardOptions[_previewRewardIndex];
                Ui.ThemedLabel(detailBar,
                    Strings.T("battle.reward.detail_line", ("charId", picked), ("brief", Brief(picked))),
                    21, Theme.TextMain, align: TextAnchor.MiddleLeft);
                Ui.ThemedLabel(detailBar, Strings.T("battle.reward.tap_again_suffix"), 19, Theme.CinnabarDark);
            }

            DrawRewardAdBadge(footRow);
            var spacer = Ui.Panel(footRow, "Spacer");
            Ui.Sized(spacer, flexWidth: 1f);
            Ui.RoundButton(footRow, Strings.T("battle.btn.reward_skip"), () =>
            {
                _previewRewardIndex = -1;
                _run.SkipReward();
                _tutorial?.Notify(TutorialAction.PickReward);
                _message = Strings.T("battle.reward.skip_msg");
                CancelSelection();
            }, Theme.LockedBg, Theme.TextMain, 25, new Vector2(280, 63));
        }
```

⚠ `battle.reward.tap_again_suffix` 现值是「｜再点一次收下」，带一个前导竖线（此前是拼在同一句尾）。改成独立标签后要把竖线去掉：把 `strings.zh-CN.json` 里该值改成 `"再点一次收下"`。同一个 key 还被 `DrawReviveCharStep` 用，一起受益。

⚠ 那句 `tile = picksRow.GetChild(...)` 靠子物体层级取 GameObject，脆。更稳的做法是让 `PickTile` 返回 `Button`：把签名改成 `private Button PickTile(...)` 并 `return button;`，然后写 `tile = PickTile(picksRow, id, index == _previewRewardIndex, tap).gameObject;`。**照后一种写。**

- [ ] **Step 5: `DrawReviveCharStep` 换同一版面**

```csharp
        private void DrawReviveCharStep()
        {
            if (_pendingReviveIndex >= 0 && Battle.Library.Count < Battle.LibraryCapacity)
                _pendingReviveIndex = -1;
            if (_pendingReviveIndex >= 0) { DrawReviveReplaceStep(); return; }

            DrawPickSheet(
                Strings.T("battle.revive.pick_title", ("left", _run.ReviveCharPicksLeft)),
                Strings.T("battle.reward.pick_hint",
                    ("count", Battle.Library.Count), ("capacity", Battle.LibraryCapacity)),
                out var picksRow, out var detailBar, out var footRow);

            for (int i = 0; i < _run.RewardOptions.Count; i++)
            {
                int index = i;
                var id = _run.RewardOptions[i];
                PickTile(picksRow, id, index == _previewRewardIndex, () =>
                {
                    if (_previewRewardIndex != index) { _previewRewardIndex = index; Refresh(); return; }
                    _previewRewardIndex = -1;
                    if (Battle.Library.Count >= Battle.LibraryCapacity) _pendingReviveIndex = index;
                    else if (_run.PickReviveChar(index))
                        _message = Strings.T("battle.reward.added_msg", ("charId", id));
                    Refresh();
                });
            }

            if (_previewRewardIndex >= 0)
            {
                var picked = _run.RewardOptions[_previewRewardIndex];
                Ui.ThemedLabel(detailBar,
                    Strings.T("battle.reward.detail_line", ("charId", picked), ("brief", Brief(picked))),
                    21, Theme.TextMain, align: TextAnchor.MiddleLeft);
                Ui.ThemedLabel(detailBar, Strings.T("battle.reward.tap_again_suffix"), 19, Theme.CinnabarDark);
            }

            var spacer = Ui.Panel(footRow, "Spacer");
            Ui.Sized(spacer, flexWidth: 1f);
            Ui.RoundButton(footRow, Strings.T("battle.btn.revive_skip"), () =>
            {
                _previewRewardIndex = -1;
                _run.SkipReviveReward();
                _message = Strings.T("battle.revive.skip_msg");
                CancelSelection();
            }, Theme.LockedBg, Theme.TextMain, 25, new Vector2(280, 63));
        }
```

- [ ] **Step 6: `DrawRewardAdBadge` 尺寸按稿**

把 `new Vector2(190, 44)` 改成 `new Vector2(280, 63)`（稿 `.adbadge` 高 30pt）。

- [ ] **Step 7: 编译 + 字符串表 + 字体子集**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```
```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```
```bash
python3 tools/fonts/subset_fonts.py && python3 -m pytest tools/fonts/tests/ -q
```

- [ ] **Step 8: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation/BattleView.cs Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json Brushblade/Assets/_Project/Presentation/Fonts/Resources tools/fonts/charset.txt
git commit -m "feat(ui): 战利品与复活补给按稿改版 —— 效果说明落到牌下的定高横条"
```

---

### Task 4: 奇遇页

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`（`DrawEvent` 3367 起、`DrawEventCharChoices` 3572、`DrawEventPoolPicker` 3621）

**Interfaces:**
- Consumes：`TileW` / `TileH`（T2）
- Produces：无对外接口。

**背景：** `Event.dc.html` 的版面是一整块**垂直居中**的 body：标题 → 正文 → 选项 → 部件池，说明留在屏幕最底的 `.say` 行。稿的原话：「奇遇没有战场要让位，整块内容居中才不会上半屏挤、下半屏空」，且「阅读顺序是硬要求：标题 → 正文 → 选项 → 说明」。

实现侧现在把这几件**散在三个不同的战场排**上：标题画 `_enemyFrontRow`、正文画 `_summonFrontRow`、选项画 `_centerRow`、部件池画在拆合台。阅读顺序碰巧是对的（那两排比 `_centerRow` 高），但它们之间隔着空排，读起来是散的。

改法：奇遇阶段**不用那三排**，在 `transform` 下另起一个居中的 `EventBody` 容器，四件依次排进去。屏底的说明行（`_message`）不动 —— 它已经在最底部通栏，正是稿的 `.say`。

- [ ] **Step 1: 加常量**

```csharp
        private const float EventTextW = 1172f;   // 稿 .text max-width 560pt
        private const float EventOptW = 260f;     // 稿 .opt 124pt
        private const float EventOptH = 92f;      // 稿 .opt 44pt
        private const float EventPartW = 96f;     // 稿 .tile.sm 46pt
        private const float EventPartH = 121f;    // 稿 .tile.sm 58pt
```

- [ ] **Step 2: `DrawEvent` 开头建居中容器**

在 `DrawEvent` 里，把这两句：

```csharp
            Ui.ThemedLabel(_enemyFrontRow, Strings.T("battle.event.title", ("eventName", evt.Id)), 30, Theme.TextMain, Theme.TitleFont);
            …
            Ui.ThemedLabel(_summonFrontRow, evt.Text, 18, Theme.TextDim);
```

替换成：

```csharp
            // 稿 Event.dc.html 的 .body:一整块垂直居中的内容,标题 → 正文 → 选项 → 部件池。
            // 此前这四件散在 _enemyFrontRow / _summonFrontRow / _centerRow / 拆合台四处 ——
            // 阅读顺序碰巧是对的(那两排比 _centerRow 高),但中间隔着空排,读起来是散的。
            // 稿的原话:「奇遇没有战场要让位,整块内容居中才不会上半屏挤、下半屏空」。
            // 效果说明**不在这里**:选中某个选项时才画进屏幕最底那条提示行(_message),
            // 与稿的 .say 同一条带。
            var body = Ui.VStack(transform, "EventBody", 17);
            Ui.Anchor((RectTransform)body.transform, new Vector2(0.08f, 0.10f), new Vector2(0.92f, 0.88f),
                Vector2.zero, Vector2.zero);
            body.GetComponent<VerticalLayoutGroup>().childAlignment = TextAnchor.MiddleCenter;

            Ui.ThemedLabel(body.transform, Strings.T("battle.event.title", ("eventName", evt.Id)),
                40, Theme.TextMain, Theme.TitleFont);
            var text = Ui.ThemedLabel(body.transform, evt.Text, 25, Theme.TextDim);
            text.horizontalOverflow = HorizontalWrapMode.Wrap;
            text.verticalOverflow = VerticalWrapMode.Overflow;
            Ui.Sized(text.gameObject, width: EventTextW,
                height: Ui.WrappedTextHeight(evt.Text, 25, EventTextW));
```

⚠ `EventBody` 是每次 `Refresh` 新建的，要在建之前销毁旧的 —— 照 `Ui.Sheet` 的做法在开头加：
```csharp
            var staleBody = transform.Find("EventBody");
            if (staleBody != null) Object.Destroy(staleBody.gameObject);
```

- [ ] **Step 3: 选项钮改画进 body 并按稿改高**

`DrawEvent` 后半段那个画选项钮的循环，把父节点从 `_centerRow` 改成一个建在 `body` 下的 `Ui.Row(body.transform, "Options", 25)`（稿 `.opts` gap 12pt），并把 `new Vector2(260, 72)` 改成 `new Vector2(EventOptW, EventOptH)`。

⚠ 稿明确写着「钮上**只放名称**」，且 `EventLabelWidthTests` 钉着这条 —— 不要把效果说明拼进 label。

- [ ] **Step 4: 部件池与选字改画进 body**

`DrawEventPoolPicker` / `DrawEventCharChoices` 里的父节点改成 body 下新建的 `Ui.Row(body.transform, "Pool", 17)`，牌尺寸改 `new Vector2(EventPartW, EventPartH)`。

两个方法现在收的是什么参数、往哪画，实现时先读一遍这两个方法（`BattleView.cs:3572` 与 `:3621`）；若它们内部自己找父节点，就加一个 `Transform parent` 参数由 `DrawEvent` 传入。

- [ ] **Step 5: 编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

- [ ] **Step 6: 回归（`EventLabelWidthTests` 必须仍绿）**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```
Expected：`Failed: 0`。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation/BattleView.cs
git commit -m "feat(ui): 奇遇页四件收进一块垂直居中的版面,不再散在三条战场排上"
```

---

### Task 5: 段末横幅

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`（`DrawBattleSettle` 3026、`ShowVictoryBanner` 3052、`DrawRunEnd` 3697）

**Interfaces:**
- Consumes：`Theme.ScrimPaper`（T1）
- Produces：无对外接口。

**背景：** `RunEnd.dc.html` 的横幅是**整屏纸色罩**（`rgba(246,241,231,.72)`）上一块居中的大字 + 一句 msg + 一个大钮，胜负只换文案与色。实现侧现在是**墨色横带**（只压中间那一条 0.42–0.62）+ 72/44 号字。稿的原话：「横幅是一整屏里字号最大的一处：它要在余光里就读得出胜负」。

- [ ] **Step 1: 加常量**

```csharp
        private const int BannerFont = 92;      // 稿 .banner 44pt
        private const int BannerMsgFont = 23;   // 稿 .msg 11pt
        private const float BannerPillH = 100f; // 稿 .pill 48pt
```

- [ ] **Step 2: `ShowVictoryBanner` 改整屏纸罩**

把方法体里从 `var banner = Ui.Panel(transform, "VictoryBanner");` 到 `Ui.Stretch(label.rectTransform);` 的那一段替换成：

```csharp
            // 纸色罩铺满整屏(稿 RunEnd.dc.html:rgba(246,241,231,.72))。此前是压在中间
            // 一条的墨色横带 —— 墨罩把战场压成深色,与「本段告捷」的明快读感相反,
            // 且大字只在那条带里,余光扫不到。整屏罩 + 居中大字才是稿要的读法。
            var banner = Ui.Panel(transform, "VictoryBanner");
            Ui.Stretch((RectTransform)banner.transform);
            var scrim = banner.AddComponent<Image>();
            scrim.color = Theme.ScrimPaper;
            var group = banner.AddComponent<CanvasGroup>();
            group.blocksRaycasts = false;   // 只是提示,不拦点击

            var wrap = Ui.VStack(banner.transform, "Wrap", 31);   // 稿 .wrap gap 15pt
            Ui.Stretch((RectTransform)wrap.transform);
            wrap.GetComponent<VerticalLayoutGroup>().childAlignment = TextAnchor.MiddleCenter;
            Ui.ThemedLabel(wrap.transform,
                boss ? Strings.T("battle.phase.boss_broken_banner") : Strings.T("battle.phase.floor_cleared_banner"),
                BannerFont, boss ? Theme.CinnabarDark : Theme.TextMain, Theme.TitleFont);
```

- [ ] **Step 3: `DrawRunEnd` 按稿改**

方法体替换成：

```csharp
        private void DrawRunEnd()
        {
            bool won = _run.Phase == RunPhase.RunWon;
            bool tower = _onExit != null; // 无尽:胜=Boss 层告捷进安全层,负=塔结算

            // 稿 RunEnd.dc.html:整屏纸罩 + 横幅 + 一句 msg + 一个大钮,胜负只换文案与色。
            var stale = transform.Find("RunEndBanner");
            if (stale != null) Object.Destroy(stale.gameObject);
            var overlay = Ui.Panel(transform, "RunEndBanner");
            Ui.Stretch((RectTransform)overlay.transform);
            overlay.AddComponent<Image>().color = Theme.ScrimPaper;
            var wrap = Ui.VStack(overlay.transform, "Wrap", 31);
            Ui.Stretch((RectTransform)wrap.transform);
            wrap.GetComponent<VerticalLayoutGroup>().childAlignment = TextAnchor.MiddleCenter;

            Ui.ThemedLabel(wrap.transform, won
                    ? (tower ? Strings.T("battle.phase.run_won_tower_banner") : Strings.T("battle.phase.run_won_stage_banner"))
                    : Strings.T("battle.phase.defeat_banner"),
                BannerFont, won ? Theme.TextMain : Theme.CinnabarDark, Theme.TitleFont);
            Ui.ThemedLabel(wrap.transform, won
                    ? (tower ? Strings.T("battle.phase.run_won_tower_msg") : Strings.T("battle.phase.run_won_stage_msg"))
                    : (tower ? Strings.T("battle.phase.run_lost_tower_msg") : Strings.T("battle.phase.run_lost_stage_msg")),
                BannerMsgFont, Theme.TextDim);
            Ui.PillButton(wrap.transform,
                won && tower ? Strings.T("battle.btn.to_safe_floor")
                    : tower ? Strings.T("battle.btn.settle") : Strings.T("common.back_to_map"),
                () => _onRunEnded(won), won ? Theme.Jade : Theme.InkSoft, Color.white, 36,
                new Vector2(400, BannerPillH));
            _message = "";   // 那句话已经画在横幅里,底部提示行不再重复一遍
        }
```

⚠ `_message` 原本被赋成横幅那句话（底部提示行与横幅重复）。稿上 msg 就在横幅里，所以清空。

- [ ] **Step 4: `DrawBattleSettle` 的败北支同样走纸罩**

把 `Ui.ThemedLabel(_centerRow, Strings.T("battle.phase.defeat_ellipsis"), 36, …)` 那一段的父节点从 `_centerRow` 改成一个与上面同款的整屏 `Ui.Panel(transform, "SettleBanner")` + `ScrimPaper` + 居中 `Wrap`，字号 36 → `BannerFont`，色 `Theme.CinnabarDark`。复活徽章与「结算」钮一并挪进那个 Wrap（尺寸分别改 `new Vector2(300, 67)` 与 `new Vector2(400, BannerPillH)`，对应稿 `.revive` 32pt 与 `.pill` 48pt）。

同样要在建之前销毁同名旧节点。

- [ ] **Step 5: 编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation/BattleView.cs
git commit -m "feat(ui): 段末横幅改整屏纸色罩 + 居中大字,墨色横带撤掉"
```

---

### Task 6: 安全层与登塔结算两个整屏

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/GameRoot.cs`（`ShowSafeLayer` 443、`SettleTower` 479、`ShowTowerSettle` 522）
- Modify: `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`

**Interfaces:**
- Consumes：`Ui.Sheet` 不用（这两个是整屏页不是浮层）；用现有 `Ui.CardPanel` / `Ui.OutlinedPanel` / `Ui.VStack` / `Ui.Row` / `Ui.PillButton` / `Ui.IngotLabel`
- Produces：无对外接口。

**背景：** 两屏现在都是「一张比例锚点定的居中卡 + 一列 `ThemedLabel` + 两个钮」。稿要的是：

**SafeLayer** — 巨字水印（已有）+ 1340 宽卡：标题 → sub（段位 · 历史最高）→ roll（大金数字 + 「滚存墨锭」）→ state 三项（生命 / 字库 / 已破 Boss）→ **forks 两路各占一半**，每路 = 钮 + 钮**下面**的取舍说明 → risk 行。稿的原话：「两条路各占一半，取舍写在钮下面 —— 这一屏的全部意义就是把这个取舍讲清楚」。

**Settle** — 1172 宽卡：标题 → head → amount（墨锭图标 + 84 号数字）→ **rec 新纪录 chip（仅破纪录时）** → chestrow（色块图标 + 标题 + 说明；无箱走虚线 `.none` 态）→「回地图」钮。

⚠ **裁定 B 生效**：稿上「途中阵亡则墨锭减半」「本次未撤退前这 340 墨都还不是你的」「墨锭半额结算」全是**过期文案**（半额 2026-08-30 已取消）。这里按**现行规则**写，Task 7 再把稿改过来。

- [ ] **Step 1: 新增 strings（按字母序插入）**

```json
  "root.safelayer.descend_why": "滚存带进下一段继续累加\n墨锭赚到即入账，阵亡也不打折",
  "root.safelayer.retreat_why": "立即收官，本趟到此为止\n层数纪录保留，下次从第 1 层重来",
  "root.safelayer.risk": "深入只赚不赔——墨锭早已入账，再往下是多挣一笔还是空手，全看这一段",
  "root.safelayer.rollover_unit": "滚存墨锭",
  "root.safelayer.state_boss": "已破 Boss 第 {depth} 层",
  "root.safelayer.state_hp": "生命 {hp} / {maxHp}",
  "root.safelayer.state_library": "字库 {count} / {capacity}",
  "root.settle.chest_row_desc": "第 {chestDepth} 层 Boss 战利",
  "root.settle.chest_row_pending_desc": "箱位已满，开掉一只即可领取",
  "root.settle.chest_row_title": "获得{tierName}",
  "root.settle.record_chip": "新纪录 · {from} → {to} 层",
```

同时**删掉** `root.safelayer.rollover_ink` 与 `root.safelayer.hint`（它们的位置被 `rollover_unit` 与 `risk` 取代），以及 `root.settle.chest_note_awarded` / `root.settle.chest_note_pending`（被拆成 title/desc 两截）。删完跑一次 `StringsTable` 测试确认没有孤儿也没有缺 key。

- [ ] **Step 2: `ShowSafeLayer` 换版面**

从 `var card = Ui.CardPanel(view.transform, "Panel");` 到方法末尾替换成：

```csharp
            // 稿 SafeLayer.dc.html。640pt 宽的宣纸卡居中,内容自上而下:
            // 标题 → 段位 → 滚存 → 三项状态 → 两条路(各占一半,取舍写在钮下面) → risk 行。
            const float PanelW = 1340f;   // 稿 .panel 640pt
            const float PanelH = 620f;
            var card = Ui.OutlinedPanel(view.transform, "Panel", Theme.PanelPaper, Theme.PanelBorder,
                25, 2f, out var face);
            Ui.Anchor((RectTransform)card.transform, new Vector2(0.5f, 0.5f), new Vector2(0.5f, 0.5f),
                new Vector2(-PanelW / 2f, -PanelH / 2f), new Vector2(PanelW / 2f, PanelH / 2f));
            var stack = Ui.VStack(face.transform, "Stack", 19);   // 稿 .panel gap 9pt
            Ui.Stretch((RectTransform)stack.transform);
            var stackLayout = stack.GetComponent<VerticalLayoutGroup>();
            stackLayout.childAlignment = TextAnchor.MiddleCenter;
            stackLayout.padding = new RectOffset(54, 54, 38, 42);   // 稿 18/26/20pt

            Ui.ThemedLabel(stack.transform, Strings.T("root.safelayer.title", ("depth", depth)),
                46, Theme.TextMain, Theme.TitleFont);
            Ui.ThemedLabel(stack.transform,
                Strings.T("common.rank_summary",
                    ("rank", EndlessRules.RankTitle(_meta.BestDepth)), ("depth", _meta.BestDepth)),
                22, Theme.TextDim);

            // 滚存:大金数字 + 单位小字(稿 .roll)
            var roll = Ui.Row(stack.transform, "Roll", 15);
            roll.GetComponent<HorizontalLayoutGroup>().childAlignment = TextAnchor.MiddleCenter;
            Ui.ThemedLabel(roll.transform, totalEarned.ToString(), 71, Theme.GoldDeep, Theme.TitleFont);
            Ui.ThemedLabel(roll.transform, Strings.T("root.safelayer.rollover_unit"), 21, Theme.LockGray);

            // 三项状态(稿 .state)
            var state = Ui.Row(stack.transform, "State", 33);
            state.GetComponent<HorizontalLayoutGroup>().childAlignment = TextAnchor.MiddleCenter;
            var snap = _meta.EndlessV2;
            Ui.ThemedLabel(state.transform,
                Strings.T("root.safelayer.state_hp", ("hp", snap.PlayerHp), ("maxHp", snap.PlayerMaxHp)),
                21, Theme.TextDim);
            Ui.ThemedLabel(state.transform,
                Strings.T("root.safelayer.state_library",
                    ("count", snap.Library.Count), ("capacity", snap.LibraryExpanded ? 11 : 9)),
                21, Theme.TextDim);
            Ui.ThemedLabel(state.transform,
                Strings.T("root.safelayer.state_boss", ("depth", depth)), 21, Theme.TextDim);

            // 两条路各占一半,取舍写在钮下面 —— 这一屏的全部意义就是把这个取舍讲清楚(稿原话)
            var forks = Ui.Row(stack.transform, "Forks", 29);   // 稿 .forks gap 14pt
            forks.GetComponent<HorizontalLayoutGroup>().childForceExpandWidth = true;
            Ui.Sized(forks, width: PanelW - 108f, height: 190f);
            Fork(forks.transform,
                Strings.T("root.safelayer.descend_button",
                    ("nextBandName", nextBand.Name), ("from", depth + 1), ("to", depth + endless.BossEvery)),
                Strings.T("root.safelayer.descend_why"), Theme.Cinnabar,
                () => StartSegment(firstTower: false));
            Fork(forks.transform,
                Strings.T("root.safelayer.retreat_button"),
                Strings.T("root.safelayer.retreat_why"), Theme.InkSoft,
                () => SettleTower(died: false, depth, totalEarned));

            Ui.ThemedLabel(stack.transform, Strings.T("root.safelayer.risk"), 19, Theme.LockGray);
        }

        /// <summary>安全层的一条岔路:钮 + 钮**下面**那句取舍说明(稿 SafeLayer.dc.html 的 .fork)。
        /// 说明放在钮下面而不是一行小字,是这一轮贯穿的规矩「凡是不可逆的都要在按下去之前说清楚」。</summary>
        private static void Fork(Transform parent, string label, string why, Color color, System.Action onClick)
        {
            var column = Ui.VStack(parent, "Fork", 13);
            column.GetComponent<VerticalLayoutGroup>().childAlignment = TextAnchor.UpperCenter;
            Ui.Sized(column, flexWidth: 1f);
            Ui.PillButton(column.transform, label, onClick, color, Color.white, 31, new Vector2(600, 84));
            var note = Ui.ThemedLabel(column.transform, why, 20, Theme.TextDim);
            note.horizontalOverflow = HorizontalWrapMode.Wrap;
            note.verticalOverflow = VerticalWrapMode.Overflow;
            Ui.Sized(note.gameObject, width: 600f, height: Ui.WrappedTextHeight(why, 20, 600f));
```

⚠ `snap.PlayerMaxHp` 若 `EndlessSaveState` 上没有这个字段，用 `grep -n "MaxHp" Brushblade/Assets/_Project/Core/Meta.cs` 查真名；查不到就从 `_meta` 的养成加成算，或退一步只显示当前生命（那就把 `state_hp` 的模板改成 `"生命 {hp}"`）。**实现时先查清楚再写，不要猜。**
⚠ `LibraryExpanded ? 11 : 9` 是硬编码的容量。先 `grep -n "LibraryCapacity" Brushblade/Assets/_Project/Core/` 找到真实来源改用它。

- [ ] **Step 3: `SettleTower` 把 chestNote 拆成两截**

`SettleTower` 现在拼一个 `chestNote` 字符串传给 `ShowTowerSettle`。改成传三个值：

```csharp
            string chestTitle = null, chestDesc = null;
            if (chestDepth > 0)
            {
                var tier = EndlessRules.ChestTierFor(chestDepth, new GameRandom(System.Environment.TickCount));
                chestTitle = Strings.T("root.settle.chest_row_title", ("tierName", ChestRules.TierName(tier)));
                if (ChestRules.TryAwardChest(_meta, tier, ChestCardPool(), Time))
                    chestDesc = Strings.T("root.settle.chest_row_desc", ("chestDepth", chestDepth));
                else
                {
                    _meta.PendingChests.Add(tier);
                    chestDesc = Strings.T("root.settle.chest_row_pending_desc");
                }
            }
```

并把 `ShowTowerSettle` 的签名改成：

```csharp
        private static void ShowTowerSettle(string headline, int ink, string chestTitle, string chestDesc,
            int previousBest, int clearedDepth)
```

`previousBest` 要在 `EndlessRules.UpdateBest(_meta, clearedDepth)` **之前**读一次 `_meta.BestDepth` 存下来 —— 新纪录 chip 要「43 → 45」两个数。

- [ ] **Step 4: `ShowTowerSettle` 换版面**

```csharp
        private static void ShowTowerSettle(string headline, int ink, string chestTitle, string chestDesc,
            int previousBest, int clearedDepth)
        {
            var view = NewView("TowerSettleView");
            Ui.Stretch((RectTransform)view.transform);
            BalanceCorner(view.transform);

            // 稿 Settle.dc.html:560pt 卡。宝箱行是这一屏的第二主角 ——
            // 一场爬塔只发一个箱,档位由本次最高 Boss 层定。
            const float PanelW = 1172f;   // 稿 .panel 560pt
            const float PanelH = 560f;
            var card = Ui.OutlinedPanel(view.transform, "Panel", Theme.PanelPaper, Theme.PanelBorder,
                25, 2f, out var face);
            Ui.Anchor((RectTransform)card.transform, new Vector2(0.5f, 0.5f), new Vector2(0.5f, 0.5f),
                new Vector2(-PanelW / 2f, -PanelH / 2f), new Vector2(PanelW / 2f, PanelH / 2f));
            var stack = Ui.VStack(face.transform, "Stack", 21);   // 稿 .panel gap 10pt
            Ui.Stretch((RectTransform)stack.transform);
            var stackLayout = stack.GetComponent<VerticalLayoutGroup>();
            stackLayout.childAlignment = TextAnchor.MiddleCenter;
            stackLayout.padding = new RectOffset(59, 59, 42, 46);   // 稿 20/28/22pt

            Ui.ThemedLabel(stack.transform, Strings.T("root.towersettle.title"), 46, Theme.TextMain, Theme.TitleFont);
            Ui.ThemedLabel(stack.transform, headline, 23, Theme.TextDim);
            Ui.IngotLabel(stack.transform, ink.ToString(), 84);

            if (clearedDepth > previousBest)
            {
                var chip = Ui.Chip(stack.transform,
                    Strings.T("root.settle.record_chip", ("from", previousBest), ("to", clearedDepth)),
                    Theme.GoldSoft, Theme.GoldDeep, 20, padX: 19, padY: 11);
                Ui.Sized(chip, height: 42);   // 稿 .rec 20pt
            }

            ChestRow(stack.transform, chestTitle, chestDesc, PanelW - 118f);

            Ui.PillButton(stack.transform, Strings.T("common.back_to_map"), () => ShowMap(),
                Theme.Cinnabar, Color.white, 31, new Vector2(544, 84));
        }

        /// <summary>结算页的宝箱行(稿 Settle.dc.html 的 .chestrow):色块图标 + 标题 + 说明。
        /// 无箱走虚线的 .none 态 —— 一场爬塔只发一个箱,一个 Boss 都没破就没有。</summary>
        private static void ChestRow(Transform parent, string title, string desc, float width)
        {
            bool has = title != null;
            var row = Ui.OutlinedPanel(parent, "ChestRow",
                has ? Theme.CardWhite : Theme.Paper, has ? Theme.PanelBorder : Theme.LockGray,
                21, 2f, out var face);
            Ui.Sized(row.gameObject, width: width, height: 100f);
            var inner = Ui.Row(face.transform, "Inner", 23);   // 稿 gap 11pt
            Ui.Stretch((RectTransform)inner.transform);
            var innerLayout = inner.GetComponent<HorizontalLayoutGroup>();
            innerLayout.childAlignment = TextAnchor.MiddleLeft;
            innerLayout.padding = new RectOffset(25, 25, 19, 19);   // 稿 9/12pt

            if (!has)
            {
                Ui.ThemedLabel(inner.transform, Strings.T("root.towersettle.no_chest"), 22, Theme.LockGray);
                return;
            }
            var icon = Ui.CardPanel(inner.transform, "Icon", Theme.Gold, 13);
            Ui.Sized(icon.gameObject, width: 84f, height: 63f).minWidth = 84f;   // 稿 .ic 40×30pt
            var column = Ui.VStack(inner.transform, "Text", 4);
            column.GetComponent<VerticalLayoutGroup>().childAlignment = TextAnchor.MiddleLeft;
            Ui.ThemedLabel(column.transform, title, 26, Theme.TextMain, Theme.TitleFont, TextAnchor.MiddleLeft);
            Ui.ThemedLabel(column.transform, desc, 20, Theme.LockGray, null, TextAnchor.MiddleLeft);
        }
```

- [ ] **Step 5: 编译 + 全量测试**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```
```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```
Expected：`Build succeeded`；`Failed: 0`。

- [ ] **Step 6: 字体子集**

```bash
python3 tools/fonts/subset_fonts.py && python3 -m pytest tools/fonts/tests/ -q
```
本任务引入了新字（「滚存」「赔」「纪录」等）。**用 cmap 比对确认它们进去了**：
```bash
python3 -c "
from fontTools.ttLib import TTFont
import glob
for f in sorted(glob.glob('Brushblade/Assets/_Project/Presentation/Fonts/Resources/*.ttf')):
    cm = TTFont(f).getBestCmap()
    missing = [c for c in '滚存赔纪录赚挣' if ord(c) not in cm]
    print(f, '缺:', missing or '无')
"
```
Expected：每个文件都打印「缺: 无」。若有缺，说明 `subset_fonts.py` 没扫到 —— 检查它的 JSON 扫描来源是否覆盖 `strings.zh-CN.json`。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation/GameRoot.cs Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json Brushblade/Assets/_Project/Presentation/Fonts/Resources tools/fonts/charset.txt
git commit -m "feat(ui): 安全层与登塔结算改整屏版面 —— 取舍写在钮下面,宝箱行独立成块"
```

---

### Task 7: 四张过期稿回填 + README 转「已接线」

**Files:**
- Modify: `docs/design/ui/scenes/SafeLayer.dc.html`
- Modify: `docs/design/ui/scenes/Settle.dc.html`
- Modify: `docs/design/ui/scenes/RunEnd.dc.html`
- Modify: `docs/design/ui/scenes/Reward.dc.html`
- Modify: `docs/design/ui/scenes/Popups.dc.html`
- Modify: `docs/design/ui/scenes/README.md`

**Interfaces:** 无代码接口；纯文档。

**背景（裁定 B）：** 这四张稿写着已被后来的用户拍板推翻的规则。改稿不改码，且每一处回填**必须写明日期与推翻它的那条决策出处**，不然下一个读稿的人会以为稿本来就错。

- [ ] **Step 1: `SafeLayer.dc.html` 去掉半额**

`.fork .why` 的两句改成与 `root.safelayer.descend_why` / `root.safelayer.retreat_why` 一致：

```html
          <div class="fork">
            <button class="pill cinnabar">深入「墨海」第 31~35 层</button>
            <!-- 2026-09-01 回填：原文是「途中阵亡则墨锭减半」。半额 2026-08-30 已取消
                 （见 GameRoot.SettleTower 的注释：「半额取消后每笔收入都是赚到即结」），
                 稿比代码旧，按现行规则改写。 -->
            <div class="why">滚存带进下一段继续累加<br><i>墨锭赚到即入账，阵亡也不打折</i></div>
          </div>
          <div class="fork">
            <button class="pill ink">收官撤退</button>
            <div class="why"><i>立即收官，本趟到此为止</i><br>层数纪录保留，下次从第 1 层重来</div>
          </div>
```

`.risk` 那句改成：

```html
        <div class="risk">
          <svg width="9" height="9" viewBox="0 0 24 24" fill="none" stroke="#88909C" stroke-width="2">
            <circle cx="12" cy="12" r="9"></circle><path d="M12 7v6M12 16.5v.01"></path>
          </svg>
          深入只赚不赔——墨锭早已入账，再往下是多挣一笔还是空手，全看这一段
        </div>
```

⚠ `.fork .why b` 的红色样式若不再有 `<b>` 用它，样式留着不删（其它稿可能复用）；`<i>`（绿色）改用在两处正面表述上。

- [ ] **Step 2: `Settle.dc.html` 去掉半额 chip**

删掉 `.amount .half` 这条 CSS 规则与任何 `class="half"` 的 span（若标记里有）。在 `.amount` 上方加注释：

```html
        <!-- 2026-09-01 回填：此前这里有一枚「半额」小红字。半额 2026-08-30 已取消，
             撤退、阵亡、弃塔拿到的墨锭完全一样（GameRoot.SettleTower）。 -->
```

- [ ] **Step 3: `RunEnd.dc.html` 改败北那句**

```html
      <!-- 2026-09-01 回填：原文「卒……墨锭半额结算，纪录保留。」半额 2026-08-30 已取消，
           文案与 strings 的 battle.phase.run_lost_tower_msg 对齐。 -->
      <div class="msg">卒……墨锭一分不少，纪录保留。</div>
```

- [ ] **Step 4: `Reward.dc.html` 去掉部件候选**

删掉第四个 `.pick`（`<span class="g">刂</span>` 那一块），并在 `.picks` 上方加：

```html
    <!-- 2026-09-01 回填：此前第四个候选画的是部件「刂」。2026-08-04 起战利品与复活补给
         都不再给部件（见 RunEngine 的「部件补给随 Core 一并删除——五行部件今后只能靠
         拆字获得」），候选只有字。 -->
```

- [ ] **Step 5: `Popups.dc.html` 三处对齐**

（a）`.lede` 里「颜色说的是『谁干的』」那段之后补一段：

```html
    <br><b>2026-09-01 回填</b>：直伤（普通 / 暴击 / 引爆 / 灼烧跳伤）的颜色 2026-08-30
    起改成<b>按攻击方五行分色</b>（火系飘红、水系飘蓝，相克镶一圈金边），不再一律朱砂。
    下面几格的「朱砂」标注是改版前的口径，保留作对照。被克那一记向纸色淡 42%、字号下限降到 0.78。
```

（b）在 18 格之后补第 19 格：

```html
      <div class="pcell">
        <div class="stage"><span class="pop" style="color: #2E7D46; font-size: 13px;">愈+40</span></div>
        <div class="meta"><b>Jade 翠玉</b> · 锚在敌人格</div>
        <div class="why">敌方自愈 · <code>enemy_mend</code>（2026-09-01 补：实现侧共 19 种，不是 18）</div>
      </div>
```

并把小标题 `<h3>18 种飘字…` 改成 `<h3>19 种飘字…`。

（c）删掉「相 生」那张 `.chart`，并在 `.charts` 上方加：

```html
      <!-- 2026-09-01 回填：相生环图 2026-08-31 拍板整个撤掉，战斗屏左窄栏只留相克。
           理由（见 BattleView.BuildSkeleton 的注释）：战斗中要实时查的是「我这张字克不克它」，
           相生 ×3 由配方静态决定，属牌面信息，长按详情弹窗已经显示。
           WuxingChart.Mount 的 sheng:true 分支保留未删，图鉴类页面仍可能用。 -->
```

（d）`.flashnote` 的全屏闪补上第三条实况：

```html
        · 引爆且伤害 ≥ 40 → 0.12s<br>
        · 击杀 → 0.16s<br>
        · 普通命中且伤害 ≥ 40（暴击 ≥ 20）→ 0.12s（<code>Juice.HitFx</code>；2026-09-01 补记，
          被克那一记不闪）<br>
```

- [ ] **Step 6: README 8 块转「已接线」**

在 `docs/design/ui/scenes/README.md` 的「2026-08-28 补齐：局内流程与弹窗」小节末尾，把那段 ⚠「这 8 块是**稿子先行**」替换成：

```markdown
⚠ ~~这 8 块是**稿子先行**~~ **2026-09-01 全部已接线**（轮三，见
`docs/superpowers/plans/2026-09-01-轮三-局内流程串联.md`）：

- `Ui.Sheet` 抽出统一浮层外壳，`Ui.ModalShell` 与 `UnitSheet` 共用，弹窗卡按稿补上 1pt 描边，
  同屏只留一个弹窗（Task 1）。
- 四个换字入口（战利品 / 回合掉字 / 奇遇 / 广告复活）合并成一版 `DrawReplaceSheet`，
  横排不折行 + 标红告警条（Task 2）。
- 战利品与复活补给的效果说明落到牌下的定高横条，不再拼进标题行（Task 3）。
- 奇遇四件收进一块垂直居中的版面，不再散在三条战场排上（Task 4）。
- 段末横幅改整屏纸色罩 + 居中大字，墨色横带撤掉（Task 5）。
- 安全层与登塔结算改整屏版面，取舍写在钮下面，宝箱行独立成块（Task 6）。

回填的四处「稿比代码旧」（Task 7）：`SafeLayer` / `Settle` / `RunEnd` 的**墨锭半额**
（2026-08-30 已取消）、`Reward` 的**部件候选**（2026-08-04 起不再给部件）、
`Popups` 的**直伤朱砂 / 18 种 / 相生环图**（分别被 8-30 五行分色、`enemy_mend`、
8-31 撤相生推翻）。
```

- [ ] **Step 7: 文档新鲜度测试**

```bash
python3 -m pytest tools/design/tests/ -q
```
Expected：`test_doc_freshness.py::test_doc_is_fresh` **仍然红**（`字表功能解析.md` 与 `chars.json` 不同步，`main` 上就是红的，与本轮无关，**不要顺手修**）；其余绿。

- [ ] **Step 8: 提交**

```bash
git add docs/design/ui/scenes/
git commit -m "docs(ui): 四张稿的过期记载回填,8 块局内流程从稿子先行转已接线"
```

---

### Task 8: 全量验证与试玩交付

**Files:** 无（只跑验证）

- [ ] **Step 1: 三条工装全跑**

```bash
python3 -m pytest tools/ -q
```
Expected：`1 failed, N passed` —— 唯一那条 `test_doc_freshness.py::test_doc_is_fresh` 是 `main` 上就存在的既有失败。

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```
Expected：`Failed: 0`。

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```
Expected：`Build succeeded`。

- [ ] **Step 2: 死方法扫描**

本轮抽了几个新的 private 方法（`DrawReplaceSheet` / `DrawPickSheet` / `PickTile` / `Fork` / `ChestRow`）。逐个数调用点，零调用点的要么删要么说明理由：

```bash
for m in DrawReplaceSheet DrawPickSheet PickTile Fork ChestRow Sheet ScrimSoft ScrimPaper WarnBg WarnText; do
  echo "$m: $(grep -rc "$m" Brushblade/Assets/_Project/Presentation/ --include='*.cs' 2>/dev/null | awk -F: '{s+=$2} END {print s}')"
done
```
每个都应 ≥ 2（定义 + 至少一个调用）。

- [ ] **Step 3: 孤儿文案扫描**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter StringsTable
```
Expected：`Failed: 0`。⚠ 这个检查**只认字面量** —— 若报某个 key 是孤儿，先确认它不是被写成了变量传参。

- [ ] **Step 4: 字体子集终检**

```bash
python3 tools/fonts/subset_fonts.py && python3 -m pytest tools/fonts/tests/ -q && git diff --stat Brushblade/Assets/_Project/Presentation/Fonts/Resources
```
若 `git diff --stat` 有变化但前面每个任务都跑过子集，说明某个任务漏跑了 —— 把这次的结果一并提交。

- [ ] **Step 5: 交给用户人工试玩**

Presentation 没有自动化测试，版面只能人工验。交付时列出要看的六处：
1. 安全层：两条路的取舍是否写在钮下面、risk 行是否还提「半额」（不该提）。
2. 登塔结算：破纪录时是否出现「新纪录 43 → 45 层」chip；无箱时宝箱行是否是虚线态。
3. 战利品：点第一下是否在牌下的横条里出现效果说明、版面是否不跳。
4. 换字：字库满员 9 张是否一横排不折行、告警条是否标红。
5. 奇遇：标题/正文/选项/部件池是否是一整块居中，不是散在三排。
6. 段末：横幅是否整屏纸色罩，不是中间一条墨带。

- [ ] **Step 6: 报告**

把跑出来的实际数字（coretests 通过数、pytest 通过/失败数、cmap 字数变化）如实写进交付说明，不含糊。

---

## 自查

**规格覆盖：** 轮三小节四条 —— 两个整屏（T6）、`BattleView` 五阶段（战利品 T3 / 换字 T2 / 广告复活 T3 / 奇遇 T4 / 段末横幅 T5）、弹窗族 13 个调用点统一外壳（T1）、飘字口径对齐 `Popups.dc.html`（T7，按裁定 B 是改稿）。贯穿规矩「凡是不可逆的都要在按下去之前说清楚」落在 T2 的标红告警条与 T6 的 fork 说明。

**占位扫描：** 无 TBD / TODO / 「同 Task N」。三处标了「实现时先查清楚再写」的（`CharDef` 稀有度字段名、`EndlessSaveState.PlayerMaxHp`、`LibraryCapacity` 来源），是**必须核实的真实事实**而不是留白 —— 每处都给了具体的 grep 命令与查不到时的退路。

**类型一致性：** `Ui.Sheet` 的签名在 T1 定义、T2/T3 消费，参数序一致；`TileW`/`TileH` 在 T2 定义、T3 复用；`DrawReplaceSheet` 在 Step 2 先写成 `void` 后在 Step 3 改成返回 `Transform` 并加 `cancelLabel` 参数 —— **实现时直接按最终形态写**：
```csharp
private Transform DrawReplaceSheet(string title, string incoming,
    System.Collections.Generic.IReadOnlyList<string> library,
    System.Action<int> onPick, System.Action onCancel, string cancelLabel);
```
