# 字表平衡重做 · P0 引擎与规则 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把《字表平衡重做》设计稿里的 10 项引擎规则改动落地,让 P2 的数值重配有一个正确的引擎可以依附。

**Architecture:** 纯 Core 层改动,不碰 `chars.json` 一个数字 —— 本批的验收标准是「规则变了、而所有既有数值在基准值下的行为该变的变、不该变的逐字节不变」。新增两个 `EffectKind`(魅惑)与一个 `SummonPassive` 字段(光环攻),其余是既有常量、既有算式的修改。数值落地在 P2,表现层接线在 P1。

**Tech Stack:** C# / .NET(Unity 6000.5.2f1 自带 SDK)、NUnit、Unity asmdef 四层架构

**Spec:** `docs/superpowers/specs/2026-09-05-字表平衡重做-design.md`

## Global Constraints

以下每一条都来自 `CLAUDE.md` 或 spec,**每个任务的要求都隐含包含本节**:

- **Core 与 Data 禁止引用 `UnityEngine`**(asmdef 已设 `noEngineReferences: true`)。本批全部改动都在 Core,一行 `using UnityEngine` 都不许出现。
- **随机性一律走 Core 内带种子的 RNG**,禁用 `UnityEngine.Random`。
- **测试断言只用 Unity 版 NUnit 也支持的 API**:禁用 `Is.AnyOf` / `Is.All.AnyOf`(多选一写 `Is.EqualTo(a).Or.EqualTo(b)`);`Does.Not.Contain(x)` **只在 x 是 string 时可用**,集合判包含一律写成 `Assert.That(list.Contains(x), Is.True/False, "…")`。
- **测试里定位仓库根只能用 `TestContext.CurrentContext.TestDirectory`**,禁用 `AppContext.BaseDirectory`。
- **测试代码禁止直接引用 Newtonsoft**(`JsonConvert` 等);Tests asmdef 只放行 `nunit.framework.dll` + Core + Data。
- **恒等性硬线**:`PlayerAttack == BattleConfig.AttackBaseline`(100)且无局内增益时,既有伤害/护盾/治疗结算必须**逐字节不变**。任务 5、6、7 都会碰到缩放式子,改完必须确认这条。
- **Tests 只许追加,不许改既有断言的期望值** —— 本批不动数值,任何既有测试变红都是回归,不是"锁了旧值"。
- 提交信息用 conventional commits(`feat`/`fix`/`refactor` + 范围)。

**验证命令**(每个任务的最后一步都跑前两条,任务 4/5/9/10 额外跑第三条):

```bash
# Core/Data 单元测试(首选,毫秒级,不依赖编辑器锁)
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q

# 管线(本批不改管线,但 chars.json 的 regenerable 测试是护栏)
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ tools/design/tests/ -q

# 仿真工装能编译(它引用 Core,新增 EffectKind 会让它的 Power() switch 漏分支)
cd tools/balance && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

---

## Spec 覆盖对照(§3 的 20 项引擎/管线改动)

| spec §3 项 | 内容 | 归属 |
|---|---|---|
| 1 | 厚/泉 阈值 `MaxHp/10 → /5` | **P0 Task 1** |
| 2 | 泉倍率 10% → 5% | **P0 Task 2** |
| 3 | 护盾战斗结束衰减 50% | **P0 Task 3** |
| 5 | `SummonPassive.AuraAttack` | **P0 Task 4** |
| 6 | 召唤物攻击接战意 + 厚 | **P0 Task 5** |
| 11 | 封禁(`Silence` 扩展 + Boss 降级) | **P0 Task 9** |
| 14 | 魅惑 `Charm` | **P0 Task 10** |
| 15 | 限时增益(`Turns`) | **P0 Task 7** |
| 16 | 卡等级缩放回合数 | **P0 Task 8** |
| 18 | 反伤总量钳位 60% | **P0 Task 6** |
| 9 | `CardTraits` 形状迁移 + 新被动分支 | P1 |
| 10 | `gen_char_doc.py` 特性列收进形状 | P1 |
| 7 | `SpendWellspring` 移到攻面 | P2 |
| 8 | 浴→活 / 葬移出 | P2 |
| 12 | 穿透并入破甲、净化+驱散并入封禁 | P2 |
| 13 | 不保留净化 | P2 |
| 17 | 新字 `花` 入表 + 字体子集 | P2 |
| 19 | 双方向字按 §1.5 归属规则重排 | P2 |
| 20 | `extract_values.py` 的 turns 校验 | P2 |

> spec §3 原第 4 项(`SummonPassive.onHitMorale`)**已在设计阶段作废** —— `锥` / `剑`
> 双双转成攻击字后金系已无召唤字,那条印记补丁失去对象。不是遗漏。

## File Structure

| 文件 | 职责 | 本批改动 |
|---|---|---|
| `Brushblade/Assets/_Project/Core/BattleEngine.cs` | 战斗结算总入口(常量、效果分派、伤害管道) | 任务 1/2/6/7/9/10 —— 常量 2 处、效果 case 3 处、伤害管道 2 处 |
| `Brushblade/Assets/_Project/Core/EnemyDef.cs` | `EnemyState` / `SummonState`(含 `EffectiveAttack` / `EffectiveDefense`) | 任务 5 |
| `Brushblade/Assets/_Project/Core/SummonPassive.cs` | 召唤物被动 POCO(同时是存档结构) | 任务 4 |
| `Brushblade/Assets/_Project/Core/EffectDef.cs` | `EffectKind` 枚举 + 单条效果 POCO | 任务 10(新 Kind) |
| `Brushblade/Assets/_Project/Core/Meta.cs` | 养成规则(`ScaleByCardLevel` 等) | 任务 8 |
| `Brushblade/Assets/_Project/Core/RunEngine.cs` | 跑图/爬塔,战斗间携带态 | 任务 3 |
| `Brushblade/Assets/_Project/Data/ConfigLoader.cs` | `chars.json` → Core POCO | 任务 4(新字段反序列化)、任务 10 |
| `Brushblade/Assets/_Project/Tests/HeftTests.cs` | 厚/泉 既有测试 | 任务 1/2 追加 |
| `Brushblade/Assets/_Project/Tests/ShieldDecayTests.cs` | **新建** —— 护盾战斗结束衰减 | 任务 3 |
| `Brushblade/Assets/_Project/Tests/SummonAuraTests.cs` | **新建** —— 光环攻 + 召唤物吃玩家 buff | 任务 4/5 |
| `Brushblade/Assets/_Project/Tests/ReflectClampTests.cs` | **新建** —— 反伤总量钳位 | 任务 6 |
| `Brushblade/Assets/_Project/Tests/TimedBuffTests.cs` | **新建** —— 限时增攻/暴击 + 回合数吃卡等级 | 任务 7/8 |
| `Brushblade/Assets/_Project/Tests/SuppressTests.cs` | **新建** —— 封禁(含 Boss 降级) | 任务 9 |
| `Brushblade/Assets/_Project/Tests/CharmTests.cs` | **新建** —— 魅惑 | 任务 10 |
| `tools/balance/Program.cs` | 仿真机器人的 `Power()` 记分 | 任务 10(新 Kind 记分) |

**每个新建的 `.cs` 都要配一个 `.meta`** —— 从同目录别的测试 `.meta` 复制并换掉 `guid:`(随便改几位十六进制即可,只要全仓唯一)。漏了 Unity 打开工程时会重新生成、产生无谓 diff。

---

### Task 0: 共享测试夹具

**Files:**
- Create: `Brushblade/Assets/_Project/Tests/RebalanceFixture.cs` + `.meta`

**Interfaces:**
- Consumes: 无
- Produces:(任务 3~10 全部依赖)
  - `RebalanceFixture.BaseMaxHp` = 500
  - `RebalanceFixture.Graph(params CharDef[] extra)` → `RecipeGraph`(总含一张 `"甲"` = `DamageSingle 20`)
  - `RebalanceFixture.Char(string id, params EffectDef[] effects)` → `CharDef`
  - `RebalanceFixture.Mob(int hp = 100000, int attack = 0, int armor = 0)` → `EnemyDef`
  - `RebalanceFixture.Boss(int hp = 100000, int attack = 0, int armor = 0)` → `EnemyDef`(带 `phases`,`IsBoss` 才为 true)
  - `RebalanceFixture.Battle(RecipeGraph graph, IReadOnlyList<string> library, params EnemyDef[] enemies)` → `BattleEngine`
  - `RebalanceFixture.Run(int normalShield = 0, int persistShield = 0)` → `RunEngine`

> 七个新测试文件本来各造一份夹具,签名一改要改七处。抽到一处,且沿用 `HeftTests`
> 的既有取向:测试字一律 `Element.Heart` 且**不给配方**(心对全属性生克 1.0×、无配方不触发相生,
> 断言里看到的数字就是被测机制本身),`PlayerAttack = 100`(基准,保住恒等性硬线),
> 一律走真实 `Cast()` / `EndTurn()`,不给引擎开新的生产可调用面。

- [ ] **Step 1: 建夹具文件**

```csharp
using System;
using System.Collections.Generic;
using System.Linq;
using Brushblade.Core;

namespace Brushblade.Core.Tests
{
    /// <summary>平衡重做 P0 那批测试的共享夹具(2026-09-05)。
    ///
    /// 取向沿用 HeftTests:测试字一律 Element.Heart 且不给配方(心对全属性生克 1.0×、
    /// 无配方不触发相生 —— 相生 ×3 虽已于 2026-09-02 取消,这条惯例仍保着"断言里看到的
    /// 数字就是被测机制本身"这个性质),PlayerAttack = 100 保住恒等性硬线,
    /// 一律走真实 Cast() / EndTurn()。</summary>
    internal static class RebalanceFixture
    {
        public const int BaseMaxHp = 500;

        /// <summary>图谱里总有一张 "甲"(单体 20)—— 秒杀 20 血靶子用它结束战斗。</summary>
        public static RecipeGraph Graph(params CharDef[] extra) =>
            new(new[]
                {
                    new CharDef("甲", Element.Heart,
                        effects: new[] { new EffectDef(EffectKind.DamageSingle, 20) }),
                }.Concat(extra).ToArray());

        public static CharDef Char(string id, params EffectDef[] effects) =>
            new(id, Element.Heart, effects: effects);

        /// <summary>杂兵靶子。hp 缺省给大值 = 打不死,便于在同一场里连续断言。</summary>
        public static EnemyDef Mob(int hp = 100000, int attack = 0, int armor = 0) =>
            new("怔", Element.Heart, hp, attack, defense: armor);

        /// <summary>Boss 靶子。<c>EnemyState.IsBoss</c> 判的是 <c>Def.Phases.Count > 0</c>,
        /// 所以**必须给 phases**,只给个 id 叫 "钧" 是不够的 —— 那是本任务最容易漏的一处。</summary>
        public static EnemyDef Boss(int hp = 100000, int attack = 0, int armor = 0) =>
            new("钧", Element.Heart, hp, attack, defense: armor,
                phases: new[]
                {
                    new BossPhaseDef("钧", Element.Heart, hp, attack, defense: armor),
                });

        public static BattleEngine Battle(RecipeGraph graph, IReadOnlyList<string> library,
            params EnemyDef[] enemies) =>
            new(graph, new BattleConfig { PlayerMaxHp = BaseMaxHp, PlayerAttack = 100 },
                library, Array.Empty<string>(), enemies, seed: 1);

        /// <summary>一场「一发就能打完」的爬塔,起始护盾可指定 ——
        /// RunEngine 的构造函数本来就有 startingNormalShield / startingPersistShield 两个参数,
        /// 不需要给引擎开新口子。</summary>
        public static RunEngine Run(int normalShield = 0, int persistShield = 0)
        {
            var config = new RunConfig
            {
                Encounters = new[] { new[] { Mob(hp: 20) } },
                RewardPool = new[] { "甲" },
            };
            return new RunEngine(Graph(), config,
                new BattleConfig { PlayerMaxHp = BaseMaxHp, PlayerAttack = 100 },
                new[] { "甲" }, Array.Empty<string>(), seed: 1,
                startingNormalShield: normalShield, startingPersistShield: persistShield);
        }
    }
}
```

- [ ] **Step 2: 建 `.meta`**

从同目录别的测试 `.meta` 复制并换掉 `guid:`(改几位十六进制即可,只要全仓唯一):

```bash
cd /Users/eugenewu/code/game
sed 's/guid: \(.\)/guid: a/' Brushblade/Assets/_Project/Tests/HeftTests.cs.meta \
  > Brushblade/Assets/_Project/Tests/RebalanceFixture.cs.meta
grep guid Brushblade/Assets/_Project/Tests/RebalanceFixture.cs.meta
```

确认输出的 guid 与 `HeftTests.cs.meta` 的不同。

- [ ] **Step 3: 编译确认夹具本身没问题**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿(夹具还没被任何测试用,只要能编过)。若 `RunConfig` 的 `Encounters` / `RewardPool`
属性名不对,照 `HeftTests.cs:73` 那段现成的 `RunConfig` 写法改 —— 那是本仓已验证的用法。

- [ ] **Step 4: 提交**

```bash
git add Brushblade/Assets/_Project/Tests/RebalanceFixture.cs Brushblade/Assets/_Project/Tests/RebalanceFixture.cs.meta
git commit -m "test: 加平衡重做 P0 的共享测试夹具

七个新测试文件本来各造一份,签名一改要改七处。"
```

---

### Task 1: 厚/泉 攒层阈值 `MaxHp/10` → `MaxHp/5`

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`ResourceThreshold`,约 727 行)
- Test: `Brushblade/Assets/_Project/Tests/HeftTests.cs`(追加)

**Interfaces:**
- Consumes: 无
- Produces: `BattleEngine.ResourceThreshold`(private)语义变为 `Math.Max(1, PlayerMaxHp / 5)`;公开面 `HeftStacks` / `WellspringStacks` / `ShieldAccum` / `HealAccum` 签名不变

- [ ] **Step 1: 写失败测试**

追加到 `HeftTests.cs` 的类体内:

```csharp
        /// <summary>攒层阈值 = MaxHp/5(2026-09-05,平衡重做 P0 任务 1;原为 MaxHp/10)。
        ///
        /// MaxHp 500 → 阈值 100。给 199 点护盾应当只攒 1 层、余数 99;
        /// 按旧阈值(50)那会是 3 层。断的是「一张高档字不再一发攒满」。</summary>
        [Test]
        public void ResourceThreshold_IsOneFifthOfMaxHp()
        {
            var battle = NewBattle(500);
            battle.GainHeftForTest(199);
            Assert.That(battle.HeftStacks, Is.EqualTo(1), "199 / (500/5) = 1 层");
            Assert.That(battle.ShieldAccum, Is.EqualTo(99), "余数 199 − 100");
        }

        /// <summary>阈值随 MaxHp 走,不是写死的常量。MaxHp 1000 → 阈值 200。</summary>
        [Test]
        public void ResourceThreshold_ScalesWithMaxHp()
        {
            var battle = NewBattle(1000);
            battle.GainWellspringForTest(199);
            Assert.That(battle.WellspringStacks, Is.EqualTo(0), "199 < 1000/5 = 200,攒不满一层");
            Assert.That(battle.HealAccum, Is.EqualTo(199));
        }
```

- [ ] **Step 2: 跑测试确认它失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "ResourceThreshold_IsOneFifthOfMaxHp|ResourceThreshold_ScalesWithMaxHp"
```

预期:两条都 FAIL —— 第一条实得 3 层(旧阈值 50),第二条实得 1 层。

- [ ] **Step 3: 改常量**

`BattleEngine.cs` 里把 `ResourceThreshold` 整块替换:

```csharp
        /// <summary>攒一层厚/泉需要的量 = 玩家生命上限的**五分之一**(2026-09-05,原为十分之一)。
        ///
        /// 用百分比而不是固定值:固定 100 点在早期(垒 50 盾)攒不出一层、在深层
        /// (㙓 630 盾)一次给 6 层。百分比口径自动跟着角色成长走。
        ///
        /// ⚠ 2026-09-05 由 /10 改 /5:旧阈值下 㵘 一张治 540 = 10.8 层,**一发攒满 10 层**,
        /// 「攒」这个过程根本不存在。改后配合 P2 把治疗压回锚点(㵘 350),一张红档水字
        /// 攒 3 层,满层要三张、跨几层楼 —— 即设计稿 §1 定的「几层楼攒一波,Boss 层倒出来」。
        /// 下钳 1:MaxHp &lt; 5 时整数除会得 0,那会让 while 循环永不终止。</summary>
        private int ResourceThreshold => Math.Max(1, _config.PlayerMaxHp / 5);
```

- [ ] **Step 4: 跑测试确认通过,并跑全量**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:新增两条 PASS。**既有测试里凡是断言厚/泉层数的都会变红** —— 那是规则改动的正确后果,不是回归。逐条改期望值,并在每条被改的测试上补一句注释说明「2026-09-05 阈值 /10 → /5」。若有测试断言的是"某字一发攒满",把它改成断言新层数;若断言的是"攒不满",确认它仍成立。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/HeftTests.cs
git commit -m "refactor(battle): 厚/泉 攒层阈值 MaxHp/10 → MaxHp/5

旧阈值下 㵘 一张治 540 = 10.8 层,一发攒满,「攒」的过程不存在。
配合 P2 把治疗压回锚点后,一张红档水字攒 3 层、满层要三张。"
```

---

### Task 2: 泉的被动倍率 10%/层 → 5%/层

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`WellspringPercentPerStack`,约 369 行)
- Test: `Brushblade/Assets/_Project/Tests/HeftTests.cs`(追加)

**Interfaces:**
- Consumes: 任务 1 的新阈值(测试夹具要按 MaxHp/5 造层数)
- Produces: `AmplifyByWellspring(int)` 语义不变,系数变 5

- [ ] **Step 1: 写失败测试**

```csharp
        /// <summary>泉每层 +5% 治疗(2026-09-05,原 +10%)——与厚的 +5% 攻击对齐。
        ///
        /// 引擎注释原先声称两者「同顶」(泉 10×10 = 厚 5×10 = +50%),但那忽略了
        /// **泉充得比厚快一倍**(治疗量普遍高于护盾量)。两条都拉到 5% 才真对齐。
        ///
        /// MaxHp 500 → 阈值 100。攒 400 治疗 = 4 层 → 下一次治疗 ×1.20。</summary>
        [Test]
        public void Wellspring_AmplifiesFivePercentPerStack()
        {
            var battle = NewBattle(500);
            battle.GainWellspringForTest(400);
            Assert.That(battle.WellspringStacks, Is.EqualTo(4));
            Assert.That(battle.AmplifyByWellspringForTest(100), Is.EqualTo(120),
                "4 层 × 5% = +20% → 100 → 120(旧口径是 +40% → 140)");
        }

        /// <summary>0 层时恒等 —— 这条是恒等性硬线,别让系数改动破了它。</summary>
        [Test]
        public void Wellspring_ZeroStacks_IsIdentity()
        {
            var battle = NewBattle(500);
            Assert.That(battle.AmplifyByWellspringForTest(137), Is.EqualTo(137));
        }
```

- [ ] **Step 2: 跑测试确认它失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "Wellspring_AmplifiesFivePercentPerStack|Wellspring_ZeroStacks_IsIdentity"
```

预期:第一条 FAIL(编译错 —— `AmplifyByWellspringForTest` 还不存在)。

- [ ] **Step 3: 改系数并开测试钩子**

`BattleEngine.cs`:

```csharp
        /// <summary>泉每层的治疗加成(百分点)。**2026-09-05 由 10 改 5**,与厚对齐。
        ///
        /// 原注释声称泉 10×10 与厚 5×10 「同顶 +50%」,但那只比了上限、忽略了**充电速度**:
        /// 治疗量普遍高于护盾量,同一条 MaxHp/N 阈值下泉攒得比厚快一倍。
        /// 两条都取 5% 才是真对齐(设计稿 §1)。</summary>
        private const int WellspringPercentPerStack = 5;
```

在 `GainWellspringForTest` 旁边追加钩子(与既有 `GainHeftForTest` 同一手法,仅供测试):

```csharp
        /// <summary>泉放大治疗的测试钩子(2026-09-05)。与 <see cref="GainWellspringForTest"/>
        /// 同理:不给引擎加新的生产可调用面,只把既有私有算式暴露给断言。</summary>
        internal int AmplifyByWellspringForTest(int value) => AmplifyByWellspring(value);
```

- [ ] **Step 4: 跑全量测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:新增两条 PASS;既有断言泉放大后治疗量的测试会变红,逐条改期望值并注明「2026-09-05 泉 10% → 5%」。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/HeftTests.cs
git commit -m "refactor(battle): 泉倍率 10%/层 → 5%/层,与厚对齐

原注释只比了上限(泉 10×10 = 厚 5×10 = +50%),忽略了泉充电快一倍。"
```

---

### Task 3: 护盾在每场战斗结束时衰减 50%

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/RunEngine.cs`(约 540 行,`_carriedNormalShield` / `_carriedPersistShield` 赋值处)
- Create: `Brushblade/Assets/_Project/Tests/ShieldDecayTests.cs` + `.meta`

**Interfaces:**
- Consumes: `BattleEngine.ShieldNormal` / `ShieldPersist`(既有公开面)
- Produces: `RunEngine` 携带的护盾在战后减半;`ShieldAccum` / `HealAccum` **不减半**

- [ ] **Step 1: 写失败测试**

新建 `ShieldDecayTests.cs`:

```csharp
using System;
using NUnit.Framework;
using Brushblade.Core;

namespace Brushblade.Core.Tests
{
    /// <summary>护盾在每场战斗结束时衰减 50%(2026-09-05,平衡重做 P0 任务 3)。
    ///
    /// 为什么要这条:护盾此前 `_shieldNormal += shield` 只加不减、整场爬塔通吃,
    /// 是**第二条血条**而不是临时保护。玩家只要有一个回合「每回合获得 > 每回合承伤」,
    /// 那份盾就永久留在身上 —— 战斗不会输也打不死怪,即「中层刮痧」。
    /// 压单张数值只能把转正点往后挪几层(已砍过两次),挪不掉这个性质。
    ///
    /// ⚠ 衰减挂在**战斗结束**而不是回合末(2026-09-05 用户裁定)。这解决的是**跨场累积**;
    /// 单场内的净增长它碰不到,那是刻意保留的残留(设计稿 §1.4)。
    ///
    /// 攒层余数(ShieldAccum / HealAccum)**不减半** —— 它们不是护盾,是厚/泉的进度条。</summary>
    public sealed class ShieldDecayTests
    {
        [Test]
        public void CarriedShield_HalvesAfterBattle()
        {
            var snap = WinAndCapture(normal: 301, persist: 100);
            Assert.That(snap.CarriedNormalShield, Is.EqualTo(150), "301 / 2 = 150(整数除,向下取整)");
            Assert.That(snap.CarriedPersistShield, Is.EqualTo(50), "两个桶各自减半");
        }

        [Test]
        public void ResourceAccumulators_DoNotHalve()
        {
            var snap = WinAndCapture(normal: 0, persist: 0, shieldAccum: 77, healAccum: 33);
            Assert.That(snap.CarriedShieldAccum, Is.EqualTo(77), "余数是厚的进度条,不是护盾");
            Assert.That(snap.CarriedHealAccum, Is.EqualTo(33));
        }

        [Test]
        public void CarriedShield_OfOne_DecaysToZero()
        {
            var snap = WinAndCapture(normal: 1, persist: 0);
            Assert.That(snap.CarriedNormalShield, Is.EqualTo(0), "1 / 2 = 0,不留残渣");
        }
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

携带态从 `RunEngine.Capture()` 返回的 `RunSnapshot` 上读 —— `CarriedNormalShield` /
`CarriedPersistShield` / `CarriedShieldAccum` / `CarriedHealAccum` 四个字段
在 `RunEngine.cs:149~152` 已经写进快照,不需要给引擎加新的公开面。
胜利流程照 `HeftTests.NewRunAfterWinningWithHeft`(HeftTests.cs:69)那段现成的:
`Cast("甲", 0)` 秒杀 20 血靶子 → 断言 `Phase == BattlePhase.Won` → `AdvanceAfterBattle()`。

把上面三条测试里的 `ShieldDecayFixture.*` 换成:

```csharp
        private static RunSnapshot WinAndCapture(int normal, int persist,
            int shieldAccum = 0, int healAccum = 0)
        {
            var run = RebalanceFixture.Run(normalShield: normal, persistShield: persist);
            if (shieldAccum > 0) run.Battle.GainHeftForTest(shieldAccum);
            if (healAccum > 0) run.Battle.GainWellspringForTest(healAccum);
            run.Battle.Cast("甲", 0);
            Assert.That(run.Battle.Phase, Is.EqualTo(BattlePhase.Won), "夹具前提:必须一发秒杀");
            run.AdvanceAfterBattle();
            return run.Capture();
        }
```

⚠ 灌余数用 `GainHeftForTest` 时要注意任务 1 已把阈值改成 `MaxHp/5 = 100`,
所以 `shieldAccum: 77` 会得到 0 层 + 余 77(正是 `ResourceAccumulators_DoNotHalve` 想要的)。

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "ShieldDecayTests"
```

预期:三条都 FAIL(护盾未减半,实得 301 / 100 / 1)。

- [ ] **Step 3: 实现衰减**

`RunEngine.cs` 里把两行携带态赋值改成:

```csharp
            // 护盾战斗结束衰减 50%(2026-09-05):护盾此前只加不减、整场爬塔通吃,
            // 是第二条血条而不是临时保护 —— 玩家一旦有一回合「获得 > 承伤」,那份盾
            // 就永久留在身上,战斗不会输也打不死怪(中层刮痧)。整数除、向下取整。
            // ⚠ 只衰减护盾,不动 _carriedShieldAccum / _carriedHealAccum:
            // 那两个是厚/泉的攒层余数,不是护盾。
            _carriedNormalShield = Battle.ShieldNormal / 2;
            _carriedPersistShield = Battle.ShieldPersist / 2;
            _carriedShieldAccum = Battle.ShieldAccum;
            _carriedHealAccum = Battle.HealAccum;
```

- [ ] **Step 4: 跑全量测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:三条新测试 PASS;既有断言「护盾跨战斗原样延续」的测试会变红,改成断言减半并注明日期。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/RunEngine.cs Brushblade/Assets/_Project/Tests/ShieldDecayTests.cs Brushblade/Assets/_Project/Tests/ShieldDecayTests.cs.meta
git commit -m "feat(run): 护盾在每场战斗结束时衰减 50%

护盾此前只加不减、整场爬塔通吃,是第二条血条 —— 玩家一旦有一回合
「获得 > 承伤」那份盾就永久留身上,即中层刮痧。攒层余数不衰减。"
```

---

### Task 4: `SummonPassive.AuraAttack` —— 攻击光环(含自己)

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/SummonPassive.cs`(新属性 + `Clone()`)
- Modify: `Brushblade/Assets/_Project/Core/EnemyDef.cs`(`SummonState.EffectiveAttack` 读光环)
- Modify: `Brushblade/Assets/_Project/Data/ConfigLoader.cs`(`auraAttack` 反序列化)
- Create: `Brushblade/Assets/_Project/Tests/SummonAuraTests.cs` + `.meta`

**Interfaces:**
- Consumes: 无
- Produces: `SummonPassive.AuraAttack { get; set; }`(int,0 = 无);`SummonState.EffectiveAttack` 多加一项 —— **任务 5 会在同一个属性上再加百分比乘区,两个任务改同一行,任务 5 必须在本任务之后做**

- [ ] **Step 1: 写失败测试**

```csharp
using System;
using System.Linq;
using NUnit.Framework;
using Brushblade.Core;

namespace Brushblade.Core.Tests
{
    /// <summary>攻击光环 `AuraAttack`(2026-09-05,平衡重做 P0 任务 4)。
    ///
    /// 给场上**全部**召唤物 +N 攻,**含自己**。「含自己」是 2026-09-05 用户裁定的配套修补:
    /// 召唤只数收归 1 只后,只加别人的光环在场上没有作用对象、纯空转。</summary>
    public sealed class SummonAuraTests
    {
        [Test]
        public void AuraAttack_BuffsItself()
        {
            var summon = new SummonPassive { AuraAttack = 30 };
            Assert.That(summon.AuraAttack, Is.EqualTo(30));
        }

        [Test]
        public void AuraAttack_SurvivesClone()
        {
            var original = new SummonPassive { AuraAttack = 30, Thorns = 50 };
            var copy = original.Clone();
            Assert.That(copy.AuraAttack, Is.EqualTo(30), "Clone 漏字段的表现是光环在存档往返后消失");
            Assert.That(copy.Thorns, Is.EqualTo(50));
        }
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "SummonAuraTests"
```

预期:编译错 —— `SummonPassive` 没有 `AuraAttack`。

- [ ] **Step 3: 加字段**

`SummonPassive.cs`,放在 `Regen` 之后(与 `HealAlly` / `Regen` 这一组同区):

```csharp
        /// <summary>攻击光环:给场上**全部**召唤物 +N 攻,**含自己**(2026-09-05,𣛧)。
        ///
        /// 「含自己」是必要的配套:2026-09-05 召唤只数收归 1 只后,只加别人的光环
        /// 在场上没有作用对象、纯空转。同场打出别的召唤字时光环照常外溢。
        ///
        /// 与 <see cref="EffectDef.SummonShield"/> 的区别:那个是**召唤当时**发一次性护盾,
        /// 这条是**持续**加成 —— 由 <c>SummonState.EffectiveAttack</c> 每次出手时现读,
        /// 所以后来入场的召唤物也吃得到。
        ///
        /// 不吃卡等级 —— 同 <see cref="Regen"/>,回归「被动数值是节奏,不随等级变」的缺省。</summary>
        public int AuraAttack { get; set; }
```

**同时补 `Clone()`** —— 打开 `SummonPassive.Clone()`,在字段拷贝列表里加 `AuraAttack = AuraAttack,`。漏了的表现是光环在存档往返后消失,不是编译错。

`ConfigLoader.cs`:`SummonPassive` 走 Newtonsoft 按属性名自动绑定,`chars.json` 里写 `"auraAttack": 30` 即可,**不需要改 ConfigLoader**。用下面这条确认:

```bash
grep -n "auraAttack\|AuraAttack\|Regen\|regen" Brushblade/Assets/_Project/Data/ConfigLoader.cs
```

若 `regen` 也没有出现在 ConfigLoader 里,说明确实是自动绑定,本步不动它;若有显式映射表,照 `regen` 那行的样子加一行。

- [ ] **Step 4: 让光环真正生效**

`EnemyDef.cs` 的 `SummonState.EffectiveAttack` 需要读到「场上所有召唤物的光环之和」,而 `SummonState` 拿不到场面。照既有 `PlayerAttackPercent` 之外再加一个由引擎注入的字段:

```csharp
        /// <summary>场上全部召唤物的攻击光环之和(2026-09-05),由 <c>BattleEngine</c>
        /// 在每次结算前刷新。长在这里而不是引擎里算:详情弹窗(Presentation.SummonInfo)
        /// 要显示同一个数,表现层不该自己再推一遍规则。</summary>
        public int AuraAttackBonus { get; internal set; }
```

`BattleEngine` 里在召唤物出手前(`ActSummonTurn` 开头)与召唤/死亡后刷新:

```csharp
        /// <summary>刷新全场召唤物的光环加成(2026-09-05)。光环含自己,所以就是所有
        /// 存活召唤物 AuraAttack 之和,人人相同 —— 不需要「排除自己」的分支。
        /// 召唤、死亡、替换之后都要调:后来入场的召唤物也吃得到既有光环。</summary>
        private void RefreshSummonAura()
        {
            int total = 0;
            foreach (var summon in _summons)
                if (summon != null && summon.Alive) total += summon.Passive?.AuraAttack ?? 0;
            foreach (var summon in _summons)
                if (summon != null) summon.AuraAttackBonus = total;
        }
```

`EffectiveAttack` 改为(本步只加光环那一项,百分比乘区留给任务 5):

```csharp
        public int EffectiveAttack => System.Math.Max(0,
            Attack + Statuses.TotalMagnitude(StatusKind.AttackBuff) + AuraAttackBonus);
```

补一条集成测试(同文件):

```csharp
        /// <summary>两只带光环的召唤物同场:各自都吃到两份光环(含自己 + 对方)。</summary>
        [Test]
        public void AuraAttack_StacksAcrossSummonsIncludingSelf()
        {
            // SummonState(summonChar, element, hp, attack, passive, sourceChar) —— EnemyDef.cs:237
            var a = new SummonState("甲", Element.Heart, 100, 50,
                new SummonPassive { AuraAttack = 20 });
            var b = new SummonState("乙", Element.Heart, 100, 50,
                new SummonPassive { AuraAttack = 20 });
            // AuraAttackBonus 由引擎的 RefreshSummonAura 注入;单元测试里直接设,
            // 集成路径由下面 AuraAttack_RefreshedOnSummon 那条覆盖。
            a.AuraAttackBonus = 40; b.AuraAttackBonus = 40;
            Assert.That(a.EffectiveAttack, Is.EqualTo(90), "50 + 20(自己) + 20(对方)");
            Assert.That(b.EffectiveAttack, Is.EqualTo(90));
        }

        /// <summary>集成路径:真实 Cast() 出两张带光环的召唤字,引擎必须把光环刷进去。
        /// 只测单元不测这条的话,`RefreshSummonAura()` 忘了在召唤后调都发现不了。</summary>
        [Test]
        public void AuraAttack_RefreshedOnSummon()
        {
            var graph = RebalanceFixture.Graph(
                RebalanceFixture.Char("召甲", new EffectDef(EffectKind.Summon, 100,
                    summonAttack: 50, summonChar: "甲",
                    passive: new SummonPassive { AuraAttack = 20 })),
                RebalanceFixture.Char("召乙", new EffectDef(EffectKind.Summon, 100,
                    summonAttack: 50, summonChar: "乙",
                    passive: new SummonPassive { AuraAttack = 20 })));
            var battle = RebalanceFixture.Battle(graph, new[] { "召甲", "召乙" },
                RebalanceFixture.Mob());
            battle.Cast("召甲");
            battle.Cast("召乙");
            foreach (var summon in battle.Summons)
                if (summon != null && summon.Alive)
                    Assert.That(summon.EffectiveAttack, Is.EqualTo(90),
                        "两只各吃两份光环(含自己)");
        }
```

- [ ] **Step 5: 跑全量 + 仿真编译**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd ../balance && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

预期:全绿,`error CS` 一条都没有。既有测试**不应该**有任何变化 —— `AuraAttack` 缺省 0,`AuraAttackBonus` 缺省 0,恒等。若有测试变红,是 `RefreshSummonAura` 的调用点插错了位置。

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Core/SummonPassive.cs Brushblade/Assets/_Project/Core/EnemyDef.cs Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/SummonAuraTests.cs Brushblade/Assets/_Project/Tests/SummonAuraTests.cs.meta
git commit -m "feat(summon): 新增攻击光环 AuraAttack(含自己)

召唤只数收归 1 只后,只加别人的光环在场上没有作用对象、纯空转,
故含自己。由引擎在召唤/死亡/出手前刷新,后来入场的也吃得到。"
```

---

### Task 5: 召唤物攻击接入战意 + 厚的百分比乘区

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EnemyDef.cs`(`SummonState.EffectiveAttack` + 新字段 `PlayerAttackPercent`)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(注入 percent)
- Test: `Brushblade/Assets/_Project/Tests/SummonAuraTests.cs`(追加)

**Interfaces:**
- Consumes: 任务 4 的 `AuraAttackBonus`
- Produces: `SummonState.PlayerAttackPercent { get; internal set; }`(int,缺省 **100**);`EffectiveAttack` 变为 `(Attack + AttackBuff + AuraAttackBonus) × PlayerAttackPercent / 100`

> ⚠ **本任务推翻 2026-08-28 的拍板**。`EnemyDef.cs:214` 的注释原文:「与玩家侧 `BattleEngine.EffectiveAttack` 同形,但刻意**不含战意那个乘区**:战意是玩家专属……用户 2026-08-28 明确留在玩家侧」。2026-09-05 用户重新裁定:召唤物攻击跟随战意与厚变动(基础 100 → 战意 1 层 110 → 5 层 150 → 归零回 100)。**实现时要把那段旧注释改掉,不能留着自相矛盾。**

> ⚠ **`ScaleByBaseAttack`(护盾 / 治疗)绝不能跟着改成读 `EffectiveAttack`**。它自己的注释写明了原因:厚进 `EffectiveAttack` 的百分比乘区 → 护盾吃 `EffectiveAttack` → 堆盾涨厚 → 厚放大护盾 → 涨更多厚,是正反馈环。召唤物攻击接厚**是安全的**(召唤物攻击不产生护盾),护盾/治疗接厚不安全。本任务只碰召唤物那一条。

- [ ] **Step 1: 写失败测试**

追加到 `SummonAuraTests.cs`:

```csharp
        /// <summary>召唤物攻击吃玩家的战意 + 厚(2026-09-05,推翻 2026-08-28 的「战意玩家专属」)。
        ///
        /// 战意 1 层 = +10%、5 层 = +50%;厚 1 层 = +5%、10 层 = +50%。满 buff = ×2。
        /// 归零后回到基础值 —— 乘区是现读的,不是出手时冻结的。</summary>
        [Test]
        public void SummonAttack_ReadsMoraleAndHeft()
        {
            var summon = new SummonState("甲", Element.Heart, 100, 100);
            summon.PlayerAttackPercent = 100;
            Assert.That(summon.EffectiveAttack, Is.EqualTo(100), "无 buff 时恒等 —— 恒等性硬线");

            summon.PlayerAttackPercent = 110;   // 战意 1 层
            Assert.That(summon.EffectiveAttack, Is.EqualTo(110));

            summon.PlayerAttackPercent = 150;   // 战意 5 层
            Assert.That(summon.EffectiveAttack, Is.EqualTo(150));

            summon.PlayerAttackPercent = 200;   // 战意 5 层 + 厚 10 层
            Assert.That(summon.EffectiveAttack, Is.EqualTo(200));

            summon.PlayerAttackPercent = 100;   // 战意衰减归零
            Assert.That(summon.EffectiveAttack, Is.EqualTo(100), "归零回到基础值,乘区是现读的");
        }

        /// <summary>缺省 100:老存档里的召唤物没有这个字段,反序列化得 0 会把攻击归零。</summary>
        [Test]
        public void SummonAttack_DefaultPercent_IsHundred()
        {
            var summon = new SummonState("甲", Element.Heart, 100, 77);
            Assert.That(summon.EffectiveAttack, Is.EqualTo(77), "没显式设 percent 也不能打成 0");
        }

        /// <summary>加点(AttackBuff / 光环)先加,百分比后乘 —— 与玩家侧
        /// EffectiveAttack 的「先加后乘」同序。反过来会让加点吃不到战意的放大。</summary>
        [Test]
        public void SummonAttack_FlatBeforePercent()
        {
            var summon = new SummonState("甲", Element.Heart, 100, 100);
            summon.AuraAttackBonus = 50;
            summon.PlayerAttackPercent = 150;
            Assert.That(summon.EffectiveAttack, Is.EqualTo(225), "(100 + 50) × 1.5,不是 100×1.5 + 50");
        }
```

构造签名是 `SummonState(summonChar, element, hp, attack, passive = null, sourceChar = null)`(`EnemyDef.cs:237`),`passive` 可省 —— 本组测试不需要被动。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "SummonAttack_"
```

预期:编译错 —— 没有 `PlayerAttackPercent`。

- [ ] **Step 3: 加字段并改算式**

`EnemyDef.cs`,`SummonState` 里:

```csharp
        /// <summary>玩家侧的攻击百分比乘区(2026-09-05):100 + 战意层×10 + 厚层×5,
        /// 由 <c>BattleEngine</c> 在结算前注入。**缺省 100** —— 缺省 0 会把老存档里
        /// 召唤物的攻击整体归零,而那是静默的。
        ///
        /// ⚠ 这条推翻 2026-08-28 的「战意是玩家专属,召唤物不吃」。2026-09-05 用户重新裁定:
        /// 召唤物攻击跟随战意与厚变动(基础 100 → 战意 1 层 110 → 5 层 150 → 归零回 100),
        /// 木土/木金混色卡组因此成为一条真流派。木系召唤字的基础攻已在 P2 按 ×0.70 重定标
        /// (仿真实测:×1.00 时木系均卒层 22.4,远超水 15.6 / 土 17.4)。</summary>
        public int PlayerAttackPercent { get; internal set; } = 100;

        /// <summary>这一拍的有效攻击力 = (基础攻击 + 自己袋子里的攻击增益 + 攻击光环)
        /// × 玩家侧百分比乘区 ÷ 100。
        ///
        /// **先加后乘**,与玩家侧 <c>BattleEngine.EffectiveAttack</c> 同序 —— 反过来会让
        /// 加点(剡 的 Empower、光环)吃不到战意的放大。
        /// 钳到 ≥0:负攻击力会打出负伤害 = 给敌人回血,且全程无声。
        ///
        /// 长在这里而不是 BattleEngine 里:详情弹窗(Presentation.SummonInfo)要显示
        /// 同一个数,而表现层不该自己再推一遍规则 —— 那正是两处口径分叉的起点。</summary>
        public int EffectiveAttack => System.Math.Max(0,
            (Attack + Statuses.TotalMagnitude(StatusKind.AttackBuff) + AuraAttackBonus)
            * System.Math.Max(0, PlayerAttackPercent) / 100);
```

`BattleEngine` 里抽一个属性并在刷新光环的同一处注入:

```csharp
        /// <summary>注入给召唤物的攻击百分比乘区(2026-09-05)= 100 + 战意层×10 + 厚层×5。
        /// 与玩家侧 <see cref="EffectiveAttack"/> 的 percent 同一个式子,故两处永远同步。</summary>
        private int SummonAttackPercent => 100
            + _playerStatuses.TotalMagnitude(StatusKind.Morale) * MoralePercentPerStack
            + _playerStatuses.TotalMagnitude(StatusKind.Heft) * HeftPercentPerStack;
```

并把 `RefreshSummonAura()` 里那个第二个循环改成同时刷两项:

```csharp
            int percent = SummonAttackPercent;
            foreach (var summon in _summons)
                if (summon != null) { summon.AuraAttackBonus = total; summon.PlayerAttackPercent = percent; }
```

⚠ **战意/厚会在回合中途变化**(出一张金系字就 +1 层战意)。所以 `RefreshSummonAura()` 必须在**每次召唤物出手前**调,不能只在召唤时调。检查 `ActSummonTurn` 的开头有没有这一句。

- [ ] **Step 4: 跑全量测试 + 确认恒等**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:新增三条 PASS。**既有召唤物测试凡是没挂战意/厚的都必须逐字节不变**(percent = 100 → `v × 100 / 100 == v`)。有挂战意的集成测试会变红 —— 那是本任务的正确后果,改期望值并注明。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/EnemyDef.cs Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/SummonAuraTests.cs
git commit -m "feat(summon): 召唤物攻击接入战意 + 厚的百分比乘区

推翻 2026-08-28 的「战意玩家专属」(用户 2026-09-05 重新裁定)。
先加后乘,与玩家侧 EffectiveAttack 同序。ScaleByBaseAttack 刻意不动 ——
护盾/治疗接厚会造出「堆盾涨厚 → 厚放大护盾」的正反馈环。"
```

---

### Task 6: 反伤总量钳位 60%

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`DamagePlayerDirect` 里的 reflect 段,约 3339 行;以及召唤物那一支约 3452 行)
- Create: `Brushblade/Assets/_Project/Tests/ReflectClampTests.cs` + `.meta`

**Interfaces:**
- Consumes: 无
- Produces: `BattleEngine.MaxReflectPercent`(private const int = 60)

> 本任务兑现引擎里一条已存在的警告(`BattleEngine.cs:3335`):「日后加第二张反弹字之前,先想清楚上限 —— 两张 60% 同在身会反弹 120%,比挨的还多。」P2 会让 `壁`(绿 30%)与 `圭`(金 50%)同时存在,叠加 80%。

- [ ] **Step 1: 写失败测试**

```csharp
using System;
using NUnit.Framework;
using Brushblade.Core;

namespace Brushblade.Core.Tests
{
    /// <summary>反伤总量钳位 60%(2026-09-05,平衡重做 P0 任务 6)。
    ///
    /// 兑现 BattleEngine.cs:3335 的既有警告:此前刻意不钳位,理由是「字表只有一个
    /// Reflect 字,多来源叠加现实不可达」。P2 让 壁(绿 30%)与 圭(金 50%)同时存在,
    /// 叠加 80% —— 那条前提失效了。
    ///
    /// 60 不是拍的:30 层一轮敌方总伤 936,936 × 60% = 562,约等于红档单攻锚点 600。
    /// 即「站着挨满一整轮」的反伤收益 ≈ 一张红档输出字,再高站桩流的输出就会超过
    /// 专职输出系(设计稿 §1.6)。</summary>
    public sealed class ReflectClampTests
    {
        [Test]
        public void Reflect_TwoSources_ClampedToSixtyPercent()
        {
            Assert.That(BounceFrom(30, 50), Is.EqualTo(60), "合 80% 被钳到 60%,不是 80");
        }

        [Test]
        public void Reflect_UnderCap_NotClamped()
        {
            Assert.That(BounceFrom(30), Is.EqualTo(30), "没到上限就原样反");
        }

        [Test]
        public void Reflect_Zero_BouncesNothing()
        {
            Assert.That(BounceFrom(), Is.EqualTo(0), "0 层不反 —— 恒等");
        }
    }
}
```

夹具:出两张 `Reflect` 测试字把状态挂上,再用 `EndTurn()` 让敌人打一记(同
`HeftTests.NewBattleWithCharTakingDamage` 的手法),敌人攻击力设 100 好对账。
测试类里加这一个私有方法即可,不另建 Fixture 类:

```csharp
        /// <summary>挂上给定百分比的反伤,让敌人打玩家一记,返回敌人掉了多少血。
        /// 敌人攻击 100、血 100000(打不死),玩家 MaxHp 500 且不出护盾 ——
        /// 反伤按「打过来的总伤害」算,不掺护盾吸收。</summary>
        private static int BounceFrom(params int[] reflectPercents)
        {
            var chars = new System.Collections.Generic.List<CharDef>();
            var library = new System.Collections.Generic.List<string>();
            for (int i = 0; i < reflectPercents.Length; i++)
            {
                if (reflectPercents[i] <= 0) continue;
                string id = $"映{i}";
                chars.Add(RebalanceFixture.Char(id,
                    new EffectDef(EffectKind.Reflect, reflectPercents[i], turns: 5)));
                library.Add(id);
            }
            var battle = RebalanceFixture.Battle(
                RebalanceFixture.Graph(chars.ToArray()), library,
                RebalanceFixture.Mob(attack: 100));
            foreach (var id in library) battle.Cast(id, 0);
            int before = battle.Enemies[0].Hp;
            battle.EndTurn();
            return before - battle.Enemies[0].Hp;
        }
```

⚠ 两张字必须是**不同的 id**(`映0` / `映1`)—— `StatusBag.Apply()` 对同一 `SourceId`
走覆盖刷新而不是叠加,同 id 两张只会得到一份,那就测不到叠加了。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "ReflectClampTests"
```

预期:第一条 FAIL(实得 80),后两条 PASS。

- [ ] **Step 3: 加钳位**

`BattleEngine.cs` 常量区:

```csharp
        /// <summary>反伤总量上限(百分点,2026-09-05)。
        ///
        /// 此前刻意不钳位,理由是「字表只有一个 Reflect 字,多来源叠加现实不可达」;
        /// P2 让 壁(绿 30%)与 圭(金 50%)同时存在,那条前提失效。
        /// 60 的依据:30 层一轮敌方总伤 936,×60% = 562 ≈ 红档单攻锚点 600 ——
        /// 「站着挨满一整轮」的反伤收益约等于一张红档输出字(设计稿 §1.6)。</summary>
        private const int MaxReflectPercent = 60;
```

玩家侧(`DamagePlayerDirect`)把取值那行包起来:

```csharp
            int reflect = allowReflect
                ? Math.Min(MaxReflectPercent, _playerStatuses.TotalMagnitude(StatusKind.Reflect))
                : 0;
```

召唤物那一支(约 3452 行)同样钳 —— **玩家与召唤物的反伤各自钳,不是合起来钳**:两条是分开的伤害管道,合起来钳会让「打召唤物」意外吃到玩家身上的层数上限。

```csharp
            int reflect = Math.Min(MaxReflectPercent,
                _playerStatuses.TotalMagnitude(StatusKind.Reflect)
                + summon.Statuses.TotalMagnitude(StatusKind.Reflect));
```

顺手把 3335 行那段「刻意不钳位」的旧注释删掉 —— 留着会与新常量自相矛盾。

- [ ] **Step 4: 跑全量测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿。既有反伤测试的单字最高是 50%,都在上限内,**不应有任何既有测试变化**。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/ReflectClampTests.cs Brushblade/Assets/_Project/Tests/ReflectClampTests.cs.meta
git commit -m "feat(battle): 反伤总量钳位 60%

兑现 BattleEngine.cs:3335 的既有警告 —— P2 让 壁(30%)与 圭(50%)
同时存在,叠加 80%,「多来源不可达」的前提失效。
60% 的依据:30 层一轮总伤 936 × 60% = 562 ≈ 红档单攻锚点 600。"
```

---

### Task 7: 限时增益 —— `Empower` / `CritBuff` 支持 `Turns`

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`Empower` / `CritBuff` 两个 case)
- Create: `Brushblade/Assets/_Project/Tests/TimedBuffTests.cs` + `.meta`

**Interfaces:**
- Consumes: 无
- Produces: `Empower` / `CritBuff` 施加的 `StatusEffect.TurnsLeft` 取 `effect.Turns`,`Turns <= 0` 时仍为 `-1`(本场持久,保住既有行为)

> P2 会让 `利`(白档,增攻 +30 × 2 回合)与 `锋`(绿档,暴击 +20% × 3 回合)成为唯一两张吃 `Turns` 的增益字。本场持久的变体随之 0 载体、进休眠。

- [ ] **Step 1: 写失败测试**

```csharp
using System;
using NUnit.Framework;
using Brushblade.Core;

namespace Brushblade.Core.Tests
{
    /// <summary>限时增益(2026-09-05,平衡重做 P0 任务 7)。
    ///
    /// Empower / CritBuff 此前一律本场持久(TurnsLeft = -1)。P2 的 利(增攻 +30 × 2 回合)
    /// 与 锋(暴击 +20% × 3 回合)需要限时版,且回合数吃卡等级(任务 8)。
    ///
    /// **Turns <= 0 仍是本场持久** —— 既有字表全部没填 turns,这条兜住它们的行为不变。</summary>
    public sealed class TimedBuffTests
    {
        [Test]
        public void Empower_WithTurns_ExpiresAfterThatManyTurnEnds()
        {
            var battle = BuffBattle(EffectKind.Empower, value: 30, turns: 2);
            battle.Cast("增");
            Assert.That(battle.EffectiveAttack, Is.EqualTo(130), "基准 100 + 30");

            battle.EndTurn();
            Assert.That(battle.EffectiveAttack, Is.EqualTo(130), "第 1 个回合末还在");

            battle.EndTurn();
            Assert.That(battle.EffectiveAttack, Is.EqualTo(100), "第 2 个回合末到期,回到基准");
        }

        [Test]
        public void Empower_WithoutTurns_StaysAllBattle()
        {
            var battle = BuffBattle(EffectKind.Empower, value: 50, turns: 0);
            battle.Cast("增");
            battle.EndTurn();
            battle.EndTurn();
            battle.EndTurn();
            Assert.That(battle.EffectiveAttack, Is.EqualTo(150), "turns 缺省 → 本场持久,既有行为不变");
        }

        [Test]
        public void CritBuff_WithTurns_Expires()
        {
            var battle = BuffBattle(EffectKind.CritBuff, value: 20, turns: 3);
            battle.Cast("增");
            Assert.That(battle.EffectiveCrit, Is.EqualTo(20));
            battle.EndTurn(); battle.EndTurn(); battle.EndTurn();
            Assert.That(battle.EffectiveCrit, Is.EqualTo(0), "3 个回合末后到期");
        }
    }
}
```

测试类里加这一个私有方法:

```csharp
        /// <summary>一张纯增益字 "增" 的战斗。敌人血量给大值 —— 增益字不打伤害,
        /// 但 EndTurn() 会让敌人反击,靶子不能被打死(死了 Phase 变 Won,EndTurn 就不再推进)。</summary>
        private static BattleEngine BuffBattle(EffectKind kind, int value, int turns)
        {
            var graph = RebalanceFixture.Graph(
                RebalanceFixture.Char("增", new EffectDef(kind, value, turns: turns)));
            return RebalanceFixture.Battle(graph, new[] { "增", "增", "增", "增" },
                RebalanceFixture.Mob());
        }
```

⚠ 库里放四张 "增" 是因为「出字即消耗」(`BattleEngine.cs:965`)—— 一张出完就没了,
而测试要在出字后连着 `EndTurn()` 三次。库里只放一张不影响这几条断言,但放多张更抗改。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "TimedBuffTests"
```

预期:第一、三条 FAIL(buff 永不到期),第二条 PASS。

- [ ] **Step 3: 改两个 case**

`BattleEngine.cs` 的 `Empower` case,把 `TurnsLeft = -1` 换成:

```csharp
                            // 限时增益(2026-09-05):turns > 0 时按回合到期,
                            // **turns <= 0 仍为 -1(本场持久)** —— 既有字表全没填 turns,
                            // 这条兜住它们逐字节不变。
                            TurnsLeft = effect.Turns > 0 ? effect.Turns : -1,
```

`CritBuff` case 同样一行。

⚠ **确认 `TickTurns` 真的会碰到它们**。`BattleEngine` 里有一段注释说 `ApBoost / CritBuff / PierceBuff / Empower` 是「本场持久」、`TickTurns` 只认 `TurnsLeft`。既然现在 `TurnsLeft` 可能是正数,要确认这四条状态确实走 `TickTurns`,而不是被某个白名单排除掉了:

```bash
grep -n "TickTurns" -A 20 Brushblade/Assets/_Project/Core/BattleEngine.cs | head -30
```

若 `TickTurns` 有 Kind 白名单/黑名单,把 `Empower`(`AttackBuff`)与 `CritBuff` 放进能被递减的那一侧,并更新那段说「它们是本场持久」的注释。

- [ ] **Step 4: 跑全量测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:三条新测试 PASS,既有测试**全部不变**(既有字表没有一张填了 turns)。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/TimedBuffTests.cs Brushblade/Assets/_Project/Tests/TimedBuffTests.cs.meta
git commit -m "feat(battle): Empower / CritBuff 支持 Turns(限时增益)

turns <= 0 仍为本场持久,兜住既有字表逐字节不变。
P2 的 利(+30×2 回合)与 锋(+20%×3 回合)是唯二载体。"
```

---

### Task 8: 卡等级缩放回合数

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Meta.cs`(`ScaleByCardLevel` 旁新增 `ScaleTurnsByCardLevel`,约 512 行)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`Empower` / `CritBuff` 两个 case 的 turns 取值)
- Test: `Brushblade/Assets/_Project/Tests/TimedBuffTests.cs`(追加)

**Interfaces:**
- Consumes: 任务 7 的 `TurnsLeft = effect.Turns > 0 ? … : -1`
- Produces: `MetaRules.ScaleTurnsByCardLevel(int baseTurns, int cardLevel)` → int

- [ ] **Step 1: 写失败测试**

```csharp
        /// <summary>回合数吃卡等级(2026-09-05,平衡重做 P0 任务 8)。
        ///
        /// 现有 ScaleByCardLevel 是「基础值 × (1 + 0.1 × (等级 − 1)),向上取整」——
        /// 那条给数值用没问题,给回合数用就太快了:2 回合的字在 6 级就变 3 回合、
        /// 11 级变 4 回合。回合数是**节奏**,每 5 级 +1 才合适。</summary>
        [TestCase(2, 1, 2)]
        [TestCase(2, 4, 2)]
        [TestCase(2, 5, 3)]
        [TestCase(2, 10, 4)]
        [TestCase(3, 1, 3)]
        [TestCase(3, 5, 4)]
        public void ScaleTurnsByCardLevel_AddsOnePerFiveLevels(int baseTurns, int level, int expected)
        {
            Assert.That(MetaRules.ScaleTurnsByCardLevel(baseTurns, level), Is.EqualTo(expected));
        }

        /// <summary>1 级恒等 —— 与 ScaleByCardLevel 同一条硬线。</summary>
        [Test]
        public void ScaleTurnsByCardLevel_LevelOne_IsIdentity()
        {
            Assert.That(MetaRules.ScaleTurnsByCardLevel(7, 1), Is.EqualTo(7));
            Assert.That(MetaRules.ScaleTurnsByCardLevel(0, 1), Is.EqualTo(0), "0 回合(=本场持久)不许被抬成 1");
        }
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "ScaleTurnsByCardLevel"
```

预期:编译错 —— 没有这个方法。

- [ ] **Step 3: 实现**

`Meta.cs`,紧跟 `ScaleByCardLevel` 之后:

```csharp
        /// <summary>卡等级回合数系数(2026-09-05):**每 5 级 +1 回合**,1 级恒等。
        ///
        /// 刻意**不复用** <see cref="ScaleByCardLevel"/> 的 ×(1 + 0.1×(级−1)) + ceiling ——
        /// 那条对回合数太快:2 回合的字 6 级就变 3、11 级变 4。回合数是节奏不是数值,
        /// 一张限时增攻字在满级变成半永久会把「限时」这个设计整个抹掉。
        ///
        /// `baseTurns == 0` 原样返回 —— 0 在效果侧的语义是「本场持久」(见
        /// BattleEngine 的 Empower/CritBuff case),被抬成 1 会把持久字改成 1 回合字。</summary>
        public static int ScaleTurnsByCardLevel(int baseTurns, int cardLevel)
        {
            if (baseTurns <= 0 || cardLevel <= 1) return baseTurns;
            return baseTurns + (cardLevel - 1) / 5;
        }
```

`BattleEngine.cs` 的 `Empower` / `CritBuff` 两个 case,把 turns 取值改成过缩放:

```csharp
                            TurnsLeft = effect.Turns > 0
                                ? MetaRules.ScaleTurnsByCardLevel(effect.Turns, cardLevel)
                                : -1,
```

⚠ 确认那两个 case 的作用域里**拿得到 `cardLevel`**。`ApplyEffects` 里有 `MetaRules.ScaleByCardLevel(effect.SummonShield, cardLevel)` 这样的用法,说明变量在;若不在,从 `ApplyEffects` 的签名往下传,别在 case 里重新查一遍卡等级(那会成为第二个真相来源)。

- [ ] **Step 4: 跑全量测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿,既有测试不变(既有字表 turns 全为 0)。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/Meta.cs Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/TimedBuffTests.cs
git commit -m "feat(meta): 新增 ScaleTurnsByCardLevel —— 每 5 级 +1 回合

刻意不复用 ScaleByCardLevel:×(1+0.1×(级−1)) 对回合数太快,
2 回合的字 6 级变 3、11 级变 4,「限时」这个设计会被抹掉。
baseTurns == 0 原样返回 —— 0 的语义是本场持久。"
```

---

### Task 9: 封禁 —— `Silence` 语义扩展 + Boss 降级

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`EffectiveEnemyDefense` 约 543 行、`IsSilenced` 约 2882 行、`Silence` case 约 2207 行)
- Create: `Brushblade/Assets/_Project/Tests/SuppressTests.cs` + `.meta`

**Interfaces:**
- Consumes: 无
- Produces:
  - `EffectiveEnemyDefense(EnemyState, int, StatusBag)` 行为变更:目标带 `Silence` 时,杂兵护甲按 **0** 算、Boss 按 **一半** 算
  - `IsSilenced(EnemyState)` 语义不变(仍只在**非 Boss** 时让 `EnemyAbility` 哑火)—— 新增 `IsAbilitySilenced(EnemyState)` 承担这个判断,`IsSilenced` 退化成纯状态查询
  - 新事件:`BattleEventKind.SuppressDowngraded`(Boss 吃到降级时发一条,表现层才能告诉玩家)

> 玩家可见名定为**「封禁」**。语义从「目标的主动机制哑火」扩到「特殊能力全部失效:护甲归零、被动不触发、主动机制哑火」。**对 Boss 降级**为「护甲减半,持续 N 回合」,被动与大招不受影响 —— 与斩杀对 Boss 从「直杀」降为「吃双倍」是同一条设计纪律(高价值控制不该在最关键的战斗里变成废牌,但也不能全效)。

- [ ] **Step 1: 写失败测试**

```csharp
using System;
using NUnit.Framework;
using Brushblade.Core;

namespace Brushblade.Core.Tests
{
    /// <summary>封禁(2026-09-05,平衡重做 P0 任务 9)。复用 0 载体的 EffectKind.Silence,
    /// 把语义从「主动机制哑火」扩到「特殊能力全部失效」:护甲归零、被动不触发、主动机制哑火。
    ///
    /// **对 Boss 降级**为「护甲减半」,被动与大招不受影响 —— 同斩杀对 Boss 从「直杀」
    /// 降为「吃双倍」的设计纪律。P2 的载体:灭(火白)· 海(绿)· 澡(紫)· 湮(紫)·
    /// 沐(金)· 淋(橙,2 回合)。</summary>
    public sealed class SuppressTests
    {
        [Test]
        public void Suppress_ZeroesMobArmor()
        {
            var battle = SuppressBattle(armor: 20, isBoss: false);
            battle.Cast("封", 0);
            Assert.That(HitFor(battle), Is.EqualTo(100), "护甲归零,100 全打进去");
        }

        [Test]
        public void Suppress_HalvesBossArmor()
        {
            var battle = SuppressBattle(armor: 60, isBoss: true);
            battle.Cast("封", 0);
            Assert.That(HitFor(battle), Is.EqualTo(70), "Boss 降级:护甲 60 → 30,100 − 30 = 70");
        }

        [Test]
        public void Suppress_OnBoss_DoesNotSilenceAbility()
        {
            var battle = SuppressBattle(armor: 0, isBoss: true, turns: 2);
            battle.Cast("封", 0);
            battle.EndTurn();
            Assert.That(AbilityFired(battle), Is.True,
                "Boss 的大招不受封禁影响 —— 只削护甲");
        }

        [Test]
        public void Suppress_OnMob_SilencesAbility()
        {
            var battle = SuppressBattle(armor: 0, isBoss: false, turns: 2);
            battle.Cast("封", 0);
            battle.EndTurn();
            Assert.That(AbilityFired(battle), Is.False,
                "杂兵的主动机制哑火");
        }

        [Test]
        public void Suppress_OnBoss_EmitsDowngradeEvent()
        {
            var battle = SuppressBattle(armor: 60, isBoss: true);
            battle.Cast("封", 0);
            bool found = false;
            foreach (var e in battle.LastEvents)
                if (e.Kind == BattleEventKind.SuppressDowngraded) found = true;
            Assert.That(found, Is.True,
                "不发事件的话玩家只会以为是 bug —— 卡面写着全禁,Boss 身上却只掉一半甲");
        }

        [Test]
        public void Suppress_Expires()
        {
            var battle = SuppressBattle(armor: 20, isBoss: false);
            battle.Cast("封", 0);
            battle.EndTurn();
            Assert.That(HitFor(battle), Is.EqualTo(80), "到期后护甲 20 回来");
        }
    }
}
```

测试类里加这两个私有方法:

```csharp
        /// <summary>一场带「封」(封禁 N 回合)与「打」(单体 100)两张字的战斗。
        /// 靶子按 isBoss 选杂兵或 Boss —— Boss 必须带 phases,IsBoss 判的就是 Phases.Count > 0。</summary>
        private static BattleEngine SuppressBattle(int armor, bool isBoss, int turns = 1,
            int enemyAttack = 0)
        {
            var graph = RebalanceFixture.Graph(
                RebalanceFixture.Char("封", new EffectDef(EffectKind.Silence, 0, turns: turns)),
                RebalanceFixture.Char("打", new EffectDef(EffectKind.DamageSingle, 100)));
            var target = isBoss
                ? RebalanceFixture.Boss(attack: enemyAttack, armor: armor)
                : RebalanceFixture.Mob(attack: enemyAttack, armor: armor);
            return RebalanceFixture.Battle(graph, new[] { "封", "打", "打" }, target);
        }

        /// <summary>出「打」并返回敌人掉了多少血。</summary>
        private static int HitFor(BattleEngine battle)
        {
            int before = battle.Enemies[0].Hp;
            battle.Cast("打", 0);
            return before - battle.Enemies[0].Hp;
        }
```

`EnemyAbility` 那两条测试还要第三个方法。**先给 `RebalanceFixture.Mob` / `Boss` 各加一个
`EnemyAbility ability = EnemyAbility.None` 可选参数**(`EnemyDef` 构造函数第 5 位就是它,
`EnemyDef.cs:150`),然后:

```csharp
        /// <summary>这一回合敌人的 EnemyAbility 发动了吗。
        /// 用 EnemyAbility.Buff 造靶子 —— 它给「另一只存活敌人」加攻,
        /// 所以要两只敌人才看得见效果(HasOtherAliveEnemy 是它的前置)。
        /// 判据是敌人身上有没有多出 AttackBuff,而不是找某个事件 Kind ——
        /// 事件枚举将来可能重命名,状态袋是行为本身。</summary>
        private static bool AbilityFired(BattleEngine battle)
        {
            foreach (var enemy in battle.Enemies)
                if (enemy.Statuses.TotalMagnitude(StatusKind.AttackBuff) > 0) return true;
            return false;
        }
```

⚠ 这两条测试的 `SuppressBattle` 要改成造**两只**带 `EnemyAbility.Buff` 的靶子
(`Buff` 能力的前置是 `HasOtherAliveEnemy`,单只时它本来就不发动 —— 那样两条测试都会
"通过"而什么也没测到)。给 `SuppressBattle` 加一个 `EnemyAbility ability` 参数,
`isBoss` 时两只都用 `Boss(...)`、否则都用 `Mob(...)`,被封禁的是 index 0。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "SuppressTests"
```

预期:编译错(`BattleEventKind.SuppressDowngraded` 不存在)+ 护甲相关几条 FAIL。

- [ ] **Step 3: 实现护甲那一半**

`BattleEngine.cs`,`EffectiveEnemyDefense` 改成:

```csharp
        /// <summary>敌人挨一记时的有效护甲(点数)= 基础护甲 − 身上的破甲 − 攻击方穿透,下钳 0。
        ///
        /// **封禁(2026-09-05)**:目标带 <see cref="StatusKind.Silence"/> 时,
        /// 杂兵的护甲按 0 算、**Boss 按一半算**(降级,见 <see cref="SuppressArmorOf"/>)。
        /// 封禁挂在敌人身上,削的是它的防御,不是玩家的输出 —— 与穿透(已并入破甲、
        /// 2026-09-05 休眠)那条「给我方挂 buff」的旧路子相反。</summary>
        private int EffectiveEnemyDefense(EnemyState enemy, int pierce, StatusBag attackerBag = null) => Math.Max(0,
            SuppressArmorOf(enemy)
            - enemy.Statuses.TotalMagnitude(StatusKind.ArmorBreak)
            - (pierce + (attackerBag ?? _playerStatuses).TotalMagnitude(StatusKind.PierceBuff)));

        /// <summary>封禁作用后的基础护甲(2026-09-05)。杂兵归零、Boss 减半、无封禁原值。
        ///
        /// Boss 降级而不是免疫:同斩杀对 Boss 从「直杀」降为「吃双倍」的纪律 ——
        /// 高价值控制不该在最关键的战斗里变成废牌,但也不能全效。</summary>
        private static int SuppressArmorOf(EnemyState enemy)
        {
            if (!enemy.Statuses.Has(StatusKind.Silence)) return enemy.Defense;
            return enemy.IsBoss ? enemy.Defense / 2 : 0;
        }
```

- [ ] **Step 4: 实现「Boss 的主动机制不哑火」**

`IsSilenced` 现在被 8 处 `EnemyAbility` 判断调用。**不要改 `IsSilenced` 的语义**(它是纯状态查询,别的地方可能也用),而是新增一个专管能力的判定,并把那 8 处换过去:

```csharp
        /// <summary>目标身上有封禁状态吗(纯查询,2026-09-05 起语义收窄为"有没有这条状态")。</summary>
        private static bool IsSilenced(EnemyState enemy) => enemy.Statuses.Has(StatusKind.Silence);

        /// <summary>这只敌人的**主动机制**是否被封禁哑火(2026-09-05)。
        ///
        /// Boss 不吃这一半 —— 封禁对 Boss 降级为「只削护甲」,大招照常放。
        /// `EnemyAbility` 的每一处判断都要走这个方法,而不是裸 <see cref="IsSilenced"/>:
        /// 漏一处的表现是那个 Boss 的那一条能力被封住了,而别的没有 —— 静默不一致。</summary>
        private static bool IsAbilitySilenced(EnemyState enemy) => IsSilenced(enemy) && !enemy.IsBoss;
```

用下面这条把 8 处调用点全找出来,逐个换成 `IsAbilitySilenced`:

```bash
grep -n "IsSilenced(" Brushblade/Assets/_Project/Core/BattleEngine.cs
```

⚠ 换完再 grep 一遍确认没有裸 `IsSilenced(` 留在 `EnemyAbility` 分支里 —— 这正是 `CLAUDE.md` 记的那类坑:「同一个字段被两条路径读、只改了其中一条」。

- [ ] **Step 5: 加降级事件**

在 `BattleEventKind` 枚举里加:

```csharp
        /// <summary>封禁打在 Boss 身上、降级为「只削一半护甲」时发一条(2026-09-05)。
        /// 不发的话玩家只会以为是 bug —— 卡面写着「特殊能力全部失效」,
        /// Boss 身上却只掉一半甲、大招照放。</summary>
        SuppressDowngraded,
```

`Silence` case 里,施加状态之后补:

```csharp
                        if (targetIndex >= 0 && _enemies[targetIndex].IsBoss)
                            _events.Add(new BattleEvent(BattleEventKind.SuppressDowngraded, targetIndex, 0));
```

- [ ] **Step 6: 跑全量 + 仿真编译**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd ../balance && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

预期:六条新测试 PASS;既有 `Silence` 测试若断言的是 Boss 的能力被哑火,会变红 —— 那是本任务的正确后果,改期望值并注明。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/SuppressTests.cs Brushblade/Assets/_Project/Tests/SuppressTests.cs.meta
git commit -m "feat(battle): 封禁 —— Silence 语义扩到护甲,对 Boss 降级为减半

杂兵:护甲归零 + 主动机制哑火。Boss:只削一半护甲,大招照放。
同斩杀对 Boss 从「直杀」降为「吃双倍」的纪律。
新增 SuppressDowngraded 事件 —— 不发的话玩家只会以为是 bug。
IsSilenced 收窄为纯查询,能力判断走新的 IsAbilitySilenced(8 处调用点全换)。"
```

---

### Task 10: 魅惑 —— 新 `EffectKind.Charm`

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EffectDef.cs`(新 `EffectKind`)
- Modify: `Brushblade/Assets/_Project/Core/StatusEffect.cs`(新 `StatusKind.Charm`)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(新 case + 敌人行动的目标选择分支)
- Modify: `tools/balance/Program.cs`(`Power()` 记分)
- Create: `Brushblade/Assets/_Project/Tests/CharmTests.cs` + `.meta`

**Interfaces:**
- Consumes: 无
- Produces: `EffectKind.Charm`(`Value` 不用,`Turns` = 持续回合数);`StatusKind.Charm`

> P2 的唯一载体是新字 `花`(木绿):「魅惑敌方,自相残杀,持续 1 回合」。定价 0.40,高于冻结 0.35 —— 冻结只是让敌人少一次出手,魅惑是把那次出手**转成对敌方自己的伤害**,控制与输出两头都占。

- [ ] **Step 1: 写失败测试**

```csharp
using System;
using NUnit.Framework;
using Brushblade.Core;

namespace Brushblade.Core.Tests
{
    /// <summary>魅惑(2026-09-05,平衡重做 P0 任务 10)。被魅惑的敌人这一回合
    /// 攻击**自己阵营**,而不是玩家。
    ///
    /// 与冻结的区别就是定价的依据:冻结只是少一次出手,魅惑把那次出手转成对敌方
    /// 自己的伤害 —— 控制与输出两头都占,故 0.40 > 冻结 0.35。</summary>
    public sealed class CharmTests
    {
        [Test]
        public void Charm_MakesEnemyHitItsOwnSide()
        {
            var battle = CharmBattle(enemyCount: 2, attack: 50);
            battle.Cast("魅", 0);
            int allyHpBefore = battle.Enemies[1].Hp;
            int playerHpBefore = battle.PlayerHp;

            battle.EndTurn();

            Assert.That(battle.Enemies[1].Hp, Is.LessThan(allyHpBefore), "打了自己队友");
            Assert.That(battle.PlayerHp, Is.EqualTo(playerHpBefore), "没打玩家");
        }

        [Test]
        public void Charm_SoleEnemy_DoesNothing()
        {
            var battle = CharmBattle(enemyCount: 1, attack: 50);
            battle.Cast("魅", 0);
            int selfHpBefore = battle.Enemies[0].Hp;
            int playerHpBefore = battle.PlayerHp;

            battle.EndTurn();

            Assert.That(battle.Enemies[0].Hp, Is.EqualTo(selfHpBefore),
                "场上只剩它一只时不自伤 —— 那会让魅惑在单敌局面变成纯伤害");
            Assert.That(battle.PlayerHp, Is.EqualTo(playerHpBefore), "也不许打玩家");
        }

        [Test]
        public void Charm_Expires()
        {
            var battle = CharmBattle(enemyCount: 2, attack: 50);
            battle.Cast("魅", 0);
            battle.EndTurn();

            int playerHpBefore = battle.PlayerHp;
            battle.EndTurn();
            Assert.That(battle.PlayerHp, Is.LessThan(playerHpBefore), "1 回合到期,恢复正常攻击玩家");
        }

        [Test]
        public void Charm_KillingOwnAlly_StillCountsForWin()
        {
            var battle = CharmBattle(enemyCount: 2, attack: 50, allyHp: 10);
            battle.Cast("魅", 0);
            battle.EndTurn();
            Assert.That(battle.Enemies[1].Alive, Is.False, "被魅惑者打死了队友");
            // 死亡必须走同一条 ResolveDefeat 通路 —— 掉落/计数/胜利判定都挂在那里。
            bool sawDeath = false;
            foreach (var e in battle.LastEvents)
                if (e.Kind == BattleEventKind.EnemyDied) sawDeath = true;
            Assert.That(sawDeath, Is.True, "魅惑致死也要发 EnemyDied,否则掉落与计数会静默丢失");
        }
    }
}
```

测试类里加这一个私有方法:

```csharp
        /// <summary>一场带「魅」(魅惑 N 回合)的战斗。enemyCount 只能是 1 或 2 ——
        /// 单敌那条测的正是「场上只剩它一只时空转」。allyHp 给小值可测「魅惑致死队友」。</summary>
        private static BattleEngine CharmBattle(int enemyCount, int attack, int turns = 1,
            int allyHp = 100000)
        {
            var graph = RebalanceFixture.Graph(
                RebalanceFixture.Char("魅", new EffectDef(EffectKind.Charm, 0, turns: turns)));
            var enemies = enemyCount == 1
                ? new[] { RebalanceFixture.Mob(attack: attack) }
                : new[] { RebalanceFixture.Mob(attack: attack),
                          RebalanceFixture.Mob(hp: allyHp, attack: attack) };
            return RebalanceFixture.Battle(graph, new[] { "魅", "魅", "魅" }, enemies);
        }
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "CharmTests"
```

预期:编译错 —— 没有 `EffectKind.Charm`。

- [ ] **Step 3: 加枚举与状态**

`EffectDef.cs`,`EffectKind` 末尾:

```csharp
        Charm,        // 魅惑(2026-09-05,花):目标本回合改为攻击**自己阵营**,持续 Turns 回合。
                      // Value 不用。场上只剩它一只时空转(不自伤、也不打玩家)——
                      // 否则魅惑在单敌局面会退化成一发纯伤害,与它 0.40 的定价不符。
```

`StatusEffect.cs`,`StatusKind` 末尾:

```csharp
        Charm,        // 魅惑:持有者攻击自己阵营(2026-09-05,花)。Magnitude 不用,只看 TurnsLeft。
```

- [ ] **Step 4: 施加与生效**

`BattleEngine.ApplyEffects` 加 case(放在 `Freeze` / `Slow` 附近,控制类聚在一起):

```csharp
                    case EffectKind.Charm:
                        if (targetIndex >= 0)
                            _enemies[targetIndex].Statuses.Apply(new StatusEffect
                            {
                                Kind = StatusKind.Charm, Polarity = StatusPolarity.Debuff,
                                Magnitude = 1,
                                TurnsLeft = MetaRules.ScaleTurnsByCardLevel(
                                    Math.Max(1, effect.Turns), cardLevel),
                                SourceId = def.Id,
                            });
                        break;
```

敌人行动那一支:找到敌人选择攻击目标的地方,加一条前置分支。

```bash
grep -n "ActEnemyTurn" -A 40 Brushblade/Assets/_Project/Core/BattleEngine.cs | head -50
```

在它决定「打玩家还是打召唤物」之前插入:

```csharp
            // 魅惑(2026-09-05,花):这一记改打自己阵营。场上没有别的存活敌人就空转 ——
            // 不自伤(那会让魅惑在单敌局面退化成纯伤害),也不打玩家。
            // 走 DamageEnemy 而不是自造一条伤害通路:掉落、EnemyDied 事件、
            // 分裂(叠字怪)、胜利判定全挂在那条通路上,绕过去会静默丢失。
            if (enemy.Statuses.Has(StatusKind.Charm))
            {
                int victim = FirstOtherAliveEnemy(enemyIndex);
                if (victim >= 0)
                    DamageEnemy(victim, enemy.EffectiveAttack, enemy.Element, crit: false);
                return;   // 这一拍用掉了,不再走正常攻击
            }
```

并补一个小助手(照 `MostWoundedAlly` / `HasOtherAliveEnemy` 的既有命名习惯):

```csharp
        /// <summary>除自己以外第一只存活的敌人;没有则 −1(2026-09-05,魅惑)。</summary>
        private int FirstOtherAliveEnemy(int selfIndex)
        {
            for (int i = 0; i < _enemies.Count; i++)
                if (i != selfIndex && _enemies[i].Alive) return i;
            return -1;
        }
```

- [ ] **Step 5: 给仿真工装的 `Power()` 记分**

`tools/balance/Program.cs` 的 `Power()` switch 里加:

```csharp
                    // 魅惑(2026-09-05,花):按「一次敌人攻击转成对敌伤害」估值。
                    // 系数是多少不重要,**是不是 0 才重要** —— 记 0 分的字机器人永远不会出,
                    // 那与「没把它加进出阵表」完全等价(注释里那条踩过三次的坑)。
                    case EffectKind.Charm: sum += 60; break;
```

- [ ] **Step 6: 跑全量 + 仿真编译**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd ../balance && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ tools/design/tests/ -q
```

预期:四条新测试 PASS,既有全绿(`Charm` 无载体,不影响任何既有字)。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/EffectDef.cs Brushblade/Assets/_Project/Core/StatusEffect.cs Brushblade/Assets/_Project/Core/BattleEngine.cs tools/balance/Program.cs Brushblade/Assets/_Project/Tests/CharmTests.cs Brushblade/Assets/_Project/Tests/CharmTests.cs.meta
git commit -m "feat(battle): 新增魅惑 EffectKind.Charm

被魅惑的敌人这一回合攻击自己阵营。走 DamageEnemy 而不是自造伤害通路 ——
掉落/EnemyDied/分裂/胜利判定全挂在那条通路上。
单敌时空转:不自伤也不打玩家,否则会退化成一发纯伤害。
顺带给 tools/balance 的 Power() 记分 —— 记 0 分等于机器人永远不出这张字。"
```

---

## P0 收尾验收

- [ ] **全量三条命令一次过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd ../prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
cd ../balance && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
cd ../.. && python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ tools/design/tests/ -q
```

`prescompile` 那条**大概率会红** —— 新增的 `EffectKind.Charm` 与 `StatusKind.Charm` 会让 Presentation 里那些穷举 switch 缺分支。这正是 P1 要做的事:**不要在 P0 里顺手修**,记下报错清单交给 P1。只看 `error CS`,`warning MSB3245` 忽略。

- [ ] **跑一轮仿真,记下 P0 后的四系读数**

```bash
cd tools/balance && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet run --nologo -v q
```

此时字表还是旧数值,所以这一轮读的是「**新规则 + 旧数值**」。它的用途是隔离变量:如果 P2 之后读数异常,能判断是规则的问题还是数值的问题。把这张表贴进 spec 的 §8 作为第二组基线。

- [ ] **补一档木土混色画像**(spec §8 的待办)

纯木系卡组里没有战意来源也没有厚来源,所以任务 5 的改动在纯召唤流上几乎看不出来 —— 涨的是混色。在 `tools/balance/Program.cs` 的 `profiles` 里追加:

```csharp
                // 2026-09-05(P0 收尾):召唤物接战意+厚之后,纯木系卡组几乎不涨 ——
                // 因为纯木里既没有金系攻击字(战意来源)也没有土系护盾字(厚来源)。
                // 涨的是混色,而混色此前一个观测点都没有(spec §8 的待办)。
                new Profile("木土混色(柘圭垚㙓,卡5级,10级)", new[] { "柘", "圭", "垚", "㙓" },
                    WoodCards.Concat(EarthCards).ToDictionary(c => c, _ => 5), level: 10,
                    deck: WoodCards.Concat(EarthCards).ToArray()),
```

跑完把读数一并贴进 spec §8。若混色画像的均卒层显著超过单系(比如 > 25),在 spec §9 记一条风险,P2 的数值定标要把它算进去。

- [ ] **提交收尾**

```bash
git add docs/superpowers/specs/2026-09-05-字表平衡重做-design.md tools/balance/Program.cs
git commit -m "docs(balance): P0 收尾 —— 补木土混色画像,记录新规则+旧数值的仿真基线"
```

---

## 交给 P1 的清单

实现者在 P0 结束时要产出这三样,写进 `docs/superpowers/plans/` 下 P1 的计划或一个交接 note:

1. **`prescompile` 的 `error CS` 全清单** —— 新增的 `EffectKind.Charm` / `StatusKind.Charm` / `BattleEventKind.SuppressDowngraded` / `SummonPassive.AuraAttack` 各自打断了哪些 Presentation 穷举点。
2. **P1 必须接的五处**(`CLAUDE.md` 明写、且 2026-09-02 栽过一次):`StatusText.Of` 的 case、`BattleView` 的状态 chip 手写列表、`CharInfo` 的 `EffectsText` switch、`CardTraits.Of` / `CardTraits.Modes`、`gen_char_doc.py` 的特性列。
3. **仿真两组读数**(P0 前 / P0 后),供 P2 判断读数异常是规则问题还是数值问题。
