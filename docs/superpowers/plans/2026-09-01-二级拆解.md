# 二级拆解 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让部件也能拆 —— `蒸 = 艹 + 烝` 之后,`烝` 还能再拆成 `丞 + 灬`。

**Architecture:** 现在 `CharDef.IsLeaf`(没配方)一个字段同时管「能不能拆」和「归字库还是部件池」,14 处调用点混用。新增显式的 `CharDef.IsComponent`(由管线在 `chars.json` 上标 `"component": true`)把两个概念劈开,然后给 12 个部件配上配方。

**Tech Stack:** Unity 6000.5.2f1 / C# 9(Core、Data 纯 C#,禁引 UnityEngine)、Python 3 数据管线、NUnit。

**Spec:** `docs/superpowers/specs/2026-09-01-二级拆解-design.md`

## Global Constraints

- **工作目录**:`/Users/eugenewu/code/game/.claude/worktrees/feat-second-level-decomposition`(git worktree,分支 `worktree-feat-second-level-decomposition`,基线 `origin/main` @ `53b8418`)。所有命令从这里跑,**不要 `cd` 回主检出**。
- **Core 与 Data 禁止引用 UnityEngine**(asmdef 已设 `noEngineReferences: true`)。
- **测试断言只用 Unity 版 NUnit 也支持的 API**:禁用 `Is.AnyOf` / `Is.All.AnyOf`;`Does.Not.Contain(x)` 只在 x 是 string 时能用,集合判包含一律写 `Assert.That(list.Contains(x), Is.True/False, "…")`。
- **测试定位仓库根只能用 `TestContext.CurrentContext.TestDirectory`**,禁用 `AppContext.BaseDirectory`。
- **测试代码禁止直接引用 Newtonsoft**(`JsonConvert` 等),要测序列化就走 `Data.ConfigLoader` / `Data.SaveSerializer` 这些真实入口。
- **`chars.json` 是管线产物**,禁止手改;一律改 `tools/pipeline/export_chars.py` 后重跑。
- 提交信息用 conventional commits(`feat`/`fix`/`docs`/`chore` + 范围)。

**验证命令(全部从 worktree 根跑):**

```bash
# Core/Data 单元测试
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q

# Python 管线
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q

# Presentation 离线编译(改完 Presentation 必跑;借主检出的程序集)
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

只看 `error CS`,`warning MSB3245` 是 Unity 程序集自带的无关引用,忽略。

## 任务顺序的用意:恒等到最后一刻

Task 1–4 **不改变任何现有行为** —— 那时每一条字表条目的 `IsLeaf` 与 `IsComponent` 恰好相等,所以换判据是逐字节恒等的,全量测试绿就是证明。直到 Task 5 给部件配上配方,两个谓词才正式分叉。别打乱这个顺序。

---

### Task 1: 管线给部件条目打 `component` 标注

只加字段,不加配方。做完 `chars.json` 仍是 131 条,内容除新字段外逐字节不变。

**Files:**
- Modify: `tools/pipeline/export_chars.py`(`build_chars`)
- Test: `tools/pipeline/tests/test_export_chars.py`
- Regenerate: `Brushblade/Assets/StreamingAssets/config/chars.json`

**Interfaces:**
- Consumes: 无
- Produces: `chars.json` 里每个部件条目多一个 `"component": true`;可出牌字不带该字段。Task 2 的 `ConfigLoader` 读它。

- [ ] **Step 1: 写失败测试**

追加到 `tools/pipeline/tests/test_export_chars.py` 末尾:

```python
def test_component_entries_are_flagged():
    """部件条目带 component: true;可出牌字不带这个字段。

    Core 侧的 CharDef.IsComponent 靠它区分「部件池成员」和「可出牌字」——
    不能从 recipe/effects 推导,二级拆解之后部件也会有配方。
    """
    chars = build_chars(MINI_IDS, {"烫": {"element": "Fire", "rarity": "Purple", "effects": []}})
    byid = {c["id"]: c for c in chars["chars"]}
    assert byid["汤"]["component"] is True, "配方原料合成出来的部件要标 component"
    assert byid["火"]["component"] is True, "开头硬编码的五行五条也是部件"
    assert "component" not in byid["烫"], "可出牌字不带 component 字段"


def test_real_table_flags_every_component():
    """实船字表:74 个可出牌字都不带 component,其余全部带。"""
    chars = json.loads(CHARS_JSON.read_text(encoding="utf-8"))["chars"]
    playable = {c["id"] for c in chars if "effects" in c}
    assert len(playable) == 74
    for c in chars:
        if c["id"] in playable:
            assert "component" not in c, f"{c['id']} 是可出牌字,不该带 component"
        else:
            assert c.get("component") is True, f"{c['id']} 是部件,该带 component"
```

- [ ] **Step 2: 跑测试确认失败**

```bash
python3 -m pytest tools/pipeline/tests/test_export_chars.py -q -k component
```

Expected: FAIL —— `KeyError: 'component'`。

- [ ] **Step 3: 改 `build_chars`**

在 `tools/pipeline/export_chars.py` 的 `build_chars` 里,把五行五条的构造改成:

```python
    index = build_index(parse_ids_text(ids_text))
    # component: true 是 Core 侧 CharDef.IsComponent 的唯一来源 —— 部件属部件池,
    # 不进奖励池/宝箱池/商城/收集/叠字前置。与「有没有配方」正交(2026-09-01 二级拆解:
    # 部件也可以有配方),所以必须显式标,不能让 Core 去推导。
    entries = [{"id": e, "element": _ELEMENT_NAME[e], "component": True} for e in ELEMENTS]
```

再把文件末尾合成部件条目那段改成:

```python
    for part in sorted(components):
        if part not in values:
            leaf = {"id": _output_id(part)}
            attr = attr_of(part) or COMPOUND_ATTR.get(part)
            if attr:
                leaf["element"] = _ELEMENT_NAME[attr]
            leaf["component"] = True
            entries.append(leaf)
```

- [ ] **Step 4: 重跑管线并核对恒等**

```bash
python3 tools/pipeline/export_chars.py
git diff --stat Brushblade/Assets/StreamingAssets/config/chars.json
git diff Brushblade/Assets/StreamingAssets/config/chars.json | grep '^-' | grep -v '^---' | head
```

Expected: 输出 `131 条(字 74)`;diff 里**只有增行**(每个部件条目多一行 `"component": true`),`grep '^-'` 除了 `---` 一行外没有任何输出。若出现删行,说明动到了别的内容,停下查。

- [ ] **Step 5: 跑测试确认通过**

```bash
python3 -m pytest tools/pipeline/tests/ -q
```

Expected: 全绿。

- [ ] **Step 6: 提交**

```bash
git add tools/pipeline/export_chars.py tools/pipeline/tests/test_export_chars.py Brushblade/Assets/StreamingAssets/config/chars.json
git commit -m "feat(data): 部件条目打 component 标注 —— 与「有没有配方」解耦的部件判据"
```

---

### Task 2: `CharDef.IsComponent` + `ConfigLoader` 读取

Core 侧能读到这个字段,但还没有任何调用点用它。零行为变化。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/CharDef.cs`
- Modify: `Brushblade/Assets/_Project/Data/ConfigLoader.cs`(`CharDto`、`LoadGraph` 的 `new CharDef(...)`)
- Test: `Brushblade/Assets/_Project/Tests/ConfigShapeParsingTests.cs`

**Interfaces:**
- Consumes: Task 1 产出的 `"component": true`
- Produces: `public bool CharDef.IsComponent { get; }`;构造参数 `bool isComponent = false` 排在参数表**最后**(`attackEffects` 之后),现有位置参数调用点一律不受影响。Task 3/4/5 用它。

- [ ] **Step 1: 写失败测试**

追加到 `Brushblade/Assets/_Project/Tests/ConfigShapeParsingTests.cs` 的 `ConfigShapeParsingTests` 类里:

```csharp
        /// <summary>component 字段 → CharDef.IsComponent(2026-09-01 二级拆解)。
        /// 与 IsLeaf 正交:部件可以有配方(烝 = 丞 + 灬),字永远不是部件。</summary>
        [Test]
        public void LoadGraph_ParsesComponentFlag()
        {
            const string json = @"{""chars"":[
                {""id"":""丞"",""component"":true},
                {""id"":""灬"",""element"":""Fire"",""component"":true},
                {""id"":""烝"",""component"":true,""recipe"":[""丞"",""灬""]},
                {""id"":""蒸"",""element"":""Fire"",""rarity"":""Purple"",""recipe"":[""烝""],
                 ""effects"":[{""kind"":""DamageSingle"",""value"":45}]}
            ]}";
            var graph = ConfigLoader.LoadGraph(json);

            Assert.That(graph.Get("丞").IsComponent, Is.True);
            Assert.That(graph.Get("蒸").IsComponent, Is.False, "可出牌字不是部件");

            // 两个谓词正交:烝 既是部件(归部件池)又有配方(可以拆)
            var zheng = graph.Get("烝");
            Assert.That(zheng.IsComponent, Is.True);
            Assert.That(zheng.IsLeaf, Is.False);
        }

        [Test]
        public void LoadGraph_ComponentDefaultsFalseWhenAbsent()
        {
            var graph = ConfigLoader.LoadGraph(@"{ ""chars"": [ { ""id"": ""火"" } ] }");
            Assert.That(graph.Get("火").IsComponent, Is.False);
        }
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "FullyQualifiedName~ConfigShapeParsingTests"
```

Expected: 编译失败 —— `error CS1061: 'CharDef' does not contain a definition for 'IsComponent'`。

- [ ] **Step 3: 加 `CharDef.IsComponent`**

在 `Brushblade/Assets/_Project/Core/CharDef.cs` 里,`IsLeaf` 那行**上面**插入属性声明:

```csharp
        /// <summary>是不是部件(2026-09-01 二级拆解):部件属**部件池**,不进战后奖励池 /
        /// 宝箱池 / 商城 / 收集图鉴 / 叠字前置。
        ///
        /// 与 <see cref="IsLeaf"/> **正交** —— 部件可以有配方(烝 = 丞 + 灬 可以再拆),
        /// 字永远不是部件。此前两件事都靠 IsLeaf 兼任,给部件配上配方会让它被全系统判成字。
        ///
        /// ⚠ 由 chars.json 的 `"component"` **显式标注**(管线在合成部件条目时写),
        /// 不许改成从 Effects / Rarity 推导 —— 同 <see cref="ComponentKin"/> 的理由:
        /// 推导迟早会在某个反例上误判,而这里误判是无声的。</summary>
        public bool IsComponent { get; }

```

在构造函数参数表末尾加 `isComponent`,并在函数体里赋值:

```csharp
        public CharDef(string id, Element? element, IReadOnlyList<string> recipe = null,
            IReadOnlyList<EffectDef> effects = null, CardRarity rarity = CardRarity.White,
            string pinyin = null, string gloss = null, IReadOnlyList<EffectDef> attackEffects = null,
            bool isComponent = false)
        {
            Id = id ?? throw new ArgumentNullException(nameof(id));
            Element = element;
            Recipe = recipe ?? Array.Empty<string>();
            ApCost = ApCostFor(rarity);
            Effects = effects ?? Array.Empty<EffectDef>();
            AttackEffects = attackEffects ?? Array.Empty<EffectDef>();
            Rarity = rarity;
            Pinyin = pinyin;
            Gloss = gloss;
            IsComponent = isComponent;
        }
```

- [ ] **Step 4: 让 `ConfigLoader` 透传**

在 `Brushblade/Assets/_Project/Data/ConfigLoader.cs` 的 `CharDto` 里,`Gloss` 后面加一行:

```csharp
            public bool Component { get; set; } // 部件标记(2026-09-01):→ CharDef.IsComponent
```

再把 `LoadGraph` 里建 `CharDef` 那句(约 462 行)改成:

```csharp
                defs.Add(new CharDef(dto.Id, ParseElement(dto),
                    dto.Recipe, ParseEffects(dto, dto.Effects), ParseRarity(dto),
                    dto.Pinyin, dto.Gloss, ParseEffects(dto, dto.AttackEffects), dto.Component));
```

- [ ] **Step 5: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿(不只是新测试 —— 全量必须一条不红,`isComponent` 是尾部可选参数,现有调用点不该受影响)。

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Core/CharDef.cs Brushblade/Assets/_Project/Data/ConfigLoader.cs Brushblade/Assets/_Project/Tests/ConfigShapeParsingTests.cs
git commit -m "feat(core): CharDef.IsComponent —— 把「是部件」从「没配方」里劈出来"
```

---

### Task 3: 9 处归属判据换成 `IsComponent`

**这一步是纯恒等重构** —— 此刻字表里每一条的 `IsLeaf` 与 `IsComponent` 都相等,所以行为一字不变。全量测试绿就是证明,不需要新测试。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/ForgeEngine.cs:97`
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs:757`
- Modify: `Brushblade/Assets/_Project/Core/RunEngine.cs:625`
- Modify: `Brushblade/Assets/_Project/Core/Meta.cs:85`、`:486`(注释)、`:500`
- Modify: `Brushblade/Assets/_Project/Presentation/GameRoot.cs:115`、`:133`、`:144`
- Modify: `Brushblade/Assets/_Project/Presentation/MapView.cs:100`

**Interfaces:**
- Consumes: `CharDef.IsComponent`(Task 2)
- Produces: 无新接口。Task 5 加配方之后,这 9 处才开始与 `IsLeaf` 分道扬镳。

- [ ] **Step 1: 逐处替换(共 9 处 + 1 处注释)**

`Core/ForgeEngine.cs:97` —— 拆解产物归位:

```csharp
                (graph.TryGet(ingredient, out var idef) && !idef.IsComponent ? toLibrary : toPool).Add(ingredient);
```

`Core/BattleEngine.cs:757` —— 部件从池里直出:

```csharp
            bool fromPool = !fromLibrary && def.IsComponent && _forge.Pool.Contains(charId);
```

`Core/RunEngine.cs:625` —— 战后奖励池:

```csharp
                if (!_graph.TryGet(id, out var def) || def.IsComponent)
```

`Core/Meta.cs:85` —— `DeckComponents`(喂登塔起手部件池):

```csharp
                    if (graph.TryGet(part, out var partDef) && partDef.IsComponent && !seen.Contains(part))
```

`Core/Meta.cs:486` —— 注释里的 `IsLeaf` 改成 `IsComponent`:

```csharp
        /// 部件(IsComponent)不参与:它们靠掉落获得,不存在"解锁"一说。
```

`Core/Meta.cs:500` —— `PrerequisitesMet`:

```csharp
                if (!graph.TryGet(ingredient, out var idef) || idef.IsComponent) continue;
```

`Presentation/GameRoot.cs:115` —— 旧货架摆着部件就作废重摆:

```csharp
            if (_meta.Shop.CardSlots.Exists(id => _graph.TryGet(id, out var def) && def.IsComponent))
```

`Presentation/GameRoot.cs:133` —— 商城卡池:

```csharp
                if (_graph.TryGet(card, out var def) && !def.IsComponent && !pool.Contains(card))
```

`Presentation/GameRoot.cs:144` —— 宝箱卡池:

```csharp
                if (!def.IsComponent)
```

`Presentation/MapView.cs:100` —— 宝箱卡池(与 GameRoot 同逻辑):

```csharp
                if (!def.IsComponent)
```

- [ ] **Step 2: 核对没有漏改、也没有多改**

```bash
grep -rn "IsLeaf" --include="*.cs" Brushblade/Assets/_Project | grep -v Tests/
```

Expected: **恰好 6 行**,全是「能不能拆」语义 ——

1. `Core/CharDef.cs:51`(定义本身)
2. `Core/ForgeEngine.cs:89`(TryDismantle 门禁)
3. `Core/ForgeEngine.cs:177`(Suggest)
4. `Presentation/CharInfo.cs:26`(卡面印配方)
5. `Presentation/BattleView.cs:2383`(拆字预览行「炎 → 火 + 火」)
6. `Presentation/BattleView.cs:2442`(拆按钮 —— Task 4 才动它,这一步保持原样)

若不是 6 行,停下核对:多了说明有归属判据没换,少了说明换错了可拆判据。

- [ ] **Step 3: 跑 Core 测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿,**且通过条数与 Task 2 结束时完全相同**。这是恒等性的证明 —— 有任何一条变红,说明两个谓词在某条数据上已经不等了,停下查。

- [ ] **Step 4: 跑 Presentation 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

Expected: 无 `error CS`。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core Brushblade/Assets/_Project/Presentation
git commit -m "refactor(core): 库/池归属判据从 IsLeaf 换成 IsComponent(9 处,行为恒等)"
```

---

### Task 4: 拆池中部件

放开 `TryDismantle` 的来源限制,并让战斗界面给池中部件也画出「拆」按钮。此刻还没有带配方的部件,所以对实船数据仍是零行为变化;用假图谱测新能力。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/ForgeEngine.cs`(`TryDismantle`)
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs:2435,2442`
- Test: `Brushblade/Assets/_Project/Tests/ForgeEngineTests.cs`

**Interfaces:**
- Consumes: `CharDef.IsComponent`(Task 2)
- Produces: `ForgeEngine.TryDismantle(charId, graph, state, poolCapacity, libraryCapacity)` 签名不变,但 `charId` 现在可以在 `state.Pool` 里。产物按**性质**归位(`IsComponent` → 池,否则 → 库),与来源无关。

- [ ] **Step 1: 写失败测试**

先在 `ForgeEngineTests.cs` 的 `Graph()` 里加两条带配方的部件(加在 `new CharDef("利", ...)` 那行**后面**,注意 `丁` 已经存在,是中性部件):

```csharp
            // 带配方的部件(2026-09-01 二级拆解):烝 = 丞 + 灬 —— 既归部件池,又能再拆
            new CharDef("丞", null, isComponent: true),
            new CharDef("灬", Element.Fire, isComponent: true),
            new CharDef("烝", null, new[] { "丞", "灬" }, isComponent: true),
            new CharDef("蒸", Element.Fire, new[] { "艹", "烝" }),
            new CharDef("艹", Element.Wood, isComponent: true),
```

再在文件的「拆(Dismantle)」一节末尾追加测试:

```csharp
        // ---- 拆池中部件(2026-09-01 二级拆解)----

        [Test]
        public void Dismantle_ComponentInPool_ProductsStayInPool()
        {
            // 烝 = 丞 + 灬,两个都是部件 → 全进池;源 烝 从池里移除
            var result = ForgeEngine.TryDismantle("烝", Graph(),
                State(Array.Empty<string>(), new[] { "烝" }), 6, 12);
            Assert.That(result.Success, Is.True);
            Assert.That(result.State.Pool, Is.EquivalentTo(new[] { "丞", "灬" }));
            Assert.That(result.State.Library, Is.Empty);
        }

        [Test]
        public void Dismantle_ComponentInPool_CostsOnePoolSlotNet()
        {
            // 池容量 3,已有 烝 + 2 个占位 → 拆完是 丞 + 灬 + 2 = 4 > 3,应当整体失败
            var before = State(Array.Empty<string>(), new[] { "烝", "木", "火" });
            var result = ForgeEngine.TryDismantle("烝", Graph(), before, 3, 12);
            Assert.That(result.Success, Is.False);
            Assert.That(result.Error, Is.EqualTo(ForgeError.PoolWouldOverflow));
            Assert.That(result.State.Pool, Is.EquivalentTo(new[] { "烝", "木", "火" }), "失败不动状态");

            // 容量放到 4 就过得去(净 +1)
            Assert.That(ForgeEngine.TryDismantle("烝", Graph(), before, 4, 12).Success, Is.True);
        }

        [Test]
        public void Dismantle_ComponentNotInPoolOrLibrary_Fails()
        {
            var result = ForgeEngine.TryDismantle("烝", Graph(),
                State(Array.Empty<string>(), new[] { "木" }), 6, 12);
            Assert.That(result.Success, Is.False);
            Assert.That(result.Error, Is.EqualTo(ForgeError.NotInLibrary));
        }

        [Test]
        public void Dismantle_ComponentWithoutRecipe_StillNotDismantlable()
        {
            var result = ForgeEngine.TryDismantle("丞", Graph(),
                State(Array.Empty<string>(), new[] { "丞" }), 6, 12);
            Assert.That(result.Success, Is.False);
            Assert.That(result.Error, Is.EqualTo(ForgeError.NotDismantlable));
        }

        [Test]
        public void Dismantle_CharWithComponentIngredient_ComponentGoesToPool()
        {
            // 蒸 = 艹 + 烝:两个都是部件(烝 有配方但仍是部件)→ 都进池,不进字库
            var result = ForgeEngine.TryDismantle("蒸", Graph(),
                State(new[] { "蒸" }, Array.Empty<string>()), 6, 12);
            Assert.That(result.Success, Is.True);
            Assert.That(result.State.Pool, Is.EquivalentTo(new[] { "艹", "烝" }));
            Assert.That(result.State.Library, Is.Empty);
        }
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "FullyQualifiedName~ForgeEngineTests"
```

Expected: `Dismantle_ComponentInPool_*` 三条红(`NotInLibrary`),其余绿。

- [ ] **Step 3: 改 `TryDismantle`**

把 `Brushblade/Assets/_Project/Core/ForgeEngine.cs` 的 `TryDismantle` 整个方法体换成:

```csharp
        public static ForgeResult TryDismantle(string charId, RecipeGraph graph, ForgeState state,
            int poolCapacity, int libraryCapacity)
        {
            if (!graph.TryGet(charId, out var def))
                return ForgeResult.Fail(ForgeError.UnknownChar, state);
            if (def.IsLeaf)
                return ForgeResult.Fail(ForgeError.NotDismantlable, state);

            // 来源:字库里的字,或**部件池里带配方的部件**(2026-09-01 二级拆解:烝 = 丞 + 灬)。
            // 两者都不在就是 NotInLibrary —— 错误名沿用旧的,含义扩成「手上没有这个东西」。
            bool fromLibrary = state.Library.Contains(charId);
            if (!fromLibrary && !state.Pool.Contains(charId))
                return ForgeResult.Fail(ForgeError.NotInLibrary, state);

            // 归位按原料**自身的性质**,与拆的来源无关:部件回池、可出牌字回字库
            // (2026-07-22 拍板,如 森 = 林 + 木,林 回库、木 回池)。
            var toPool = new List<string>();
            var toLibrary = new List<string>();
            foreach (var ingredient in def.Recipe)
                (graph.TryGet(ingredient, out var idef) && !idef.IsComponent ? toLibrary : toPool).Add(ingredient);

            // 容量先验后扣,任一去处放不下则整体失败、不动状态。
            // 源先移除腾出 1 位:拆字库的字腾字库位,拆池里的部件腾池位。
            int poolAfter = state.Pool.Count + toPool.Count - (fromLibrary ? 0 : 1);
            if (poolAfter > poolCapacity)
                return ForgeResult.Fail(ForgeError.PoolWouldOverflow, state);
            int libraryAfter = state.Library.Count + toLibrary.Count - (fromLibrary ? 1 : 0);
            if (libraryAfter > libraryCapacity)
                return ForgeResult.Fail(ForgeError.LibraryFull, state);

            var library = new List<string>(state.Library);
            var pool = new List<string>(state.Pool);
            if (fromLibrary) library.Remove(charId);
            else pool.Remove(charId);
            library.AddRange(toLibrary);
            pool.AddRange(toPool);
            return ForgeResult.Ok(new ForgeState(library, pool));
        }
```

同时把方法上面那段 XML 注释的第二句改成:

```csharp
        /// <summary>拆:配方原料按性质归位——部件(IsComponent)回部件池,可出牌字回字库
        /// (2026-07-22 拍板:如森=林+木,林回库、木回池;原「全进池」废止)。
        /// 源可以是字库里的字,也可以是部件池里带配方的部件(2026-09-01 二级拆解)。
        /// 任一去处放不下则整体失败,不动状态(先验后扣)。</summary>
```

并删掉它上面那行重复的旧 `<summary>拆:字库中的字 → 配方全部原料入池(无损返还)。</summary>`(那是一句遗留的重复摘要,与本次改动同一处、且已被下面那段取代)。

- [ ] **Step 4: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿。旧的拆字测试(`Dismantle_CharsToLibrary_LeavesToPool` 等)必须一条不红 —— 库来源那一支的容量算式与改前逐项等价。

- [ ] **Step 5: 放开战斗界面的拆按钮**

`Brushblade/Assets/_Project/Presentation/BattleView.cs`:删掉 2435 行的 `inLibrary`(改完只剩这一处用,会成孤儿变量),并把 2442 行的条件改成只看能不能拆:

```csharp
            // 动作按钮 ≥50 高(2026-07-19 iOS 反馈:手指可点性)
            Ui.RoundButton(_actionRow, Strings.T("battle.btn.cast"), () => OnCastPressed(def), Theme.Cinnabar, Color.white, 17, new Vector2(76, 52));
            // 2026-09-01 二级拆解:去掉「必须在字库里」的前提 —— 部件池里带配方的部件
            // (烝 = 丞 + 灬)同样该给拆按钮,ForgeEngine.TryDismantle 认两种来源。
            if (!def.IsLeaf)
                Ui.RoundButton(_actionRow, Strings.T("battle.btn.dismantle"), () => OnDismantle(def.Id), Theme.SplitBlue, Color.white, 17, new Vector2(76, 52));
```

- [ ] **Step 6: 跑 Presentation 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

Expected: 无 `error CS`(特别确认没有 `CS0219 变量已赋值但从未使用` 之类关于 `inLibrary` 的残留)。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/ForgeEngine.cs Brushblade/Assets/_Project/Presentation/BattleView.cs Brushblade/Assets/_Project/Tests/ForgeEngineTests.cs
git commit -m "feat(forge): 部件池里带配方的部件也能拆,产物按性质归位"
```

---

### Task 5: 12 条部件配方 + `荆`/`湮` 改一级配方

到这一步语义才正式分叉。做完 `chars.json` 从 131 条变 **143 条**(实施时更正:计划原写 141,漏算了 `茾`/`垔` 这两个本身也是新条目的中间层部件)。

**Files:**
- Modify: `tools/pipeline/export_chars.py`(新增 `COMPONENT_RECIPES`、改 `MANUAL_RECIPES`、`build_chars` 的部件收集改闭包)
- Test: `tools/pipeline/tests/test_export_chars.py`
- Test: `Brushblade/Assets/_Project/Tests/CharTableTests.cs`
- Test: `Brushblade/Assets/_Project/Tests/ComponentKinTests.cs`
- Regenerate: `Brushblade/Assets/StreamingAssets/config/chars.json`

**Interfaces:**
- Consumes: Task 1–4 的全部
- Produces: `chars.json` 143 条(74 字 + 69 部件);12 个部件带 `recipe`;新增 10 个终点部件 `己 合 歹 品 丞 贝 几 七 开 覀`。

- [ ] **Step 1: 写失败的管线测试**

追加到 `tools/pipeline/tests/test_export_chars.py`:

```python
def test_component_recipes_yield_one_element_part_each():
    """选中的 12 条部件配方,每条都恰好产出 1 个五行部件。

    这是范围的判据(spec §六):拆的价值 = 换五行部件,中性部件是残渣。
    文档「二级组成」的筛选条件「比一级多给出五行信息」与这个价值模型是同一件事。
    """
    from export_chars import COMPONENT_RECIPES, COMPOUND_ATTR
    from filter_chars import attr_of
    assert len(COMPONENT_RECIPES) == 12
    for part, recipe in COMPONENT_RECIPES.items():
        hits = [p for p in recipe if attr_of(p) or p in COMPOUND_ATTR]
        assert len(hits) == 1, f"{part} = {' + '.join(recipe)} 产出 {hits},应当恰好 1 个五行部件"


def test_real_table_has_component_recipes():
    """实船字表:12 个部件带上了配方,10 个新部件条目在场。"""
    chars = json.loads(CHARS_JSON.read_text(encoding="utf-8"))["chars"]
    byid = {c["id"]: c for c in chars}
    expected = {
        "秋": ["禾", "火"], "崔": ["山", "隹"], "岂": ["山", "己"], "荅": ["艹", "合"],
        "列": ["歹", "刂"], "喿": ["品", "木"], "烝": ["丞", "灬"], "则": ["贝", "刂"],
        "朵": ["几", "木"], "切": ["七", "刀"], "茾": ["艹", "开"], "垔": ["覀", "土"],
    }
    for part, recipe in expected.items():
        assert byid[part]["recipe"] == recipe, f"{part} 的配方不对"
        assert byid[part]["component"] is True, f"{part} 有了配方,但仍然必须是部件"
    for part in "己合歹品丞贝几七开覀":
        assert part in byid, f"新部件 {part} 不在字表里"
        assert "recipe" not in byid[part], f"{part} 是终点,不该有配方"


def test_jing_and_yan_recipes_route_through_the_middle_layer():
    """荆 / 湮 的一级配方引回中间层(2026-09-01 用户复核后拍板)。"""
    chars = json.loads(CHARS_JSON.read_text(encoding="utf-8"))["chars"]
    byid = {c["id"]: c for c in chars}
    assert byid["荆"]["recipe"] == ["茾", "刂"]
    assert byid["湮"]["recipe"] == ["氵", "垔"]


def test_real_table_entry_count():
    chars = json.loads(CHARS_JSON.read_text(encoding="utf-8"))["chars"]
    playable = [c for c in chars if "effects" in c]
    assert len(playable) == 74
    assert len(chars) == 143, "74 字 + 69 部件(含 茾/垔 两个新的中间层部件)"
```

- [ ] **Step 2: 跑测试确认失败**

```bash
python3 -m pytest tools/pipeline/tests/test_export_chars.py -q
```

Expected: FAIL —— `ImportError: cannot import name 'COMPONENT_RECIPES'`。

- [ ] **Step 3: 加 `COMPONENT_RECIPES` 与 `MANUAL_RECIPES` 改动**

在 `tools/pipeline/export_chars.py` 的 `MANUAL_RECIPES` 定义**之后**插入:

```python
# 部件的配方(2026-09-01「二级拆解」):让部件自己也能再拆一层。
#
# 取值范围 = docs/design/字选型/字表功能解析.md「二级组成」列蕴含的那一批,不做 IDS 全量
# 展开 —— 全展开会引入 𠅃 / 𭕄 / 㳟 / 龴 这类生僻与增补平面部件(40 个),正是下面
# MANUAL_RECIPES 当初刻意绕开的东西。
#
# 判据(spec §六):拆是**单向变现**,价值 = 换到五行部件(五行部件能去合金/橙/红那 25 个
# 纯五行字),中性部件是残渣。选中的这 12 条**每条都恰好产出 1 个五行部件**;没选的 17 个
# 部件拆出来 0/17 产五行部件。守卫测试:test_component_recipes_yield_one_element_part_each。
#
# 注释里的字是这条配方服务的可出牌字。
COMPONENT_RECIPES = {
    "秋": ["禾", "火"],   # 楸
    "崔": ["山", "隹"],   # 熣
    "岂": ["山", "己"],   # 桤、铠
    "荅": ["艹", "合"],   # 塔
    "列": ["歹", "刂"],   # 烈
    "喿": ["品", "木"],   # 燥、澡
    "烝": ["丞", "灬"],   # 蒸
    "则": ["贝", "刂"],   # 铡
    "朵": ["几", "木"],   # 剁
    "切": ["七", "刀"],   # 沏
    # 荆 / 湮 的一级配方引回中间层后新出现的两个(见 MANUAL_RECIPES 里那两条的注释)
    "茾": ["艹", "开"],   # 荆
    "垔": ["覀", "土"],   # 湮
}

# 己 合 歹 品 丞 贝 几 七 开 覀 是**终点**,不再往下拆(2026-09-01 拍板:只做两级)。
# 品 = 口+口+口、合 = 亼+口 再往下就是笔画,没有拆合语义 —— 与「五行部件不拆」
# 「禾 不拆」同一条规则。
```

再改 `MANUAL_RECIPES` 里 `荆` 和 `湮` 两条:

```python
    # 荆:IDS 是 ⿰茾刂。2026-08 曾改成 艹+刂 绕开生僻的 茾,2026-09-01 二级拆解**推翻**
    # 这个绕开 —— 照 艹+刂 展开会原地打转(两个都是五行部件,拆不动),文档里的二级
    # 艹+开+刂 在游戏里永远不可达。引回 茾(茾 = 艹+开,见 COMPONENT_RECIPES)。
    # 已知代价:拆一次的产出从 2 个五行部件降到 1 个(spec §六,用户复核后仍拍板引回)。
    "荆": ["茾", "刂"],
    ...
    # 湮:IDS 是 ⿰氵垔。同 荆 —— 2026-08 改成 氵+土 绕开生僻的 垔,2026-09-01 引回,
    # 让文档的二级 氵+覀+土 可达(垔 = 覀+土,见 COMPONENT_RECIPES)。
    "湮": ["氵", "垔"],
```

(`塞` / `锁` / `戮` / `锋` 四条保持原样不动。)

- [ ] **Step 4: 让 `build_chars` 的部件收集变成闭包**

`build_chars` 里合成部件条目那段改成 worklist 循环 —— 部件现在也可能带配方,它的原料要一并收进来:

```python
    # 部件条目:先做闭包 —— 部件自己也可能有配方(COMPONENT_RECIPES),它的原料同样要落地。
    # 写成 worklist 而不是「再来一轮」,是为了将来加第三级时不会静默漏掉一层。
    # 收敛之后再按字典序输出,保证 chars.json 的部件段顺序稳定(否则 diff 会无谓翻腾)。
    emitted = set()
    pending = sorted(components)
    while pending:
        part = pending.pop()
        if part in values or part in emitted:
            continue
        emitted.add(part)
        pending.extend(p for p in COMPONENT_RECIPES.get(part, []) if p not in ELEMENTS)

    for part in sorted(emitted):
        leaf = {"id": _output_id(part)}
        attr = attr_of(part) or COMPOUND_ATTR.get(part)
        if attr:
            leaf["element"] = _ELEMENT_NAME[attr]
        leaf["component"] = True
        sub = COMPONENT_RECIPES.get(part)
        if sub:
            leaf["recipe"] = [_output_id(p) for p in sub]
        entries.append(leaf)

    return {"chars": entries}
```

把它替换掉原来的:

```python
    for part in sorted(components):
        if part not in values:
            leaf = {"id": _output_id(part)}
            attr = attr_of(part) or COMPOUND_ATTR.get(part)
            if attr:
                leaf["element"] = _ELEMENT_NAME[attr]
            leaf["component"] = True
            entries.append(leaf)

    return {"chars": entries}
```

⚠ 原来那段是 `for part in sorted(components): if part not in values:` —— 新写法把「过滤 + 闭包」和「输出」拆成两个循环,`values` 的过滤挪进了闭包那一轮,别漏掉。

- [ ] **Step 5: 重跑管线**

```bash
python3 tools/pipeline/export_chars.py
python3 -m pytest tools/pipeline/tests/ -q
```

Expected: 输出 `143 条(字 74)`;pytest 全绿。

- [ ] **Step 6: 加真实字表的 Core 守卫测试**

追加到 `Brushblade/Assets/_Project/Tests/CharTableTests.cs` 的类里:

```csharp
        /// <summary>二级拆解(2026-09-01):12 个部件有了配方,但仍然是部件。
        /// 两个谓词正交是这次改动的支点 —— 谁把 IsComponent 又推导回 IsLeaf,这条就红。</summary>
        [Test]
        public void RealConfig_ComponentsWithRecipes_AreStillComponents()
        {
            var graph = RealGraph();
            var expected = new (string Part, string[] Recipe)[]
            {
                ("秋", new[] { "禾", "火" }), ("崔", new[] { "山", "隹" }),
                ("岂", new[] { "山", "己" }), ("荅", new[] { "艹", "合" }),
                ("列", new[] { "歹", "刂" }), ("喿", new[] { "品", "木" }),
                ("烝", new[] { "丞", "灬" }), ("则", new[] { "贝", "刂" }),
                ("朵", new[] { "几", "木" }), ("切", new[] { "七", "刀" }),
                ("茾", new[] { "艹", "开" }), ("垔", new[] { "覀", "土" }),
            };
            foreach (var (part, recipe) in expected)
            {
                var def = graph.Get(part);
                Assert.That(def.Recipe, Is.EqualTo(recipe), $"{part} 的配方不对");
                Assert.That(def.IsComponent, Is.True, $"{part} 有了配方,但它仍然是部件");
                Assert.That(def.IsLeaf, Is.False, $"{part} 该能拆");
            }
        }

        /// <summary>新部件是终点,只做两级(2026-09-01 拍板)。</summary>
        [Test]
        public void RealConfig_NewComponentsAreTerminal()
        {
            var graph = RealGraph();
            foreach (var part in new[] { "己", "合", "歹", "品", "丞", "贝", "几", "七", "开", "覀" })
            {
                Assert.That(graph.TryGet(part, out var def), Is.True, $"{part} 不在字表里");
                Assert.That(def.IsComponent, Is.True, $"{part} 该是部件");
                Assert.That(def.IsLeaf, Is.True, $"{part} 是终点,不该有配方");
            }
        }

        /// <summary>荆 / 湮 的一级配方引回中间层(2026-09-01 用户复核后拍板,spec §六)。
        /// 代价是这两个字拆一次的产出从 2 个五行部件降到 1 个,已明确接受。</summary>
        [Test]
        public void RealConfig_JingAndYanRouteThroughTheMiddleLayer()
        {
            var graph = RealGraph();
            Assert.That(graph.Get("荆").Recipe, Is.EqualTo(new[] { "茾", "刂" }));
            Assert.That(graph.Get("湮").Recipe, Is.EqualTo(new[] { "氵", "垔" }));
        }

        /// <summary>74 个可出牌字一个都不是部件;部件一个都不是可出牌字。
        /// 库/池归属的 9 处判据全压在这条上。</summary>
        [Test]
        public void RealConfig_PlayableCharsAndComponentsDoNotOverlap()
        {
            var graph = RealGraph();
            int playable = 0, components = 0;
            foreach (var def in graph.All)
            {
                if (def.Effects.Count > 0)
                {
                    playable++;
                    Assert.That(def.IsComponent, Is.False, $"{def.Id} 有效果,不该是部件");
                }
                else
                {
                    components++;
                    Assert.That(def.IsComponent, Is.True, $"{def.Id} 没效果,该是部件");
                }
            }
            Assert.That(playable, Is.EqualTo(74));
            Assert.That(components, Is.EqualTo(67));
        }

        /// <summary>相生倍率全表恒等(spec §五):RecipeGraph.RecipeElements 只读直接原料的
        /// element,而荆 / 湮 换掉的原料两边都不含各自的「母」属性,倍率仍是 1。</summary>
        [Test]
        public void RealConfig_JingAndYanKeepMultiplierOne()
        {
            var graph = RealGraph();
            // 荆 是木系,木的母是水 —— 配方里不含 Water
            Assert.That(graph.RecipeElements("荆").Contains(Element.Water), Is.False);
            // 湮 是水系,水的母是金 —— 配方里不含 Metal
            Assert.That(graph.RecipeElements("湮").Contains(Element.Metal), Is.False);
        }

        /// <summary>叠字前置不因部件有了配方而收紧(spec §一列出的三个回归之一)。
        /// 把 IsComponent 换回 IsLeaf,合 蒸 会开始要求玩家「拥有 烝」这张收集卡,
        /// 而 烝 根本不在收集图鉴里 —— 蒸 变得永远合不出来,且无声。</summary>
        [Test]
        public void RealConfig_ComponentWithRecipe_IsNotAPrerequisite()
        {
            var graph = RealGraph();
            var ownNothing = new List<string>();
            // 蒸 = 艹 + 烝,烝 现在有配方了,但它是部件,不该成为前置
            Assert.That(MetaRules.PrerequisitesMet("蒸", graph, ownNothing), Is.True);
            Assert.That(MetaRules.PrerequisitesMet("荆", graph, ownNothing), Is.True, "荆 = 茾 + 刂,茾 是部件");
            // 对照:森 = 木 + 林,林 是可出牌字 → 仍然是前置
            Assert.That(MetaRules.PrerequisitesMet("森", graph, ownNothing), Is.False);
            Assert.That(MetaRules.PrerequisitesMet("森", graph, new List<string> { "林" }), Is.True);
        }

        /// <summary>登塔起手部件池仍然收得到带配方的部件(spec §一列出的三个回归之一)。
        /// 把 IsComponent 换回 IsLeaf,烝 不再算可掉落部件,蒸 的原料就掉不出来了。</summary>
        [Test]
        public void RealConfig_ComponentWithRecipe_StillCountsAsDeckComponent()
        {
            var graph = RealGraph();
            var parts = new List<string>(MetaRules.DeckComponents(new List<string> { "蒸" }, graph));
            Assert.That(parts.Contains("烝"), Is.True, "烝 有了配方,但它仍是 蒸 的可掉落部件");
            Assert.That(parts.Contains("艹"), Is.True);
        }
```

⚠ `MetaRules` 的真实类名以 `Core/Meta.cs` 里 `DeckComponents` / `PrerequisitesMet` 所在的静态类为准(`Core/RunEngine.cs:51` 用的是 `MetaRules.DeckComponents`)。若类名不符,按文件里的实际名字改,别新建包装。`CharTableTests.cs` 顶部若还没 `using System.Collections.Generic;`,补上。

- [ ] **Step 7: 加强 `ComponentKin` 守卫**

`Brushblade/Assets/_Project/Tests/ComponentKinTests.cs` 的 `RealConfig_AllMembersAreLeavesInTheRealCharTable` 里,在 `IsLeaf` 那条断言**后面**补一条:

```csharp
                Assert.That(def.IsLeaf, Is.True, $"{part} 已经有配方了,五系等价与宝箱前置的互不干扰假设被打破");
                Assert.That(def.IsComponent, Is.True, $"{part} 必须是部件 —— 五系等价只在部件之间成立");
```

- [ ] **Step 8: 跑全量 Core 测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿。若 `PierceBuffCharTests`(钅 / 兑 / 锐)或 `ConfigLoaderTests`(火)相关的红了,说明范围溢出到了不该动的部件上,停下查。

- [ ] **Step 9: 提交**

```bash
git add tools/pipeline/export_chars.py tools/pipeline/tests/test_export_chars.py Brushblade/Assets/StreamingAssets/config/chars.json Brushblade/Assets/_Project/Tests/CharTableTests.cs Brushblade/Assets/_Project/Tests/ComponentKinTests.cs
git commit -m "feat(data): 12 条部件配方落地,荆/湮 一级配方引回 茾/垔"
```

---

### Task 6: 字体子集、字表文档、全量回归

**Files:**
- Regenerate: `Brushblade/Assets/.../NotoSansSC-Subset.ttf`、`NotoSerifSC-Subset.ttf`
- Regenerate: `docs/design/字选型/字表功能解析.md`

**Interfaces:**
- Consumes: Task 5 的 `chars.json`
- Produces: 无

- [ ] **Step 1: 重跑字体子集**

新字形会出现在卡面(配方行「烝 → 丞 + 灬」)。**这一步漏掉的话离线编译和 Core 单测都发现不了,只有真机上显示成豆腐块。**

```bash
python3 tools/fonts/subset_fonts.py
```

- [ ] **Step 2: 比 cmap 确认 7 个新码位真的进去了**

⚠ 光看 diff 大小说明不了问题(重新生成必然产生时间戳 churn)。必须比 cmap:

```bash
python3 - <<'PY'
from fontTools.ttLib import TTFont
import glob
for f in sorted(glob.glob('Brushblade/Assets/**/*Subset*.ttf', recursive=True)):
    cm = set(TTFont(f).getBestCmap())
    miss = ''.join(c for c in '己合歹品丞贝几七开覀茾垔' if ord(c) not in cm)
    print(f.split('/')[-1], '缺:', miss or '无')
PY
```

Expected: 两个字体都输出「缺: 无」。(`己 合 品 几 开` 本来就在子集里;新进的是 `歹 丞 贝 七 茾 垔 覀`。)

- [ ] **Step 3: 跑字体测试**

```bash
python3 -m pytest tools/fonts/tests/ -q
```

Expected: 全绿。

- [ ] **Step 4: 重跑字表文档**

```bash
python3 tools/design/gen_char_doc.py
git diff --stat docs/design/字选型/字表功能解析.md
git diff docs/design/字选型/字表功能解析.md | grep '^[-+]|' | head -20
```

Expected:`荆` 与 `湮` 的**一级组成**列从 `艹 + 刂` / `氵 + 土` 变成 `茾 + 刂` / `氵 + 垔`;**二级组成列全表不变**(`_expand` 优先取字表配方,新配方与它原先走的 IDS 拆解一致)。若二级列出现变化,记下来但不要自行改判据 —— 留到最后统一报告。

- [ ] **Step 5: 全量回归**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

Expected: 三样全绿。

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets docs/design/字选型/字表功能解析.md
git commit -m "chore(assets): 重跑字体子集(+7 字形)与字表功能解析文档"
```

---

## 收尾

跑一遍 `git log --oneline origin/main..HEAD` 确认 6 个提交都在,把分支名 `worktree-feat-second-level-decomposition` 交回给用户。**不要自己动 main。**

Unity EditMode 集成测试要用户在编辑器里跑(编辑器开着时命令行会因项目锁失败):

```bash
/Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/MacOS/Unity \
  -batchmode -projectPath Brushblade -runTests -testPlatform EditMode \
  -testResults /tmp/results.xml -logFile /tmp/unity_test.log
```
