# 掉落改造 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把三处掉落从「部件」改为「出战牌组里的字」,并为满库掉落加一个强制决议阶段。

**Architecture:** `BattlePhase` 新增 `DropChoice`。`StartTurn()` 从 `BattleConfig.UnlockedChars`
抽一个字入库;满库则挂起待决议字并切阶段,由 `ResolveDrop`/`SkipDrop` 收口。
战利品与复活改数量常量并删除部件选取路径。

**Tech Stack:** C#(Core/Data 禁引 UnityEngine)、NUnit、Unity 6000.5.2f1。

## Global Constraints

- Core 与 Data **禁止引用 UnityEngine**;随机性一律走 `GameRandom`,禁用 `UnityEngine.Random`。
- 测试断言只用 Unity 版 NUnit 支持的 API:**禁用 `Is.AnyOf`/`Is.All.AnyOf`**,多选一用 `Is.EqualTo(a).Or.EqualTo(b)`。
- 测试代码**禁止引用 Newtonsoft**;只能用 Tests asmdef 放行的程序集(Core / Data)。
- 每个任务提交前跑:`cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
- 改动 Presentation 的任务**必须**加跑:`cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q`(只看 `error CS`,`warning MSB3245` 忽略)
- 当前测试基线:**542 全绿**。
- 提交信息用 conventional commits,中文正文。
- **只提交你自己改动的文件**,工作区可能有用户未完成的改动(`Brushblade/Assets/Settings/` 未跟踪,不要碰)。

---

### Task 1: DropChoice 阶段与回合掉字

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`BattlePhase` 枚举 7-12 行、`StartTurn()` 627-641 行)
- Modify: `Brushblade/Assets/_Project/Core/RunSnapshot.cs`(`BattleSnapshot` 11-27 行)
- Test: `Brushblade/Assets/_Project/Tests/BattleEngineTests.cs`

**Interfaces:**
- Consumes: `BattleConfig.UnlockedChars`(`IReadOnlyCollection<string>`,可为 null)、
  `BattleConfig.LibraryCapacity`、`BattleConfig.DropsPerTurn`、`GameRandom.Next(int)`
- Produces: `BattlePhase.DropChoice`;`BattleEngine.PendingDrop`(`string`,无待决议时 null);
  `BattleEngine.ResolveDrop(int replaceIndex)` → `BattleError`;
  `BattleEngine.SkipDrop()` → `BattleError`;`BattleSnapshot.PendingDrop`(`string`)

**背景**:`StartTurn()` 有三个调用点——构造函数(168 行)、`Revive()`(387 行)、`EndTurn()` 末尾(624 行)。
三处都可能切进 `DropChoice`。好消息是 `Compose`/`Dismantle`/`Cast`/`EndTurn` 的入口守卫全是
`if (Phase != BattlePhase.PlayerTurn)`,新增阶段后**自动**拒绝这些操作,无需额外守卫。

- [ ] **Step 1: 写失败测试**

加到 `BattleEngineTests.cs` 末尾(类内)。注意测试库 `Graph()` 里已有 `林`/`焚`/`灯` 等字。

```csharp
// ---- 回合掉字(2026-08-04):从出战牌组掉 1 字,满库停下决议 ----

/// <summary>掉落专用引擎:库位留空,牌组只有「林」以便断言掉的是什么。</summary>
private static BattleEngine DropEngine(int libraryCount, params string[] deck)
{
    var library = new List<string>();
    for (int i = 0; i < libraryCount; i++) library.Add("灯");
    return new BattleEngine(Graph(),
        new BattleConfig { LibraryCapacity = 3, DropsPerTurn = 1, UnlockedChars = deck },
        library, Array.Empty<string>(), new[] { WoodMinion() }, 42);
}

[Test]
public void Drop_LibraryNotFull_EntersLibraryDirectly()
{
    var engine = DropEngine(libraryCount: 1, deck: "林");
    Assert.That(engine.Phase, Is.EqualTo(BattlePhase.PlayerTurn));
    Assert.That(engine.Library, Does.Contain("林"));
    Assert.That(engine.PendingDrop, Is.Null);
}

[Test]
public void Drop_LibraryFull_EntersDropChoice()
{
    var engine = DropEngine(libraryCount: 3, deck: "林"); // 3/3 满
    Assert.That(engine.Phase, Is.EqualTo(BattlePhase.DropChoice));
    Assert.That(engine.PendingDrop, Is.EqualTo("林"));
    Assert.That(engine.Library.Count, Is.EqualTo(3));
}

[Test]
public void Drop_NoDeck_DoesNotDropNorSwitchPhase() // 工装与旧调用:UnlockedChars 为 null
{
    var engine = new BattleEngine(Graph(),
        new BattleConfig { LibraryCapacity = 3, DropsPerTurn = 1 },
        new[] { "灯" }, Array.Empty<string>(), new[] { WoodMinion() }, 42);
    Assert.That(engine.Phase, Is.EqualTo(BattlePhase.PlayerTurn));
    Assert.That(engine.PendingDrop, Is.Null);
    Assert.That(engine.Library.Count, Is.EqualTo(1));
}
```

- [ ] **Step 2: 跑测试,确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:`error CS0117: 'BattlePhase' does not contain a definition for 'DropChoice'` 等编译错误。

- [ ] **Step 3: 加枚举值与字段**

`BattleEngine.cs` 7-12 行:

```csharp
    public enum BattlePhase
    {
        PlayerTurn,
        Won,
        Lost,
        DropChoice, // 回合掉字遇满库:停下让玩家替换或跳过(2026-08-04)
    }
```

在 `PlayerHp` 属性附近(242 行一带)加只读属性,并在私有字段区加 `_pendingDrop`:

```csharp
        // 回合掉字遇满库时挂起的那个字;Phase == DropChoice 期间非 null
        private string _pendingDrop;

        /// <summary>待决议的掉落字(满库时挂起);无待决议时为 null。</summary>
        public string PendingDrop => _pendingDrop;
```

- [ ] **Step 4: 改写 StartTurn 的掉落段**

把 `StartTurn()` 里 633-640 行的部件掉落整段替换为:

```csharp
            // 回合掉字(2026-08-04):从出战牌组掉 N 个字入库,满库则停下让玩家决议。
            // 部件不再掉落 —— 五行部件只能靠拆字获得(拆免 AP 是这条的对冲)。
            if (_config.UnlockedChars != null && _config.UnlockedChars.Count > 0)
            {
                var deck = new List<string>(_config.UnlockedChars);
                for (int i = 0; i < _config.DropsPerTurn; i++)
                {
                    string pick = deck[_random.Next(deck.Count)];
                    if (_forge.Library.Count >= _config.LibraryCapacity)
                    {
                        _pendingDrop = pick;
                        Phase = BattlePhase.DropChoice;
                        return; // 决议完才继续;剩余份额本回合作废
                    }
                    var library = new List<string>(_forge.Library) { pick };
                    _forge = new ForgeState(library, _forge.Pool);
                }
            }
```

同时把 `BattleConfig.DropsPerTurn` 的默认值与注释(31 行)改为:

```csharp
        public int DropsPerTurn { get; set; } = 1; // 回合掉字数(2026-08-04:由「掉 2 部件」改为「掉 1 字」)
```

- [ ] **Step 5: 跑测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:三条新测试通过。**存量测试会红一批**(依赖部件掉落的),Step 8 统一处理。

- [ ] **Step 6: 写决议出口的失败测试**

```csharp
[Test]
public void ResolveDrop_ReplacesChosenSlot()
{
    var engine = DropEngine(libraryCount: 3, deck: "林");
    Assert.That(engine.ResolveDrop(0), Is.EqualTo(BattleError.None));

    Assert.That(engine.Phase, Is.EqualTo(BattlePhase.PlayerTurn));
    Assert.That(engine.PendingDrop, Is.Null);
    Assert.That(engine.Library.Count, Is.EqualTo(3));
    Assert.That(engine.Library, Does.Contain("林"));
}

[Test]
public void SkipDrop_KeepsLibraryUnchanged()
{
    var engine = DropEngine(libraryCount: 3, deck: "林");
    Assert.That(engine.SkipDrop(), Is.EqualTo(BattleError.None));

    Assert.That(engine.Phase, Is.EqualTo(BattlePhase.PlayerTurn));
    Assert.That(engine.PendingDrop, Is.Null);
    Assert.That(engine.Library, Does.Not.Contain("林"));
}

[Test]
public void ResolveDrop_OutOfRange_Rejected()
{
    var engine = DropEngine(libraryCount: 3, deck: "林");
    Assert.That(engine.ResolveDrop(9), Is.EqualTo(BattleError.NotCastable));
    Assert.That(engine.Phase, Is.EqualTo(BattlePhase.DropChoice)); // 仍卡在决议
}

[Test]
public void DropChoice_BlocksCastAndEndTurn() // 阶段机强制决议:操作入口自动拒绝
{
    var engine = DropEngine(libraryCount: 3, deck: "林");
    Assert.That(engine.Cast("灯"), Is.EqualTo(BattleError.BattleOver));
    engine.EndTurn();
    Assert.That(engine.Phase, Is.EqualTo(BattlePhase.DropChoice)); // EndTurn 被守卫挡住
}

// ---- 阶段回归:StartTurn 的三个调用点都可能切进 DropChoice ----

[Test]
public void EndTurn_FullLibrary_EntersDropChoice_NotMistakenForBattleOver()
{
    // 库位 2/3 起手,掉 1 字后满库;打完这回合的 EndTurn 末尾再掉一次 → 应进决议而非被当成战斗结束
    var engine = DropEngine(libraryCount: 2, deck: "林");
    Assert.That(engine.Phase, Is.EqualTo(BattlePhase.PlayerTurn)); // 首回合还有位,直接入库
    Assert.That(engine.Library.Count, Is.EqualTo(3));

    engine.EndTurn();

    Assert.That(engine.Phase, Is.EqualTo(BattlePhase.DropChoice));
    Assert.That(engine.PendingDrop, Is.EqualTo("林"));
    Assert.That(engine.Enemies[0].Alive, Is.True); // 敌人还活着,确实不是战斗结束
}

[Test]
public void Revive_FullLibrary_EntersDropChoice() // Revive 也走 StartTurn
{
    var engine = new BattleEngine(Graph(),
        new BattleConfig { LibraryCapacity = 3, DropsPerTurn = 1, UnlockedChars = new[] { "林" } },
        new[] { "灯", "灯", "灯" }, Array.Empty<string>(), new[] { Strong() }, 42);
    Assert.That(engine.Phase, Is.EqualTo(BattlePhase.DropChoice)); // 开局就满库
    engine.SkipDrop();
    while (engine.Phase == BattlePhase.PlayerTurn) engine.EndTurn(); // 挨打到死
    Assert.That(engine.Phase, Is.EqualTo(BattlePhase.Lost));

    engine.Revive();

    Assert.That(engine.Phase, Is.EqualTo(BattlePhase.DropChoice));
    Assert.That(engine.PendingDrop, Is.EqualTo("林"));
}
```

⚠ `Strong()` 是打得死玩家的强怪;若 `BattleEngineTests` 里没有同名辅助,
用该文件既有的高攻 `EnemyDef` 构造(参考 `MetaBoss()` / `WoodMinion()` 的写法),
攻击力要足以在若干回合内打穿 50 血。第二条测试里的 `while` 循环若不收敛就是构造错了。

- [ ] **Step 7: 实现两个出口**

加在 `GrantLibraryChar`(390 行)附近:

```csharp
        /// <summary>掉落决议:用待决议字换掉字库第 <paramref name="replaceIndex"/> 张
        /// (被换的字永久移除,与战利品 PickRewardReplacing 同口径)。</summary>
        public BattleError ResolveDrop(int replaceIndex)
        {
            if (Phase != BattlePhase.DropChoice) return BattleError.BattleOver;
            if (replaceIndex < 0 || replaceIndex >= _forge.Library.Count)
                return BattleError.NotCastable;

            var library = new List<string>(_forge.Library);
            library[replaceIndex] = _pendingDrop;
            _forge = new ForgeState(library, _forge.Pool);
            _pendingDrop = null;
            Phase = BattlePhase.PlayerTurn;
            return BattleError.None;
        }

        /// <summary>掉落决议:弃掉这次掉落,字库不变。</summary>
        public BattleError SkipDrop()
        {
            if (Phase != BattlePhase.DropChoice) return BattleError.BattleOver;
            _pendingDrop = null;
            Phase = BattlePhase.PlayerTurn;
            return BattleError.None;
        }
```

- [ ] **Step 8: 修存量测试**

跑测试,逐个修红掉的用例。已知受影响面:`DropTable`/`DropsPerTurn` 被 10 个测试文件引用。
**判断原则**:测试若断言「回合末池里多了部件」,现在改为断言库里多了字;
若该测试与掉落无关只是恰好配了 `DropTable`,把 `DropTable` 换成不配 `UnlockedChars` 即可
(不配就不掉落,行为回到旧的「什么都不掉」)。

不要为了让测试变绿而削弱断言——若某条测试因此失去意义,在报告里说明,不要静默删除。

- [ ] **Step 9: 快照往返测试**

加到 `SnapshotRoundTripTests.cs`:

```csharp
[Test]
public void DropChoice_SurvivesRoundTrip() // 阶段与待决议字丢了 = 续爬后掉落凭空消失
{
    var graph = Graph();
    var enemyDef = new EnemyDef("枯", Element.Wood, 10, 2);
    var engine = new BattleEngine(graph,
        new BattleConfig { LibraryCapacity = 2, DropsPerTurn = 1, UnlockedChars = new[] { "林" } },
        new[] { "炎", "炎" }, Array.Empty<string>(), new[] { enemyDef }, 42);
    Assert.That(engine.Phase, Is.EqualTo(BattlePhase.DropChoice)); // 前置:确实卡在决议

    var restored = BattleEngine.Restore(engine.Capture(), graph,
        new BattleConfig { LibraryCapacity = 2, DropsPerTurn = 1, UnlockedChars = new[] { "林" } },
        null, new Dictionary<string, EnemyDef> { ["枯"] = enemyDef });

    Assert.That(restored.Phase, Is.EqualTo(BattlePhase.DropChoice));
    Assert.That(restored.PendingDrop, Is.EqualTo("林"));
}
```

⚠ `SnapshotRoundTripTests` 里的 `Graph()` 需含 `炎`/`林`;若没有,按该文件既有风格补进它自己的 `Graph()`。

- [ ] **Step 10: 快照字段与接线**

`RunSnapshot.cs` 的 `BattleSnapshot` 加字段:

```csharp
        public string PendingDrop { get; set; } // 待决议的掉落字(DropChoice 阶段,2026-08-04)
```

`BattleEngine.Capture()` 加 `PendingDrop = _pendingDrop,`;
`BattleEngine.Restore()` 加 `_pendingDrop = snapshot.PendingDrop`(照该方法既有的字段灌入风格写)。

- [ ] **Step 11: 全量跑绿并提交**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Core/RunSnapshot.cs Brushblade/Assets/_Project/Tests/BattleEngineTests.cs Brushblade/Assets/_Project/Tests/SnapshotRoundTripTests.cs
git commit -m "feat(core): 回合掉落改为掉字,满库进 DropChoice 决议

部件掉落取消,改从出战牌组(UnlockedChars)掉 1 字入库。满库时挂起待
决议字并切 BattlePhase.DropChoice,由 ResolveDrop(替换)或 SkipDrop
(跳过)收口 —— 与战利品 PickRewardReplacing、奇遇 EventOverflow 同一
心智模型。

Compose/Dismantle/Cast/EndTurn 的入口守卫都是 Phase != PlayerTurn,
新增阶段后自动拒绝,无需额外守卫。"
```

---

### Task 2: 起手字库 6 → 5

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Meta.cs:34`
- Test: `Brushblade/Assets/_Project/Tests/MetaTests.cs`

**Interfaces:**
- Consumes: Task 1 的回合掉字行为
- Produces: `MetaRules.StartingLibrarySize == 5`

**背景**:起手 6 = 字库容量 6,不改则**第一回合必弹决议窗**(构造函数就调 `StartTurn()`)。
留一格给掉落,让「出字腾位 → 回合补 1」的循环从第一回合就顺畅。

- [ ] **Step 1: 写失败测试**

加到 `MetaTests.cs`:

```csharp
[Test]
public void StartingLibrary_LeavesOneSlotForDrop() // 起手 = 容量则第一回合必弹决议窗
{
    Assert.That(MetaRules.StartingLibrarySize, Is.EqualTo(5));
    Assert.That(MetaRules.StartingLibrarySize, Is.LessThan(new BattleConfig().LibraryCapacity));
}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:`Expected: 5 But was: 6`。

- [ ] **Step 3: 改常量**

`Meta.cs:34`:

```csharp
        public const int StartingLibrarySize = 5; // 留一格给回合掉字(2026-08-04);容量仍是 6
```

- [ ] **Step 4: 跑测试并修存量断言**

依赖起手字库的测试文件:`TutorialTests` / `RunEngineTests` / `MetaTests` / `SnapshotRoundTripTests` / `PerkTests`。
逐个修,原则同 Task 1 Step 8。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/Meta.cs Brushblade/Assets/_Project/Tests/
git commit -m "feat(core): 起手字库 6→5,给回合掉字留一格

起手 = 容量 6 时,构造函数里的 StartTurn 会让第一回合就弹满库决议窗。"
```

---

### Task 3: 战利品 5 选 2,删部件选取

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/RunEngine.cs`(`RewardPicks` 39 行、`BeginRewards` 492-498 行、`PickRewardComponent`、`MaybeFinishRewards`)
- Modify: `Brushblade/Assets/_Project/Core/RunSnapshot.cs`(删 `ComponentPicksLeft`)
- Test: `Brushblade/Assets/_Project/Tests/RunEngineTests.cs`

**Interfaces:**
- Consumes: `RunEngine.RollRewardOptions()`(私有,已存在,抽 `RewardOptionCount = 5` 个字候选)
- Produces: `RunEngine.CharPicksLeft` 初值为 2;`ComponentPicksLeft` / `PickRewardComponent` 已删除

**背景**:`RewardOptionCount` 已经是 5,只需把可取数从 1 改为 2 并删掉部件那一路。
`ComponentRewardChoices` 与 `_componentOptions` **保留**——奇遇的 `randomComponents` 仍在用。

- [ ] **Step 1: 写失败测试**

加到 `RunEngineTests.cs`(照该文件既有的 run 构造风格):

```csharp
[Test]
public void Reward_NormalBattle_PicksTwoChars()
{
    var run = Run();
    WinCurrentBattle(run);
    run.AdvanceAfterBattle();

    Assert.That(run.Phase, Is.EqualTo(RunPhase.Reward));
    Assert.That(run.CharPicksLeft, Is.EqualTo(2));
    Assert.That(run.RewardOptions.Count, Is.EqualTo(5));
}
```

⚠ `Run()` 的 `BattleConfig` 配了 `DropTable = new[] { "木" }` 但**没配 `UnlockedChars`**,
所以 Task 1 之后它不掉落 —— 这条测试不受掉字影响,专心测战利品即可。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:`CharPicksLeft` 断言 `Expected: 2 But was: 1`。

- [ ] **Step 3: 改常量并删部件路径**

`RunEngine.cs:39`:

```csharp
        private const int RewardPicks = 2;       // 普通战斗 5 选 2(2026-08-04;Boss 层奖励走宝箱,不经此)
```

`BeginRewards`(492-498 行)删掉部件两行:

```csharp
            RollRewardOptions();
            CharPicksLeft = RewardPicks;
            Phase = RunPhase.Reward;
```

删除方法 `PickRewardComponent`、属性 `ComponentPicksLeft`,并把 `MaybeFinishRewards` 改为只看字:

```csharp
        private void MaybeFinishRewards()
        {
            if (CharPicksLeft == 0 || _rewardOptions.Count == 0)
                ProceedAfterReward();
        }
```

⚠ 改前先读一遍 `MaybeFinishRewards` 现状——若它现在做的不止「判完就走」,保留其余逻辑,只摘掉部件条件。

- [ ] **Step 4: 删快照字段并跑测试**

`RunSnapshot.cs` 删 `ComponentPicksLeft`;`RunEngine.Capture()`/`Restore()` 同步删该行。
跑测试,修红掉的存量用例。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/RunEngine.cs Brushblade/Assets/_Project/Core/RunSnapshot.cs Brushblade/Assets/_Project/Tests/RunEngineTests.cs
git commit -m "feat(core): 战利品改 5 选 2,取消部件选取

RewardOptionCount 本就是 5,只改可取数 1→2。部件不再作为战利品发放
(五行部件改为只能靠拆字获得),PickRewardComponent 与 ComponentPicksLeft
一并删除。ComponentRewardChoices 保留 —— 奇遇的 randomComponents 仍在用。"
```

---

### Task 4: 复活改两轮 5 选 2

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/RunEngine.cs`(`ReviveCharPicks`/`ReviveComponentPicks` 389-390 行、`Revive` 流程 408-414 行、`PickReviveChar`、`PickReviveComponent`、`MaybeFinishRevive` 451-457 行)
- Modify: `Brushblade/Assets/_Project/Core/RunSnapshot.cs`(删 `ReviveComponentPicksLeft`,加 `ReviveRoundsLeft`)
- Test: `Brushblade/Assets/_Project/Tests/RunEngineTests.cs`

**Interfaces:**
- Consumes: Task 3 删干净的部件路径;`RollRewardOptions()`
- Produces: `RunEngine.ReviveCharPicksLeft`(每轮 2)、`RunEngine.ReviveRoundsLeft`(初值 2);
  `ReviveComponentPicksLeft` / `PickReviveComponent` 已删除

**背景**:「随机两次」是**两轮独立重抽**——第一轮 5 选 2,选完**重新抽** 5 个再选 2,共 4 字。
不是一次抽 5 选 4(候选池小一半,手感不同)。轮次计数必须进快照,否则第一轮选完存盘、
续爬后第二轮就丢了。

- [ ] **Step 1: 写失败测试**

```csharp
[Test]
public void Revive_TwoRounds_YieldsFourChars()
{
    var run = LostRun();
    Assert.That(run.TryRevive(), Is.True);
    Assert.That(run.ReviveCharPicksLeft, Is.EqualTo(2));
    Assert.That(run.ReviveRoundsLeft, Is.EqualTo(2));

    run.PickReviveChar(0);
    run.PickReviveChar(0);

    // 第一轮取尽 → 自动开第二轮,候选重新抽满 5 个
    Assert.That(run.ReviveRoundsLeft, Is.EqualTo(1));
    Assert.That(run.ReviveCharPicksLeft, Is.EqualTo(2));
    Assert.That(run.RewardOptions.Count, Is.EqualTo(5));
}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:`ReviveRoundsLeft` 不存在,编译失败。

- [ ] **Step 3: 改常量与字段**

`RunEngine.cs:389-390` 换成:

```csharp
        private const int ReviveCharPicks = 2;   // 复活补给:每轮选字次数
        private const int ReviveRounds = 2;      // 复活补给:重抽轮数(2026-08-04:两轮各 5 选 2 = 4 字)
```

加公开属性(照 `ReviveCharPicksLeft` 既有风格):

```csharp
        public int ReviveRoundsLeft { get; private set; }
```

- [ ] **Step 4: 改复活流程**

`Revive` 流程(408-414 行)删掉部件两行、加轮次初始化:

```csharp
            RollRewardOptions();
            ReviveCharPicksLeft = ReviveCharPicks;
            ReviveRoundsLeft = ReviveRounds;
            Phase = RunPhase.Reviving;
```

`MaybeFinishRevive`(451-457 行)改为轮次驱动:

```csharp
        private void MaybeFinishRevive()
        {
            if (ReviveCharPicksLeft > 0 && _rewardOptions.Count > 0) return; // 本轮还能取

            ReviveRoundsLeft -= 1;
            if (ReviveRoundsLeft > 0)
            {
                RollRewardOptions();                  // 下一轮:候选重新抽满
                ReviveCharPicksLeft = ReviveCharPicks;
                return;
            }
            Phase = RunPhase.InBattle; // 轮次用尽,接着打这一场
        }
```

删除 `PickReviveComponent` 与 `ReviveComponentPicksLeft`。

- [ ] **Step 5: 快照接线**

`RunSnapshot.cs` 删 `ReviveComponentPicksLeft`,加:

```csharp
        public int ReviveRoundsLeft { get; set; } // 复活补给剩余轮数(2026-08-04)
```

`RunEngine.Capture()` 加 `ReviveRoundsLeft = ReviveRoundsLeft,`;`Restore()` 加 `ReviveRoundsLeft = snapshot.ReviveRoundsLeft,`。

- [ ] **Step 6: 加快照往返测试**

```csharp
[Test]
public void ReviveRounds_SurviveRoundTrip() // 轮次丢了 = 续爬后第二轮凭空消失
{
    var run = LostRun();
    run.TryRevive();
    run.PickReviveChar(0);

    var restored = RunEngine.Restore(run.Capture(), Graph(),
        new RunConfig
        {
            Encounters = new[] { new[] { Strong() } },
            RewardPool = new[] { "灯", "焚", "林" },
        },
        new BattleConfig { DropTable = new[] { "木" } }, null, 0, 0);

    Assert.That(restored.ReviveRoundsLeft, Is.EqualTo(2));
    Assert.That(restored.ReviveCharPicksLeft, Is.EqualTo(1));
}
```

⚠ `RunConfig` 必须与 `LostRun()` 里那份**逐字一致**(`Restore` 要求外层用同一份配置重建),
`Graph()` / `Strong()` 用 `RunEngineTests` 该文件里既有的同名辅助。

- [ ] **Step 7: 跑绿并提交**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

```bash
git add Brushblade/Assets/_Project/Core/RunEngine.cs Brushblade/Assets/_Project/Core/RunSnapshot.cs Brushblade/Assets/_Project/Tests/RunEngineTests.cs
git commit -m "feat(core): 复活补给改两轮 5 选 2 共 4 字,取消部件补给

「随机两次」是两轮独立重抽:第一轮取尽后候选重新抽满 5 个再选 2。
不是一次抽 5 选 4 —— 后者候选池小一半。轮次计数进快照,否则第一轮
选完存盘、续爬后第二轮会丢。"
```

---

### Task 5: 表现层接线

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`
- Modify: `Brushblade/Assets/_Project/Presentation/GameRoot.cs`(若引用了已删的部件选取 API)

**Interfaces:**
- Consumes: `BattleEngine.PendingDrop`、`ResolveDrop(int)`、`SkipDrop()`、`BattlePhase.DropChoice`;
  Task 3/4 删除的 `PickRewardComponent` / `PickReviveComponent` / `ComponentPicksLeft` / `ReviveComponentPicksLeft`
- Produces: 无(终端任务)

**背景**:Core 改完后表现层会编译失败(引用了已删的部件选取 API),且 `DropChoice` 阶段没有 UI 就会卡死。
本任务**必须**跑 `tools/prescompile`——`coretests` 盖不到 Presentation,漏了会一路漏到用户打开 Unity 才炸(已发生过两次)。

- [ ] **Step 1: 找出所有编译断点**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q 2>&1 | grep "error CS"
```

把报错清单记下来——每一条都对应一处要么删、要么改的 UI。

- [ ] **Step 2: 移除战利品/复活的部件选取 UI**

按 Step 1 的清单,删掉战利品面板与复活面板里的部件那一栏(选项列表、取件按钮、剩余次数文案)。
保留字那一栏,只把「可取 1」相关文案改为跟随 `CharPicksLeft`。

⚠ 只删部件选取相关的;奇遇面板的部件展示**不要动**(`randomComponents` 仍在用)。

- [ ] **Step 3: 加掉落决议弹窗**

照抄 `DrawEventReplaceStep`(1417-1455 行)的结构——它就是「满库换哪一张」的现成模板,
只是数据源从 `_run.CarriedLibrary` 换成 `Battle.Library`。新增方法:

```csharp
        /// <summary>回合掉字遇满库(2026-08-04):停下让玩家选替换哪一张,或跳过这次掉落。
        /// 结构照搬 DrawEventReplaceStep —— 同一个「满库换哪张」的心智模型。</summary>
        private void DrawDropChoiceStep()
        {
            string incoming = Battle.PendingDrop;

            if (_modal != null) Object.Destroy(_modal);
            _modal = Ui.ModalShell(transform, $"字库已满 · 用掉落的「{incoming}」换掉哪一张?",
                new Vector2(360, 240), dismissable: false, out var stack);
            Ui.ThemedLabel(stack, "被换掉的字永久失去", 15, Theme.TextDim);

            Transform row = null;
            for (int i = 0; i < Battle.Library.Count; i++)
            {
                if (i % 4 == 0) row = Ui.Row(stack, $"Row{i / 4}", 8).transform;
                int replaceIndex = i;
                var def = _graph.Get(Battle.Library[i]);
                Ui.GlyphTile(row, def, $"{def.ApCost} AP", false, () =>
                {
                    string dropped = Battle.Library[replaceIndex];
                    if (Battle.ResolveDrop(replaceIndex) == BattleError.None)
                    {
                        _message = $"「{incoming}」替换「{dropped}」";
                        if (_modal != null) Object.Destroy(_modal);
                    }
                    Refresh();
                }, new Vector2(74, 96));
            }

            Ui.PillButton(stack, "不要,跳过", () =>
            {
                Battle.SkipDrop();
                if (_modal != null) Object.Destroy(_modal);
                _message = $"弃掉了「{incoming}」";
                Refresh();
            }, Theme.LockedBg, Theme.TextMain, 16, new Vector2(150, 46));
        }
```

再在重绘入口挂上调用。找到 `Refresh()`(或该文件里负责整屏重绘的方法)中按 `Battle.Phase`
分支的位置,加一条:

```csharp
            if (Battle.Phase == BattlePhase.DropChoice) DrawDropChoiceStep();
```

⚠ 该弹窗与战斗其它操作互斥——`DropChoice` 阶段 `Cast`/`EndTurn` 已被 Core 拒绝,
UI 侧只要保证弹窗盖住操作区即可,不需要再加一套禁用逻辑。
⚠ `Refresh()` 每次都会 `Destroy` 并重建 `_modal`,所以不必担心弹窗残留。

- [ ] **Step 4: 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q 2>&1 | grep -c "error CS"
```

预期:`0`。

- [ ] **Step 5: 全量跑绿并提交**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

⚠ 若新增了玩家可见文案,必须重跑字体子集:`python3 tools/fonts/subset_fonts.py`,
否则 `tools/fonts/tests` 会因缺字形而红(本月已踩过一次)。

```bash
git add Brushblade/Assets/_Project/Presentation/
git commit -m "feat(presentation): 掉落决议弹窗,移除战利品/复活的部件选取

DropChoice 阶段弹窗让玩家选替换哪一张或跳过。战利品与复活的部件栏
随 Core 侧删除一并移除;奇遇面板的部件展示保留。"
```

---

## 收尾(控制器执行,不派给实施者)

- [ ] 让**用户**在 Unity EditMode 跑一次全绿。`ConfigLoaderTests` 被 `tools/coretests` 排除
  (它引 `UnityEngine.Application`),其中 `ShippedConfig_FirstTowerTutorial_CanClearFloorOne`
  依赖开局状态,本轮改起手字库很可能踩到。这个坑本月已踩过一次。
- [ ] 让用户试玩确认:决议弹窗的频率是否可接受(风险 3);开局部件是否卡手(风险 1)。
- [ ] 走 `superpowers:finishing-a-development-branch` 收尾。
