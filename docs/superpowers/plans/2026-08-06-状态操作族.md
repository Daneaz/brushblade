# 状态操作族 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地驱散/净化/免疫/斩杀/复活五种机制共 14 字,并补上引擎缺失的「敌方施加给玩家的减益」这一整类,字表可用字数 109 → 123。

**Architecture:** 驱散/净化/免疫/复活是四个新 `EffectKind`(因为 `灭`/`浴`/`塞`/`岿`/`杜`/`活` 没有伤害效果可挂),斩杀是 `EffectDef` 上的两个修饰字段。玩家侧减益靠两条新路:Boss「倾覆」的 AP 惩罚**状态化**成 `StatusKind.Seal`(零新内容,顺带修掉一个存档 bug),以及新字怪「灯花」带新能力 `EnemyAbility.Sear` 给玩家挂灼烧(复用既有 `StatusKind.Burn`)。

**Tech Stack:** Unity 6000.5.2f1 / C# 纯逻辑层(Core、Data 禁引 UnityEngine);Python 数据管线(pytest);测试走 `tools/coretests`(dotnet)+ `tools/prescompile`(离线编译)。

**规格来源:** `docs/superpowers/specs/2026-08-06-状态操作族-design.md`

## Global Constraints

- **四层 asmdef 依赖单向**:`Presentation → {Core, Data, Platform}`,`Data → Core`。**Core 与 Data 禁止 `using UnityEngine`**。
- **随机性一律走 Core 内带种子的 `GameRandom`**,禁用 `UnityEngine.Random`。本子项目不引入任何随机。
- **测试断言只用 Unity 版 NUnit 也支持的 API**:禁用 `Is.AnyOf` / `Is.All.AnyOf`。多选一用 `Is.EqualTo(a).Or.EqualTo(b)`,集合子集用 `Has.All.Matches<T>`。
- **测试代码禁止直接引用 Newtonsoft**(`JsonConvert` 等)。要测序列化就走 `Brushblade.Data.SaveSerializer.ToJson/FromJson`。测试只能引用 Core / Data。
- **`chars.json` 是生成物,不是手写的**:`tools/pipeline/export_chars.py` 读 `docs/design/字选型/技能机制详表.md` → `extract_values.py` 抽数值 → 写 `Brushblade/Assets/StreamingAssets/config/chars.json`。**永远不要直接手改 `chars.json`** —— 改详表然后重新生成。`enemies.json` 相反,它**是手写的**,直接改。
- **详表是数值的唯一真相**。本批 14 字已逐字核过相生「他生我」口径(配方原料需含生本字属性的那一系),**无一吃 ×3**,故详表填的就是最终值。
- **新枚举值一律追加在末尾**(`StatusKind` / `EffectKind` / `EnemyAbility` / `BattleEventKind`)—— 老存档里已持久化的序数不能位移。
- **UI 文案禁止 emoji**:字体子集补不出来,上线渲染成空框(`test_subset_fonts_cover_charset` 拦这个)。用中文短词。
- **每个 Core/Data 模块先写失败测试再实现(TDD)**;改完 Presentation **必须过离线编译**。
- **提交信息用 conventional commits**(`feat`/`fix`/`docs`/`chore` + 范围)。
- **只提交你自己改的文件**。用户在并行开发,`Brushblade/Assets/Settings/` 与 `Brushblade/ProjectSettings/ProjectSettings.asset` 有未提交改动,仓库里还有若干未跟踪的 `.meta` —— **绝不要 `git add -A` / `git add .`,绝不要碰这些**。

### 每个任务收尾必跑

```bash
# Core/Data 单测(毫秒级,不依赖编辑器锁)
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q

# 管线 + 字体(Task 8 起必跑)
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q

# Presentation 离线编译(Task 9 必跑)
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

只看 `error CS`;`warning MSB3245` 是 Unity 程序集自带的无关引用,忽略。基线:coretests **644 passed**、pytest **226 passed**、prescompile **0 error**。

### 一个反复踩到的时序事实

`BattleEngine.EndTurn()` 的顺序是:

```
敌人灼烧结算 → 敌人流血 → 玩家持续治疗 → 召唤物光环治疗 → 召唤物出手
→ 敌方行动 → 状态回合递减 → 玩家阵亡早退 → 判胜 → 缺笔妖补全 → StartTurn()
```

**在敌方段挂上的、`TurnsLeft` 为正的状态,会被同一个 `EndTurn` 的「状态回合递减」立刻减一次。**
断言对不上时先核这个顺序再怀疑实现。

---

## 文件结构

| 文件 | 职责 | 任务 |
|---|---|---|
| `Core/StatusEffect.cs` | `StatusKind` 加 `Seal`/`Immunity`;`StatusBag` 加 `RemoveFirst`/`RemoveEntry` | 1、3、4 |
| `Core/EffectDef.cs` | `EffectKind` 加 `Dispel`/`Cleanse`/`Immunity`/`Revive`;`EffectDef` 加两个斩杀字段 | 3、4、5、6 |
| `Core/EnemyDef.cs` | `EnemyAbility` 加 `Sear` | 2 |
| `Core/BattleEngine.cs` | 全部结算接线;`BattleEventKind` 加 4 个事件 | 1~6 |
| `Data/ConfigLoader.cs` | `EffectDto` 加两个斩杀字段 | 7 |
| `StreamingAssets/config/enemies.json` | 新字怪「灯花」+ 三个 band 的 `enemyPool`(**手写文件**) | 7 |
| `docs/design/字选型/技能机制详表.md` | 14 行由 ⚠ 改 ✅;§7.2 标记;§7.4 移除已解撞车的字 | 8 |
| `tools/pipeline/extract_values.py` | 无数值效果 token、`DispelEach`、斩杀修饰 | 8 |
| `tools/pipeline/export_chars.py` | `MANUAL_RECIPES` 加 `塞`/`湮` | 8 |
| `StreamingAssets/config/chars.json`、两个 `.ttf`、`tools/fonts/charset.txt` | **生成物** | 8 |
| `Presentation/UI/EnemyInfo.cs` | `Sear` 的三处文案 | 9 |
| `Presentation/UI/Theme.cs` | `Sear` 的 chip 配色 | 9 |
| `Presentation/BattleView.cs` | 玩家状态条(封字/灼烧/免疫)、免疫挡下反馈 | 9 |
| `Tests/StatusOpsTests.cs` | **新建**,本子项目的行为测试主场 | 1~6 |
| `Tests/CharTableTests.cs` | 真实配置接线守卫 | 8 |
| `tools/pipeline/tests/test_export_chars.py` | 管线侧抽取测试 | 8 |

---

### Task 1: `Seal`(封字)—— AP 惩罚状态化

Boss「倾覆」现在写的是裸字段 `_apPenaltyNextTurn`。状态化后它可被净化、可被免疫,并且跟着存档走 —— **顺带修掉「倾覆后存档续爬白丢惩罚」这个既有 bug**(`_apPenaltyNextTurn` 从来没进过 `BattleSnapshot`)。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/StatusEffect.cs`(`StatusKind` 枚举与类注释)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`_apPenaltyNextTurn` 字段、`StartTurn`、`CastBossSkill` 的 `Topple` 分支)
- Create: `Brushblade/Assets/_Project/Tests/StatusOpsTests.cs`

**Interfaces:**
- Produces:`StatusKind.Seal`,`Magnitude` = AP 扣减量,`Polarity = Debuff`,`SourceId = "倾覆"`

- [ ] **Step 1: 写失败测试**

新建 `Brushblade/Assets/_Project/Tests/StatusOpsTests.cs`:

```csharp
using System;
using System.Collections.Generic;
using System.Linq;
using Brushblade.Core;
using NUnit.Framework;

namespace Brushblade.Core.Tests
{
    /// <summary>状态操作族(2026-08-06,子项目 A):驱散/净化/免疫/斩杀/复活,
    /// 以及配套的玩家侧减益(封字 / 玩家灼烧)。
    /// 规格见 docs/superpowers/specs/2026-08-06-状态操作族-design.md。</summary>
    public class StatusOpsTests
    {
        // 出字库:每个字只带一种待测机制,敌人一律用「心」属性避开生克干扰。
        private static RecipeGraph Graph() => new(new[]
        {
            new CharDef("木", Element.Wood),
            new CharDef("素", Element.Wood,   // 无被动的基准召唤(10 血 / 攻 3)
                effects: new[] { new EffectDef(EffectKind.Summon, 10, summonCount: 1, summonAttack: 3, summonChar: "木") }),
        });

        private static BattleEngine Engine(string[] library, EnemyDef[] enemies,
            BattleConfig config = null, int? startingHp = null) =>
            new(Graph(), config ?? new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 50 },
                library, Array.Empty<string>(), enemies, seed: 1, startingHp: startingHp);

        private static EnemyDef Dummy(int hp = 200, int attack = 0) => new("靶", Element.Heart, hp, attack);

        /// <summary>带倾覆大招的 Boss。BossChargeEvery = 1 → 第一个敌方回合蓄力,第二个释放。</summary>
        private static EnemyDef ToppleBoss(int attack = 4) =>
            new("覆", Element.Heart, 300, attack,
                phases: new[] { new BossPhaseDef("覆", Element.Heart, 300, attack, skill: BossSkill.Topple) });

        private static BattleConfig BossConfig() =>
            new() { DropTable = new[] { "木" }, PlayerMaxHp = 200, BossChargeEvery = 1 };

        // ---- 封字:倾覆的 AP 惩罚 ----

        [Test]
        public void Topple_AppliesSealStatus_NotABareField()
        {
            var engine = Engine(Array.Empty<string>(), new[] { ToppleBoss() }, BossConfig());
            engine.EndTurn();  // 蓄力
            engine.EndTurn();  // 释放倾覆
            Assert.That(engine.PlayerStatuses.Has(StatusKind.Seal), Is.True, "倾覆应挂上封字");
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.Seal), Is.EqualTo(1));
        }

        [Test]
        public void Seal_SurvivesTheSameEndTurnAndCutsNextTurnAp()
        {
            // TurnsLeft 必须填 2:倾覆在敌方段挂上,而同一个 EndTurn 的「状态回合递减」排在
            // StartTurn 之前。填 1 会被当场减到 0 移除,StartTurn 读到 0 —— 效果凭空消失。
            var engine = Engine(Array.Empty<string>(), new[] { ToppleBoss() }, BossConfig());
            int fullAp = engine.Ap;
            engine.EndTurn();  // 蓄力
            Assert.That(engine.Ap, Is.EqualTo(fullAp), "蓄力回合不该扣 AP");
            engine.EndTurn();  // 释放倾覆 → 本次 StartTurn 就该少 1 点
            Assert.That(engine.Ap, Is.EqualTo(fullAp - 1));
        }

        [Test]
        public void Seal_ExpiresAfterExactlyOnePenalizedTurn()
        {
            var engine = Engine(Array.Empty<string>(), new[] { ToppleBoss() }, BossConfig());
            int fullAp = engine.Ap;
            engine.EndTurn();  // 蓄力
            engine.EndTurn();  // 释放 → AP 少 1
            Assert.That(engine.Ap, Is.EqualTo(fullAp - 1));
            engine.EndTurn();  // 下一轮蓄力 → 封字到期
            Assert.That(engine.Ap, Is.EqualTo(fullAp), "只该罚一个回合");
            Assert.That(engine.PlayerStatuses.Has(StatusKind.Seal), Is.False);
        }

        [Test]
        public void Seal_SurvivesSaveRoundTrip()
        {
            // 既有 bug:_apPenaltyNextTurn 从来没进过 BattleSnapshot,倾覆后存档续爬白丢惩罚。
            // 状态化后它跟着 PlayerStatuses 一起存。
            var engine = Engine(Array.Empty<string>(), new[] { ToppleBoss() }, BossConfig());
            engine.EndTurn();
            engine.EndTurn();  // 封字已挂
            var snapshot = engine.Capture();
            Assert.That(snapshot.PlayerStatuses.Any(s => s.Kind == StatusKind.Seal), Is.True,
                "封字必须进快照");
        }
    }
}
```

⚠ `engine.Capture()` 的方法名与返回类型以仓库现状为准 —— 打开 `Core/BattleEngine.cs` 找那个产出 `BattleSnapshot` 的公开方法照抄。若它是 `internal`,改用 `RunEngine` 的存档入口或断言 `engine.PlayerStatuses` 本身,不要为测试放宽生产代码可见性。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:编译失败,`StatusKind` 没有 `Seal`。

- [ ] **Step 3: 加 `StatusKind.Seal`**

`Core/StatusEffect.cs`,在 `Curse` 之后**追加**(不能插在中间,老存档序数会位移):

```csharp
        Curse,            // 诅咒:攻击 −Magnitude%,不叠层只刷新(2026-08-05)
        Seal,             // 封字:玩家下回合 AP −Magnitude(2026-08-06,Boss 倾覆)
```

类注释末尾补 `Seal=AP 扣减量(StartTurn 读它)`。

- [ ] **Step 4: `StartTurn` 改读状态,删掉裸字段**

`Core/BattleEngine.cs`:

删除字段声明 `private int _apPenaltyNextTurn;`(及其注释)。

`StartTurn()` 里把

```csharp
            Ap = Math.Max(1, _config.ApPerTurn - _apPenaltyNextTurn); // 下限 1:不出现完全不能动的回合
            _apPenaltyNextTurn = 0;
```

改成

```csharp
            // 封字(2026-08-06):AP 扣减从裸字段改成 StatusKind.Seal —— 这样它可被净化、
            // 可被免疫,并且跟着 PlayerStatuses 进存档(裸字段从来没进过 BattleSnapshot,
            // 倾覆后存档续爬会白丢惩罚)。到期移除由统一的状态回合递减负责,这里不清。
            Ap = Math.Max(1, _config.ApPerTurn - _playerStatuses.TotalMagnitude(StatusKind.Seal));
```

- [ ] **Step 5: 倾覆改挂状态**

`CastBossSkill` 的 `case BossSkill.Topple:` 里,把 `_apPenaltyNextTurn = 1;` 改成:

```csharp
                    // TurnsLeft = 2 而不是 1(2026-08-06):倾覆在敌方段挂上,而同一个 EndTurn
                    // 的「状态回合递减」排在 StartTurn 之前 —— 填 1 会被当场减到 0 移除,
                    // StartTurn 读到 0,效果凭空消失。填 2 才等价于「只罚下一个玩家回合」。
                    _playerStatuses.Apply(new StatusEffect
                    {
                        Kind = StatusKind.Seal, Polarity = StatusPolarity.Debuff,
                        Magnitude = 1, TurnsLeft = 2, SourceId = "倾覆",
                    });
```

- [ ] **Step 6: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿。**特别确认既有的 `BossSkillTests` 里关于倾覆扣 AP 的断言没被打破。**

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/StatusEffect.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/StatusOpsTests.cs
git commit -m "feat(core): 倾覆的 AP 惩罚状态化为 Seal——顺带修掉它从不进存档的老 bug"
```

⚠ 新建的 `.cs` 需要 Unity 生成 `.meta`,Unity 没开时不会生成。**不要手写 `.meta`,也不要 `git add` 未跟踪的 `.meta`。**

---

### Task 2: 玩家灼烧与新能力 `Sear`

玩家侧的第二种减益。复用 `StatusKind.Burn`,不新增枚举。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EnemyDef.cs`(`EnemyAbility` 枚举)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`EndTurn` 的 DOT 段;敌人行动循环)
- Modify: `Brushblade/Assets/_Project/Tests/StatusOpsTests.cs`

**Interfaces:**
- Produces:`EnemyAbility.Sear`;私有常量 `SearStacks = 1`;玩家侧 `StatusKind.Burn`(`TurnsLeft = -1`,段内持久)

- [ ] **Step 1: 写失败测试**

追加到 `StatusOpsTests` 类里:

```csharp
        // ---- 玩家灼烧与 Sear ----

        private static EnemyDef Searer(int attack = 3) =>
            new("灯花", Element.Fire, 200, attack, EnemyAbility.Sear);

        [Test]
        public void Sear_AppliesBurnToPlayerOnAttack()
        {
            var engine = Engine(Array.Empty<string>(), new[] { Searer() });
            engine.EndTurn();
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(1));
        }

        [Test]
        public void PlayerBurn_TicksThenDecays_AndIgnoresWuxing()
        {
            // 玩家没有五行属性,灼烧结算不套任何倍率:1 层 × 系数 2 = 2 伤。
            // 灯花攻 3 → 挂灼烧那一记也打 3。第一回合:挨 3(灼烧还没结算)。
            // 第二回合:先结算 1 层灼烧掉 2、层数降到 0,再挨 3、再挂 1 层。
            var engine = Engine(Array.Empty<string>(), new[] { Searer() });
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(47), "第 1 回合只挨普攻 3");
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(42), "第 2 回合:灼烧 2 + 普攻 3");
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(1),
                "结算 −1 层、攻击 +1 层,净 0");
        }

        [Test]
        public void PlayerBurn_StaysAtOneStack_DoesNotSnowball()
        {
            // 子项目 C 的烓因为「每回合挂 3、只减 1」净 +2 而失控。灯花挂 1 减 1,恒定 1 层。
            var engine = Engine(Array.Empty<string>(), new[] { Searer() }, startingHp: 200,
                config: new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 200 });
            for (int turn = 0; turn < 5; turn++) engine.EndTurn();
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(1));
        }

        [Test]
        public void PlayerBurn_DoesNotEarlyReturnBeforeStatusTick()
        {
            // 玩家被灼烧烧到 0 血时不能立刻早退——状态回合递减必须照跑,
            // 否则广告复活后所有状态多续一回合(既有约束,BattleEngine 里有注释守着)。
            // 这里用 1 血玩家 + 已有灼烧验证:烧死后 Phase 是 Lost,且封字之类的状态照常递减。
            var engine = Engine(Array.Empty<string>(), new[] { Searer() }, startingHp: 1);
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(0));
            Assert.That(engine.Phase, Is.EqualTo(BattlePhase.Lost));
        }
```

⚠ `BattleConfig` 的默认 `ApPerTurn` 是 3、`PlayerMaxHp` 见类定义。`_burnPerStack` 的初值(灼烧系数)以 `BattleEngine` 现状为准 —— 若不是 2,把上面几条的期望值按实际系数重算,并在报告里说明。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:编译失败,`EnemyAbility` 没有 `Sear`。

- [ ] **Step 3: 加 `EnemyAbility.Sear`**

`Core/EnemyDef.cs`,在 `Scorch` 之后**追加**:

```csharp
        Scorch,   // 焦痕:每次被击中且存活,攻 +2(越磨越烫,宜速杀)
        Sear,     // 灯花:每次攻击给玩家挂 1 层灼烧(2026-08-06;玩家侧减益的来源之一)
```

- [ ] **Step 4: 玩家灼烧结算**

`Core/BattleEngine.cs` 的 `EndTurn()` 里,**紧接在敌人灼烧那一段的 `CheckWin()` 之后、流血段之前**插入:

```csharp
            // 玩家灼烧(2026-08-06):层数 × 系数掉血,然后 −1 层。玩家没有五行属性,
            // 所以**不走生克** —— 敌人侧那条 KeMultiplier(Fire, enemy.Element) 不适用。
            // 烧到 0 血也不在这里早退:统一交给下方「状态回合递减」之后的判负点,
            // 否则本回合的状态递减会整个跳过,广告复活后所有状态多续一回合。
            var playerBurn = _playerStatuses.Find(StatusKind.Burn);
            if (playerBurn != null && playerBurn.Magnitude > 0)
            {
                int playerTick = playerBurn.Magnitude * _burnPerStack;
                PlayerHp = Math.Max(0, PlayerHp - playerTick);
                playerBurn.Magnitude -= 1;
                if (playerBurn.Magnitude <= 0) _playerStatuses.Remove(StatusKind.Burn);
                _events.Add(new BattleEvent(BattleEventKind.BurnTick, -1, playerTick)); // −1 = 玩家
            }
```

- [ ] **Step 5: `Sear` 接线**

敌人行动循环里,每次出手之后(与「通假字首次行动后现形」同一位置,紧挨着它)插入:

```csharp
                    // 灯花(2026-08-06):每次攻击给玩家挂 1 层灼烧。TurnsLeft = -1 段内持久,
                    // 靠上方的玩家灼烧结算段自减 Magnitude,不受 TickTurns 影响(与敌人侧同口径)
                    if (enemy.Def.Ability == EnemyAbility.Sear)
                    {
                        int stacks = (_playerStatuses.Find(StatusKind.Burn)?.Magnitude ?? 0) + SearStacks;
                        _playerStatuses.Apply(new StatusEffect
                        {
                            Kind = StatusKind.Burn, Polarity = StatusPolarity.Debuff,
                            Magnitude = stacks, TurnsLeft = -1,
                        });
                        _events.Add(new BattleEvent(BattleEventKind.Burn, -1, SearStacks)); // −1 = 玩家
                    }
```

在常量区(`MaxActionsPerTurn` 附近)加:

```csharp
        private const int SearStacks = 1;  // 灯花每次攻击给玩家挂的灼烧层数(2026-08-06)
```

- [ ] **Step 6: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/EnemyDef.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/StatusOpsTests.cs
git commit -m "feat(core): 玩家侧灼烧与新能力 Sear——净化终于有靶子了"
```

---

### Task 3: 驱散与净化

两个新 `EffectKind`,共用 `StatusBag` 的极性批量清除。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/StatusEffect.cs`(`StatusBag` 加 `RemoveFirst`)
- Modify: `Brushblade/Assets/_Project/Core/EffectDef.cs`(`EffectKind` 加两项)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`BattleEventKind` 加两项;`ApplyEffects` 两个 case;私有方法 `DispelFrom`)
- Modify: `Brushblade/Assets/_Project/Tests/StatusOpsTests.cs`

**Interfaces:**
- Produces:
  - `StatusBag.RemoveFirst(StatusPolarity polarity, int count)` → `int`(实际移除条数)
  - `EffectKind.Dispel`(`Value` = 条数,`-1` = 全部;复用既有 `TargetAll` 定范围)、`EffectKind.Cleanse`
  - `BattleEventKind.Dispel`(`TargetIndex` = 敌人,`Amount` = 移除条数)、`BattleEventKind.Cleanse`(`TargetIndex` = −1,`Amount` = 移除条数)

- [ ] **Step 1: 写失败测试**

`StatusOpsTests.Graph()` 里补四个字:

```csharp
            new CharDef("扫", Element.Heart,   // 灭:纯驱散全部(单体)
                effects: new[] { new EffectDef(EffectKind.Dispel, -1) }),
            new CharDef("剐", Element.Heart,   // 削:伤 9 + 驱散 1 条
                effects: new[] { new EffectDef(EffectKind.DamageSingle, 9),
                                 new EffectDef(EffectKind.Dispel, 1) }),
            new CharDef("荡", Element.Heart,   // 淡:全体伤 + 全体各驱散 1 条
                effects: new[] { new EffectDef(EffectKind.DamageAll, 5),
                                 new EffectDef(EffectKind.Dispel, 1, targetAll: true) }),
            new CharDef("涤", Element.Heart,   // 浴:纯净化
                effects: new[] { new EffectDef(EffectKind.Cleanse, 0) }),
```

追加测试:

```csharp
        // ---- 驱散 ----

        /// <summary>给敌人挂两条可驱散的增益(与标点小妖加攻同形:AttackBuff、段内持久、可叠)。</summary>
        private static void GiveTwoBuffs(EnemyState enemy)
        {
            for (int i = 0; i < 2; i++)
                enemy.Statuses.Apply(new StatusEffect
                {
                    Kind = StatusKind.AttackBuff, Polarity = StatusPolarity.Buff,
                    Magnitude = 3, TurnsLeft = -1, SourceId = $"妖#{i}",
                });
        }

        [Test]
        public void Dispel_MinusOne_RemovesEveryBuff()
        {
            var engine = Engine(new[] { "扫" }, new[] { Dummy() });
            GiveTwoBuffs(engine.Enemies[0]);
            Assert.That(engine.Enemies[0].Attack, Is.EqualTo(6), "基础 0 + 两条各 3");
            engine.Cast("扫", 0);
            Assert.That(engine.Enemies[0].Statuses.TotalMagnitude(StatusKind.AttackBuff), Is.EqualTo(0));
            Assert.That(engine.Enemies[0].Attack, Is.EqualTo(0));
        }

        [Test]
        public void Dispel_Counted_RemovesExactlyThatMany()
        {
            var engine = Engine(new[] { "剐" }, new[] { Dummy() });
            GiveTwoBuffs(engine.Enemies[0]);
            engine.Cast("剐", 0);
            Assert.That(engine.Enemies[0].Statuses.TotalMagnitude(StatusKind.AttackBuff), Is.EqualTo(3),
                "只清一条,剩一条");
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(191), "伤害照打");
        }

        [Test]
        public void Dispel_TargetAll_HitsEveryLivingEnemy()
        {
            var engine = Engine(new[] { "荡" }, new[] { Dummy(), Dummy() });
            GiveTwoBuffs(engine.Enemies[0]);
            GiveTwoBuffs(engine.Enemies[1]);
            engine.Cast("荡", 0);
            foreach (var enemy in engine.Enemies)
                Assert.That(enemy.Statuses.TotalMagnitude(StatusKind.AttackBuff), Is.EqualTo(3),
                    "每只各清一条");
        }

        [Test]
        public void Dispel_LeavesDebuffsAlone()
        {
            // 驱散只打增益。敌人身上的灼烧是减益,不该被自己的驱散字误清
            var engine = Engine(new[] { "扫" }, new[] { Dummy() });
            engine.Enemies[0].Statuses.Apply(new StatusEffect
            {
                Kind = StatusKind.Burn, Polarity = StatusPolarity.Debuff,
                Magnitude = 4, TurnsLeft = -1,
            });
            GiveTwoBuffs(engine.Enemies[0]);
            engine.Cast("扫", 0);
            Assert.That(engine.Enemies[0].Statuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(4));
        }

        [Test]
        public void Dispel_DoesNotTouchPlayerBuffs()
        {
            // 玩家自己的减伤/持续治疗也是 Buff 极性,但它们挂在 _playerStatuses 上,
            // 不在驱散的作用域里
            var engine = Engine(new[] { "扫" }, new[] { Dummy() });
            engine.PlayerStatuses.Apply(new StatusEffect
            {
                Kind = StatusKind.DamageReduction, Polarity = StatusPolarity.Buff,
                Magnitude = 30, TurnsLeft = -1, SourceId = "铠",
            });
            engine.Cast("扫", 0);
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.DamageReduction), Is.EqualTo(30));
        }

        // ---- 净化 ----

        [Test]
        public void Cleanse_RemovesPlayerDebuffs_KeepsBuffs()
        {
            var engine = Engine(new[] { "涤" }, new[] { Dummy() });
            engine.PlayerStatuses.Apply(new StatusEffect
            {
                Kind = StatusKind.Seal, Polarity = StatusPolarity.Debuff,
                Magnitude = 1, TurnsLeft = 2, SourceId = "倾覆",
            });
            engine.PlayerStatuses.Apply(new StatusEffect
            {
                Kind = StatusKind.Burn, Polarity = StatusPolarity.Debuff,
                Magnitude = 3, TurnsLeft = -1,
            });
            engine.PlayerStatuses.Apply(new StatusEffect
            {
                Kind = StatusKind.DamageReduction, Polarity = StatusPolarity.Buff,
                Magnitude = 30, TurnsLeft = -1, SourceId = "铠",
            });

            engine.Cast("涤", 0);

            Assert.That(engine.PlayerStatuses.Has(StatusKind.Seal), Is.False);
            Assert.That(engine.PlayerStatuses.Has(StatusKind.Burn), Is.False);
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.DamageReduction), Is.EqualTo(30),
                "增益不该被净化误伤");
        }
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:编译失败,`EffectKind` 没有 `Dispel` / `Cleanse`。

- [ ] **Step 3: `StatusBag` 加 `RemoveFirst`**

`Core/StatusEffect.cs`,在 `RemoveAll(StatusPolarity)` 旁边加:

```csharp
        /// <summary>按极性从头移除至多 count 条,返回实际移除条数(计数式驱散用)。</summary>
        public int RemoveFirst(StatusPolarity polarity, int count)
        {
            int removed = 0;
            for (int i = 0; i < _list.Count && removed < count; )
            {
                if (_list[i].Polarity != polarity) { i++; continue; }
                _list.RemoveAt(i);
                removed++;
            }
            return removed;
        }
```

- [ ] **Step 4: 加两个 `EffectKind`**

`Core/EffectDef.cs`,在 `ArmorBreak` 之后**追加**:

```csharp
        ArmorBreak,   // 破甲:目标承伤 +25%,持续 Value 回合。不叠层,重复施加只刷新(2026-08-05)
        Dispel,       // 驱散:清敌方增益。Value = 条数(−1 = 全部);TargetAll = 全体各清(2026-08-06)
        Cleanse,      // 净化:清玩家自身全部减益(Value 不用,2026-08-06)
```

- [ ] **Step 5: 加两个 `BattleEventKind`**

`Core/BattleEngine.cs` 的 `BattleEventKind` 末尾**追加**:

```csharp
        Dispel,      // 驱散敌方增益(TargetIndex = 敌人,Amount = 移除条数;2026-08-06)
        Cleanse,     // 净化玩家减益(TargetIndex = −1,Amount = 移除条数;2026-08-06)
```

- [ ] **Step 6: 实现两个 case 与 `DispelFrom`**

`ApplyEffects` 的 `switch` 里加(位置紧跟 `case EffectKind.ArmorBreak:` 之后):

```csharp
                    case EffectKind.Dispel:
                        // 条数用 effect.Value 而不是 value —— 驱散条数不吃卡等级(与召唤被动同口径:
                        // 「资源」随等级涨,「节奏」不涨),而且 −1 这个哨兵值过 ScaleByCardLevel 会算歪
                        if (effect.TargetAll)
                        {
                            int count = _enemies.Count; // 分裂产生的新怪不吃同一发(与 DamageAll 同口径)
                            for (int i = 0; i < count; i++)
                                if (_enemies[i].Alive) DispelFrom(i, effect.Value);
                        }
                        else if (_enemies[targetIndex].Alive)
                        {
                            DispelFrom(targetIndex, effect.Value);
                        }
                        break;

                    case EffectKind.Cleanse:
                    {
                        int cleansed = _playerStatuses.RemoveAll(StatusPolarity.Debuff);
                        if (cleansed > 0)
                            _events.Add(new BattleEvent(BattleEventKind.Cleanse, -1, cleansed));
                        break;
                    }
```

在 `ApplyBurn` 旁边加:

```csharp
        /// <summary>驱散一名敌人的增益:count &lt; 0 清全部,否则从头清至多 count 条。
        /// 现存的唯一靶子是 AttackBuff(标点小妖给同伴加攻、焦痕受击自燃)——两者都是
        /// TurnsLeft = -1 的永久增益且本场累计,所以驱散是它们唯一的解法。</summary>
        private void DispelFrom(int enemyIndex, int count)
        {
            var statuses = _enemies[enemyIndex].Statuses;
            int removed = count < 0
                ? statuses.RemoveAll(StatusPolarity.Buff)
                : statuses.RemoveFirst(StatusPolarity.Buff, count);
            if (removed > 0)
                _events.Add(new BattleEvent(BattleEventKind.Dispel, enemyIndex, removed));
        }
```

- [ ] **Step 7: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿。

- [ ] **Step 8: 提交**

```bash
git add Brushblade/Assets/_Project/Core/StatusEffect.cs \
        Brushblade/Assets/_Project/Core/EffectDef.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/StatusOpsTests.cs
git commit -m "feat(core): 驱散与净化——StatusBag 的极性清除终于有调用方了"
```

---

### Task 4: 免疫

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/StatusEffect.cs`(`StatusKind` 加 `Immunity`;`StatusBag` 加 `RemoveEntry`)
- Modify: `Brushblade/Assets/_Project/Core/EffectDef.cs`(`EffectKind` 加 `Immunity`)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`BattleEventKind` 加两项;`ApplyEffects` 一个 case;`DamagePlayerDirect` 开头;私有方法 `ConsumeImmunity`)
- Modify: `Brushblade/Assets/_Project/Tests/StatusOpsTests.cs`

**Interfaces:**
- Consumes:Task 3 加的 `EffectKind.Cleanse`(岿 要用)
- Produces:
  - `StatusKind.Immunity`(`Magnitude` = 剩余次数,`Polarity = Buff`,`TurnsLeft = -1`)
  - `StatusBag.RemoveEntry(StatusEffect effect)`
  - `EffectKind.Immunity`(`Value` = 次数)
  - `BattleEventKind.Immunity`(获得,`Amount` = 次数)、`BattleEventKind.ImmunityBlocked`(挡下,`Amount` = 挡掉的伤害)

- [ ] **Step 1: 写失败测试**

`Graph()` 里补三个字:

```csharp
            new CharDef("堵", Element.Heart,   // 塞:免疫 1 次
                effects: new[] { new EffectDef(EffectKind.Immunity, 1) }),
            new CharDef("绝", Element.Heart,   // 杜:免疫 2 次
                effects: new[] { new EffectDef(EffectKind.Immunity, 2) }),
            new CharDef("峙", Element.Heart,   // 岿:免疫 1 次 + 立即净化
                effects: new[] { new EffectDef(EffectKind.Immunity, 1),
                                 new EffectDef(EffectKind.Cleanse, 0) }),
```

追加测试:

```csharp
        // ---- 免疫 ----

        [Test]
        public void Immunity_BlocksOneHitEntirely_NotPartially()
        {
            var engine = Engine(new[] { "堵" }, new[] { Dummy(attack: 20) });
            engine.Cast("堵", 0);
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(50), "整记 20 伤被完全挡下,不是减免");
            Assert.That(engine.PlayerStatuses.Has(StatusKind.Immunity), Is.False, "一次性,用完即消");
        }

        [Test]
        public void Immunity_ConsumedBeforeShield()
        {
            // 免疫是稀缺的一次性资源,让它去挡小伤而把护盾留着更亏。
            // 护盾必须原封不动地留到免疫用完之后。
            var engine = Engine(new[] { "堵" }, new[] { Dummy(attack: 8) });
            engine.Cast("堵", 0);
            int shieldBefore = engine.PlayerShield;
            engine.EndTurn();
            Assert.That(engine.PlayerShield, Is.EqualTo(shieldBefore), "护盾一点没掉");
            Assert.That(engine.PlayerHp, Is.EqualTo(50));
        }

        [Test]
        public void Immunity_TwoChargesBlockTwoHits()
        {
            var engine = Engine(new[] { "绝" }, new[] { Dummy(attack: 9) });
            engine.Cast("绝", 0);
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(50), "第 1 记挡下");
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.Immunity), Is.EqualTo(1));
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(50), "第 2 记也挡下");
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(41), "第 3 记吃满");
        }

        [Test]
        public void Immunity_SameCharRefreshes_DifferentCharsStack()
        {
            var engine = Engine(new[] { "堵", "堵", "绝" }, new[] { Dummy(attack: 5) });
            engine.Cast("堵", 0);
            engine.Cast("堵", 0);
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.Immunity), Is.EqualTo(1),
                "同字只刷新,不叠成 2");
            engine.Cast("绝", 0);
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.Immunity), Is.EqualTo(3),
                "不同字是不同来源,可叠:1 + 2");
        }

        [Test]
        public void KuiGrantsImmunityAndCleansesAtOnce()
        {
            var engine = Engine(new[] { "峙" }, new[] { Dummy(attack: 20) });
            engine.PlayerStatuses.Apply(new StatusEffect
            {
                Kind = StatusKind.Burn, Polarity = StatusPolarity.Debuff,
                Magnitude = 3, TurnsLeft = -1,
            });
            engine.Cast("峙", 0);
            Assert.That(engine.PlayerStatuses.Has(StatusKind.Burn), Is.False, "净化那一半");
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.Immunity), Is.EqualTo(1),
                "免疫那一半");
        }

        [Test]
        public void Immunity_DoesNotProtectSummons()
        {
            // 免疫是玩家的资源。召唤物替玩家承伤走 DamageSummon,不经 DamagePlayerDirect
            var engine = Engine(new[] { "堵", "素" }, new[] { Dummy(attack: 4) });
            engine.Cast("素");
            engine.Cast("堵", 0);
            engine.EndTurn();
            Assert.That(engine.Summons[0].Hp, Is.EqualTo(6), "召唤物照常挨打");
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.Immunity), Is.EqualTo(1),
                "免疫没被召唤物那记消耗掉");
        }
```

⚠ `engine.PlayerShield` 的属性名以仓库现状为准(`BattleView` 里读过它)。`Immunity_ConsumedBeforeShield` 那条若玩家默认无盾,先出一张护盾字或直接断言 `PlayerShield` 不变即可 —— 关键是证明免疫先消耗。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:编译失败,`StatusKind` / `EffectKind` 都没有 `Immunity`。

- [ ] **Step 3: 加枚举与 `RemoveEntry`**

`Core/StatusEffect.cs`,`StatusKind` 在 `Seal` 之后**追加**:

```csharp
        Seal,             // 封字:玩家下回合 AP −Magnitude(2026-08-06,Boss 倾覆)
        Immunity,         // 免疫:完全挡下 Magnitude 次伤害(2026-08-06)
```

`StatusBag` 加:

```csharp
        /// <summary>移除指定的那一条(按引用)。免疫消耗到 0 时用——袋子里可能有多条
        /// 同 Kind 不同来源的免疫,不能用按 Kind 的 Remove 一把全清。</summary>
        public void RemoveEntry(StatusEffect effect) => _list.Remove(effect);
```

`Core/EffectDef.cs`,`EffectKind` 在 `Cleanse` 之后**追加**:

```csharp
        Immunity,     // 免疫:完全挡下 Value 次伤害,先于护盾消耗(2026-08-06)
```

- [ ] **Step 4: 加两个 `BattleEventKind`**

`BattleEventKind` 末尾**追加**:

```csharp
        Immunity,        // 获得免疫(TargetIndex = −1,Amount = 次数;2026-08-06)
        ImmunityBlocked, // 免疫挡下一记(TargetIndex = 攻击者敌人下标,Amount = 挡掉的伤害;2026-08-06)
```

- [ ] **Step 5: 施加免疫**

`ApplyEffects` 的 `switch` 里,`case EffectKind.Cleanse:` 之后加:

```csharp
                    case EffectKind.Immunity:
                        // SourceId 用字 ID:同字再出只刷新,不无限叠层数;
                        // 不同字之间可叠(塞 1 + 杜 2 = 3 次),因为它们是不同来源
                        _playerStatuses.Apply(new StatusEffect
                        {
                            Kind = StatusKind.Immunity, Polarity = StatusPolarity.Buff,
                            Magnitude = value, TurnsLeft = -1, SourceId = def.Id,
                        });
                        _events.Add(new BattleEvent(BattleEventKind.Immunity, -1, value));
                        break;
```

- [ ] **Step 6: 消耗免疫**

`DamagePlayerDirect` 的**最开头**插入:

```csharp
            // 免疫(2026-08-06):先于护盾消耗 —— 免疫是稀缺的一次性资源,让它去挡一记小伤
            // 而把护盾留着更亏;玩家的预期是「免疫牌打出去,下一记不管多重都不疼」。
            // 完全挡下,不是减免。召唤物承伤走 DamageSummon,不经这里,所以免疫只保护玩家。
            if (ConsumeImmunity())
            {
                _events.Add(new BattleEvent(BattleEventKind.ImmunityBlocked, enemyIndex, damage));
                return;
            }
```

在 `DamagePlayerDirect` 下方加:

```csharp
        /// <summary>消耗一层免疫;成功返回 true。袋子里可能同时有多条(不同字来源可叠),
        /// 所以从第一条非零的扣 1,扣到 0 就移除那一条,而不是按 Kind 一把清。</summary>
        private bool ConsumeImmunity()
        {
            var all = _playerStatuses.All;
            for (int i = 0; i < all.Count; i++)
            {
                if (all[i].Kind != StatusKind.Immunity || all[i].Magnitude <= 0) continue;
                all[i].Magnitude -= 1;
                if (all[i].Magnitude <= 0) _playerStatuses.RemoveEntry(all[i]);
                return true;
            }
            return false;
        }
```

- [ ] **Step 7: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿。**特别确认既有的 `BossSkillTests` 没被打破** —— 无免疫时 `ConsumeImmunity()` 返回 false,`DamagePlayerDirect` 行为完全不变。

- [ ] **Step 8: 提交**

```bash
git add Brushblade/Assets/_Project/Core/StatusEffect.cs \
        Brushblade/Assets/_Project/Core/EffectDef.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/StatusOpsTests.cs
git commit -m "feat(core): 免疫——完全挡下一记,先于护盾消耗"
```

---

### Task 5: 斩杀

唯一做成 `EffectDef` 修饰字段的机制 —— 它本质上就是伤害的修饰(「这一刀对残血目标更重」)。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EffectDef.cs`(两个新字段 + 构造参数)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`ApplyEffects` 的 `DamageSingle` / `DamageAll` 两个 case;三个私有方法)
- Modify: `Brushblade/Assets/_Project/Tests/StatusOpsTests.cs`

**Interfaces:**
- Produces:`EffectDef.ExecuteBelowPercent`(int)、`EffectDef.ExecuteKills`(bool);构造函数尾部追加 `int executeBelowPercent = 0, bool executeKills = false`

- [ ] **Step 1: 写失败测试**

`Graph()` 里补三个字:

```csharp
            new CharDef("斩", Element.Heart,   // 铡:伤 20,HP<25% 且非 Boss → 直接击杀
                effects: new[] { new EffectDef(EffectKind.DamageSingle, 20,
                    executeBelowPercent: 25, executeKills: true) }),
            new CharDef("割", Element.Heart,   // 镰:伤 9,HP<30% → ×2
                effects: new[] { new EffectDef(EffectKind.DamageSingle, 9,
                    executeBelowPercent: 30) }),
            new CharDef("扫荡", Element.Heart, // 剿:全体伤 6,对 HP<30% 的目标 ×2
                effects: new[] { new EffectDef(EffectKind.DamageAll, 6,
                    executeBelowPercent: 30) }),
```

`Graph()` 里再补一个磨血用的字:

```csharp
            new CharDef("凿", Element.Heart,   // 1 点伤害,用来把敌人精确磨到目标血线
                effects: new[] { new EffectDef(EffectKind.DamageSingle, 1) }),
```

追加测试:

```csharp
        // ---- 斩杀 ----

        /// <summary>把首个敌人精确磨到 targetHp。`EnemyState.Hp` 的 setter 是 internal,
        /// 测试程序集改不到,所以用一张 1 点伤害的字磨——AP 开到够大,一回合内磨完,
        /// 期间敌人攻 0 不还手,不干扰血线。</summary>
        private static BattleEngine EngineWithEnemyAt(EnemyDef enemy, int targetHp, params string[] cards)
        {
            int hits = enemy.MaxHp - targetHp;
            var library = new List<string>(Enumerable.Repeat("凿", hits));
            library.AddRange(cards);
            var engine = new BattleEngine(Graph(),
                new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 200, ApPerTurn = hits + 20 },
                library, Array.Empty<string>(), new[] { enemy }, seed: 1);
            for (int i = 0; i < hits; i++) engine.Cast("凿", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(targetHp), "磨血辅助本身没磨准");
            return engine;
        }

        private static EnemyDef Target(int maxHp) => new("靶", Element.Heart, maxHp, 0);

        private static EnemyDef BossTarget(int maxHp) =>
            new("覆", Element.Heart, maxHp, 0,
                phases: new[] { new BossPhaseDef("覆", Element.Heart, maxHp, 0) });

        [Test]
        public void Execute_KillsNonBossBelowThreshold()
        {
            // 上限 100、现血 24 = 24% < 25% → 直接击杀
            var engine = EngineWithEnemyAt(Target(100), 24, "斩");
            engine.Cast("斩", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(0));
            Assert.That(engine.Phase, Is.EqualTo(BattlePhase.Won));
        }

        [Test]
        public void Execute_JudgesHpBeforeTheHit_NotAfter()
        {
            // 现血 26 = 26% ≥ 25%:打之前不到线,只吃普通 20 伤剩 6。
            // 若改成打之后判定,20 伤打完剩 6 = 6% 就会触发处决,结果完全不同。
            var engine = EngineWithEnemyAt(Target(100), 26, "斩");
            engine.Cast("斩", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(6), "普通结算,没被处决");
        }

        [Test]
        public void Execute_DoesNotKillBoss()
        {
            // 上限 200、现血 40 = 20% < 25%,但 Boss 免疫处决 → 只吃普通 20 伤剩 20。
            // 血线特意选得让「处决」与「普通伤害」的结果不同,否则测不出区别。
            var engine = EngineWithEnemyAt(BossTarget(200), 40, "斩");
            engine.Cast("斩", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(20), "Boss 退化成普通 20 伤");
            Assert.That(engine.Enemies[0].Alive, Is.True);
        }

        [Test]
        public void ExecuteBonus_DoesNotDoubleAboveThreshold()
        {
            // 现血 31 = 31% ≥ 30% → 普通 9 伤
            var engine = EngineWithEnemyAt(Target(100), 31, "割");
            engine.Cast("割", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(22));
        }

        [Test]
        public void ExecuteBonus_DoublesDamageBelowThreshold()
        {
            // 现血 29 = 29% < 30% → 9 × 2 = 18
            var engine = EngineWithEnemyAt(Target(100), 29, "割");
            engine.Cast("割", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(11));
        }

        [Test]
        public void ExecuteBonus_AppliesToBossToo()
        {
            // 免疫的只是「直接击杀」,不是「残血加伤」。现血 20/100 = 20% < 30% → 18 伤
            var engine = EngineWithEnemyAt(BossTarget(100), 20, "割");
            engine.Cast("割", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(2));
        }

        [Test]
        public void ExecuteBonus_TargetAll_JudgesEachEnemySeparately()
        {
            // 两只上限都 100:甲磨 80 下到 20(20% < 30% → 12 伤),
            // 乙磨 50 下到 50(50% ≥ 30% → 6 伤)。共需 130 张「凿」。
            var library = new List<string>(Enumerable.Repeat("凿", 130)) { "扫荡" };
            var engine = new BattleEngine(Graph(),
                new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 200, ApPerTurn = 200 },
                library, Array.Empty<string>(),
                new[] { Target(100), Target(100) }, seed: 1);
            for (int i = 0; i < 80; i++) engine.Cast("凿", 0);   // 甲 → 20
            for (int i = 0; i < 50; i++) engine.Cast("凿", 1);   // 乙 → 50
            engine.Cast("扫荡", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(8), "20 − 12");
            Assert.That(engine.Enemies[1].Hp, Is.EqualTo(44), "50 − 6");
        }
```

⚠ 磨血张数与 `ApPerTurn` 必须自洽:`EngineWithEnemyAt` 里那条
`Assert.That(..., "磨血辅助本身没磨准")` 是为此设的哨兵 —— 它一红就说明张数或 AP 算错了,
先查这里再怀疑斩杀实现。`Enumerable` / `List<>` 需要 `using System.Linq;` 与
`using System.Collections.Generic;`,文件顶部已有。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:编译失败,`EffectDef` 没有 `executeBelowPercent` 具名参数。

- [ ] **Step 3: `EffectDef` 加两个字段**

`Core/EffectDef.cs`,在 `IgnoreArmor` 之后加:

```csharp
        /// <summary>斩杀:目标 HP 百分比低于此值时触发(0 = 不启用)。**打之前**判血——
        /// 让玩家看着血条就能决定出哪张;打之后判定虽然更像补刀,但结果不可预期。</summary>
        public int ExecuteBelowPercent { get; }

        /// <summary>true = 命中阈值直接击杀(Boss 免疫);false = 命中阈值伤害 ×2(对 Boss 照常生效)。</summary>
        public bool ExecuteKills { get; }
```

构造函数尾部追加:

```csharp
            SummonPassive passive = null, int summonShield = 0,
            int executeBelowPercent = 0, bool executeKills = false)
        {
            // …既有赋值不动…
            ExecuteBelowPercent = executeBelowPercent;
            ExecuteKills = executeKills;
        }
```

- [ ] **Step 4: 三个私有方法**

`Core/BattleEngine.cs`,加在 `BaseValue` 旁边:

```csharp
        /// <summary>目标现血是否低于斩杀阈值。MaxHp 取 EnemyState.MaxHp(Boss 的**当前阶段上限**),
        /// 不是 Def.MaxHp —— 后者对分阶段 Boss 是错的。</summary>
        private bool BelowExecuteThreshold(EffectDef effect, int enemyIndex)
        {
            if (effect.ExecuteBelowPercent <= 0) return false;
            var enemy = _enemies[enemyIndex];
            return enemy.Alive && enemy.Hp * 100 < enemy.MaxHp * effect.ExecuteBelowPercent;
        }

        /// <summary>处决:命中阈值且非 Boss 则直接击杀,返回 true(调用方不要再走伤害)。
        /// Boss 是一条总血池,25% 也是很大一截,一刀没掉太破坏节奏,故免疫。</summary>
        private bool TryExecuteKill(EffectDef effect, int enemyIndex)
        {
            if (!effect.ExecuteKills || !BelowExecuteThreshold(effect, enemyIndex)) return false;
            var enemy = _enemies[enemyIndex];
            if (enemy.IsBoss) return false;
            int lost = enemy.Hp;              // 报实际抹掉的血量,别报 0 —— 0 会让表现层飘「-0」
            enemy.Hp = 0;
            _events.Add(new BattleEvent(BattleEventKind.Damage, enemyIndex, lost));
            ResolveDefeat(enemyIndex);
            return true;
        }

        /// <summary>残血加伤:命中阈值则该次基础值 ×2。**对 Boss 照常生效** ——
        /// 免疫的只是「直接击杀」,不是「残血加伤」。</summary>
        private int ExecuteBonus(EffectDef effect, int enemyIndex, int baseValue) =>
            !effect.ExecuteKills && BelowExecuteThreshold(effect, enemyIndex) ? baseValue * 2 : baseValue;
```

- [ ] **Step 5: 接进两个伤害 case**

`ApplyEffects` 里,把

```csharp
                    case EffectKind.DamageSingle:
                        DamageEnemy(targetIndex, BaseValue(effect, value, _enemies[targetIndex]), recipeElements, attacker, effect.IgnoreArmor);
                        break;
```

改成

```csharp
                    case EffectKind.DamageSingle:
                        if (TryExecuteKill(effect, targetIndex)) break; // 处决:直接击杀,不再走伤害
                        DamageEnemy(targetIndex,
                            ExecuteBonus(effect, targetIndex, BaseValue(effect, value, _enemies[targetIndex])),
                            recipeElements, attacker, effect.IgnoreArmor);
                        break;
```

把

```csharp
                    case EffectKind.DamageAll:
                        int aoeCount = _enemies.Count; // 分裂产生的新怪不吃同一发 AOE
                        for (int i = 0; i < aoeCount; i++)
                            if (_enemies[i].Alive)
                                DamageEnemy(i, BaseValue(effect, value, _enemies[i]), recipeElements, attacker, effect.IgnoreArmor);
                        break;
```

改成

```csharp
                    case EffectKind.DamageAll:
                        int aoeCount = _enemies.Count; // 分裂产生的新怪不吃同一发 AOE
                        for (int i = 0; i < aoeCount; i++)
                        {
                            if (!_enemies[i].Alive) continue;
                            if (TryExecuteKill(effect, i)) continue; // 斩杀对每个目标分别判定
                            DamageEnemy(i,
                                ExecuteBonus(effect, i, BaseValue(effect, value, _enemies[i])),
                                recipeElements, attacker, effect.IgnoreArmor);
                        }
                        break;
```

- [ ] **Step 6: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿。既有伤害测试不受影响(`ExecuteBelowPercent` 默认 0 → `BelowExecuteThreshold` 直接返回 false)。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/EffectDef.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/StatusOpsTests.cs
git commit -m "feat(core): 斩杀——打之前判血,处决对 Boss 免疫但残血加伤照常"
```

---

### Task 6: 复活

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EffectDef.cs`(`EffectKind` 加 `Revive`)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`ApplyEffects` 一个 case;私有方法 `FirstDeadSummonIndex`)
- Modify: `Brushblade/Assets/_Project/Tests/StatusOpsTests.cs`

**Interfaces:**
- Produces:`EffectKind.Revive`(`Value` = 复活几只)

- [ ] **Step 1: 写失败测试**

`Graph()` 里补一个字:

```csharp
            new CharDef("苏", Element.Heart,   // 活:复活一名阵亡召唤物
                effects: new[] { new EffectDef(EffectKind.Revive, 1) }),
```

追加测试:

```csharp
        // ---- 复活 ----

        [Test]
        public void Revive_RestoresDeadSummonToHalfHp()
        {
            var engine = Engine(new[] { "素", "苏" }, new[] { Dummy(attack: 40) });
            engine.Cast("素");
            engine.EndTurn();                         // 10 血召唤物挨 40,死透
            Assert.That(engine.Summons[0].Alive, Is.False);

            engine.Cast("苏", 0);
            Assert.That(engine.Summons[0].Alive, Is.True);
            Assert.That(engine.Summons[0].Hp, Is.EqualTo(5), "半血,向上取整");
            Assert.That(engine.Summons[0].ActionMeter, Is.EqualTo(0), "重新攒节拍,不继承死前余额");
            Assert.That(engine.Summons[0].Shield, Is.EqualTo(0), "盾不跟着复活");
        }

        [Test]
        public void Revive_KeepsPassiveIdentity()
        {
            // Passive 是这只召唤物的身份,复活后必须还在
            var graph = new RecipeGraph(new[]
            {
                new CharDef("木", Element.Wood),
                new CharDef("棘", Element.Wood,
                    effects: new[] { new EffectDef(EffectKind.Summon, 10, summonCount: 1, summonAttack: 0,
                        summonChar: "木", passive: new SummonPassive { Thorns = 3 }) }),
                new CharDef("苏", Element.Heart,
                    effects: new[] { new EffectDef(EffectKind.Revive, 1) }),
            });
            var engine = new BattleEngine(graph,
                new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 50 },
                new[] { "棘", "苏" }, Array.Empty<string>(),
                new[] { Dummy(attack: 40) }, seed: 1);
            engine.Cast("棘");
            engine.EndTurn();
            engine.Cast("苏", 0);
            Assert.That(engine.Summons[0].Passive, Is.Not.Null);
            Assert.That(engine.Summons[0].Passive.Thorns, Is.EqualTo(3));
        }

        [Test]
        public void Revive_WithNoDeadSummon_IsANoOp()
        {
            // 与「无敌人时出 AOE」同口径:消耗 AP 但无效果,不抛异常
            var engine = Engine(new[] { "苏" }, new[] { Dummy() });
            Assert.That(engine.Cast("苏", 0), Is.EqualTo(BattleError.None));
            Assert.That(engine.Summons, Is.Empty);
        }

        [Test]
        public void Revive_RefusesWhenSummonCapIsFull()
        {
            // 死尸占着槽位,复活不新增条目但存活数 +1 —— 满员时必须停手,否则会变成 7 只。
            // 构造:召满 6 只 → 敌人打死最前一只(场上多一具尸体)→ 补召 1 只回到 6 只存活
            // → 此时出「苏」,尸体还在但已满员。
            var graph = new RecipeGraph(new[]
            {
                new CharDef("木", Element.Wood),
                new CharDef("丛", Element.Wood,   // 一次召 6 只,正好塞满上限
                    effects: new[] { new EffectDef(EffectKind.Summon, 4, summonCount: 6, summonChar: "木") }),
                new CharDef("苗", Element.Wood,   // 补位用的单召
                    effects: new[] { new EffectDef(EffectKind.Summon, 4, summonCount: 1, summonChar: "木") }),
                new CharDef("苏", Element.Heart,
                    effects: new[] { new EffectDef(EffectKind.Revive, 1) }),
            });
            var engine = new BattleEngine(graph,
                new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 200, ApPerTurn = 10 },
                new[] { "丛", "苗", "苏" }, Array.Empty<string>(),
                new[] { new EnemyDef("靶", Element.Heart, 500, 4) }, seed: 1);

            engine.Cast("丛");
            Assert.That(engine.AliveSummonCount, Is.EqualTo(6));
            engine.EndTurn();                      // 敌人攻 4 打死最前一只(4 血)
            Assert.That(engine.AliveSummonCount, Is.EqualTo(5));
            engine.Cast("苗");                      // 补回 6 只存活,场上留着那具尸体
            Assert.That(engine.AliveSummonCount, Is.EqualTo(6));

            engine.Cast("苏", 0);
            Assert.That(engine.AliveSummonCount, Is.EqualTo(6),
                "满员时复活必须停手,不许变成 7 只");
        }
```

⚠ `engine.AliveSummonCount` 与 `engine.SummonCapacity` 都是 `BattleEngine` 上已有的公开属性。
`Cast("丛")` 不传目标下标是因为召唤不需要目标 —— 照抄既有召唤测试的调用方式。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:编译失败,`EffectKind` 没有 `Revive`。

- [ ] **Step 3: 加 `EffectKind.Revive`**

`Core/EffectDef.cs`,在 `Immunity` 之后**追加**:

```csharp
        Revive,       // 复活:救回 Value 名阵亡召唤物,各回半血(2026-08-06)
```

- [ ] **Step 4: 实现**

`ApplyEffects` 的 `switch` 里,`case EffectKind.Immunity:` 之后加:

```csharp
                    case EffectKind.Revive:
                        for (int n = 0; n < value; n++)
                        {
                            // 死尸占着槽位,复活不新增条目但存活数 +1 —— 满员时停手,免得超上限
                            if (AliveSummons() >= SummonCap) break;
                            int slot = FirstDeadSummonIndex();
                            if (slot < 0) break; // 没有阵亡召唤物 → 空放(与无敌人时出 AOE 同口径)
                            var revived = _summons[slot];
                            revived.Hp = (revived.MaxHp + 1) / 2; // 半血,向上取整
                            revived.ActionMeter = 0;              // 重新攒节拍,不继承死前余额
                            revived.Shield = 0;                   // 盾不跟着复活
                            // Passive 是只读属性,天然保留 —— 它是这只召唤物的身份
                            _events.Add(new BattleEvent(BattleEventKind.Summon, -1, revived.Hp, slot));
                        }
                        break;
```

在 `FirstAliveSummonIndex` 旁边加:

```csharp
        /// <summary>第一具尸体的槽位;没有返回 −1。引擎从不移除阵亡召唤物
        /// (表现层只是不画它们),所以复活直接就地救回。</summary>
        private int FirstDeadSummonIndex()
        {
            for (int s = 0; s < _summons.Count; s++)
                if (!_summons[s].Alive) return s;
            return -1;
        }
```

- [ ] **Step 5: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿。

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Core/EffectDef.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/StatusOpsTests.cs
git commit -m "feat(core): 复活——就地救回阵亡召唤物,半血、重攒节拍、保留被动"
```

---

### Task 7: `ConfigLoader` 与新字怪「灯花」

**Files:**
- Modify: `Brushblade/Assets/_Project/Data/ConfigLoader.cs`(`EffectDto` 加两个斩杀字段;`ParseEffects` 传下去)
- Modify: `Brushblade/Assets/StreamingAssets/config/enemies.json`(**手写文件**,直接改)
- Modify: `Brushblade/Assets/_Project/Tests/ConfigLoaderTests.cs`
- Modify: `Brushblade/Assets/_Project/Tests/CharTableTests.cs`

**Interfaces:**
- Consumes:Task 5 的 `EffectDef` 构造参数 `int executeBelowPercent = 0, bool executeKills = false`
- Produces:`chars.json` 支持 `"executeBelowPercent": 25, "executeKills": true`;`enemies.json` 里的新字怪 `灯花`

**注意**:四个新 `EffectKind` 与 `EnemyAbility.Sear` 都走 `Enum.TryParse`,**不需要改解析代码**,自动认识。只有 `EffectDef` 的两个修饰字段要手动接线。

- [ ] **Step 1: 写失败测试**

追加到 `Brushblade/Assets/_Project/Tests/ConfigLoaderTests.cs`(照抄该文件既有的 `ConfigLoader.LoadGraph(json)` 调用风格):

```csharp
        [Test]
        public void LoadGraph_ParsesExecuteFieldsAndNewEffectKinds()
        {
            const string json = @"{""chars"":[
                {""id"":""木"",""element"":""Wood""},
                {""id"":""甲"",""element"":""Metal"",""effects"":[
                    {""kind"":""DamageSingle"",""value"":20,
                     ""executeBelowPercent"":25,""executeKills"":true}]},
                {""id"":""乙"",""element"":""Metal"",""effects"":[
                    {""kind"":""DamageSingle"",""value"":9,""executeBelowPercent"":30}]},
                {""id"":""丙"",""element"":""Fire"",""effects"":[{""kind"":""Dispel"",""value"":-1}]},
                {""id"":""丁"",""element"":""Water"",""effects"":[
                    {""kind"":""DamageAll"",""value"":20},
                    {""kind"":""Dispel"",""value"":1,""targetAll"":true}]},
                {""id"":""戊"",""element"":""Water"",""effects"":[{""kind"":""Cleanse"",""value"":0}]},
                {""id"":""己"",""element"":""Earth"",""effects"":[{""kind"":""Immunity"",""value"":2}]},
                {""id"":""庚"",""element"":""Water"",""effects"":[{""kind"":""Revive"",""value"":1}]}
            ]}";
            var graph = ConfigLoader.LoadGraph(json);

            var jia = graph.Get("甲").Effects[0];
            Assert.That(jia.ExecuteBelowPercent, Is.EqualTo(25));
            Assert.That(jia.ExecuteKills, Is.True);

            var yi = graph.Get("乙").Effects[0];
            Assert.That(yi.ExecuteBelowPercent, Is.EqualTo(30));
            Assert.That(yi.ExecuteKills, Is.False, "没写就是残血加伤,不是处决");

            Assert.That(graph.Get("丙").Effects[0].Kind, Is.EqualTo(EffectKind.Dispel));
            Assert.That(graph.Get("丙").Effects[0].Value, Is.EqualTo(-1));

            var ding = graph.Get("丁").Effects[1];
            Assert.That(ding.Kind, Is.EqualTo(EffectKind.Dispel));
            Assert.That(ding.TargetAll, Is.True);

            Assert.That(graph.Get("戊").Effects[0].Kind, Is.EqualTo(EffectKind.Cleanse));
            Assert.That(graph.Get("己").Effects[0].Kind, Is.EqualTo(EffectKind.Immunity));
            Assert.That(graph.Get("己").Effects[0].Value, Is.EqualTo(2));
            Assert.That(graph.Get("庚").Effects[0].Kind, Is.EqualTo(EffectKind.Revive));
        }
```

⚠ `ConfigLoaderTests.cs` 被 `tools/coretests` 工装**排除**(它引 `UnityEngine.Application`),这条测试**工装跑不到**,只能由用户在 Unity Test Runner 里跑。请在报告里明确写出这一点。你要确认的是**工装编译通过**(说明类型接线对得上)。

追加到 `Brushblade/Assets/_Project/Tests/CharTableTests.cs`(这条工装跑得到,走真实 `enemies.json`):

```csharp
        [Test]
        public void RealConfig_DengHuaCarriesSearAbility()
        {
            // ability 若没从 JSON 传到 EnemyDef,灯花照常能打但不会给玩家挂灼烧,
            // 净化与免疫这批字就全成了死牌
            var campaign = ConfigLoader.LoadCampaign(
                File.ReadAllText(Path.Combine(RepoRoot(),
                    "Brushblade/Assets/StreamingAssets/config/enemies.json")), RealGraph());
            var dengHua = campaign.Endless.Bands
                .SelectMany(b => b.EnemyPool)
                .FirstOrDefault(e => e.Id == "灯花");
            Assert.That(dengHua, Is.Not.Null, "灯花应出现在层段的敌人池里");
            Assert.That(dengHua.Ability, Is.EqualTo(EnemyAbility.Sear));
        }
```

⚠ `RepoRoot()` / `RealGraph()` / `campaign.Endless.Bands[i].EnemyPool` 的确切名字与类型以 `CharTableTests.cs` 和 `StartingSetupTests.cs` 里的既有用法为准 —— 照抄它们。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:`RealConfig_DengHuaCarriesSearAbility` FAIL(灯花还不存在)。

- [ ] **Step 3: `EffectDto` 加两个字段并接线**

`Brushblade/Assets/_Project/Data/ConfigLoader.cs`,`EffectDto` 的 `SummonShield` 之后加:

```csharp
            public int ExecuteBelowPercent { get; set; } // 斩杀:目标 HP 低于此百分比时触发
            public bool ExecuteKills { get; set; }       // true = 直接击杀(Boss 免疫);false = 伤害 ×2
```

`ParseEffects` 里把构造调用改成:

```csharp
                effects.Add(new EffectDef(kind, effect.Value,
                    effect.DoubleVsBurning, effect.PersistOnce,
                    effect.Count, effect.Attack, effect.SummonChar,
                    effect.Turns, effect.TargetAll, effect.IgnoreArmor,
                    effect.Passive, effect.SummonShield,
                    effect.ExecuteBelowPercent, effect.ExecuteKills));
```

- [ ] **Step 4: 加新字怪「灯花」**

`Brushblade/Assets/StreamingAssets/config/enemies.json`(**这是手写文件,直接改**)。

在 `enemies` 数组里、`焦痕` 那条之后加:

```json
    {"id": "灯花", "element": "Fire", "maxHp": 12, "attack": 3, "ability": "Sear"},
```

然后把 `"灯花"` 加进 `endless.bands` 里 **`词渊`、`文山`、`墨海`** 三段的 `enemyPool` 末尾。
**`字林`(第一段)不加** —— 与 `焦痕` 同待遇,首塔引导段保持简单。

命名取自古人夜读时灯芯爆出的火星子,与既有字怪「错字鬼 / 缺笔妖 / 标点小妖 / 叠字怪 /
夯土妖 / 通假字 / 生僻字 / 墨渍 / 焦痕」这套「书写病症」的规律一致。

- [ ] **Step 5: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿。

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Data/ConfigLoader.cs \
        Brushblade/Assets/StreamingAssets/config/enemies.json \
        Brushblade/Assets/_Project/Tests/ConfigLoaderTests.cs \
        Brushblade/Assets/_Project/Tests/CharTableTests.cs
git commit -m "feat(data): ConfigLoader 解析斩杀字段;新字怪灯花入池"
```

---

### Task 8: 详表落地十四字,重新生成字表与字体

**Files:**
- Modify: `docs/design/字选型/技能机制详表.md`(14 行 + §7.2 三行 + §7.4 名单)
- Modify: `tools/pipeline/extract_values.py`
- Modify: `tools/pipeline/export_chars.py`
- Modify: `tools/pipeline/tests/test_export_chars.py`
- Modify: `Brushblade/Assets/_Project/Tests/CharTableTests.cs`
- Regenerate: `Brushblade/Assets/StreamingAssets/config/chars.json`、`Brushblade/Assets/_Project/Presentation/Fonts/Resources/*.ttf`、`tools/fonts/charset.txt`

**Interfaces:**
- Consumes:Task 7 的 JSON 字段名 `executeBelowPercent` / `executeKills`
- Produces:`chars.json` 多出 14 个字与 10 个叶子部件

**背景**:`chars.json` 是生成物。管线从详表「效果配置」列用正则抽数值,通用正则是
`` `(\w+) (\d+)` `` —— **认不出负数,也认不出无数值的标记**。本任务扩它。

- [ ] **Step 1: 写失败测试(管线侧)**

追加到 `tools/pipeline/tests/test_export_chars.py`:

```python
def test_valueless_effect_tokens():
    """`Cleanse` 与 `DispelAll` 是无数值标记,通用正则抓不到,要单独认。"""
    from extract_values import _parse_effects
    assert _parse_effects("`Cleanse`", "水") == [{"kind": "Cleanse", "value": 0}]
    assert _parse_effects("`DispelAll`", "火") == [{"kind": "Dispel", "value": -1}]


def test_dispel_each_becomes_target_all():
    from extract_values import _parse_effects
    effects = _parse_effects("`DamageAll 20` + `DispelEach 1`", "水")
    assert effects[0] == {"kind": "DamageAll", "value": 20}
    assert effects[1] == {"kind": "Dispel", "value": 1, "targetAll": True}


def test_execute_tokens_attach_to_damage_not_become_effects():
    """斩杀是伤害的修饰,不该变成独立效果。"""
    from extract_values import _parse_effects
    kill = _parse_effects("`DamageSingle 20` + `ExecuteKill 25`", "金")
    assert kill == [{"kind": "DamageSingle", "value": 20,
                     "executeBelowPercent": 25, "executeKills": True}]
    bonus = _parse_effects("`DamageSingle 9` + `ExecuteBonus 30`", "金")
    assert bonus == [{"kind": "DamageSingle", "value": 9,
                      "executeBelowPercent": 30, "executeKills": False}]


def test_dispel_all_marker_does_not_swallow_counted_dispel():
    """`Dispel 1` 里不含 `DispelAll` 这个带反引号的整词,别误判。"""
    from extract_values import _parse_effects
    effects = _parse_effects("`DamageSingle 9` + `Dispel 1`", "金")
    assert effects == [{"kind": "DamageSingle", "value": 9}, {"kind": "Dispel", "value": 1}]


def test_manual_recipes_avoid_smp_and_rare_parts():
    """塞 的 IDS 部件 𡨄 是增补平面(会让整字降级成叶子);湮 的 垔 生僻。"""
    from export_chars import MANUAL_RECIPES
    assert MANUAL_RECIPES["塞"] == ["宀", "土"]
    assert MANUAL_RECIPES["湮"] == ["氵", "土"]
```

- [ ] **Step 2: 跑测试确认失败**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
```

预期:5 条新测试 FAIL。

- [ ] **Step 3: 扩 `extract_values`**

`tools/pipeline/extract_values.py`,在 `SUMMON_PASSIVE` 常量旁边加:

```python
# 无数值的效果 token(布尔标记式):通用正则 `(\w+) (\d+)` 抓不到,单独认。
# 顺带绕开负数——`Dispel -1` 那个负号通用正则也认不出。
VALUELESS_EFFECTS = {
    "Cleanse": {"kind": "Cleanse", "value": 0},
    "DispelAll": {"kind": "Dispel", "value": -1},
}

# 斩杀是**伤害的修饰**,不是独立效果:抽出来挂到同一行的伤害效果上。
# 值 = executeKills(True = 直接击杀,False = 残血加伤 ×2)
EXECUTE_TOKENS = {"ExecuteKill": True, "ExecuteBonus": False}
```

`_parse_effects` 里,通用循环改成:

```python
    effects = []
    for kind, value in re.findall(r"`(\w+) (\d+)`", config):
        if kind in EXECUTE_TOKENS:
            continue  # 修饰而非效果,下面统一挂到伤害上
        effect = {"kind": kind, "value": int(value)}
        if kind == "DispelEach":       # 全体各驱散 N 条(淡)
            effect["kind"] = "Dispel"
            effect["targetAll"] = True
        if kind.startswith("Damage") and "DoubleVsBurning" in config:
            effect["doubleVsBurning"] = True
        if kind.startswith("Damage") and "ignoreArmor" in config:
            effect["ignoreArmor"] = True
        if kind == "Shield" and "PersistOnce" in config:
            effect["persistOnce"] = True
        effects.append(effect)

    for token, spec in VALUELESS_EFFECTS.items():
        if f"`{token}`" in config:
            effects.append(dict(spec))

    for token, kills in EXECUTE_TOKENS.items():
        found = re.search(rf"`{token} (\d+)`", config)
        if not found:
            continue
        for effect in effects:
            if effect["kind"].startswith("Damage"):
                effect["executeBelowPercent"] = int(found.group(1))
                effect["executeKills"] = kills
```

既有的 `turns` / `targetAll`(HealOverTime 用)那段保持不动。

- [ ] **Step 4: `export_chars` 补两条人工配方**

`tools/pipeline/export_chars.py` 的 `MANUAL_RECIPES` 里加:

```python
MANUAL_RECIPES = {
    "荆": ["艹", "刂"],
    # 塞 的 IDS 是 ⿱𡨄土,𡨄(U+21A04)在增补平面 —— _blocked_smp_part 会因此把整个字
    # 降级成不可拆的叶子(只能靠掉落获得)。换成常见部首 宀 直接绕开。
    "塞": ["宀", "土"],
    # 湮 的 IDS 是 ⿰氵垔,垔 生僻;它的核心就是土,义通(水土掩埋),且零新部件。
    "湮": ["氵", "土"],
}
```

- [ ] **Step 5: 跑管线测试确认通过**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
```

预期:全绿。

- [ ] **Step 6: 改详表十四行**

`docs/design/字选型/技能机制详表.md`。**只改这 14 行的「效果配置」列与「实现」列**,数值一个不动。

火系(灭 在「攻 · 单攻」或它现在所在的表里,原地改):

```
| 灭 | 🟢绿 | `DispelAll` | 清空单体全部增益 | ✅ 唯一解:标点加攻与焦痕自燃都是段内持久且累计 |
```

水系:

```
| 淡 | 🟡金 | `DamageAll 20` + `DispelEach 1` | 全体 20 + 各削 1 层 | ✅ |
| 湮 | 🟣紫 | `DamageSingle 20` + `DispelAll` | 单体 20 + 清空增益 | ✅ |
| 澡 | 🟣紫 | **单体即时+净化** | `HealSelf 12` + `Cleanse` | 治疗 12 + 净化 | ✅ |
| 浴 | 🟢绿 | **净化** | `Cleanse` | — | ✅ 纯功能牌 |
| 活 | 🔵蓝 | **复活** | `Revive 1` | — | ✅ 半血救回,保留被动 |
```

⚠ 水系「治 · …」那张表的列数与其余表不同(多一列),改的时候**照抄同表其它行的列结构**,别把列数改乱 —— `_parse_row` 靠 `len(cells) < 4` 与 `len(cells[0]) != 1` 判行,列错了整行会被静默跳过。

金系:

```
| 铡 | 🟣紫 | `DamageSingle 20` + `ExecuteKill 25` | 单体 20,残血处决 | ✅ Boss 免疫处决 |
| 镰 | 🟢绿 | `DamageSingle 9` + `ExecuteBonus 30` | 单体 9,残血 ×2 | ✅ 与剿靠单体/全体分开 |
| 剿 | 🟢绿 | `DamageAll 6` + `ExecuteBonus 30` | 全体 6,残血 ×2 | ✅ 剿灭一群 |
| 削 | 🟢绿 | `DamageSingle 9` + `Dispel 1` | 单体 9 + 削 1 层 | ✅ 与刮靠「削一条 / 刮干净」分开 |
| 刮 | 🟢绿 | `DamageSingle 6` + `DispelAll` | 单体 6 + 清空增益 | ✅ 刮得干净,打得轻 |
| 杜 | 🟣紫 | `Immunity 2` | 免疫 2 次伤害 | ✅ |
```

土系:

```
| 岿 | 🔵蓝 | `Immunity 1` + `Cleanse` | 免疫 1 次 + 净化 | ✅ 岿然不动:稳如山,顺手抖掉杂七杂八 |
| 塞 | 🟢绿 | `Immunity 1` | 免疫 1 次伤害 | ✅ |
```

- [ ] **Step 7: 更新 §7.2 与 §7.4**

§7.2 汇总表里,把 `Dispel` / `Immunity` / `Execute` / `Cleanse`/`Revive` 四行标成已实现:

```
| ~~P1~~ ✅ | `Dispel` | 5 | 灭 湮 淡 削 刮 —— 已实现(2026-08-06) |
| ~~P1~~ ✅ | `Immunity` | 3 | 杜 岿 塞 —— 已实现(2026-08-06) |
| ~~P1~~ ✅ | `Execute` | 3 | 铡 镰 剿 —— 已实现(2026-08-06;烬 早已用 DoubleVsBurning 落地,不在此列) |
| ~~P2~~ ✅ | `Cleanse` / `Revive` | 3 | 澡 浴 活 —— 已实现(2026-08-06) |
```

§7.4「建议暂缓入池」的名单里,**移除 灭 / 削 / 刮 / 镰 / 剿 五字**,并在该节的说明段补一句:

```
2026-08-06:`Dispel`/`Execute` 落地后,灭·削·刮(驱散)与 镰·剿(斩杀)已用
「清一条 / 刮干净」「单体 / 全体」区分开,撞车前提消除,从名单移除。
```

- [ ] **Step 8: 重新生成 `chars.json`**

```bash
cd tools/pipeline && python3 export_chars.py
```

**核对改动范围**。应当只多出 24 条(14 个字 + 10 个叶子部件 `则 廉 肖 舌 巢 谷 归 宀 一 喿`),总条数 188 → 212。若还有别的字被动了,**停下来查原因** —— 详表改动波及了不该波及的行。

```bash
python3 -c "
import json
d=json.load(open('Brushblade/Assets/StreamingAssets/config/chars.json'))
want=set('灭湮淡削刮杜岿塞铡镰剿澡浴活则廉肖舌巢谷归宀一喿')
for c in d['chars']:
    if c['id'] in want: print(json.dumps(c,ensure_ascii=False))
print('total', len(d['chars']))
"
```

逐条对照本计划 Step 6 的详表行:稀有度、属性、配方、每个效果的 kind/value/修饰字段。

- [ ] **Step 9: 重新生成字体子集**

```bash
cd tools/fonts && python3 subset_fonts.py
```

⚠ `subset_fonts.py` 用裸正则扫 `"..."` 形状取字符,**分不清代码与注释** —— 前面任务写的中文注释里若有字符串字面量,也会被吸进字集。`charset.txt` 的 diff 比预期大是正常的。字体产物的真实输出路径以脚本的 `OUT_DIR` 为准。

- [ ] **Step 10: 补真实配置的接线守卫**

追加到 `Brushblade/Assets/_Project/Tests/CharTableTests.cs`:

```csharp
        [Test]
        public void RealConfig_ExecuteChars_CarryTheirThresholds()
        {
            var graph = RealGraph();
            var zha = graph.Get("铡").Effects.First(e => e.Kind == EffectKind.DamageSingle);
            Assert.That(zha.ExecuteBelowPercent, Is.EqualTo(25));
            Assert.That(zha.ExecuteKills, Is.True);

            var lian = graph.Get("镰").Effects.First(e => e.Kind == EffectKind.DamageSingle);
            Assert.That(lian.ExecuteBelowPercent, Is.EqualTo(30));
            Assert.That(lian.ExecuteKills, Is.False, "残血加伤,不是处决");

            var jiao = graph.Get("剿").Effects.First(e => e.Kind == EffectKind.DamageAll);
            Assert.That(jiao.ExecuteBelowPercent, Is.EqualTo(30));
        }

        [Test]
        public void RealConfig_DispelChars_CarryTheirCounts()
        {
            var graph = RealGraph();
            Assert.That(graph.Get("灭").Effects.First(e => e.Kind == EffectKind.Dispel).Value,
                Is.EqualTo(-1), "灭清全部");
            Assert.That(graph.Get("削").Effects.First(e => e.Kind == EffectKind.Dispel).Value,
                Is.EqualTo(1), "削只清一条");
            Assert.That(graph.Get("刮").Effects.First(e => e.Kind == EffectKind.Dispel).Value,
                Is.EqualTo(-1), "刮清全部");
            var dan = graph.Get("淡").Effects.First(e => e.Kind == EffectKind.Dispel);
            Assert.That(dan.TargetAll, Is.True, "淡是全体各清一条");
            Assert.That(dan.Value, Is.EqualTo(1));
        }

        [Test]
        public void RealConfig_ImmunityAndCleanseAndRevive()
        {
            var graph = RealGraph();
            Assert.That(graph.Get("杜").Effects.First(e => e.Kind == EffectKind.Immunity).Value,
                Is.EqualTo(2));
            Assert.That(graph.Get("塞").Effects.First(e => e.Kind == EffectKind.Immunity).Value,
                Is.EqualTo(1));
            // 岿 = 免疫 1 次 + 立即净化,两半都要在
            var kui = graph.Get("岿").Effects;
            Assert.That(kui.Any(e => e.Kind == EffectKind.Immunity), Is.True);
            Assert.That(kui.Any(e => e.Kind == EffectKind.Cleanse), Is.True);
            Assert.That(graph.Get("浴").Effects.Any(e => e.Kind == EffectKind.Cleanse), Is.True);
            Assert.That(graph.Get("活").Effects.First(e => e.Kind == EffectKind.Revive).Value,
                Is.EqualTo(1));
        }

        [Test]
        public void RealConfig_SaiUsesManualRecipe_NotSupplementaryPlanePart()
        {
            // 塞 的 IDS 部件 𡨄 在增补平面,UGUI Text 显示不出代理对 ——
            // 走 IDS 会让塞退化成不可拆的叶子(只能靠掉落获得)
            var graph = RealGraph();
            Assert.That(graph.Get("塞").Recipe, Is.EqualTo(new[] { "宀", "土" }));
            Assert.That(graph.Get("湮").Recipe, Is.EqualTo(new[] { "氵", "土" }));
        }
```

- [ ] **Step 11: 全套测试**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:两套全绿。

- [ ] **Step 12: 提交**

```bash
git add docs/design/字选型/技能机制详表.md \
        tools/pipeline/extract_values.py \
        tools/pipeline/export_chars.py \
        tools/pipeline/tests/test_export_chars.py \
        Brushblade/Assets/_Project/Tests/CharTableTests.cs \
        Brushblade/Assets/StreamingAssets/config/chars.json \
        Brushblade/Assets/_Project/Presentation/Fonts/Resources/NotoSansSC-Subset.ttf \
        Brushblade/Assets/_Project/Presentation/Fonts/Resources/NotoSerifSC-Subset.ttf \
        tools/fonts/charset.txt
git commit -m "feat(data): 状态操作族 14 字落地——109 → 123 字可用"
```

---

### Task 9: 表现层

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/UI/EnemyInfo.cs`(`AbilityName` / `AbilityText` / `AbilityChipText` 三处)
- Modify: `Brushblade/Assets/_Project/Presentation/UI/Theme.cs`(`AbilityChipColor`)
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`(玩家状态条;免疫挡下反馈)
- Modify: `docs/design/字选型/技能机制详表.md`(§7.5 落地顺序补实现说明)

**Interfaces:**
- Consumes:`EnemyAbility.Sear`、`StatusKind.Seal` / `StatusKind.Immunity` / `StatusKind.Burn`、`BattleEventKind.ImmunityBlocked` / `Dispel` / `Cleanse` / `Immunity`

- [ ] **Step 1: `Sear` 的三处文案**

`Presentation/UI/EnemyInfo.cs`,三个 `switch` 各加一行(照抄 `Scorch` 那行的位置与风格):

```csharp
        // AbilityName 里
        EnemyAbility.Sear => "灼身",

        // AbilityText 里
        EnemyAbility.Sear => "每次攻击给你挂 1 层灼烧 —— 拖得越久烧得越久",

        // AbilityChipText 里
        EnemyAbility.Sear => "灼身",
```

`Presentation/UI/Theme.cs` 的 `AbilityChipColor` 里加:

```csharp
            EnemyAbility.Sear => Cinnabar, // 朱砂:与焦痕同为火系威胁
```

⚠ 这一步是「技能注入完整性」的三侧之一。本仓库有过只做一半的教训 —— `EnemyAbility` 的新值在
`EnemyInfo` 有三处、`Theme` 有一处、`MobAssets.StateAmountFor` 有一处。`MobAssets` 那处**不用改**
(`Sear` 没有可视化的进度状态,落到 `_ => 0f` 即可),但你要打开确认它有兜底分支而不是会抛。

- [ ] **Step 2: 玩家状态条**

`Presentation/BattleView.cs` 的 `DrawPlayerStats()`,在护盾条那一块之后加:

```csharp
            // 玩家侧减益/增益一行小字(2026-08-06):封字 / 灼烧 / 免疫。
            // 禁用 emoji —— 字体子集补不出来,上线渲染成空框
            var statusRow = Ui.Row(hpStack.transform, "PlayerStatus", 6);
            int seal = Battle.PlayerStatuses.TotalMagnitude(StatusKind.Seal);
            if (seal > 0)
                Ui.Chip(statusRow.transform, $"封字 −{seal}AP", Theme.InkSoft, Color.white, 12);
            int playerBurn = Battle.PlayerStatuses.TotalMagnitude(StatusKind.Burn);
            if (playerBurn > 0)
                Ui.Chip(statusRow.transform, $"灼烧 {playerBurn}", Theme.Cinnabar, Color.white, 12);
            int immunity = Battle.PlayerStatuses.TotalMagnitude(StatusKind.Immunity);
            if (immunity > 0)
                Ui.Chip(statusRow.transform, $"免疫 {immunity}", Theme.Jade, Color.white, 12);
```

⚠ `Ui.Row` / `Ui.Chip` / `Theme.InkSoft` / `Theme.Cinnabar` / `Theme.Jade` 的真实签名以仓库现状为准 ——
照抄 `DrawEnemies` 里画敌人 chips 的那几行。`Battle.PlayerStatuses` 是 `BattleEngine` 上已有的公开属性。

- [ ] **Step 3: 免疫挡下的反馈**

`BattleView` 里处理逐记命中的那个 `switch`(有 `case BattleEventKind.EnemyAttack:` 的那个),加:

```csharp
                case BattleEventKind.ImmunityBlocked: // 免疫完全挡下:血条护盾条都不动,只给一个「免」的表达
                    _juice.FloatText(_playerHpBar.fill, "免", Theme.Jade);
                    break;
```

⚠ `_juice.FloatText` 的真实方法名与参数以 `Juice.cs` 现状为准 —— 打开它,找既有的飘字调用照抄
(护盾吸伤那条飘字就是现成参考)。**不要新写动效**,复用既有表达。

⚠ 顺带确认 `BurnTick` 与 `Burn` 两个事件的处理分支能扛住 `TargetIndex = -1`(玩家灼烧用 −1)。
既有代码是按敌人下标索引数组的,−1 会越界。**若会越界,加一条 `if (e.TargetIndex < 0)` 的玩家分支**
(玩家灼烧掉血走血条,与 `EnemyAttack` 同款推进)。这条必须实测,不能只看代码。

- [ ] **Step 4: 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

预期:0 个 `error CS`。**这一步不能跳** —— 工装只编译 Core/Data,Presentation 的编译错会一路漏到用户打开 Unity 才炸(已发生过两次)。

- [ ] **Step 5: 字体子集补新文案**

`封字` `灼烧` `免疫` `灼身` `免` 里的字若不在字集,会渲染成空框:

```bash
cd tools/fonts && python3 subset_fonts.py
python3 -m pytest tools/fonts/tests/ -q
```

- [ ] **Step 6: 详表 §7.5 补实现说明**

`docs/design/字选型/技能机制详表.md` 的 §7.5 落地顺序,在第 4 条(P1 三族)后补:

```
   —— 2026-08-06 落地。驱散/净化/免疫/复活做成四个新 `EffectKind`(而非修饰字段:
   灭·浴·塞·岿·杜·活 六字没有伤害效果可挂),斩杀是 `EffectDef` 的两个修饰字段。
   为了让净化与免疫有靶子,顺带补上了引擎从未有过的**敌方施加给玩家的减益**:
   Boss 倾覆的 AP 惩罚状态化为 `Seal`(零新内容,顺带修掉它从不进存档的老 bug),
   新字怪「灯花」带 `Sear` 每次攻击给玩家挂 1 层灼烧(复用既有 `StatusKind.Burn`)。
   岿 的「免疫位移与控制」改为「免疫 1 次伤害 + 立即净化」——位移机制不存在,
   冻结/减速是玩家打敌人的方向,原口径没有靶子。
```

- [ ] **Step 7: 全套测试**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

- [ ] **Step 8: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation/UI/EnemyInfo.cs \
        Brushblade/Assets/_Project/Presentation/UI/Theme.cs \
        Brushblade/Assets/_Project/Presentation/BattleView.cs \
        docs/design/字选型/技能机制详表.md \
        Brushblade/Assets/_Project/Presentation/Fonts/Resources/NotoSansSC-Subset.ttf \
        Brushblade/Assets/_Project/Presentation/Fonts/Resources/NotoSerifSC-Subset.ttf \
        tools/fonts/charset.txt
git commit -m "feat(presentation): 玩家状态条与灼身文案——封字/灼烧/免疫上屏"
```

---

## 收尾清单

1. **三套测试全绿**:`coretests`、`pytest`、`prescompile`。
2. **提醒用户跑一次 Unity EditMode** —— `ConfigLoaderTests` 被工装排除,Task 7 加的解析测试只在 Test Runner 里跑得到:

   ```bash
   /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/MacOS/Unity \
     -batchmode -projectPath Brushblade -runTests -testPlatform EditMode \
     -testResults /tmp/results.xml -logFile /tmp/unity_test.log
   ```
   (编辑器开着时会因项目锁失败,让用户在 Test Runner 里跑。)
3. **提醒用户新建的 `.cs` 需要 Unity 生成 `.meta`** —— `StatusOpsTests.cs`,用户开一次 Unity 即可。
4. **提醒用户「灯花」还没有形象**(自动回落圆形字头像),图鉴条目文案已补但美术待做。
5. **确认没碰用户的并行改动**:`git status` 里 `Brushblade/Assets/Settings/` 与 `Brushblade/ProjectSettings/ProjectSettings.asset` 应仍是未提交状态。
