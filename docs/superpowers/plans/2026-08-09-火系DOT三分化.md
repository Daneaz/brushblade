# 火系 DOT 三分化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地火系「攻 · DOT」表仅剩的三个 ⚠ 字(炑 / 燥 / 灱),按**延长 / 加速 / 兑现**三个方向分化,字表可用字数 129 → 132。

**Architecture:** 先把 `EndTurn` 里内联的灼烧结算循环体抽成私有方法 `SettleBurnOn(int)`(纯重构,行为逐位不变),再在它上面加三个机制:`BurnNoDecay` 是一个新 `StatusKind`(挂敌人、段内持久、只挡减层)、`BurnSettleNow` 复用抽出来的方法、`Detonate` 是一个独立私有方法。三者都不新增随机数消费点。

**Tech Stack:** Unity 6000.5.2f1 / C# 纯逻辑层(Core、Data 禁引 UnityEngine);Python 数据管线(pytest);测试走 `tools/coretests`(dotnet)+ `tools/prescompile`(离线编译)。

**规格来源:** `docs/superpowers/specs/2026-08-09-火系DOT三分化-design.md`

## Global Constraints

- **四层 asmdef 依赖单向**:`Presentation → {Core, Data, Platform}`,`Data → Core`。**Core 与 Data 禁止 `using UnityEngine`**。
- **随机性一律走 Core 内带种子的 `GameRandom`**,禁用 `UnityEngine.Random`。**本子项目不该新增任何 `_random` 消费点** —— 三个机制全是确定性的。
- **新枚举值一律追加在末尾**(`StatusKind` / `EffectKind` / `BattleEventKind`)—— 老存档里已持久化的序数不能位移。当前尾部:`StatusKind` 是 `Reflect`,`EffectKind` 是 `Reflect`,`BattleEventKind` 是 `Missed`。
- **测试断言只用 Unity 版 NUnit 也支持的 API**:禁用 `Is.AnyOf` / `Is.All.AnyOf`。多选一用 `Is.EqualTo(a).Or.EqualTo(b)`,集合子集用 `Has.All.Matches<T>`。
- **测试代码禁止直接引用 Newtonsoft**;Tests asmdef 是 `overrideReferences: true`,只放行 `nunit.framework.dll`,引用范围只有 Core / Data。**不要为了测试放宽生产代码的可见性。**
- **`chars.json` 是生成物**:由 `tools/pipeline/export_chars.py` 读详表生成。**永远不要手改它**。`enemies.json` 相反,是手写的,本子项目不碰。
- **详表是数值的唯一真相**。
- **UI 文案禁止 emoji**(字体子集补不出来 → 空框),一律中文短词。
- **中文注释里用「」书名号,不要用 ASCII 直引号 `"`** —— `tools/fonts/subset_fonts.py` 用裸正则扫 `.cs` 的 `"..."` 取字符、分不清代码与注释,且**扫 Core 与 Presentation**(只跳过 `Tests/`)。本项目已因此踩过两次。
- **提交信息用 conventional commits**。
- **只提交你自己改的文件**。用户在并行开发,`Brushblade/Assets/Settings/` 与 `Brushblade/ProjectSettings/ProjectSettings.asset` 有未提交改动,还有 6 个未跟踪的 `.meta` —— **绝不要 `git add -A` / `git add .`,绝不用 `git stash`**(它会把用户的未提交改动一起收走)。要对比干净状态用 `git show HEAD:<path>`。

### 每个任务收尾必跑

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q        # Task 5 起必跑
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q   # Task 7 必跑
```

只看 `error CS`,`warning MSB3245` 忽略。基线:coretests **743 passed**、pytest **238 passed**、prescompile **0 error**、`chars.json` **224 条**。

### 灼烧的现有模型(先记住,三个任务都依赖它)

`BattleEngine.cs:522-541`,`EndTurn` 的第一段:

```csharp
for (int i = 0; i < _enemies.Count; i++)
{
    var enemy = _enemies[i];
    if (!enemy.Alive) continue;
    var burn = enemy.Statuses.Find(StatusKind.Burn);
    if (burn == null || burn.Magnitude <= 0) continue;
    int tick = (int)Math.Floor(burn.Magnitude * _burnPerStack
        * WuxingResolver.KeMultiplier(Element.Fire, enemy.Element));
    enemy.Hp = Math.Max(0, enemy.Hp - tick);
    burn.Magnitude -= 1;
    if (burn.Magnitude <= 0) enemy.Statuses.Remove(StatusKind.Burn);
    _events.Add(new BattleEvent(BattleEventKind.BurnTick, i, tick));
    if (!enemy.Alive)
        ResolveDefeat(i);
    else
        CheckBossPhase(i);
}
```

- `Magnitude` = **层数**,`TurnsLeft = -1`(段内持久),靠结算自减而非回合递减
- `_burnPerStack`(`:129`)基础 2,`EffectKind.BurnPotency`(炽 / 炼)**本场全局**抬高它;它在存档里是 `BurnPerStack`
- **灼烧属火,只结算克制不结算相生**
- N 层烧完的总伤害 = **N(N+1)/2 × 系数**
- `ApplyBurn`(`:1115`)是**累加**语义;`RefreshBurn`(`:1144`)是 `Math.Max` 语义、只给光环用 —— **本子项目一律用 `ApplyBurn`**

### `NeedsTarget` 的一个坑(Task 4 要处理)

`NeedsTarget`(`:500`,`public static`)的白名单**已经包含 `BurnSingle`**。三个新字都自带 `BurnSingle`,所以**它们本来就会要求选目标** —— 把三个新 `EffectKind` 加进白名单是**防御性的**,对这三个字零判别力。

要让那条测试真有判别力,必须构造一个**只带 `Detonate`、不带 `BurnSingle`** 的测试字。Task 4 里写清楚了。

---

## 文件结构

| 文件 | 职责 | 任务 |
|---|---|---|
| `Core/BattleEngine.cs` | `SettleBurnOn` 抽方法;三个机制;`BattleEventKind.Detonate`;`NeedsTarget` | 1~4 |
| `Core/StatusEffect.cs` | `StatusKind` 加 `BurnNoDecay` | 2 |
| `Core/EffectDef.cs` | `EffectKind` 加三值 | 2、3、4 |
| `Tests/BurnVariantTests.cs` | **新建**,本子项目的行为测试主场 | 1~4 |
| `tools/pipeline/extract_values.py` | `VALUELESS_EFFECTS` 加三个 token | 5 |
| `tools/pipeline/tests/test_export_chars.py` | 管线侧抽取测试(正向 + 负向) | 5 |
| `docs/design/字选型/技能机制详表.md` | 三行 + §169 加注 + §7.2 + §7.5 | 5、7 |
| `chars.json`、两个 `.ttf`、`charset.txt` | **生成物** | 5、7 |
| `Tests/CharTableTests.cs` | 真实配置接线守卫 | 5 |
| `tools/balance/Program.cs` | 仿真(只读,通常不改) | 6 |
| `Presentation/BattleView.cs` / `Juice.cs` | 「不灭」chip、「爆」飘字与震屏 | 7 |

---

### Task 1: 抽出 `SettleBurnOn`(纯重构)

**这一任务不改变任何行为。** 交付物是「743 条测试一条不动地全绿」。之所以单独成任务,是因为后面三个机制都要挂在这个方法上,先把重构和新行为分开、评审才看得清。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`
- Create: `Brushblade/Assets/_Project/Tests/BurnVariantTests.cs`

**Interfaces:**
- Produces:私有 `void SettleBurnOn(int enemyIndex)` —— 对一名敌人结算一次灼烧(掉血、减层、发 `BurnTick`、判死/判阶段)

- [ ] **Step 1: 新建测试文件,写「重构前后行为一致」的基线测试**

新建 `Brushblade/Assets/_Project/Tests/BurnVariantTests.cs`:

```csharp
using System;
using System.Collections.Generic;
using System.Linq;
using Brushblade.Core;
using NUnit.Framework;

namespace Brushblade.Core.Tests
{
    /// <summary>火系 DOT 三分化(2026-08-09,子项目 E-a):不灭 / 立即结算 / 引爆。
    /// 规格见 docs/superpowers/specs/2026-08-09-火系DOT三分化-design.md。</summary>
    public class BurnVariantTests
    {
        private static RecipeGraph Graph() => new(new[]
        {
            new CharDef("木", Element.Wood),
            // 燃:纯灼烧 4 层(与真实字表的 燃 逐字同配置,便于对照)
            new CharDef("燃", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.BurnSingle, 4) }),
            // 炽:灼烧系数 +1(与真实字表的 炽 同配置)
            new CharDef("炽", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.BurnPotency, 1) }),
        });

        private static BattleEngine Engine(string[] library, EnemyDef[] enemies,
            BattleConfig config = null, int seed = 1) =>
            new(Graph(), config ?? new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 200 },
                library, Array.Empty<string>(), enemies, seed);

        private static EnemyDef Dummy(int hp = 300, int attack = 0) =>
            new("靶", Element.Heart, hp, attack);

        // ---- 灼烧结算的基线(重构守卫)----

        [Test]
        public void Burn_TicksThenDecaysOneStack()
        {
            var engine = Engine(new[] { "燃" }, new[] { Dummy() });
            engine.Cast("燃", 0);
            int before = engine.Enemies[0].Hp;

            engine.EndTurn();
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before - 8), "4 层 × 系数 2 = 8");
            Assert.That(engine.Enemies[0].Statuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(3),
                "结算后减一层");

            engine.EndTurn();
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before - 8 - 6), "3 层 × 2 = 6");
            Assert.That(engine.Enemies[0].Statuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(2));
        }

        [Test]
        public void Burn_EmitsOneBurnTickEventPerEnemy()
        {
            var engine = Engine(new[] { "燃" }, new[] { Dummy() });
            engine.Cast("燃", 0);
            engine.EndTurn();
            Assert.That(engine.LastEvents.Count(e => e.Kind == BattleEventKind.BurnTick),
                Is.EqualTo(1));
        }

        [Test]
        public void Burn_RespectsKeMultiplier_NotShengMultiplier()
        {
            // 火克金 ×1.5:4 层 × 2 × 1.5 = 12。用金属性靶子才测得出克制,
            // 心属性对全属性都是 1.0x(子项目 D 的教训:同属性对同属性也是 1.0,同样测不出来)
            var engine = Engine(new[] { "燃" }, new[] { new EnemyDef("锈", Element.Metal, 300, 0) });
            engine.Cast("燃", 0);
            int before = engine.Enemies[0].Hp;
            engine.EndTurn();
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before - 12), "4 × 2 × 1.5(火克金)");
        }

        [Test]
        public void Burn_LastStackRemovesTheStatus()
        {
            var engine = Engine(new[] { "燃" }, new[] { Dummy() });
            engine.Cast("燃", 0);
            for (int i = 0; i < 4; i++) engine.EndTurn();
            Assert.That(engine.Enemies[0].Statuses.Has(StatusKind.Burn), Is.False,
                "烧完 4 层后状态条目被移除");
        }

        [Test]
        public void Burn_KillingTheLastEnemy_WinsTheBattle()
        {
            var engine = Engine(new[] { "燃" }, new[] { Dummy(hp: 6) });
            engine.Cast("燃", 0);
            engine.EndTurn();
            Assert.That(engine.Enemies[0].Alive, Is.False);
            Assert.That(engine.Phase, Is.EqualTo(BattlePhase.Won));
        }
    }
}
```

⚠ `engine.LastEvents` / `engine.Enemies` / `BattleConfig` 各字段 / `EnemyDef` 与 `CharDef` 的构造签名**以仓库现状为准** —— 打开 `BattleEngine.cs`、`EnemyDef.cs`、`CharDef.cs` 确认后照实际的写,不要硬套上面的形状。参照已有的 `Brushblade/Assets/_Project/Tests/DamageVariantTests.cs`,它是同型的测试文件。

- [ ] **Step 2: 跑测试确认通过(重构前的基线)**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:743 + 5 条新测试**全部通过**。这一步和别的任务不同 —— 新测试**现在就该是绿的**,它们钉的是既有行为,是重构的安全网。若有一条红,说明你对现有模型的理解不对,**先查清楚再往下走**。

- [ ] **Step 3: 抽出 `SettleBurnOn`**

把 `EndTurn` 里那段循环(`BattleEngine.cs:522-541`)替换为:

```csharp
            // 3.7 结算顺序第 1 条:灼烧(X 层 → X×系数 伤害,然后 −1 层;系数基础 2,炽可加,10.2)
            for (int i = 0; i < _enemies.Count; i++) SettleBurnOn(i);
```

在 `ApplyBurn`(`:1115`)旁边加:

```csharp
        /// <summary>对一名敌人结算一次灼烧(2026-08-09 抽出):层数 × 系数 × 克制 掉血,然后 −1 层。
        /// 回合末逐个调用;燥 的 BurnSettleNow 也调这里 —— 不留两份实现。
        ///
        /// 灼烧属火(2026-08-03):只结算克制,不结算相生 —— 层数是平值,
        /// 相生已在施加时由 WuxingResolver 体现过。</summary>
        private void SettleBurnOn(int enemyIndex)
        {
            var enemy = _enemies[enemyIndex];
            if (!enemy.Alive) return;
            var burn = enemy.Statuses.Find(StatusKind.Burn);
            if (burn == null || burn.Magnitude <= 0) return;
            int tick = (int)Math.Floor(burn.Magnitude * _burnPerStack
                * WuxingResolver.KeMultiplier(Element.Fire, enemy.Element));
            enemy.Hp = Math.Max(0, enemy.Hp - tick);
            burn.Magnitude -= 1;
            if (burn.Magnitude <= 0) enemy.Statuses.Remove(StatusKind.Burn);
            _events.Add(new BattleEvent(BattleEventKind.BurnTick, enemyIndex, tick));
            if (!enemy.Alive)
                ResolveDefeat(enemyIndex);
            else
                CheckBossPhase(enemyIndex);
        }
```

⚠ 原来的 `continue` 变成 `return`(循环体变方法体),语义等价。**逐行核对没有漏掉任何一句**,尤其末尾的 `ResolveDefeat` / `CheckBossPhase` 分支。

- [ ] **Step 4: 跑测试确认行为逐位不变**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:**748 全绿**(743 基线 + 5 条新测试),**一条不红**。红了就是重构漏了什么。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/BurnVariantTests.cs
git commit -m "refactor(core): 灼烧结算抽出 SettleBurnOn,补 5 条既有行为守卫"
```

⚠ 新建的 `.cs` 需要 Unity 生成 `.meta`,Unity 没开时不会生成。**不要手写 `.meta`,也不要 `git add` 未跟踪的 `.meta`。**

---

### Task 2: `BurnNoDecay`(炑 —— 延长)

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/StatusEffect.cs`(`StatusKind` 加 `BurnNoDecay`)
- Modify: `Brushblade/Assets/_Project/Core/EffectDef.cs`(`EffectKind` 加 `BurnNoDecay`)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`
- Modify: `Brushblade/Assets/_Project/Tests/BurnVariantTests.cs`

**Interfaces:**
- Consumes:Task 1 的 `SettleBurnOn(int)`
- Produces:`StatusKind.BurnNoDecay`(挂敌人,`Magnitude = 1` 只作布尔用,`TurnsLeft = -1` 段内持久)、`EffectKind.BurnNoDecay`

- [ ] **Step 1: 写失败测试**

`Graph()` 里补一个字:

```csharp
            // 燋:2 层 + 不灭(炑 的等价配置)
            new CharDef("燋", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.BurnSingle, 2),
                                 new EffectDef(EffectKind.BurnNoDecay, 0) }),
```

追加测试:

```csharp
        // ---- 不灭(炑)----

        [Test]
        public void NoDecay_StacksDoNotDrop()
        {
            var engine = Engine(new[] { "燋" }, new[] { Dummy() });
            engine.Cast("燋", 0);
            int before = engine.Enemies[0].Hp;

            engine.EndTurn();
            Assert.That(engine.Enemies[0].Statuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(2),
                "不灭:结算后层数不掉");
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before - 4), "2 层 × 2");

            engine.EndTurn();
            Assert.That(engine.Enemies[0].Statuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(2));
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before - 8), "第二回合照样 2 层 × 2");
        }

        [Test]
        public void NoDecay_DoesNotChangeTickFormula()
        {
            // 不灭只挡减层,不碰伤害算式。用金属性靶子确认克制仍然生效
            var engine = Engine(new[] { "燋" }, new[] { new EnemyDef("锈", Element.Metal, 300, 0) });
            engine.Cast("燋", 0);
            int before = engine.Enemies[0].Hp;
            engine.EndTurn();
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before - 6), "2 × 2 × 1.5(火克金)");
        }

        [Test]
        public void NoDecay_SameCharDoesNotStack()
        {
            var engine = Engine(new[] { "燋", "燋" }, new[] { Dummy() });
            engine.Cast("燋", 0);
            engine.Cast("燋", 0);
            Assert.That(engine.Enemies[0].Statuses.All
                .Count(s => s.Kind == StatusKind.BurnNoDecay), Is.EqualTo(1),
                "同字重放只刷新,不挂两条");
            Assert.That(engine.Enemies[0].Statuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(4),
                "但灼烧层数照常累加");
        }

        [Test]
        public void NoDecay_SurvivesSaveRoundTrip()
        {
            // 规格 §8:不灭挂在 EnemyState.Statuses 上,走既有的 StatusBag 存档路径。
            // 这条钉住新 StatusKind 真的跟着快照走了——漏了的话断点续爬回来层数就开始掉了
            var engine = Engine(new[] { "燋" }, new[] { Dummy() });
            engine.Cast("燋", 0);

            // 签名(BattleEngine.cs:232):Restore(snapshot, graph, config, cardLevels, enemyDefs)
            var restored = BattleEngine.Restore(
                engine.Capture(),
                Graph(),
                new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 200 },
                new Dictionary<string, int>(),
                new Dictionary<string, EnemyDef> { ["靶"] = Dummy() });

            Assert.That(restored.Enemies[0].Statuses.Has(StatusKind.BurnNoDecay), Is.True);
            int before = restored.Enemies[0].Hp;
            restored.EndTurn();
            Assert.That(restored.Enemies[0].Statuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(2),
                "读档回来仍然不衰减");
            Assert.That(restored.Enemies[0].Hp, Is.EqualTo(before - 4));
        }

        [Test]
        public void NoDecay_DoesNotAffectPlayerOwnBurn()
        {
            // 炑 是玩家出的牌,没有理由让自己身上的灼烧也不衰减。
            // 灯花(Sear)每次攻击给玩家挂 1 层,玩家灼烧走 _playerStatuses,是另一段结算
            var engine = Engine(new[] { "燋" },
                new[] { new EnemyDef("灯", Element.Heart, 300, 0, EnemyAbility.Sear) });
            engine.Cast("燋", 0);
            engine.EndTurn();                       // 灯花出手 → 玩家挂 1 层
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(1));
            engine.EndTurn();                       // 玩家灼烧结算 → 该减到 0(灯花又补 1 层)
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(1),
                "玩家身上的灼烧照常衰减,只是灯花每回合又补一层——不该因为敌人带不灭而堆积");
        }
```

⚠ `EnemyAbility.Sear`、`engine.PlayerStatuses`、`StatusBag.All` 的确切名字以仓库现状为准。`NoDecay_DoesNotAffectPlayerOwnBurn` 那条的期望值**必须先按实际结算顺序推一遍再写** —— `EndTurn` 的顺序是「敌人灼烧 → 玩家灼烧 → …… → 敌方行动」,灯花是在敌方行动段挂的层。推不准就先跑一次看实际值,但**要在注释里写清楚为什么是这个值**,不要只抄结果。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:编译失败,`StatusKind` / `EffectKind` 都没有 `BurnNoDecay`。

- [ ] **Step 3: 两个枚举**

`Core/StatusEffect.cs`,`StatusKind` 在 `Reflect` 之后**追加**:

```csharp
        BurnNoDecay,      // 不灭:该敌人身上的灼烧层数不再每回合衰减(2026-08-09,炑)
```

`Core/EffectDef.cs`,`EffectKind` 在 `Reflect` 之后**追加**:

```csharp
        BurnNoDecay,  // 不灭:目标灼烧本场不衰减(Value 不用,2026-08-09)
```

- [ ] **Step 4: 结算里加条件**

`SettleBurnOn` 里,把

```csharp
            burn.Magnitude -= 1;
            if (burn.Magnitude <= 0) enemy.Statuses.Remove(StatusKind.Burn);
```

改成

```csharp
            // 不灭(2026-08-09,炑):带 BurnNoDecay 时层数不衰减 —— 伤害算式一个字不动,
            // 只挡这一步。Task 3 的 BurnSettleNow 落地后,它会同时变成「免费兑现」
            //(立即结算也不掉层)—— 那是规格 §4.2 那条爆发链的根
            if (!enemy.Statuses.Has(StatusKind.BurnNoDecay))
            {
                burn.Magnitude -= 1;
                if (burn.Magnitude <= 0) enemy.Statuses.Remove(StatusKind.Burn);
            }
```

- [ ] **Step 5: 施加不灭**

`ApplyEffects` 的 `switch` 里,`case EffectKind.Reflect:` 之后加:

```csharp
                    case EffectKind.BurnNoDecay:
                        // SourceId 用字 ID:同字再出只刷新,不挂两条
                        if (targetIndex >= 0 && _enemies[targetIndex].Alive)
                            _enemies[targetIndex].Statuses.Apply(new StatusEffect
                            {
                                Kind = StatusKind.BurnNoDecay, Polarity = StatusPolarity.Debuff,
                                Magnitude = 1, TurnsLeft = -1, SourceId = def.Id,
                            });
                        break;
```

⚠ `Polarity` 取 `Debuff`:它对敌人不利。这也意味着**驱散(`Dispel`,清敌方增益)清不掉它** —— 那是刻意的。

- [ ] **Step 6: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿,既有测试一条不红(无不灭时 `Has(BurnNoDecay)` 恒 false,行为与 Task 1 逐位相同)。

- [ ] **Step 7: 变异验证**

逐条做,**每条验完还原**:

| 变异 | 预期 |
|---|---|
| 去掉 `if (!enemy.Statuses.Has(StatusKind.BurnNoDecay))` 条件(照常减层) | `NoDecay_StacksDoNotDrop` 红 |
| 把 `Magnitude = 1` 改成每次新建不同 `SourceId` | `NoDecay_SameCharDoesNotStack` 红 |
| 让不灭也作用于 `_playerStatuses` 的灼烧结算 | `NoDecay_DoesNotAffectPlayerOwnBurn` 红 |
| `StatusBag` 的存档路径里跳过 `BurnNoDecay` | `NoDecay_SurvivesSaveRoundTrip` 红 |

**若某条变异后测试仍绿,说明那条测试没有判别力,回去重写它。** 报告里逐条写结果。

- [ ] **Step 8: 提交**

```bash
git add Brushblade/Assets/_Project/Core/StatusEffect.cs \
        Brushblade/Assets/_Project/Core/EffectDef.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/BurnVariantTests.cs
git commit -m "feat(core): 不灭——灼烧层数不再衰减,只挡减层不碰伤害算式"
```

---

### Task 3: `BurnSettleNow`(燥 —— 加速)

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EffectDef.cs`(`EffectKind` 加 `BurnSettleNow`)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`ApplyEffects` 一个 case)
- Modify: `Brushblade/Assets/_Project/Tests/BurnVariantTests.cs`

**Interfaces:**
- Consumes:Task 1 的 `SettleBurnOn(int)`、Task 2 的 `StatusKind.BurnNoDecay`
- Produces:`EffectKind.BurnSettleNow`(`Value` 不用)

- [ ] **Step 1: 写失败测试**

`Graph()` 里补:

```csharp
            // 熇:2 层 + 系数 +1 + 立即结算(燥 的等价配置,效果顺序即结算顺序)
            new CharDef("熇", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.BurnSingle, 2),
                                 new EffectDef(EffectKind.BurnPotency, 1),
                                 new EffectDef(EffectKind.BurnSettleNow, 0) }),
            // 熯:只有立即结算,不带灼烧也不带系数(用来单独验结算逻辑)
            new CharDef("熯", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.BurnSettleNow, 0) }),
```

追加测试:

```csharp
        // ---- 立即结算(燥)----

        [Test]
        public void SettleNow_BurnsImmediatelyWithinThePlayerTurn()
        {
            var engine = Engine(new[] { "燃", "熯" }, new[] { Dummy() });
            engine.Cast("燃", 0);                    // 4 层
            int before = engine.Enemies[0].Hp;
            engine.Cast("熯", 0);                    // 立即结算一次
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before - 8), "4 层 × 2,当场掉血");
            Assert.That(engine.Enemies[0].Statuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(3),
                "立即结算照常减一层");
        }

        [Test]
        public void SettleNow_EatsPotencyFromTheSameCard()
        {
            // 熇 的三个效果按数组顺序结算:先上 2 层,再把系数抬到 3,最后立即兑现
            var engine = Engine(new[] { "熇" }, new[] { Dummy() });
            int before = engine.Enemies[0].Hp;
            engine.Cast("熇", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before - 6),
                "2 层 × 系数 3(同一张牌抬的系数,立即结算就吃到了)");
        }

        [Test]
        public void SettleNow_EmitsBurnTickEvent()
        {
            var engine = Engine(new[] { "燃", "熯" }, new[] { Dummy() });
            engine.Cast("燃", 0);
            engine.Cast("熯", 0);
            Assert.That(engine.LastEvents.Count(e => e.Kind == BattleEventKind.BurnTick),
                Is.EqualTo(1), "复用既有 BurnTick 事件,不新建事件种类");
        }

        [Test]
        public void SettleNow_OnCleanTarget_DoesNothing()
        {
            var engine = Engine(new[] { "熯" }, new[] { Dummy() });
            int before = engine.Enemies[0].Hp;
            engine.Cast("熯", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before), "没有灼烧就没得结算");
            Assert.That(engine.LastEvents.Any(e => e.Kind == BattleEventKind.BurnTick), Is.False);
        }

        [Test]
        public void SettleNow_WithNoDecay_IsFreeAndKeepsStacks()
        {
            // 规格 §3.1 点名的连锁:不灭之下立即结算不掉层 = 免费兑现
            var engine = Engine(new[] { "燋", "熯" }, new[] { Dummy() });
            engine.Cast("燋", 0);                    // 2 层 + 不灭
            int before = engine.Enemies[0].Hp;
            engine.Cast("熯", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before - 4), "2 层 × 2");
            Assert.That(engine.Enemies[0].Statuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(2),
                "不灭之下立即结算也不掉层");
        }

        [Test]
        public void SettleNow_CanKillAndWin()
        {
            var engine = Engine(new[] { "燃", "熯" }, new[] { Dummy(hp: 8) });
            engine.Cast("燃", 0);
            engine.Cast("熯", 0);
            Assert.That(engine.Enemies[0].Alive, Is.False);
            Assert.That(engine.Phase, Is.EqualTo(BattlePhase.Won),
                "玩家回合内也能靠灼烧结算杀敌并判胜");
        }
```

✅ `SettleNow_CanKillAndWin` 成立:`Cast` 末尾确有 `CheckWin()`(`BattleEngine.cs:378`),玩家回合内杀敌会立刻判胜。
另外 `Cast` 开头有 `_events.Clear()`,所以每次 `Cast` 之后 `LastEvents` 只含这一次出字的事件 —— 上面几条数事件的断言依赖这一点。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:编译失败,`EffectKind` 没有 `BurnSettleNow`。

- [ ] **Step 3: 加枚举**

`Core/EffectDef.cs`,`EffectKind` 在 `BurnNoDecay` 之后**追加**:

```csharp
        BurnSettleNow, // 立即结算一次灼烧(与回合末同公式;Value 不用,2026-08-09)
```

- [ ] **Step 4: 加 case**

`ApplyEffects` 的 `switch` 里,`case EffectKind.BurnNoDecay:` 之后加:

```csharp
                    case EffectKind.BurnSettleNow:
                        // 复用回合末那一套(SettleBurnOn 自带存活与空层守卫),不留两份实现
                        if (targetIndex >= 0) SettleBurnOn(targetIndex);
                        break;
```

- [ ] **Step 5: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

- [ ] **Step 6: 变异验证**

| 变异 | 预期 |
|---|---|
| `case BurnSettleNow` 改成空 `break`(什么都不做) | `SettleNow_BurnsImmediatelyWithinThePlayerTurn` 等多条红 |
| 把 `熇` 的效果顺序改成 `BurnSettleNow` 在 `BurnPotency` 之前 | `SettleNow_EatsPotencyFromTheSameCard` 红(**这条钉的是「效果按数组顺序结算」这个通则**) |
| 让立即结算走一份**复制的**结算代码而不是 `SettleBurnOn`(比如漏掉减层) | `SettleNow_BurnsImmediatelyWithinThePlayerTurn` 的层数断言红 |

**每条验完还原**,报告里逐条写结果。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/EffectDef.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/BurnVariantTests.cs
git commit -m "feat(core): 立即结算——复用 SettleBurnOn 在玩家回合内提前兑现灼烧"
```

---

### Task 4: `Detonate`(灱 —— 兑现)+ 爆发链端到端

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EffectDef.cs`(`EffectKind` 加 `Detonate`)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`BattleEventKind` 加 `Detonate`、私有 `Detonate` 方法、`ApplyEffects` 一个 case、`NeedsTarget`)
- Modify: `Brushblade/Assets/_Project/Tests/BurnVariantTests.cs`

**Interfaces:**
- Consumes:Task 2 的 `StatusKind.BurnNoDecay`、Task 3 的 `EffectKind.BurnSettleNow`
- Produces:`EffectKind.Detonate`、`BattleEventKind.Detonate`(`TargetIndex` = 被引爆的敌人下标,`Amount` = 引爆伤害)

- [ ] **Step 1: 写失败测试**

`Graph()` 里补:

```csharp
            // 焌:4 层 + 引爆(灱 的等价配置)
            new CharDef("焌", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.BurnSingle, 4),
                                 new EffectDef(EffectKind.Detonate, 0) }),
            // 煸:只有引爆、不带灼烧 —— 专门用来给 NeedsTarget 白名单提供判别力。
            // 刻意避开真实字表里已有的「爆」(那是 DamageAll 7),免得读者以为是同一个字
            new CharDef("煸", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.Detonate, 0) }),
```

追加测试:

```csharp
        // ---- 引爆(灱)----

        [Test]
        public void Detonate_DealsFullRemainingBurnDamage()
        {
            // 4 层正常烧完 = (4+3+2+1) × 2 = 20;引爆一次性打完
            var engine = Engine(new[] { "燃", "煸" }, new[] { Dummy() });
            engine.Cast("燃", 0);
            int before = engine.Enemies[0].Hp;
            engine.Cast("煸", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before - 20), "4×5/2 × 系数 2 = 20");
        }

        [Test]
        public void Detonate_ClearsAllStacks()
        {
            var engine = Engine(new[] { "燃", "煸" }, new[] { Dummy() });
            engine.Cast("燃", 0);
            engine.Cast("煸", 0);
            Assert.That(engine.Enemies[0].Statuses.Has(StatusKind.Burn), Is.False,
                "引爆后一层不剩");
        }

        [Test]
        public void Detonate_SelfSuppliedStacksParticipate()
        {
            // 焌 自带 4 层,对空白目标也有 20 伤地板(紫档单攻上沿)
            var engine = Engine(new[] { "焌" }, new[] { Dummy() });
            int before = engine.Enemies[0].Hp;
            engine.Cast("焌", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before - 20),
                "自带 4 层先加、再引爆——结算顺序即数组顺序");
        }

        [Test]
        public void Detonate_StacksOnTopOfExistingBurn()
        {
            // 目标已有 4 层,焌 再加 4 层 = 8 层 → 8×9/2 × 2 = 72
            var engine = Engine(new[] { "燃", "焌" }, new[] { Dummy() });
            engine.Cast("燃", 0);
            int before = engine.Enemies[0].Hp;
            engine.Cast("焌", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before - 72), "8×9/2 × 2 = 72");
        }

        [Test]
        public void Detonate_RespectsKeMultiplier_NotShengMultiplier()
        {
            // 火克金 ×1.5:4 层引爆 = floor(4×5/2 × 2 × 1.5) = 30。
            // 必须用金属性靶子——心属性与同属性对同属性都是 1.0x,测不出来(子项目 D 的教训)
            var engine = Engine(new[] { "燃", "煸" }, new[] { new EnemyDef("锈", Element.Metal, 300, 0) });
            engine.Cast("燃", 0);
            int before = engine.Enemies[0].Hp;
            engine.Cast("煸", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before - 30));
        }

        [Test]
        public void Detonate_UsesCurrentPotency()
        {
            var engine = Engine(new[] { "燃", "炽", "煸" }, new[] { Dummy() });
            engine.Cast("燃", 0);
            engine.Cast("炽");                       // 系数 2 → 3
            int before = engine.Enemies[0].Hp;
            engine.Cast("煸", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before - 30), "4×5/2 × 3 = 30");
        }

        [Test]
        public void Detonate_OnCleanTarget_DoesNothing()
        {
            var engine = Engine(new[] { "煸" }, new[] { Dummy() });
            int before = engine.Enemies[0].Hp;
            engine.Cast("煸", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before));
            Assert.That(engine.LastEvents.Any(e => e.Kind == BattleEventKind.Detonate), Is.False);
        }

        [Test]
        public void Detonate_EmitsItsOwnEvent()
        {
            var engine = Engine(new[] { "燃", "煸" }, new[] { Dummy() });
            engine.Cast("燃", 0);
            engine.Cast("煸", 0);
            var ev = engine.LastEvents.First(e => e.Kind == BattleEventKind.Detonate);
            Assert.That(ev.TargetIndex, Is.EqualTo(0));
            Assert.That(ev.Amount, Is.EqualTo(20), "表现层靠 Amount 决定震屏强度");
        }

        [Test]
        public void Detonate_KillingLastEnemy_WinsTheBattle()
        {
            var engine = Engine(new[] { "燃", "煸" }, new[] { Dummy(hp: 20) });
            engine.Cast("燃", 0);
            engine.Cast("煸", 0);
            Assert.That(engine.Enemies[0].Alive, Is.False);
            Assert.That(engine.Phase, Is.EqualTo(BattlePhase.Won));
        }

        [Test]
        public void Detonate_KeepsNoDecayStatus()
        {
            // 引爆清的是灼烧层数,不是不灭状态——之后重新点燃仍然不衰减
            var engine = Engine(new[] { "燋", "煸", "燃" }, new[] { Dummy() });
            engine.Cast("燋", 0);
            engine.Cast("煸", 0);
            Assert.That(engine.Enemies[0].Statuses.Has(StatusKind.BurnNoDecay), Is.True);
            engine.Cast("燃", 0);
            engine.EndTurn();
            Assert.That(engine.Enemies[0].Statuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(4),
                "重新点的 4 层照样不衰减");
        }

        [Test]
        public void Detonate_NeedsExplicitTarget_WhenMultipleEnemiesAlive()
        {
            // 「爆」只有 Detonate、不带 BurnSingle,所以它是 NeedsTarget 白名单的**唯一**判别力来源
            // ——三个真实字都自带 BurnSingle,靠 BurnSingle 那条就已经进白名单了
            Assert.That(BattleEngine.NeedsTarget(Graph().Get("煸")), Is.True,
                "纯引爆是单体效果,UI 必须让玩家选目标;漏进白名单会让 targetIndex 停在 -1");
        }

        // ---- 爆发链端到端(规格 §4.2)----

        [Test]
        public void BurstChain_FourCards_DealsExpectedTotal()
        {
            // 燃(4 层)→ 燋(+2 且不灭)→ 熇(+2、系数→3、立即结算)→ 焌(+4、引爆)
            // 逐段断言,不要只断最终血量:只断总数的话,「层数算多了但系数算少了」
            // 这类互相抵消的错会蒙混过关
            var config = new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 200, ApPerTurn = 10 };
            var engine = Engine(new[] { "燃", "燋", "熇", "焌" }, new[] { Dummy(hp: 1000) }, config);
            var enemy = engine.Enemies[0];

            engine.Cast("燃", 0);
            Assert.That(enemy.Statuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(4));

            engine.Cast("燋", 0);
            Assert.That(enemy.Statuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(6));
            Assert.That(enemy.Statuses.Has(StatusKind.BurnNoDecay), Is.True);

            int beforeSettle = enemy.Hp;
            engine.Cast("熇", 0);
            Assert.That(enemy.Statuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(8),
                "8 层:6 + 熇 的 2,且不灭之下立即结算不掉层");
            Assert.That(enemy.Hp, Is.EqualTo(beforeSettle - 24), "立即结算 8 层 × 系数 3");

            int beforeDetonate = enemy.Hp;
            engine.Cast("焌", 0);
            Assert.That(enemy.Hp, Is.EqualTo(beforeDetonate - 234), "12 层 → 12×13/2 × 3 = 234");

            Assert.That(enemy.Statuses.Has(StatusKind.Burn), Is.False, "引爆后清空");
        }
```

⚠ `BattleEngine.NeedsTarget` 的签名与可见性、`RecipeGraph.Get` 的行为、`BattleConfig.ApPerTurn` 的字段名**以仓库现状为准**。爆发链那条需要 AP 够出四张牌,`ApPerTurn` 给足。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:编译失败,`EffectKind` 没有 `Detonate`、`BattleEventKind` 没有 `Detonate`。

- [ ] **Step 3: 两个枚举**

`Core/EffectDef.cs`,`EffectKind` 在 `BurnSettleNow` 之后**追加**:

```csharp
        Detonate,     // 引爆:把目标剩余灼烧层数的全部未来伤害一次打出并清空(Value 不用,2026-08-09)
```

`Core/BattleEngine.cs` 的 `BattleEventKind` 在 `Missed` 之后**追加**:

```csharp
        Detonate,    // 灼烧引爆(TargetIndex = 被引爆的敌人,Amount = 引爆伤害;2026-08-09)
```

- [ ] **Step 4: `Detonate` 方法**

加在 `SettleBurnOn` 旁边:

```csharp
        /// <summary>引爆(2026-08-09,灱):把剩余层数的**全部未来伤害**一次打出,然后清空层数。
        ///
        /// N 层正常烧完是 N + (N−1) + … + 1 = N(N+1)/2 个「层·回合」,所以总量口径就是那个和
        /// 乘系数 —— **只改兑现时机,不改总量**。价值在抢杀,以及防止敌人被别的牌提前打死
        /// 而浪费层数。
        ///
        /// 与回合末结算同口径:属火、只算克制不算相生。
        /// 清的是灼烧层数,**不动 BurnNoDecay** —— 之后重新点燃仍然不衰减。</summary>
        private void Detonate(int enemyIndex)
        {
            var enemy = _enemies[enemyIndex];
            if (!enemy.Alive) return;
            var burn = enemy.Statuses.Find(StatusKind.Burn);
            if (burn == null || burn.Magnitude <= 0) return;
            int stacks = burn.Magnitude;
            int damage = (int)Math.Floor(stacks * (stacks + 1) / 2.0 * _burnPerStack
                * WuxingResolver.KeMultiplier(Element.Fire, enemy.Element));
            enemy.Statuses.Remove(StatusKind.Burn);
            enemy.Hp = Math.Max(0, enemy.Hp - damage);
            _events.Add(new BattleEvent(BattleEventKind.Detonate, enemyIndex, damage));
            if (!enemy.Alive)
                ResolveDefeat(enemyIndex);
            else
                CheckBossPhase(enemyIndex);
        }
```

⚠ `stacks * (stacks + 1) / 2.0` —— **必须写 `2.0` 而不是 `2`**。整数除法会在奇数层时先截断,例如 5 层:`5*6/2 = 15` 对,但 `stacks*(stacks+1)` 若先乘系数再除就会错。照上面的式子写,`Math.Floor` 只在最后取整一次。

- [ ] **Step 5: 加 case 与白名单**

`ApplyEffects` 的 `switch` 里,`case EffectKind.BurnSettleNow:` 之后加:

```csharp
                    case EffectKind.Detonate:
                        if (targetIndex >= 0) Detonate(targetIndex);
                        break;
```

`NeedsTarget`(`:500`)的白名单补上三个新 Kind:

```csharp
                    || effect.Kind == EffectKind.BurnNoDecay
                    || effect.Kind == EffectKind.BurnSettleNow
                    || effect.Kind == EffectKind.Detonate
```

⚠ 白名单**已经包含 `BurnSingle`**,而三个真实字(炑/燥/灱)都自带 `BurnSingle` —— 所以这三行对它们零判别力,是给「将来某个只带引爆的字」留的防御。`Detonate_NeedsExplicitTarget_WhenMultipleEnemiesAlive` 用只带 `Detonate` 的测试字 `爆` 才守得住它。

- [ ] **Step 6: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

- [ ] **Step 7: 变异验证**

| 变异 | 预期 |
|---|---|
| 引爆公式改成 `stacks * _burnPerStack`(只结算当前层) | `Detonate_DealsFullRemainingBurnDamage` 红 |
| 引爆后不 `Remove(StatusKind.Burn)` | `Detonate_ClearsAllStacks` 红 |
| 引爆时也 `Remove(StatusKind.BurnNoDecay)` | `Detonate_KeepsNoDecayStatus` 红 |
| `Element.Fire` 改成 `enemy.Element`(即不走克制) | `Detonate_RespectsKeMultiplier_NotShengMultiplier` 红 |
| `2.0` 改成 `2`(整数除法) | 至少一条奇数层的断言红 —— **若全绿,补一条奇数层用例** |
| `NeedsTarget` 去掉 `Detonate` 那行 | `Detonate_NeedsExplicitTarget_WhenMultipleEnemiesAlive` 红 |
| 爆发链里把 `熇` 的立即结算去掉 | `BurstChain_FourCards_DealsExpectedTotal` 红 |

**每条验完还原**,报告里逐条写结果。

- [ ] **Step 8: 提交**

```bash
git add Brushblade/Assets/_Project/Core/EffectDef.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/BurnVariantTests.cs
git commit -m "feat(core): 引爆——按 N(N+1)/2 全额兑现剩余灼烧并清空,不动不灭状态"
```

---

### Task 5: 管线 token、详表三行、重新生成字表

**Files:**
- Modify: `tools/pipeline/extract_values.py`
- Modify: `tools/pipeline/tests/test_export_chars.py`
- Modify: `docs/design/字选型/技能机制详表.md`
- Modify: `Brushblade/Assets/_Project/Tests/CharTableTests.cs`
- Regenerate: `chars.json`

**Interfaces:**
- Consumes:Task 2~4 的三个 `EffectKind`
- Produces:`chars.json` 多出 3 个字(炑 燥 灱),**不新增任何叶子部件**

**注意**:三个新 `EffectKind` 走 `Enum.TryParse` / Newtonsoft 自动绑定,**`ConfigLoader.cs` 不需要改**。

- [ ] **Step 1: 写失败测试(管线侧)**

⚠ `VALUELESS_EFFECTS` 里的 token 是在**带数值效果之后**追加的(`extract_values.py` 的 `_parse_effects`,先 `re.findall(r"`(\w+) (\d+)`", config)` 收带数值的,再 `for token, spec in VALUELESS_EFFECTS.items()` 追加)。所以 `` `BurnSingle 2` + `BurnNoDecay` `` 抽出来的顺序天然是 `[BurnSingle, BurnNoDecay]` —— **正是引擎需要的结算顺序**。

追加到 `tools/pipeline/tests/test_export_chars.py`:

```python
def test_burn_no_decay_is_extracted_after_the_valued_effect():
    """炑:不灭是无数值标记,且必须排在 BurnSingle 之后(结算顺序 = 数组顺序)。"""
    from extract_values import _parse_effects
    assert _parse_effects("`BurnSingle 2` + `BurnNoDecay`", "火") == [
        {"kind": "BurnSingle", "value": 2},
        {"kind": "BurnNoDecay", "value": 0}]


def test_burn_settle_now_keeps_potency_before_it():
    """燥:立即结算必须排在 BurnPotency 之后,否则兑现的那一下吃不到 +1 系数。"""
    from extract_values import _parse_effects
    assert _parse_effects("`BurnSingle 2` + `BurnPotency 1` + `BurnSettleNow`", "火") == [
        {"kind": "BurnSingle", "value": 2},
        {"kind": "BurnPotency", "value": 1},
        {"kind": "BurnSettleNow", "value": 0}]


def test_detonate_is_extracted_after_its_own_burn():
    """灱:自带 4 层先加,再引爆。"""
    from extract_values import _parse_effects
    assert _parse_effects("`BurnSingle 4` + `Detonate`", "火") == [
        {"kind": "BurnSingle", "value": 4},
        {"kind": "Detonate", "value": 0}]


def test_new_valueless_tokens_do_not_leak_into_unrelated_rows():
    """负向:没写这些 token 的行不该凭空多出效果。

    只写正向断言的话,把 VALUELESS_EFFECTS 的匹配条件删成恒真都没有测试会红
    ——子项目 D 的教训(白名单方向性覆盖)。
    """
    from extract_values import _parse_effects
    assert _parse_effects("`DamageSingle 16`", "火") == [
        {"kind": "DamageSingle", "value": 16}]
    assert _parse_effects("`BurnSingle 2` + `DamageSingle 3`", "火") == [
        {"kind": "BurnSingle", "value": 2},
        {"kind": "DamageSingle", "value": 3}]
```

- [ ] **Step 2: 跑测试确认失败**

```bash
python3 -m pytest tools/pipeline/tests/ -q
```

预期:前 3 条新测试 FAIL(第 4 条负向的可能本来就绿 —— 那正常,它守的是「不要退化」)。

- [ ] **Step 3: 扩 `VALUELESS_EFFECTS`**

`tools/pipeline/extract_values.py` 的 `VALUELESS_EFFECTS` 里加三条:

```python
    "BurnNoDecay": {"kind": "BurnNoDecay", "value": 0},
    "BurnSettleNow": {"kind": "BurnSettleNow", "value": 0},
    "Detonate": {"kind": "Detonate", "value": 0},
```

⚠ 同一行若出现**两个**无数值 token,它们之间的顺序取决于 dict 的插入顺序。本批三个字每行**最多一个**,不受影响 —— 但在 `VALUELESS_EFFECTS` 上方的注释里补一句提醒。

- [ ] **Step 4: 跑管线测试确认通过**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
```

- [ ] **Step 5: 改详表三行**

⚠ **列数必须与所在表的表头一致。** 管线的 `_parse_row` 靠 `len(cells) < 4` 与 `len(cells[0]) != 1` 判行,列错位不会报错、只会让抽出来的「效果配置」取错格(它取的是**第一个含反引号的格**)。**改之前先看那张表的表头有几列。**

火系「攻 · DOT(层数平值)」表(6 列:字 | 稀 | 效果配置(基础值) | 相生 | **最终值** | 实现)—— 原地改三行:

```
| 燥 | 🟣紫 | `BurnSingle 2` + `BurnPotency 1` + `BurnSettleNow` | — | 2 层 + 系数 +1 + 立即兑现一次 | ✅ 加速:系数 +1 是本场全局,是 炼(绿)的紫档升阶 |
| 炑 | 🟣紫 | `BurnSingle 2` + `BurnNoDecay` | — | 2 层,本场不衰减 | ✅ 延长:配方 火+木 虽吃 ×3,但灼烧不走生克故无影响(见 §169 注) |
| 灱 | 🟣紫 | `BurnSingle 4` + `Detonate` | — | 4 层 → 引爆 20 | ✅ 兑现:引爆 = N(N+1)/2 × 系数,层数清零;与 燃 的区别是「收状态」而非「铺状态」 |
```

⚠ 「相生」列填 `—`(不要填 `×3`)—— 炑 的 ×3 对灼烧无效果,填 `×3` 会误导读者以为数值里含了它。理由写在「实现」列与 §169 的注里。

**改完用这条自检,必须没有输出:**

```bash
python3 - <<'PY'
lines=open('docs/design/字选型/技能机制详表.md',encoding='utf-8').read().split('\n')
hdr=None
for i,l in enumerate(lines):
    if l.startswith('| 字 |'): hdr=len(l.split('|'))-2
    elif l.startswith('|') and not l.startswith('|---') and len(l.split('|')[1].strip())!=1: hdr=None
    if hdr and l.startswith('| ') and not l.startswith('|---') and not l.startswith('| 字 |'):
        n=len(l.split('|'))-2
        if n!=hdr: print(f"行{i+1} 列数{n} ≠ 表头{hdr}: {l[:50]}")
PY
```

- [ ] **Step 6: §169 的 ×3 名单加注**

详表 §169 那句现在是:

> 只有跨系配方才可能,且必须是母属性。全表最终只有 **5 字**吃到 ×3:燊 焚 蒸(木生火)、刲(土生金)、沏(金生水)。

在它后面补一句:

```
> 2026-08-09 补:`炑`(火 + 木)的配方也命中木生火,但它的效果只有 `BurnSingle` 与 `BurnNoDecay`,
> 两者都不走 `WuxingResolver`(灼烧层数是平值,施加时不过生克),所以 ×3 对它**无任何效果**,
> 数值表里不体现。⚠ 若日后给 炑 加伤害成分,×3 会立刻生效,数值要重算。
```

- [ ] **Step 7: 重新生成 `chars.json`**

```bash
cd tools/pipeline && python3 export_chars.py
```

**核对改动范围**:炑 / 燥 / 灱 此前都是 ⚠ 未落地,`chars.json` 里一条都没有;三个字的部件(火 木 喿 刀)**全部已在表中**。所以**只多出 3 条**,总条数 **224 → 227**。

用这条逐条比对,**既有字必须零改动**:

```bash
cd /Users/eugenewu/code/game && python3 - <<'PY'
import json, subprocess
old = json.loads(subprocess.run(['git','show','HEAD:Brushblade/Assets/StreamingAssets/config/chars.json'],
                                capture_output=True,text=True).stdout)
new = json.load(open('Brushblade/Assets/StreamingAssets/config/chars.json'))
o = {c['id']: json.dumps(c,ensure_ascii=False,sort_keys=True) for c in old['chars']}
n = {c['id']: json.dumps(c,ensure_ascii=False,sort_keys=True) for c in new['chars']}
print(f"条数 {len(o)} → {len(n)}")
print("新增:", ''.join(sorted(set(n)-set(o))))
print("删除:", ''.join(sorted(set(o)-set(n))) or '无')
print("既有字被改动:", ''.join(sorted(k for k in set(o)&set(n) if o[k]!=n[k])) or '无')
PY
```

**若「既有字被改动」不是「无」,或条数不是 224 → 227,停下来查原因。**

- [ ] **Step 8: 补真实配置守卫**

追加到 `Brushblade/Assets/_Project/Tests/CharTableTests.cs`(照抄该文件既有的 `RealGraph()` 风格):

```csharp
        [Test]
        public void RealConfig_MuCarriesBurnAndNoDecay()
        {
            var effects = RealGraph().Get("炑").Effects;
            Assert.That(effects[0].Kind, Is.EqualTo(EffectKind.BurnSingle));
            Assert.That(effects[0].Value, Is.EqualTo(2));
            Assert.That(effects[1].Kind, Is.EqualTo(EffectKind.BurnNoDecay),
                "不灭必须排在灼烧之后——结算顺序就是数组顺序");
        }

        [Test]
        public void RealConfig_ZaoSettlesAfterRaisingPotency()
        {
            var effects = RealGraph().Get("燥").Effects;
            Assert.That(effects.Select(e => e.Kind), Is.EqualTo(new[]
            {
                EffectKind.BurnSingle, EffectKind.BurnPotency, EffectKind.BurnSettleNow,
            }), "顺序错了立即结算就吃不到自己抬的系数");
            Assert.That(effects[0].Value, Is.EqualTo(2));
            Assert.That(effects[1].Value, Is.EqualTo(1));
        }

        [Test]
        public void RealConfig_XiaoIsFourStacksThenDetonate()
        {
            var effects = RealGraph().Get("灱").Effects;
            Assert.That(effects[0].Kind, Is.EqualTo(EffectKind.BurnSingle));
            Assert.That(effects[0].Value, Is.EqualTo(4), "4 层给引爆 20 伤的地板");
            Assert.That(effects[1].Kind, Is.EqualTo(EffectKind.Detonate));
        }

        [Test]
        public void RealConfig_NewBurnCharsAddNoLeafParts()
        {
            // 三个字的部件(火 木 喿 刀)全部已在表中,本批不该新增任何叶子
            var graph = RealGraph();
            foreach (var id in new[] { "炑", "燥", "灱" })
                Assert.That(graph.Get(id).Recipe, Has.All.Matches<string>(p => graph.Get(p) != null),
                    $"{id} 的部件都该已在表中");
        }
```

⚠ `Is.EqualTo` 比较序列在 Unity 版 NUnit 里的行为、`RecipeGraph.Get` 对未知字的行为、`CharDef.Effects` 的类型(数组还是 `IReadOnlyList`)**以仓库现状为准**。`Has.All.Matches<T>` 是本项目允许的写法(`Is.All.AnyOf` 禁用)。

- [ ] **Step 9: 全套测试**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

- [ ] **Step 10: 变异验证**

| 变异 | 预期 |
|---|---|
| `VALUELESS_EFFECTS` 删掉 `BurnNoDecay` | 管线测试 + `RealConfig_MuCarriesBurnAndNoDecay` 红(需重跑 `export_chars.py`) |
| 详表把 燥 的 `BurnSettleNow` 挪到 `BurnPotency` 之前 | `RealConfig_ZaoSettlesAfterRaisingPotency` 红 |
| 详表把 灱 的 `BurnSingle 4` 改成 `BurnSingle 2` | `RealConfig_XiaoIsFourStacksThenDetonate` 红 |

⚠ **变异详表之后必须重新生成 `chars.json` 才看得出效果。验完务必把详表还原并重新生成**,用 Step 7 的比对脚本确认回到 227 条、既有字零改动。**这一步别马虎** —— 忘了还原会把错误的生成物提交上去。

- [ ] **Step 11: 提交**

```bash
git add tools/pipeline/extract_values.py \
        tools/pipeline/tests/test_export_chars.py \
        docs/design/字选型/技能机制详表.md \
        Brushblade/Assets/_Project/Tests/CharTableTests.cs \
        Brushblade/Assets/StreamingAssets/config/chars.json
git commit -m "feat(data): 火系 DOT 三字落地——129 → 132 字可用,chars.json 224 → 227"
```

---

### Task 6: 仿真验证与数值裁定

**这一任务不写功能代码。** 交付物是一份数据结论 + (可能的)数值调整。

**Files:**
- Run: `tools/balance/`(只读,通常不改)
- Possibly modify: `docs/design/字选型/技能机制详表.md` + 重新生成 `chars.json`

**Interfaces:**
- Consumes:Task 5 生成的 `chars.json`

- [ ] **Step 1: 跑基线仿真(改动前)**

```bash
cd tools/balance && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet run -c Release
```

⚠ 仿真读 `Brushblade/Assets/StreamingAssets/config/chars.json`(`tools/balance/Program.cs:27`)。三个新字**已经在里面了**,所以「改动前」的基线要用 `git stash` 之外的办法拿 —— **不要用 `git stash`**。用:

```bash
git show HEAD~1:Brushblade/Assets/StreamingAssets/config/chars.json > /tmp/chars-before.json
```

然后临时把它拷到配置目录跑一次,跑完**立即用 `git checkout` 还原**。或者更稳:直接读 `tools/balance/Program.cs` 看它的配置目录参数,若支持传路径就传 `/tmp` 的那份,完全不碰工作区。**优先选不碰工作区的那条路。**

记录三画像(新手 / 卡 3 级 / 卡 5 级)的卒层 P50。

- [ ] **Step 2: 跑改动后仿真**

同样命令,用当前 `chars.json`。记录同样三个数。

⚠ **仿真的机器人是贪心的,不一定会打出「燃 → 炑 → 燥 → 灱」这条链。** 若两次结果几乎一样,**先确认机器人到底有没有出这三个字**(在 `Program.cs` 里加临时计数,或看出字日志),再下结论。「没变化」可能是「机器人不会用」而不是「不影响平衡」。

- [ ] **Step 3: 单独验爆发链对 Boss 的杀伤**

Task 4 的 `BurstChain_FourCards_DealsExpectedTotal` 已经钉住 258 这个数。这一步要回答的是**成语 Boss 扛不扛得住**:

- 查 `enemies.json` 里成语 Boss 的总血池与阶段阈值
- 算一次 258 伤会跨掉几个阶段
- 写进报告

- [ ] **Step 4: 裁定**

把三组数据整理成一段结论,交给控制器。**若仿真显示失控**,规格 §4.2 已经指定了阀门顺序:

1. **第一个该动的是给 炑 加回合数上限**(`BurnNoDecay(turns N)`,详表写成 `` `BurnNoDecay 2` `` 并进 `DURATION_KINDS`)
2. **不要先砍引爆公式** —— 那会破坏「只改时机不改总量」这个干净口径

**这一步不要自己拍板改数值**,把数据和建议交给控制器,由用户裁定。

- [ ] **Step 5: 提交(仅在有数值改动时)**

若无改动,本任务不产生 commit,只产出报告。若有:

```bash
git add docs/design/字选型/技能机制详表.md \
        Brushblade/Assets/StreamingAssets/config/chars.json
git commit -m "balance(data): 火系 DOT 三字数值调整——<写清楚改了什么、依据哪组仿真数据>"
```

---

### Task 7: 表现层

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`
- Modify: `Brushblade/Assets/_Project/Presentation/Juice.cs`
- Modify: `docs/design/字选型/技能机制详表.md`(§7.2 与 §7.5)
- Regenerate: 两个 `.ttf` + `tools/fonts/charset.txt`

**Interfaces:**
- Consumes:`StatusKind.BurnNoDecay`、`BattleEventKind.Detonate`

- [ ] **Step 1: 敌人 chip 加「不灭」**

`BattleView.DrawEnemies` 的 chips 段,在灼烧 chip 之后加:

```csharp
                if (enemy.Statuses.Has(StatusKind.BurnNoDecay))
                    Ui.Chip(chips.transform, "不灭", Theme.Cinnabar, Color.white, 12);
```

⚠ **文案必须是 2 字。** 敌人 chip 行已经在 190px 格宽上溢出(`EnemyCellWidth = 190f`,而「致盲 −30%」7 字符就占 102px)—— 这是子项目 D 终审记下的既有债,本任务**不修它**,但不能再加长文案。

⚠ `Ui.Chip` 的签名、`Theme.Cinnabar` 是否存在**以仓库现状为准** —— 照抄同文件里灼烧 chip 那一行的写法。灼烧系是朱砂色,不灭属同族,用同色系。

- [ ] **Step 2: 引爆的飘字与震屏**

`Juice.cs` 里找到 `case BattleEventKind.BurnTick:` 那个分支,在它旁边加:

```csharp
                    // 引爆(2026-08-09,灱):一次性把剩余层数全打出来,给一记比普通灼烧结算更重的反馈
                    case BattleEventKind.Detonate:
                        Shake(e.Amount);
                        Popup($"爆 {e.Amount}", Theme.Cinnabar, enemyAnchor(e.TargetIndex));
                        break;
```

⚠ `Shake` / `Popup` / `enemyAnchor` / `Theme.Cinnabar` 的真实签名**以仓库现状为准** —— 照抄同文件里 `BurnTick` 与 `Damage` 分支的写法。若震屏方法名不是 `Shake`,用实际的。

⚠ **`Juice.cs` 的事件主循环里有一个 `lastDamageTarget` 状态变量**(子项目 D 加的,用于多段伤害分两拍播)。`Detonate` 分支**不要碰它** —— 引爆不是 `Damage` 事件,不参与那套分拍逻辑。

- [ ] **Step 3: 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

预期:**0 个 `error CS`**。**这一步不能跳** —— 工装只编译 Core/Data,Presentation 的编译错会一路漏到用户打开 Unity 才炸(本项目已发生过两次)。

- [ ] **Step 4: 字体子集补新文案**

`不` `灭` `爆` 三个字若不在字集会渲染成空框:

```bash
cd tools/fonts && python3 subset_fonts.py
python3 -m pytest tools/fonts/tests/ -q
```

⚠ 跑完确认 `charset.txt` 的变化**只多了该多的字**。若多出别的字,八成是你在某个 `.cs` 的中文注释里用了 ASCII 直引号 `"` 包中文 —— 改成「」再重新生成。

- [ ] **Step 5: 详表 §7.2 与 §7.5**

§7.2 的 P3 那一行现在是:

> | **P3** | `BurnNoDecay` / `BurnRefresh` / `HealReduction` | 4 | 燊 炑 燥 灱 |

改成:

```
| ~~P3~~ ✅ | `BurnNoDecay` / `BurnSettleNow` / `Detonate` | 3 | 炑 燥 灱 —— 已实现(2026-08-09;燊 早已实现,`BurnRefresh`/`HealReduction` 两个 Kind 废止) |
```

⚠ **打开详表确认那一行的实际内容与列数再改**,不要照抄上面的形状。`BurnRefresh` 与 `HealReduction` 是本次废止的 —— 前者被 `BurnNoDecay` 取代,后者因靶子太薄放弃(规格 §1)。

§7.5 落地顺序末尾补一条:

```
7. **火系 DOT 三分化** —— 2026-08-09 落地。炑 / 燥 / 灱 按「延长 / 加速 / 兑现」三个方向分化:
   `BurnNoDecay`(层数不衰减)、`BurnSettleNow`(玩家回合内立即兑现一次)、`Detonate`
   (按 N(N+1)/2 全额兑现剩余层数并清空)。为此把 `EndTurn` 里内联的灼烧结算抽成
   `SettleBurnOn(int)`,回合末与立即结算共用一份实现。
   原案的 `BurnRefresh` 被 `BurnNoDecay` 取代;`HealReduction` 因引擎里唯一会回血的敌人
   只有缺笔妖(且第 3 次补全是直接回满)而废止。
```

- [ ] **Step 6: 全套测试**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation/BattleView.cs \
        Brushblade/Assets/_Project/Presentation/Juice.cs \
        docs/design/字选型/技能机制详表.md \
        Brushblade/Assets/_Project/Presentation/Fonts/Resources/NotoSansSC-Subset.ttf \
        Brushblade/Assets/_Project/Presentation/Fonts/Resources/NotoSerifSC-Subset.ttf \
        tools/fonts/charset.txt
git commit -m "feat(presentation): 不灭 chip 与引爆飘字上屏;详表 §7.2/§7.5 同步"
```

⚠ 字体产物的真实输出路径以 `subset_fonts.py` 的 `OUT_DIR` 为准,**自己确认后再写 `git add` 的路径**。

---

## 收尾清单

1. **三套测试全绿**:`coretests`、`pytest`、`prescompile`。
2. **提醒用户跑一次 Unity EditMode** —— `ConfigLoaderTests` 被 `tools/coretests` 工装排除(它引 `UnityEngine.Application`),只在 Test Runner 里跑得到:

   ```bash
   /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/MacOS/Unity \
     -batchmode -projectPath Brushblade -runTests -testPlatform EditMode \
     -testResults /tmp/results.xml -logFile /tmp/unity_test.log
   ```
3. **提醒用户新建的 `.cs` 需要 Unity 生成 `.meta`** —— `BurnVariantTests.cs`(以及子项目 A / D 遗留的 6 个 `.meta` 至今未提交)。
4. **确认没碰用户的并行改动**:`git status` 里 `Brushblade/Assets/Settings/` 与 `Brushblade/ProjectSettings/ProjectSettings.asset` 应仍是未提交状态。
5. **Task 6 的仿真结论**要在合并前呈给用户,连同「是否给 炑 加回合上限」的建议。
