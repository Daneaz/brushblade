# 伤害变体 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地致盲/闪避/沉默/反弹/多段五种机制共 6 字,并给引擎补上**命中判定**这一整套,字表可用字数 123 → 129。

**Architecture:** 命中判定是一个私有辅助方法,插在 `DamagePlayerDirect` 与 `DamageSummon` 的最开头,只判敌人打过来的那一侧。致盲/沉默/反弹是三个新 `StatusKind` + 三个新 `EffectKind`;闪避是 `SummonPassive` 加一个字段(子项目 C 建好的结构);多段是 `EffectDef` 加一个 `HitCount`。

**Tech Stack:** Unity 6000.5.2f1 / C# 纯逻辑层(Core、Data 禁引 UnityEngine);Python 数据管线(pytest);测试走 `tools/coretests`(dotnet)+ `tools/prescompile`(离线编译)。

**规格来源:** `docs/superpowers/specs/2026-08-07-伤害变体-design.md`

## Global Constraints

- **四层 asmdef 依赖单向**:`Presentation → {Core, Data, Platform}`,`Data → Core`。**Core 与 Data 禁止 `using UnityEngine`**。
- **随机性一律走 Core 内带种子的 `GameRandom`**,禁用 `UnityEngine.Random`。本子项目第一次在战斗结算里引入随机,必须走 `BattleEngine._random`。
- **新枚举值一律追加在末尾**(`StatusKind` / `EffectKind` / `BattleEventKind`)—— 老存档里已持久化的序数不能位移。
- **测试断言只用 Unity 版 NUnit 也支持的 API**:禁用 `Is.AnyOf` / `Is.All.AnyOf`。多选一用 `Is.EqualTo(a).Or.EqualTo(b)`,集合子集用 `Has.All.Matches<T>`。
- **测试代码禁止直接引用 Newtonsoft**;测试只能引用 Core / Data。**不要为了测试放宽生产代码的可见性。**
- **`chars.json` 是生成物**:`tools/pipeline/export_chars.py` 读 `docs/design/字选型/技能机制详表.md` 生成。**永远不要手改它** —— 改详表然后重新生成。`enemies.json` 相反,是手写的,本子项目不碰。
- **详表是数值的唯一真相**。本批 6 字已核过相生「他生我」口径,**无一吃 ×3**,详表填的就是最终值。
- **UI 文案禁止 emoji**(字体子集补不出来 → 空框),一律中文短词。
- **提交信息用 conventional commits**。
- **只提交你自己改的文件**。用户在并行开发,`Brushblade/Assets/Settings/` 与 `Brushblade/ProjectSettings/ProjectSettings.asset` 有未提交改动,还有若干未跟踪的 `.meta` —— **绝不要 `git add -A` / `git add .`,绝不要碰这些**。

### 每个任务收尾必跑

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q     # Task 5 起必跑
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q   # Task 7 必跑
```

只看 `error CS`;`warning MSB3245` 忽略。基线:coretests **686 passed**、pytest **231 passed**、prescompile **0 error**。

### 一条压倒一切的约束:命中率 100 时必须短路,不摇随机数

`BattleEngine._random` 的**唯一**既有消费方是 `StartTurn` 里的回合掉字
(`deck[_random.Next(deck.Count)]`)。命中判定若无条件摇一次,就会平移掉落序列,
**让所有依赖种子的既有测试全部红掉**。

所以辅助方法必须在命中率 ≥ 100 时**直接返回 true 而不摇**。既有测试里没有任何致盲/闪避,
于是一次随机都不多摇,行为逐位不变。

(致盲真的生效时掉落序列确实会平移 —— 那是同一条种子流的正常结果,`BattleSnapshot.RandomState`
已经在存档里,断点续爬不受影响。)

### `EndTurn` 的结算顺序(反复踩到,先记住)

```
敌人灼烧 → 玩家灼烧(归零即死,早退)→ 敌人流血 → 玩家持续治疗
→ 召唤物光环治疗 → 召唤物出手 → 敌方行动 → 状态回合递减
→ 玩家阵亡早退 → 判胜 → 缺笔妖补全 → StartTurn
```

**在敌方段挂上的、`TurnsLeft` 为正的状态,会被同一个 `EndTurn` 的状态递减立刻减一次。**

---

## 文件结构

| 文件 | 职责 | 任务 |
|---|---|---|
| `Core/StatusEffect.cs` | `StatusKind` 加 `Blind`/`Silence`/`Reflect` | 1、2、3 |
| `Core/SummonPassive.cs` | 加 `Dodge` 字段 | 1 |
| `Core/EffectDef.cs` | `EffectKind` 加三值;`EffectDef` 加 `HitCount` | 1、2、3、4 |
| `Core/BattleEngine.cs` | `AttackHits` 辅助 + 两处插入;沉默五处;反弹;多段;`BattleEventKind` 加 `Missed` | 1~4 |
| `Data/ConfigLoader.cs` | `EffectDto` 加 `HitCount`;`SummonPassive` 的 `Dodge` 自动带 | 5 |
| `tools/pipeline/extract_values.py` | `turns`/`targetAll` 从写死给 `HealOverTime` 改成按 Kind 白名单 | 5 |
| `tools/pipeline/export_chars.py` | `MANUAL_RECIPES` 加 `锁` | 6 |
| `docs/design/字选型/技能机制详表.md` | 6 行 + `钩` 移出 + §7.2 标记 | 6、7 |
| `chars.json`、两个 `.ttf`、`charset.txt` | **生成物** | 6 |
| `Presentation/BattleView.cs` / `Juice.cs` / `UI/EnemyInfo.cs` | 打空飘字、致盲/沉默 chip、反弹状态、闪避小字、多段分拍 | 7 |
| `Tests/DamageVariantTests.cs` | **新建**,本子项目的行为测试主场 | 1~4 |
| `Tests/CharTableTests.cs` | 真实配置接线守卫 | 6 |
| `tools/pipeline/tests/test_export_chars.py` | 管线侧抽取测试 | 5 |

---

### Task 1: 命中判定、致盲与闪避

本子项目的地基。**风险最高的一条是随机流** —— 见 Global Constraints 里那段。

⚠ **对 spec §九 第 1 条的一处刻意偏离**:spec 写的是「用固定种子断言具体的命中/落空序列」。
这里不那么做 —— 那种测试只能先跑一次再把结果抄进断言,本质是「让测试符合实现」,
改坏实现它也未必红。改成两条更有判别力的:**确定性两端**(致盲 0 → 必中、致盲 100 → 必空、
致盲+闪避 ≥ 100 → 必空)与**随机流不被污染**(同种子同操作必须掉一样的字)。
前者钉住命中率算式,后者钉住短路。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/StatusEffect.cs`(`StatusKind` 加 `Blind`)
- Modify: `Brushblade/Assets/_Project/Core/SummonPassive.cs`(加 `Dodge`)
- Modify: `Brushblade/Assets/_Project/Core/EffectDef.cs`(`EffectKind` 加 `Blind`)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`
- Create: `Brushblade/Assets/_Project/Tests/DamageVariantTests.cs`

**Interfaces:**
- Produces:
  - `StatusKind.Blind`(`Magnitude` = 命中降低百分比,`Polarity = Debuff`)
  - `SummonPassive.Dodge`(int,百分比)
  - `EffectKind.Blind`(`Value` = 百分比,复用 `TargetAll` 定范围、`Turns` 定回合)
  - `BattleEventKind.Missed`(`TargetIndex` = 攻击者敌人下标,`SecondIndex` = 被打空的召唤物下标,玩家为 −1)
  - 私有 `bool AttackHits(int enemyIndex, int dodgePercent)`

- [ ] **Step 1: 写失败测试**

新建 `Brushblade/Assets/_Project/Tests/DamageVariantTests.cs`:

```csharp
using System;
using System.Collections.Generic;
using System.Linq;
using Brushblade.Core;
using NUnit.Framework;

namespace Brushblade.Core.Tests
{
    /// <summary>伤害变体(2026-08-07,子项目 D):命中判定 / 致盲 / 闪避 / 沉默 / 反弹 / 多段。
    /// 规格见 docs/superpowers/specs/2026-08-07-伤害变体-design.md。</summary>
    public class DamageVariantTests
    {
        private static RecipeGraph Graph() => new(new[]
        {
            new CharDef("木", Element.Wood),
            // 素:无被动的基准召唤(10 血 / 攻 0),用来当承伤靶
            new CharDef("素", Element.Wood,
                effects: new[] { new EffectDef(EffectKind.Summon, 10, summonCount: 1, summonAttack: 0, summonChar: "木") }),
            // 闪:召唤物带 50% 闪避(柳)
            new CharDef("闪", Element.Wood,
                effects: new[] { new EffectDef(EffectKind.Summon, 10, summonCount: 1, summonAttack: 0, summonChar: "木",
                    passive: new SummonPassive { Dodge = 50 }) }),
            // 昏:单体致盲 100%,2 回合(熣 的极端版,用来做确定性断言)
            new CharDef("昏", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.Blind, 100, turns: 2) }),
            // 雾:全体致盲 100%,1 回合(烟 的极端版)
            new CharDef("雾", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.Blind, 100, turns: 1, targetAll: true) }),
            // 挡:护盾 20;御:免疫 1 次 —— 用来验「打空时这两样都不该被消耗」
            new CharDef("挡", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.Shield, 20) }),
            new CharDef("御", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.Immunity, 1) }),
            // 凿:1 点伤害,用来精确磨血线 / 制造一次「受击存活」
            new CharDef("凿", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.DamageSingle, 1) }),
        });

        private static BattleEngine Engine(string[] library, EnemyDef[] enemies,
            BattleConfig config = null, int seed = 1) =>
            new(Graph(), config ?? new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 50 },
                library, Array.Empty<string>(), enemies, seed);

        private static EnemyDef Attacker(int attack = 8) => new("靶", Element.Heart, 200, attack);

        // ---- 命中判定的确定性两端 ----

        [Test]
        public void NoBlindNoDodge_AlwaysHits()
        {
            var engine = Engine(Array.Empty<string>(), new[] { Attacker() });
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(42), "无致盲无闪避:必中");
        }

        [Test]
        public void FullBlind_AlwaysMisses()
        {
            var engine = Engine(new[] { "昏" }, new[] { Attacker() });
            engine.Cast("昏", 0);
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(50), "致盲 100% → 必空");
        }

        [Test]
        public void FullDodge_SummonAlwaysMisses()
        {
            var engine = Engine(new[] { "闪" }, new[] { Attacker() });
            engine.Cast("闪");
            // 闪避 50% 不是确定性的,这里只验证「有闪避时会走摇点路径」——
            // 确定性断言交给下面 BlindPlusDodge 那条(合计 ≥ 100)
            Assert.That(engine.Summons[0].Passive.Dodge, Is.EqualTo(50));
        }

        [Test]
        public void BlindPlusDodge_ClampsHitRateAtZero_NotNegative()
        {
            // 致盲 100 + 闪避 50 = 命中率 −50。若只钳致盲不钳最终值,
            // _random.Next(100) 永远不小于负数,反而变成**必中** —— 正是这条要拦的
            var engine = Engine(new[] { "闪", "昏" }, new[] { Attacker() });
            engine.Cast("闪");
            engine.Cast("昏", 0);
            engine.EndTurn();
            Assert.That(engine.Summons[0].Hp, Is.EqualTo(10), "召唤物一点没掉血");
        }

        [Test]
        public void BlindAll_HitsEveryEnemy()
        {
            var engine = Engine(new[] { "雾" }, new[] { Attacker(), Attacker() });
            engine.Cast("雾", 0);
            foreach (var enemy in engine.Enemies)
                Assert.That(enemy.Statuses.TotalMagnitude(StatusKind.Blind), Is.EqualTo(100));
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(50), "两只都打空");
        }

        [Test]
        public void Blind_ExpiresAfterItsTurns()
        {
            // 昏 是 turns 2:挂上那回合的状态递减会减 1,所以只挡下一个敌方回合
            var engine = Engine(new[] { "昏" }, new[] { Attacker() });
            engine.Cast("昏", 0);
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(50), "第 1 个敌方回合被挡空");
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(42), "致盲到期,第 2 个敌方回合吃满");
        }

        [Test]
        public void Miss_EmitsMissedEvent()
        {
            var engine = Engine(new[] { "昏" }, new[] { Attacker() });
            engine.Cast("昏", 0);
            engine.EndTurn();
            Assert.That(engine.LastEvents.Any(e => e.Kind == BattleEventKind.Missed), Is.True,
                "打空要发事件 —— 没反馈玩家只会以为敌人这回合没动");
        }

        [Test]
        public void Miss_DoesNotConsumeImmunityOrShield()
        {
            // 打空 = 什么都没发生:免疫层数不掉、护盾不掉。免疫是稀缺资源,
            // 被一记本来就打不中的攻击吃掉是最亏的
            var engine = Engine(new[] { "御", "挡", "昏" }, new[] { Attacker(attack: 8) });
            engine.Cast("御", 0);
            engine.Cast("挡", 0);
            engine.Cast("昏", 0);
            int shieldBefore = engine.PlayerShield;
            Assert.That(shieldBefore, Is.GreaterThan(0), "护盾字得真生效了这条才有判别力");
            engine.EndTurn();
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.Immunity), Is.EqualTo(1),
                "免疫层数一点没掉");
            Assert.That(engine.PlayerShield, Is.EqualTo(shieldBefore), "护盾一点没掉");
        }

        [Test]
        public void Blind_DoesNotStopToppleShieldBreak()
        {
            // 刻意的裁定:致盲让「伤害」落空,但掀盾不经 DamagePlayerDirect,打不空。
            // 与免疫同口径(免疫也挡不住掀盾)
            var boss = new EnemyDef("覆", Element.Heart, 300, 4,
                phases: new[] { new BossPhaseDef("覆", Element.Heart, 300, 4, skill: BossSkill.Topple) });
            var config = new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 200, BossChargeEvery = 1 };
            var engine = Engine(new[] { "挡", "昏" }, new[] { boss }, config);
            engine.Cast("挡", 0);
            engine.EndTurn();                       // Boss 蓄力
            engine.Cast("昏", 0);                    // 致盲 100%
            engine.EndTurn();                       // 释放倾覆
            Assert.That(engine.PlayerShield, Is.EqualTo(0), "掀盾照常发生");
            Assert.That(engine.PlayerHp, Is.EqualTo(200), "但伤害那一记被打空");
        }

        [Test]
        public void Blind_DoesNotStopDevour()
        {
            // 吞噬直接置 0 血、不经 DamageSummon —— 「无视血量必杀」不受命中判定影响
            var boss = new EnemyDef("噬", Element.Heart, 300, 4,
                phases: new[] { new BossPhaseDef("噬", Element.Heart, 300, 4, skill: BossSkill.Devour) });
            var config = new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 200, BossChargeEvery = 1 };
            var engine = Engine(new[] { "素", "昏" }, new[] { boss }, config);
            engine.Cast("素");
            engine.EndTurn();                       // Boss 蓄力
            engine.Cast("昏", 0);
            engine.EndTurn();                       // 释放吞噬
            Assert.That(engine.Summons[0].Alive, Is.False, "秒杀打不空");
        }

        [Test]
        public void Dodge_SurvivesSaveRoundTrip()
        {
            // 闪避走 SummonPassive 的存档路径(子项目 C 已铺好 Clone + Capture/Restore),
            // 这条钉住新字段真的跟着走了 —— 漏进 Clone() 的话跨战斗就白闪了
            var engine = Engine(new[] { "闪" }, new[] { Attacker(attack: 0) });
            engine.Cast("闪");

            var meta = new MetaState { Endless = new EndlessSaveState { Depth = 3, PlayerHp = 40, Seed = 7 } };
            foreach (var summon in engine.Summons) meta.Endless.CarriedSummons.Add(summon.Capture());
            var restored = Data.SaveSerializer.FromJson(Data.SaveSerializer.ToJson(meta));

            var revived = new BattleEngine(Graph(),
                new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 50 },
                Array.Empty<string>(), Array.Empty<string>(),
                new[] { Attacker(attack: 0) }, seed: 1,
                startingSummons: restored.Endless.CarriedSummons);
            Assert.That(revived.Summons[0].Passive.Dodge, Is.EqualTo(50));
        }

        // ---- 随机流不被污染 ----

        [Test]
        public void NoBlindNoDodge_DoesNotConsumeRandom()
        {
            // 命中率 100 时必须短路不摇。_random 的唯一既有消费方是回合掉字,
            // 多摇一次就会平移掉落序列,让所有依赖种子的既有测试全红。
            // 用同一颗种子跑两场:一场有致盲一场没有,掉落序列应当只在有致盲时才分叉。
            var config = new BattleConfig
            {
                DropTable = new[] { "木" }, PlayerMaxHp = 50,
                UnlockedChars = new[] { "素", "闪", "昏", "雾" }, DropsPerTurn = 1,
            };
            var plain = Engine(Array.Empty<string>(), new[] { Attacker(attack: 0) }, config, seed: 7);
            plain.EndTurn();
            plain.EndTurn();
            var dropsWithoutBlind = new List<string>(plain.Library);

            var reference = Engine(Array.Empty<string>(), new[] { Attacker(attack: 0) }, config, seed: 7);
            reference.EndTurn();
            reference.EndTurn();
            Assert.That(reference.Library, Is.EqualTo(dropsWithoutBlind),
                "同种子同操作必须掉一样的字 —— 不一样说明命中判定偷摇了随机数");
        }
```

⚠ `engine.Library` / `engine.LastEvents` / `BattleConfig.UnlockedChars` / `DropsPerTurn` 的确切名字以仓库现状为准 —— 打开 `BattleEngine.cs` 与 `BattleConfig` 确认后照抄。`Attacker(attack: 0)` 是为了让掉落测试不受伤害干扰。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:编译失败,`StatusKind` 没有 `Blind`、`SummonPassive` 没有 `Dodge`、`EffectKind` 没有 `Blind`。

- [ ] **Step 3: 三个枚举/字段**

`Core/StatusEffect.cs`,`StatusKind` 在 `Immunity` 之后**追加**:

```csharp
        Immunity,         // 免疫:完全挡下 Magnitude 次伤害(2026-08-06)
        Blind,            // 致盲:该敌人攻击的命中率 −Magnitude%(2026-08-07)
```

类注释末尾补 `Blind=命中降低百分比(AttackHits 读它)`。

`Core/SummonPassive.cs`,在 `OnHitCurse` 之后加:

```csharp
        /// <summary>被攻击时的闪避百分比(2026-08-07,柳)。与攻击者的致盲相加后
        /// 一起从命中率里扣。</summary>
        public int Dodge { get; set; }
```

`Clone()` 里补 `Dodge = Dodge,`。

`Core/EffectDef.cs`,`EffectKind` 在 `Revive` 之后**追加**:

```csharp
        Blind,        // 致盲:目标命中率 −Value%,持续 Turns 回合;TargetAll = 全体(2026-08-07)
```

`Core/BattleEngine.cs` 的 `BattleEventKind` 末尾**追加**:

```csharp
        Missed,      // 攻击被打空(TargetIndex = 攻击者敌人下标,SecondIndex = 被打空的召唤物下标,玩家为 −1;2026-08-07)
```

- [ ] **Step 4: `AttackHits` 辅助**

加在 `DamagePlayerDirect` 上方:

```csharp
        /// <summary>命中判定(2026-08-07):命中率 = 100 − 攻击者致盲 − 目标闪避,钳到 [0,100]。
        ///
        /// **钳的是最终命中率,不是单项** —— 致盲 100 + 闪避 50 若只钳致盲就会算出 −50,
        /// 而 _random.Next(100) 永远不小于负数,反而变成必中。
        ///
        /// **命中率 ≥ 100 时直接返回,一次随机都不摇** —— _random 的唯一既有消费方是
        /// StartTurn 的回合掉字,无条件摇会平移掉落序列,让所有依赖种子的既有测试全红。
        /// 既有战斗里没有任何致盲/闪避,于是走的都是这条短路,行为逐位不变。</summary>
        private bool AttackHits(int enemyIndex, int dodgePercent)
        {
            int blind = _enemies[enemyIndex].Statuses.TotalMagnitude(StatusKind.Blind);
            int hitRate = Math.Clamp(100 - blind - dodgePercent, 0, 100);
            if (hitRate >= 100) return true;
            return _random.Next(100) < hitRate;
        }
```

- [ ] **Step 5: 两处插入**

`DamagePlayerDirect` 的**最开头**(在免疫之前):

```csharp
            // 命中判定(2026-08-07):打空则什么都不发生 —— 免疫不消耗、护盾不掉、反弹不触发。
            // 玩家没有闪避,只吃攻击者的致盲
            if (!AttackHits(enemyIndex, 0))
            {
                _events.Add(new BattleEvent(BattleEventKind.Missed, enemyIndex, 0));
                return;
            }
```

`DamageSummon` 的**最开头**:

```csharp
            var summon = _summons[summonIndex];
            // 命中判定(2026-08-07):召唤物的闪避与攻击者的致盲一起从命中率里扣
            if (!AttackHits(enemyIndex, summon.Passive?.Dodge ?? 0))
            {
                _events.Add(new BattleEvent(BattleEventKind.Missed, enemyIndex, 0, summonIndex));
                return;
            }
```

(原本的 `var summon = _summons[summonIndex];` 上移到判定之前,不要留两份。)

- [ ] **Step 6: 施加致盲**

`ApplyEffects` 的 `switch` 里,`case EffectKind.Revive:` 之后加:

```csharp
                    case EffectKind.Blind:
                        // SourceId 用字 ID:同字再出只刷新,不无限叠命中惩罚
                        if (effect.TargetAll)
                        {
                            int blindCount = _enemies.Count; // 分裂产生的新怪不吃同一发(与 DamageAll 同口径)
                            for (int i = 0; i < blindCount; i++)
                                if (_enemies[i].Alive) ApplyBlind(i, value, effect.Turns, def.Id);
                        }
                        else if (targetIndex >= 0 && _enemies[targetIndex].Alive)
                        {
                            ApplyBlind(targetIndex, value, effect.Turns, def.Id);
                        }
                        break;
```

在 `ApplyBurn` 旁边加:

```csharp
        /// <summary>给一名敌人挂致盲。TurnsLeft 直接用配置的回合数 —— 致盲是玩家在自己回合
        /// 挂上的,不像 Boss 倾覆那样在敌方段挂(那种要 +1 才能熬过同回合的状态递减)。</summary>
        private void ApplyBlind(int enemyIndex, int percent, int turns, string sourceId)
        {
            _enemies[enemyIndex].Statuses.Apply(new StatusEffect
            {
                Kind = StatusKind.Blind, Polarity = StatusPolarity.Debuff,
                Magnitude = percent, TurnsLeft = turns, SourceId = sourceId,
            });
        }
```

- [ ] **Step 7: 补 `NeedsTarget`**

⚠ **这条不能漏**。`NeedsTarget` 的白名单决定 UI 要不要让玩家选目标;漏掉的单体效果会拿到
`targetIndex = -1`。上个子项目正因为漏了 `Dispel` 导致「出灭必崩」。

在 `NeedsTarget` 里补:

```csharp
|| (effect.Kind == EffectKind.Blind && !effect.TargetAll)
```

**补测试**:`Cast("昏")` **不传目标下标**,断言不抛异常且致盲挂上了。

- [ ] **Step 8: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿,**686 + 新增全部通过,既有测试一条不红**。若有既有测试红了,八成是随机流被污染 —— 回头查 Step 4 的短路。

- [ ] **Step 9: 提交**

```bash
git add Brushblade/Assets/_Project/Core/StatusEffect.cs \
        Brushblade/Assets/_Project/Core/SummonPassive.cs \
        Brushblade/Assets/_Project/Core/EffectDef.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/DamageVariantTests.cs
git commit -m "feat(core): 命中判定与致盲/闪避——命中率满格时短路不摇,不污染掉落随机流"
```

⚠ 新建的 `.cs` 需要 Unity 生成 `.meta`,Unity 没开时不会生成。**不要手写 `.meta`,也不要 `git add` 未跟踪的 `.meta`。**

---

### Task 2: 沉默

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/StatusEffect.cs`(`StatusKind` 加 `Silence`)
- Modify: `Brushblade/Assets/_Project/Core/EffectDef.cs`(`EffectKind` 加 `Silence`)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`ResolveBossTurn` + 五处能力判断 + `ApplyEffects` 一个 case + `NeedsTarget`)
- Modify: `Brushblade/Assets/_Project/Tests/DamageVariantTests.cs`

**Interfaces:**
- Produces:`StatusKind.Silence`、`EffectKind.Silence`(`Value` 不用,`Turns` 定回合);私有 `bool IsSilenced(EnemyState enemy)`

- [ ] **Step 1: 写失败测试**

`Graph()` 里补一个字:

```csharp
            // 禁:单体沉默 2 回合(锁)
            new CharDef("禁", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.Silence, 0, turns: 2) }),
```

追加测试:

```csharp
        // ---- 沉默 ----

        [Test]
        public void Silence_StopsScorchSelfBuff()
        {
            var engine = Engine(new[] { "禁" }, new[] { new EnemyDef("焦", Element.Heart, 200, 4, EnemyAbility.Scorch) });
            engine.Cast("禁", 0);
            int attackBefore = engine.Enemies[0].Attack;
            engine.EndTurn();  // 召唤物没有,敌人直接打玩家;焦痕本该在受击时自燃 —— 但它这回合没受击
            Assert.That(engine.Enemies[0].Attack, Is.EqualTo(attackBefore), "沉默中不自燃");
        }

        [Test]
        public void Silence_StopsRegrow()
        {
            var engine = Engine(new[] { "禁" }, new[] { new EnemyDef("缺", Element.Heart, 200, 0, EnemyAbility.Regrow) });
            engine.Cast("禁", 0);
            int hpBefore = engine.Enemies[0].Hp;
            int attackBefore = engine.Enemies[0].Attack;
            engine.EndTurn();
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(hpBefore), "沉默中不补全");
            Assert.That(engine.Enemies[0].Attack, Is.EqualTo(attackBefore));
        }

        [Test]
        public void Silence_StopsSear()
        {
            var engine = Engine(new[] { "禁" }, new[] { new EnemyDef("灯", Element.Heart, 200, 3, EnemyAbility.Sear) });
            engine.Cast("禁", 0);
            engine.EndTurn();
            Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.Burn), Is.EqualTo(0),
                "沉默中不挂灼烧");
        }

        [Test]
        public void Silence_StopsSplit()
        {
            var engine = Engine(new[] { "禁", "凿" }, new[] { new EnemyDef("叠", Element.Heart, 200, 0, EnemyAbility.Split) });
            engine.Cast("禁", 0);
            engine.Cast("凿", 0);   // 受击存活,本该分裂
            Assert.That(engine.Enemies.Count, Is.EqualTo(1), "沉默中不分裂");
        }

        [Test]
        public void Silence_SilencedBuffMinionAttacksInstead()
        {
            // 标点小妖有同伴时用加攻代替出手。被沉默后加攻哑火,它就该**改为亲自攻击**,
            // 而不是站着什么都不做 —— 沉默压的是「能力」,不是「行动」
            var engine = Engine(new[] { "禁" }, new[]
            {
                new EnemyDef("标", Element.Heart, 200, 3, EnemyAbility.Buff),
                new EnemyDef("伴", Element.Heart, 200, 0),
            });
            engine.Cast("禁", 0);
            int companionAttack = engine.Enemies[1].Attack;
            engine.EndTurn();
            Assert.That(engine.Enemies[1].Attack, Is.EqualTo(companionAttack), "同伴没被加攻");
            Assert.That(engine.PlayerHp, Is.EqualTo(47), "标点小妖改为亲自出手,打了 3");
        }

        [Test]
        public void Silence_CancelsBossChargeAndResetsCounter()
        {
            var boss = new EnemyDef("覆", Element.Heart, 300, 4,
                phases: new[] { new BossPhaseDef("覆", Element.Heart, 300, 4, skill: BossSkill.Topple) });
            var config = new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 200, BossChargeEvery = 1 };
            var engine = Engine(new[] { "禁" }, new[] { boss }, config);
            engine.EndTurn();                       // Boss 进入蓄力
            Assert.That(engine.Enemies[0].IsCharging, Is.True);

            engine.Cast("禁", 0);
            engine.EndTurn();                       // 沉默 → 蓄力取消,不放大招

            Assert.That(engine.Enemies[0].IsCharging, Is.False, "蓄力被取消");
            Assert.That(engine.Enemies[0].ChargeCounter, Is.EqualTo(0), "计数清零,解锁后从头攒");
            Assert.That(engine.PlayerStatuses.Has(StatusKind.Seal), Is.False, "倾覆没放出来");
        }

        [Test]
        public void Silence_DoesNotAffectDisguiseOrObscure()
        {
            // 通假/生僻是信息隐藏,不是主动机制 —— 锁一下就看穿了不符合「锁」的语义
            var engine = Engine(new[] { "禁" }, new[] { new EnemyDef("通", Element.Wood, 200, 3, EnemyAbility.Disguise) });
            engine.Cast("禁", 0);
            engine.EndTurn();
            Assert.That(engine.Enemies[0].ApparentElement, Is.EqualTo(engine.Enemies[0].Element),
                "首次行动后照常现形");
        }
```

⚠ `EnemyState.IsCharging` / `ChargeCounter` 是不是公开可读以仓库现状为准。若读不到,改成断言可观测的后果(大招没放出来 = 玩家没被扣 AP / 没掉双倍血),**不要为测试放宽可见性**。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:编译失败,`StatusKind` / `EffectKind` 都没有 `Silence`。

- [ ] **Step 3: 两个枚举 + 辅助**

`Core/StatusEffect.cs`,`StatusKind` 在 `Blind` 之后**追加**:

```csharp
        Silence,          // 沉默:该敌人的主动机制全部哑火(2026-08-07)
```

`Core/EffectDef.cs`,`EffectKind` 在 `Blind` 之后**追加**:

```csharp
        Silence,      // 沉默:目标的主动机制哑火,持续 Turns 回合(Value 不用,2026-08-07)
```

`Core/BattleEngine.cs` 加辅助(放在 `HasOtherAliveEnemy` 旁边):

```csharp
        /// <summary>该敌人是否被沉默(2026-08-07,锁)。压的是**主动机制** ——
        /// Boss 大招、缺笔妖补全、叠字分裂、标点加攻、焦痕自燃、灯花灼身。
        /// 通假/生僻不在其列:那两个是信息隐藏,锁一下就看穿了不符合「锁」的语义。</summary>
        private static bool IsSilenced(EnemyState enemy) => enemy.Statuses.Has(StatusKind.Silence);
```

- [ ] **Step 4: 五处能力判断 + Boss**

`ResolveBossTurn` 的**最开头**加:

```csharp
            // 沉默(2026-08-07):锁住的是「正在攒的那一下」——蓄力当场取消、计数清零,
            // 解锁后从头攒,而不是解锁即放
            if (IsSilenced(enemy))
            {
                enemy.IsCharging = false;
                enemy.ChargeCounter = 0;
                return false; // 交回普攻
            }
```

其余五处,各自把条件补上 `&& !IsSilenced(enemy)`:

| 行为 | 现有条件 | 改成 |
|---|---|---|
| 标点加攻 | `enemy.Def.Ability != EnemyAbility.Buff) continue;` | `enemy.Def.Ability != EnemyAbility.Buff \|\| IsSilenced(enemy)) continue;` |
| 标点让位 | `enemy.Def.Ability == EnemyAbility.Buff && HasOtherAliveEnemy(enemy)` | `enemy.Def.Ability == EnemyAbility.Buff && !IsSilenced(enemy) && HasOtherAliveEnemy(enemy)` |
| 灯花灼身 | `enemy.Def.Ability == EnemyAbility.Sear` | `enemy.Def.Ability == EnemyAbility.Sear && !IsSilenced(enemy)` |
| 缺笔补全 | `enemy.Def.Ability != EnemyAbility.Regrow \|\| enemy.RegrowProgress >= 3) continue;` | `enemy.Def.Ability != EnemyAbility.Regrow \|\| IsSilenced(enemy) \|\| enemy.RegrowProgress >= 3) continue;` |
| 焦痕自燃 | `enemy.Def.Ability == EnemyAbility.Scorch` | `enemy.Def.Ability == EnemyAbility.Scorch && !IsSilenced(enemy)` |
| 叠字分裂 | `enemy.Def.Ability == EnemyAbility.Split && !enemy.HasSplit && ...` | `enemy.Def.Ability == EnemyAbility.Split && !IsSilenced(enemy) && !enemy.HasSplit && ...` |

⚠ **「标点让位」那条最容易做错**。它现在的语义是「有同伴时用加攻代替出手」。若只改了「加攻」那处而不改这处,被沉默的标点小妖会**既不加攻也不出手**,站着发呆 —— 沉默压的是能力不是行动,它应当改为亲自攻击。上表里两处都要改。

- [ ] **Step 5: 施加沉默**

`ApplyEffects` 的 `switch` 里,`case EffectKind.Blind:` 之后加:

```csharp
                    case EffectKind.Silence:
                        if (targetIndex >= 0 && _enemies[targetIndex].Alive)
                            _enemies[targetIndex].Statuses.Apply(new StatusEffect
                            {
                                Kind = StatusKind.Silence, Polarity = StatusPolarity.Debuff,
                                Magnitude = 1, TurnsLeft = effect.Turns, SourceId = def.Id,
                            });
                        break;
```

`NeedsTarget` 补 `|| effect.Kind == EffectKind.Silence`(沉默只有单体版,不需要排除 `TargetAll`)。

- [ ] **Step 6: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿。**特别确认既有的 `EnemyAbilityTests` 与 `BossSkillTests` 没被打破** —— 无沉默时 `IsSilenced` 恒 false,六处条件的取值完全不变。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/StatusEffect.cs \
        Brushblade/Assets/_Project/Core/EffectDef.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/DamageVariantTests.cs
git commit -m "feat(core): 沉默——Boss 蓄力取消、五种字怪能力哑火,标点改为亲自出手"
```

---

### Task 3: 反弹

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/StatusEffect.cs`(`StatusKind` 加 `Reflect`)
- Modify: `Brushblade/Assets/_Project/Core/EffectDef.cs`(`EffectKind` 加 `Reflect`)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`DamagePlayerDirect` 末尾 + `ApplyEffects` 一个 case)
- Modify: `Brushblade/Assets/_Project/Tests/DamageVariantTests.cs`

**Interfaces:**
- Consumes:Task 1 的 `AttackHits`(打空时不反弹)
- Produces:`StatusKind.Reflect`(`Magnitude` = 反弹百分比,`Polarity = Buff`)、`EffectKind.Reflect`

- [ ] **Step 1: 写失败测试**

`Graph()` 里补:

```csharp
            // 映:反弹 50%,2 回合(镜)
            new CharDef("映", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.Reflect, 50, turns: 2) }),
            // 挡(护盾 20)与 御(免疫 1 次)在 Task 1 已加进 Graph(),这里直接用
```

追加测试:

```csharp
        // ---- 反弹 ----

        [Test]
        public void Reflect_SendsBackHalfTheDamage()
        {
            var engine = Engine(new[] { "映" }, new[] { Attacker(attack: 8) });
            engine.Cast("映", 0);
            int enemyHpBefore = engine.Enemies[0].Hp;
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(42), "照常挨 8");
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(enemyHpBefore - 4), "反弹 8 的 50% = 4");
        }

        [Test]
        public void Reflect_UsesTotalDamage_NotHpActuallyLost()
        {
            // 护盾吸掉的部分也照样照回去 —— 「镜」是把东西原样反射,不管你挡没挡住。
            // 与召唤物 荆 的反伤同口径(被打死的那一击也照样扎)
            var engine = Engine(new[] { "挡", "映" }, new[] { Attacker(attack: 8) });
            engine.Cast("挡", 0);
            engine.Cast("映", 0);
            int enemyHpBefore = engine.Enemies[0].Hp;
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(50), "8 点全被 20 点护盾吃掉");
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(enemyHpBefore - 4), "仍按总伤害 8 反弹 4");
        }

        [Test]
        public void Reflect_DoesNotFireWhenImmunityBlocks()
        {
            // 免疫是完全挡下,压根没吃到那记伤害 —— 没吃到就没得反
            var engine = Engine(new[] { "御", "映" }, new[] { Attacker(attack: 8) });
            engine.Cast("御", 0);
            engine.Cast("映", 0);
            int enemyHpBefore = engine.Enemies[0].Hp;
            engine.EndTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(50));
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(enemyHpBefore), "免疫挡下 → 不反弹");
        }

        [Test]
        public void Reflect_DoesNotFireWhenAttackMisses()
        {
            var engine = Engine(new[] { "昏", "映" }, new[] { Attacker(attack: 8) });
            engine.Cast("昏", 0);
            engine.Cast("映", 0);
            int enemyHpBefore = engine.Enemies[0].Hp;
            engine.EndTurn();
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(enemyHpBefore), "打空 → 不反弹");
        }

        [Test]
        public void Reflect_KillingLastEnemy_WinsTheBattle()
        {
            // 反弹与召唤物反伤同型:会在敌方回合里杀敌。子项目 C 为反伤补的
            // 「敌方段后判胜」在这里直接受益 —— 这条钉住它确实覆盖了反弹
            var engine = Engine(new[] { "映" }, new[] { new EnemyDef("靶", Element.Heart, 4, 8) });
            engine.Cast("映", 0);
            engine.EndTurn();
            Assert.That(engine.Enemies[0].Alive, Is.False);
            Assert.That(engine.Phase, Is.EqualTo(BattlePhase.Won));
        }
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:编译失败,`EffectKind` 没有 `Reflect`。

- [ ] **Step 3: 两个枚举**

`Core/StatusEffect.cs`,`StatusKind` 在 `Silence` 之后**追加**:

```csharp
        Reflect,          // 反弹:把打到玩家的伤害按 Magnitude% 照回攻击者(2026-08-07)
```

`Core/EffectDef.cs`,`EffectKind` 在 `Silence` 之后**追加**:

```csharp
        Reflect,      // 反弹:受到的伤害按 Value% 照回攻击者,持续 Turns 回合(2026-08-07)
```

- [ ] **Step 4: `DamagePlayerDirect` 末尾结算反弹**

在 `_events.Add(new BattleEvent(BattleEventKind.EnemyAttack, ...))` **之后**加:

```csharp
            // 反弹(2026-08-07,镜):按**打过来的总伤害**照回去,不是按实际掉血 ——
            // 护盾吸掉的那部分也照样反。「镜」是把东西原样反射,不管你挡没挡住,
            // 与召唤物 荆 的反伤同口径(被打死的那一击也照样扎)。
            // 命中判定打空与免疫完全挡下都在方法更早处 return 了,走不到这里 —— 没吃到就没得反。
            // attacker 传 Element.Heart:心对全属性都是 1.0x,等价于「不走生克」。
            int reflect = _playerStatuses.TotalMagnitude(StatusKind.Reflect);
            if (reflect > 0 && _enemies[enemyIndex].Alive)
            {
                int bounced = damage * reflect / 100;
                if (bounced > 0)
                    DamageEnemy(enemyIndex, bounced, Array.Empty<Element>(), Element.Heart);
            }
```

- [ ] **Step 5: 施加反弹**

`ApplyEffects` 的 `switch` 里,`case EffectKind.Silence:` 之后加:

```csharp
                    case EffectKind.Reflect:
                        _playerStatuses.Apply(new StatusEffect
                        {
                            Kind = StatusKind.Reflect, Polarity = StatusPolarity.Buff,
                            Magnitude = value, TurnsLeft = effect.Turns, SourceId = def.Id,
                        });
                        break;
```

反弹是给自己上的增益,**不需要动 `NeedsTarget`**。

- [ ] **Step 6: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/StatusEffect.cs \
        Brushblade/Assets/_Project/Core/EffectDef.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/DamageVariantTests.cs
git commit -m "feat(core): 反弹——按总伤害照回,护盾吸掉的也反,免疫挡下则不反"
```

---

### Task 4: 多段

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EffectDef.cs`(加 `HitCount` 字段 + 构造参数)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`ApplyEffects` 的 `DamageSingle` case)
- Modify: `Brushblade/Assets/_Project/Tests/DamageVariantTests.cs`

**Interfaces:**
- Produces:`EffectDef.HitCount`(int,默认 1);构造函数尾部追加 `int hitCount = 1`

- [ ] **Step 1: 写失败测试**

`Graph()` 里补:

```csharp
            // 斫:10 伤 ×2 段(剁)
            new CharDef("斫", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.DamageSingle, 10, hitCount: 2) }),
            // 斩:20 伤,HP<25% 且非 Boss 直接击杀(用来验多段与斩杀的交互)
            new CharDef("斩", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.DamageSingle, 10, hitCount: 2,
                    executeBelowPercent: 25, executeKills: true) }),
            // 凿(1 点伤害)在 Task 1 已加进 Graph(),这里直接用
```

追加测试:

```csharp
        // ---- 多段 ----

        [Test]
        public void MultiHit_DealsEachSegmentSeparately()
        {
            var engine = Engine(new[] { "斫" }, new[] { new EnemyDef("靶", Element.Heart, 200, 0) });
            engine.Cast("斫", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(180), "10 × 2 段");
            Assert.That(engine.LastEvents.Count(e => e.Kind == BattleEventKind.Damage), Is.EqualTo(2),
                "两段各发一条伤害事件——一拍打完玩家看不出是两段");
        }

        [Test]
        public void MultiHit_StopsWhenTargetDies()
        {
            // 12 血靶:第一段 10 剩 2,第二段打死;若目标在第一段就死了,剩余段数不该对尸体发事件
            var engine = Engine(new[] { "斫" }, new[] { new EnemyDef("靶", Element.Heart, 8, 0) });
            engine.Cast("斫", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(0));
            Assert.That(engine.LastEvents.Count(e => e.Kind == BattleEventKind.Damage), Is.EqualTo(1),
                "第一段就打死了,第二段停手");
        }

        [Test]
        public void MultiHit_SecondSegmentCanTriggerExecute()
        {
            // 上限 100、磨到 30:第一段 10 伤 → 20(20% < 25%),第二段判血命中阈值 → 直接击杀。
            // 「打之前判血」是每段各自判,所以这是真会发生的涌现
            var library = new List<string>(Enumerable.Repeat("凿", 70)) { "斩" };
            var engine = new BattleEngine(Graph(),
                new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 200, ApPerTurn = 200 },
                library, Array.Empty<string>(),
                new[] { new EnemyDef("靶", Element.Heart, 100, 0) }, seed: 1);
            for (int i = 0; i < 70; i++) engine.Cast("凿", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(30), "磨血辅助本身没磨准");

            engine.Cast("斩", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(0), "第二段处决");
            Assert.That(engine.Phase, Is.EqualTo(BattlePhase.Won));
        }

        [Test]
        public void MultiHit_EachSegmentGoesThroughArmorBreakSeparately()
        {
            // 破甲让承伤 +25%。两段各自过一次,所以 10→12 两次 = 24,而不是「20 整体 +25% = 25」
            var graph = new RecipeGraph(new[]
            {
                new CharDef("木", Element.Wood),
                new CharDef("裂", Element.Heart,
                    effects: new[] { new EffectDef(EffectKind.ArmorBreak, 3) }),
                new CharDef("斫", Element.Heart,
                    effects: new[] { new EffectDef(EffectKind.DamageSingle, 10, hitCount: 2) }),
            });
            var engine = new BattleEngine(graph,
                new BattleConfig { DropTable = new[] { "木" }, PlayerMaxHp = 50 },
                new[] { "裂", "斫" }, Array.Empty<string>(),
                new[] { new EnemyDef("靶", Element.Heart, 200, 0) }, seed: 1);
            engine.Cast("裂", 0);
            engine.Cast("斫", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(176), "floor(10×1.25) × 2 段 = 12 × 2 = 24");
        }

        [Test]
        public void HitCountDefaultsToOne_ExistingDamageUnchanged()
        {
            var engine = Engine(new[] { "凿" }, new[] { new EnemyDef("靶", Element.Heart, 200, 0) });
            engine.Cast("凿", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(199));
            Assert.That(engine.LastEvents.Count(e => e.Kind == BattleEventKind.Damage), Is.EqualTo(1));
        }
```

⚠ 磨血张数与 `ApPerTurn` 必须自洽,那条 `"磨血辅助本身没磨准"` 的断言就是哨兵 —— 它一红先查张数,别怀疑多段实现。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:编译失败,`EffectDef` 没有 `hitCount` 具名参数。

- [ ] **Step 3: `EffectDef` 加字段**

在 `ExecuteKills` 之后加:

```csharp
        /// <summary>伤害分几段打(剁 = 2)。默认 1。每段完全独立:各自过生克、破甲、穿甲,
        /// 也各自过斩杀的「打之前判血」——所以「第一段把敌人打进阈值、第二段触发处决」
        /// 是真会发生的涌现,不是 bug。</summary>
        public int HitCount { get; }
```

构造函数尾部追加 `int hitCount = 1`,并 `HitCount = hitCount <= 0 ? 1 : hitCount;`(0 或负数当 1,免得配置写错让字变成哑弹)。

- [ ] **Step 4: `DamageSingle` case 分段**

把

```csharp
                    case EffectKind.DamageSingle:
                        if (TryExecuteKill(effect, targetIndex)) break; // 处决:直接击杀,不再走伤害
                        DamageEnemy(targetIndex,
                            ExecuteBonus(effect, targetIndex, BaseValue(effect, value, _enemies[targetIndex])),
                            recipeElements, attacker, effect.IgnoreArmor);
                        break;
```

改成

```csharp
                    case EffectKind.DamageSingle:
                        // 多段(2026-08-07,剁):每段完全独立 —— 各自判存活、各自过斩杀阈值、
                        // 各自过生克与破甲。目标中途死了就停,不对尸体发事件
                        for (int hit = 0; hit < effect.HitCount; hit++)
                        {
                            if (!_enemies[targetIndex].Alive) break;
                            if (TryExecuteKill(effect, targetIndex)) break; // 处决:击杀后无需再打
                            DamageEnemy(targetIndex,
                                ExecuteBonus(effect, targetIndex, BaseValue(effect, value, _enemies[targetIndex])),
                                recipeElements, attacker, effect.IgnoreArmor);
                        }
                        break;
```

`DamageAll` **不做多段** —— 详表里没有多段 AOE 的字,做了就是 YAGNI。

- [ ] **Step 5: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:全绿。既有伤害测试不受影响(`HitCount` 默认 1,循环恰好跑一次)。

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Core/EffectDef.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/DamageVariantTests.cs
git commit -m "feat(core): 多段伤害——每段独立判存活与斩杀阈值,目标死了就停"
```

---

### Task 5: `ConfigLoader` 与管线的 `turns` / `targetAll`

**Files:**
- Modify: `Brushblade/Assets/_Project/Data/ConfigLoader.cs`(`EffectDto` 加 `HitCount`)
- Modify: `tools/pipeline/extract_values.py`
- Modify: `tools/pipeline/tests/test_export_chars.py`
- Modify: `Brushblade/Assets/_Project/Tests/ConfigLoaderTests.cs`

**Interfaces:**
- Consumes:Task 4 的 `EffectDef` 构造参数 `int hitCount = 1`
- Produces:`chars.json` 支持 `"hitCount": 2`;详表的 `(turns N)` / `targetAll` 对 `Blind`/`Silence`/`Reflect` 也生效

**注意**:三个新 `EffectKind` 与 `SummonPassive.Dodge` 都走 `Enum.TryParse` / Newtonsoft 自动绑定,**不需要改解析代码**。只有 `HitCount` 要手动接线。

- [ ] **Step 1: 写失败测试(管线侧)**

⚠ **这是本任务的重点。** `extract_values.py` 现在的 `turns` / `targetAll` 解析是**写死给 `HealOverTime` 的**:

```python
for effect in effects:
    if effect["kind"] == "HealOverTime":
        if turns: effect["turns"] = int(turns.group(1))
        if "targetAll" in config: effect["targetAll"] = True
```

`Blind` / `Silence` / `Reflect` 都要 `turns`,`Blind` 还要 `targetAll`。不扩这段,详表里写的
`(turns 2)` 会被**静默丢弃**,生成出来 `turns = 0` —— 状态挂上去当场到期,而且不报任何错。

追加到 `tools/pipeline/tests/test_export_chars.py`:

```python
def test_turns_applies_to_all_duration_kinds_not_just_hot():
    """Blind / Silence / Reflect 都要 turns —— 写死给 HealOverTime 会静默丢掉数值。"""
    from extract_values import _parse_effects
    assert _parse_effects("`Blind 50`(turns 2)", "火") == [
        {"kind": "Blind", "value": 50, "turns": 2}]
    assert _parse_effects("`Silence 0`(turns 1)", "金") == [
        {"kind": "Silence", "value": 0, "turns": 1}]
    assert _parse_effects("`Reflect 50`(turns 2)", "金") == [
        {"kind": "Reflect", "value": 50, "turns": 2}]


def test_blind_supports_target_all():
    from extract_values import _parse_effects
    assert _parse_effects("`Blind 30`(turns 1, targetAll)", "火") == [
        {"kind": "Blind", "value": 30, "turns": 1, "targetAll": True}]


def test_hit_count_token_attaches_to_damage():
    """剁 的分段数是伤害的修饰,不是独立效果。"""
    from extract_values import _parse_effects
    assert _parse_effects("`DamageSingle 10` + `HitCount 2`", "金") == [
        {"kind": "DamageSingle", "value": 10, "hitCount": 2}]


def test_suo_uses_manual_recipe_not_supplementary_plane_part():
    """锁 的 IDS 部件 𭕆(U+2D546)在增补平面,会让整字降级成叶子。"""
    from export_chars import MANUAL_RECIPES
    assert MANUAL_RECIPES["锁"] == ["钅", "贝"]
```

- [ ] **Step 2: 跑测试确认失败**

```bash
python3 -m pytest tools/pipeline/tests/ -q
```

预期:4 条新测试 FAIL。

- [ ] **Step 3: 扩 `extract_values` 的 `turns` / `targetAll`**

在常量区加:

```python
# 需要 turns 的 Kind(白名单):写死给 HealOverTime 会让新加的持续类状态静默丢掉回合数。
DURATION_KINDS = {"HealOverTime", "Blind", "Silence", "Reflect"}

# 支持 targetAll 的 Kind
TARGET_ALL_KINDS = {"HealOverTime", "Blind"}

# 分段数是伤害的修饰,不是独立效果(与 ExecuteKill / ExecuteBonus 同处理)
HIT_COUNT_TOKEN = "HitCount"
```

把

```python
    turns = re.search(r"turns (\d+)", config)
    for effect in effects:
        if effect["kind"] == "HealOverTime":
            if turns:
                effect["turns"] = int(turns.group(1))
            if "targetAll" in config:
                effect["targetAll"] = True
```

改成

```python
    turns = re.search(r"turns (\d+)", config)
    for effect in effects:
        if effect["kind"] in DURATION_KINDS:
            if turns:
                effect["turns"] = int(turns.group(1))
        if effect["kind"] in TARGET_ALL_KINDS and "targetAll" in config:
            effect["targetAll"] = True
```

并把 `HitCount` 加进既有的 `EXECUTE_TOKENS` 同款处理 —— 在通用循环里跳过它,再挂到伤害效果上:

```python
    hit_count = re.search(rf"`{HIT_COUNT_TOKEN} (\d+)`", config)
    if hit_count:
        for effect in effects:
            if effect["kind"].startswith("Damage"):
                effect["hitCount"] = int(hit_count.group(1))
```

通用循环里同样要 `if kind == HIT_COUNT_TOKEN: continue`,否则会多生成一个 `{"kind":"HitCount"}` 的假效果。

- [ ] **Step 4: `export_chars` 加人工配方**

`MANUAL_RECIPES` 里加:

```python
    # 锁 的 IDS 是 ⿰钅𭕆,𭕆(U+2D546)在增补平面 —— _blocked_smp_part 会把整字
    # 降级成不可拆的叶子。换成 贝,义也通(锁住财物)。
    "锁": ["钅", "贝"],
```

- [ ] **Step 5: 跑管线测试确认通过**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
```

预期:全绿。

- [ ] **Step 6: `EffectDto` 加 `HitCount`**

`Brushblade/Assets/_Project/Data/ConfigLoader.cs`,`EffectDto` 的 `ExecuteKills` 之后加:

```csharp
            public int HitCount { get; set; } = 1;  // 多段:伤害分几段打(剁 = 2)
```

`ParseEffects` 的构造调用尾部追加 `effect.HitCount`。

- [ ] **Step 7: 补 `ConfigLoaderTests`**

追加(照抄该文件既有的 `ConfigLoader.LoadGraph(json)` 风格):

```csharp
        [Test]
        public void LoadGraph_ParsesHitCountAndNewEffectKinds()
        {
            const string json = @"{""chars"":[
                {""id"":""木"",""element"":""Wood""},
                {""id"":""甲"",""element"":""Metal"",""effects"":[
                    {""kind"":""DamageSingle"",""value"":10,""hitCount"":2}]},
                {""id"":""乙"",""element"":""Fire"",""effects"":[
                    {""kind"":""Blind"",""value"":30,""turns"":1,""targetAll"":true}]},
                {""id"":""丙"",""element"":""Metal"",""effects"":[
                    {""kind"":""Silence"",""value"":0,""turns"":1}]},
                {""id"":""丁"",""element"":""Metal"",""effects"":[
                    {""kind"":""Reflect"",""value"":50,""turns"":2}]},
                {""id"":""戊"",""element"":""Wood"",""effects"":[
                    {""kind"":""Summon"",""value"":8,""count"":1,""attack"":3,""summonChar"":""木"",
                     ""passive"":{""dodge"":50}}]}
            ]}";
            var graph = ConfigLoader.LoadGraph(json);

            Assert.That(graph.Get("甲").Effects[0].HitCount, Is.EqualTo(2));
            var blind = graph.Get("乙").Effects[0];
            Assert.That(blind.Kind, Is.EqualTo(EffectKind.Blind));
            Assert.That(blind.Turns, Is.EqualTo(1));
            Assert.That(blind.TargetAll, Is.True);
            Assert.That(graph.Get("丙").Effects[0].Turns, Is.EqualTo(1));
            Assert.That(graph.Get("丁").Effects[0].Kind, Is.EqualTo(EffectKind.Reflect));
            Assert.That(graph.Get("戊").Effects[0].Passive.Dodge, Is.EqualTo(50));
        }
```

⚠ `ConfigLoaderTests.cs` 被 `tools/coretests` 工装**排除**(它引 `UnityEngine.Application`),这条测试**工装跑不到**,只能由用户在 Unity Test Runner 里跑。请在报告里明确写出这一点。工装跑得到的覆盖是 Task 6 的 `CharTableTests` 守卫。

- [ ] **Step 8: 全套测试**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

- [ ] **Step 9: 提交**

```bash
git add Brushblade/Assets/_Project/Data/ConfigLoader.cs \
        tools/pipeline/extract_values.py \
        tools/pipeline/export_chars.py \
        tools/pipeline/tests/test_export_chars.py \
        Brushblade/Assets/_Project/Tests/ConfigLoaderTests.cs
git commit -m "feat(data): turns/targetAll 改按 Kind 白名单;解析多段与锁的人工配方"
```

---

### Task 6: 详表落地六字,`钩` 移出,重新生成字表与字体

**Files:**
- Modify: `docs/design/字选型/技能机制详表.md`
- Modify: `Brushblade/Assets/_Project/Tests/CharTableTests.cs`
- Regenerate: `chars.json`、两个 `.ttf`、`tools/fonts/charset.txt`

**Interfaces:**
- Consumes:Task 5 的管线 token(`turns` / `targetAll` / `HitCount`)与 `MANUAL_RECIPES["锁"]`
- Produces:`chars.json` 多出 6 个字与 6 个叶子部件(`朵 竟 崔 因 卯 贝`)

- [ ] **Step 1: 改详表六行**

⚠ **列数必须与所在表的表头一致。** 管线的 `_parse_row` 靠 `len(cells) < 4` 与
`len(cells[0]) != 1` 判行,列错位不会报错、只会让抽出来的「效果配置」取错格。
上个子项目正因为行模板少一列返工过。**下面给的行已按各表真实列数写好,照抄,不要自己删格子。**

**火系「攻 · 单攻」表(6 列:字 | 稀 | 效果配置 | 相生 | 最终值 | 实现)** —— 原地改 `熣`:

```
| 熣 | 🟣紫 | `DamageSingle 16` + `Blind 50`(turns 2) | — | 单体 16 + 致盲 2 回合 | ✅ 数值由 21 降为 16:带了致盲还拿 21 会压死同档的 灿(18) |
```

**火系「攻 · 控制 / 召 · 器物 / 不沾」表(6 列)** —— 原地改 `烟`:

```
| 烟 | 🟢绿 | `Blind 30`(turns 1, targetAll) | — | 全体致盲 1 回合 | ✅ 30% 而非 50%:全体 50%/1 回合在 2 怪场就追平蓝档的 藤,绿卡不该盖蓝卡 |
```

**金系「攻 · 单攻」表(6 列)** —— 原地改 `剁`,并新增 `镜` / `锁`(照该表的稀有度顺序插):

```
| 剁 | 🟣紫 | `DamageSingle 10` + `HitCount 2` | — | 单体 20(分两段) | ✅ 每段独立过生克/破甲/斩杀阈值 |
| 锁 | 🔵蓝 | `Silence 0`(turns 1) | — | 沉默 1 回合 | ✅ Boss 蓄力取消 + 五种字怪能力哑火 |
| 镜 | 🔵蓝 | `Reflect 50`(turns 2) | — | 反弹 50%,2 回合 | ✅ 按总伤害反,护盾吸掉的也反 |
```

**木系「召 · 单体 · 带被动」表(5 列:字 | 稀 | 效果配置 | 最终值 | 实现)** —— 原地改 `柳`:

```
| 柳 | 🟢绿 | `Summon 1`(8 血/攻 3)+ `Dodge 50` | 1 只,50% 闪避 | ✅ 数值由 12/4 降为 8/3:闪避把期望有效血翻倍,拿原值会让同档的 松(12 血/攻 3)没有出场理由 |
```

⚠ 木系召唤表的列数与火/金系不同(它没有「相生」列)—— **改之前先看那张表的表头有几列**,
上面这行是按 5 列写的,若实际不是 5 列就照实际的补/删。

**改完用这条自检,必须没有输出:**

```bash
python3 - <<'PY'
lines=open('docs/design/字选型/技能机制详表.md',encoding='utf-8').read().split('\n')
hdr=None
for i,l in enumerate(lines):
    if l.startswith('| 字 |'): hdr=len(l.split('|'))-2
    elif l.startswith('|') and not l.startswith('|---') and len(l.split('|')[1].strip())!=1: hdr=None
    if hdr and l.startswith('| ') and not l.startswith('|---') and not l.startswith('| 字 |'):
        n=len(l.split('|'))-2
        if n!=hdr: print(f"行{i+1} 列数{n} ≠ 表头{hdr}: {l[:50]}")
PY
```

- [ ] **Step 2: `钩` 移出字表**

找到 `钩` 那一行(金系表里,现在是 `⚠ Pull`),把它改成明确的暂缓标记 —— **保留行但不给 ✅**,
这样管线不会抽它:

```
| 钩 | 🟢绿 | 把后排拉到前排 | — | — | ⚠ **模型缺口**:引擎里敌人是平表、无排位概念,当前战斗模型下无从实现 |
```

在 §7.4「建议暂缓入池」的表里加一行:

```
| 金 | 钩 | 无降级方案 | 模型缺口:敌人无排位概念,「拉到前排」无从实现 |
```

并在该节说明段补一句:

```
2026-08-07:`钩` 与名单里其余「降级后同质」的字性质不同 —— 它不是数值撞车而是**模型缺口**
(引擎里敌人是一个平表,「前排」只对召唤物存在)。补数值放不进来,要动战斗模型才行。
```

- [ ] **Step 3: 更新 §7.2**

```
| ~~P3~~ ✅ | `Blind` / `Silence` / `Reflect` | 4 | 熣 烟 锁 镜 —— 已实现(2026-08-07;钩 为模型缺口,移出字表) |
| ~~P3~~ ✅ | `MultiHit` | 1 | 剁 —— 已实现(2026-08-07) |
```

(原来 `Blind`/`Silence`/`Pull`/`Reflect` 那行是 5 字含 `钩`,现在是 4 字。)

- [ ] **Step 4: 重新生成 `chars.json`**

```bash
cd tools/pipeline && python3 export_chars.py
```

**核对改动范围**:熣 / 烟 / 锁 / 镜 / 剁 / 柳 六字此前都是 ⚠ 未落地,`chars.json` 里一条都没有,
所以**全是纯新增**。应当只多出 12 条(6 个字 + 6 个叶子部件 `朵 竟 崔 因 卯 贝`),
总条数 **212 → 224**。

用这条逐条比对,**既有字必须零改动**:

```bash
cd /Users/eugenewu/code/game && python3 - <<'PY'
import json, subprocess
old = json.loads(subprocess.run(['git','show','HEAD:Brushblade/Assets/StreamingAssets/config/chars.json'],
                                capture_output=True,text=True).stdout)
new = json.load(open('Brushblade/Assets/StreamingAssets/config/chars.json'))
o = {c['id']: json.dumps(c,ensure_ascii=False,sort_keys=True) for c in old['chars']}
n = {c['id']: json.dumps(c,ensure_ascii=False,sort_keys=True) for c in new['chars']}
print(f"条数 {len(o)} → {len(n)}")
print("新增:", ''.join(sorted(set(n)-set(o))))
print("删除:", ''.join(sorted(set(o)-set(n))) or '无')
print("既有字被改动:", ''.join(sorted(k for k in set(o)&set(n) if o[k]!=n[k])) or '无')
PY
```

**若「既有字被改动」不是「无」,停下来查原因。**

- [ ] **Step 5: 重新生成字体子集**

```bash
cd tools/fonts && python3 subset_fonts.py
```

⚠ `subset_fonts.py` 用裸正则扫 `"..."` 形状取字符,**分不清代码与注释** —— 前面任务写的
中文注释里若有 ASCII 直引号包着的中文,会被吸进字集。用「」书名号写注释可避开。
字体产物的真实输出路径以脚本的 `OUT_DIR` 为准。

- [ ] **Step 6: 补真实配置守卫**

追加到 `Brushblade/Assets/_Project/Tests/CharTableTests.cs`:

```csharp
        [Test]
        public void RealConfig_BlindCharsCarryTheirPercentAndTurns()
        {
            var graph = RealGraph();
            var sui = graph.Get("熣").Effects.First(e => e.Kind == EffectKind.Blind);
            Assert.That(sui.Value, Is.EqualTo(50));
            Assert.That(sui.Turns, Is.EqualTo(2), "turns 被静默丢掉的话会是 0——挂上去当场到期");
            Assert.That(sui.TargetAll, Is.False);

            var yan = graph.Get("烟").Effects.First(e => e.Kind == EffectKind.Blind);
            Assert.That(yan.Value, Is.EqualTo(30));
            Assert.That(yan.Turns, Is.EqualTo(1));
            Assert.That(yan.TargetAll, Is.True, "烟 是全体致盲");
        }

        [Test]
        public void RealConfig_SilenceAndReflectCarryTurns()
        {
            var graph = RealGraph();
            Assert.That(graph.Get("锁").Effects.First(e => e.Kind == EffectKind.Silence).Turns,
                Is.EqualTo(1));
            var jing = graph.Get("镜").Effects.First(e => e.Kind == EffectKind.Reflect);
            Assert.That(jing.Value, Is.EqualTo(50));
            Assert.That(jing.Turns, Is.EqualTo(2));
        }

        [Test]
        public void RealConfig_DuoIsTwoSegments()
        {
            var graph = RealGraph();
            var duo = graph.Get("剁").Effects.First(e => e.Kind == EffectKind.DamageSingle);
            Assert.That(duo.Value, Is.EqualTo(10));
            Assert.That(duo.HitCount, Is.EqualTo(2));
        }

        [Test]
        public void RealConfig_LiuCarriesDodge()
        {
            var graph = RealGraph();
            var summon = graph.Get("柳").Effects.First(e => e.Kind == EffectKind.Summon);
            Assert.That(summon.Value, Is.EqualTo(8));
            Assert.That(summon.SummonAttack, Is.EqualTo(3));
            Assert.That(summon.Passive, Is.Not.Null);
            Assert.That(summon.Passive.Dodge, Is.EqualTo(50));
        }

        [Test]
        public void RealConfig_SuoUsesManualRecipe_NotSupplementaryPlanePart()
        {
            var graph = RealGraph();
            Assert.That(graph.Get("锁").Recipe, Is.EqualTo(new[] { "钅", "贝" }));
        }

        [Test]
        public void RealConfig_GouIsNotInTheTable()
        {
            // 钩 是模型缺口(敌人无排位概念),已移出字表
            var graph = RealGraph();
            Assert.That(() => graph.Get("钩"), Throws.Exception,
                "钩 不该出现在字表里");
        }
```

⚠ `graph.Get` 对未知字抛什么异常以 `RecipeGraph` 现状为准 —— 若它返回 null 而不是抛,
把最后那条改成 `Assert.That(graph.Get("钩"), Is.Null)`。**照实际行为写,不要改生产代码。**

- [ ] **Step 7: 全套测试**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

- [ ] **Step 8: 提交**

```bash
git add docs/design/字选型/技能机制详表.md \
        Brushblade/Assets/_Project/Tests/CharTableTests.cs \
        Brushblade/Assets/StreamingAssets/config/chars.json \
        Brushblade/Assets/_Project/Presentation/Fonts/Resources/NotoSansSC-Subset.ttf \
        Brushblade/Assets/_Project/Presentation/Fonts/Resources/NotoSerifSC-Subset.ttf \
        tools/fonts/charset.txt
git commit -m "feat(data): 伤害变体 6 字落地——123 → 129 字可用;钩 因模型缺口移出字表"
```

---

### Task 7: 表现层

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`
- Modify: `Brushblade/Assets/_Project/Presentation/Juice.cs`
- Modify: `docs/design/字选型/技能机制详表.md`(§7.5 补实现说明)

**Interfaces:**
- Consumes:`BattleEventKind.Missed`、`StatusKind.Blind` / `Silence` / `Reflect`、`SummonPassive.Dodge`

- [ ] **Step 1: 打空的飘字**

`Juice.cs` 里找到 `case BattleEventKind.ImmunityBlocked:` 那个分支,在它旁边加:

```csharp
                    // 打空(2026-08-07,致盲/闪避):敌人照常下扑,但什么都没打到。
                    // 没有反馈的话玩家只会以为敌人这回合没动
                    case BattleEventKind.Missed:
                        Lunge(enemyAnchor(e.TargetIndex));
                        Popup("空", Theme.InkSoft, e.SecondIndex >= 0
                            ? summonAnchor(e.SecondIndex) : null);
                        break;
```

`BattleView` 处理逐记命中的那个 `switch` 里加一条空分支(打空时血条护盾条都不动):

```csharp
                case BattleEventKind.Missed: // 打空:血条护盾条都不动,表达交给 Juice 的飘字
                    break;
```

⚠ `Lunge` / `Popup` / `summonAnchor` / `Theme.InkSoft` 的真实签名以仓库现状为准 ——
照抄同文件里 `ImmunityBlocked` 与 `SummonHit` 分支的写法。

- [ ] **Step 2: 敌人 chip 加致盲与沉默**

`BattleView.DrawEnemies` 的 chips 段,在灼烧 chip 之后加:

```csharp
                int blind = enemy.Statuses.TotalMagnitude(StatusKind.Blind);
                if (blind > 0)
                    Ui.Chip(chips.transform, $"致盲 −{blind}%", Theme.InkSoft, Color.white, 12);
                if (enemy.Statuses.Has(StatusKind.Silence))
                    Ui.Chip(chips.transform, "沉默", Theme.InkSoft, Color.white, 12);
```

- [ ] **Step 3: 玩家状态条加反弹**

`BattleView.DrawPlayerStats` 里那一行状态 chip(封字 / 灼烧 / 免疫)之后加:

```csharp
            int reflect = Battle.PlayerStatuses.TotalMagnitude(StatusKind.Reflect);
            if (reflect > 0)
                Ui.Chip(statusRow.transform, $"反弹 {reflect}%", Theme.Jade, Color.white, 12);
```

⚠ 上个子项目把 `statusRow` 改成了**按需创建**(全为 0 时不留空 Row)。加这一条时保持那个模式,
别退回无条件创建。

- [ ] **Step 4: 召唤物被动小字加闪避**

`BattleView` 的 `SummonPassiveTag` 里,在既有的 if 链中加一条(位置放在 `Speed > 100` 之前):

```csharp
            if (passive.Dodge > 0) return $"闪避{passive.Dodge}%";
```

- [ ] **Step 5: 多段分两拍播**

`Juice.cs` 的 `case BattleEventKind.Damage:` 现在是「全体攻击并行,本记不 yield」。
多段需要玩家看得出是两段,所以在同一目标连续两记 `Damage` 之间拉开一拍。

最小改法:在 `Damage` 分支里判断**上一记是否也是打同一个目标的 `Damage`**,是则先 `yield`:

```csharp
                    case BattleEventKind.Damage:
                        // 多段(2026-08-07,剁):同一目标连续两记伤害要拉开一拍,
                        // 否则一拍打完两段,玩家看不出是两段。跨目标的全体攻击仍并行
                        if (idx > 0 && events[idx - 1].Kind == BattleEventKind.Damage
                            && events[idx - 1].TargetIndex == e.TargetIndex)
                            yield return new WaitForSecondsRealtime(StepGap);
                        // …既有逻辑不动…
```

⚠ `idx` / `events` / `StepGap` 的真实变量名以 `Juice.cs` 现状为准 —— 照抄同文件里
`BurnTick` 分支的串行写法(它已经在用 `serialPending` + `StepGap` 做同类事)。

- [ ] **Step 6: 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

预期:0 个 `error CS`。**这一步不能跳** —— 工装只编译 Core/Data,Presentation 的编译错会一路漏到用户打开 Unity 才炸。

- [ ] **Step 7: 字体子集补新文案**

`空` `致盲` `沉默` `反弹` `闪避` 里的字若不在字集,会渲染成空框:

```bash
cd tools/fonts && python3 subset_fonts.py
python3 -m pytest tools/fonts/tests/ -q
```

- [ ] **Step 8: 详表 §7.5 补实现说明**

在 §7.5 落地顺序末尾补一条:

```
6. **P3 伤害变体** —— 2026-08-07 落地。致盲/沉默/反弹做成三个新 `EffectKind` + 三个新
   `StatusKind`,闪避是 `SummonPassive` 加一个字段,多段是 `EffectDef.HitCount`。
   为此给引擎补上了**命中判定**:插在 `DamagePlayerDirect` 与 `DamageSummon` 最开头,
   只判敌人打过来的那一侧(玩家出字必中)。**命中率满格时短路不摇随机数** ——
   `_random` 的唯一既有消费方是回合掉字,无条件摇会平移掉落序列。
   `钩` 因模型缺口(敌人无排位概念)移出字表,见 §7.4。
```

- [ ] **Step 9: 全套测试**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

- [ ] **Step 10: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation/BattleView.cs \
        Brushblade/Assets/_Project/Presentation/Juice.cs \
        docs/design/字选型/技能机制详表.md \
        Brushblade/Assets/_Project/Presentation/Fonts/Resources/NotoSansSC-Subset.ttf \
        Brushblade/Assets/_Project/Presentation/Fonts/Resources/NotoSerifSC-Subset.ttf \
        tools/fonts/charset.txt
git commit -m "feat(presentation): 打空飘字与致盲/沉默/反弹/闪避上屏;多段分两拍播"
```

---

## 收尾清单

1. **三套测试全绿**:`coretests`、`pytest`、`prescompile`。
2. **提醒用户跑一次 Unity EditMode** —— `ConfigLoaderTests` 被工装排除,Task 5 加的解析测试只在 Test Runner 里跑得到:

   ```bash
   /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/MacOS/Unity \
     -batchmode -projectPath Brushblade -runTests -testPlatform EditMode \
     -testResults /tmp/results.xml -logFile /tmp/unity_test.log
   ```
3. **提醒用户新建的 `.cs` 需要 Unity 生成 `.meta`** —— `DamageVariantTests.cs`。
4. **确认没碰用户的并行改动**:`git status` 里 `Brushblade/Assets/Settings/` 与 `Brushblade/ProjectSettings/ProjectSettings.asset` 应仍是未提交状态。
