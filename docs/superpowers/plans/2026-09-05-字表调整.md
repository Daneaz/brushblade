# 字表调整 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 从字表移除 17 字与金系同源变体 戈/刀,加入木系同源变体 竹 与 藻/箭/葬 三字,并把 枪/藤 各降一档补住木系白/绿档。

**Architecture:** 字表是管线产物 —— 真相在《技能机制详表》,`extract_values` 抽 ✅ 行,`export_chars` 配上配方生成 `chars.json`。所以主线是「改详表 → 重跑管线 → 修对账点」。唯一的代码新增是 `SummonPassive.Regen`(藻 的自愈),先做,后面的 chars.json 才有字段可落。

**Tech Stack:** Unity 6000.5.2f1 / C#(Core、Data、Presentation 四层 asmdef)、Python 3 数据管线、NUnit、pytest。

**Spec:** `docs/superpowers/specs/2026-09-05-字表调整-design.md`

## Global Constraints

- **Core 与 Data 禁止引用 UnityEngine**;随机性走 Core 内带种子的 RNG。
- 测试断言只用 Unity 版 NUnit 也支持的 API:禁用 `Is.AnyOf`;`Does.Not.Contain(x)` 只在 x 是 string 时可用,集合判包含一律写 `Assert.That(list.Contains(x), Is.True/False, "…")`。
- 测试里定位仓库根只能用 `TestContext.CurrentContext.TestDirectory`,禁用 `AppContext.BaseDirectory`。
- 测试代码禁止直接引用 Newtonsoft,序列化走 `Data.SaveSerializer` / `Data.ConfigLoader`。
- 本工作树在 `.claude/worktrees/chartable-2026-09-05`,`Brushblade/Library/` 不入 git —— prescompile 必须借主检出的程序集:
  `-p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies`
- 禁用 `git stash`(与主检出共享 stash 栈)。对比改动前后用 `git show HEAD:<path>`。
- 提交信息用 conventional commits。
- dotnet 一律用 Unity 自带的:`/Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet`

**本计划反复用到的三条命令**(下文简称「管线测试」「Core 测试」「离线编译」):

```bash
# 管线测试(三个目录都要跑)
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ tools/design/tests/ -q

# Core 测试
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q

# 离线编译(改完 Presentation 必跑)
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q \
  -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

---

### Task 1: 引擎 —— `SummonPassive.Regen`

召唤物每回合给自己回血。纯追加:缺省 0 时逐位等价于改动前。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/SummonPassive.cs`(加字段 + `Clone()`)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs:1550-1568`(`ActSummonTurn`)
- Test: `Brushblade/Assets/_Project/Tests/SummonPassiveTests.cs`

**Interfaces:**
- Consumes: 无(本任务是起点)
- Produces: `SummonPassive.Regen`(int,每回合自愈点数,0 = 无);chars.json 侧的字段名是 `passive.regen`;详表 token 是 `` `Regen N` ``

- [ ] **Step 1: 写失败测试**

在 `SummonPassiveTests.Graph()` 的字表里,紧跟 `荫` 那条之后加一个夹具字:

```csharp
            // 苔:攻 0 + 每回合自愈 3(藻)。与 荫(HealAlly)成对 —— 那个外溢给全队,这个只回自己
            new CharDef("苔", Element.Wood,
                effects: new[] { new EffectDef(EffectKind.Summon, 10, summonCount: 1, summonAttack: 0, summonChar: "木",
                    passive: new SummonPassive { Regen = 3 }) }),
```

在文件末尾 `---- 光环治疗 ----` 那一段之后追加:

```csharp
        // ---- 自愈(2026-09-05,藻)----

        [Test]
        public void Regen_HealsOnlyItself_NotPlayerOrOtherSummons()
        {
            // 用**受伤的存档召唤物**开局,避开「敌人这一拍打谁」的随机(靶 攻 0,场上没有
            // 别的血量变化):0 号带自愈 3、1 号不带,两只都是 4/10 血。
            // 走 SaveSerializer 是这个文件既有的造快照手法(见 Snapshot_LegacySaveWithoutSpeedField)。
            const string save =
                "{\"EndlessV2\":{\"Depth\":1,\"PlayerHp\":40,\"Seed\":7,\"CarriedSummons\":[" +
                "{\"Char\":\"木\",\"Element\":\"Wood\",\"Hp\":4,\"MaxHp\":10,\"Attack\":0," +
                "\"Speed\":100,\"Slot\":0,\"Passive\":{\"Regen\":3}}," +
                "{\"Char\":\"木\",\"Element\":\"Wood\",\"Hp\":4,\"MaxHp\":10,\"Attack\":0," +
                "\"Speed\":100,\"Slot\":1}]}}";
            var restored = Data.SaveSerializer.FromJson(save);
            var engine = Engine(new[] { "素" }, new[] { Dummy() },
                startingSummons: restored.EndlessV2.CarriedSummons);
            int playerHp = engine.PlayerHp;

            engine.EndTurn();

            Assert.That(engine.Summons[0].Hp, Is.EqualTo(7), "自愈 3");
            Assert.That(engine.Summons[1].Hp, Is.EqualTo(4), "自愈不该外溢给别的召唤物");
            Assert.That(engine.PlayerHp, Is.EqualTo(playerHp), "自愈不该外溢给玩家");
        }
```

再追加三条:

```csharp
        [Test]
        public void Regen_DoesNotExceedMaxHp()
        {
            var engine = Engine(new[] { "苔" }, new[] { Dummy() });   // 靶 攻 0,苔 一直满血
            engine.Cast("苔");
            engine.EndTurn();
            engine.EndTurn();
            Assert.That(engine.Summons[0].Hp, Is.EqualTo(10), "满血时自愈不溢出");
        }

        [Test]
        public void Regen_ZeroIsNoOp()
        {
            // 素 没有 passive —— 自愈这条通道不该动它分毫(缺省等价性)
            var engine = Engine(new[] { "素" }, new[] { new EnemyDef("靶", Element.Heart, 200, 4) });
            engine.Cast("素");
            engine.EndTurn();
            int hp = engine.Summons[0].Hp;
            engine.EndTurn();
            Assert.That(engine.Summons[0].Hp, Is.EqualTo(hp - 4), "无被动的召唤物只挨打,不回血");
        }

        [Test]
        public void Regen_SurvivesSnapshotRoundTrip()
        {
            var engine = Engine(new[] { "苔" }, new[] { Dummy() });
            engine.Cast("苔");

            var meta = new MetaState
            {
                EndlessV2 = new EndlessSaveState { Depth = 3, PlayerHp = 40, Seed = 7 },
            };
            for (int s = 0; s < engine.Summons.Count; s++)
                if (engine.Summons[s] != null) meta.EndlessV2.CarriedSummons.Add(engine.Summons[s].Capture(s));

            var restored = Data.SaveSerializer.FromJson(Data.SaveSerializer.ToJson(meta));
            var revived = Engine(new[] { "苔" }, new[] { Dummy() },
                startingSummons: restored.EndlessV2.CarriedSummons);

            Assert.That(revived.Summons[0].Passive, Is.Not.Null);
            Assert.That(revived.Summons[0].Passive.Regen, Is.EqualTo(3));
        }
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:编译失败,`SummonPassive` 不含 `Regen`(CS0117)。

- [ ] **Step 3: 实现**

`Core/SummonPassive.cs` —— 在 `HealAlly` 那条之后加:

```csharp
        /// <summary>每回合给**自己**回血(2026-09-05,藻)。0 = 无。
        ///
        /// 与 <see cref="HealAlly"/> 是一对,别合并:那个是「给玩家 + 全部存活召唤物」的光环,
        /// 这条只作用于持有者本身。藻 由用户指定做自愈型召唤,不外溢。
        ///
        /// 不吃卡等级 —— 回归 2026-08-05 的「被动数值是节奏,不随等级变」缺省
        /// (吃等级的只有 OnHitFreezeChance / OnHitSlow 两项,是 2026-08-25 的显式例外)。</summary>
        public int Regen { get; set; }
```

同一文件的 `Clone()` 里,把 `HealAlly = HealAlly,` 那一行改成:

```csharp
            OnHitBurn = OnHitBurn, OnHitBurnAll = OnHitBurnAll, OnHitCurse = OnHitCurse,
```
前一行补上 `Regen = Regen,`(即第一行改为 `Speed = Speed, Thorns = Thorns, HealAlly = HealAlly, Regen = Regen,`)。

`Core/BattleEngine.cs` 的 `ActSummonTurn`,在 `if (heal > 0) HealPlayerAndSummons(heal);` 之后、
`if (_enemies.Any(e => e.Alive)) StrikeOnceWithSummon(s);` 之前插入:

```csharp
            int regen = summon.Passive?.Regen ?? 0;
            // 自愈(2026-09-05,藻):只回自己。与上面的光环同序 —— 都排在出手之前,
            // 于是「召唤物清场的那一拍照样回血」对两者一致(见 HealAlly_StillHealsOnTheTurnSummonsClearTheField)。
            // 复用 HealAlly(slot, amount):它自带 MaxHp 夹取与 Heal 事件,表现层无需另加通道。
            if (regen > 0) HealAlly(s, regen);
```

同时把 `ActSummonTurn` 头上的文档注释里「自身灼烧 → 光环治疗 → 出手 → 自身状态递减」改成
「自身灼烧 → 光环治疗 → 自愈 → 出手 → 自身状态递减」。

- [ ] **Step 4: 跑测试确认通过**

跑 Core 测试。四条新测试全绿,且其余测试**一条都不许变红** —— `Regen` 缺省 0,
对既有行为是恒等。若有旧测试变红,是实现落错了位置(比如挪到了出手之后),回 Step 3 重看。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/SummonPassive.cs Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/SummonPassiveTests.cs
git commit -m "feat(core): 召唤物被动加自愈 Regen —— 每回合只回自己,不外溢"
```

---

### Task 2: 详表 —— 移除 17 字、枪/藤 改档、新增 3 字

《技能机制详表》是字表的唯一真相。`extract_values.extract()` **只收「实现」列以 ✅ 开头的行**,所以移除 = 把 ✅ 改成 ⚠。

**Files:**
- Modify: `docs/design/字选型/技能机制详表.md`

**Interfaces:**
- Consumes: Task 1 的 `` `Regen N` `` token 约定
- Produces: 详表里 藻/箭/葬 三行 + 枪/藤 的新数值,供 Task 3 的管线抽取

- [ ] **Step 1: 把 17 行的 ✅ 改成 ⚠**

17 个字:劈 战 刺 铠 · 杖 松 柏 蕉 · 沝 淡 沏 · 碾 砸 · 烧 焦 灼 熣

每行只改「实现」列的开头,**保留该列原有的全部历史记录**(那是移出决策的依据)。格式照抄
2026-08-25 那批,把行首的 `✅` 替换成:

```
⚠ **2026-09-05 用户裁定移出字表**。移除前:
```

例:烧 那一行的实现列
`✅;**2026-08-15**:1 层 → 攻 30 + 2 层 = 30 + 60 = 90 = 绿档锚点`
改成
`⚠ **2026-09-05 用户裁定移出字表**。移除前:;**2026-08-15**:1 层 → 攻 30 + 2 层 = 30 + 60 = 90 = 绿档锚点`

其中四个字额外补一句理由(接在「移出字表」之后、句号之前):
- 铠 —— `,全表唯一的 DefenseBuff 载体,点数护甲机制随之休眠`
- 刺 —— `,全表唯一的 pierce 载体,穿透机制随之休眠`
- 沝 —— `,水系叠字链改由 冰 顶替中间环(见 §1.5)`
- 焦 —— `,BurnNoDecay 随之休眠`

- [ ] **Step 2: 改 枪 / 藤 的档位与数值**

在 `## 三 · 木系` 的「召 · 单体 · 肉盾 / 控制 / 常规」表里:

枪 那一行,`🟢绿` → `⚪白`,效果配置 `` `Summon 1`(36 血/攻 48) `` → `` `Summon 1`(24 血/攻 32) ``,
最终值列的「1 只 · 攻击型」保留,实现列末尾追加:

```
;**2026-09-05**:降白档 —— 柏/杖 移出后木系白档归零,而起始收藏的口径是「五系 × 白/绿/蓝各一张」。数值按白档召唤锚点 1 × 40/20 × 攻击型系数 0.6/1.6 重算。⚠ 与金系白档的 锥(24/32)主数值全同,只差攻击形状(Skewer vs Volley),同质记录在案
```

藤 那一行,`🔵蓝` → `🟢绿`,效果配置 `` `Summon 1`(80 血/攻 40) `` → `` `Summon 1`(60 血/攻 30) ``,
实现列末尾追加:

```
;**2026-09-05**:降绿档 —— 随 枪 降白连锁(见 枪)。数值按绿档召唤锚点 1 × 60/30 × 平衡型 1.0/1.0 重算;出手 10% 冻结 1 回合**不动**,它在蓝档时就是锚点之外白送的边角,按 §1.4.1 本就没计价
```

- [ ] **Step 3: 加 藻 / 箭 / 葬 三行**

藻 加在「召 · 单体 · 肉盾 / 控制 / 常规」表里(桂 那一行之后):

```
| 藻 | 🟠橙 | `Summon 3`(260 血/攻 50)+ `Regen 60` | 3 只 · 自愈型,每只每回合回 60 | ✅ **2026-09-05 新增**:配方 艹 + 澡,五行部件展开 艹+氵+木 = 3 个 → 橙档。第二个五行部件是 氵 → 治疗型系数(血 ×1.0 / 攻 ×0.5)。⚠ 「治疗型」按 §3 配的是 `HealAlly`(全队光环),**本字由用户指定改为自愈** —— 继 蕉、杖 之后第三个「第二五行部件定型」特例 |
```

「攻 · 单攻」那一节的标题改成 `### 攻`(葬 是全体,原标题只说单攻会不自洽),表里加两行:

```
| 箭 | 🟣紫 | `DamageSingle 200` | 单体 200 | ✅ **2026-09-05 新增**:配方 竹 + 前,五行部件展开 竹(木)+ 刂(金)= 混 2 → 紫档。竹 由本字带进字表,并同批加入 `ComponentKin` 木组。无副效果故取满紫档单攻锚点 200。⚠ 与 葬 一并推翻 2026-08-25 的「木系转纯召唤」(棒/棍 当时正因此移出) |
| 葬 | 🔵蓝 | `DamageAll 70` | 全体 70 | ✅ **2026-09-05 新增**:配方 **艹 + 死**(手写,IDS 的 艹+死+廾 是三部件,而全表配方一律两部件)。只有 1 个五行部件,按 §1.2 词组数定档该落白档(埋葬/葬送 的搭档字都不在表内 = 0 词)——**用户指定上调蓝档**,记为语义特例。取蓝档全体锚点 70 |
```

- [ ] **Step 4: 改 §1.2 的五行配额表与 §1.5 的叠字链**

§1.2 的配额表,木列改为 白 1 / 绿 1 / 蓝 1(原 2 / 2 / 1),并在表下补一句:

```
**2026-09-05**:木系白/绿/蓝各 1 —— 柏/杖/松/蕉 移出后由 枪 降白、藤 降绿、葬 新增蓝补齐。
```

§1.5「五系叠字链配方」里,水系那条改成 `淼 = 水 + 冰`,并补一条例外说明:

```
⚠ **2026-09-05 例外**:水系的二叠字 沝 随本批移出字表,链中断,由 冰(冫 + 水)顶替中间环 ——
`水 → 冰 → 淼 → 㵘`。淼 的五行部件展开仍是 水+冫+水 = 3 个纯同系,§1.1 判据下档位不变(🟠橙)。
代价:五系二叠字里只有水系不再是可出牌卡(林/炎/圭/鍂 仍在金档),对称性破一个口。
```

- [ ] **Step 5: 在 §九 补三行拼音与释义**

`## 九 · 拼音与释义` 的表里按现有顺序插入(表格是 `| 字 | 拼音 | 释义 |` 三列):

```
| 藻 | zǎo | 水中丛生的绿色植物 |
| 箭 | jiàn | 弓弩发射的尖头长杆 |
| 葬 | zàng | 掩埋亡者 |
```

被移除那 17 字的行**留着不动** —— `_merge_readings` 只并已在 values 里的字,留着无害且是历史记录。

- [ ] **Step 6: 改标题里的字数**

文件第 1 行 `# 技能机制详表 · 139 字实现规格` 改成 `# 技能机制详表 · 142 字实现规格`(139 + 藻/箭/葬)。
`## 二 · 火系 · 31 字` 这类各节标题**不动** —— 它们数的是「详表收录过的字」,含已移出的。

- [ ] **Step 7: 提交**

```bash
git add docs/design/字选型/技能机制详表.md
git commit -m "docs(chars): 详表移出 17 字、枪降白藤降绿、新增 藻/箭/葬"
```

---

### Task 3: 管线 —— 配方表、Regen token、重生成 chars.json

**Files:**
- Modify: `tools/pipeline/export_chars.py`(`STACK_RECIPES` / `MANUAL_RECIPES` / `COMPONENT_RECIPES` / `COMPOUND_ATTR`)
- Modify: `tools/pipeline/extract_values.py`(`SUMMON_PASSIVE`)
- Modify: `Brushblade/Assets/StreamingAssets/config/chars.json`(产物,由脚本生成)
- Test: `tools/pipeline/tests/test_export_chars.py`、`tools/pipeline/tests/test_extract_values.py`

**Interfaces:**
- Consumes: Task 2 的详表
- Produces: 新的 `chars.json`(60 个可出牌字),供 Task 4~7 对账

- [ ] **Step 1: 写失败测试**

在 `tools/pipeline/tests/test_export_chars.py` 末尾追加。读真实产物走这个文件已有的
`CHARS_JSON` 常量(`test_shipped_chars_json_carries_the_new_row_fields` 就是这么读的),
别新造读取路径:

```python
def _shipped():
    return {c["id"]: c for c in json.loads(CHARS_JSON.read_text(encoding="utf-8"))["chars"]}


def test_water_stack_chain_goes_through_bing_not_zhui():
    """2026-09-05:沝 移出字表,水系叠字链的中间环换成 冰。"""
    by_id = _shipped()
    assert by_id["淼"]["recipe"] == ["水", "冰"]
    assert "沝" not in by_id, "沝 已移出,且不该作为部件残留(没有任何配方再引用它)"


def test_removed_chars_are_gone():
    """2026-09-05 用户裁定移出的 17 字,一个都不该出现在 chars.json 里。"""
    by_id = _shipped()
    for char in "劈战刺铠杖松柏蕉沝淡沏碾砸烧焦灼熣":
        assert char not in by_id, f"{char} 应已移出字表"
    for part in "戈刀切崔":
        assert part not in by_id, f"部件 {part} 已无字引用,应随之消失"


def test_new_wood_chars_land_with_expected_recipes():
    """藻/箭/葬 的配方:藻 与 箭 走 IDS 一级拆解,葬 走手写兜底(IDS 是三部件)。"""
    by_id = _shipped()
    assert by_id["藻"]["recipe"] == ["艹", "澡"]
    assert by_id["箭"]["recipe"] == ["竹", "前"]
    assert by_id["葬"]["recipe"] == ["艹", "死"]
    assert by_id["竹"]["component"] is True
    assert by_id["竹"]["element"] == "Wood"
    assert "recipe" not in by_id["竹"], "竹 必须是叶子部件 —— ComponentKin 的守卫要求"


def test_zao_carries_regen_passive():
    """藻 的自愈落进 passive.regen(SUMMON_PASSIVE 那张手写映射表认不得就会静默丢弃)。"""
    zao = _shipped()["藻"]["effects"][0]
    assert zao["passive"] == {"regen": 60}
    assert zao["count"] == 3
    assert zao["value"] == 260
    assert zao["attack"] == 50
```

- [ ] **Step 2: 跑测试确认失败**

```bash
python3 -m pytest tools/pipeline/tests/test_export_chars.py -q
```

预期:四条新测试全红(淼 仍是 `["水","沝"]`;藻/箭/葬 不存在)。

- [ ] **Step 3: 改管线四张表 + token 映射**

`tools/pipeline/export_chars.py`:

1. `STACK_RECIPES` 里删掉 `"沝": ["水", "水"],`,并把 `"淼": ["水", "沝"]` 改成 `"淼": ["水", "冰"]`。同时在这个 dict 上方的注释里补一句:

```python
# 2026-09-05:沝 随字表调整移出,水系链的中间环换成 冰(冫+水)—— 见详表 §1.5 的例外说明。
```

2. `MANUAL_RECIPES` 里追加:

```python
    # 葬:IDS 是 ⿳艹死廾 三部件,而全表配方一律两部件(一步合成 Mode A)。
    # 取 艹 + 死 —— 草覆其尸,义形都通;死 是常用字,不像 荆 的 茾 那样是字体子集负担。
    "葬": ["艹", "死"],
```

3. `COMPONENT_RECIPES` 里删掉 `"切": ["七", "刀"],   # 沏` 与 `"崔": ["山", "隹"],   # 熣` 两条(它们服务的字都已移出)。

4. `COMPOUND_ATTR` 里删掉 `"切": "金"`,只留 `"兑": "金"`,并把该常量上方注释里讲 切 撑起 沏 相生的那段改写成一句历史说明:

```python
# 2026-09-05:切 随 沏 移出字表一并退场(它当初只为 沏 的金生水而存在,而相生 ×3 早在
# 2026-09-02 已取消)。现在只剩 兑 —— 它不撑相生,纯粹是设计拍板的展示属性。
COMPOUND_ATTR = {"兑": "金"}
```

`tools/pipeline/extract_values.py` 的 `SUMMON_PASSIVE` 里追加一条(⚠ 这张表认不得的 token 会**静默丢弃**):

```python
    "Regen": "regen",                         # 自愈(2026-09-05,藻):每回合只回自己
```

- [ ] **Step 4: 重生成 chars.json 并跑测试**

```bash
python3 tools/pipeline/export_chars.py
python3 -m pytest tools/pipeline/tests/ -q
```

`test_export_chars.py` 里针对 刺 / 淡 / 碾 / 砸 / 蕉 的既有断言会红(那些字没了),
`test_extract_values.py` 里 灼 的拼音/释义断言同理。逐条处理,规则:

| 原断言 | 改法 |
|---|---|
| `expected = {"刺": 15}`(穿透) | 刺 是全表唯一穿透字,移出后**没有任何字有 pierce**。把这条测试改成断言 `expected = {}`,并在 docstring 里写明「2026-09-05 起穿透机制休眠,无载体;将来有字重新装配穿透时把它加回来」 |
| 淡 的 `Dispel` 断言 | 换成同样带 `Dispel` 的留存字。先 `grep -l Dispel` 查 chars.json,取 灭(`DispelAll`)或 淋,断言形状照旧 |
| `by_id["碾"]["attackEffects"][0]["shape"] == "Sweep"` | 碾 是唯一的字卡 Sweep 载体。这条改成断言**召唤物**侧的 Sweep 仍在:`by_id["剑"]["effects"][0]["passive"]["shape"] == "Sweep"` |
| `by_id["砸"]["attackEffects"][0]["shape"] == "Cleave"` | Cleave 移出后无载体。删掉这条断言,并在同一个测试的 docstring 里记一句「Cleave 自 2026-09-05 起无载体」 |
| `by_id["蕉"]["effects"][0]["passive"] == {...onHitSlow...}` | 删掉这条(藻 的 passive 已由 Step 1 的 `test_zao_carries_regen_passive` 覆盖) |
| `by_id["刺"]["effects"][0]` 的 Skewer / shapePercent 那组 | 删掉。同一个测试里 `by_id["枪"]`、`by_id["剑"]`、`by_id["锥"]` 的召唤形状断言保留 —— 形状机制在召唤侧仍有载体 |
| `by_id["藤"]["effects"][0]["passive"]` | 数值不变(`{"onHitFreezeChance": 10, "onHitFreezeTurns": 1}`),藤 只降档不改被动 |
| `by_id["枪"]["effects"][0]["passive"]` | 数值不变(`{"shape": "Skewer", "shapePercent": 70}`),枪 只降档不改形状 |
| `test_extract_pulls_74_implementable_chars` 的 `assert len(values) == 74` | 改成 `== 60`,函数名改成 `test_extract_pulls_60_implementable_chars`,并在 docstring 末尾按既有格式追加一行:`2026-09-05 字表调整:74 → 60,移出 17 字、新增 藻/箭/葬(spec docs/superpowers/specs/2026-09-05-字表调整-design.md)。` |
| `values["灼"]["pinyin"] == "zhuó"` | 换成 `values["藻"]["pinyin"] == "zǎo"` 与 `values["藻"]["gloss"] == "水中丛生的绿色植物"` |

- [ ] **Step 5: 人工核对产物**

```bash
python3 - <<'EOF'
import json
d = json.load(open('Brushblade/Assets/StreamingAssets/config/chars.json'))['chars']
play = [c for c in d if not c.get('component')]
from collections import Counter
print('可出牌字', len(play))                       # 期望 60
print(Counter(c['element'] for c in play))          # 期望 木13 火12 水12 金12 土11
print([c['id'] for c in play if c['element'] == 'Wood'])
print('配方长度', Counter(len(c.get('recipe', [])) for c in d))  # 期望只有 0 和 2
EOF
```

- [ ] **Step 6: 提交**

```bash
git add tools/pipeline/ Brushblade/Assets/StreamingAssets/config/chars.json
git commit -m "feat(chars): 重生成字表 —— 60 字,淼 改走 冰,藻/箭/葬 落地"
```

---

### Task 4: `ComponentKin` —— 金组去 戈/刀,木组加 竹

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/ComponentKin.cs:15-21`
- Test: `Brushblade/Assets/_Project/Tests/ComponentKinTests.cs`

**Interfaces:**
- Consumes: Task 3 的 chars.json(竹 已作为叶子部件存在,戈/刀 已消失)
- Produces: 金组 `{金, 钅, 刂}`、木组 `{木, 艹, 竹}`

- [ ] **Step 1: 写失败测试**

在 `ComponentKinTests.cs` 里,把既有的 `TryGetGroup(&quot;刀&quot;, …)`(第 22 行附近)那条测试改写,并追加新的:

```csharp
        [Test]
        public void MetalGroup_NoLongerHasDaoOrGe()
        {
            // 2026-09-05:戈/刀 随 战/劈/沏 移出字表而消失,清单必须同步 ——
            // 留着会让 RealConfig_AllMembersAreLeavesInTheRealCharTable 报「不存在」
            Assert.That(ComponentKin.TryGetGroup("刀", out _), Is.False);
            Assert.That(ComponentKin.TryGetGroup("戈", out _), Is.False);
            Assert.That(ComponentKin.TryGetGroup("刂", out var group), Is.True);
            Assert.That(group.Contains("金"), Is.True);
            Assert.That(group.Contains("钅"), Is.True);
            Assert.That(group.Count, Is.EqualTo(3));
        }

        [Test]
        public void WoodGroup_IncludesZhu()
        {
            // 2026-09-05:箭 = 竹 + 前 带 竹 进字表,竹 与 木/艹 在配方匹配上等价
            Assert.That(ComponentKin.AreKin("木", "竹"), Is.True);
            Assert.That(ComponentKin.AreKin("艹", "竹"), Is.True);
            Assert.That(ComponentKin.AreKin("竹", "禾"), Is.False, "禾 仍然不是木系等价部件");
        }
```

⚠ `group.Contains(...)` 断在 bool 上 —— Unity 自带 NUnit 没有集合版 `Does.Not.Contain`。

- [ ] **Step 2: 跑 Core 测试确认失败**

预期:两条新测试红;`RealConfig_AllMembersAreLeavesInTheRealCharTable` 也红(「戈 在真实字表里不存在」)。

- [ ] **Step 3: 实现**

`Core/ComponentKin.cs` 的 `Groups` 改成:

```csharp
        private static readonly string[][] Groups =
        {
            new[] { "水", "氵", "冫" },
            new[] { "木", "艹", "竹" },
            new[] { "金", "钅", "刂" },
            new[] { "土", "山", "石" },
            new[] { "火", "灬" },
        };
```

`AllParts` 的文档注释里「全部 15 个成员」改成「全部 14 个成员」。

- [ ] **Step 4: 跑 Core 测试确认这三条通过**

其余测试此时可能仍有红(那是 Task 6 的活),只确认 `ComponentKinTests` 整个类全绿。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/ComponentKin.cs Brushblade/Assets/_Project/Tests/ComponentKinTests.cs
git commit -m "feat(core): 五系等价清单去 戈/刀、木组加 竹"
```

---

### Task 5: `Meta` 起始收藏 / 出阵 / 教程演示字

三处硬编码字名。测试抓不到「幽灵字」——字表里没有的字在这里只会静默不生效。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Meta.cs:56-77`
- Modify: `Brushblade/Assets/_Project/Core/Tutorial.cs:32`
- Test: `Brushblade/Assets/_Project/Tests/MetaTests.cs`

**Interfaces:**
- Consumes: Task 3 的 chars.json
- Produces: `StartingCollection` 15 张、`StartingDeck` 5 张、`Tutorial.DemoChar = "剿"`

- [ ] **Step 1: 写失败测试**

在 `MetaTests.cs` 末尾追加(⚠ 用 `CharTableTests.RealGraph()` 读真实字表,别自己造读取路径):

```csharp
        [Test]
        public void StartingCollection_EveryCharExistsAndIsCraftable()
        {
            // 幽灵字守卫(2026-09-05):字表里没有的字放在这里不会报错,只会静默不生效。
            // 「有配方」是 2026-08-05 拍板的口径 —— 拆了要回得来。
            var graph = CharTableTests.RealGraph();
            foreach (var id in Meta.StartingCollection)
            {
                Assert.That(graph.TryGet(id, out var def), Is.True, $"起始收藏的「{id}」不在字表里");
                Assert.That(def.IsLeaf, Is.False, $"起始收藏的「{id}」没有配方,拆了回不来");
                Assert.That(def.IsComponent, Is.False, $"起始收藏的「{id}」是部件,不该进收藏");
            }
        }

        [Test]
        public void StartingDeck_IsSubsetOfCollection_AndHoldsTheDemoChar()
        {
            foreach (var id in Meta.StartingDeck)
                Assert.That(Meta.StartingCollection.Contains(id), Is.True, $"出阵的「{id}」不在起始收藏里");
            Assert.That(Meta.StartingDeck.Contains(Tutorial.DemoChar), Is.True,
                "教程要拆的字必须在起手字库里");
        }
```

- [ ] **Step 2: 跑 Core 测试确认失败**

预期:两条都红(劈/刺/柏/松/灼/烧/碾 已不在字表;`DemoChar = "刺"` 同理)。

- [ ] **Step 3: 实现**

`Core/Meta.cs` 的 `StartingCollection` 改成:

```csharp
        public static readonly IReadOnlyList<string> StartingCollection = new[]
        {
            // 2026-09-05 字表调整:劈/刺/柏/松/灼/烧/碾 随本轮移出字表,枪 降白、藤 降绿、
            // 葬 新增蓝补齐木系三档(spec docs/superpowers/specs/2026-09-05-字表调整-design.md)。
            "锥", "锐", "剿", // 金:白 召唤连发 2 / 绿 穿透+40 伤 / 蓝 100 伤+斩杀+战意
            "枪", "藤", "葬", // 木:白 召唤贯穿 / 绿 召唤出手冻结 / 蓝 全体 70
            "冻", "海", "冷", // 水:控制线(白 冻结+30 伤 / 绿 全体 50 / 蓝 减速+90 伤)
            "灭", "热", "爆", // 火:白 驱散+全体 30 / 绿 70 伤+1 层 / 蓝 全体 50+全体 1 层
            "碉", "垒", "碎", // 土:防御与破防(白 召唤肉盾 / 绿 盾 55+20 伤 / 蓝 90 伤+破甲 20)
        };
```

`StartingDeck` 改成:

```csharp
        public static readonly IReadOnlyList<string> StartingDeck = new[]
        {
            "剿", "葬", "冷", "爆", "碎",
        };
```

`Core/Tutorial.cs` 第 32 行:

```csharp
        public const string DemoChar = "剿";
```

同时把 `DemoChar` 上方的文档注释里提到 刺 的地方改成 剿,并补一句
`// 2026-09-05:刺 移出字表,换成同为金系蓝档纯直伤的 剿(配方 巢 + 刂)。`

⚠ 改完在 `Core/` 与 `Presentation/` 全仓 `grep -rn '"刺"'`,确认没有别的地方靠 `DemoChar == "刺"` 的字面量做判断。

- [ ] **Step 4: 跑 Core 测试**

两条新测试转绿。`MetaTests` 里既有的、拿 烧/灼 当样本字的用例会红 —— 那些是**合成字表**还是**真实字表**要逐条看:合成的(自己 `new CharDef(...)`)不用动,读真实表的把样本换成 热/爆。

- [ ] **Step 5: 离线编译**

跑离线编译,确认 Presentation 没有引用这几个常量的编译错。

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Core/Meta.cs Brushblade/Assets/_Project/Core/Tutorial.cs Brushblade/Assets/_Project/Tests/MetaTests.cs
git commit -m "fix(core): 起始收藏/出阵/教程演示字换掉移出的 7 张"
```

---

### Task 6: 修真实字表用例

移除 17 字会打红一批「拿这些字当样本」的用例。它们分两类,处理方式不同。

**Files:**
- Modify: `Brushblade/Assets/_Project/Tests/` 下由测试运行结果指出的文件

**Interfaces:**
- Consumes: Task 3~5
- Produces: 全绿的 Core 测试

- [ ] **Step 1: 跑一次拿到红名单**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q 2>&1 | tee /tmp/coretests.log
grep -E "Failed|error CS" /tmp/coretests.log | head -60
```

- [ ] **Step 2: 逐条按类别处理**

**类别 A —— 测试自己 `new CharDef("铠", …)` 造的合成字表**:字名只是个标签,与真实字表无关。
**不要动**。(`StatusBagTests` / `StatusOpsTests` / `SnapshotRoundTripTests` 里的 "铠" 是 `SourceId` 字符串,`BuffCharTests` 的 "战"、`TargetingTests` 的 刺/砸/松、`SummonPassiveTests` 的 "焦" 都属此类。)

**类别 B —— 读真实字表(`CharTableTests.RealGraph()` / `ConfigLoader`)并断言具体字**:换样本字。
已知需要处理的:

| 文件 | 原样本 | 换成 | 理由 |
|---|---|---|---|
| `ConfigLoaderTests.cs:53,59,60` | 灼 | 热 | 同为火系带灼烧的留存字 |
| `ConfigLoaderTests.cs:129,130,171` | 沝 | 冰 | 水系金档,叠字链的新中间环 |
| `CharTableTests.cs:193` | `{ "砸", "冷", "熣", "刲" }` | `{ "冷", "刲" }` | 砸/熣 移出,偷袭字只剩两张;数组缩短即可,断言逻辑不动 |
| `DualDirectionTests.cs:17-20` `WaterChars` | 含 沏 / 沝 / 淡 | 删这三个 | 三张水系双方向字随本批移出 |
| `DualDirectionTests.cs:26-28` `EarthChars` | 含 砸 / 碾 | 删这两个 | 同上 |
| `DefenseValuesTests.cs:182` | 铠 | —— | 铠 是唯一 DefenseBuff 载体。这条测试整体删掉,并在类的 docstring 里记一句「点数护甲自 2026-09-05 起无载体,机制休眠;重新装配时把这条测试恢复」 |
| `DefenseValuesTests.cs:199` | 刺 | —— | 同上,穿透无载体,同样删测试 + 记 docstring |
| `MomentumTests.cs:176-292` | 沝 | 冰 | 水势测试要一张水系高档双方向字,冰 是最接近的(金档、双方向) |
| `RarityTests.cs:100-199` | 烧 | 热 | 绿档火系留存字 |
| `EndlessConfigTests.cs:11-85` | 灼 | 灭 | 白档火系留存字 |
| `BattleEngineTests.cs` / `BossSkillTests.cs` / `BuffTargetTests.cs` / `DamageVariantTests.cs` / `EnemyAbilityTests.cs` | 逐条看 | 逐条判类别 | 这几个文件同时有 A、B 两类,按上面的判据一条条分 |

⚠ 判据:看这条测试的字是从 `RealGraph()` / `graph.Get(id)` 取的,还是测试文件顶部自己 `new CharDef(...)` 造的。**只有前者要改。**

⚠ 换样本字后,断言里的**数值**多半也要跟着换(热 的伤害不等于烧的)。跑一次拿实际值,但要先算清楚这个值应该是多少,再和实际值对 —— 不要反过来照实际值填。

- [ ] **Step 3: 跑到全绿**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

- [ ] **Step 4: 提交**

```bash
git add Brushblade/Assets/_Project/Tests/
git commit -m "test: 把引用移出字的真实字表用例换到留存字上"
```

---

### Task 7: 字表功能解析文档(生成物)

`gen_char_doc.py` 的 `READINGS` 缺字会**直接 assert 失败**,`test_doc_is_fresh` 会因文档没重跑而红。

**Files:**
- Modify: `tools/design/gen_char_doc.py`(`READINGS`)
- Modify: `docs/design/字选型/字表功能解析.md`(产物)

**Interfaces:**
- Consumes: Task 3 的 chars.json
- Produces: 与 chars.json 同步的字表功能解析文档

- [ ] **Step 1: 跑生成器,让它自己报缺字**

```bash
python3 tools/design/gen_char_doc.py
```

预期:`AssertionError: READINGS 缺字,请补条目:['箭', '藻', '葬']`

- [ ] **Step 2: 补 READINGS**

在 `READINGS` 的木系那一段里加三条(⚠ 这里的第二个字段是**近代字意**,与详表 §九 的释义口径一致即可):

```python
    '藻': ('zǎo', '水中丛生的绿色植物'),
    '箭': ('jiàn', '弓弩发射的尖头长杆'),
    '葬': ('zàng', '掩埋亡者'),
```

`PHRASES` **不动** —— 它刻意保留已移出字的词(松柏、劈刺、碾碎、砸碎…)作为移出决策的依据,
渲染时按「两个字都还在可出牌字里」过滤。`test_phrases_match_the_scoring_doc` 拿它与
《词组计分表》交叉钉住,删任何一条都会打红。

- [ ] **Step 3: 重跑生成器并验证**

```bash
python3 tools/design/gen_char_doc.py
python3 -m pytest tools/design/tests/ -q
```

- [ ] **Step 4: 提交**

```bash
git add tools/design/gen_char_doc.py docs/design/字选型/字表功能解析.md
git commit -m "docs(chars): 字表功能解析随字表重生成,READINGS 补 藻/箭/葬"
```

---

### Task 8: 字体子集

新增字形不重跑子集,真机上会渲染成豆腐块 —— 而离线编译与 Core 单测**都发现不了**。

**Files:**
- Modify: `Brushblade/Assets/.../fonts/`(子集字体,产物)

**Interfaces:**
- Consumes: Task 3(chars.json 里的 藻/箭/葬/竹/前/死)、Task 7(文档里的释义汉字)
- Produces: 含新码位的子集字体

- [ ] **Step 1: 记下改前的 cmap**

```bash
python3 - <<'EOF'
from fontTools.ttLib import TTFont
import glob
for p in sorted(glob.glob('Brushblade/Assets/**/*.ttf', recursive=True)):
    print(p, len(TTFont(p).getBestCmap()))
EOF
```

⚠ 路径若对不上,先 `python3 tools/fonts/subset_fonts.py --help` 或读该脚本头部注释确认输出位置。

- [ ] **Step 2: 重跑子集**

```bash
python3 tools/fonts/subset_fonts.py
python3 -m pytest tools/fonts/tests/ -q
```

- [ ] **Step 3: 比对 cmap 确认新字真的进去了**

```bash
python3 - <<'EOF'
from fontTools.ttLib import TTFont
import glob
need = '藻箭葬竹前死'
for p in sorted(glob.glob('Brushblade/Assets/**/*.ttf', recursive=True)):
    cmap = TTFont(p).getBestCmap()
    missing = [c for c in need if ord(c) not in cmap]
    print(p, '缺:', missing or '无')
EOF
```

⚠ **光看 diff 大小说明不了问题** —— 重新生成必然产生几百 KB 的时间戳 churn。要看的是上面这份 cmap 报告。
⚠ 至少有一个字体是需要这些字的(卡面字形);若某个字体报「缺」是因为它本来就不含汉字(如数字字体),对照 Step 1 的基线判断,别盲目加字。

- [ ] **Step 4: 提交**

```bash
git add Brushblade/Assets
git commit -m "chore(fonts): 子集补 藻/箭/葬/竹/前/死"
```

---

### Task 9: 仿真工装的画像卡组

`tools/balance` 与 `tools/trace` 的画像卡组是硬编码字名。留着移出的字不会报错,只会让仿真读数**静默失真**(2026-08-12 的「幽灵字」前科)。

**Files:**
- Modify: `tools/balance/Program.cs:46,52,56,71,88,148`
- Modify: `tools/trace/Program.cs:50,62`

**Interfaces:**
- Consumes: Task 3 的 chars.json
- Produces: 无幽灵字的仿真画像

- [ ] **Step 1: 找出全部幽灵字**

```bash
grep -rno '"[劈战刺铠杖松柏沝淡碾砸烧焦灼熣沏蕉]"' tools/balance/Program.cs tools/trace/Program.cs
```

- [ ] **Step 2: 逐个删除或替换**

规则:
- **五系画像卡组**(`tools/balance/Program.cs:46/52/56`、`tools/trace/Program.cs:50/62`):这些是「该系的全部可出牌字」清单,直接**删掉**移出的字,并把新字加进对应系 —— 木系加 藻/箭/葬,水系不加(冰 已在)。
- **`ArmorCards` / `ArmorHand`(`balance/Program.cs:71,88`)**:唯一成员是 铠,而点数护甲已无载体。把这两个数组连同用到它们的画像一并删掉,在删除处留一行注释:
  `// 2026-09-05:铠 移出字表,点数护甲无载体,护甲画像随之下线。重新装配 DefenseBuff 时恢复。`
- **`new Profile("水系双方向(冻沝淼㵘,卡5级,10级)", new[] { "冻", "沝", "淼", "㵘" }, …)`(`balance/Program.cs:148`)**:把 沝 换成 冰,画像名同步改成 `水系双方向(冻冰淼㵘,卡5级,10级)`。

- [ ] **Step 3: 编译两个工装**

```bash
cd tools/balance && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
cd tools/trace && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

- [ ] **Step 4: 跑一次仿真确认不炸**

```bash
cd tools/balance && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet run --nologo 2>&1 | head -40
```

只看有没有异常/空卡组警告,读数本身不在本计划的验收范围。

- [ ] **Step 5: 提交**

```bash
git add tools/balance tools/trace
git commit -m "chore(tools): 仿真画像清掉幽灵字,水系画像 沝 换 冰"
```

---

### Task 10: 设计文档同步

**Files:**
- Modify: `docs/design/第5章-流派系统.md:21-24`
- Modify: `docs/design/第10章-战斗数值框架.md:44`
- Modify: `docs/design/第19章-养成与经济系统.md:162`

**Interfaces:**
- Consumes: Task 3 的最终字表
- Produces: 与字表一致的设计文档

- [ ] **Step 1: 第 5 章 —— 木系不再是纯召唤**

`:21-24` 那段「2026-08-25 字表重构:木系不再是召唤流的唯一载体」的框注下补一行:

```
> ⚠ **2026-09-05:木系也不再是纯召唤。** 箭(🟣紫,单体 200)与 葬(🔵蓝,全体 70)是直伤字,
> 推翻了 2026-08-25 的「木系转纯召唤」(棒/棍 当时正因此移出)。召唤物职能仍由配方里的
> 第二个五行部件决定,那条规则不变。
```

同时核对该章「现行 74 字里有 19 个召唤字」这类计数,按新字表重算(用 Task 3 Step 5 的脚本扩一个「统计 Summon 效果的字数」即可)。

- [ ] **Step 2: 第 10 章 —— 穿透 / 破甲 / 护甲的现状**

`:44` 那条「穿透字只剩 刺(15),破甲字只剩 碎(20)/ 溃(10)」改成:

```
    ⚠ **2026-09-05 字表调整后更严重**:刺 随本批移出,**穿透字归零**;破甲字仍是 碎(20)/ 溃(10);
    铠 移出后**点数护甲(`DefenseBuff`)也归零**。三条机制的引擎代码全部保留,等新载体。
```

- [ ] **Step 3: 第 19 章 —— 可收集字的统计口径**

`:162` 那段引用「8-25 字表重构后白/金/橙/红共 37 个字」的数字,按新字表重算后改写。
用这个脚本取数:

```bash
python3 - <<'EOF'
import json
from collections import Counter
d = json.load(open('Brushblade/Assets/StreamingAssets/config/chars.json'))['chars']
play = [c for c in d if not c.get('component')]
c = Counter(x['rarity'] for x in play)
print(c, '可收集字合计', len(play))
print('白金橙红合计', c['White'] + c['Gold'] + c['Orange'] + c['Red'])
EOF
```

改写时**保留**原文那句「白档整档掉不出来」的论证结构,只换数字 —— 那条结论是否仍成立要看新数;
若不再成立,把该句改成描述新状况,并在句末标 `(2026-09-05 重算)`。

- [ ] **Step 4: 提交**

```bash
git add docs/design/
git commit -m "docs: 第 5/10/19 章同步字表调整后的口径"
```

---

### Task 11: 全量验证

**Files:** 无(只跑不改)

- [ ] **Step 1: 三套测试全绿**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ tools/design/tests/ -q
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q \
  -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

离线编译只看 `error CS`;`warning MSB3245` 忽略。

- [ ] **Step 2: 人工对账清单**

```bash
python3 - <<'EOF'
import json
d = json.load(open('Brushblade/Assets/StreamingAssets/config/chars.json'))['chars']
by = {c['id']: c for c in d}
play = [c for c in d if not c.get('component')]
assert len(play) == 60, len(play)
wood = [c['id'] for c in play if c['element'] == 'Wood']
assert len(wood) == 13, wood
for part in ['金', '钅', '刂', '木', '艹', '竹', '水', '氵', '冫', '土', '山', '石', '火', '灬']:
    d0 = by[part]
    assert d0.get('component'), f'{part} 不是部件'
    assert not d0.get('recipe'), f'{part} 有配方了,ComponentKin 的假设被打破'
for gone in '劈战刺铠杖松柏蕉沝淡沏碾砸烧焦灼熣戈刀切崔':
    assert gone not in by, gone
print('对账通过:可出牌字 60、木系 13、等价清单 14 个成员全是叶子部件')
EOF
```

- [ ] **Step 3: 请用户在 Unity Test Runner 里跑 EditMode**

工装的 NUnit 与 Unity 自带的不是同一份(`Is.AnyOf`、集合版 `Does.Not.Contain` 那类差异),
工装绿不等于编辑器绿。请用户打开 Unity 跑一次 EditMode 全量。

- [ ] **Step 4: 请用户 Play 验收三件事**

1. 开局起始收藏 15 张齐全,木系那三张是 枪 / 藤 / 葬。
2. 教程能拆 剿,拿到 巢 与 刂。
3. 出 藻,召出 3 只,挨打后下一回合它们自己回血(玩家血量不变)。

---

## 自查记录

- **规格覆盖**:§1 移除 → Task 2/3;§1.1 部件连带 → Task 3/4;§1.2 叠字链 → Task 2/3;
  §1.3 孤儿机制 → Task 2(详表标注)+ Task 6(删两条守卫测试)+ Task 10(第 10 章);
  §2 新增三字 → Task 2/3;§2.4 竹 → Task 3/4;§3 枪/藤 改档 → Task 2/3;
  §4 引擎 → Task 1;§5 连带触点 → Task 5/7/8/9/10;§6 验证 → Task 11。
- **类型一致性**:`SummonPassive.Regen`(C#)↔ `passive.regen`(chars.json)↔ `"Regen": "regen"`
  (`SUMMON_PASSIVE`)↔ `` `Regen 60` ``(详表 token)四处同名,Task 1/2/3 里逐处对上。
- **已知的判断留给执行者的两处**:Task 3 Step 1 的 `_build()` 助手实际名字、Task 6 的 A/B 分类。
  两处都给了明确判据,不是待定项。
