# 战斗择敌随机化 + 木 L4 择伐 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把战斗里「打谁」的三处恒定选择改成随机,修掉远程敌人看不见前排召唤物的缺陷,并把木脉 L4 从「蕃息」换成「择伐」(召唤物按生克三档择敌)。

**Architecture:** 规则改动全部落在 `Core/Targeting.cs` 这一处唯一裁定点(纯 C#,纯函数)。新增第二条随机流 `_targetRandom` 专供择敌,把既有 `_random` 的流位移面压到最小。技能树侧是一次节点替换:删「蕃息」整条链路,加 `PerkEffect.CounterTargeting`。

**Tech Stack:** Unity 6000.5.2f1 / C#;Core 与 Data 禁止引用 UnityEngine。测试走 `tools/coretests`(Unity 自带 dotnet SDK),Presentation 走 `tools/prescompile` 离线编译。

**Spec:** `docs/superpowers/specs/2026-09-13-战斗择敌随机化-design.md`

## Global Constraints

- **不要在主检出上工作。** 全程在 git worktree 里,从 `main` 分叉。分支名 `feat/battle-targeting-random`。
- **worktree 里跑 prescompile 必须覆盖程序集路径**(`Brushblade/Library/` 不入 git):
  `dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies`
- **禁用 `git stash`** —— worktree 与主检出共享同一个 stash 栈,pop 会弹出别人的改动。对比改动前后用 `git show HEAD:<path>`。
- **只提交你自己改的文件。** 逐个 `git add <path>`,禁用 `git add -A` / `git add .`。
- **测试断言只用 Unity 版 NUnit 也支持的 API**:禁用 `Is.AnyOf` / `Is.All.AnyOf`;`Does.Not.Contain(x)` 只在 x 是 string 时可用,集合判包含一律写成 `Assert.That(list.Contains(x), Is.True/False, "…")`。
- **测试里定位仓库根只能用 `TestContext.CurrentContext.TestDirectory`**,禁用 `AppContext.BaseDirectory`。
- **测试代码禁止直接引用 Newtonsoft**;只能引用 Core / Data 两个程序集。
- **新建 `.cs` 文件必须配 `.meta`**:从同目录别的 `.cs.meta` 复制,换掉 `guid:` 的值(随便改几位十六进制即可,只要全仓库唯一)。
- **Core 与 Data 禁止 `using UnityEngine`,随机一律走 `Core.GameRandom`。**
- 提交信息用 conventional commits,结尾加 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`。

### 单元测试命令(每个任务都用这条)

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

跑单条:在上面命令尾部加 `--filter FullyQualifiedName~TargetingTests.你的测试名`

---

## File Structure

| 文件 | 责任 | 任务 |
|---|---|---|
| `Brushblade/Assets/_Project/Core/Targeting.cs` | 「打谁」的唯一裁定点。本次全部规则改动都在这里 | 2、3、4 |
| `Brushblade/Assets/_Project/Core/BattleEngine.cs` | 持有 `_targetRandom`、快照往返、`BattleConfig` 字段、调用 `Targeting` | 1、2、3、4、5 |
| `Brushblade/Assets/_Project/Core/RunSnapshot.cs` | `BattleSnapshot.TargetRandomState` 字段 | 1 |
| `Brushblade/Assets/_Project/Core/Perk.cs` | `PerkEffect` 枚举、木枝节点定义 | 5 |
| `Brushblade/Assets/_Project/Core/Meta.cs` | perk → `BattleConfig` 注入 | 5 |
| `Brushblade/Assets/_Project/Core/SummonPassive.cs` | 只改一条过时注释(:92) | 2 |
| `Brushblade/Assets/_Project/Presentation/PerkInfo.cs` | 技能节点的名称/描述/详情文案 | 5 |
| `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json` | 玩家可见文案 | 5 |
| `Brushblade/Assets/_Project/Tests/TargetingTests.cs` | 择敌规则的主测试面(纯函数,毫秒级) | 2、3、4 |
| `Brushblade/Assets/_Project/Tests/CounterTargetingTests.cs` | **新建**:择伐从 perk 到引擎的端到端 | 5 |
| `Brushblade/Assets/_Project/Tests/WoodSummonSpeedTests.cs` | **删除**(蕃息废止) | 5 |
| `Brushblade/Assets/_Project/Tests/PerkInjectionTests.cs` | 注入侧断言,改 4 处 | 5 |

---

## Task 1:第二条随机流 `_targetRandom`

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/RunSnapshot.cs`(`BattleSnapshot`,约 :19 `RandomState` 之后)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(字段 :384 附近、公开构造 :806、私有构造 :862、`Capture()` :879、`Restore()` :901)
- Test: `Brushblade/Assets/_Project/Tests/TargetingTests.cs`(本任务只加一条往返测试;放这里是因为它测的是择敌流)

**Interfaces:**
- Produces:`BattleEngine` 私有字段 `private readonly GameRandom _targetRandom;`、常量 `private const int TargetSeedSalt = 0x5BF0_3635;`、`BattleSnapshot.TargetRandomState`(`uint`)。Task 2/3/4 把它传给 `Targeting`。

- [ ] **Step 1: 写失败测试**

在 `TargetingTests.cs` 里加。文件里**已经有**现成的造引擎辅助 `Trio()`(约 :144),
直接拿来用 —— 别自己拼 `RecipeGraph` / `CharDef`,那些签名容易猜错。

```csharp
        // ---- 择敌随机流(2026-09-13)----

        [Test]
        public void TargetRandomStream_SurvivesSnapshotRoundTrip()
        {
            // 择敌走独立的一条流,它必须跟 RandomState 一样进存档 —— 不存的话
            // 挂起再进,择敌序列会从头重来,断点续爬前后分叉。
            var snapshot = Trio().Capture();
            Assert.That(snapshot.TargetRandomState, Is.Not.EqualTo(0u),
                "择敌流的状态要真的存进去");
            Assert.That(snapshot.TargetRandomState, Is.Not.EqualTo(snapshot.RandomState),
                "两条流必须是各自独立的状态,不能是同一个对象");
        }
```

- [ ] **Step 2: 跑测试确认它失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter FullyQualifiedName~TargetRandomStream_SurvivesSnapshotRoundTrip
```

Expected: 编译失败,`'BattleSnapshot' does not contain a definition for 'TargetRandomState'`

- [ ] **Step 3: 加快照字段**

`RunSnapshot.cs`,在 `BattleSnapshot` 里 `public uint RandomState { get; set; }` 那一行之后插入:

```csharp
        /// <summary>择敌专用随机流的状态(2026-09-13)。与 <see cref="RandomState"/> 并列而不是合并:
        /// 择敌的随机量随召唤物只数、敌人只数波动,混进主流会让暴击/命中/掉字的序列跟着漂,
        /// 上千条带种子的既有测试会一起位移。两条流由同一颗 Seed 派生,可复现性不变。</summary>
        public uint TargetRandomState { get; set; }
```

- [ ] **Step 4: 加引擎字段与常量**

`BattleEngine.cs`,在 `private readonly GameRandom _random;`(约 :384)那一行之后插入:

```csharp
        /// <summary>择敌专用随机流(2026-09-13)。见 <see cref="BattleSnapshot.TargetRandomState"/>。
        /// 唯一消费方是 <see cref="Targeting"/> 的三个择敌函数,别拿它摇别的。</summary>
        private readonly GameRandom _targetRandom;

        /// <summary>择敌流的种子偏移。与主种子异或即可 —— GameRandom 构造时会再 Scramble 一道,
        /// 两条流不会因为种子只差一个常数而相关。</summary>
        private const int TargetSeedSalt = 0x5BF03635;
```

- [ ] **Step 5: 在两个构造里初始化**

公开构造(约 :806),在 `_random = new GameRandom(seed);` 之后插入:

```csharp
            _targetRandom = new GameRandom(seed ^ TargetSeedSalt);
```

私有构造(约 :862)的签名加一个参数,并赋值:

```csharp
        private BattleEngine(RecipeGraph graph, BattleConfig config,
            IReadOnlyDictionary<string, int> cardLevels, GameRandom random, GameRandom targetRandom)
        {
            _graph = graph;
            _config = config;
            _slotMask = ClampSlotMask(config);
            _cardLevels = cardLevels;
            _random = random;
            _targetRandom = targetRandom;
            _forge = new ForgeState(new List<string>(), new List<string>());
        }
```

- [ ] **Step 6: 接上 Capture / Restore**

`Capture()`(约 :879),在 `RandomState = _random.State,` 之后插入:

```csharp
                TargetRandomState = _targetRandom.State,
```

`Restore()`(约 :901),把

```csharp
            var engine = new BattleEngine(graph, config, cardLevels, GameRandom.FromState(snapshot.RandomState))
```

改成

```csharp
            var engine = new BattleEngine(graph, config, cardLevels,
                GameRandom.FromState(snapshot.RandomState),
                GameRandom.FromState(snapshot.TargetRandomState))
```

- [ ] **Step 7: 跑全量测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿。本任务没有改任何行为,`_targetRandom` 还没有消费方。若有红,先修红再往下。

> ⚠ 若有既存的快照往返测试逐字段比对 `BattleSnapshot`,新字段可能让它变红 —— 那是该测试要补一行的正常情况,补上即可。

- [ ] **Step 8: 提交**

```bash
git add Brushblade/Assets/_Project/Core/RunSnapshot.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/TargetingTests.cs
git commit -m "feat(core): 择敌改走独立随机流 _targetRandom

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 2:敌方选我方目标(spec §1)

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Targeting.cs`(`PickAllyTarget` 及其私有助手)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(:2208 附近,改传 `_targetRandom`)
- Modify: `Brushblade/Assets/_Project/Core/SummonPassive.cs`(:92 一条过时注释)
- Test: `Brushblade/Assets/_Project/Tests/TargetingTests.cs`

**Interfaces:**
- Consumes: Task 1 的 `_targetRandom`
- Produces: `Targeting.PickAllyTarget` 签名**不变**(仍是 `(AttackRange, AttackFocus, IReadOnlyList<SummonState>, int frontRow, GameRandom)`),只改行为。新增私有助手 `PickOne(IReadOnlyList<int>, GameRandom)` 与 `AliveSlots(IReadOnlyList<SummonState>, int from, int toExclusive)`,Task 3/4 不依赖它们。

- [ ] **Step 1: 写失败测试**

在 `TargetingTests.cs` 里加这四条。同时**改掉两条会被本任务改变语义的既有测试**(见 Step 2 说明)。

```csharp
        // ---- 2026-09-13:够得着的那一段里均匀随机 ----

        [Test]
        public void Melee_PicksRandomlyAmongFrontRowSummons()
        {
            // 改前恒打前排槽序最小的那只(槽 1),前排站两只时第二只永远不挨打。
            var seen = new HashSet<int>();
            var random = new GameRandom(5);
            for (int i = 0; i < 200; i++)
                seen.Add(Targeting.PickAllyTarget(AttackRange.Melee, AttackFocus.Default,
                    Line(1, 2, 4), FrontRow, random));
            Assert.That(seen.Count, Is.EqualTo(2), "前排两只都摇得到,后排那只不该进池");
            Assert.That(seen.Contains(1), Is.True);
            Assert.That(seen.Contains(2), Is.True);
        }

        [Test]
        public void Ranged_CanHitFrontRowSummons()
        {
            // 改前远程的候选池从 frontRow 起算,前排召唤物一次都打不到(spec §0)。
            var seen = new HashSet<int>();
            var random = new GameRandom(11);
            for (int i = 0; i < 300; i++)
                seen.Add(Targeting.PickAllyTarget(AttackRange.Ranged, AttackFocus.Default,
                    Line(0, 1, 5), FrontRow, random));
            Assert.That(seen.Count, Is.EqualTo(4), "前排两只 + 后排一只 + 玩家,全在池里");
            Assert.That(seen.Contains(0), Is.True, "前排槽 0 也该挨得着");
            Assert.That(seen.Contains(1), Is.True);
            Assert.That(seen.Contains(5), Is.True);
            Assert.That(seen.Contains(Targeting.PlayerTarget), Is.True);
        }

        [Test]
        public void RangedFocusPlayer_IsPulledByFrontRowTaunt()
        {
            // 用户 2026-09-13 第 5 条诉求:锁人要先吃嘲讽,再打玩家。
            // 改前嘲讽扫描区间是 [frontRow, Count),前排的嘲讽者对远程完全不可见。
            var line = TauntLine(new[] { 0, 5 }, 0);
            for (int i = 0; i < 50; i++)
                Assert.That(Targeting.PickAllyTarget(AttackRange.Ranged, AttackFocus.Player,
                    line, FrontRow, new GameRandom(i + 1)), Is.EqualTo(0),
                    "前排嘲讽把锁人拉过去");
        }

        [Test]
        public void Ranged_PicksRandomlyAmongTauntersAcrossBothRows()
        {
            // 两个嘲讽者分居前后排,远程够得着两个 —— 该在两者之间随机,不能恒定一个。
            var line = TauntLine(new[] { 0, 5 }, 0, 5);
            var seen = new HashSet<int>();
            var random = new GameRandom(13);
            for (int i = 0; i < 200; i++)
                seen.Add(Targeting.PickAllyTarget(AttackRange.Ranged, AttackFocus.Default,
                    line, FrontRow, random));
            Assert.That(seen.Count, Is.EqualTo(2), "只在两个嘲讽者之间随机,玩家不进池");
            Assert.That(seen.Contains(0), Is.True);
            Assert.That(seen.Contains(5), Is.True);
        }
```

`TauntLine` 是新的辅助函数,加在文件里现有的 `Line(...)` 辅助函数旁边。
**`SummonState.Passive` 是只读属性(`{ get; }`),只能从构造参数传**,不能造完再赋值:

```csharp
        /// <summary>同 Line,但 tauntSlots 里的槽位带嘲讽被动。
        /// Passive 是只读属性,只能走构造参数(`SummonState(char, element, hp, attack, passive)`)。</summary>
        private static SummonState[] TauntLine(int[] aliveSlots, params int[] tauntSlots)
        {
            var slots = new SummonState[6];
            foreach (int s in aliveSlots)
                slots[s] = new SummonState($"木{s}", Element.Wood, 100, 10,
                    System.Array.IndexOf(tauntSlots, s) >= 0
                        ? new SummonPassive { Taunt = true } : null);
            return slots;
        }
```

- [ ] **Step 2: 改两条被本任务改变语义的既有测试**

`Melee_HitsFrontmostFrontRowSummon`(约 :22):前排 `Line(1, 2, 4)` 现在会在 1、2 之间随机,`Is.EqualTo(1)` 不再成立。**把这条测试的阵型改成前排只有一只**,让它继续守「近战先吃前排」这条:

```csharp
        [Test]
        public void Melee_HitsTheOnlyFrontRowSummon()
        {
            // 前排只有一只时候选池退化成单元素 —— 守的是「近战被前排拦下」,不是「取槽序最小」。
            // 前排多只时的随机分布另有 Melee_PicksRandomlyAmongFrontRowSummons 守。
            Assert.That(Targeting.PickAllyTarget(AttackRange.Melee, AttackFocus.Default,
                Line(1, 4), FrontRow, new GameRandom(1)), Is.EqualTo(1),
                "前排那只挡下这一击");
        }
```

`Ranged_IgnoresFrontRow`(约 :88):这条钉的正是被本次拍板推翻的旧口径。**整条删掉** —— 新的 `Ranged_CanHitFrontRowSummons` 是它的替身。

`BlockedByFrontRow_ConsumesNoRandomness`(约 :112)**保留不动**:它的阵型 `Line(0, 4, 5)` 前排只有槽 0 一只,候选池单元素、走短路,仍然一个随机数都不摇。改完要确认它还是绿的。

- [ ] **Step 3: 跑测试确认新增的四条失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter FullyQualifiedName~TargetingTests
```

Expected: `Melee_PicksRandomlyAmongFrontRowSummons`、`Ranged_CanHitFrontRowSummons`、`RangedFocusPlayer_IsPulledByFrontRowTaunt`、`Ranged_PicksRandomlyAmongTauntersAcrossBothRows` 四条 FAIL。

- [ ] **Step 4: 重写 `PickAllyTarget`**

`Targeting.cs`,把整个 `PickAllyTarget` 方法体(含文档注释)替换成:

```csharp
        /// <summary>敌人选我方目标。返回召唤物槽位,或 <see cref="PlayerTarget"/>。
        ///
        /// 一条口径(2026-09-13 重写):**先按排位定出「这一击够得着的那一段」,
        /// 段内有嘲讽就收窄到嘲讽者,再在剩下的候选里均匀随机**。
        ///
        /// 站位先于嘲讽(2026-09-03 用户拍板):嘲讽只决定「够得着的那些人里先打谁」,
        /// 不决定「够不够得着」。后排的嘲讽者因此仍要等前排清空才挨得上近战 —— 否则玩家把
        /// 嘲讽物往后排一塞,前排的召唤物就永远不会被近战碰到,站位这套规则整个失效。
        ///
        /// 均匀随机的口径(spec §4.1):候选池是**一个**池子,不是先五五开决定「打召唤物
        /// 还是打玩家」。池子越厚玩家越安全。
        ///
        /// ⚠ 2026-09-13 之前,远程的候选池与嘲讽扫描都从 <paramref name="frontRow"/> 起算,
        /// 前排召唤物对远程攻击完全不可见 —— 玩家把嘲讽肉盾摆在前排,远程「锁人」照样直穿打脸。
        /// 现在远程够得着全场,所以这两处都从 0 起算。近战那一支不受影响:它被前排拦下时
        /// 走上面的前排分支,前排清空后下面这段扫全场也等价于扫后排(前排已无活人)。
        ///
        /// ⚠ 候选只有一个时**不摇随机数**(<see cref="PickOne"/>)——这一保证是两层叠的:
        /// 第一层是 <see cref="GameRandom.Next"/> 自己的性质(maxExclusive ≤ 1 直接 return 0,
        /// 不碰内部状态),PickOne 里的短路是第二层纵深防御。两层任一在位都够,但两层都要在。</summary>
        public static int PickAllyTarget(AttackRange range, AttackFocus focus,
            IReadOnlyList<SummonState> summons, int frontRow, GameRandom random)
        {
            if (range == AttackRange.Melee)
            {
                var front = AliveSlots(summons, 0, frontRow);
                if (front != null)
                    // 被前排拦下:候选段就是前排,嘲讽在这个段里生效。
                    return PickOne(TauntingSlots(summons, 0, frontRow) ?? front, random);
            }

            // 够得着全场了(远程,或近战但前排已空)。
            var taunters = TauntingSlots(summons, 0, summons.Count);
            if (taunters != null) return PickOne(taunters, random);

            // 嘲讽压过的只剩 Focus.Player 的死盯玩家 —— 那不是排位,是「打谁」的偏好。
            if (focus == AttackFocus.Player) return PlayerTarget;

            var pool = AliveSlots(summons, 0, summons.Count) ?? new List<int>();
            pool.Add(PlayerTarget);
            return PickOne(pool, random);
        }

        /// <summary>槽位区间 [from, toExclusive) 里全部**存活**召唤物的槽号;一个都没有返回 null。
        /// 返回**新表**,调用方可以直接往里 Add(玩家那一项就是这么加的)。</summary>
        private static List<int> AliveSlots(IReadOnlyList<SummonState> summons,
            int from, int toExclusive)
        {
            List<int> slots = null;
            for (int s = from; s < toExclusive && s < summons.Count; s++)
                if (summons[s] != null && summons[s].Alive) (slots ??= new List<int>()).Add(s);
            return slots;
        }

        /// <summary>候选表里抽一个。**单候选不摇随机数** —— 见 PickAllyTarget 的方法头注释,
        /// 上千条带种子的既有测试靠这条不位移。</summary>
        private static int PickOne(IReadOnlyList<int> candidates, GameRandom random) =>
            candidates.Count == 1 ? candidates[0] : candidates[random.Next(candidates.Count)];
```

`TauntingSlots` 的文档注释里有一句「吃区间而不是扫全场(2026-09-03):嘲讽在『排位已经放行的那一段』里生效,段外的嘲讽者对这一击不存在。」—— 这句仍然成立,不用改。

- [ ] **Step 5: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter FullyQualifiedName~TargetingTests
```

Expected: `TargetingTests` 全绿。

- [ ] **Step 6: 把引擎改成用择敌流**

`BattleEngine.cs` 约 :2208,把

```csharp
                int tankIdx = Targeting.PickAllyTarget(enemy.Def.Range, enemy.Def.Focus,
                    _summons, FrontRowSize, _random);
```

改成

```csharp
                int tankIdx = Targeting.PickAllyTarget(enemy.Def.Range, enemy.Def.Focus,
                    _summons, FrontRowSize, _targetRandom);
```

同时把它上方那段注释的第一句改成:

```csharp
                // 目标裁定(2026-08-20,2026-09-13 重写):近战被我方前排拦下、段内随机;
                // 远程够得着全场;嘲讽在够得着的那一段里收窄候选;Focus.Player 的够得着玩家时
                // 死盯玩家。规则全在 Targeting,这里只执行。走 _targetRandom 而不是 _random,
                // 见该字段的注释。
```

- [ ] **Step 7: 改 `SummonPassive.cs:92` 的过时注释**

那一行现在写的是「完全由 `Targeting.PickAllyTarget` 的均匀随机决定,输出量不受玩家控制。」
把它改成:

```csharp
        /// 完全由 <see cref="Targeting.PickAllyTarget"/> 决定(2026-09-13 起:够得着的那一段里
        /// 嘲讽优先、段内均匀随机),输出量不受玩家控制。
```

> 先 `sed -n '85,95p' Brushblade/Assets/_Project/Core/SummonPassive.cs` 看清上下文再改,别改错行。

- [ ] **Step 8: 跑全量测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿,或只有**带后排/多只召唤物**的用例变红。

⚠ **变红处理纪律**:
- 变红的用例若**没有召唤物**,说明 `_targetRandom` 分流没做对(有代码还在用 `_random` 择敌)——**回头查分流,不要改期望值**。
- 变红的用例若有多只召唤物,逐条看:是不是只有「敌人这一拍打了谁」变了、其余数值口径没变。确认后再改期望值,并在测试里补一句注释说明为什么改。
- **禁止**用「重跑一遍把断言值抄成实际输出」的方式批量修红。

- [ ] **Step 9: 提交**

```bash
git add Brushblade/Assets/_Project/Core/Targeting.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Core/SummonPassive.cs \
        Brushblade/Assets/_Project/Tests/TargetingTests.cs
# 若 Step 8 改了别的测试文件,逐个 git add 它们
git commit -m "feat(core): 敌方择敌段内随机,远程改为够得着全场

- 近战被前排拦下时在前排存活者里随机,不再恒打槽序最小的
- 远程的候选池与嘲讽扫描从 [frontRow,N) 扩到 [0,N):修掉前排嘲讽
  对远程「锁人」完全不可见的缺陷
- 择敌改走 _targetRandom

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 3:召唤物选敌同排随机(spec §2)

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Targeting.cs`(`PickEnemyTargetForSummon`、`PickByRow`、`Subset`、`IndexIn`)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(:2080 附近)
- Test: `Brushblade/Assets/_Project/Tests/TargetingTests.cs`

**Interfaces:**
- Consumes: Task 1 的 `_targetRandom`;Task 2 的 `PickOne`
- Produces: 新签名

  ```csharp
  public static int PickEnemyTargetForSummon(IReadOnlyList<EnemyState> enemies, bool ranged,
      GameRandom random, TargetShape shape = TargetShape.Single,
      bool preferUnfrozen = false, bool preferUnslowed = false)
  ```

  新私有助手 `RowCandidates(IReadOnlyList<EnemyState>, bool ranged) → List<int>`(原表下标,或 null)与 `Prefer(IReadOnlyList<EnemyState>, List<int>, Func<EnemyState,bool>) → List<int>`。Task 4 在这个签名上再加一个 `Element? counterTargeting = null` 参数。

**⚠ 本任务包含一处刻意的行为变化。** 改前 `preferUnfrozen` / `preferUnslowed` / 贯穿选列这三道筛子作用于**全体敌人**,筛完才走排位 —— 也就是筛子压过排位,近战召唤物会被引去打后排。改后改成**排位先筛,筛子在该排内生效**,与用户 2026-09-13 拍板的「排位先筛,同排内随机」统一,也为 Task 4 的三档留出正确层级(见 spec §3.3)。既有的三条筛子测试(`SummonPassiveTests.SummonTargeting_*`)敌人都在同一排,不受影响。

- [ ] **Step 1: 写失败测试**

`TargetingTests.cs` 里加。文件里**已经有**造敌阵的辅助函数 `Grid(params (EnemyRow Row, int Column)[] slots)`
(约 :320),直接用它 —— `EnemyState` 的构造是 `internal`,Tests 程序集能访问,但 `Row`/`Column`
必须走对象初始化器设,别自己另造一份。

```csharp
        // ---- 2026-09-13:召唤物同排随机 ----

        [Test]
        public void SummonMelee_PicksRandomlyAmongFrontRowEnemies()
        {
            // 改前恒取 _enemies 下标序最小的存活者,前排三只时后两只永远不挨打。
            var enemies = Grid((EnemyRow.Front, 0), (EnemyRow.Front, 1), (EnemyRow.Front, 2));
            var seen = new HashSet<int>();
            var random = new GameRandom(17);
            for (int i = 0; i < 300; i++)
                seen.Add(Targeting.PickEnemyTargetForSummon(enemies, ranged: false, random));
            Assert.That(seen.Count, Is.EqualTo(3), "前排三只都摇得到");
        }

        [Test]
        public void SummonRanged_PicksRandomlyAmongBackRowEnemies()
        {
            var enemies = Grid((EnemyRow.Back, 0), (EnemyRow.Back, 1));
            var seen = new HashSet<int>();
            var random = new GameRandom(19);
            for (int i = 0; i < 200; i++)
                seen.Add(Targeting.PickEnemyTargetForSummon(enemies, ranged: true, random));
            Assert.That(seen.Count, Is.EqualTo(2), "后排两只都摇得到");
        }

        [Test]
        public void SummonMelee_StillPrefersFrontRow()
        {
            // 排位仍然算数:前排还有人时,近战召唤物一次都不该摸到后排。
            var enemies = Grid((EnemyRow.Front, 0), (EnemyRow.Back, 0), (EnemyRow.Back, 1));
            var random = new GameRandom(23);
            for (int i = 0; i < 100; i++)
                Assert.That(Targeting.PickEnemyTargetForSummon(enemies, ranged: false, random),
                    Is.EqualTo(0), "前排那只挡着,近战够不着后排");
        }

        [Test]
        public void SummonTargeting_SingleCandidate_ConsumesNoRandomness()
        {
            // 单候选短路:场上只有一只敌人时一个随机数都不该摇,
            // 否则每一拍召唤物出手都会推着整条择敌流走,分流的意义打折。
            var enemies = Grid((EnemyRow.Front, 0));
            var a = new GameRandom(42);
            var b = new GameRandom(42);
            Targeting.PickEnemyTargetForSummon(enemies, ranged: false, a);
            Assert.That(a.Next(1000), Is.EqualTo(b.Next(1000)), "单候选不消耗随机数");
        }

        [Test]
        public void SummonTargeting_NoEnemies_ReturnsMinusOne()
        {
            Assert.That(Targeting.PickEnemyTargetForSummon(new List<EnemyState>(),
                ranged: false, new GameRandom(1)), Is.EqualTo(-1));
        }
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter FullyQualifiedName~TargetingTests
```

Expected: 编译失败(签名没有 `random` 参数)。

- [ ] **Step 3: 重写 `PickEnemyTargetForSummon`**

`Targeting.cs`,把 `PickEnemyTargetForSummon`、`PickByRow`、`Subset`、`IndexIn` 这四个方法整体替换成下面这段(`HasBothRowsInColumn` 与 `FirstAliveInRow` 保留不动,它们还有别的调用方):

```csharp
        /// <summary>召唤物出手选敌。近战打敌方前排(全清则打后排);远程优先打后排(后排空了才打前排)。
        /// 无敌可打返回 −1。
        ///
        /// 排位不影响召唤物**自己**能不能出手:站后排的近战照常攻击(用户 2026-08-20 拍板)。
        ///
        /// 裁定次序(2026-09-13 重写):
        /// <code>
        /// 排位先筛(远程→后排 / 近战→前排)
        ///   → 控场偏好(冻结/减速;筛不出来退回本排全体)
        ///     → 贯穿选列
        ///       → 剩余候选里均匀随机
        /// </code>
        /// ⚠ 改前这三道筛子作用于**全体敌人**、筛完才走排位,也就是筛子压过排位 ——
        /// 近战召唤物会为了追一只没被冻住的后排怪而越过前排。2026-09-13 用户拍板
        /// 「排位先筛,同排内随机」,故整体移到排位之后。
        ///
        /// 控场偏好的顺序理由不变:冻结/减速都是刷新而非叠加,打已中招的目标等于白费;
        /// 贯穿选列只关乎「能不能多打一个」,排在后面。
        ///
        /// 候选只有一个时不摇随机数(<see cref="PickOne"/>),与 PickAllyTarget 同一条纪律。</summary>
        public static int PickEnemyTargetForSummon(IReadOnlyList<EnemyState> enemies, bool ranged,
            GameRandom random,
            TargetShape shape = TargetShape.Single,
            bool preferUnfrozen = false, bool preferUnslowed = false)
        {
            var pool = RowCandidates(enemies, ranged);
            if (pool == null) return -1;

            if (preferUnfrozen)
                pool = Prefer(enemies, pool, e => !e.Statuses.Has(StatusKind.Freeze));
            if (preferUnslowed)
                // 「已减速」= 速度修正为负,与 DamageCondition.Controlled 的减速那一半同判据。
                // 不看是谁挂的:别人挂的减速同样让这一下失去意义。
                pool = Prefer(enemies, pool, e => e.Statuses.TotalMagnitude(StatusKind.SpeedModifier) >= 0);
            if (shape == TargetShape.Skewer)
                // 贯穿打的是整列。优先挑**前后排都有人**的那一列 —— 挑到空对位的列
                // 只会中一只,贯穿就白给了。挑不出来(没有任何列是满的)就不挑。
                pool = Prefer(enemies, pool, e => HasBothRowsInColumn(enemies, e.Column));

            return PickOne(pool, random);
        }

        /// <summary>这一击够得着的那一排的存活者下标:远程先后排、近战先前排,该排空了退另一排。
        /// 两排都空返回 null。
        ///
        /// 跨排 Boss 两排都占,所以它在两种情形下都进得了候选 —— 走 <see cref="EnemyState.Occupies"/>
        /// 而不是 <c>Row ==</c>,与 <see cref="FirstAliveInRow"/> 同判据。</summary>
        private static List<int> RowCandidates(IReadOnlyList<EnemyState> enemies, bool ranged)
        {
            var first = AliveInRow(enemies, ranged ? EnemyRow.Back : EnemyRow.Front);
            return first ?? AliveInRow(enemies, ranged ? EnemyRow.Front : EnemyRow.Back);
        }

        /// <summary>这一排全部存活者的下标;一个都没有返回 null。</summary>
        private static List<int> AliveInRow(IReadOnlyList<EnemyState> enemies, EnemyRow row)
        {
            List<int> slots = null;
            for (int i = 0; i < enemies.Count; i++)
                if (enemies[i].Alive && enemies[i].Occupies(row)) (slots ??= new List<int>()).Add(i);
            return slots;
        }

        /// <summary>候选里满足谓词的那些;**一个都没有就原样退回**(偏好是筛子,不是硬性条件)。</summary>
        private static List<int> Prefer(IReadOnlyList<EnemyState> enemies, List<int> pool,
            System.Func<EnemyState, bool> keep)
        {
            List<int> kept = null;
            foreach (int i in pool)
                if (keep(enemies[i])) (kept ??= new List<int>()).Add(i);
            return kept ?? pool;
        }
```

删掉的 `PickByRow` / `Subset` / `IndexIn` 是本次改动造成的孤儿,确认全仓库无其他调用方后删净:

```bash
grep -rn "PickByRow\|Targeting.Subset\|IndexIn" Brushblade/Assets/_Project/ --include="*.cs"
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter FullyQualifiedName~TargetingTests
```

Expected: `TargetingTests` 全绿。

- [ ] **Step 5: 接上引擎**

`BattleEngine.cs` 约 :2080,把

```csharp
            int target = Targeting.PickEnemyTargetForSummon(_enemies, passive?.Ranged ?? false,
                shape,
                preferUnfrozen: (passive?.OnHitFreezeChance ?? 0) > 0,
                preferUnslowed: (passive?.OnHitSlowPercent ?? 0) > 0);
```

改成

```csharp
            int target = Targeting.PickEnemyTargetForSummon(_enemies, passive?.Ranged ?? false,
                _targetRandom, shape,
                preferUnfrozen: (passive?.OnHitFreezeChance ?? 0) > 0,
                preferUnslowed: (passive?.OnHitSlowPercent ?? 0) > 0);
```

它上方那句注释「全部敌人默认前排时,本行与改前的『从 0 扫到第一个存活』逐位等价 —— 既有战斗零行为变化。」已经不成立了,改成:

```csharp
            // 近战打敌方前排、远程优先打后排(2026-08-20);2026-09-13 起同排内均匀随机,
            // 走 _targetRandom。敌人只剩一只时短路不摇随机数,那一档仍与改前逐位等价。
```

- [ ] **Step 6: 跑全量测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿,或只有**敌人 ≥2 只且有召唤物出手**的用例变红。变红处理纪律同 Task 2 Step 8。

特别复核这三条(它们钉的是筛子语义,敌人都在同排,应当仍绿):
`SummonPassiveTests.SummonTargeting_PrefersUnfrozenEnemy`、`SummonTargeting_PrefersUnslowedEnemy`、`SummonTargeting_AllSlowed_FallsBackToNormalPick`。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/Targeting.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Tests/TargetingTests.cs
git commit -m "feat(core): 召唤物选敌改为同排内均匀随机

排位先筛、筛子与随机在该排内生效;原先三道偏好筛子压过排位的次序
一并调整(spec §3.3)。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 4:三档择敌(Targeting 层,spec §3.2/§3.3)

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Targeting.cs`
- Test: `Brushblade/Assets/_Project/Tests/TargetingTests.cs`

**Interfaces:**
- Consumes: Task 3 的 `PickEnemyTargetForSummon` 与 `Prefer`
- Produces: 签名末尾加 `Element? counterTargeting = null`:

  ```csharp
  public static int PickEnemyTargetForSummon(IReadOnlyList<EnemyState> enemies, bool ranged,
      GameRandom random, TargetShape shape = TargetShape.Single,
      bool preferUnfrozen = false, bool preferUnslowed = false,
      Element? counterTargeting = null)
  ```

  `null` = 未点择伐,行为与 Task 3 完全相同。Task 5 从 `BattleConfig.CounterTargeting` 决定传不传。

- [ ] **Step 1: 写失败测试**

`TargetingTests.cs`。既有的 `Grid` 辅助函数把元素写死成 `Element.Heart`,三档裁定要指定元素,
所以在它旁边加一个变体(**不要改 `Grid` 本身**,它被别的测试用着):

```csharp
        /// <summary>同 Grid,但每项还指定元素 —— 择伐的三档裁定要用。</summary>
        private static List<EnemyState> ElementGrid(
            params (EnemyRow Row, int Column, Element Element)[] slots)
        {
            var list = new List<EnemyState>();
            foreach (var (row, column, element) in slots)
            {
                var def = new EnemyDef($"怪{list.Count}", element, 100, 10, row: row);
                list.Add(new EnemyState(def, 0, null) { Row = row, Column = column });
            }
            return list;
        }
```

⚠ **生克环以 `docs/design/wuxing-reference.md` 为准。** 下面的测试假定「木克土、金克木」。
写测试前先跑这两条确认,别凭记忆:

```bash
sed -n '10,35p' Brushblade/Assets/_Project/Core/WuxingResolver.cs
grep -n "克" docs/design/wuxing-reference.md | head -20
```

若实际的环与假定不符,把下面测试里的元素按真实的环换掉(结构不变:一只被召唤物克的、
一只中立的、一只克召唤物的)。

```csharp
        // ---- 2026-09-13:择伐(木 L4)三档择敌 ----

        [Test]
        public void CounterTargeting_PrefersTheElementItCounters()
        {
            // 木系召唤物:克土,被金克,对火/水/心中立。三档 = 土 > 火 > 金。
            var enemies = ElementGrid((EnemyRow.Front, 0, Element.Metal),
                (EnemyRow.Front, 1, Element.Fire), (EnemyRow.Front, 2, Element.Earth));
            var random = new GameRandom(29);
            for (int i = 0; i < 100; i++)
                Assert.That(Targeting.PickEnemyTargetForSummon(enemies, ranged: false, random,
                    counterTargeting: Element.Wood), Is.EqualTo(2), "只打被它克的土");
        }

        [Test]
        public void CounterTargeting_FallsToNeutralWhenNoVictimAlive()
        {
            var enemies = ElementGrid((EnemyRow.Front, 0, Element.Metal),
                (EnemyRow.Front, 1, Element.Fire));
            var random = new GameRandom(31);
            for (int i = 0; i < 100; i++)
                Assert.That(Targeting.PickEnemyTargetForSummon(enemies, ranged: false, random,
                    counterTargeting: Element.Wood), Is.EqualTo(1), "没有土就打中立的火,不打克它的金");
        }

        [Test]
        public void CounterTargeting_StillAttacksWhenOnlyCounteredRemain()
        {
            // 三档全空不是「不出手」:只剩克它的敌人时照打。
            var enemies = ElementGrid((EnemyRow.Front, 0, Element.Metal),
                (EnemyRow.Front, 1, Element.Metal));
            var seen = new HashSet<int>();
            var random = new GameRandom(37);
            for (int i = 0; i < 200; i++)
                seen.Add(Targeting.PickEnemyTargetForSummon(enemies, ranged: false, random,
                    counterTargeting: Element.Wood));
            Assert.That(seen.Count, Is.EqualTo(2), "同档两只之间仍然随机");
        }

        [Test]
        public void CounterTargeting_DoesNotOutrankRowPosition()
        {
            // spec §3.3:排位压过三档。相克的土在后排,近战召唤物仍打前排的金。
            var enemies = ElementGrid((EnemyRow.Front, 0, Element.Metal),
                (EnemyRow.Back, 0, Element.Earth));
            var random = new GameRandom(41);
            for (int i = 0; i < 100; i++)
                Assert.That(Targeting.PickEnemyTargetForSummon(enemies, ranged: false, random,
                    counterTargeting: Element.Wood), Is.EqualTo(0),
                    "不为了追相克敌人越过前排");
        }

        [Test]
        public void CounterTargeting_HeartSummonHasNoPreference()
        {
            // 心不在生克环内 —— 全部敌人同档,退化成均匀随机。
            var enemies = ElementGrid((EnemyRow.Front, 0, Element.Metal),
                (EnemyRow.Front, 1, Element.Earth), (EnemyRow.Front, 2, Element.Water));
            var seen = new HashSet<int>();
            var random = new GameRandom(43);
            for (int i = 0; i < 300; i++)
                seen.Add(Targeting.PickEnemyTargetForSummon(enemies, ranged: false, random,
                    counterTargeting: Element.Heart));
            Assert.That(seen.Count, Is.EqualTo(3), "三只都摇得到");
        }

        [Test]
        public void CounterTargeting_Off_IsUnchanged()
        {
            // 未点择伐(counterTargeting 缺省 null)时,分布与 Task 3 完全一致。
            var enemies = ElementGrid((EnemyRow.Front, 0, Element.Metal),
                (EnemyRow.Front, 1, Element.Earth));
            var seen = new HashSet<int>();
            var random = new GameRandom(47);
            for (int i = 0; i < 200; i++)
                seen.Add(Targeting.PickEnemyTargetForSummon(enemies, ranged: false, random));
            Assert.That(seen.Count, Is.EqualTo(2), "不挑元素,两只都摇得到");
        }
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter FullyQualifiedName~TargetingTests
```

Expected: 编译失败(没有 `counterTargeting` 参数)。

- [ ] **Step 3: 实现三档**

`Targeting.cs`,给 `PickEnemyTargetForSummon` 加参数,并在**排位之后、控场偏好之前**插入三档:

```csharp
            var pool = RowCandidates(enemies, ranged);
            if (pool == null) return -1;

            // 择伐(木 L4,2026-09-13):排内按生克三档取最高的非空档。
            // 排在排位**之后**是硬次序 —— 三档是全覆盖的(任何敌人必落入某一档),
            // 放到控场偏好那一层会永远生效、把排位整个压掉(spec §3.3)。
            if (counterTargeting.HasValue) pool = TopKeTier(enemies, pool, counterTargeting.Value);

            if (preferUnfrozen)
                pool = Prefer(enemies, pool, e => !e.Statuses.Has(StatusKind.Freeze));
```

在 `Prefer` 旁边加:

```csharp
        /// <summary>候选里生克档位最高的那些(择伐,2026-09-13)。
        /// 相克 > 中立 > 被克,取最高的非空档 —— 三档全覆盖,所以返回值恒非空。
        ///
        /// 走整数档而不是直接比 <see cref="WuxingResolver.KeMultiplier"/> 的 float:
        /// 浮点相等比较在不同运行时下是雷(见 float 精度那一条教训),而这里要的本来
        /// 就只是序关系。判据仍然读同一张 Ke 表(Victim / Counter),没有第二份相克环。</summary>
        private static List<int> TopKeTier(IReadOnlyList<EnemyState> enemies, List<int> pool,
            Element attacker)
        {
            int best = int.MinValue;
            foreach (int i in pool)
            {
                int tier = KeTier(attacker, enemies[i].Element);
                if (tier > best) best = tier;
            }
            var top = new List<int>();
            foreach (int i in pool)
                if (KeTier(attacker, enemies[i].Element) == best) top.Add(i);
            return top;
        }

        /// <summary>生克档位:2 = 我克它,1 = 互不克(含心),0 = 它克我。</summary>
        private static int KeTier(Element attacker, Element defender)
        {
            if (WuxingResolver.Victim(attacker) == defender) return 2;
            if (WuxingResolver.Counter(attacker) == defender) return 0;
            return 1;
        }
```

> `WuxingResolver.Victim(a)` = 「a 克谁」,`WuxingResolver.Counter(a)` = 「谁克 a」。两个都返回 `Element?`,心系恒为 `null`,与任何 `Element` 比较都不相等 → 心恒落中立档。**动手前先 `sed -n '20,40p' Brushblade/Assets/_Project/Core/WuxingResolver.cs` 确认这两个方法的语义与返回类型。**

- [ ] **Step 4: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter FullyQualifiedName~TargetingTests
```

Expected: `TargetingTests` 全绿。

- [ ] **Step 5: 跑全量测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿。本任务还没接进引擎(`counterTargeting` 缺省 `null`),不该有任何行为变化。**若有红,说明默认参数写错了或误改了 Task 3 的路径。**

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Core/Targeting.cs \
        Brushblade/Assets/_Project/Tests/TargetingTests.cs
git commit -m "feat(core): Targeting 支持按生克三档择敌(择伐的裁定部分)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 5:木 L4 蕃息 → 择伐(spec §3.1/§3.4/§3.5/§6)

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Perk.cs`(`PerkEffect` :25、`AddWuxing(list, "wood", …)` :150-152)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`BattleConfig.WoodSummonSpeedBonus` :201-210、消费点 :2861、召唤物出手 :2080)
- Modify: `Brushblade/Assets/_Project/Core/Meta.cs`(:309)
- Modify: `Brushblade/Assets/_Project/Presentation/PerkInfo.cs`(:166、:229-231 附近)
- Modify: `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`(:519、:568、:614)
- Delete: `Brushblade/Assets/_Project/Tests/WoodSummonSpeedTests.cs` 及其 `.meta`
- Modify: `Brushblade/Assets/_Project/Tests/PerkInjectionTests.cs`(:214-234、:332)
- Create: `Brushblade/Assets/_Project/Tests/CounterTargetingTests.cs` 及其 `.meta`

**Interfaces:**
- Consumes: Task 4 的 `counterTargeting` 参数
- Produces: `PerkEffect.CounterTargeting`、`BattleConfig.CounterTargeting`(`bool`)

- [ ] **Step 1: 写失败测试(新文件)**

创建 `Brushblade/Assets/_Project/Tests/CounterTargetingTests.cs`。端到端:从 `MetaState` 点亮 `wood_4` → `BattleConfig.CounterTargeting` → 引擎里召唤物真的挑了相克的敌人。

```csharp
using System;
using System.Collections.Generic;
using Brushblade.Core;
using NUnit.Framework;

namespace Brushblade.Core.Tests
{
    /// <summary>择伐(木 L4,2026-09-13):召唤物按生克三档择敌。
    /// 裁定本身的单测在 TargetingTests;这里钉的是「从技能树到引擎这条线接没接上」。</summary>
    public class CounterTargetingTests
    {
        // 与 PerkInjectionTests.cs:14 同款的注入入口。
        private static BattleConfig Build(MetaState meta) =>
            MetaRules.BuildBattleConfig(meta, Array.Empty<string>());

        [Test]
        public void UnlockingWoodFour_TurnsOnCounterTargeting()
        {
            var meta = new MetaState();
            meta.UnlockedPerks.Add("wood_4");
            Assert.That(Build(meta).CounterTargeting, Is.True);
        }

        [Test]
        public void EmptySave_LeavesCounterTargetingOff()
        {
            Assert.That(Build(new MetaState()).CounterTargeting, Is.False,
                "缺省关 —— 一条没点时引擎行为与改前一致");
        }

        [Test]
        public void OtherBranches_DoNotTurnItOn()
        {
            var meta = new MetaState();
            meta.UnlockedPerks.Add("metal_4");
            meta.UnlockedPerks.Add("fire_4");
            Assert.That(Build(meta).CounterTargeting, Is.False);
        }
    }
}
```

配 `.meta`:

```bash
cd /path/to/worktree
sed 's/^guid: .*/guid: 7c41e9a2b8d34f169a5e0c3b7d812f64/' \
  Brushblade/Assets/_Project/Tests/TargetingTests.cs.meta \
  > Brushblade/Assets/_Project/Tests/CounterTargetingTests.cs.meta
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter FullyQualifiedName~CounterTargetingTests
```

Expected: 编译失败,`'BattleConfig' does not contain a definition for 'CounterTargeting'`

- [ ] **Step 3: 换枚举成员**

`Perk.cs` :25,把

```csharp
        MoraleCap, SummonSpeed, WellspringCap, BurnPerStack, HeftCap,
```

改成

```csharp
        MoraleCap, CounterTargeting, WellspringCap, BurnPerStack, HeftCap,
```

上方那条注释「末尾五条对应五行 L4,各自挂靠 BattleEngine 里一个原本硬编码的天花板」不再准确(择伐不是天花板),改成:

```csharp
    /// <summary>节点效果类别。前四条落在既有 BattleConfig 字段上;Element* 三条按元素筛选;
    /// 末尾五条对应五行 L4 —— 四条是各系专属天花板,木系那条(择伐)是择敌规则的开关。</summary>
```

> 删 `PerkEffect.SummonSpeed` 是安全的:存档存的是 `UnlockedPerks` 里的节点 id 字符串,枚举序号不进档。

- [ ] **Step 4: 换木枝节点定义**

`Perk.cs` :150-152:

```csharp
            AddWuxing(list, "wood",  Element.Wood,
                PerkEffect.SummonDeathHealPercent, 20, // 归根:召唤物阵亡回复其最大生命 20%
                PerkEffect.CounterTargeting, 1);       // 择伐:召唤物优先打自己克得动的敌人
```

- [ ] **Step 5: 换 BattleConfig 字段**

`BattleEngine.cs`,把 `WoodSummonSpeedBonus` 的整段文档注释 + 字段(约 :201-210)替换成:

```csharp
        /// <summary>木脉 L4「择伐」:场上**全部**召唤物出手时按生克三档择敌
        /// (相克 > 中立 > 被克)。缺省 false = 关,行为与改前一致。
        ///
        /// 作用域是全量召唤物而不是只木系 —— 用户 2026-09-13 拍板。节点仍挂在木枝上、
        /// 仍走 ElementBonus(…, Element.Wood) 读取,但读出的值当**全局开关**用。
        ///
        /// ⚠ 排在排位**之后**生效(Targeting.PickEnemyTargetForSummon):三档是全覆盖的,
        /// 放到控场偏好那一层会把排位整个压掉。</summary>
        public bool CounterTargeting { get; set; }
```

- [ ] **Step 6: 删蕃息的消费点**

`BattleEngine.cs` 约 :2861,把 `new SummonState(...)` 调用里的

```csharp
                                // 木脉 L4(spec §3.4.1):判据是**打出的那张字**的元素(attacker,
                                // 即 def.Element ?? Heart),不是召唤物自己的 Element —— 与五行
                                // L3 的乘区(ElementPercentOf(attacker))同一判据,两处口径不分叉。
                                // 与 SummonState.Attack 同为快照语义:召唤那一刻算完写进去,
                                // 运行期不再查表,之后再点技能已在场的这只不变。
                                speedBonus: attacker == Element.Wood ? _config?.WoodSummonSpeedBonus ?? 0 : 0);
```

整段删掉,把上一行的 `,` 改成 `)`,让调用停在 `sourceChar: def.Id)`。

然后查 `SummonState` 的 `speedBonus` 构造参数是否还有别的调用方:

```bash
grep -rn "speedBonus" Brushblade/Assets/_Project/ --include="*.cs"
```

- 若**只有**这一处 → 把 `SummonState` 的 `speedBonus` 参数与它写入的字段一并删净(这是本次改动造成的孤儿)。
- 若还有别的调用方 → **只删这一处**,`SummonState` 不动。

- [ ] **Step 7: 换注入**

`Meta.cs` :309,把

```csharp
                WoodSummonSpeedBonus = PerkRules.ElementBonus(meta, PerkEffect.SummonSpeed, Element.Wood),
```

改成

```csharp
                // 木脉 L4「择伐」:节点在木枝上、按木系读,但读出的值当**全局开关**用
                // (作用域是全量召唤物,用户 2026-09-13 拍板)。
                CounterTargeting = PerkRules.ElementBonus(meta, PerkEffect.CounterTargeting, Element.Wood) > 0,
```

- [ ] **Step 8: 接进召唤物出手**

`BattleEngine.cs` 约 :2080(Task 3 改过的那处),把

```csharp
            int target = Targeting.PickEnemyTargetForSummon(_enemies, passive?.Ranged ?? false,
                _targetRandom, shape,
                preferUnfrozen: (passive?.OnHitFreezeChance ?? 0) > 0,
                preferUnslowed: (passive?.OnHitSlowPercent ?? 0) > 0);
```

改成

```csharp
            int target = Targeting.PickEnemyTargetForSummon(_enemies, passive?.Ranged ?? false,
                _targetRandom, shape,
                preferUnfrozen: (passive?.OnHitFreezeChance ?? 0) > 0,
                preferUnslowed: (passive?.OnHitSlowPercent ?? 0) > 0,
                // 择伐(木 L4):判据是**这只召唤物自己**的元素,不是召它的那张字 ——
                // 它问的是「谁打谁划算」,而生克乘区算的就是召唤物 vs 敌人。
                counterTargeting: (_config?.CounterTargeting ?? false) ? summon.Element : null);
```

> `summon` 就是本方法开头 `var summon = _summons[summonIndex];` 取到的那只,`SummonState.Element`
> 是它的元素属性(已确认存在)。

- [ ] **Step 9: 删测试与改注入断言**

```bash
git rm Brushblade/Assets/_Project/Tests/WoodSummonSpeedTests.cs \
       Brushblade/Assets/_Project/Tests/WoodSummonSpeedTests.cs.meta
```

`PerkInjectionTests.cs`:删掉 `EmptySave_GivesNoSummonSpeedBonus`、`OtherBranches_DoNotGrantSummonSpeed`,以及 :224 那条断言 `Is.EqualTo(40)` 所在的测试。它们的替身是 Task 5 Step 1 新建的 `CounterTargetingTests`。

:332 那条 `Assert.That(cfg.WoodSummonSpeedBonus, Is.EqualTo(0), "召唤速度仍是现值");` 改成:

```csharp
            Assert.That(cfg.CounterTargeting, Is.False, "择伐仍是关的");
```

先把这些位置都看一遍再动手:

```bash
grep -n "SummonSpeed\|WoodSummonSpeedBonus" Brushblade/Assets/_Project/Tests/PerkInjectionTests.cs
```

- [ ] **Step 10: 换 Presentation 文案接线**

`PerkInfo.cs` :166,把

```csharp
            PerkEffect.SummonSpeed => SummonSpeedText(def),
```

改成

```csharp
            PerkEffect.CounterTargeting => Strings.T("perk.detail.counter_targeting"),
```

删掉 `SummonSpeedText` 整个方法(:229 起)。`"wood_4" => Strings.T("perk.node.wood_4.name")`(:22)与 `.desc`(:89)已经在位,**但 desc 那行传了 `("value", def.Value)`**,新文案没有占位符 —— 留着无害(多余的参数不会报错),但为了不误导读者,改成:

```csharp
            "wood_4" => Strings.T("perk.node.wood_4.desc"),
```

- [ ] **Step 11: 换字符串表**

`Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`:

- :519 `"perk.node.wood_4.name": "蕃息",` → `"perk.node.wood_4.name": "择伐",`
- :568 `"perk.node.wood_4.desc": "木系召唤物速度 +{value}",` → `"perk.node.wood_4.desc": "召唤物优先攻击自己克制的敌人",`
- :614 `"perk.detail.summon_speed": …` **整行删掉**,在同一位置加:

```json
  "perk.detail.counter_targeting": "场上全部召唤物出手时按五行择敌:先打自己克制的,没有就打互不克制的,最后才打克制自己的。只影响「打谁」,不改伤害倍率——相克 ×1.5、被克 ×0.5 照旧结算。排位仍然优先:近战召唤物不会为了追后排的相克目标而越过敌方前排。",
```

> 删/加行后**必须确认 JSON 仍合法**(别留下多余或缺失的逗号):
> ```bash
> python3 -c "import json;json.load(open('Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json'));print('json ok')"
> ```

- [ ] **Step 12: 重跑字体子集**

新文案引入了新汉字(「择」「伐」等)。游戏打包的是**子集字体**,新字不在里面就会在真机上显示成豆腐块 —— 而离线编译和 Core 单测都发现不了。

```bash
python3 tools/fonts/subset_fonts.py
python3 -m pytest tools/fonts/tests/ -q
```

Expected: pytest 全绿。

> 重新生成会产生时间戳 churn,**光看 diff 大小说明不了变没变,要比 cmap**。若 `git status` 显示字体文件没变化,那是因为这几个字本来就在子集里 —— 正常,不是漏跑。

- [ ] **Step 13: 跑全量 Core 测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿。

- [ ] **Step 14: Presentation 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q \
  -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

Expected: 无 `error CS`。`warning MSB3245` 是 Unity 程序集自带的无关引用,忽略。

- [ ] **Step 15: 提交**

```bash
git add Brushblade/Assets/_Project/Core/Perk.cs \
        Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Core/Meta.cs \
        Brushblade/Assets/_Project/Presentation/PerkInfo.cs \
        Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json \
        Brushblade/Assets/_Project/Tests/CounterTargetingTests.cs \
        Brushblade/Assets/_Project/Tests/CounterTargetingTests.cs.meta \
        Brushblade/Assets/_Project/Tests/PerkInjectionTests.cs
# 字体产物(若 subset_fonts.py 改了文件)逐个 git add
git commit -m "feat(core,presentation): 木脉 L4 蕃息改为择伐

召唤物按生克三档择敌(相克>中立>被克),作用于场上全部召唤物。
蕃息(木系召唤速度 +40)整条链路一并删除。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 6:全量回归与收尾

**Files:** 无改动(除非发现问题)

- [ ] **Step 1: 管线测试(三个目录都要跑)**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ -q
```

Expected: 全绿。

- [ ] **Step 2: Core/Data 全量**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿。

- [ ] **Step 3: Presentation 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q \
  -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

Expected: 无 `error CS`。

- [ ] **Step 4: 扫一遍孤儿**

```bash
grep -rn "SummonSpeed\|WoodSummonSpeedBonus\|蕃息\|PickByRow" Brushblade/Assets/ --include="*.cs" --include="*.json"
```

Expected: 只剩 `BattleConfig.BaseSummonSpeed`、`SummonPassive.Speed`、`EnemyDef.EffectiveSpeed`、`collection.trait.summon_speed.*` 这些**与技能无关**的命中。其余一个不剩。

- [ ] **Step 5: 汇报**

把下面这些交给用户,不要自行合并到 main:
- 分支名
- 三条验证命令的实际输出(测试条数、绿/红)
- Task 2 / Task 3 里因随机流位移而改过期望值的测试清单(逐条说明改了什么、为什么)
- 需要人工在 Unity 里看的两件事:①技能树木枝 L4 显示「择伐」与新描述,无英文枚举名、无 `?` 兜底字形;②开一场带多只召唤物的战斗,确认敌人不再总打同一只

---

## 附:本计划**没有**覆盖的事

- 远程敌人从此能打前排召唤物,难度实打实上升(spec §1.4)。这是拍板口径,**不在本计划内调数值**。上线前若要重平衡,另开任务。
- 合并回 `main` 由用户决定。从 worktree 往 main push 会被 git 拒(main 正被主检出 checkout),要用 `ExitWorktree` 回到主检出再 merge。
