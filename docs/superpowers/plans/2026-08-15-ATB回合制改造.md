# ATB 回合制改造 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把战斗从「玩家回合 → 敌方回合」的固定交替改为 CTB:玩家、召唤物、敌人共用一条时间轴,按行动计量器排队出手,速度成为玩家的第五条角色属性。

**Architecture:** 新增无状态的 `TurnScheduler`(计量器仍存在各单位上,调度器只做纯计算),`BattleEngine.EndTurn` 的 265 行流水线按「谁的一拍」切成 `ActOneEnemy` / `ActOneSummon` / `BeginPlayerTurn` / `YieldTurn`,由 `AdvanceOnce()` 逐个驱动;`EndTurn()` 退化为「让出 + 循环 AdvanceOnce」的包装,测试与游戏共用同一份实现。表现层改为逐行动者播放动画,顶部加行动条 UI。

**Tech Stack:** C# / Unity 6000.5.2f1 / NUnit(Unity 自带版本) / Core 与 Data 禁引 UnityEngine

**Spec:** `docs/superpowers/specs/2026-08-15-ATB回合制改造-design.md`

## Global Constraints

- **Core 与 Data 禁止引用 UnityEngine**(asmdef `noEngineReferences: true`);`TurnScheduler` 是纯 C#。
- **随机性一律走 Core 内带种子的 `GameRandom`**,禁用 `UnityEngine.Random`。本改造**不得新增任何 `_random` 消费方** —— 现有消费方只有回合掉字、`AttackHits`、`EnemyState` 构造时的 Boss 阈值浮动,无条件新增会平移随机流,让所有依赖种子的既有测试全红。
- **测试断言只用 Unity 版 NUnit 也支持的 API**:禁用 `Is.AnyOf` / `Is.All.AnyOf`。多选一用 `Is.EqualTo(a).Or.EqualTo(b)`,集合子集用 `Has.All.Matches<T>`。
- **测试里定位仓库根只能用 `TestContext.CurrentContext.TestDirectory`**,禁用 `AppContext.BaseDirectory`。
- **测试代码禁止直接引用 Newtonsoft**(`JsonConvert` 等);要测序列化就走 `Data.SaveSerializer` / `Data.ConfigLoader`。测试只能用 Tests asmdef references 列出的程序集(Core / Data)。
- **`StatusKind` 新值一律追加在末尾**(本计划不新增 StatusKind,但若实现中发现需要,必须遵守)。
- **全程整数运算**,调度器不许引入 `float` / `double` 中间值。
- **存档不考虑向后兼容**(项目未上线):直接改结构,不写迁移、不加版本分支。
- 提交信息用 conventional commits(`feat`/`fix`/`refactor`/`test`/`docs` + 范围)。

**验证命令**(全文引用,不再重复):

```bash
# Core/Data 单元测试
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q

# 只跑某个测试类
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter FullyQualifiedName~TurnSchedulerTests

# Presentation 离线编译(改完 Presentation 必跑)
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

只看 `error CS`,`warning MSB3245` 是 Unity 程序集自带的无关引用,忽略。

---

## 文件结构

| 文件 | 职责 | 任务 |
|---|---|---|
| `Brushblade/Assets/_Project/Core/TurnScheduler.cs`(新) | `ActorKind` / `ActorRef` / `SchedulerSlot` / `SchedulerStep` / `TurnScheduler` 静态类。纯计算,不认识 `EnemyState` | 1-4 |
| `Brushblade/Assets/_Project/Tests/TurnSchedulerTests.cs`(新) | 调度器单测 | 1-4 |
| `Brushblade/Assets/_Project/Tests/AtbTimingTests.cs`(新) | 时序归属验收测试 | 8-13 |
| `Brushblade/Assets/_Project/Core/BattleEngine.cs`(改) | 拆流水线、接调度器、时序归属、立即结算 | 5-15 |
| `Brushblade/Assets/_Project/Core/RunSnapshot.cs`(改) | `BattleSnapshot.PlayerActionMeter` | 14 |
| `Brushblade/Assets/_Project/Core/Meta.cs`(改) | `MetaRules.SpeedFor` + `BuildBattleConfig` 注入 | 19 |
| `Brushblade/Assets/_Project/Presentation/Juice.cs`(改) | 删三段切分,改为逐行动者播放 | 16 |
| `Brushblade/Assets/_Project/Presentation/BattleView.cs`(改) | 逐格驱动协程 + 行动条挂载 + 速度 chip | 17-19 |
| `Brushblade/Assets/_Project/Presentation/TurnBar.cs`(新) | 顶部行动条 UI | 18 |
| `Brushblade/Assets/_Project/Presentation/MapView.cs`(改) | 养成界面显示速度 | 19 |

---

# T1 · 调度器(Task 1-4)

调度器完全独立开发,一行不碰 `BattleEngine`。这四个任务做完,`git diff` 里不该出现 `BattleEngine.cs`。

## Task 1: ActorRef 与推进算法

**Files:**
- Create: `Brushblade/Assets/_Project/Core/TurnScheduler.cs`
- Test: `Brushblade/Assets/_Project/Tests/TurnSchedulerTests.cs`

**Interfaces:**
- Consumes: 无
- Produces:
  - `enum ActorKind { Player, Summon, Enemy }`
  - `readonly struct ActorRef { ActorKind Kind; int Index; }`,构造 `new ActorRef(ActorKind kind, int index)`,`ActorRef.Player` 静态属性 = `new(ActorKind.Player, -1)`
  - `readonly struct SchedulerSlot { ActorRef Actor; int Speed; int Meter; int Priority; }`,构造 `new SchedulerSlot(ActorRef actor, int speed, int meter, int priority)`
  - `readonly struct SchedulerStep { ActorRef Actor; IReadOnlyList<int> Meters; }`
  - `static class TurnScheduler`:`const int Threshold = 100`、`static SchedulerStep Advance(IReadOnlyList<SchedulerSlot> slots)`

- [ ] **Step 1: 写失败测试**

新建 `Brushblade/Assets/_Project/Tests/TurnSchedulerTests.cs`:

```csharp
using System.Collections.Generic;
using System.Linq;
using Brushblade.Core;
using NUnit.Framework;

namespace Brushblade.Core.Tests
{
    /// <summary>行动调度器(2026-08-15,ATB 改造 T1):纯计算,不认识战斗状态。
    /// 规格见 docs/superpowers/specs/2026-08-15-ATB回合制改造-design.md §4.1。</summary>
    public class TurnSchedulerTests
    {
        // 优先级:玩家 0 / 召唤物 1 / Buff 敌 2 / 其余敌 3(与 BattleEngine 的填法一致)
        private static SchedulerSlot Player(int speed, int meter = 0) =>
            new(ActorRef.Player, speed, meter, 0);

        private static SchedulerSlot Enemy(int index, int speed, int meter = 0) =>
            new(new ActorRef(ActorKind.Enemy, index), speed, meter, 3);

        /// <summary>连推 n 拍,返回出手序列。每拍把新计量器写回槽位——
        /// 这正是 BattleEngine 接线后要做的事。</summary>
        private static List<ActorRef> Sequence(List<SchedulerSlot> slots, int n)
        {
            var result = new List<ActorRef>();
            for (int i = 0; i < n; i++)
            {
                var step = TurnScheduler.Advance(slots);
                result.Add(step.Actor);
                for (int s = 0; s < slots.Count; s++)
                    slots[s] = new SchedulerSlot(slots[s].Actor, slots[s].Speed,
                        step.Meters[s], slots[s].Priority);
            }
            return result;
        }

        [Test]
        public void SameSpeed_AlternatesInPriorityOrder()
        {
            var slots = new List<SchedulerSlot> { Player(100), Enemy(0, 100) };

            var seq = Sequence(slots, 4);

            Assert.That(seq[0].Kind, Is.EqualTo(ActorKind.Player));
            Assert.That(seq[1].Kind, Is.EqualTo(ActorKind.Enemy));
            Assert.That(seq[2].Kind, Is.EqualTo(ActorKind.Player));
            Assert.That(seq[3].Kind, Is.EqualTo(ActorKind.Enemy));
        }

        [Test]
        public void SpeedRatio_IsExactOverFiveTicks()
        {
            // 我 100 / 敌 60:五个 tick 内恰好 5 次对 3 次(spec §4.1 的口径例)
            var slots = new List<SchedulerSlot> { Player(100), Enemy(0, 60) };

            var seq = Sequence(slots, 8);

            Assert.That(seq.Take(8).Count(a => a.Kind == ActorKind.Player), Is.EqualTo(5));
            Assert.That(seq.Take(8).Count(a => a.Kind == ActorKind.Enemy), Is.EqualTo(3));
        }

        [Test]
        public void DoubleSpeed_ActsTwiceAsOften()
        {
            var slots = new List<SchedulerSlot> { Player(200), Enemy(0, 100) };

            var seq = Sequence(slots, 6);

            Assert.That(seq.Count(a => a.Kind == ActorKind.Player), Is.EqualTo(4));
            Assert.That(seq.Count(a => a.Kind == ActorKind.Enemy), Is.EqualTo(2));
        }

        [Test]
        public void Meter_KeepsRemainderAfterActing()
        {
            // 速度 150:第一拍攒到 150,行动后留 50 —— 余额必须留着,不清零
            var slots = new List<SchedulerSlot> { Player(150) };

            var step = TurnScheduler.Advance(slots);

            Assert.That(step.Actor.Kind, Is.EqualTo(ActorKind.Player));
            Assert.That(step.Meters[0], Is.EqualTo(50));
        }

        [Test]
        public void AlreadyFull_ActsWithoutAdvancingTime()
        {
            // 已经满格的不该再推进时间:否则同一 tick 内的第二个行动者会白拿一次累积
            var slots = new List<SchedulerSlot> { Player(100, meter: 120), Enemy(0, 100, meter: 110) };

            var step = TurnScheduler.Advance(slots);

            Assert.That(step.Actor.Kind, Is.EqualTo(ActorKind.Player));
            Assert.That(step.Meters[0], Is.EqualTo(20), "行动者扣 100");
            Assert.That(step.Meters[1], Is.EqualTo(110), "其他人原地不动");
        }
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter FullyQualifiedName~TurnSchedulerTests
```

Expected: 编译失败,`error CS0246: The type or namespace name 'ActorRef' could not be found`

- [ ] **Step 3: 写实现**

新建 `Brushblade/Assets/_Project/Core/TurnScheduler.cs`:

```csharp
using System;
using System.Collections.Generic;

namespace Brushblade.Core
{
    /// <summary>行动者类别(2026-08-15,ATB 改造)。</summary>
    public enum ActorKind { Player, Summon, Enemy }

    /// <summary>一个行动者的引用。Player 的 Index 恒为 −1(与 BattleEvent.TargetIndex 的
    /// 「−1 = 玩家」同口径)。</summary>
    public readonly struct ActorRef : IEquatable<ActorRef>
    {
        public ActorKind Kind { get; }
        public int Index { get; }

        public ActorRef(ActorKind kind, int index)
        {
            Kind = kind;
            Index = index;
        }

        public static ActorRef Player => new(ActorKind.Player, -1);

        public bool Equals(ActorRef other) => Kind == other.Kind && Index == other.Index;
        public override bool Equals(object obj) => obj is ActorRef other && Equals(other);
        public override int GetHashCode() => ((int)Kind * 397) ^ Index;
        public override string ToString() => Kind == ActorKind.Player ? "Player" : $"{Kind}[{Index}]";
    }

    /// <summary>调度器的输入槽:一个参战单位的速度与计量器快照。
    /// <paramref name="Priority"/> 是并列时的排序主键(小者先),由 BattleEngine 按
    /// 「玩家 0 / 召唤物 1 / Buff 敌 2 / 其余敌 3」填 —— 调度器不认识 EnemyAbility。</summary>
    public readonly struct SchedulerSlot
    {
        public ActorRef Actor { get; }
        public int Speed { get; }
        public int Meter { get; }
        public int Priority { get; }

        public SchedulerSlot(ActorRef actor, int speed, int meter, int priority)
        {
            Actor = actor;
            Speed = speed;
            Meter = meter;
            Priority = priority;
        }
    }

    /// <summary>一次推进的结果:谁行动,以及推进后全场的计量器(与入参 slots 同序,
    /// 行动者的那一格已经扣掉 Threshold)。</summary>
    public readonly struct SchedulerStep
    {
        public ActorRef Actor { get; }
        public IReadOnlyList<int> Meters { get; }

        public SchedulerStep(ActorRef actor, IReadOnlyList<int> meters)
        {
            Actor = actor;
            Meters = meters;
        }
    }

    /// <summary>行动调度器(2026-08-15,ATB 改造)。**无状态**:计量器存在各单位身上
    /// (EnemyState.ActionMeter / SummonState.ActionMeter / BattleEngine 的玩家计量器),
    /// 本类只做纯计算 —— 这也是 Forecast 不可能误改真实状态的结构性保证。
    ///
    /// 算法(spec §4.1):
    ///   1. 若已有单位 Meter >= Threshold,按 (Priority, 下标) 取第一个,扣 Threshold,返回
    ///   2. 否则推进 ticks = min ceil((Threshold − Meter) / Speed),全场累积,回到 1
    ///
    /// ⚠ 全程整数。不许引入 float/double 中间值 —— Unity Mono 与 .NET 8 的中间精度不同,
    /// 配上取整会整级翻车(2026-08-15 护甲缩放刚踩过)。</summary>
    public static class TurnScheduler
    {
        /// <summary>计量器满值:攒够即行动一次。</summary>
        public const int Threshold = 100;

        public static SchedulerStep Advance(IReadOnlyList<SchedulerSlot> slots)
        {
            if (slots == null || slots.Count == 0)
                throw new InvalidOperationException("调度器至少需要一个参战单位");

            var meters = new int[slots.Count];
            for (int i = 0; i < slots.Count; i++) meters[i] = slots[i].Meter;

            int winner = FirstFull(slots, meters);
            if (winner < 0)
            {
                int ticks = TicksUntilAnyFull(slots, meters);
                for (int i = 0; i < slots.Count; i++)
                    meters[i] += EffectiveSpeed(slots[i].Speed) * ticks;
                winner = FirstFull(slots, meters);
            }

            meters[winner] -= Threshold;
            return new SchedulerStep(slots[winner].Actor, meters);
        }

        /// <summary>满格者里 (Priority, 下标) 最小的那个;没有则 −1。</summary>
        private static int FirstFull(IReadOnlyList<SchedulerSlot> slots, int[] meters)
        {
            int best = -1;
            for (int i = 0; i < slots.Count; i++)
            {
                if (meters[i] < Threshold) continue;
                if (best < 0 || slots[i].Priority < slots[best].Priority) best = i;
            }
            return best;
        }

        /// <summary>推进多少 tick 才有人攒满。至少 1 —— 返回 0 会让 Advance 死循环。</summary>
        private static int TicksUntilAnyFull(IReadOnlyList<SchedulerSlot> slots, int[] meters)
        {
            int best = int.MaxValue;
            for (int i = 0; i < slots.Count; i++)
            {
                int need = Threshold - meters[i];
                int speed = EffectiveSpeed(slots[i].Speed);
                int ticks = (need + speed - 1) / speed; // ceil,整数除
                if (ticks < best) best = ticks;
            }
            return Math.Max(1, best);
        }

        /// <summary>速度钳位由 Task 3 接入(TDD 分步:本任务只做推进算法,原样返回)。</summary>
        private static int EffectiveSpeed(int speed) => speed;
    }
}
```

`FirstFull` 只比 `Priority` 不比下标是够的:调用方按下标升序构造 slots,同 Priority 时先遇到的下标更小,`slots[i].Priority < slots[best].Priority` 用严格小于,后来的同优先级不会顶替前面的。

- [ ] **Step 4: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter FullyQualifiedName~TurnSchedulerTests
```

Expected: 5 条全 PASS

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/TurnScheduler.cs Brushblade/Assets/_Project/Tests/TurnSchedulerTests.cs
git commit -m "feat(core): 行动调度器 —— 整数 tick 推进,计量器留余额"
```

> ⚠ Unity 会给新 `.cs` 生成 `.meta`,下次用户打开编辑器后要一并提交(前科:`SummonInfo.cs.meta` 漏提交过)。

---

## Task 2: tie-break 顺序

**Files:**
- Modify: `Brushblade/Assets/_Project/Tests/TurnSchedulerTests.cs`(追加)
- Modify: `Brushblade/Assets/_Project/Core/TurnScheduler.cs`(可能无需改,先测)

**Interfaces:**
- Consumes: Task 1 的 `SchedulerSlot.Priority`
- Produces: 锁死的排序契约 `玩家(0) → 召唤物(1) → Buff 敌(2) → 其余敌(3)`,同优先级按下标升序

- [ ] **Step 1: 写失败测试**

追加到 `TurnSchedulerTests.cs` 的类内:

```csharp
        private static SchedulerSlot Summon(int index, int speed, int meter = 0) =>
            new(new ActorRef(ActorKind.Summon, index), speed, meter, 1);

        private static SchedulerSlot Buffer(int index, int speed, int meter = 0) =>
            new(new ActorRef(ActorKind.Enemy, index), speed, meter, 2);

        [Test]
        public void TieBreak_PlayerThenSummonThenBufferThenOthers()
        {
            // 全部同速同计量器 —— 纯考排序契约。这条锁死后,谁「顺手优化」调度器
            // 都会立刻红,而不是静默改掉战斗结果。
            var slots = new List<SchedulerSlot>
            {
                Enemy(0, 100), Buffer(1, 100), Summon(0, 100), Player(100),
            };

            var seq = Sequence(slots, 4);

            Assert.That(seq[0], Is.EqualTo(ActorRef.Player));
            Assert.That(seq[1], Is.EqualTo(new ActorRef(ActorKind.Summon, 0)));
            Assert.That(seq[2], Is.EqualTo(new ActorRef(ActorKind.Enemy, 1)), "Buff 敌先于普通敌");
            Assert.That(seq[3], Is.EqualTo(new ActorRef(ActorKind.Enemy, 0)));
        }

        [Test]
        public void TieBreak_SamePriorityFollowsIndexOrder()
        {
            var slots = new List<SchedulerSlot> { Enemy(0, 100), Enemy(1, 100), Enemy(2, 100) };

            var seq = Sequence(slots, 3);

            Assert.That(seq[0].Index, Is.EqualTo(0));
            Assert.That(seq[1].Index, Is.EqualTo(1));
            Assert.That(seq[2].Index, Is.EqualTo(2));
        }
```

- [ ] **Step 2: 跑测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter FullyQualifiedName~TurnSchedulerTests
```

Expected: 两条新测试 PASS(Task 1 的 `FirstFull` 已实现该契约)。若 `TieBreak_SamePriorityFollowsIndexOrder` 红,说明 `FirstFull` 里用了 `<=` 而非 `<`,改回严格小于。

- [ ] **Step 3: 提交**

```bash
git add Brushblade/Assets/_Project/Tests/TurnSchedulerTests.cs
git commit -m "test(core): 锁死调度器 tie-break —— 玩家>召唤>Buff敌>其余敌"
```

---

## Task 3: 速度钳位 [25, 400]

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/TurnScheduler.cs`
- Modify: `Brushblade/Assets/_Project/Tests/TurnSchedulerTests.cs`(追加)

**Interfaces:**
- Produces: `TurnScheduler.MinSpeed = 25`、`TurnScheduler.MaxSpeed = 400`、`static int ClampSpeed(int raw)`

- [ ] **Step 1: 写失败测试**

追加到 `TurnSchedulerTests.cs`:

```csharp
        [Test]
        public void ClampSpeed_BoundsAreTwentyFiveToFourHundred()
        {
            Assert.That(TurnScheduler.ClampSpeed(0), Is.EqualTo(25));
            Assert.That(TurnScheduler.ClampSpeed(-999), Is.EqualTo(25));
            Assert.That(TurnScheduler.ClampSpeed(100), Is.EqualTo(100));
            Assert.That(TurnScheduler.ClampSpeed(800), Is.EqualTo(400));
        }

        [Test]
        public void ZeroSpeed_StillActsEventually_NoDeadlock()
        {
            // 速度 0 若原样使用,该单位永远攒不满 → 永远轮不到 → 它自己那拍的状态递减
            // 永远不跑 → 减速永远解不了。钳到 25 就自然消解(四拍动一次)。
            var slots = new List<SchedulerSlot> { Player(100), Enemy(0, 0) };

            var seq = Sequence(slots, 10);

            Assert.That(seq.Any(a => a.Kind == ActorKind.Enemy), Is.True, "速度 0 的单位仍须能行动");
        }

        [Test]
        public void ExtremeSpeed_IsCappedAtFourHundred()
        {
            // 800 与 400 表现必须一致:上限之外再堆速度不再有收益
            var capped = new List<SchedulerSlot> { Player(400), Enemy(0, 100) };
            var beyond = new List<SchedulerSlot> { Player(800), Enemy(0, 100) };

            var a = Sequence(capped, 10);
            var b = Sequence(beyond, 10);

            Assert.That(b.Select(x => x.Kind), Is.EqualTo(a.Select(x => x.Kind)));
        }
```

- [ ] **Step 2: 跑测试确认失败**

Expected: `error CS0117: 'TurnScheduler' does not contain a definition for 'ClampSpeed'`

- [ ] **Step 3: 实现钳位**

在 `TurnScheduler.cs` 里,把 `Threshold` 常量下面补两个常量,并把占位的 `EffectiveSpeed` 换掉:

```csharp
        /// <summary>有效速度下限。**不是保守取值,是防死锁**:速度 0 的单位永远攒不满 →
        /// 永远轮不到 → 它自己那拍的状态递减永远不跑 → 减速永远解不了(spec 口径 8)。
        /// 25 = 基准的 1/4,四拍动一次。</summary>
        public const int MinSpeed = 25;

        /// <summary>有效速度上限:防养成与叠加失控。CTB 下没有「单回合行动次数封顶」这回事,
        /// 上限全靠这条(旧的 MaxActionsPerTurn = 2 随本次改造删除)。</summary>
        public const int MaxSpeed = 400;

        public static int ClampSpeed(int raw) => Math.Clamp(raw, MinSpeed, MaxSpeed);
```

把私有的 `EffectiveSpeed` 删掉,`Advance` / `TicksUntilAnyFull` 里的两处调用改成 `ClampSpeed`。

- [ ] **Step 4: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter FullyQualifiedName~TurnSchedulerTests
```

Expected: 10 条全 PASS

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/TurnScheduler.cs Brushblade/Assets/_Project/Tests/TurnSchedulerTests.cs
git commit -m "feat(core): 有效速度钳到 [25,400] —— 下限防减速死锁,上限防叠加失控"
```

---

## Task 4: Forecast 预测序列

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/TurnScheduler.cs`
- Modify: `Brushblade/Assets/_Project/Tests/TurnSchedulerTests.cs`(追加)

**Interfaces:**
- Produces: `static IReadOnlyList<ActorRef> Forecast(IReadOnlyList<SchedulerSlot> slots, int count)`

- [ ] **Step 1: 写失败测试**

```csharp
        [Test]
        public void Forecast_MatchesActualSequence()
        {
            var slots = new List<SchedulerSlot> { Player(100), Enemy(0, 60) };
            var predicted = TurnScheduler.Forecast(slots, 8);

            var actual = Sequence(new List<SchedulerSlot>(slots), 8);

            Assert.That(predicted, Is.EqualTo(actual));
        }

        [Test]
        public void Forecast_DoesNotMutateInput()
        {
            // UI 每帧刷新一次预测;若 Forecast 动了真实计量器,光是看着行动条就能把战斗推着走。
            var slots = new List<SchedulerSlot> { Player(100, meter: 30), Enemy(0, 60, meter: 70) };

            TurnScheduler.Forecast(slots, 20);

            Assert.That(slots[0].Meter, Is.EqualTo(30));
            Assert.That(slots[1].Meter, Is.EqualTo(70));
        }

        [Test]
        public void Forecast_ZeroCountReturnsEmpty()
        {
            var slots = new List<SchedulerSlot> { Player(100) };

            Assert.That(TurnScheduler.Forecast(slots, 0), Is.Empty);
        }
```

- [ ] **Step 2: 跑测试确认失败**

Expected: `error CS0117: 'TurnScheduler' does not contain a definition for 'Forecast'`

- [ ] **Step 3: 实现**

追加到 `TurnScheduler`:

```csharp
        /// <summary>向前预测 count 个行动者,**不改动入参**——在本地拷贝上推演。
        ///
        /// ⚠ 这是「若场面不变」的预测:怪死了、召唤物上场、减速生效都会让实际序列偏离。
        /// 表现层每次重绘都要重新调用,不许缓存。</summary>
        public static IReadOnlyList<ActorRef> Forecast(IReadOnlyList<SchedulerSlot> slots, int count)
        {
            var result = new List<ActorRef>();
            if (slots == null || slots.Count == 0 || count <= 0) return result;

            var working = new SchedulerSlot[slots.Count];
            for (int i = 0; i < slots.Count; i++) working[i] = slots[i];

            for (int n = 0; n < count; n++)
            {
                var step = Advance(working);
                result.Add(step.Actor);
                for (int i = 0; i < working.Length; i++)
                    working[i] = new SchedulerSlot(working[i].Actor, working[i].Speed,
                        step.Meters[i], working[i].Priority);
            }
            return result;
        }
```

- [ ] **Step 4: 跑测试确认通过**

Expected: 13 条全 PASS

- [ ] **Step 5: 全量回归 + 提交**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 1031 + 13 条全绿(`BattleEngine.cs` 一行未动,旧测试不该有任何变化)

```bash
git add Brushblade/Assets/_Project/Core/TurnScheduler.cs Brushblade/Assets/_Project/Tests/TurnSchedulerTests.cs
git commit -m "feat(core): Forecast 预测序列 —— 纯函数,不触真实计量器"
```

---

# T2 · 接线与时序归属(Task 5-15)

风险按任务逐级释放:Task 6 是**零行为变化**的纯提取重构(旧测试全绿是硬凭据),Task 7 只换驱动方式,Task 8 起才动行为。

## Task 5: PlayerSpeed 与玩家计量器

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`BattleConfig` 约 :87 `ApPerTurn` 附近;`BattleEngine` 字段区约 :204)
- Test: `Brushblade/Assets/_Project/Tests/AtbTimingTests.cs`(新建)

**Interfaces:**
- Produces:
  - `BattleConfig.PlayerSpeed { get; set; } = 100`
  - `BattleEngine.PlayerActionMeter { get; private set; }`
  - `BattleEngine.EffectivePlayerSpeed`(public int,= `ClampSpeed(config.PlayerSpeed + SpeedModifier 总量)`)

- [ ] **Step 1: 写失败测试**

新建 `Brushblade/Assets/_Project/Tests/AtbTimingTests.cs`:

```csharp
using System;
using System.Collections.Generic;
using System.Linq;
using Brushblade.Core;
using NUnit.Framework;

namespace Brushblade.Core.Tests
{
    /// <summary>ATB 时序归属(2026-08-15):每单位自结算的 DOT / 状态递减 / 立即结算。
    /// 规格见 docs/superpowers/specs/2026-08-15-ATB回合制改造-design.md §4.3。</summary>
    public class AtbTimingTests
    {
        private static RecipeGraph Graph() => new(new[]
        {
            new CharDef("木", Element.Wood),
        });

        private static BattleEngine Engine(EnemyDef[] enemies, BattleConfig config = null) =>
            new(Graph(), config ?? new BattleConfig { PlayerMaxHp = 999 },
                Array.Empty<string>(), Array.Empty<string>(), enemies, seed: 1);

        private static EnemyDef Dummy(string id = "靶", int hp = 999, int attack = 0) =>
            new(id, Element.Heart, hp, attack);

        [Test]
        public void PlayerSpeed_DefaultsToBaseline()
        {
            var engine = Engine(new[] { Dummy() });

            Assert.That(engine.EffectivePlayerSpeed, Is.EqualTo(100));
        }

        [Test]
        public void PlayerSpeed_ReadsConfigAndSpeedModifier()
        {
            var engine = Engine(new[] { Dummy() },
                new BattleConfig { PlayerMaxHp = 999, PlayerSpeed = 150 });

            Assert.That(engine.EffectivePlayerSpeed, Is.EqualTo(150));
        }

        [Test]
        public void PlayerSpeed_IsClampedLikeEveryoneElse()
        {
            var engine = Engine(new[] { Dummy() },
                new BattleConfig { PlayerMaxHp = 999, PlayerSpeed = 9999 });

            Assert.That(engine.EffectivePlayerSpeed, Is.EqualTo(TurnScheduler.MaxSpeed));
        }

        [Test]
        public void PlayerActionMeter_StartsEmpty()
        {
            var engine = Engine(new[] { Dummy() });

            Assert.That(engine.PlayerActionMeter, Is.EqualTo(0));
        }
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter FullyQualifiedName~AtbTimingTests
```

Expected: `error CS0117: 'BattleConfig' does not contain a definition for 'PlayerSpeed'`

- [ ] **Step 3: 实现**

在 `BattleConfig` 里,`ApPerTurn` 那行(约 `BattleEngine.cs:87`)上方加:

```csharp
        /// <summary>玩家基础速度(2026-08-15,ATB 改造)。基准 100 = 与敌人同速 = 一人一手。
        /// 有效速度 = 本值 + 所有 SpeedModifier 之和,再由 TurnScheduler.ClampSpeed 钳到 [25,400]。
        ///
        /// ⚠ **战斗中永不被写**,同 <see cref="PlayerDefense"/> / <see cref="PlayerDodge"/>:
        /// 局内的加速/减速全部走 <see cref="StatusKind.SpeedModifier"/> 进 _playerStatuses,
        /// 而它本来就进 BattleSnapshot.PlayerStatuses —— 零新增快照字段。
        /// T4 起由 MetaRules.BuildBattleConfig 按 SpeedFor(角色等级) 注入。</summary>
        public int PlayerSpeed { get; set; } = 100;
```

在 `BattleEngine` 字段区(`_shieldPersist` 之后,约 :204)加:

```csharp
        /// <summary>玩家的行动计量器(2026-08-15,ATB 改造):与敌人/召唤物同走一套模型,
        /// 攒满 TurnScheduler.Threshold 就轮到玩家。进 BattleSnapshot。</summary>
        public int PlayerActionMeter { get; private set; }
```

在 `EffectivePlayerDefense`(约 :311)旁边加:

```csharp
        /// <summary>本场生效的玩家速度 = 角色属性(config)+ 局内 SpeedModifier,钳到 [25,400]。
        /// 与敌人侧 <see cref="EnemyState"/> 的算法同一条 —— 钳位统一收在
        /// <see cref="TurnScheduler.ClampSpeed"/>,两侧不许各写一份。</summary>
        public int EffectivePlayerSpeed => TurnScheduler.ClampSpeed(
            _config.PlayerSpeed + _playerStatuses.TotalMagnitude(StatusKind.SpeedModifier));
```

- [ ] **Step 4: 跑测试确认通过**

Expected: `AtbTimingTests` 4 条 PASS;全量 1031 条仍全绿(只加字段,没接线)

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/AtbTimingTests.cs
git commit -m "feat(core): 玩家速度属性与行动计量器(尚未接线)"
```

---

## Task 6: 纯提取重构 —— 把流水线切成「谁的一拍」

**这个任务零行为变化。** 旧测试全绿、且一条断言都不许改,是它唯一的验收凭据。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs:688-953`

**Interfaces:**
- Produces(全部 private,供 Task 7 的 `AdvanceOnce` 调用):
  - `void StrikeOnceWithSummon(int summonIndex)`
  - `void ApplyEnemyBuffAura(int enemyIndex)`
  - `void ActOneEnemy(int enemyIndex, int actionCount)`
  - `void RegrowOneEnemy(int enemyIndex)`

- [ ] **Step 1: 记录基线**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q 2>&1 | tail -5
```

把通过条数记下来(预期 1031 + 13 + 4 = 1048)。Task 6 做完必须一模一样。

- [ ] **Step 2: 提取召唤物那一拍**

把 `BattleEngine.cs:768-793` 的召唤物循环体(单只召唤物那部分)提成方法,循环只留调用:

```csharp
        /// <summary>一只召唤物的一次出手(2026-08-15 提取,行为与提取前逐字节一致)。
        /// 攻 0 的召唤物(烓/灶)照常出手但不走 DamageEnemy —— 见提取前的原注释。</summary>
        private void StrikeOnceWithSummon(int summonIndex)
        {
            var summon = _summons[summonIndex];
            int target = -1;
            for (int i = 0; i < _enemies.Count; i++)
                if (_enemies[i].Alive) { target = i; break; }
            if (target < 0) return;
            _events.Add(new BattleEvent(BattleEventKind.SummonAttack, target, summon.Attack, summonIndex));
            if (summon.Attack > 0)
                DamageEnemy(target, summon.Attack, Array.Empty<Element>(), summon.Element);
            ApplySummonOnHit(summon, target);
        }
```

原循环改为:

```csharp
            for (int s = 0; s < _summons.Count; s++)
            {
                var summon = _summons[s];
                if (!summon.Alive) continue;
                summon.ActionMeter += summon.Speed;
                int acts = Math.Min(summon.ActionMeter / ActionMeterThreshold, MaxActionsPerTurn);
                summon.ActionMeter -= acts * ActionMeterThreshold;
                if (summon.ActionMeter >= ActionMeterThreshold) summon.ActionMeter = 0;
                for (int act = 0; act < acts; act++)
                {
                    if (!summon.Alive) break;
                    if (!_enemies.Any(e => e.Alive)) break;
                    StrikeOnceWithSummon(s);
                }
            }
```

⚠ 原代码的 `if (target < 0) break;` 是**跳出这只召唤物的多次出手**,提取后变成 `return`(只结束这一次)。上面的 `if (!_enemies.Any(e => e.Alive)) break;` 补回了这个语义。`System.Linq` 已在文件顶部引入则直接用,否则写成显式循环。

- [ ] **Step 3: 提取敌方辅助加攻与敌人出手**

把 `BattleEngine.cs:817-838` 的辅助加攻循环体提成 `ApplyEnemyBuffAura(int enemyIndex)`,把 `:849-900` 的敌人行动循环体提成 `ActOneEnemy(int enemyIndex, int actionCount)`,把 `:927-950` 的缺笔妖循环体提成 `RegrowOneEnemy(int enemyIndex)`。三个方法各自保留原有的守卫条件(`!Alive` / `IsSilenced` / `actionCount == 0` / `RegrowProgress >= 3` 等),`EndTurn` 里只留循环与调用。

方法签名(Task 7 会直接调用这三个,签名必须与此处一致):

```csharp
        private void ApplyEnemyBuffAura(int enemyIndex)
        private void ActOneEnemy(int enemyIndex, int actionCount)
        private void RegrowOneEnemy(int enemyIndex)
```

- [ ] **Step 4: 跑全量,与基线逐条比对**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q 2>&1 | tail -5
```

Expected: 通过条数与 Step 1 完全相同,失败 0。**任何一条红都意味着提取改变了行为,回去找差异,不许改断言。**

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs
git commit -m "refactor(core): 把 EndTurn 流水线切成单位级方法(零行为变化)"
```

---

## Task 7: 调度器驱动 —— AdvanceOnce 与 EndTurn 包装

时序归属**仍是旧的**,只换驱动方式。全 100 同速时序列恰好是「召唤物 → 敌人 → 回到玩家」,旧测试应当继续全绿。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`
- Modify: `Brushblade/Assets/_Project/Tests/AtbTimingTests.cs`(追加)

**Interfaces:**
- Produces:
  - `public void YieldTurn()` — 玩家让出行动权(跑玩家侧收尾结算)
  - `public bool AdvanceOnce()` — 执行一个非玩家行动者;下一个是玩家则跑 `BeginPlayerTurn()`、置 `Phase = PlayerTurn`、返回 `false`
  - `public ActorRef LastActor { get; private set; }` — 最近一次 `AdvanceOnce` 执行的行动者
  - `public IReadOnlyList<ActorRef> Forecast(int count)`
  - `public ActorRef PeekNextActor()`
  - `EndTurn()` 改为 `YieldTurn(); while (AdvanceOnce()) { }`

- [ ] **Step 1: 写失败测试**

追加到 `AtbTimingTests.cs`:

```csharp
        [Test]
        public void EndTurn_IsAWrapperOverAdvanceOnce()
        {
            // 同速基准局:一次 EndTurn 应当恰好走完「召唤物 → 敌人 → 回到玩家」
            var engine = Engine(new[] { Dummy(attack: 10) });

            engine.EndTurn();

            Assert.That(engine.Phase, Is.EqualTo(BattlePhase.PlayerTurn));
            Assert.That(engine.Turn, Is.EqualTo(2), "回到玩家 = 新一拍开始");
        }

        [Test]
        public void Forecast_StartsWithTheEnemyWhenPlayerHasYielded()
        {
            var engine = Engine(new[] { Dummy(attack: 10) });

            engine.YieldTurn();
            var forecast = engine.Forecast(3);

            Assert.That(forecast[0].Kind, Is.EqualTo(ActorKind.Enemy));
        }

        [Test]
        public void AdvanceOnce_ReturnsFalseWhenPlayersTurnComesUp()
        {
            var engine = Engine(new[] { Dummy(attack: 10) });

            engine.YieldTurn();
            bool more = engine.AdvanceOnce();   // 敌人这一拍
            Assert.That(more, Is.True);
            Assert.That(engine.LastActor.Kind, Is.EqualTo(ActorKind.Enemy));

            more = engine.AdvanceOnce();        // 轮到玩家 → 停
            Assert.That(more, Is.False);
            Assert.That(engine.Phase, Is.EqualTo(BattlePhase.PlayerTurn));
        }

        [Test]
        public void FastPlayer_GetsTwoTurnsPerEnemyTurn()
        {
            var engine = Engine(new[] { Dummy(attack: 10) },
                new BattleConfig { PlayerMaxHp = 999, PlayerSpeed = 200 });

            engine.YieldTurn();
            var forecast = engine.Forecast(6);

            Assert.That(forecast.Count(a => a.Kind == ActorKind.Player), Is.EqualTo(4));
            Assert.That(forecast.Count(a => a.Kind == ActorKind.Enemy), Is.EqualTo(2));
        }
```

- [ ] **Step 2: 跑测试确认失败**

Expected: `error CS1061: 'BattleEngine' does not contain a definition for 'YieldTurn'`

- [ ] **Step 3: 实现槽位构建与驱动**

在 `BattleEngine` 里加:

```csharp
        /// <summary>最近一次 AdvanceOnce 执行的行动者(表现层据此高亮行动条那一格)。</summary>
        public ActorRef LastActor { get; private set; } = ActorRef.Player;

        /// <summary>当前参战单位的调度槽位。**顺序固定**:玩家、召唤物(下标升序)、敌人(下标升序)
        /// —— Forecast 与 Advance 返回的 Meters 与本列表同序,写回时按同一顺序。
        /// 死掉的单位不进调度(它们不再行动,也不该占预测格子)。</summary>
        private List<SchedulerSlot> BuildSlots()
        {
            var slots = new List<SchedulerSlot>
            {
                new(ActorRef.Player, EffectivePlayerSpeed, PlayerActionMeter, 0),
            };
            for (int s = 0; s < _summons.Count; s++)
            {
                if (!_summons[s].Alive) continue;
                slots.Add(new SchedulerSlot(new ActorRef(ActorKind.Summon, s),
                    _summons[s].Speed, _summons[s].ActionMeter, 1));
            }
            for (int i = 0; i < _enemies.Count; i++)
            {
                var enemy = _enemies[i];
                if (!enemy.Alive) continue;
                int speed = enemy.Speed + enemy.Statuses.TotalMagnitude(StatusKind.SpeedModifier);
                // Buff 能力的敌人排在普通敌人之前:保住「辅助先摇旗、同伴才带着加成出手」
                // 这个既有节拍。被减速时它自然排到后面 —— 那正是新系统该有的行为。
                int priority = enemy.Def.Ability == EnemyAbility.Buff ? 2 : 3;
                slots.Add(new SchedulerSlot(new ActorRef(ActorKind.Enemy, i), speed,
                    enemy.ActionMeter, priority));
            }
            return slots;
        }

        /// <summary>把一次推进后的计量器写回各单位。</summary>
        private void WriteBackMeters(List<SchedulerSlot> slots, IReadOnlyList<int> meters)
        {
            for (int i = 0; i < slots.Count; i++)
            {
                var actor = slots[i].Actor;
                switch (actor.Kind)
                {
                    case ActorKind.Player: PlayerActionMeter = meters[i]; break;
                    case ActorKind.Summon: _summons[actor.Index].ActionMeter = meters[i]; break;
                    case ActorKind.Enemy: _enemies[actor.Index].ActionMeter = meters[i]; break;
                }
            }
        }

        /// <summary>向前预测 count 个行动者(表现层的行动条用)。场面一变就会偏离,不许缓存。</summary>
        public IReadOnlyList<ActorRef> Forecast(int count) =>
            TurnScheduler.Forecast(BuildSlots(), count);

        /// <summary>队首是谁(不改任何状态)。</summary>
        public ActorRef PeekNextActor()
        {
            var forecast = TurnScheduler.Forecast(BuildSlots(), 1);
            return forecast.Count > 0 ? forecast[0] : ActorRef.Player;
        }
```

`AdvanceOnce` 与 `YieldTurn`:

```csharp
        /// <summary>玩家让出行动权:跑玩家侧的收尾结算。之后由 AdvanceOnce 逐个推进。</summary>
        public void YieldTurn()
        {
            if (Phase != BattlePhase.PlayerTurn) return;
            _events.Clear();
            SettlePlayerTurnEnd();
        }

        /// <summary>推进并执行**一个**非玩家行动者。轮到玩家时不执行,改为跑 BeginPlayerTurn()、
        /// 置 Phase = PlayerTurn 并返回 false —— 调用方据此停止循环。
        ///
        /// _events 在每次调用开头清空:逐格驱动时表现层每次拿到的就是**这一个单位**产生的事件。
        /// EndTurn 包装靠 _endTurnEvents 跨段累积,拿到的仍是完整一整轮。</summary>
        public bool AdvanceOnce()
        {
            if (Phase != BattlePhase.PlayerTurn && Phase != BattlePhase.DropChoice)
                return false;   // Won / Lost:战斗已结束
            if (Phase == BattlePhase.DropChoice) return false; // 等玩家决议

            var slots = BuildSlots();
            var step = TurnScheduler.Advance(slots);
            if (step.Actor.Kind == ActorKind.Player)
            {
                WriteBackMeters(slots, step.Meters);
                LastActor = ActorRef.Player;
                BeginPlayerTurn();
                return false;
            }

            _events.Clear();
            WriteBackMeters(slots, step.Meters);
            LastActor = step.Actor;
            if (step.Actor.Kind == ActorKind.Summon) ActSummonTurn(step.Actor.Index);
            else ActEnemyTurn(step.Actor.Index);
            return Phase == BattlePhase.PlayerTurn;
        }
```

`EndTurn` 改为包装(跨段累积事件):

```csharp
        public void EndTurn()
        {
            if (Phase != BattlePhase.PlayerTurn) return;
            YieldTurn();
            var accumulated = new List<BattleEvent>(_events);
            while (AdvanceOnce())
            {
                accumulated.AddRange(_events);
            }
            accumulated.AddRange(_events);
            _events.Clear();
            _events.AddRange(accumulated);
        }
```

⚠ `AdvanceOnce` 返回 `false` 的那一次(轮到玩家)也可能产生事件(`BeginPlayerTurn` 里的缺笔妖补全),所以循环外要再 `AddRange` 一次。

- [ ] **Step 4: 把旧流水线切成三个入口**

`SettlePlayerTurnEnd()` / `ActSummonTurn(int)` / `ActEnemyTurn(int)` / `BeginPlayerTurn()` 四个方法,**本任务里仍按旧归属填内容**:

- `SettlePlayerTurnEnd()`:原 `EndTurn` 的 `:693-764`(敌人灼烧 → 玩家灼烧 → 敌人流血 → 玩家 HoT → 召唤物光环治疗),含玩家灼烧致死早退
- `ActSummonTurn(s)`:调 `StrikeOnceWithSummon(s)` + `CheckWin()`
- `ActEnemyTurn(i)`:`ApplyEnemyBuffAura(i)` 或 `ActOneEnemy(i, 1)`(二选一,沿用原来的「已用加攻代替出手」判断)
- `BeginPlayerTurn()`:`TickAllStatuses()` → 判负 → `CheckWin()` → 对所有敌人 `RegrowOneEnemy(i)` → `StartTurn()`

- [ ] **Step 5: 跑全量**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: `AtbTimingTests` 8 条全绿。旧测试**可能开始有零星红**(多次行动的怪从「一拍打 2 次」变成「排两次队」,事件顺序略有差异)。把红的记进 `docs/superpowers/plans/atb-测试分类.md`,Task 15 统一处理,**本步不改任何旧断言**。

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/AtbTimingTests.cs
git commit -m "feat(core): 调度器驱动战斗 —— AdvanceOnce 逐格,EndTurn 退化为包装"
```

---

## Task 8: 敌人那一拍 —— DOT 前置、状态递减后置

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`ActEnemyTurn` / `SettlePlayerTurnEnd` / `BeginPlayerTurn`)
- Modify: `Brushblade/Assets/_Project/Tests/AtbTimingTests.cs`(追加)

**Interfaces:**
- Consumes: Task 7 的 `ActEnemyTurn(int)`
- Produces: `ActEnemyTurn` 的最终形态(spec §4.3「每个敌人那一拍」六步)

- [ ] **Step 1: 写失败测试**

```csharp
        [Test]
        public void EnemyBurn_SettlesBeforeThatEnemyActs_NotAtPlayerTurnEnd()
        {
            // 灼烧从「玩家回合末全场统一烧」改为「它自己动之前烧」。
            // 观察点:玩家让出后、敌人那一拍执行前,敌人血量不该已经掉。
            var engine = Engine(new[] { Dummy(hp: 100, attack: 10) });
            engine.Enemies[0].Statuses.Apply(new StatusEffect
            {
                Kind = StatusKind.Burn, Polarity = StatusPolarity.Debuff,
                Magnitude = 2, TurnsLeft = -1, SourceId = "测",
            });
            int before = engine.Enemies[0].Hp;

            engine.YieldTurn();
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(before), "让出行动权时还不该烧");

            engine.AdvanceOnce();
            Assert.That(engine.Enemies[0].Hp, Is.LessThan(before), "轮到它自己那拍才烧");
        }

        [Test]
        public void EnemyStatus_TicksAfterThatEnemyActs()
        {
            var engine = Engine(new[] { Dummy(attack: 10) });
            engine.Enemies[0].Statuses.Apply(new StatusEffect
            {
                Kind = StatusKind.Bleed, Polarity = StatusPolarity.Debuff,
                Magnitude = 5, TurnsLeft = 3, SourceId = "测",
            });

            engine.YieldTurn();
            engine.AdvanceOnce();   // 敌人这一拍:结算流血 + 行动 + 自身递减

            Assert.That(engine.Enemies[0].Statuses.Find(StatusKind.Bleed).TurnsLeft, Is.EqualTo(2));
        }

        [Test]
        public void SlowedEnemy_TicksItsOwnDotSlower()
        {
            // 口径 1 的直接后果:被减速的敌人中的毒也跌得慢。
            var engine = Engine(new[] { Dummy(hp: 999, attack: 0), Dummy("快", 999, 0) });
            engine.Enemies[0].Statuses.Apply(new StatusEffect
            {
                Kind = StatusKind.SpeedModifier, Polarity = StatusPolarity.Debuff,
                Magnitude = -50, TurnsLeft = -1, SourceId = "缓",
            });
            foreach (var e in engine.Enemies)
                e.Statuses.Apply(new StatusEffect
                {
                    Kind = StatusKind.Bleed, Polarity = StatusPolarity.Debuff,
                    Magnitude = 10, TurnsLeft = 99, SourceId = "血",
                });

            for (int i = 0; i < 4; i++) engine.EndTurn();

            Assert.That(engine.Enemies[0].Hp, Is.GreaterThan(engine.Enemies[1].Hp),
                "半速的怪流血次数少一半,应该更健康");
        }

        [Test]
        public void BossCharge_CountsItsOwnActions_NotGlobalTurns()
        {
            // spec §4.5:BossChargeEvery 从「阶段内第 N 个敌方回合」改为「该 Boss 自己的行动次数」。
            // 同速下等价 —— 但被减速的 Boss 攒大招也该变慢,这条锁住它。
            // Boss 的构造照 BossSkillTests 里既有的工厂写(带 Phases 的 EnemyDef)。
            var normal = BossEngine(speedModifier: 0);
            var slowed = BossEngine(speedModifier: -50);

            for (int i = 0; i < 4; i++) { normal.EndTurn(); slowed.EndTurn(); }

            int normalCasts = CountBossSkillEvents(normal);
            int slowedCasts = CountBossSkillEvents(slowed);
            Assert.That(slowedCasts, Is.LessThan(normalCasts), "半速的 Boss 攒大招也该慢一半");
        }
```

`BossEngine(int speedModifier)` 与 `CountBossSkillEvents(BattleEngine)` 是本测试类要新写的两个私有辅助:前者建一只带 `Phases` 的 Boss 并按需挂 `SpeedModifier` 状态,后者数 `LastEvents` 里的 Boss 大招事件。Boss 的构造与大招事件种类照 `BossSkillTests.cs` 里既有的工厂与断言抄,别自创。

- [ ] **Step 2: 跑测试确认失败**

Expected: `EnemyBurn_SettlesBeforeThatEnemyActs_NotAtPlayerTurnEnd` 红(「让出行动权时还不该烧」),因为归属还在 `SettlePlayerTurnEnd`

- [ ] **Step 3: 搬家**

- 从 `SettlePlayerTurnEnd()` 里**删掉**敌人灼烧循环(`for … SettleBurnOn(i)`)与敌人流血循环
- `ActEnemyTurn(int i)` 改为:

```csharp
        /// <summary>一个敌人的完整一拍(spec §4.3):自身 DOT → 缺笔妖补全 → 冻结跳过
        /// → 行动 → 自身状态递减。任何一步打死它/打死玩家都当场结算。</summary>
        private void ActEnemyTurn(int i)
        {
            var enemy = _enemies[i];
            if (!enemy.Alive) return;

            SettleBurnOn(i);
            if (!enemy.Alive) { ResolveDefeat(i); CheckWin(); return; }

            SettleBleedOn(i);
            if (!enemy.Alive) { ResolveDefeat(i); CheckWin(); return; }

            RegrowOneEnemy(i);

            if (enemy.Def.Ability == EnemyAbility.Buff && !IsSilenced(enemy) && HasOtherAliveEnemy(enemy))
                ApplyEnemyBuffAura(i);
            else
                ActOneEnemy(i, 1);

            enemy.Statuses.TickTurns();
        }
```

- 新增 `SettleBleedOn(int enemyIndex)`,把 `SettlePlayerTurnEnd` 里那段流血循环体搬进来(单只版本)
- `BeginPlayerTurn()` 里删掉缺笔妖循环(已归到敌人自己那拍)
- `TickAllStatuses()` 里删掉敌人侧循环,只留玩家侧(敌人已在自己那拍递减);方法改名为 `TickPlayerStatuses()`

⚠ `ResolveDefeat(i)` 现在只发事件、不判胜(见既有口径),所以每处都要跟一个 `CheckWin()`。

- [ ] **Step 4: 跑测试**

Expected: 三条新测试 PASS。旧测试的红条数会上升,继续记进 `atb-测试分类.md`,不改断言。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/AtbTimingTests.cs
git commit -m "feat(core): 敌人那一拍 —— DOT 前置、缺笔妖前置、状态递减后置"
```

---

## Task 9: 召唤物那一拍

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`ActSummonTurn` / `SettlePlayerTurnEnd`)
- Modify: `Brushblade/Assets/_Project/Tests/AtbTimingTests.cs`(追加)

- [ ] **Step 1: 写失败测试**

```csharp
        [Test]
        public void SummonAura_HealsOnItsOwnTurn_NotAtPlayerTurnEnd()
        {
            // 光环治疗(桃)从「全体召唤物集体先治疗」改为「该召唤物自己那拍先治疗再出手」。
            // 扣血走真实路径(挨敌人一记),不为测试新增生产 API —— 2026-08-11 用户裁定过这条。
            var graph = new RecipeGraph(new[]
            {
                new CharDef("木", Element.Wood),
                new CharDef("桃", Element.Wood, effects: new[]
                {
                    new EffectDef(EffectKind.Summon, 20, summonCount: 1, summonAttack: 2,
                        summonChar: "木", passive: new SummonPassive { HealAlly = 5 }),
                }),
            });
            var engine = new BattleEngine(graph,
                new BattleConfig { PlayerMaxHp = 999, UnlockedChars = new[] { "桃" } },
                new[] { "桃" }, Array.Empty<string>(),
                new[] { new EnemyDef("凶", Element.Heart, 999, 30) }, seed: 1);
            engine.EndTurn();          // 挨一记,掉血
            engine.Cast("桃");         // 召唤带光环的树
            int hurt = engine.PlayerHp;

            engine.YieldTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(hurt), "让出行动权时还不该治疗");

            while (engine.AdvanceOnce() && engine.LastActor.Kind != ActorKind.Summon) { }
            Assert.That(engine.PlayerHp, Is.GreaterThan(hurt), "召唤物自己那拍才治疗");
        }
```

`HealAlly` 长在 `SummonPassive` 上(`SummonPassive.cs:21`),经由 `EffectDef` 的 `passive:` 具名参数(`EffectDef.cs:107`)传入 —— 与 `SummonPassiveTests.cs:47` 的既有写法一致。

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 搬家**

- 从 `SettlePlayerTurnEnd()` 删掉召唤物光环治疗循环
- `ActSummonTurn(int s)` 改为:

```csharp
        /// <summary>一只召唤物的完整一拍(spec §4.3):光环治疗 → 出手 → 自身状态递减。</summary>
        private void ActSummonTurn(int s)
        {
            var summon = _summons[s];
            if (!summon.Alive) return;

            int heal = summon.Passive?.HealAlly ?? 0;
            if (heal > 0) HealPlayerAndSummons(heal);

            if (_enemies.Any(e => e.Alive)) StrikeOnceWithSummon(s);
            CheckWin();
        }
```

召唤物目前没有状态容器(`SummonState` 无 `Statuses` 字段),所以「自身状态递减」这一步暂无内容 —— **不要为此新增字段**,等真有召唤物状态时再加。在方法注释里写明这一点。

- [ ] **Step 4: 跑测试确认通过**

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/AtbTimingTests.cs
git commit -m "feat(core): 召唤物那一拍 —— 光环治疗归到它自己那拍"
```

---

## Task 10: 玩家那一拍

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`
- Modify: `Brushblade/Assets/_Project/Tests/AtbTimingTests.cs`(追加)

- [ ] **Step 1: 写失败测试**

```csharp
        [Test]
        public void PlayerBurn_SettlesAtPlayerTurnStart_NotWhenYielding()
        {
            var engine = Engine(new[] { Dummy(attack: 0) });
            engine.EndTurn();   // 走到一个干净的玩家拍
            engine.PlayerStatuses.Apply(new StatusEffect
            {
                Kind = StatusKind.Burn, Polarity = StatusPolarity.Debuff,
                Magnitude = 2, TurnsLeft = -1, SourceId = "灯",
            });
            int before = engine.PlayerHp;

            engine.YieldTurn();
            Assert.That(engine.PlayerHp, Is.EqualTo(before), "让出行动权那一刻不烧");

            while (engine.AdvanceOnce()) { }   // 推到下一个玩家拍
            Assert.That(engine.PlayerHp, Is.LessThan(before), "玩家自己那拍开头才烧");
        }

        [Test]
        public void PlayerStatus_TicksWhenPlayerYields()
        {
            var engine = Engine(new[] { Dummy(attack: 0) });
            engine.PlayerStatuses.Apply(new StatusEffect
            {
                Kind = StatusKind.HealOverTime, Polarity = StatusPolarity.Buff,
                Magnitude = 5, TurnsLeft = 3, SourceId = "滋",
            });

            engine.YieldTurn();

            Assert.That(engine.PlayerStatuses.Find(StatusKind.HealOverTime).TurnsLeft, Is.EqualTo(2));
        }

        [Test]
        public void FastPlayer_GetsApAndDropTwiceAsOften()
        {
            // 口径 5:一次行动 = 一份完整回合(3 AP + 1 掉字)
            var engine = Engine(new[] { Dummy(attack: 0) },
                new BattleConfig { PlayerMaxHp = 999, PlayerSpeed = 200 });
            int turnBefore = engine.Turn;

            engine.EndTurn();
            engine.EndTurn();

            Assert.That(engine.Turn, Is.EqualTo(turnBefore + 2));
            Assert.That(engine.Ap, Is.EqualTo(engine.ApPerTurn), "每拍都回满 AP");
        }
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现最终形态**

```csharp
        /// <summary>玩家让出行动权(spec §4.3 玩家那一拍第 6 步):只做玩家侧状态递减。
        /// DOT 与 AP 补给都挪到了 BeginPlayerTurn —— 玩家的一拍从「自己开始」算起。</summary>
        private void SettlePlayerTurnEnd()
        {
            TickPlayerStatuses();
        }

        /// <summary>轮到玩家(spec §4.3 玩家那一拍第 1~4 步):灼烧 → HoT → 判负 → StartTurn。</summary>
        private void BeginPlayerTurn()
        {
            SettlePlayerBurn();
            if (Phase == BattlePhase.Lost) return;
            SettlePlayerHots();
            if (PlayerHp <= 0) { Phase = BattlePhase.Lost; return; }
            CheckWin();
            if (Phase != BattlePhase.PlayerTurn) return;
            StartTurn();
        }
```

`SettlePlayerBurn()` / `SettlePlayerHots()` 从原 `SettlePlayerTurnEnd` 里拆出(内容照搬 `BattleEngine.cs:700-708` 与 `:744-755`)。玩家灼烧致死的早退在 `SettlePlayerBurn` 内部置 `Phase = Lost` 即可 —— 递减已经在上一拍的 `YieldTurn` 里跑过了,不需要补。

- [ ] **Step 4: 跑测试确认通过**

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/AtbTimingTests.cs
git commit -m "feat(core): 玩家那一拍 —— 灼烧/HoT/AP 归到拍首,状态递减归到拍尾"
```

---

## Task 11: 冻结跳过

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`ActEnemyTurn`)
- Modify: `Brushblade/Assets/_Project/Tests/AtbTimingTests.cs`(追加)

- [ ] **Step 1: 写失败测试**

```csharp
        [Test]
        public void Frozen_KeepsItsSlotButSkipsTheAction()
        {
            var engine = Engine(new[] { Dummy(hp: 999, attack: 50) });
            engine.Enemies[0].Statuses.Apply(new StatusEffect
            {
                Kind = StatusKind.Freeze, Polarity = StatusPolarity.Debuff,
                Magnitude = 1, TurnsLeft = 2, SourceId = "冻",
            });
            int hp = engine.PlayerHp;

            engine.EndTurn();

            Assert.That(engine.PlayerHp, Is.EqualTo(hp), "冻结中不出手");
            Assert.That(engine.Enemies[0].Statuses.Find(StatusKind.Freeze).TurnsLeft, Is.EqualTo(1),
                "轮到它就 −1,不靠别人的回合数");
        }

        [Test]
        public void Frozen_ThawsAndResumesActing()
        {
            var engine = Engine(new[] { Dummy(hp: 999, attack: 50) });
            engine.Enemies[0].Statuses.Apply(new StatusEffect
            {
                Kind = StatusKind.Freeze, Polarity = StatusPolarity.Debuff,
                Magnitude = 1, TurnsLeft = 2, SourceId = "冻",
            });

            engine.EndTurn();
            engine.EndTurn();
            int hp = engine.PlayerHp;
            engine.EndTurn();

            Assert.That(engine.Enemies[0].Statuses.Has(StatusKind.Freeze), Is.False, "两拍后解冻");
            Assert.That(engine.PlayerHp, Is.LessThan(hp), "解冻后照常打人");
        }

        [Test]
        public void Frozen_DoesNotDeadlockTheScheduler()
        {
            // 冻结单位照常上行动条(口径 6):它若被排除出调度,自身状态就永远不递减
            var engine = Engine(new[] { Dummy(hp: 999, attack: 0) });
            engine.Enemies[0].Statuses.Apply(new StatusEffect
            {
                Kind = StatusKind.Freeze, Polarity = StatusPolarity.Debuff,
                Magnitude = 1, TurnsLeft = 99, SourceId = "冻",
            });

            var forecast = engine.Forecast(6);

            Assert.That(forecast.Any(a => a.Kind == ActorKind.Enemy), Is.True,
                "冻结单位仍要出现在预测里(会被跳过,但占位)");
        }
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现**

在 `ActEnemyTurn` 的「缺笔妖补全」之后、「行动」之前插:

```csharp
            // 冻结(2026-08-15 口径 6):照常上行动条,轮到就跳过并 −1。
            // 旧语义是「计量器冻住」—— 那在 CTB 下会死锁:永远轮不到 → 自身状态永远不递减
            // → 冻结永远解不了。
            if (enemy.Statuses.Has(StatusKind.Freeze))
            {
                enemy.Statuses.TickTurns();
                return;
            }
```

同时把 `BuildSlots()` 里**不要**加任何冻结过滤 —— 冻结单位必须照常进调度。

`TickAllStatuses` 里原先那个「冻结时 SpeedModifier 不递减」的豁免(`BattleEngine.cs:966-967`)删除:计量器已经照常推进,SpeedModifier 也就该照常倒计时。

- [ ] **Step 4: 跑测试确认通过**

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/AtbTimingTests.cs
git commit -m "feat(core): 冻结改为轮到即跳过 —— 消除 CTB 下的冻结死锁"
```

---

## Task 12: 立即判负/判胜与复活

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`DamagePlayerDirect` / `DamageSummon` / `DamageEnemy` / `Revive`)
- Modify: `Brushblade/Assets/_Project/Tests/AtbTimingTests.cs`(追加)

- [ ] **Step 1: 写失败测试**

```csharp
        [Test]
        public void PlayerDeath_EndsBattleImmediately_RemainingEnemiesDoNotAct()
        {
            // 三只怪,第一只就能打死玩家:后两只不该再动(UI 也不该再读条)
            var engine = Engine(new[]
            {
                new EnemyDef("甲", Element.Heart, 999, 500),
                new EnemyDef("乙", Element.Heart, 999, 500),
                new EnemyDef("丙", Element.Heart, 999, 500),
            }, new BattleConfig { PlayerMaxHp = 100 });

            engine.EndTurn();

            Assert.That(engine.Phase, Is.EqualTo(BattlePhase.Lost));
            Assert.That(engine.LastEvents.Count(e => e.Kind == BattleEventKind.EnemyAttack),
                Is.EqualTo(1), "第一记就该收口");
        }

        [Test]
        public void LastEnemyDeath_WinsImmediately()
        {
            var engine = Engine(new[] { Dummy(hp: 1, attack: 0) });
            engine.Enemies[0].Statuses.Apply(new StatusEffect
            {
                Kind = StatusKind.Bleed, Polarity = StatusPolarity.Debuff,
                Magnitude = 99, TurnsLeft = 3, SourceId = "血",
            });

            engine.EndTurn();

            Assert.That(engine.Phase, Is.EqualTo(BattlePhase.Won), "流血打死最后一只就当场赢");
        }

        [Test]
        public void Revive_ContinuesTheTimeline_RemainingEnemiesStillAct()
        {
            // spec §4.3.1:复活 = 满血站起来,时间轴原地继续。
            // 旧行为是 StartTurn() 开新一拍,等于白捡「剩下的怪本回合不再出手」。
            var engine = Engine(new[]
            {
                new EnemyDef("甲", Element.Heart, 999, 150),
                new EnemyDef("乙", Element.Heart, 999, 150),
            }, new BattleConfig { PlayerMaxHp = 100 });

            engine.EndTurn();
            Assert.That(engine.Phase, Is.EqualTo(BattlePhase.Lost));

            engine.Revive();
            Assert.That(engine.PlayerHp, Is.EqualTo(100));

            while (engine.AdvanceOnce()) { }
            Assert.That(engine.PlayerHp, Is.LessThan(100), "没行动的怪照常打");
        }
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现**

1. `DamagePlayerDirect` / `DamageSummon` 里,扣完血立刻:

```csharp
            if (PlayerHp <= 0) Phase = BattlePhase.Lost;
```

2. `ActEnemyTurn` / `ActOneEnemy` 的多次行动循环、`AdvanceOnce` 的循环里,每步后检查 `Phase != BattlePhase.PlayerTurn` 就 `return`。
3. `ResolveDefeat` 之后统一跟 `CheckWin()`(Task 8 已铺)。`DamageEnemy` 里敌人死亡的分支同样补 `CheckWin()`。
4. `Revive()` 改为:

```csharp
        /// <summary>广告复活(spec §4.3.1):满血站起来,**时间轴原地继续** ——
        /// 不重置调度器、不跳过剩余行动者、不补任何递减。
        /// 旧实现调 StartTurn() 开新一拍,等于白送「剩下的怪本回合不再出手」;
        /// 玩家买到的是满血,不是无敌一轮。</summary>
        public void Revive()
        {
            if (Phase != BattlePhase.Lost) return;
            PlayerHp = _config.PlayerMaxHp;
            Phase = BattlePhase.PlayerTurn;
        }
```

⚠ 去掉 `StartTurn()` 后,复活当拍玩家**不会**拿到新 AP —— 这是正确的:AP 在轮到玩家时由 `BeginPlayerTurn` 给。

- [ ] **Step 4: 跑测试确认通过**

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/AtbTimingTests.cs
git commit -m "feat(core): HP 归零当场结算,复活不再跳过剩余行动者"
```

---

## Task 13: 通假字现形双路径

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`DamageEnemy` + `ActOneEnemy`)
- Modify: `Brushblade/Assets/_Project/Tests/AtbTimingTests.cs`(追加)

- [ ] **Step 1: 写失败测试**

```csharp
        [Test]
        public void Disguise_RevealsWhenTakingDamage()
        {
            var graph = new RecipeGraph(new[]
            {
                new CharDef("木", Element.Wood,
                    effects: new[] { new EffectDef(EffectKind.DamageSingle, 10) }),
            });
            var engine = new BattleEngine(graph,
                new BattleConfig { PlayerMaxHp = 999, UnlockedChars = new[] { "木" } },
                new[] { "木" }, Array.Empty<string>(),
                new[] { new EnemyDef("通", Element.Wood, 999, 0, EnemyAbility.Disguise) }, seed: 1);
            var disguised = engine.Enemies[0].ApparentElement;

            engine.Cast("木", 0);

            Assert.That(engine.Enemies[0].ApparentElement, Is.EqualTo(engine.Enemies[0].Element),
                "挨打就现形");
        }

        [Test]
        public void Disguise_StillRevealsAfterActing()
        {
            // 旧口径(8.3 / 2026-08-08)保持不变:它出手就现形,打空也算
            var engine = Engine(new[] { new EnemyDef("通", Element.Wood, 999, 10, EnemyAbility.Disguise) });

            engine.EndTurn();

            Assert.That(engine.Enemies[0].ApparentElement, Is.EqualTo(engine.Enemies[0].Element));
        }
```

- [ ] **Step 2: 跑测试确认失败**

Expected: `Disguise_RevealsWhenTakingDamage` 红

- [ ] **Step 3: 实现**

把 `ActOneEnemy` 里的现形代码块(`BattleEngine.cs:881-885`)提成方法,并在 `DamageEnemy` 的伤害落地处也调一次:

```csharp
        /// <summary>通假字现形(8.3 + 2026-08-15 口径 7):挨打或出手,先到先触发。
        /// 「出手但打空也现形」的旧口径不变 —— 敌人确实动了,与命中判定无关。</summary>
        private void RevealDisguise(int enemyIndex)
        {
            var enemy = _enemies[enemyIndex];
            if (enemy.Def.Ability != EnemyAbility.Disguise) return;
            if (enemy.ApparentElement == enemy.Element) return;
            enemy.ApparentElement = enemy.Element;
            _events.Add(new BattleEvent(BattleEventKind.EnemyRevealed, enemyIndex, 0));
        }
```

`DamageEnemy` 里,确认伤害真的落到该敌人之后(`enemy.HitsTaken += 1` 附近)调 `RevealDisguise(enemyIndex)`。灼烧/流血走的是 `SettleBurnOn` / `SettleBleedOn` 里的直接扣血,那两处也各加一次调用。

- [ ] **Step 4: 跑测试确认通过**

同时确认既有的 `InfoHidingTests.Disguise_RevealsAfterFirstAction` 与 `DamageVariantTests.Silence_DoesNotAffectDisguiseOrObscure` 仍绿。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/AtbTimingTests.cs
git commit -m "feat(core): 通假字挨打也现形(出手现形保持不变)"
```

---

## Task 14: 快照

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/RunSnapshot.cs`
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`Capture` / `Restore`)
- Modify: `Brushblade/Assets/_Project/Tests/AtbTimingTests.cs`(追加)

- [ ] **Step 1: 写失败测试**

```csharp
        [Test]
        public void PlayerActionMeter_SurvivesRoundTrip()
        {
            var engine = Engine(new[] { Dummy(attack: 0) },
                new BattleConfig { PlayerMaxHp = 999, PlayerSpeed = 150 });
            engine.EndTurn();   // 速度 150:行动后计量器留 50 的余额

            var snapshot = engine.Capture();
            var restored = BattleEngine.Restore(snapshot, Graph(),
                new BattleConfig { PlayerMaxHp = 999, PlayerSpeed = 150 }, null, null);

            Assert.That(restored.PlayerActionMeter, Is.EqualTo(engine.PlayerActionMeter));
        }

        [Test]
        public void RestoredBattle_ContinuesTheSameRhythm()
        {
            var engine = Engine(new[] { Dummy(attack: 0), Dummy("乙", 999, 0) },
                new BattleConfig { PlayerMaxHp = 999, PlayerSpeed = 150 });
            engine.EndTurn();

            var restored = BattleEngine.Restore(engine.Capture(), Graph(),
                new BattleConfig { PlayerMaxHp = 999, PlayerSpeed = 150 }, null, null);

            Assert.That(restored.Forecast(6), Is.EqualTo(engine.Forecast(6)));
        }
```

签名是 `Restore(BattleSnapshot snapshot, RecipeGraph graph, BattleConfig config, IReadOnlyDictionary<string,int> cardLevels, IReadOnlyDictionary<string,EnemyDef> enemyDefs)`(`BattleEngine.cs:397`)—— 后两个传 `null` 即「全 1 级卡 / 不需要按 Id 还原敌人定义」。

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现**

`RunSnapshot.cs` 的 `BattleSnapshot` 里加:

```csharp
        /// <summary>玩家的行动计量器(2026-08-15,ATB 改造)。不存会让续爬后玩家的节拍
        /// 从 0 重来 —— 敌人侧的 ActionMeter 早就在存了,这条是补齐。</summary>
        public int PlayerActionMeter { get; set; }
```

`Capture()` 里加 `PlayerActionMeter = PlayerActionMeter,`,`Restore` 里加 `engine.PlayerActionMeter = snapshot.PlayerActionMeter;`。

- [ ] **Step 4: 跑测试确认通过**

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/RunSnapshot.cs Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/AtbTimingTests.cs
git commit -m "feat(core): 玩家计量器进快照,续爬后节拍不重来"
```

---

## Task 15: 旧测试全量分类审 + 删 MaxActionsPerTurn

**Files:**
- Modify: 多个 `Brushblade/Assets/_Project/Tests/*.cs`
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(删 `MaxActionsPerTurn` / `ActionMeterThreshold`)
- Create: `docs/superpowers/plans/atb-测试分类.md`

- [ ] **Step 1: 删掉已被调度器接管的常量**

`BattleEngine.cs:193` 的 `MaxActionsPerTurn` 与 `:192` 的 `ActionMeterThreshold` 删除,残留引用改用 `TurnScheduler.Threshold`。编译错就是残留清单。

- [ ] **Step 2: 跑全量,产出红名单**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q 2>&1 | grep -E "^\s+(Failed|失败)" > /tmp/atb-failures.txt
wc -l /tmp/atb-failures.txt
```

- [ ] **Step 3: 逐条分类,写进 `docs/superpowers/plans/atb-测试分类.md`**

每条红测试填一行:

```markdown
| 测试 | 原断言 | 判定 | 处理 |
|---|---|---|---|
| `BattleEngineTests.Xxx` | 灼烧在玩家回合末结算 | 行为该变(spec §4.3) | 改断言,注释写明原因 |
| `BurnVariantTests.Yyy` | 三层灼烧共 60 伤害 | 仍应成立 | 是缺陷,改实现 |
```

判定标准:

| 分类 | 处理 |
|---|---|
| 断言表达的行为**按新设计确实该变** | 改断言,并在测试注释里写明「2026-08-15 CTB 改造:原为 X,现为 Y,因为 Z」 |
| 断言表达的行为**仍然应该成立** | 是缺陷,改实现不改断言 |

- [ ] **Step 4: 点名处理两条已知失去前提的**

1. `BattleEngineTests.EndTurn_EnemyTurnBegan_SeparatesSummonPhaseFromEnemyPhase` —— `EnemyTurnBegan` 在 Task 16 退役。本任务先把它标 `[Ignore("ATB:等 Task 16 换 ActorActed")]`,Task 16 改写为守 `ActorActed` 的段首语义。
2. `Revive_DoesNotGrantExtraStatusTurn`(在 `StatusOpsTests` 或 `BattleEngineTests`,先 `grep -rn "Revive_DoesNotGrantExtraStatusTurn" Brushblade/Assets/_Project/Tests/`)—— 前提消失(递减已分散到各单位自己那拍)。改写为守 spec §4.3.1:复活后时间轴继续,剩余行动者照常行动。

- [ ] **Step 5: 跑到全绿**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿,0 失败

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Tests docs/superpowers/plans/atb-测试分类.md Brushblade/Assets/_Project/Core/BattleEngine.cs
git commit -m "test(core): ATB 改造的旧测试逐条分类审,附判定留痕"
```

---

# T3 · 逐格驱动与 TAB UI(Task 16-18)

## Task 16: ActorActed 事件 + Juice 简化

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`BattleEventKind` 约 :130)
- Modify: `Brushblade/Assets/_Project/Presentation/Juice.cs:60-85`
- Modify: `Brushblade/Assets/_Project/Tests/BattleEngineTests.cs`(改写那条)

**Interfaces:**
- Produces: `BattleEventKind.ActorActed`(`TargetIndex` = 行动者下标,`Amount` = `(int)ActorKind`)

- [ ] **Step 1: 写失败测试**

改写 `BattleEngineTests.EndTurn_EnemyTurnBegan_SeparatesSummonPhaseFromEnemyPhase`:

```csharp
        [Test]
        public void AdvanceOnce_EmitsActorActedAsTheSegmentHeader()
        {
            // 2026-08-15 CTB 改造:原为「整个事件流里恰好一条 EnemyTurnBegan」,
            // 现为「每个行动者的事件段以一条 ActorActed 开头」——
            // 逐格驱动后表现层不再需要猜边界(Juice 那段三段切分随之删除)。
            var engine = Engine(new[] { new EnemyDef("靶", Element.Heart, 999, 10) });

            engine.YieldTurn();
            engine.AdvanceOnce();

            Assert.That(engine.LastEvents[0].Kind, Is.EqualTo(BattleEventKind.ActorActed));
            Assert.That(engine.LastEvents[0].Amount, Is.EqualTo((int)ActorKind.Enemy));
            Assert.That(engine.LastEvents[0].TargetIndex, Is.EqualTo(0));
        }
```

⚠ `Engine(...)` 是 `BattleEngineTests` 自己的私有工厂,签名与 `AtbTimingTests` 的不同 —— 照该文件里既有的写法用。

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现**

1. `BattleEventKind` 末尾追加 `ActorActed`(⚠ 追加在**末尾**,该枚举随事件流走表现层,不进存档但仍按同一纪律)
2. 删掉 `EnemyTurnBegan` 及其唯一产出点
3. `AdvanceOnce` 里 `_events.Clear()` 之后立刻:

```csharp
            _events.Add(new BattleEvent(BattleEventKind.ActorActed,
                step.Actor.Index, (int)step.Actor.Kind));
```

4. `Juice.PlayRoutine`(`Juice.cs:60-85`)删掉 `preRest` / `strikes` / `postRest` 三段切分,改为顺序播完整批事件:

```csharp
            // 逐格驱动后(2026-08-15 ATB 改造),每批事件天生属于一个行动者,
            // 不再需要猜段边界 —— 原先靠 SummonAttack / EnemyTurnBegan 划界的三段切分已删除,
            // 段间停顿由 BattleView 的驱动协程控制。
            yield return ApplyBatch(events, enemyAnchor, summonAnchor, onImpact);
```

`ActorActed` 本身不播(在 `ApplyBatch` 的 switch 里加一个空分支或直接跳过)。

- [ ] **Step 4: 验证**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

Expected: 测试全绿;离线编译 0 个 `error CS`

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Presentation/Juice.cs Brushblade/Assets/_Project/Tests/BattleEngineTests.cs
git commit -m "refactor(ui): EnemyTurnBegan 退役换 ActorActed,Juice 三段切分删除"
```

---

## Task 17: BattleView 逐格驱动

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs:2081-2093`(`OnEndTurn`)

- [ ] **Step 1: 改成协程循环**

```csharp
        private void OnEndTurn()
        {
            if (Battle.Phase != BattlePhase.PlayerTurn) return;
            BeginAnim();               // 锁输入:整段推进期间不许出字
            CancelSelection();
            Battle.YieldTurn();
            _tutorial?.Notify(TutorialAction.EndTurn);
            StartCoroutine(AdvanceRoutine());
        }

        /// <summary>逐格推进(2026-08-15 ATB 改造):一次 AdvanceOnce 播一个行动者的动画。
        /// 原先是一次 EndTurn 拿到整轮事件、由 Juice 猜边界切三段;现在边界由引擎给。</summary>
        private System.Collections.IEnumerator AdvanceRoutine()
        {
            while (true)
            {
                SnapshotPreHp();       // 每个行动者出手前的血量:动画逐记扣
                bool more = Battle.AdvanceOnce();
                var events = new System.Collections.Generic.List<BattleEvent>(Battle.LastEvents);
                var deaths = DeathsThisAction();
                _dyingEnemies.UnionWith(deaths);
                if (events.Count > 0)
                {
                    bool done = false;
                    _juice.Play(events, EnemyAnchor, SummonAnchor, () => done = true, OnImpact);
                    while (!done) yield return null;
                    yield return new WaitForSecondsRealtime(0.12f);   // 行动者之间的停顿
                }
                Refresh();
                if (!more) break;
                if (Battle.Phase == BattlePhase.Won || Battle.Phase == BattlePhase.Lost) break;
            }
            _message = Battle.Phase == BattlePhase.PlayerTurn
                ? $"回合 {Battle.Turn}:+{Battle.ApPerTurn} AP,字掉落" : "";
            AppendBossSkillMessage();
            EndAnim();
            Refresh();
        }
```

`EnemyAnchor` / `SummonAnchor` / `OnImpact` 都是既有成员:现行 `PlayAnimated`(`BattleView.cs:119-122`)调的就是 `_juice.Play(events, EnemyAnchor, SummonAnchor, () => OnAnimDone(deaths), OnImpact)`。上面把第四个实参从 `OnAnimDone(deaths)` 换成了协程的 `done` 标志 —— `OnAnimDone` 里原本做的收尾(解锁输入、重绘)移到协程末尾统一做,所以 `PlayAnimated` / `OnAnimDone` 这对方法在本任务后只剩 `Cast` 那条路径还在用,**不要删**。

- [ ] **Step 2: 处理 MaybeAutoEndTurn 的复用**

`AutoEndTurn` 协程(`:1986`)里的 `OnEndTurn()` 调用不用改 —— 新的 `OnEndTurn` 自己起协程。但 `while (Time.unscaledTime < _autoEndDueAt || Animating)` 的 `Animating` 判断现在会一直为真直到整段推进结束,这是对的。

- [ ] **Step 3: 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

Expected: 0 个 `error CS`

- [ ] **Step 4: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation/BattleView.cs
git commit -m "feat(ui): 战斗改为逐行动者驱动,一格一段动画"
```

---

## Task 18: TurnBar 行动条

**Files:**
- Create: `Brushblade/Assets/_Project/Presentation/TurnBar.cs`
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`(布局挂载 + `Refresh` 里刷新)

- [ ] **Step 1: 写 TurnBar**

```csharp
using System.Collections.Generic;
using Brushblade.Core;
using UnityEngine;
using UnityEngine.UI;

namespace Brushblade.Presentation
{
    /// <summary>顶部行动条(2026-08-15,ATB 改造):最左是当前行动者(放大 + 高亮),
    /// 往右是 Forecast 出的未来 N 拍。冻结格置灰。
    ///
    /// ⚠ 每次 Refresh 都重新问引擎要预测,**不缓存** —— 怪死了、召唤物上场、减速生效
    /// 都会让上一次的预测失效(见 TurnScheduler.Forecast 的注释)。</summary>
    public sealed class TurnBar : MonoBehaviour
    {
        private const int Slots = 8;
        private readonly List<GameObject> _cells = new();
        private RectTransform _row;

        public void Build(Transform parent)
        {
            var go = Ui.Row(parent, "TurnBar");
            _row = (RectTransform)go.transform;
        }

        public void Refresh(BattleEngine battle)
        {
            foreach (var cell in _cells) if (cell != null) Object.Destroy(cell);
            _cells.Clear();
            if (battle == null || _row == null) return;

            var forecast = battle.Forecast(Slots);
            for (int i = 0; i < forecast.Count; i++)
            {
                var actor = forecast[i];
                bool current = i == 0;
                _cells.Add(Ui.Chip(_row, LabelFor(battle, actor),
                    ColorFor(battle, actor), Color.white, current ? 18 : 14));
            }
        }

        private static string LabelFor(BattleEngine battle, ActorRef actor) => actor.Kind switch
        {
            ActorKind.Player => "我",
            ActorKind.Summon => battle.Summons[actor.Index].Char,
            _ => battle.Enemies[actor.Index].Def.Id,
        };

        private static Color ColorFor(BattleEngine battle, ActorRef actor)
        {
            if (actor.Kind == ActorKind.Enemy
                && battle.Enemies[actor.Index].Statuses.Has(StatusKind.Freeze))
                return Theme.TextDim;   // 冻结:这一拍会被跳过
            return actor.Kind == ActorKind.Player ? Theme.Cinnabar : Theme.InkSoft;
        }
    }
}
```

签名已核对:`Ui.Row(Transform parent, string name, float spacing = 8)` 返回 `GameObject`(`UI/Ui.cs:43`),`Ui.Chip(Transform parent, string text, Color bg, Color fg, …)`(`UI/Ui.cs:221`)。`battle.Summons` / `battle.Enemies` 是既有只读属性。

- [ ] **Step 2: 在 BattleView 里挂载**

在 `BattleView` 建 UI 的地方(`_topRight` 那块附近,约 `:538`)加 `_turnBar = gameObject.AddComponent<TurnBar>(); _turnBar.Build(transform);`,并在 `Refresh()` 末尾加 `_turnBar.Refresh(Battle);`。

- [ ] **Step 3: 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

- [ ] **Step 4: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation/TurnBar.cs Brushblade/Assets/_Project/Presentation/BattleView.cs
git commit -m "feat(ui): 顶部行动条 —— 当前行动者 + 未来 8 拍预测"
```

- [ ] **Step 5: 请用户在 Unity 里试玩验收**

告诉用户:打开 Unity,进一场战斗,确认(1)行动条随出手推进;(2)减速字打上去后敌人格在行动条上后移;(3)冻结格置灰且轮到时被跳过。

---

# T4 · 玩家速度属性(Task 19)

## Task 19: MetaRules 速度曲线与 UI 显示

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Meta.cs`(`DodgeFor` 之后 + `BuildBattleConfig`)
- Modify: `Brushblade/Assets/_Project/Presentation/MapView.cs:100-101`
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs:648` 附近
- Modify: `Brushblade/Assets/_Project/Tests/MetaTests.cs`(追加)

- [ ] **Step 1: 写失败测试**

追加到 `MetaTests.cs`:

```csharp
        [Test]
        public void SpeedFor_StartsAtBaselineAndGrowsSlowly()
        {
            // 速度是最强属性(同时翻倍输出与资源产出,spec 口径 5),成长必须压得很慢
            Assert.That(MetaRules.SpeedFor(1), Is.EqualTo(100), "1 级 = 基准 = 与敌人同速");
            Assert.That(MetaRules.SpeedFor(26), Is.EqualTo(125), "封顶级 +25%");
            Assert.That(MetaRules.SpeedFor(99), Is.EqualTo(125), "封顶后不再涨");
        }

        [Test]
        public void BuildBattleConfig_InjectsPlayerSpeed()
        {
            // ⚠ 这条存在的理由见 BuildBattleConfig 的注释:漏注入一条属性是**静默**的
            var meta = new MetaState { CharacterXp = 0 };

            var config = MetaRules.BuildBattleConfig(meta, System.Array.Empty<string>());

            Assert.That(config.PlayerSpeed, Is.EqualTo(MetaRules.SpeedFor(1)));
        }
```

⚠ `MetaState` 的构造与 `CharacterXp` 的用法照 `MetaTests.cs` 里既有测试抄。封顶级是 26(与 `MaxHpFor` / `AttackFor` 同口径),先确认 `CharacterLevel` 在 26 级封顶。

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现**

`Meta.cs` 的 `DodgeFor` 之后加:

```csharp
        /// <summary>速度成长:100 + 1×(等级−1),上限 125(2026-08-15,ATB 改造)。
        /// 与其余四条同形同封顶级(26 级)。
        ///
        /// ⚠ **斜率刻意最小**。速度在 CTB 下是唯一同时翻倍「输出」与「资源产出」的属性
        /// (一次行动 = 3 AP + 1 掉字,spec 口径 5)—— 满级 +25% 已经相当于每四拍白拿一整回合,
        /// 再陡会让另外四条属性失去意义。基准 100 = 与敌人同速 = 一人一手,
        /// 1 级角色的战斗节奏与引入这条曲线之前完全一致。</summary>
        public static int SpeedFor(int level) => Math.Min(125, 100 + (level - 1));
```

`BuildBattleConfig` 里,`PlayerDodge` 那行之后加 `PlayerSpeed = SpeedFor(level),`。

- [ ] **Step 4: UI 显示**

`MapView.cs:100-101` 那条属性行末尾补速度:

```csharp
                $"经验 {_meta.CharacterXp}    HP 上限 {stats.PlayerMaxHp}    攻击 {stats.PlayerAttack}"
                + $"    护甲 {stats.PlayerDefense}    闪避 {stats.PlayerDodge}%    速度 {stats.PlayerSpeed}",
```

`BattleView.cs:648` 那排 chip 里补一个(只在偏离基准时显示,避免恒定写「速度 100」占版面):

```csharp
            if (Battle.EffectivePlayerSpeed != 100)
                Ui.Chip(statusRow.transform, $"速度 {Battle.EffectivePlayerSpeed}", Theme.Jade, Color.white, 12);
```

- [ ] **Step 5: 验证 + 提交**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

```bash
git add Brushblade/Assets/_Project/Core/Meta.cs Brushblade/Assets/_Project/Presentation/MapView.cs Brushblade/Assets/_Project/Presentation/BattleView.cs Brushblade/Assets/_Project/Tests/MetaTests.cs
git commit -m "feat(meta): 速度作为第五条角色属性接入养成曲线与 UI"
```

---

## 收尾检查

- [ ] `cd tools/coretests && … dotnet test` 全绿
- [ ] `cd tools/prescompile && … dotnet build` 0 个 `error CS`
- [ ] `python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q` 全绿(本改造不碰管线,跑一遍确认没误伤)
- [ ] 用户在 Unity Test Runner 里跑一遍 EditMode(工装绿 ≠ 编辑器绿)
- [ ] 用户真机试玩:行动条推进、减速改变顺序、冻结跳过、复活后剩余敌人照常行动
- [ ] `docs/superpowers/plans/atb-测试分类.md` 里每条红测试都有判定留痕
- [ ] 新增的 `.cs` 文件对应的 `.meta` 已提交(`TurnScheduler.cs.meta` / `TurnBar.cs.meta` / 两个测试文件的 meta)
