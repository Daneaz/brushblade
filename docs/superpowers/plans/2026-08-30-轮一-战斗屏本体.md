# 轮一 · 战斗屏本体 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `docs/design/ui/scenes/Battle.dc.html` 落到实现——三栏版面、格内横排、敌人护盾、列区间、30 枚图标、四步引导弹层。

**Architecture:** 骨架从「比例锚点 + 手算纵向预算」换成 `Horizontal/VerticalLayoutGroup + LayoutElement`，直译稿的 flex 语义；`DrawEnemies` / `DrawPlayerStats` / `DrawPool` 等绘制方法挂到新父节点，方法体不搬家。Core 侧先做完（列区间、护盾），表现层才动。

**Tech Stack:** Unity 6000.5.2f1（勿升版本）、uGUI、C#（Core/Data 禁引 UnityEngine）、Python 3（图标与字体管线）。

**Spec:** `docs/superpowers/specs/2026-08-30-战斗屏与局内流程接稿-design.md`

## Global Constraints

- **Core 与 Data 禁止引用 UnityEngine**（asmdef `noEngineReferences: true`）。
- **随机性一律走 Core 内带种子的 RNG**，禁用 `UnityEngine.Random`。本计划不新增任何随机。
- **玩家可见 UI 文案走字符串表**（`Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`），禁止硬编码。
- **新增任何玩家可见中文后必须重跑字体子集**：`python3 tools/fonts/subset_fonts.py`，然后 `python3 -m pytest tools/fonts/tests/ -q`。**光看 diff 大小说明不了变没变，要比 cmap。**
- **测试断言只用 Unity 版 NUnit 也支持的 API**：禁用 `Is.AnyOf` / `Is.All.AnyOf`；`Does.Not.Contain(x)` 只在 x 是 string 时能用，集合判包含一律写 `Assert.That(list.Contains(x), Is.True/False, "…")`。
- **测试里定位仓库根只能用 `TestContext.CurrentContext.TestDirectory`**，禁用 `AppContext.BaseDirectory`。
- **测试代码禁止直接引用 Newtonsoft**（Tests asmdef `overrideReferences: true`，只放行 `nunit.framework.dll`）。
- **提交信息用 conventional commits**（feat/fix/docs/chore + 范围），中文正文。
- **单位换算 1pt = 2.093 逻辑单位**（稿基准 iPhone 16 Pro Max 横屏 932×430pt；`CanvasScaler` 1600×900 按高匹配 → 逻辑画布 1950×900）。写常量时在旁边注明稿上的 pt 值。

### 验证命令（逐字照抄）

```bash
# Core/Data 单元测试
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

```bash
# Presentation 离线编译(改完 Presentation 必跑)
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

```bash
# Python 管线
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ tools/design/tests/ -q
```

只看 `error CS`；`warning MSB3245` 是 Unity 程序集自带的无关引用，忽略。

---

## File Structure

| 文件 | 责任 | 动作 |
| --- | --- | --- |
| `Brushblade/Assets/_Project/Core/EnemyDef.cs` | `EnemyDef.ColumnSpan` 字段、`EnemyState.ColumnSpan` 转发、`EnemyState.Shield` | 改 |
| `Brushblade/Assets/_Project/Core/Targeting.cs` | 区间裁定；删 `RowCells` | 改 |
| `Brushblade/Assets/_Project/Core/BattleEngine.cs` | `AssignSlots` 居中往外按宽打包、`FreeColumnIn` 连续空段、`DamageEnemy` 护盾吸收 | 改 |
| `Brushblade/Assets/_Project/Core/Campaign.cs` | `Scale` 透传 `ColumnSpan`（漏传会静默丢字段，speed 就是前车之鉴）| 改 |
| `Brushblade/Assets/_Project/Data/ConfigLoader.cs` | `EnemyDto.ColumnSpan` + 构造透传 | 改 |
| `Brushblade/Assets/StreamingAssets/config/enemies.json` | 三只 Boss 配 `"columnSpan": 4` | 改 |
| `Brushblade/Assets/_Project/Tests/TargetingTests.cs` | 区间裁定测试；删 `RowCells` 那 4 条 | 改 |
| `Brushblade/Assets/_Project/Tests/EnemyShieldTests.cs` | 敌人护盾吸收 | 建 |
| `Brushblade/Assets/_Project/Tests/EnemySlotTests.cs` | 站位分配（居中往外、按宽打包、分裂找连续空段）| 建 |
| `tools/icons/build_icons.py` | 补 12 枚图标（11 枚抄稿 + heal）| 改 |
| `tools/icons/tests/test_icons.py` | `EXPECTED` 同步 | 改 |
| `Brushblade/Assets/_Project/Presentation/UI/Icons.cs` | `Glyphs` 兜底汉字同步 | 改 |
| `Brushblade/Assets/_Project/Presentation/BattleView.cs` | 骨架换布局组；各区绘制改横排/新父节点 | 改 |
| `Brushblade/Assets/_Project/Presentation/UI/CoachOverlay.cs` | 四步引导弹层（新增能力，不进 4007 行）| 建 |
| `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json` | 引导三段文案、`✕`/`?` 无障碍名 | 改 |
| `docs/design/ui/scenes/StatusGlossary.dc.html` | 回填第 30 枚 `heal` 图标 | 改 |
| `docs/design/ui/scenes/README.md` | 回填「每排 4 格位」已完成、本轮落地清单 | 改 |

---

### Task 1: Core — 列区间（`ColumnSpan`）

给敌人「占几列」的概念，三处目标裁定改成区间运算，且 `Span = 1` 时与现在逐字节等价。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EnemyDef.cs`（`EnemyDef` 字段 + 构造；`EnemyState` 转发属性）
- Modify: `Brushblade/Assets/_Project/Core/Targeting.cs:196-250`
- Modify: `Brushblade/Assets/_Project/Core/Campaign.cs:88-93`
- Modify: `Brushblade/Assets/_Project/Data/ConfigLoader.cs:408-426`
- Modify: `Brushblade/Assets/StreamingAssets/config/enemies.json`
- Test: `Brushblade/Assets/_Project/Tests/TargetingTests.cs`

**Interfaces:**
- Produces: `EnemyDef.ColumnSpan` (int, 默认 1)、`EnemyState.ColumnSpan` (int, 转发 `Def.ColumnSpan`)、`EnemyState.ColumnEnd` (int = `Column + ColumnSpan`)。Task 2 的 `AssignSlots`/`FreeColumnIn` 与 Task 6 的表现层格宽都读它们。

- [ ] **Step 1: 写失败测试 —— 区间裁定的三条**

在 `TargetingTests.cs` 的 `Grid` 辅助**下方**追加一个带 span 的辅助与四条测试。`Grid` 现有签名不动（既有 20+ 条测试在用）：

```csharp
        /// <summary>带列宽的阵:span &gt; 1 的怪横跨 [Column, Column + span) 若干列。
        /// Boss 是目前唯一的 span &gt; 1 —— 编成里 Boss 独占一场,所以这些用例是**构造出来**的,
        /// 真机上跑不到。构造它们正是本条测试的意义:等真给 Boss 配了小怪,裁定已经是对的。</summary>
        private static List<EnemyState> SpanGrid(params (EnemyRow Row, int Column, int Span)[] slots)
        {
            var list = new List<EnemyState>();
            foreach (var (row, column, span) in slots)
            {
                var def = new EnemyDef($"怪{list.Count}", Element.Heart, 100, 10,
                    row: row, columnSpan: span);
                list.Add(new EnemyState(def, 0, null) { Row = row, Column = column });
            }
            return list;
        }

        [Test]
        public void Expand_Skewer_SpanningBossOverlapsEveryColumn()
        {
            // Boss 占满前排 4 列;后排三只各站一列 —— 贯穿从任意一只打上去都串到 Boss
            var grid = SpanGrid((EnemyRow.Front, 0, 4), (EnemyRow.Back, 0, 1),
                (EnemyRow.Back, 2, 1), (EnemyRow.Back, 3, 1));
            Assert.That(Targeting.ExpandTargets(grid, 1, TargetShape.Skewer, 0),
                Is.EquivalentTo(new[] { 0, 1 }), "后排第 0 列 + 跨列 Boss");
            Assert.That(Targeting.ExpandTargets(grid, 3, TargetShape.Skewer, 0),
                Is.EquivalentTo(new[] { 0, 3 }), "后排第 3 列同样串到 Boss");
        }

        [Test]
        public void Expand_Cleave_SpanIsAdjacentOnlyAtItsEdge()
        {
            // Boss 占前排 0..1,小怪站前排 2、3:只有第 2 列贴着 Boss 的右缘
            var grid = SpanGrid((EnemyRow.Front, 0, 2), (EnemyRow.Front, 2, 1),
                (EnemyRow.Front, 3, 1));
            Assert.That(Targeting.ExpandTargets(grid, 0, TargetShape.Cleave, 0),
                Is.EquivalentTo(new[] { 0, 1 }), "Boss 只溅到贴着它右缘的那只");
            Assert.That(Targeting.ExpandTargets(grid, 1, TargetShape.Cleave, 0),
                Is.EquivalentTo(new[] { 0, 1, 2 }), "夹在中间那只:左邻 Boss、右邻小怪");
        }

        [Test]
        public void Expand_Chain_SpanUsesCenterDistance()
        {
            // Boss 占前排 0..3(中心 1.5),后排第 0 列(中心 0)与第 2 列(中心 2)
            // 到 Boss 的中心距分别是 1.5 与 0.5 —— 第 2 列更近,先跳
            var grid = SpanGrid((EnemyRow.Front, 0, 4), (EnemyRow.Back, 0, 1),
                (EnemyRow.Back, 2, 1));
            var hit = Targeting.ExpandTargets(grid, 0, TargetShape.Chain, 2);
            Assert.That(hit[0], Is.EqualTo(0), "首项恒为主目标");
            Assert.That(hit[1], Is.EqualTo(2), "第 2 列离 Boss 中心更近");
        }

        [Test]
        public void Expand_Sweep_IgnoresSpanEntirely()
        {
            var grid = SpanGrid((EnemyRow.Front, 0, 2), (EnemyRow.Front, 2, 1),
                (EnemyRow.Back, 0, 1));
            Assert.That(Targeting.ExpandTargets(grid, 0, TargetShape.Sweep, 0),
                Is.EquivalentTo(new[] { 0, 1 }), "整排,与占几列无关;后排那只不中");
        }
```

- [ ] **Step 2: 跑测试确认它失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期：编译失败，`EnemyDef` 没有 `columnSpan` 具名参数（CS1739）。

- [ ] **Step 3: 加 `EnemyDef.ColumnSpan`**

在 `EnemyDef.cs` 的 `Row` / `Range` / `Focus` 三个属性**之后**加：

```csharp
        /// <summary>占几列(2026-08-30)。默认 1 = 占一格,与本字段引入之前逐字节等价。
        /// 三只 Boss 配 4(占满整排);将来想让护法站在 Boss 旁边就把 Boss 配成 2 或 3。
        ///
        /// <see cref="EnemyState.Column"/> 的语义因此收紧为「**起始列(左端)**」,
        /// 实际占据 [Column, Column + ColumnSpan)。Skewer/Cleave/Chain 三处裁定
        /// 全部按区间算(Targeting),Span = 1 时与旧写法逐字节相同。
        ///
        /// **不进快照**:由 Def 推得,与 <see cref="EnemyState.Defense"/> 做成计算属性
        /// 同一条理由(见那条注释里「零新增快照字段」的推理)。</summary>
        public int ColumnSpan { get; }
```

构造函数签名尾部追加参数并赋值（**追加在末尾**，不要插在中间——既有调用点全是位置参数）：

```csharp
        public EnemyDef(string id, Element element, int maxHp, int attack,
            EnemyAbility ability = EnemyAbility.None, IReadOnlyList<BossPhaseDef> phases = null,
            int defense = 0, int speed = 0,
            EnemyRow row = EnemyRow.Front, AttackRange range = AttackRange.Melee,
            AttackFocus focus = AttackFocus.Default, int columnSpan = 1)
        {
            // …既有赋值不动…
            ColumnSpan = columnSpan < 1 ? 1 : columnSpan;
        }
```

- [ ] **Step 4: 给 `EnemyState` 加两个转发属性**

在 `EnemyState.Column` 属性**之后**加。同时把 `Column` 的文档注释改成「起始列」：

```csharp
        /// <summary>占几列(2026-08-30),转发 <see cref="EnemyDef.ColumnSpan"/>。
        /// 不存快照 —— Def 里就有。</summary>
        public int ColumnSpan => Def.ColumnSpan;

        /// <summary>占位区间的**右开端**= Column + ColumnSpan。裁定一律用 [Column, ColumnEnd)。</summary>
        public int ColumnEnd => Column + Def.ColumnSpan;
```

- [ ] **Step 5: `Targeting.ExpandTargets` 三处改区间运算**

把 `Targeting.cs:210-220` 那个 `switch` 与 `GridDistance` 换成：

```csharp
                bool hit = shape switch
                {
                    TargetShape.Sweep => enemies[i].Row == primary.Row,
                    // 相邻 = 一方的右开端正好顶着另一方的起始列(2026-08-30 列区间)。
                    // Span 全 1 时 ⟺ |ΔColumn| == 1,与旧写法逐字节相同。
                    TargetShape.Cleave => enemies[i].Row == primary.Row
                        && (enemies[i].ColumnEnd == primary.Column
                            || primary.ColumnEnd == enemies[i].Column),
                    // 同列 = 两个列区间相交。Span 全 1 时 ⟺ Column 相等。
                    TargetShape.Skewer => enemies[i].Column < primary.ColumnEnd
                        && primary.Column < enemies[i].ColumnEnd,
                    _ => false,
                };
```

```csharp
        /// <summary>列中心的**两倍**(2026-08-30 列区间):Span=1 时恒为 2×Column。
        /// 用两倍是为了让跨偶数列的中心(如占 0..3 的 Boss 中心 1.5)仍是整数,不引浮点。</summary>
        private static int ColumnCenter2(EnemyState e) => 2 * e.Column + e.ColumnSpan - 1;

        /// <summary>弹射排序用的格子距离。**只被 ChainTargets 的比较器调用**(全仓库唯一调用点),
        /// 所以整体乘 2 是安全的:单调线性变换不改排序,Span 全 1 时排序结果与旧式子逐位相同。
        /// 列用中心距(半列为单位),排差记 2(= 旧式子的 1 乘 2)。</summary>
        private static int GridDistance(EnemyState a, EnemyState b) =>
            System.Math.Abs(ColumnCenter2(a) - ColumnCenter2(b)) + (a.Row == b.Row ? 0 : 2);
```

- [ ] **Step 6: 三个构造调用点透传 `ColumnSpan`**

`Campaign.cs` 的 `Scale`（**漏传会静默丢字段**——`EnemyDef.Speed` 的注释记着同型的坑）：

```csharp
            return new EnemyDef(enemy.Id, enemy.Element,
                Scaled(enemy.MaxHp, scale), Scaled(enemy.Attack, scale),
                enemy.Ability, phases, ScaledDefense(enemy.Defense, scale), enemy.Speed,
                enemy.Row, enemy.Range, enemy.Focus, enemy.ColumnSpan);
```

`Endless.cs:126` 的 `BuildIdiomBoss`（成语 Boss 也占满整排）：

```csharp
            return new EnemyDef(idiom.Chars, idiom.Elements[0], 180, 60, EnemyAbility.None, phases,
                columnSpan: Targeting.RowCapacity);
```

`ConfigLoader.cs`：`EnemyDto` 加字段，构造调用透传。

```csharp
            public int ColumnSpan { get; set; }  // 占几列(2026-08-30);缺省 0 → EnemyDef 钳到 1
```

```csharp
                enemyDefs[dto.Id] = new EnemyDef(dto.Id, element, dto.MaxHp, dto.Attack, ability, phases,
                    dto.Defense, speed: 0, row: row, range: range, focus: focus,
                    columnSpan: dto.ColumnSpan);
```

- [ ] **Step 7: 三只 Boss 配 `columnSpan: 4`**

在 `Brushblade/Assets/StreamingAssets/config/enemies.json` 的 `enemies` 数组里，给 `排山倒海` / `翻江倒海` / `雷霆万钧` 三条各加一个字段（与既有的 `defense` 同级）：

```json
      "columnSpan": 4,
```

- [ ] **Step 8: 跑测试确认全绿**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期：新增 4 条通过；**既有测试一条不许变红**——这一步就是恒等性的验收点。若有既有测试变红，说明区间式子写错了，回到 Step 5，不要改测试。

- [ ] **Step 9: 提交**

```bash
git add Brushblade/Assets/_Project/Core Brushblade/Assets/_Project/Data Brushblade/Assets/_Project/Tests Brushblade/Assets/StreamingAssets/config/enemies.json
git commit -m "feat(core): 敌人占列改成区间 —— Boss 跨四列,贯穿横扫按区间裁定

EnemyDef 加 ColumnSpan(默认 1),Column 语义收紧为起始列。
Skewer 改区间相交、Cleave 改区间相邻、Chain 距离改半列中心距 ——
Span 全 1 时与旧写法逐字节等价,既有测试一条未动。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: Core — 站位居中往外、按宽度打包

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`（`AssignSlots` 约 `:2598`、`FreeColumnIn` 约 `:2877`）
- Modify: `Brushblade/Assets/_Project/Core/Targeting.cs`（删 `RowCells`）
- Modify: `Brushblade/Assets/_Project/Tests/TargetingTests.cs`（删 `RowCells` 那 4 条）
- Test: `Brushblade/Assets/_Project/Tests/EnemySlotTests.cs`（新建）

**Interfaces:**
- Consumes: Task 1 的 `EnemyState.ColumnSpan` / `ColumnEnd`。
- Produces: `Targeting.ColumnOrder` (`IReadOnlyList<int>`，值为 `{1, 2, 0, 3}`)——Task 6 的表现层不需要它（它按 `Column` 落格），但测试与将来的布局策略读它。

- [ ] **Step 1: 写失败测试**

新建 `Brushblade/Assets/_Project/Tests/EnemySlotTests.cs`：

```csharp
using System.Collections.Generic;
using Brushblade.Core;
using NUnit.Framework;

namespace Brushblade.Core.Tests
{
    /// <summary>敌人开场站位(2026-08-30):每排恒定 4 格,列号居中往外,跨列的按宽度打包。</summary>
    public class EnemySlotTests
    {
        private static EnemyDef Mob(string id, EnemyRow row = EnemyRow.Front, int span = 1) =>
            new(id, Element.Heart, 100, 10, row: row, columnSpan: span);

        private static BattleEngine Engine(params EnemyDef[] enemies) =>
            TestEngine.Build(enemies);

        [Test]
        public void ColumnOrder_IsCenterOutward()
        {
            Assert.That(Targeting.ColumnOrder, Is.EqualTo(new[] { 1, 2, 0, 3 }),
                "4 列没有正中,取中间偏左先");
        }

        [Test]
        public void AssignSlots_SingleEnemy_TakesTheCenterColumn()
        {
            var engine = Engine(Mob("甲"));
            Assert.That(engine.Enemies[0].Column, Is.EqualTo(1),
                "单怪落中间偏左,不再靠最左 —— 这一条取代了旧的 RowCells 折叠特例");
        }

        [Test]
        public void AssignSlots_TwoEnemies_AreAdjacentAtCenter()
        {
            var engine = Engine(Mob("甲"), Mob("乙"));
            Assert.That(engine.Enemies[0].Column, Is.EqualTo(1));
            Assert.That(engine.Enemies[1].Column, Is.EqualTo(2));
        }

        [Test]
        public void AssignSlots_BossSpansWholeRow_MinionsGoToTheOtherRow()
        {
            var engine = Engine(Mob("霸", span: Targeting.RowCapacity), Mob("卒"));
            Assert.That(engine.Enemies[0].Row, Is.EqualTo(EnemyRow.Front));
            Assert.That(engine.Enemies[0].Column, Is.EqualTo(0), "占满整排,起始列只能是 0");
            Assert.That(engine.Enemies[1].Row, Is.EqualTo(EnemyRow.Back),
                "前排被 Boss 占满,小怪改判到后排");
        }

        [Test]
        public void AssignSlots_PacksByWidthNotByCount()
        {
            // 前排:占 2 列的 + 两只占 1 列的 = 宽 4,正好铺满;第四只放不下,改判后排
            var engine = Engine(Mob("甲", span: 2), Mob("乙"), Mob("丙"), Mob("丁"));
            int frontWidth = 0;
            foreach (var e in engine.Enemies)
                if (e.Row == EnemyRow.Front) frontWidth += e.ColumnSpan;
            Assert.That(frontWidth, Is.LessThanOrEqualTo(Targeting.RowCapacity),
                "一排的总宽不许超过列数");
            Assert.That(engine.Enemies[3].Row, Is.EqualTo(EnemyRow.Back));
        }

        [Test]
        public void AssignSlots_NoTwoEnemiesOverlapInAColumn()
        {
            var engine = Engine(Mob("甲", span: 2), Mob("乙"), Mob("丙"),
                Mob("丁", EnemyRow.Back), Mob("戊", EnemyRow.Back));
            var seen = new HashSet<(EnemyRow, int)>();
            foreach (var e in engine.Enemies)
                for (int c = e.Column; c < e.ColumnEnd; c++)
                    Assert.That(seen.Add((e.Row, c)), Is.True, $"列 {c} 被占了两次");
        }
    }
}
```

⚠ `TestEngine.Build` 是既有测试里的辅助。**开工前先 grep 确认它的真实名字与签名**：

```bash
grep -rn "static BattleEngine" Brushblade/Assets/_Project/Tests/ | head
```

若不存在同名辅助，就照同目录里既有的 `BattleEngine` 构造用法（例如 `EnemyRowTests` / `TargetingTests` 邻近文件）在本文件内写一个私有 `Engine(params EnemyDef[])`，**不要**新建公共辅助类。

- [ ] **Step 2: 跑测试确认它失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter EnemySlotTests
```

预期：编译失败，`Targeting.ColumnOrder` 不存在。

- [ ] **Step 3: 加 `Targeting.ColumnOrder`，删 `RowCells`**

在 `Targeting.cs` 的 `RowCapacity` 常量之后加，并**整段删除** `RowCells` 及其文档注释：

```csharp
        /// <summary>列号的分配顺序(2026-08-30):**居中往外**。4 列没有正中,取中间偏左先。
        ///
        /// 取代了旧的「两排都 ≤1 只就把该排折叠成一格」特例(RowCells,已删)——
        /// 那条特例当年是为了修「单怪铺三格会被顶到最左」(2026-08-23 实机反馈),
        /// 而居中往外之后单怪自然落在列 1,不折叠也不靠边,稿上的「每排恒定 4 格」得以成立。
        ///
        /// ⚠ 这是**唯一**一条布局策略。将来若要加别的(例如辅助怪躲进没有前排的后排列
        /// 以避开贯穿),判据加在 <c>BattleEngine.AssignSlots</c> 那一处,不要在这里架一层抽象。</summary>
        public static readonly IReadOnlyList<int> ColumnOrder = new[] { 1, 2, 0, 3 };
```

⚠ `ColumnOrder` 的长度必须等于 `RowCapacity`。加一条守卫测试进 `TargetingTests`：

```csharp
        [Test]
        public void ColumnOrder_CoversEveryColumnExactlyOnce()
        {
            var seen = new HashSet<int>(Targeting.ColumnOrder);
            Assert.That(Targeting.ColumnOrder.Count, Is.EqualTo(Targeting.RowCapacity));
            Assert.That(seen.Count, Is.EqualTo(Targeting.RowCapacity), "不许重号");
            foreach (int c in Targeting.ColumnOrder)
                Assert.That(c >= 0 && c < Targeting.RowCapacity, Is.True, $"列 {c} 越界");
        }
```

- [ ] **Step 4: 删 `TargetingTests` 里 `RowCells` 的 4 条**

删掉 `RowCells_SingleEnemyEncounter_CollapsesToOneCell`、`RowCells_KeepsFullGridWhenTheOtherRowHasTwoOrMore`、`RowCells_MultiEnemyRow_AlwaysFillsTheGrid` 三个 `[Test]` 方法（共 4 处断言块，约 `:404-430`）。

这是**刻意破例**：项目的恒等性硬线是「Tests 纯追加」，但折叠特例正是本次要废止的行为，留着测试等于锁住一个已不存在的规则。破例只此一处，其余既有测试一条不动。

- [ ] **Step 5: 改写 `AssignSlots`**

替换 `BattleEngine.cs` 的 `AssignSlots` 整个方法（连同文档注释——原注释写的「每排上限 3」早已过期）：

```csharp
        /// <summary>按每排容量给场上敌人定实际站位与列(2026-08-20 排,2026-08-22 列,
        /// 2026-08-30 列区间 + 居中往外)。
        ///
        /// 按 _enemies 顺序依次分:先试偏好排,该排**剩余宽度**不够就改判到另一排。
        /// 判据是宽度不是只数 —— Boss 一只就占满 4 列,按只数算会让小怪叠在它身上。
        ///
        /// 列号走 <see cref="Targeting.ColumnOrder"/> 的居中往外序,取第一个能放下
        /// 连续 <c>ColumnSpan</c> 列的起点。占满整排的(Span = RowCapacity)只有起点 0 放得下。
        ///
        /// ⚠ **这里是布局策略的唯一落点。** 将来要加「辅助怪躲进没有前排的后排列」这类判据,
        /// 加在本方法里,不要架一层策略接口(用户 2026-08-30 拍板:单一实现不做抽象)。</summary>
        private void AssignSlots()
        {
            foreach (var enemy in _enemies)
            {
                var preferred = enemy.Def.Row;
                var other = preferred == EnemyRow.Front ? EnemyRow.Back : EnemyRow.Front;
                int column = FreeColumnIn(preferred, enemy.Def.ColumnSpan);
                var row = preferred;
                if (column < 0)
                {
                    row = other;
                    column = FreeColumnIn(other, enemy.Def.ColumnSpan);
                }
                // 两排都放不下:EnemyCap 8 = 4 + 4 且全员 Span 1 时走不到,
                // 但跨列的怪会让「只数没超上限、宽度却超了」成为可能。落到 −1 就把它
                // 挤在最后一排的 0 列 —— 画面会叠,但下标与列号仍然自洽,不至于崩。
                enemy.Row = row;
                enemy.Column = column < 0 ? 0 : column;
            }
        }
```

- [ ] **Step 6: 改写 `FreeColumnIn` 成「找连续空段」**

替换 `BattleEngine.cs` 的 `FreeColumnIn`（原签名 `(EnemyRow row)`，新签名多一个 `span`）：

```csharp
        /// <summary>该排里能放下连续 <paramref name="span"/> 列的起始列号,按
        /// <see cref="Targeting.ColumnOrder"/> 的居中往外序取第一个;放不下返回 −1。
        ///
        /// 开场分配与叠字怪分裂共用这一处 —— 两边对「哪里算空」必须是同一个判据,
        /// 分头写就有走岔的余地。已阵亡的怪**照样占位**(引擎从不移除阵亡敌人),
        /// 与表现层「尸体占格」一致。</summary>
        private int FreeColumnIn(EnemyRow row, int span)
        {
            foreach (int start in Targeting.ColumnOrder)
            {
                if (start + span > EnemyRowCap) continue;
                bool free = true;
                foreach (var e in _enemies)
                {
                    if (e.Row != row) continue;
                    // 区间相交 = 放不下
                    if (e.Column < start + span && start < e.ColumnEnd) { free = false; break; }
                }
                if (free) return start;
            }
            return -1;
        }
```

⚠ `AssignSlots` 在循环里调它，而 `enemy.Row` / `Column` 是**边分配边写**的——尚未分配的敌人 `Column` 是默认 0、`Row` 是默认 `Front`，会被误判成「已占前排 0 列」。**修法**：在 `AssignSlots` 开头先把全体标记为未分配。加一个私有哨兵：

```csharp
        // 未分配哨兵(2026-08-30):AssignSlots 边分配边查占位,尚未轮到的敌人不能被算成
        // 「已占默认的 Front/0」。用 −1 而不是布尔标志:Column 本来就是 int,
        // 而 FreeColumnIn 的区间相交式子对 −1 起点天然不成立(ColumnEnd = -1 + span ≤ 0 ≤ start)。
        private const int UnassignedColumn = -1;
```

`AssignSlots` 第一行加：

```csharp
            foreach (var enemy in _enemies) enemy.Column = UnassignedColumn;
```

并在 `FreeColumnIn` 的内层循环开头补一句跳过：

```csharp
                    if (e.Column == UnassignedColumn) continue;
```

- [ ] **Step 7: 改分裂那处的调用**

`BattleEngine.cs:2841` 改成传母体的 span（克隆继承母体的 Def，所以 span 相同）：

```csharp
                int cloneColumn = FreeColumnIn(cloneRow, enemy.Def.ColumnSpan);
```

同时把 `RowWithSpace` 的按只数判断改成按宽度——它现在数的是只数：

```csharp
        /// <summary>优先返回 preferred 排(**剩余宽度**放得下 span 时),否则另一排。
        /// 2026-08-30:从数只数改成数宽度 —— 跨列的怪让「只数没满、宽度已满」成为可能。</summary>
        private EnemyRow RowWithSpace(EnemyRow preferred, int span)
        {
            int width = 0;
            foreach (var e in _enemies)
                if (e.Row == preferred && e.Column != UnassignedColumn) width += e.ColumnSpan;
            return width + span <= EnemyRowCap
                ? preferred
                : (preferred == EnemyRow.Front ? EnemyRow.Back : EnemyRow.Front);
        }
```

调用点 `var cloneRow = RowWithSpace(enemy.Row);` 改成 `RowWithSpace(enemy.Row, enemy.Def.ColumnSpan);`。

- [ ] **Step 8: 表现层改用 `RowCapacity`**

`BattleView.cs:1720-1721` 的两行（`RowCells` 已删）：

```csharp
            var frontCells = new GameObject[Targeting.RowCapacity];
            var backCells = new GameObject[Targeting.RowCapacity];
```

并把 `:1713-1718` 那段数只数的代码（`frontCount` / `backCount`）连同 `:1700` 引用 `RowCells` 的文档注释一并删除——不再有人用它们。

- [ ] **Step 9: 跑 Core 测试 + 离线编译**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

预期：两条都全绿。⚠ 既有测试里凡是断言「敌人 Column == 0」的会因居中往外而变红——**这类是预期变化**，逐条改成新列号并在断言消息里注明原因；凡是断言目标形状**结果**的不该变红，若变红说明分配逻辑写错了。

- [ ] **Step 10: 提交**

```bash
git add Brushblade/Assets/_Project/Core Brushblade/Assets/_Project/Tests Brushblade/Assets/_Project/Presentation/BattleView.cs
git commit -m "feat(core): 每排恒定四格,列号居中往外 —— 单怪不再顶到最左

RowCells 的折叠特例删掉(稿要求每排恒定 4 格),改由 ColumnOrder
{1,2,0,3} 让单怪自然落在中间。AssignSlots/FreeColumnIn/RowWithSpace
三处从数只数改成数宽度,跨列的 Boss 才不会被小怪叠上去。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: Core — 敌人护盾

机制就位，**来源留白**：不改 `enemies.json` 的护盾、不做结盾技能。场上敌人的 `Shield` 恒为 0，等将来的加盾辅助怪接上。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EnemyDef.cs`（`EnemyState.Shield`、`ToSnapshot` / `Restore`）
- Modify: `Brushblade/Assets/_Project/Core/RunSnapshot.cs`（敌人快照加 `Shield`）
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`（`DamageEnemy` 约 `:2780-2786`）
- Test: `Brushblade/Assets/_Project/Tests/EnemyShieldTests.cs`（新建）

**Interfaces:**
- Produces: `EnemyState.Shield` (int, `internal set`)、`BattleEvent.Absorbed` 在 `Damage` 事件上的新含义（被敌人护盾吃掉的部分）。Task 6 的盾条读 `Shield`，飘字读 `Absorbed`。

⚠ **不新增 `BattleEventKind`。** `BattleEvent.Absorbed` 字段已经存在（`EnemyAttack` 用它表达玩家护盾吸收量），敌人侧复用同一个字段是对称的。既有的 `ShieldBroken` 是「倾覆清空**玩家**护盾」，`TargetIndex = −1`，语义不同，不要挪用。

- [ ] **Step 1: 写失败测试**

新建 `Brushblade/Assets/_Project/Tests/EnemyShieldTests.cs`。**先 grep 出邻近测试里造引擎并打一记伤害的写法**（`grep -rn "DamageEnemy\|Cast(" Brushblade/Assets/_Project/Tests/ | head`），照同一套写；下面的断言是必须成立的行为：

```csharp
using Brushblade.Core;
using NUnit.Framework;

namespace Brushblade.Core.Tests
{
    /// <summary>敌人护盾(2026-08-30):护甲减法之后、扣血之前吸收。
    ///
    /// ⚠ **来源留白**(用户 2026-08-30 拍板):enemies.json 没有护盾字段,也没有结盾技能——
    /// 盾的来源将来是「加盾辅助怪给同伴挂 buff」,那类小怪还没设计。所以真机上
    /// 场上敌人的 Shield 恒为 0,**这些用例是唯一的验证手段**,别因为「看不见」就当没做。</summary>
    public class EnemyShieldTests
    {
        [Test]
        public void Shield_AbsorbsBeforeHp()
        {
            // 盾 30、血 100,挨一记 50:盾清零、血掉 20
        }

        [Test]
        public void Shield_AbsorbsAfterDefenseSubtraction()
        {
            // 护甲 10、盾 30、血 100,挨一记 50:先减甲成 40,再由盾吃 30,血掉 10
            // —— 顺序反过来(先吃盾再减甲)会让盾+甲的组合凭空多挡 10 点
        }

        [Test]
        public void Shield_CounterAttackStillAbsorbed()
        {
            // 相克那一记绕过护甲,但盾照常吸收(用户 2026-08-30 拍板:护甲是硬度,护盾是一层临时血)
        }

        [Test]
        public void Shield_DamageEventCarriesAbsorbedAmount()
        {
            // Damage 事件的 Absorbed = 被盾吃掉的量,Amount = 打出去的总伤(与玩家侧 EnemyAttack 同口径)
        }

        [Test]
        public void Shield_SurvivesSnapshotRoundTrip()
        {
            // 存档往返后 Shield 不丢 —— 漏补快照字段是静默的(RunSnapshot.cs:9 那条警告)
        }
    }
}
```

⚠ 上面五个方法体是**待填的断言说明，不是可提交的代码**。实现者必须把每一条写成真断言（照邻近测试文件的造引擎写法），跑到红了再往下走。

- [ ] **Step 2: 跑测试确认它失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter EnemyShieldTests
```

- [ ] **Step 3: 加 `EnemyState.Shield`**

在 `EnemyState.Statuses` 之后加（与 `SummonState.Shield` 的注释同型）：

```csharp
        /// <summary>敌人护盾(2026-08-30):一次性额外血条,在护甲减法**之后**、扣血**之前**吸收;
        /// 吸完即无、不刷新、不随回合清空。与 <see cref="SummonState.Shield"/> 同型。
        ///
        /// ⚠ **眼下没有来源**(用户 2026-08-30 拍板):enemies.json 不配、也没有结盾技能。
        /// 将来的来源是「加盾辅助怪给同伴挂 buff」。所以真机上它恒为 0,
        /// 吸收逻辑只有 EnemyShieldTests 看得见 —— 改这一块别指望试玩能发现问题。
        ///
        /// **进快照**:与 <see cref="Hp"/> 同类,是战中可变状态。这一条与
        /// <see cref="Defense"/> 那条「零新增快照字段」的推理不冲突 ——
        /// 那条说的是**不可变**的基础属性做成计算属性,而护盾是会被打掉的。</summary>
        public int Shield { get; internal set; }
```

`ToSnapshot()`（约 `:385`）与 `Restore`（约 `:410`）各加一行 `Shield = Shield,` / `Shield = snapshot.Shield,`；`RunSnapshot.cs` 的敌人快照类（`Column` 那条旁边，约 `:65`）加：

```csharp
        public int Shield { get; set; }  // 敌人护盾(2026-08-30)
```

- [ ] **Step 4: 在 `DamageEnemy` 插吸收段**

`BattleEngine.cs` 把这两行

```csharp
            enemy.Hp = Math.Max(0, enemy.Hp - damage);
            _events.Add(new BattleEvent(BattleEventKind.Damage, enemyIndex, damage, crit: crit,
                ke: counters, attacker: attacker));
```

换成

```csharp
            // 护盾吸收(2026-08-30):护甲减法之后、扣血之前。
            // **相克不穿盾**(用户拍板):相克已经在上面绕过了护甲(硬度),
            // 盾是一层临时血,照常要打空 —— 连盾一起穿会让护盾对带对属性的玩家形同虚设。
            int absorbed = Math.Min(enemy.Shield, damage);
            enemy.Shield -= absorbed;
            enemy.Hp = Math.Max(0, enemy.Hp - (damage - absorbed));
            // Absorbed 复用玩家侧 EnemyAttack 那个字段的口径:Amount = 打出去的总伤,
            // Absorbed = 其中被盾吃掉的部分,两者相减 = 实际掉血。
            // 刻意不新增 BattleEventKind —— 既有的 ShieldBroken 是「倾覆清空玩家护盾」
            // (TargetIndex = −1),语义不同,挪用会让表现层分不清是谁的盾没了。
            _events.Add(new BattleEvent(BattleEventKind.Damage, enemyIndex, damage,
                absorbed: absorbed, crit: crit, ke: counters, attacker: attacker));
```

- [ ] **Step 5: 跑测试确认全绿**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期：新增 5 条通过；既有测试一条不许变红（全场 `Shield` 为 0 时 `absorbed` 恒为 0，`enemy.Hp -= damage - 0` 与旧式子逐字节相同）。

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Core Brushblade/Assets/_Project/Tests
git commit -m "feat(core): 敌人护盾就位 —— 机制做全,来源留白

护甲减法之后、扣血之前吸收;相克绕护甲但不穿盾。Damage 事件复用
Absorbed 字段带出吸收量,不新增 BattleEventKind。
enemies.json 不配盾也不做结盾技能 —— 来源等将来的加盾辅助怪,
所以真机上恒为 0,验证全靠 EnemyShieldTests。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: 图标 18 → 30

11 枚**逐字抄稿**（`Battle.dc.html` 的 `icdefs`）+ 第 30 枚 `heal` 新画并回填进稿。

**Files:**
- Modify: `tools/icons/build_icons.py`
- Modify: `tools/icons/tests/test_icons.py`
- Modify: `Brushblade/Assets/_Project/Presentation/UI/Icons.cs`
- Modify: `docs/design/ui/scenes/StatusGlossary.dc.html`（回填 `heal`）
- Modify: `docs/design/ui/scenes/Battle.dc.html`（`icdefs` 补 `heal`，保持两处 SVG 同源）

**Interfaces:**
- Produces: 图标 key `armorbreak` `bleed` `scorch` `sear` `split` `obscure` `focus` `ranged` `sweep` `skewer` `thorns` `heal`。Task 6/7 的 chip 用它们。

- [ ] **Step 1: 先把 `test_icons.py` 的 `EXPECTED` 改成 34 个（写失败测试）**

在 `EXPECTED` 集合里补：

```python
    # 战斗稿 Battle.dc.html 的 icdefs 补齐到 29 枚(2026-08-30)。前 18 枚已有,这 11 枚新增:
    # 状态类 —— 破甲 / 流血
    "armorbreak", "bleed",
    # 敌人天生能力 —— 焦痕 / 灯花 / 叠字分裂 / 生僻(属性隐藏) / 反伤
    "scorch", "sear", "split", "obscure", "thorns",
    # 攻击模式 —— 远程 / 死盯玩家 / 横扫 / 贯穿
    "ranged", "focus", "sweep", "skewer",
    # 第 30 枚(2026-08-30):持续治疗。StatusGlossary.dc.html 自己记着「29 枚里缺这一枚,
    # 落地要补第 30 枚」—— 稿上没有它的 SVG,这一枚是**新画的**,已回填进稿。
    "heal",
```

- [ ] **Step 2: 跑测试确认它失败**

```bash
python3 -m pytest tools/icons/tests/ -q
```

预期：`test_icon_set_matches_spec` 与 `test_csharp_fallback_covers_every_icon` 双红。

- [ ] **Step 3: 把 11 枚 SVG 逐字抄进 `build_icons.py`**

⚠ **逐字抄稿，不要手改坐标**——与 `nav_*` 同一条戒律（`build_icons.py` 里那段注释）。源在 `docs/design/ui/scenes/Battle.dc.html` 的 `<svg class="icdefs">` 里，每枚是一个 `<symbol id="ic-KEY">`；把 symbol 内部的 `<path>` / `<circle>` 原样搬过来，`fill="#fff"` 换成 `{FILL}`、成套的 `fill="none" stroke="#fff" stroke-width="6" stroke-linecap="round" stroke-linejoin="round"` 换成 `{STROKE}`，**其余属性与全部坐标一个字不改**。stroke-width 不是 6 的（如 `reflect` 里的 5）保留字面值，不要塞进 `{STROKE}`。

抄完在 `ICONS` 里的位置：接在既有 `"speed"` / `"shield"` 之后、`nav_*` 之前，按 `EXPECTED` 里的分组顺序排并带上同样的中文分组注释。

- [ ] **Step 4: 新画第 30 枚 `heal`**

稿上没有，这一枚要自己画。约定同其余：64×64、内容留 8px 边距、纯白、只用 path/circle。十字 + 上升箭头（与 `morale` 的旗、`defense` 的实心盾都不撞形）：

```python
    # 持续治疗(2026-08-30 新画,第 30 枚):十字 + 向上的小箭头 = 每回合回一点。
    # StatusGlossary.dc.html 记着「29 枚里缺这一枚」,本枚已回填进那一页与 Battle.dc.html 的 icdefs。
    # 与 defense(实心盾)、immunity(盾+对勾)刻意都不像 —— 那两个是「挡」,这个是「回」。
    "heal": (
        f'<path {FILL} d="M26 12h12v14h14v12H38v14H26V38H12V26h14z"/>'
    ),
```

- [ ] **Step 5: `Icons.Glyphs` 补 12 个兜底汉字**

在 `Icons.cs` 的 `Glyphs` 字典里，`shield` 之后、`nav_*` 之前插入。⚠ 这些汉字**会进字体子集**（`subset_fonts.py` 扫 `.cs` 字符串字面量），Step 8 必须重跑：

```csharp
            // 战斗稿补齐的 11 枚(2026-08-30)
            { "armorbreak", "破" },
            { "bleed", "血" },
            { "scorch", "烫" },
            { "sear", "燎" },
            { "split", "裂" },
            { "obscure", "隐" },
            { "thorns", "刺" },
            { "ranged", "远" },
            { "focus", "盯" },
            { "sweep", "扫" },
            { "skewer", "贯" },
            // 第 30 枚(2026-08-30):持续治疗
            { "heal", "愈" },
```

- [ ] **Step 6: 出图**

```bash
python3 tools/icons/build_icons.py
```

预期打印 `34 个图标 → …/Presentation/UI/Resources`。

⚠ 若打印「跳过 PNG:未找到 rsvg-convert」，先 `brew install librsvg` 再重跑——只写了 SVG 没出 PNG 的话，Unity 加载不到，会静默回落成兜底汉字。

- [ ] **Step 7: 跑图标测试**

```bash
python3 -m pytest tools/icons/tests/ -q
```

预期全绿（含 `test_repo_svgs_match_icons` 与 `test_csharp_fallback_covers_every_icon`）。

- [ ] **Step 8: 重跑字体子集并验证**

```bash
python3 tools/fonts/subset_fonts.py
```

```bash
python3 -m pytest tools/fonts/tests/ -q
```

⚠ **光看 diff 大小说明不了变没变，要比 cmap。** 12 个兜底汉字里「破血烫燎裂隐刺远盯扫贯愈」若已在子集里则 cmap 不变，那是正常的；关键是 `pytest tools/fonts/tests/` 必须绿。

- [ ] **Step 9: 回填两张稿**

`StatusGlossary.dc.html`：把「三处『有状态、没图标』」那一节里 `持续治疗` 那条的 `<em>` 从「**没有图标**——29 枚里缺这一枚，落地要补第 30 枚」改成「第 30 枚 `heal`，2026-08-30 已补」，并把它从「gap」小节挪进正表、配上 `ic-heal` 的 chip。
`Battle.dc.html` 的 `icdefs` 补一个 `<symbol id="ic-heal">`，内容与 `build_icons.py` 的 `heal` 逐字相同——两处同源是 `nav_*` 那条戒律的延伸。

- [ ] **Step 10: 提交**

```bash
git add tools/icons Brushblade/Assets/_Project/Presentation/UI/Icons.cs Brushblade/Assets/_Project/Presentation/UI/Resources docs/design/ui/scenes tools/fonts
git commit -m "feat(ui): 状态图标补到 30 枚 —— 11 枚抄稿,持续治疗新画一枚

破甲/流血/焦痕/灯花/分裂/生僻/反伤/远程/死盯/横扫/贯穿 逐字抄自
Battle.dc.html 的 icdefs;第 30 枚 heal 稿上本来就缺(StatusGlossary
自己记着),新画并回填进两张稿。兜底汉字同步,字体子集已重跑。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: 表现层 — 三栏骨架换布局组

把 `BuildSkeleton` 从比例锚点改成布局组，直译稿的 flex。**这一步只搬骨架，各区的绘制方法一行不改**——它们只是换了个父节点。

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`（`BuildSkeleton` 约 `:628-800`、`MakeSection` 约 `:794`）
- Modify: `Brushblade/Assets/_Project/Presentation/UI/Ui.cs`（补一个 `LayoutElement` 便捷方法，若既有的不够用）

**Interfaces:**
- Consumes: 无。
- Produces: 字段 `_rail` / `_mid` / `_bench`（`Transform`）与既有的 `_enemyBackRow` / `_enemyFrontRow` / `_summonFrontRow` / `_summonBackRow` / `_bottomRow` / `_libraryRow` / `_poolRow` / `_suggestRow` / `_actionRow` / `_endTurnRow` / `_statusRow` / `_centerRow` / `_hintColumn`。**字段名一个不改**——Task 6-8 靠它们定位，改名会让那三个任务全部对不上。

- [ ] **Step 1: 记下稿上的尺寸换算**

在 `BattleView` 顶部新增一段常量（照 `MapView` 顶部那批的先例，pt 值写在注释里）：

```csharp
        // ===== 稿上的尺寸(Battle.dc.html)。1pt = 2.093 逻辑单位,基准 932×430pt → 1950×900。 =====
        // 改这些数就是改版面 —— 改完要同步改稿,别让两边漂开。
        private const float RailW = 142f;      // 稿 68pt:相克环图 + 配字表
        private const float BenchW = 276f;     // 稿 132pt:拆合台
        private const float ArenaGap = 13f;    // 稿 6pt:三栏之间
        private const float TopBarH = 46f;     // 稿 22pt
        private const float PlayerBarH = 84f;  // 稿 40pt
        private const float RowGap = 17f;      // 稿 8pt:同排格与格之间
```

- [ ] **Step 2: 重写 `BuildSkeleton` 的版面部分**

保留原方法开头的 `Backdrop`（空白点击取消选中）不动，把顶栏之后的全部锚点代码换成一棵布局树。骨架形状：

```
root (VerticalLayoutGroup)
├─ TopBar        (LayoutElement.preferredHeight = TopBarH,     flexibleHeight = 0)
└─ Arena         (HorizontalLayoutGroup, spacing = ArenaGap,   flexibleHeight = 1)
   ├─ Rail       (VerticalLayoutGroup,   preferredWidth = RailW,  flexibleWidth = 0)
   │  ├─ KeChart
   │  └─ HintPanel                     → _hintColumn
   ├─ Mid        (VerticalLayoutGroup,   flexibleWidth = 1)
   │  ├─ Field   (VerticalLayoutGroup,   flexibleHeight = 1)
   │  │  ├─ EnemiesBack   → _enemyBackRow    (flexibleHeight = 0)
   │  │  ├─ EnemiesFront  → _enemyFrontRow   (flexibleHeight = 0)
   │  │  ├─ RowDivider    → _rowDivider      (flexibleHeight = 0；上下各一个 flexibleHeight = 1 的空 Spacer)
   │  │  ├─ SummonsFront  → _summonFrontRow  (flexibleHeight = 0)
   │  │  └─ SummonsBack   → _summonBackRow   (flexibleHeight = 0)
   │  ├─ PlayerStats → _bottomRow  (preferredHeight = PlayerBarH, flexibleHeight = 0)
   │  ├─ Library     → _libraryRow (flexibleHeight = 0)
   │  ├─ Pool        → _poolRow    (flexibleHeight = 0)
   │  ├─ Center      → _centerRow  (flexibleHeight = 0)
   │  └─ Status      → _statusRow  (flexibleHeight = 0)
   └─ Bench      (VerticalLayoutGroup,   preferredWidth = BenchW, flexibleWidth = 0)
      ├─ 标题 + Content → _suggestRow
      ├─ Actions        → _actionRow
      └─ EndTurn        → _endTurnRow
```

关键三条，照稿的注释落地：

```csharp
            // 稿:.erow { flex: none } —— 四排各自锁死高度,宁可 field 留白也不互相挤。
            // 默认 flexShrink 会把后排压扁,看上去就成了「后排叠在前排上」。
            rowElement.flexibleHeight = 0;
```

```csharp
            // 稿:.divider { margin: auto 0 } —— field 剩下的纵向全部堆到这一道的上下,
            // 两侧各留一半。用两个 flexibleHeight = 1 的空 Spacer 夹住分隔线来表达。
            // 富余越多,敌我离得越开。
```

```csharp
            // 中区被左右两栏夹到 1260 逻辑单位(稿 602pt)。满员 12 张手牌
            // 12×96 + 11×10 = 1262 会**刚好溢出 2 单位** —— HorizontalLayoutGroup 会等比
            // 压窄每格(既有行为,与旧版面同),压扁而不是溢出。别为这 2 单位改牌宽:
            // 稿上的 46×56pt 是量出来的,改它会让整屏比例跟稿漂开。
```

⚠ `_rowDivider` 现在是 `Ui.Panel` + 绝对锚点。改成布局树里的一行后，`raycastTarget = false` 必须保留（它只是一条线，不能拦掉空白点击）。

- [ ] **Step 3: 删掉过期的纵向预算注释**

`BuildSkeleton` 顶上那段「纵向预算(2026-08-21 竖排复原)」共约 40 行——从 `// ================= 纵向预算` 到 `// 合计 569.7px = 0.305–0.938 ✓` 整段删除，连同各 `MakeSection` 调用旁的 `// 153.0px` 之类的行内注释。

那串手算加法描述的是一个**已不存在**的布局（比例锚点 + 竖排格），留着比没有更糟——下一个读它的人会照着它算，然后发现对不上。`MakeSection` 若已无调用点，一并删除。

- [ ] **Step 4: 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

预期：无 `error CS`。

- [ ] **Step 5: 人工试玩验收**

⚠ Presentation 没有自动化测试，这一步**只能人工**。在 Unity 里进一场战斗，逐条确认：

1. 三栏都在，左栏 142 宽、右栏 276 宽，中区吃掉剩下的。
2. 四排敌我不互相压扁；敌我之间的留白随场上单位数变化而增减。
3. 手牌满员时是**压窄**不是溢出到左右两栏底下。
4. 空白处点击仍然取消选中（`Backdrop` 没被布局组顶掉）。
5. 分隔线只在战斗阶段出现，且点它不会取消选中。

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation
git commit -m "refactor(ui): 战斗屏骨架换布局组 —— 稿的 flex 直译,手算纵向预算作废

BuildSkeleton 从比例锚点改成 Layout Group + LayoutElement:三栏
(rail 142 / mid 弹性 / bench 276)、四排 flexibleHeight 0 各自锁高、
分隔线靠两个弹性 Spacer 把富余对半堆到敌我之间。
顶上那串 40 行的手算加法删除 —— 它描述的布局已不存在。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: 表现层 — 敌人格横排 + 护盾条 + Boss 跨列

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`（`DrawEnemies` 约 `:1701-1925`，及其上方的 `EnemyCellWidth` / `EnemyCellHeightFront` / `EnemyPortraitFront` 等常量）

**Interfaces:**
- Consumes: Task 1 的 `EnemyState.ColumnSpan`、Task 3 的 `EnemyState.Shield`、Task 4 的图标 key。
- Produces: 无（后续任务不依赖）。

- [ ] **Step 1: 改格子常量为稿上的值**

```csharp
        // 稿 Battle.dc.html:.foe.front { width: 140px } .blk { 60×60 } —— 横排,立绘在左、信息列在右。
        // 竖排(2026-08-21~2026-08-30)改回横排的理由写在稿里:中区横向本就富余,
        // 横排后格高由立绘单独决定(信息列 36 < 立绘 60),纵向反比竖排省 24px/排,
        // 省出来的全给敌我之间的留白。
        private const float EnemyCellWidth = 293f;        // 稿 140pt
        private const float EnemyCellHeightFront = 138f;  // 稿 66pt
        private const float EnemyCellHeightBack = 109f;   // 稿 52pt
        private const float EnemyPortraitFront = 126f;    // 稿 60pt
        private const float EnemyPortraitBack = 96f;      // 稿 46pt
```

- [ ] **Step 2: 格内从 VStack 改 HStack**

`DrawEnemies` 里每个 cell 现在是竖着堆的。改成 `Ui.Row`（`HorizontalLayoutGroup`）：立绘块 `flexibleWidth = 0` 在左，信息列 `flexibleWidth = 1` 在右。信息列内部仍是 VStack，自上而下：名字 + 属性徽章一行、chip 一行、血条、**护盾条**、行动条。

稿上的条高（逻辑单位）：血条 `7pt → 15`、护盾条 `3pt → 6`、行动条 `3pt → 6`。

- [ ] **Step 3: 加护盾条与盾角标**

护盾条按稿的标尺——**盾 = 1/4 血时满格**，不按各自血量归一：

```csharp
        /// <summary>护盾条的填充比例(稿 Battle.dc.html 的 shieldPct):盾 = 1/4 血时满格。
        /// 刻意**不按各自血量归一** —— 归一了 60 盾的召唤物与 60 盾的 Boss 看起来一样长,
        /// 横着比就没意义了。</summary>
        private static float ShieldFraction(int shield, int maxHp) =>
            maxHp <= 0 ? 0f : Mathf.Min(1f, shield * 4f / maxHp);
```

盾为 0 时**整条不画**（稿上 `.shb` 只在 `hasShield` 时出现）。立绘左下角的盾角标同理。

⚠ **敌人的 `Shield` 眼下恒为 0**（Task 3 决策），所以这两样在真机上暂时看不见。别因为试玩看不到就以为没接上——照 `Shield > 0` 写对即可，将来加盾怪上线自然显形。

- [ ] **Step 4: Boss 格跨列**

格子宽度按 span 算：

```csharp
                // 跨列的怪(眼下只有 Boss,span = 4)占满整排 —— 宽度要把被它吞掉的
                // 那几条格间距也算进去,否则会比整排窄 3 个 RowGap。
                float span = enemy.Def.ColumnSpan;
                cellElement.preferredWidth = EnemyCellWidth * span + RowGap * (span - 1);
```

并且被它占掉的那几个预建空格位要**移除**（不是隐藏——留着会把行撑宽）：从 `frontCells` / `backCells` 里把 `[Column+1, ColumnEnd)` 那几个 `GameObject` `Destroy` 掉并置 null，落格循环里跳过 null。

- [ ] **Step 5: 空格位画成稿上的虚线框**

稿 `.foeslot { border: 1px dashed #C6BCA8; opacity: .45 }`，前排 140×66pt、后排 140×52pt。当前实现的空格位是纯占位的空 Panel，改成可见的虚线框——这是「每排恒定 4 格」在屏上真正成立的那一半。

⚠ uGUI 没有虚线边框。用 `Ui.OutlinedPanel` 的实线 + 45% 透明近似即可，别为此引资源。

- [ ] **Step 6: 离线编译 + 人工试玩**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

试玩逐条确认：

1. 敌人格是横排（立绘在左），前排立绘明显大于后排。
2. 每排恒定 4 格，空位是可见的虚线框；打死一只怪后**其余的不跳位**。
3. 单怪时它落在左起第 2 格（`ColumnOrder` 的 `1`），不靠最左。
4. 进一场 Boss 战（第 5 层）：Boss 一格横跨整排、居中，且**没有多余的空格位**。
5. 贯穿/横扫的命中与屏上看到的列一致。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation
git commit -m "feat(ui): 敌人格改横排,补护盾条与虚线空位,Boss 横跨整排

立绘在左、信息列在右(稿:横排格高由立绘单独决定,比竖排省 24px/排)。
空格位从不可见占位改成稿上的虚线框 —— 「每排恒定 4 格」这才在屏上成立。
护盾条按「盾 = 1/4 血满格」的标尺,盾为 0 时整条不画(眼下恒为 0)。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: 表现层 — 我方格横排 + 玩家条

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`（`DrawSummons` 约 `:1327`、`DrawLockedSummonSlot` `:1413`、`DrawEmptySummonSlot` `:1452`、`DrawPlayerStats` `:1158`）

**Interfaces:**
- Consumes: Task 4 的图标 key、Task 5 的 `_bottomRow` / `_summonFrontRow` / `_summonBackRow`。

- [ ] **Step 1: 召唤物格常量改稿上的值**

```csharp
        // 稿:.ally.front { width: 138px } .blk { 48×48 };.ally.rear .blk { 36×36 }
        // 横排后立绘反而**放大**了 —— 竖排时挤在 34/28 的小方块里。
        private const float SummonCellWidth = 289f;        // 稿 138pt
        private const float SummonPortraitFront = 100f;    // 稿 48pt
        private const float SummonPortraitBack = 75f;      // 稿 36pt
```

- [ ] **Step 2: 格内改横排，与敌人格同构**

立绘块在左（左下角盾角标），信息列在右：`攻 N` + 右对齐的 `血/上限` 一行、chip 一行、血条、护盾条、行动条。空槽/锁定槽同样改成稿上的 `.slotfree` 虚线框（前排 138×54pt、后排 138×42pt）。

- [ ] **Step 3: 玩家条改成与战场格同构的一条**

稿 `.me`：高 40pt（= `PlayerBarH` 84），横排四段——

1. **立绘块** 34pt 方（墨底 `#11161F`，白字），右下角金色等级角标；
2. **信息列**：`执笔人` + 右对齐 `血/上限`、血条 8pt、护盾条 5pt、行动条 3pt；
3. **AP 竖笔画格**：`AP` 小标 + 若干 8×20pt 的竖格（满的是墨 `#11161F`、空的是白格）+ `N/M`。稿上写明了理由——原先三颗 8px 小圆点，分量还不如旁边的状态 chip；AP 不够出下一个字时整块转朱砂（`.ap.dry`）；
4. **状态栏**：定宽 120pt、两行、四枚以内平铺，超出收 `+N`。

⚠ 稿上玩家条**没有**「点开详情」的入口——那是轮二的事（`UnitMe.dc.html`）。本轮不加。

- [ ] **Step 4: 离线编译 + 人工试玩**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

试玩确认：召唤物立绘比改前**大**；玩家条是一条 84 高的横条，AP 是竖笔画格且 AP 不足时整块转红；状态多于 4 枚时收成 `+N` 而不是把 AP 顶出去。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation
git commit -m "feat(ui): 我方格与玩家条接稿 —— 同构横排,AP 改竖笔画格

召唤物格横排后立绘从 34/28 放大到 48/36pt。玩家条与战场格同构:
立绘块 + 信息列 + 三条,AP 从三颗小圆点改成竖笔画格(满的是墨、
空的是白格,不够出下一个字时整块转朱砂),状态栏定宽两行收 +N。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 8: 表现层 — 手牌 / 部件池 / 拆合台 / 顶栏 / 左窄栏

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`（`DrawTopBar` `:1129`、`DrawLibrary` `:1925`、`DrawPool` `:2160`、`DrawSuggest` `:2242`、`DrawActions` `:2361`、`DrawEndTurn` `:2449`、`DrawNearMissHints` `:2279`）
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`（删右上角相生环图的挂载）

- [ ] **Step 1: 顶栏接稿**

稿的顺序：关卡名（`文山`，衬线 19pt）· `44 层 · 第 2 场` · 弹性空 · 墨锭 chip · `第 N 回合` · `?` 引导钮 · `✕` 退出钮。

`?` 是新增的（打开引导，Task 9 接）。⚠ 触控目标要 ≥44pt：稿用 `.tap::after` 把视觉 32 高的钮撑到 44——Unity 侧等价做法是给按钮加一个透明的 `Image` 子物体撑开可点区域，或直接把按钮的 `preferredHeight` 设到 92 逻辑单位（44pt）而视觉靠内边距收窄。

- [ ] **Step 2: 去掉相生环图**

删掉 `BuildSkeleton` 里 `shengGo = WuxingChart.Mount(transform, sheng: true)` 那一段（用户 2026-08-30 拍板：战斗中要实时查的是「我这张字克不克它」，相生 ×3 由配方静态决定，属牌面信息）。相克那张移进左窄栏（Task 5 已经把它挂到 `_rail` 下）。

⚠ `WuxingChart.Mount` 的 `sheng: true` 分支若因此再无调用点，**不要删它**——`CollectionView` / `CharInfo` 可能还在用。先 grep 确认：

```bash
grep -rn "WuxingChart.Mount" Brushblade/Assets/_Project/
```

- [ ] **Step 3: 手牌接稿**

稿 `.tile { width: 46px; height: 56px }` → **96×117 逻辑单位**（比现在的 68×85 大约四成）。标签 `字库 N/M` 改成竖排（稿 `writing-mode: vertical-rl`）；广告扩容位 `.adslot` 42×56pt → 88×117，绿色虚线框，已用过时转灰。

- [ ] **Step 4: 部件池接稿**

稿 `.part { width: 32px; height: 38px }` → **67×80 逻辑单位**。部件卡面要带**同源变体**提示（`.kin`，如「≈氵冫」）——`ComponentKin` 同组任一都能顶替配方里的那一位，所以必须写在卡面上。行尾 `.poolnote` 靠右显示「下回合掉 1 个」。

⚠ `.kin` 引入的是符号不是新汉字，但若文案里出现新字要重跑字体子集。

- [ ] **Step 5: 拆合台接稿**

右栏自上而下：`拆 合 台` 标题 → 选中字（牌面 34×43pt + 名/效果两行）或未选中时的空态两行说明 → `出 / 拆 / 弃` 三个单字钮一行（稿 `.act`，出=朱砂、拆=靛蓝、弃=灰）→ `可 合 成` 标题 → 可合成列表（`.cr`，`部件 → 字` 一行一条，绿底）→ `结束回合` 钮钉在最底。

稿上写死了一条口径：**只列可合成的；缺料的字归左栏「配字表」**——拆合台是动手的地方，不是清单。当前 `DrawSuggest` / `DrawNearMissHints` 若把缺料的也列进拆合台，按这条拆开。

- [ ] **Step 6: 左窄栏配字表接稿**

稿 `.missing`：一行一条 `字 缺 N`，字用衬线大字并按五行着色。当前 `_hintColumn` 是五行三级目录（一级选桶、二级选字），比稿复杂。**按稿改成平铺列表**，`_hintBucket` / `_hintCharFocus` 两个字段与相关分支一并删除。

⚠ 这会**删掉一个既有交互**。稿上左窄栏只有 68pt 宽，塞不下三级目录；且配字表的作用是「扫一眼还缺什么」，不是浏览器。

- [ ] **Step 7: 离线编译 + 人工试玩**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

试玩确认：顶栏七件齐全且 `?`/`✕` 点得中；右上角不再有相生环图；手牌牌面明显变大，满员 12 张时压窄不溢出；部件卡带同源提示；拆合台四段齐全且结束回合钮在最底；左栏配字表是平铺列表。

- [ ] **Step 8: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation
git commit -m "feat(ui): 手牌/部件/拆合台/顶栏/左窄栏接稿,相生环图去掉

手牌 68×85 → 96×117(稿 46×56pt),部件补同源变体提示;拆合台收进
右栏一栏并只列可合成的(缺料的归左栏配字表);顶栏补 ? 引导钮。
相生环图删除(用户拍板:战斗中要查的是克不克,相生由配方静态决定);
左栏配字表从三级目录改平铺 —— 68pt 宽塞不下目录。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 9: 新手引导 — 四步故事弹层

**Files:**
- Create: `Brushblade/Assets/_Project/Presentation/UI/CoachOverlay.cs`
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`（`DrawTutorialHint` `:1112`、`TutorialText` `:1120`）
- Modify: `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`
- Modify: `Brushblade/Assets/_Project/Core/`（`Tutorial.DemoChar` 与文案对不上的那处）

**Interfaces:**
- Produces: `CoachOverlay.Show(Transform root, int stepNo, int stepTotal, string tale, string doIt, string then, System.Action onNext, System.Action onSkip)`。

- [ ] **Step 1: 先修「指去点一张手上没有的牌」**

`docs/design/ui/scenes/README.md` 记着：`Tutorial.DemoChar` 是「刺」，而 `strings.zh-CN.json` 的四条 `battle.hint.tutorial.*` 写的是【剑】。先确认现状：

```bash
grep -rn "DemoChar" Brushblade/Assets/_Project/Core/
grep -n "battle.hint.tutorial" Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json
```

改**文案**去跟代码（把【剑】改成【刺】），不要反过来改 `DemoChar`——教程演示字是编成里真发得出的那张，改它要动首塔剧本段。

⚠ 若「刺」不在字体子集里，Step 6 会兜住。

- [ ] **Step 2: 写 `CoachOverlay`**

稿 `.scrim` + `.coach`。三段恒定：一句道理（旁白）→ 一个动作（指令）→ 一句结果（预告）。

```csharp
        /// <summary>新手引导弹层(2026-08-30,稿 Battle.dc.html 的 .coach)。
        ///
        /// 三段恒定的故事模板:一句道理(旁白) → 一个动作(指令) → 一句结果(预告)。
        /// 此前是屏底一行「◆ 提示」—— 一行字既讲不清道理也预告不了结果。
        ///
        /// 遮罩只压到 **38%**(稿上写死的):要看得见被点名的那张牌/那只怪。
        /// 压到常规的 Theme.Scrim(55%) 就等于让玩家照着做却看不见要点的东西。</summary>
        public static GameObject Show(Transform root, int stepNo, int stepTotal,
            string tale, string doIt, string then,
            System.Action onNext, System.Action onSkip)
```

版面自上而下：右上角金色 `第 N 步 / 共 M 步` 印章、衬线大字的 `tale`、一道分隔线、`这样做` 小标 + `doIt`、灰字 `then`、底部一行（进度圆点 + `跳过引导` + 朱砂 `下一步` 钮）。

⚠ 不要新建自己的遮罩色常量——38% 只在这里用，写成本文件的私有 `const float ScrimAlpha = 0.38f;` 并注明「稿上写死；全项目共用的模态遮罩是 `Theme.Scrim` 55%，两者刻意不同」。

- [ ] **Step 3: 补三段文案进字符串表**

四步各三段 = 12 条新 key，命名沿用既有前缀：

```
battle.coach.dismantle_demo.tale / .doit / .then
battle.coach.recompose_demo.tale / .doit / .then
battle.coach.cast_demo.tale / .doit / .then
battle.coach.pick_reward.tale / .doit / .then
```

加上 `battle.coach.step`（`第 {no} 步 · 共 {total} 步`）、`battle.coach.doit_label`（`这样做`）、`battle.coach.skip`（`跳过引导`）、`battle.coach.next`（`下一步`）、`battle.coach.done`（`开始`）。

⚠ **玩家可见文案禁止硬编码**，全部走 `Strings.T`。

- [ ] **Step 4: 接进 `BattleView`**

`DrawTutorialHint` 从「往 `_statusRow` 塞一行 Label」改成「调 `CoachOverlay.Show`」；顶栏的 `?` 钮重新打开当前步。`TutorialText` 从返回一个字符串改成返回三段。

- [ ] **Step 5: 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

- [ ] **Step 6: 重跑字体子集并验证**

```bash
python3 tools/fonts/subset_fonts.py
```

```bash
python3 -m pytest tools/fonts/tests/ -q
```

⚠ 这一步**必做**：12 条新文案几乎必然引入新字形，漏跑的话真机上显示成豆腐块，而离线编译和 Core 单测都发现不了。

- [ ] **Step 7: 人工试玩**

清掉存档从第 1 层重开，走完四步引导：每步都是居中弹层、看得见被点名的牌（遮罩不过重）、`跳过引导` 有效、顶栏 `?` 能重新打开当前步。

- [ ] **Step 8: 提交**

```bash
git add Brushblade/Assets/_Project Brushblade/Assets/StreamingAssets tools/fonts
git commit -m "feat(ui): 新手引导改四步故事弹层 —— 一句道理、一个动作、一句结果

屏底一行「◆ 提示」换成稿上的居中弹层,遮罩只压 38% 以便看得见被
点名的牌。顺带修一处指错:教程演示字是「刺」而文案写的是【剑】,
新手会被指去点一张手上没有的牌 —— 改文案跟代码。字体子集已重跑。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 10: 回填稿与 README

**Files:**
- Modify: `docs/design/ui/scenes/README.md`
- Modify: `docs/design/ui/scenes/Battle.dc.html`（若落地时量出稿自身的毛病）

- [ ] **Step 1: 回填「每排 4 格位」已完成**

README 的「稿子先行、代码待跟进」小节里，「每排 4 格位」那条早在 2026-08-27 就做完了（`FrontRowSize = 4`、`EnemyCap = 8`、`SummonCap = 8`），README 没跟上。删掉该条。

- [ ] **Step 2: 把本轮落地的三条从「稿子先行」移到「已接线」**

「敌人护盾」与「状态图标扩到 29 枚」两条转为已完成，并注明本轮的两处偏差：护盾**来源留白**（恒为 0）、图标是 **30** 枚不是 29（`heal` 补齐）。「相克即破甲要不要连护盾一起穿」那条待拍板改成已拍板：**不穿**。

- [ ] **Step 3: 新增一节「2026-08-30 已接线:战斗屏本体」**

照 README 里既有的「2026-08-30 已接线:宝箱立绘 + 开箱结果面板」那一节的写法，记下：三栏骨架换布局组、格内改横排、列区间与居中往外、相生环图去掉、左栏配字表从三级目录改平铺、引导改弹层。**每条都写清「为什么」**，README 全篇的体例是这样。

- [ ] **Step 4: 回填落地时量出的稿自身毛病**

README 记着一处已知的：`Battle.dc.html` 里「碉」写的是「血 60 / 攻 10 / 反伤 20」，而 `chars.json` 现在是「血 120 / 攻 0 / 反伤 50」（2026-08-25 字表重构之后）。按 `chars.json` 改稿。

其余落地时新量出的稿实不一致，一并在这一节记下并改稿——两边都可能是错的，但任何一边动了都要留痕。

- [ ] **Step 5: 提交**

```bash
git add docs/design/ui/scenes
git commit -m "docs(design): 回填战斗屏本体一轮 —— 三条转已接线,一条过期记载删除

「每排 4 格位」2026-08-27 就做完了,README 没跟上,删。护盾与图标
两条转已接线并注明偏差(护盾来源留白、图标是 30 枚不是 29)。
相克穿不穿盾已拍板:不穿。战斗稿里「碉」的数值按 chars.json 回填。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Self-Review

**Spec 覆盖**：轮一清单 11 条 → Task 1（8b 列区间）、Task 2（8b 格位）、Task 3（8 护盾）、Task 4（9 图标）、Task 5（1 三栏骨架）、Task 6（2 敌人格横排）、Task 7（3 我方格 + 4 玩家条）、Task 8（5 手牌部件 + 6 拆合台 + 7 顶栏）、Task 9（10 引导 + `DemoChar` 指错）、Task 10（回填）。决策 1~7 各有落点。

**已知偏差（相对 spec，落地时发现的改进）**：

1. spec 说「补 `ShieldBroken` 事件」，计划改成**复用 `BattleEvent.Absorbed`**——该字段已存在且玩家侧同口径，新增事件会逼表现层做事件配对（这套代码库在配对判据上栽过两次）。
2. spec 说「状态图标 18 → 29」，实为 **30**：`StatusGlossary.dc.html` 自己记着「持续治疗没有图标，落地要补第 30 枚」。

**待实现者补全的一处**：Task 3 Step 1 的五个测试方法体是断言说明而非可提交代码——造引擎的写法必须照邻近既有测试文件抄，不能凭空发明辅助类。这一处是刻意留的，因为凭空写会引入与既有测试不一致的第二套脚手架。
