# 移除出阵 · 卡池抽卡 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 删除「出阵」(Deck)概念,登塔的一切内容改从已解锁卡池(`OwnedCards`)取,起手 6 张改为「五行各一张加权抽 + 最高档保底一张」。

**Architecture:** 新增一张全局稀有度权重表 `MetaRules.RarityWeights`,起手抽卡与战后 5 选 2 共用。`MetaRules.StartingLibrary` 由「Deck 按卡等级取前 6」重写为随机抽取(需要 `RecipeGraph` 与 `GameRandom`)。所有原本读 `meta.Deck` 的地方改读 `meta.OwnedCards`,然后把 `Deck` 字段与它的一整套校验规则删掉。表现层拆除出阵编组 UI,侧栏改为卡池概览。

**Tech Stack:** Unity 6000.5.2f1,C#(Core/Data 纯 C#,禁引用 UnityEngine),NUnit,Python 数据管线。

**Spec:** `docs/superpowers/specs/2026-09-06-移除出阵-卡池抽卡-design.md`

## Global Constraints

- **Core 与 Data 禁止引用 UnityEngine**(asmdef `noEngineReferences: true`)。
- **随机性一律走 `Brushblade.Core.GameRandom`**,禁用 `UnityEngine.Random`。
- **玩家可见 UI 文案走字符串表**(`Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`),禁止硬编码。
- **新增/改动任何玩家可见中文文案后必须重跑字体子集**:`python3 tools/fonts/subset_fonts.py`,再跑 `pytest tools/fonts/tests/`。
- **测试断言只用 Unity 版 NUnit 也支持的 API**:禁用 `Is.AnyOf` / `Is.All.AnyOf`;`Does.Not.Contain(x)` 只能用于 string;集合判包含一律写 `Assert.That(list.Contains(x), Is.True/False, "…")`。
- **测试定位仓库根只能用 `TestContext.CurrentContext.TestDirectory`**,禁用 `AppContext.BaseDirectory`。
- **测试代码禁止直接引用 Newtonsoft**;只能引用 Core / Data 两个程序集。
- 本工作树里跑 prescompile 必须覆盖程序集路径:
  `dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies`
- 提交信息用 conventional commits(feat/fix/docs/chore/refactor + 范围)。

### 本计划复用的验证命令

```bash
# Core/Data 单测
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q

# Presentation 离线编译(改完 Presentation 必跑)
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q \
  -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies

# 管线(三个目录都要跑)
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ -q
```

---

### Task 1: 全局稀有度权重表

把散在 `RunEngine` 里的那份战利品权重提升为 `MetaRules` 上的唯一来源,后续任务全部指向它。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Meta.cs`(在 `MetaRules` 类里,`CopiesToUpgrade` 之前)
- Modify: `Brushblade/Assets/_Project/Core/RunEngine.cs:653-661`
- Test: `Brushblade/Assets/_Project/Tests/MetaTests.cs`(追加)

**Interfaces:**
- Consumes: 无
- Produces:
  - `public static readonly int[] MetaRules.RarityWeights`(长度 7,索引 = `(int)rarity - 1`)
  - `public static readonly CardRarity[] MetaRules.RarityOrder`(长度 7,按枚举数值升序)

- [ ] **Step 1: 写失败测试**

追加到 `Brushblade/Assets/_Project/Tests/MetaTests.cs` 的 `MetaTests` 类里:

```csharp
[Test]
public void RarityWeights_AreMonotonicallyDecreasing_AndSumToThousand()
{
    Assert.That(MetaRules.RarityWeights.Length, Is.EqualTo(7), "七档稀有度各一个权重");
    int sum = 0;
    foreach (var w in MetaRules.RarityWeights) sum += w;
    Assert.That(sum, Is.EqualTo(1000), "千分比,合计 1000");
    // 「稀有度越高概率越低」只从绿档往上单调 —— 白档刻意压在绿之下(2026-09-06 拍板)
    for (int i = 1; i + 1 < MetaRules.RarityWeights.Length; i++)
        Assert.That(MetaRules.RarityWeights[i], Is.GreaterThan(MetaRules.RarityWeights[i + 1]),
            $"第 {i} 档权重必须高于第 {i + 1} 档");
    Assert.That(MetaRules.RarityWeights[(int)CardRarity.White - 1],
        Is.LessThan(MetaRules.RarityWeights[(int)CardRarity.Green - 1]), "白档压在绿之下");
}

[Test]
public void RarityOrder_IsAscendingAndCoversEveryRarity()
{
    Assert.That(MetaRules.RarityOrder.Length, Is.EqualTo(7));
    for (int i = 0; i + 1 < MetaRules.RarityOrder.Length; i++)
        Assert.That((int)MetaRules.RarityOrder[i], Is.LessThan((int)MetaRules.RarityOrder[i + 1]),
            "必须按枚举数值升序 —— 遍历顺序是同种子同结果的前提");
    Assert.That(MetaRules.RarityOrder[0], Is.EqualTo(CardRarity.White));
    Assert.That(MetaRules.RarityOrder[6], Is.EqualTo(CardRarity.Red));
}
```

- [ ] **Step 2: 运行测试,确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "FullyQualifiedName~RarityWeights_AreMonotonically"
```

Expected: 编译失败,`MetaRules` 不含 `RarityWeights`。

- [ ] **Step 3: 在 `MetaRules` 里新增两张表**

插到 `Meta.cs` 里 `CopiesToUpgrade` 声明之前:

```csharp
/// <summary>抽卡的稀有度权重(千分比,索引 = rarity − 1;2026-09-06 拍板)。
/// 重心在绿/蓝,两头稀:白档虽然最不稀有,但压在绿之下 —— 起手全是白字开不了局。
///
/// ⚠ **起手抽卡与战后 5 选 2 共用这一张表。** 此前 RunEngine 另揣一份
/// {0, 80, 15, 5, 0, 0, 0},两套并存的后果是改一处漏一处,而漏掉的表现
/// (某一档字整档抽不出来)没有任何测试会红。</summary>
public static readonly int[] RarityWeights = { 150, 350, 300, 130, 50, 15, 5 };
//                                              白    绿    蓝    紫   金  橙  红

/// <summary>固定遍历顺序:保证同种子同结果(不依赖字典的枚举顺序)。必须按枚举数值升序。</summary>
public static readonly CardRarity[] RarityOrder =
{
    CardRarity.White, CardRarity.Green, CardRarity.Blue,
    CardRarity.Purple, CardRarity.Gold, CardRarity.Orange, CardRarity.Red,
};
```

- [ ] **Step 4: 运行测试,确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "FullyQualifiedName~Rarity"
```

Expected: PASS。

- [ ] **Step 5: 让 RunEngine 改用共享表**

`RunEngine.cs` 删掉自己的 `RewardRarityWeights` 与 `RarityOrder` 两个字段(现在的 653-661 行),
并把 `DrawWeightedReward` 里三处引用换掉。**保留 `DrawWeightedReward` 自己的抽取逻辑**——
它抽完要把候选移除(5 选 2 不能出现重复选项),与起手那条「不去重」的规则不同,不可合并。

删除:

```csharp
        /// <summary>字奖励的稀有度权重(2026-07-20 拍板):绿 80% / 蓝 15% / 紫 5%;
        /// 白(部件)与金橙红不参与。索引 = rarity − 1。</summary>
        private static readonly int[] RewardRarityWeights = { 0, 80, 15, 5, 0, 0, 0 };

        /// <summary>固定遍历顺序:保证同种子同结果(不依赖字典插入顺序)。必须按枚举数值升序。</summary>
        private static readonly CardRarity[] RarityOrder =
        {
            CardRarity.White, CardRarity.Green, CardRarity.Blue,
            CardRarity.Purple, CardRarity.Gold, CardRarity.Orange, CardRarity.Red,
        };
```

换成:

```csharp
        /// <summary>字奖励的稀有度权重与遍历顺序都取自 <see cref="MetaRules"/>(2026-09-06):
        /// 战后 5 选 2 与登塔起手抽卡共用同一条曲线。此前这里另有一份
        /// {0, 80, 15, 5, 0, 0, 0},白与金橙红都是 0 —— Deck 放开成全卡池之后,
        /// 那份权重会让玩家开出来的金/橙/红字永远进不了战利品候选。</summary>
        private static int[] RewardRarityWeights => MetaRules.RarityWeights;
        private static CardRarity[] RarityOrder => MetaRules.RarityOrder;
```

- [ ] **Step 6: 跑全量 Core 测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿。若 `RunEngineTests` / `EventTests` 里有断言写死了「战利品不出白字」,把它改成断言新曲线(白档权重 150 意味着白字**会**出现),并在测试注释里写明 2026-09-06 改的这条规则。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/Meta.cs Brushblade/Assets/_Project/Core/RunEngine.cs Brushblade/Assets/_Project/Tests/MetaTests.cs
git commit -m "refactor(core): 稀有度抽取权重收敛到 MetaRules.RarityWeights 唯一来源"
```

---

### Task 2: 起手 6 张改为卡池随机抽

本计划的核心。`StartingLibrary` 从确定性的「Deck 取前 6」改为随机抽取。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Meta.cs:440-465`(`StartingLibrary` 与 `SortByLevelDesc`)
- Test: `Brushblade/Assets/_Project/Tests/MetaTests.cs`(追加一个新 fixture 区段)

**Interfaces:**
- Consumes: `MetaRules.RarityWeights`、`MetaRules.RarityOrder`(Task 1)
- Produces:
  - `public static IReadOnlyList<string> MetaRules.StartingLibrary(MetaState meta, RecipeGraph graph, GameRandom random)`
  - `public static string MetaRules.DrawWeighted(IReadOnlyList<string> candidates, RecipeGraph graph, GameRandom random)`
  - `public static List<string> MetaRules.PlayableCards(MetaState meta, RecipeGraph graph)`

- [ ] **Step 1: 写失败测试**

追加到 `MetaTests.cs`。先在类里备一个测试用图(与既有的 `DeckGraph()` 并列,新起一个名字避免混淆):

```csharp
/// <summary>抽卡测试用图:五系各两张(白 + 蓝),外加一张紫档心系字。
/// 每个字都给配方(部件 + 部件),否则会被当成部件滤掉。</summary>
private static RecipeGraph PoolGraph() => new(new[]
{
    // 部件(无配方,component)
    new CharDef("丶", null, new string[0], component: true),
    new CharDef("丿", null, new string[0], component: true),
    // 金
    new CharDef("金白", Element.Metal, new[] { "丶", "丿" }, rarity: CardRarity.White),
    new CharDef("金蓝", Element.Metal, new[] { "丶", "丿" }, rarity: CardRarity.Blue),
    // 木
    new CharDef("木白", Element.Wood, new[] { "丶", "丿" }, rarity: CardRarity.White),
    new CharDef("木蓝", Element.Wood, new[] { "丶", "丿" }, rarity: CardRarity.Blue),
    // 水
    new CharDef("水白", Element.Water, new[] { "丶", "丿" }, rarity: CardRarity.White),
    new CharDef("水蓝", Element.Water, new[] { "丶", "丿" }, rarity: CardRarity.Blue),
    // 火
    new CharDef("火白", Element.Fire, new[] { "丶", "丿" }, rarity: CardRarity.White),
    new CharDef("火蓝", Element.Fire, new[] { "丶", "丿" }, rarity: CardRarity.Blue),
    // 土
    new CharDef("土白", Element.Earth, new[] { "丶", "丿" }, rarity: CardRarity.White),
    new CharDef("土蓝", Element.Earth, new[] { "丶", "丿" }, rarity: CardRarity.Blue),
    // 心系紫档:不参与前 5 张,只能经第 6 张进场
    new CharDef("心紫", Element.Heart, new[] { "丶", "丿" }, rarity: CardRarity.Purple),
});

private static MetaState PoolMeta(params string[] owned)
{
    var meta = new MetaState();
    foreach (var id in owned) meta.OwnedCards.Add(id);
    return meta;
}

private static readonly string[] FullPool =
{
    "金白", "金蓝", "木白", "木蓝", "水白", "水蓝",
    "火白", "火蓝", "土白", "土蓝", "心紫",
};
```

⚠ `new CharDef(...)` 的实参名与顺序要照 `CharDef` 当前的构造函数写 —— 实现前先
`grep -n 'public CharDef(' -A 20 Brushblade/Assets/_Project/Core/CharDef.cs` 核一遍,
按真实签名调整上面这段(尤其 `component` / `rarity` 是不是具名可选参数)。

然后是断言:

```csharp
[Test]
public void StartingLibrary_CoversEveryElement_InFirstFive()
{
    var graph = PoolGraph();
    var meta = PoolMeta(FullPool);
    // 多摇几个种子:五行覆盖是硬保证,不能只在某个幸运种子上成立
    for (int seed = 1; seed <= 30; seed++)
    {
        var library = MetaRules.StartingLibrary(meta, graph, new GameRandom(seed));
        Assert.That(library.Count, Is.EqualTo(6), $"seed {seed}: 五行各一 + 最高档一张");
        var elements = new List<Element>();
        for (int i = 0; i < 5; i++) elements.Add(graph.Get(library[i]).Element.Value);
        foreach (var e in new[] { Element.Metal, Element.Wood, Element.Water, Element.Fire, Element.Earth })
            Assert.That(elements.Contains(e), Is.True, $"seed {seed}: 前 5 张缺 {e}");
    }
}

[Test]
public void StartingLibrary_SixthCard_IsFromTopRarityInPool()
{
    var graph = PoolGraph();
    var meta = PoolMeta(FullPool);   // 池里最高档 = 心紫(Purple)
    for (int seed = 1; seed <= 30; seed++)
    {
        var library = MetaRules.StartingLibrary(meta, graph, new GameRandom(seed));
        Assert.That(graph.Get(library[5]).Rarity, Is.EqualTo(CardRarity.Purple),
            $"seed {seed}: 第 6 张必须来自卡池里实际存在的最高档");
    }
}

[Test]
public void StartingLibrary_SixthCard_FallsBackToLowerTier_WhenPoolTopsOutLower()
{
    var graph = PoolGraph();
    // 池里最高只到蓝(不含心紫)
    var meta = PoolMeta("金白", "金蓝", "木白", "木蓝", "水白", "水蓝",
                        "火白", "火蓝", "土白", "土蓝");
    for (int seed = 1; seed <= 20; seed++)
    {
        var library = MetaRules.StartingLibrary(meta, graph, new GameRandom(seed));
        Assert.That(graph.Get(library[5]).Rarity, Is.EqualTo(CardRarity.Blue),
            $"seed {seed}: 最高档只到蓝就从蓝里抽");
    }
}

[Test]
public void StartingLibrary_AllowsDuplicate_WhenSixthCollidesWithFirstFive()
{
    var graph = PoolGraph();
    // 金系只有一张蓝,且蓝是全池最高档 —— 前 5 张的金位与第 6 张必然都是「金蓝」
    var meta = PoolMeta("金蓝", "木白", "水白", "火白", "土白");
    var library = MetaRules.StartingLibrary(meta, graph, new GameRandom(7));
    Assert.That(library.Count, Is.EqualTo(6), "撞了也照收,不去重");
    int golds = 0;
    foreach (var id in library) if (id == "金蓝") golds++;
    Assert.That(golds, Is.EqualTo(2), "同一张字拿两份(2026-09-06 拍板:不去重)");
}

[Test]
public void StartingLibrary_SkipsElementsWithNoOwnedCards()
{
    var graph = PoolGraph();
    var meta = PoolMeta("金白", "木白");   // 只有金木两系
    var library = MetaRules.StartingLibrary(meta, graph, new GameRandom(3));
    Assert.That(library.Count, Is.EqualTo(3), "两系各一 + 最高档一张;缺的系直接跳过,不补齐");
}

[Test]
public void StartingLibrary_EmptyPool_IsEmpty()
{
    var library = MetaRules.StartingLibrary(new MetaState(), PoolGraph(), new GameRandom(1));
    Assert.That(library, Is.Empty);
}

[Test]
public void StartingLibrary_SameSeed_SameResult()
{
    var graph = PoolGraph();
    var meta = PoolMeta(FullPool);
    var a = MetaRules.StartingLibrary(meta, graph, new GameRandom(42));
    var b = MetaRules.StartingLibrary(meta, graph, new GameRandom(42));
    Assert.That(a, Is.EqualTo(b), "同种子同结果 —— 断点续爬要靠这条");
}

[Test]
public void StartingLibrary_HeartCards_NeverEnterTheFirstFive()
{
    var graph = PoolGraph();
    var meta = PoolMeta(FullPool);
    for (int seed = 1; seed <= 30; seed++)
    {
        var library = MetaRules.StartingLibrary(meta, graph, new GameRandom(seed));
        for (int i = 0; i < 5; i++)
            Assert.That(library[i], Is.Not.EqualTo("心紫"),
                $"seed {seed}: 心系不参与五行那 5 格");
    }
}

[Test]
public void StartingLibrary_ExcludesComponents()
{
    var graph = PoolGraph();
    var meta = PoolMeta("金白", "丶", "丿");   // 部件混进 OwnedCards
    var library = MetaRules.StartingLibrary(meta, graph, new GameRandom(5));
    foreach (var id in library)
        Assert.That(graph.Get(id).IsComponent, Is.False, "部件与中间产物字不是抽卡候选");
}

[Test]
public void StartingLibrary_WeightsFavorCommonRarities()
{
    var graph = PoolGraph();
    // 金系一白一蓝:白 150‰ vs 蓝 300‰ —— 蓝该明显更多
    var meta = PoolMeta("金白", "金蓝");
    int blue = 0;
    for (int seed = 1; seed <= 400; seed++)
    {
        var library = MetaRules.StartingLibrary(meta, graph, new GameRandom(seed));
        if (library[0] == "金蓝") blue++;
    }
    // 理论 300/(150+300) ≈ 66.7%;给足余量,只守「明显偏向蓝」
    Assert.That(blue, Is.GreaterThan(220), $"400 次里蓝只出了 {blue} 次,权重没生效");
    Assert.That(blue, Is.LessThan(360), $"400 次里蓝出了 {blue} 次,白档像是被整档滤掉了");
}

[Test]
public void StartingLibrary_BowenPerkAppendsExtraDraws()
{
    var graph = PoolGraph();
    var meta = PoolMeta(FullPool);
    int baseline = MetaRules.StartingLibrary(meta, graph, new GameRandom(9)).Count;
    Assert.That(baseline, Is.EqualTo(6));
    meta.PerkLevels["bowen"] = 1;   // ⚠ 实现前用 grep 核实博闻的真实 perk id
    Assert.That(MetaRules.StartingLibrary(meta, graph, new GameRandom(9)).Count,
        Is.EqualTo(7), "博闻每级追加一张自由加权抽");
}
```

⚠ 最后一个测试里的 `"bowen"` 是占位:实现前跑
`grep -rn 'PerkEffect.Library' -B 6 Brushblade/Assets/_Project/Core/Perk*.cs`
找出博闻的真实 id 并替换。既有的 `PerkTests.StartingLibrary_GrowsWithBowen` 里就有正确写法,照抄。

- [ ] **Step 2: 运行测试,确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "FullyQualifiedName~StartingLibrary_Covers"
```

Expected: 编译失败,`StartingLibrary` 没有三参重载。

- [ ] **Step 3: 重写 `StartingLibrary`**

在 `Meta.cs` 里,把现有的 `StartingLibrary(MetaState meta)` 与它下面的 `SortByLevelDesc`
整个替换为下面这段(`SortByLevelDesc` 若在别处还有调用点就留着,`grep -n 'SortByLevelDesc' Brushblade/Assets/_Project` 确认):

```csharp
/// <summary>五行的抽取顺序:固定,保证同种子同结果。心系不在内 ——
/// 心系字只能经第 6 张(最高档保底)或战利品进场。</summary>
private static readonly Element[] StartingElements =
    { Element.Metal, Element.Wood, Element.Water, Element.Fire, Element.Earth };

/// <summary>抽卡候选 = 已收集的**字**。
///
/// 一道 <c>IsComponent</c> 同时滤掉两类东西:部件,以及「有配方但无属性无稀有度」的
/// 中间产物字(列/则/喿/垔/岂/朵/烝/秋/茾/荅 —— chars.json 里这 10 个全部
/// <c>component: true</c>,2026-09-06 核过)。中间产物字的 Rarity 会缺省成白,
/// 不滤掉就会被当成白字抽进起手。
///
/// 顺带把字表下架的幽灵字挡在外面(<c>TryGet</c> 取不到就跳过)。</summary>
public static List<string> PlayableCards(MetaState meta, RecipeGraph graph)
{
    var cards = new List<string>();
    foreach (var id in meta.OwnedCards)
        if (graph.TryGet(id, out var def) && !def.IsComponent)
            cards.Add(id);
    return cards;
}

/// <summary>从候选里按 <see cref="RarityWeights"/> 抽一张;候选为空返回 null。
///
/// ⚠ **不从候选里移除抽中的那张** —— 起手允许重复(2026-09-06 拍板:第 6 张撞上前 5 张
/// 就是同一张字拿两份)。战后 5 选 2 那条路径要的是「不重复」,所以它在
/// <see cref="RunEngine"/> 里另有一份会移除候选的实现,两者不可合并。</summary>
public static string DrawWeighted(IReadOnlyList<string> candidates, RecipeGraph graph,
    GameRandom random)
{
    if (candidates.Count == 0) return null;

    // 按稀有度分组;遍历顺序走 RarityOrder,不依赖字典枚举顺序
    var byRarity = new Dictionary<CardRarity, List<string>>();
    foreach (var id in candidates)
    {
        var rarity = graph.Get(id).Rarity;
        if (!byRarity.TryGetValue(rarity, out var group))
            byRarity[rarity] = group = new List<string>();
        group.Add(id);
    }

    int total = 0;
    foreach (var rarity in RarityOrder)
        if (byRarity.TryGetValue(rarity, out var group) && group.Count > 0)
            total += RarityWeights[(int)rarity - 1];

    if (total <= 0) return candidates[random.Next(candidates.Count)]; // 权重全零:均匀兜底

    int roll = random.Next(total);
    foreach (var rarity in RarityOrder)
    {
        if (!byRarity.TryGetValue(rarity, out var group) || group.Count == 0) continue;
        roll -= RarityWeights[(int)rarity - 1];
        if (roll < 0) return group[random.Next(group.Count)];
    }
    return candidates[candidates.Count - 1]; // 理论不可达(浮点无关,整数累加必然命中)
}

/// <summary>候选里稀有度最高的那一档的全部字。空候选返回空表。</summary>
private static List<string> TopRarityCards(IReadOnlyList<string> candidates, RecipeGraph graph)
{
    var top = new List<string>();
    CardRarity best = 0;
    foreach (var id in candidates)
    {
        var rarity = graph.Get(id).Rarity;
        if (rarity > best) { best = rarity; top.Clear(); }
        if (rarity == best) top.Add(id);
    }
    return top;
}

/// <summary>登塔起手字库(2026-09-06 拍板,取代「出阵表按等级取前 6」):
///
/// <list type="number">
///   <item>前 5 张:金/木/水/火/土 各一张,每系在**本系已收集的字**里按
///         <see cref="RarityWeights"/> 加权抽。某系一个字都没有就跳过 ——
///         起手不足 6 是合法状态,不做补齐。</item>
///   <item>第 6 张:候选里**实际存在的最高稀有度档**中均匀抽一张。卡池最高只到蓝,
///         就从蓝里抽。</item>
///   <item>博闻技能每级追加一张全池自由加权抽。</item>
/// </list>
///
/// ⚠ **全程不去重。** 第 6 张撞上前 5 张里的字,就是同一张字拿两份;博闻那几张同理。
/// 字库本来就允许重复条目(见 <see cref="BattleEngine"/> 里「同字多张也不会认错卡位」
/// 那条注释),消耗按下标走,不需要额外改造。</summary>
public static IReadOnlyList<string> StartingLibrary(MetaState meta, RecipeGraph graph,
    GameRandom random)
{
    var candidates = PlayableCards(meta, graph);
    var library = new List<string>();
    if (candidates.Count == 0) return library;

    foreach (var element in StartingElements)
    {
        var ofElement = new List<string>();
        foreach (var id in candidates)
            if (graph.Get(id).Element == element) ofElement.Add(id);
        var pick = DrawWeighted(ofElement, graph, random);
        if (pick != null) library.Add(pick);
    }

    var top = TopRarityCards(candidates, graph);
    if (top.Count > 0) library.Add(top[random.Next(top.Count)]);

    for (int i = 0; i < PerkRules.LibraryBonus(meta); i++)
    {
        var extra = DrawWeighted(candidates, graph, random);
        if (extra == null) break;
        library.Add(extra);
    }

    return library;
}
```

`StartingLibrarySize` 这个常量现在只剩「字库容量比起手多一格」那层关系在用
(`LibraryCapacityFor`)。**保留它**,但把注释改成:

```csharp
public const int StartingLibrarySize = 6; // 起手字库数量 = 五行各一 + 最高档保底一(2026-09-06)
```

- [ ] **Step 4: 运行新测试,确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "FullyQualifiedName~StartingLibrary"
```

Expected: Task 2 的新测试全绿。旧的
`MetaTests.StartingLibrary_TakesTopSixByLevel_FromRoster` /
`MetaTests.StartingLibrary_DeckOnly_NoAutoFill` /
`PerkTests.StartingLibrary_GrowsWithBowen` 会因为签名变了而编译失败 —— 那是预期的,Task 6 统一收拾。
为了让这一步能跑起来,**现在就**把这三个旧测试删掉(它们测的规则已被本任务废止),
并在 `PerkTests` 里换成 Task 2 Step 1 里那条 `StartingLibrary_BowenPerkAppendsExtraDraws` 的等价写法。

- [ ] **Step 5: 跑全量 Core 测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 只剩 `GameRoot` 那边的调用点还没改(Presentation 不在 coretests 里,不会红)。Core 全绿。

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Core/Meta.cs Brushblade/Assets/_Project/Tests/MetaTests.cs Brushblade/Assets/_Project/Tests/PerkTests.cs
git commit -m "feat(core): 起手字库改为卡池加权抽(五行各一 + 最高档保底一)"
```

---

### Task 3: 部件池改从整个卡池取

`DeckComponents` 改名 `PoolComponents`,入参从出阵表变成已解锁卡池。签名不变,是一次机械改名。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Meta.cs:78-108`(`DeckComponents` / `RollStartingPool`)
- Modify: `Brushblade/Assets/_Project/Core/RunEngine.cs:47-52`
- Test: `Brushblade/Assets/_Project/Tests/StartingSetupTests.cs`、`EventTests.cs`、`CharTableTests.cs`、`PierceBuffCharTests.cs`(改名)

**Interfaces:**
- Consumes: 无
- Produces:
  - `public static IEnumerable<string> MetaRules.PoolComponents(IReadOnlyList<string> pool, RecipeGraph graph)`(取代 `DeckComponents`,参数与返回类型完全一致)
  - `public static IReadOnlyList<string> MetaRules.RollStartingPool(IReadOnlyList<string> pool, RecipeGraph graph, GameRandom random, int count = StartingPoolSize)`(签名不变,只改参数名与注释)

- [ ] **Step 1: 改名并更新注释**

`Meta.cs` 里:

```csharp
/// <summary>卡池所需的部件(2026-09-06,原 DeckComponents):部件的一切来源都从这里取。
/// 入参从「出阵表」换成「已解锁卡池」—— 出阵废止后,能掉的部件跟着整个卡池走。
///
/// ⚠ 原先「掉到的部件永远拼得出手里的字」这条保证**不再成立**:卡池是全量的,
/// 而起手只有 6 张,拿到拼不出手上任何字的部件是合法结果(2026-09-06 用户裁定接受)。
/// 只收叶子(部件);配方里的低阶字不算,那是靠合成得来的。</summary>
public static IEnumerable<string> PoolComponents(
    IReadOnlyList<string> pool, RecipeGraph graph)
{
    var seen = new List<string>();
    foreach (var card in pool)
    {
        if (!graph.TryGet(card, out var def)) continue;
        foreach (var part in def.Recipe)
            if (graph.TryGet(part, out var partDef) && partDef.IsComponent && !seen.Contains(part))
                seen.Add(part);
    }
    return seen;
}

/// <summary>登塔初始部件池:从卡池所需部件里随机掷 <paramref name="count"/> 个(可重复)。
/// 卡池拼不出任何部件时返回空池——不回退到五行,那会把死牌塞回来。</summary>
public static IReadOnlyList<string> RollStartingPool(IReadOnlyList<string> pool,
    RecipeGraph graph, GameRandom random, int count = StartingPoolSize)
{
    var choices = new List<string>(PoolComponents(pool, graph));
    // …函数体其余部分不动…
}
```

`RunEngine.cs:47-52` 的注释与调用一起改:

```csharp
        /// <summary>奇遇随机部件的候选(2026-09-06):从**已解锁卡池所需的部件**里取。
        /// RewardPool 即卡池(GameRoot 接线);空则无部件可给。</summary>
        private IReadOnlyList<string> RandomComponentChoices =>
            new List<string>(MetaRules.PoolComponents(
                _runConfig.RewardPool ?? Array.Empty<string>(), _graph));
```

(上面的属性名照现有代码写 —— 实现前 `sed -n '45,55p' Brushblade/Assets/_Project/Core/RunEngine.cs` 核一遍真实写法,只换 `DeckComponents` → `PoolComponents` 与注释,别动别的。)

- [ ] **Step 2: 全仓改名**

```bash
grep -rln 'DeckComponents' Brushblade/Assets/_Project tools/ | grep -v '/\.claude/'
```

对列出的每个文件把 `DeckComponents` 换成 `PoolComponents`。测试里同时把方法名与注释里的「出阵表」改成「卡池」:
- `StartingSetupTests.cs`:`DeckComponents_AreTheLeavesOfDeckRecipes` → `PoolComponents_AreTheLeavesOfPoolRecipes`,同区段其余 5 个同理
- `EventTests.cs:329,339,355`
- `CharTableTests.cs:721,724`(`RealConfig_ComponentWithRecipe_StillCountsAsDeckComponent` → `…AsPoolComponent`)
- `PierceBuffCharTests.cs:172-184`(注释里那句「GameRoot 把 runConfig.RewardPool 接的就是 meta.Deck」改成 `meta.OwnedCards`)

- [ ] **Step 3: 跑测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿(纯改名,行为不变)。

- [ ] **Step 4: 提交**

```bash
git add -A Brushblade/Assets/_Project
git commit -m "refactor(core): DeckComponents 改名 PoolComponents,部件来源改述为卡池"
```

---

### Task 4: 战斗配置与战利品池改读 OwnedCards

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Meta.cs:240`(`BuildBattleConfig` 的 `UnlockedChars`)
- Modify: `Brushblade/Assets/_Project/Core/Endless.cs:16`(注释)
- Modify: `Brushblade/Assets/_Project/Presentation/GameRoot.cs:163-167, 198`
- Test: `Brushblade/Assets/_Project/Tests/MetaTests.cs`(追加)

**Interfaces:**
- Consumes: `MetaRules.StartingLibrary(meta, graph, random)`(Task 2)
- Produces: 无新接口(只改赋值来源)

- [ ] **Step 1: 写失败测试**

追加到 `MetaTests.cs`:

```csharp
[Test]
public void BuildBattleConfig_UnlockedChars_IsTheWholeCollection()
{
    var meta = new MetaState();
    meta.OwnedCards.Add("灯");
    meta.OwnedCards.Add("炎");
    var config = MetaRules.BuildBattleConfig(meta, new[] { "灯" });
    Assert.That(config.UnlockedChars, Is.EqualTo(meta.OwnedCards),
        "出阵废止后可合成集 = 整个已解锁卡池(2026-09-06)");
}
```

- [ ] **Step 2: 运行,确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "FullyQualifiedName~BuildBattleConfig_UnlockedChars"
```

Expected: FAIL(现在返回的是 `meta.Deck`,空表)。

- [ ] **Step 3: 改 `BuildBattleConfig`**

`Meta.cs:240`:

```csharp
                UnlockedChars = meta.OwnedCards, // 可合成集 = 整个已解锁卡池(2026-09-06;与战利品同源)
```

- [ ] **Step 4: 改 `Endless.cs:16` 的注释**

```csharp
        /// 战利品只出自已解锁卡池(2026-09-06,原「出阵表」),GameRoot 接线时无条件覆盖为
        /// meta.OwnedCards,
```

(保持这条注释后半句原样,只改前半句与字段名。)

- [ ] **Step 5: 改 `GameRoot.cs` 三处接线**

163-167 行(起手字库与部件池)——注意 `StartingLibrary` 现在要 graph 与 random,
且两者应共用**同一个** `GameRandom`,别各造一个:

```csharp
                    // 起手字库与初始部件池共用一条随机流:各造一个 GameRandom 会让
                    // 「这一局」拆成两条互不相关的序列,将来要把登塔种子入档时没法复现
                    var startRandom = new GameRandom(System.Environment.TickCount);
                    // …
                    Library = new System.Collections.Generic.List<string>(
                        MetaRules.StartingLibrary(_meta, _graph, startRandom)),
                    // …
                    Pool = new System.Collections.Generic.List<string>(MetaRules.RollStartingPool(
                        _meta.OwnedCards, _graph, startRandom)),
```

⚠ `startRandom` 的声明必须提到那个对象初始化器**之前** —— 对象初始化器里没法声明局部变量。
实现时先 `sed -n '150,205p' Brushblade/Assets/_Project/Presentation/GameRoot.cs` 看清结构再动。

198 行:

```csharp
            runConfig.RewardPool = _meta.OwnedCards;
```

53-54 行(默认出阵注入)整段删除:

```csharp
            if (_meta.Deck.Count < MetaRules.DeckMinimum)
                MetaRules.TrySetDeck(_meta, MetaRules.StartingDeck, _graph);
```

- [ ] **Step 6: 跑 Core 测试 + 离线编译**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿。`StartingSetupTests` 里若有断言「战利品必须来自出阵表」而现在池子变成全量,
把断言改成 `meta.OwnedCards` 并更新注释;`ShippedConfig_StartingDeck_ClearsFirstFloor` 这条
依赖 `StartingDeck` 常量,改成用 `MetaRules.StartingCollection` 起手、跑一遍
`StartingLibrary` 抽出来的库来打首层。

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

Expected: 只剩 `CollectionView` / `MapView` / `CardBadges` / `DragToDeck` 里的 `Deck` 引用报
`error CS` —— 那是 Task 5 的活。忽略 `warning MSB3245`。

- [ ] **Step 7: 提交**

```bash
git add -A Brushblade/Assets/_Project
git commit -m "feat(core): 可合成集与战利品池改读 OwnedCards"
```

---

### Task 5: 拆除出阵 UI

**Files:**
- Delete: `Brushblade/Assets/_Project/Presentation/UI/DragToDeck.cs` 与 `DragToDeck.cs.meta`
- Modify: `Brushblade/Assets/_Project/Presentation/CollectionView.cs`
- Modify: `Brushblade/Assets/_Project/Presentation/MapView.cs:249-275`
- Modify: `Brushblade/Assets/_Project/Presentation/Cards/CardBadges.cs:37,69,80`
- Modify: `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`

**Interfaces:**
- Consumes: `MetaRules.PlayableCards`(Task 2)
- Produces: `CollectionView.BuildPoolPanel(Transform parent)`(取代 `BuildDeckPanel`)

- [ ] **Step 1: 删掉 DragToDeck**

```bash
git rm Brushblade/Assets/_Project/Presentation/UI/DragToDeck.cs Brushblade/Assets/_Project/Presentation/UI/DragToDeck.cs.meta
```

- [ ] **Step 2: 从 CollectionView 里摘掉出阵**

删除这些成员与它们的全部调用:
- `ToggleDeck` / `DropFromGrid` / `DropFromSlot` / `BuildDeckPanel` / `BuildSlot` / `BuildQuotaBar`
- `BuildDropHint` / `ShowDropHint` / `HideDropHint` / `_dropHint` / `_dropHintLabel` /
  `_dropHits` / `Inside` / `_sideRect` / `_sideFrame` / `_sideFrameRest` / `_gridRect`
  —— 这些只服务拖拽,一并清掉(**先 `grep -n` 确认没有别的调用点再删**)
- 网格里 `if (owned) DragToDeck.Attach(...)` 那一整块
- `CardBadges.Spec` 初始化里的 `InDeck = _meta.Deck.Contains(def.Id),` 那一行
- `SheetActions` 里 `inDeck` 与整个 `deckButton` 那一段(保留「去开宝箱」与升级钮)
- 顶栏 `collection.header.stats` 的 `deckCount` / `deckLimit` 两个占位参数

侧栏标题从 `collection.side.title_deck` 换成 `collection.side.title_pool`,
`BuildDeckPanel(content)` 换成 `BuildPoolPanel(content)`。

- [ ] **Step 3: 写卡池概览面板**

在 CollectionView 里新增(替换掉原 `BuildDeckPanel` 的位置):

```csharp
// ---- 右栏 · 卡池概览(2026-09-06,取代出阵编组) ----

/// <summary>按稀有度分档列出卡池:档位 / 字数 / 起手抽中率。
///
/// 抽中率是**近似值**:真实的起手是「五行各一张 + 最高档保底一张」,各系内部独立加权,
/// 严格算要按系分开。这里给的是「全池加权抽一张时该档的命中率」——
/// 玩家要看的是「开箱怎么影响我的起手」这个方向,不是精确概率。</summary>
private void BuildPoolPanel(Transform parent)
{
    var playable = MetaRules.PlayableCards(_meta, _graph);
    var section = CharSheetSections.Section(parent,
        Strings.T("collection.side.section.pool", ("count", playable.Count)));

    // 各档字数
    var counts = new int[MetaRules.RarityWeights.Length];
    foreach (var id in playable)
        counts[(int)_graph.Get(id).Rarity - 1]++;

    // 分母只算「池里真有字」的档 —— 空档不参与加权
    int total = 0;
    for (int i = 0; i < counts.Length; i++)
        if (counts[i] > 0) total += MetaRules.RarityWeights[i];

    // 从高到低列:玩家最关心的是顶上那几档
    for (int i = counts.Length - 1; i >= 0; i--)
    {
        if (counts[i] == 0) continue;
        var rarity = (CardRarity)(i + 1);
        int permille = total > 0 ? MetaRules.RarityWeights[i] * 1000 / total : 0;
        BuildPoolRow(section, rarity, counts[i], permille);
    }

    var tip = CharSheetSections.Section(parent, Strings.T("collection.side.section.tip"));
    string tipText = Strings.T("collection.side.pool_tip_body");
    var text = Ui.ThemedLabel(tip, tipText, 19, Theme.TextDim);
    text.alignment = TextAnchor.UpperLeft;
    text.horizontalOverflow = HorizontalWrapMode.Wrap;
    Ui.Sized(text.gameObject, 0,
        Ui.WrappedTextHeight(tipText, 19, SideW - SidePad * 2), flexWidth: 1);
}

/// <summary>概览的一行:稀有度色条 + 档名 + 字数 + 抽中率。</summary>
private void BuildPoolRow(Transform parent, CardRarity rarity, int count, int permille)
{
    var row = Ui.Row(parent, $"PoolRow{(int)rarity}", 10);
    row.GetComponent<HorizontalLayoutGroup>().childAlignment = TextAnchor.MiddleLeft;
    Ui.Sized(row, 0, 34, flexWidth: 1);

    var swatch = Ui.Panel(row.transform, "Swatch");
    swatch.AddComponent<Image>().color = Theme.RarityColor(rarity);
    Ui.Sized(swatch, 8, 22);

    Ui.ThemedLabel(row.transform, Strings.T($"rarity.{(int)rarity}"), 19, Theme.TextMain,
        null, TextAnchor.MiddleLeft);
    Ui.Panel(row.transform, "Spring");
    Ui.ThemedLabel(row.transform,
        Strings.T("collection.side.pool_row",
            ("count", count), ("percent", $"{permille / 10}.{permille % 10}")),
        19, Theme.TextDim, null, TextAnchor.MiddleRight);
}
```

⚠ 三个 API 要先核实真名,别照抄:
- `Theme.RarityColor(rarity)` —— `grep -n 'Rarity' Brushblade/Assets/_Project/Presentation/UI/Theme.cs`
- `Strings.T($"rarity.{(int)rarity}")` —— 稀有度档名的字符串表 key 是什么形状?
  `grep -n 'rarity' Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`。
  ⚠ 若档名 key 是**变量拼出来的**,字符串表的孤儿扫描认不出它(见项目 memory
  `strings_table_scan_is_literal_only`)—— 那就改成一个写死的 `switch`,每档一个字面量 key。
- `Ui.Sized` / `Ui.Panel` / `Ui.Row` 的实参形状照 CollectionView 里既有用法。

- [ ] **Step 4: 改 MapView**

`BuildDeckMini` 改名 `BuildPoolMini`,内容从「遍历 Deck 画小牌」改成一行摘要 + 最高档那几张:

```csharp
/// <summary>角色栏里的卡池摘要(2026-09-06,取代出阵表小牌墙):
/// 出阵废止后这里没有「玩家编好的名单」可展示,改成「卡池多大 + 顶上那一档长什么样」。
/// 顶档那几张正是起手第 6 格保底会抽到的池子。</summary>
private void BuildPoolMini(Transform parent)
{
    const int PerRow = 6;
    const float TileW = 50f;
    const float TileH = 62f;

    var playable = MetaRules.PlayableCards(_meta, _graph);
    Ui.ThemedLabel(parent,
        Strings.T("map.hero.pool_title", ("count", playable.Count)),
        19, Theme.LockGray, null, TextAnchor.MiddleLeft);

    // 只画最高档那几张,最多一行
    CardRarity best = 0;
    foreach (var id in playable)
    {
        var rarity = _graph.Get(id).Rarity;
        if (rarity > best) best = rarity;
    }

    var rows = Ui.VStack(parent, "PoolRows", 8);
    rows.GetComponent<VerticalLayoutGroup>().childForceExpandWidth = true;

    Transform row = null;
    int shown = 0;
    foreach (string id in playable)
    {
        if (shown >= PerRow) break;
        if (!_graph.TryGet(id, out var def) || def.Rarity != best) continue;
        if (shown % PerRow == 0)
        {
            var rowGo = Ui.Row(rows.transform, "PoolRow0", 8);
            rowGo.GetComponent<HorizontalLayoutGroup>().childAlignment = TextAnchor.MiddleLeft;
            row = rowGo.transform;
        }
        Ui.MiniGlyphTile(row, def, new Vector2(TileW, TileH));
        shown++;
    }
}
```

`grep -n 'BuildDeckMini' Brushblade/Assets/_Project/Presentation/MapView.cs` 找到调用点一并改名。

- [ ] **Step 5: 改 CardBadges**

`CardBadges.cs:37` 删掉 `public bool InDeck;`;69 行那个 `if (spec.InDeck) { … }` 整块删掉;
80 行的 `float bottom = spec.InDeck ? chipH + pad : pad;` 改成 `float bottom = pad;`。
若删完 `chipH` 就没人用了,一并删(这是本次改动制造的孤儿,该清)。

- [ ] **Step 6: 改字符串表**

`Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`。

**删除**这 11 条:
```
collection.alert.deck_add_fail_body
collection.alert.deck_limited_title
collection.alert.deck_min_body
collection.card.deployed_bar
collection.side.section.deck
collection.side.section.quota
collection.button.equip
collection.button.unequip
collection.side.tip_body
collection.drag.to_deck
collection.drag.off_deck
```

**改**这 5 条:
```json
"battle.error.not_unlocked": "此字还没解锁——开宝箱收集到才能合",
"collection.header.stats": "收集 {owned}/{total}",
"map.hero.pool_title": "卡池 {count} 字",
"charsheet.section.craft": "能凑出什么 · 卡池内",
"charsheet.craft.empty": "卡池里暂时没有用得上它的字",
```
(`map.hero.deck_title` 这个 key 换成 `map.hero.pool_title`,不是原地改值。)

**新增**这 4 条:
```json
"collection.side.title_pool": "卡池概览",
"collection.side.section.pool": "卡池 {count} 字",
"collection.side.pool_row": "{count} 字　{percent}%",
"collection.side.pool_tip_body": "登塔起手 6 张从卡池里抽:五行各一张,外加最高一档保底一张。\n宝箱开得越多卡池越大,稀有度越高的字越难抽到。",
```

- [ ] **Step 7: 重跑字体子集**

新文案带进了新字(概览、抽、越、难、档、保底 等),不重跑会在真机上显示成豆腐块。

```bash
python3 tools/fonts/subset_fonts.py
python3 -m pytest tools/fonts/tests/ -q
```

Expected: PASS。⚠ 重新生成必然带来时间戳 churn,**光看 diff 大小说明不了变没变 —— 要比 cmap**。

- [ ] **Step 8: 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

Expected: 零 `error CS`。`warning MSB3245` 忽略。

- [ ] **Step 9: 全量验证 + 提交**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ -q
```

```bash
git add -A Brushblade tools
git commit -m "feat(ui): 拆除出阵编组,收集页侧栏改为卡池概览"
```

---

### Task 6: 删除 Deck 字段与它的一整套规则

到这一步 `meta.Deck` 已经没有任何生产代码在读了,可以安全下架。

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Meta.cs:20, 49-51, 70-76, 416-438`
- Modify: `Brushblade/Assets/_Project/Tests/MetaTests.cs`、`StartingSetupTests.cs`、`ConfigLoaderTests.cs`、`EventTests.cs`、`BattleEngineTests.cs`

**Interfaces:**
- Consumes: 无
- Produces: 无(纯删除)

- [ ] **Step 1: 确认无人引用**

```bash
grep -rn '\.Deck\b\|DeckLimit\|DeckMinimum\|DeckPerElementLimit\|StartingDeck\|TrySetDeck' \
  Brushblade/Assets/_Project/Core Brushblade/Assets/_Project/Data \
  Brushblade/Assets/_Project/Presentation Brushblade/Assets/_Project/Platform tools/
```

Expected: 只剩 Tests 目录里的引用(Core/Data/Presentation/tools 全部干净)。若还有生产代码命中,先处理它。

- [ ] **Step 2: 从 Core 删掉**

`Meta.cs` 删除:
- `MetaState.Deck` 字段(第 20 行)
- `DeckLimit` / `DeckMinimum` / `DeckPerElementLimit` 三个常量(49-51 行)
- `StartingDeck` 常量与它的 doc 注释(70-76 行)
- `TrySetDeck` 整个方法与它的 doc 注释(416-438 行)

`StartingCollection` 的 doc 注释里若提到「共 15 张 = DeckLimit」,改成「共 15 张」。

- [ ] **Step 3: 清理测试**

删除(它们测的规则已不存在):
- `MetaTests`:`TrySetDeck_ValidatesOwnershipAndDuplicates`、`TrySetDeck_PerElementLimitFive`、
  `TrySetDeck_MinimumFive`、`TrySetDeck_AllFiveElements_Allowed`,以及只服务它们的 `DeckGraph()`
  (**先确认 `DeckGraph()` 没被别的测试用**:`grep -n 'DeckGraph()' Brushblade/Assets/_Project/Tests/MetaTests.cs`)
- `StartingSetupTests`:`StartingDeck_FitsStartingLibraryAndIsOwned`、`StartingDeck_CoversEveryElement`、
  `DeckComponents_StartingDeck_CoversEveryStartingDeckRecipe`
- `StartingSetupTests.StartingDeck_ContainsTutorialChar` —— **这条按 spec 第五节直接删**。
  在它原来的位置留一条注释:

```csharp
        // TODO(2026-09-06):教程演示字的起手保证随出阵一起没了。
        // 此前靠「默认出阵必含 Tutorial.DemoChar」保证首局起手拆得动它;起手改成随机抽之后
        // 这个保证不存在,教程第一步可能无字可拆。用户将另行修改新游戏的起手解锁字卡与教程本身。
        // 见 docs/superpowers/specs/2026-09-06-移除出阵-卡池抽卡-design.md 第五节。
```

改写:
- `StartingSetupTests`:`EventRandomComponents_ComeFromDeckComponents` 与 `NewRunWithDeck` 辅助方法
  改名为 `…FromPoolComponents` / `NewRunWithPool`,语义不变(它们传的就是一个字表)
- `StartingSetupTests.ShippedConfig_StartingDeck_ClearsFirstFloor` → 改名
  `ShippedConfig_StartingCollection_ClearsFirstFloor`,起手改成
  `MetaRules.StartingLibrary(meta, graph, new GameRandom(1))`(meta 用 `EnsureStartingCollection` 铺好)
- `ConfigLoaderTests.cs:227`:`var unlockedChars = MetaRules.StartingDeck;` →
  `var unlockedChars = MetaRules.StartingCollection;`
- `EventTests`:`Stall_RejectsCharOutsideDeck_AndKeepsComponents` → `…OutsidePool…`,
  `Stall_AllowsCharInsideDeck` → `…InsidePool`,`FixedGift_AlsoDeckGated` → `FixedGift_AlsoPoolGated`
  (只改名与注释里的「出阵列表」→「卡池」,断言不动)
- `BattleEngineTests`:`Compose_IngredientOfDeckChar_IsAllowed_AfterDismantling` →
  `Compose_IngredientOfPoolChar_…`,`Compose_CharUnrelatedToDeck_StillRejected` →
  `Compose_CharUnrelatedToPool_…`,`Drop_NoDeck_*` → `Drop_NoPool_*`(它们测的是 `UnlockedChars`,行为不变)

- [ ] **Step 4: 全仓扫一遍残留的「出阵」字样**

```bash
grep -rn '出阵' Brushblade/Assets/_Project Brushblade/Assets/StreamingAssets tools/ docs/design/
```

代码注释里剩下的「出阵」逐条改成「卡池」或删掉。`docs/design/` 里的 GDD 章节**不动**
(那是设计文档的历史记录,不是代码)。`docs/superpowers/specs/` 里本次的 spec 也不动。

- [ ] **Step 5: 全量验证**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ -q
```

Expected: 三条全绿,零 `error CS`。

- [ ] **Step 6: 提交**

```bash
git add -A Brushblade tools
git commit -m "refactor(core): 删除 Deck 字段与出阵校验规则"
```

---

### Task 7: 工装对账与收尾

`tools/balance` 与 `tools/trace` 各自造 BattleConfig,不经 GameRoot,可能还揣着出阵的假设。

**Files:**
- Modify: `tools/balance/Program.cs`、`tools/trace/Program.cs`
- Modify: `CLAUDE.md`(若「规则的唯一来源」一节提到出阵)

**Interfaces:**
- Consumes: 前六个任务的全部改动
- Produces: 无

- [ ] **Step 1: 查工装里的出阵假设**

```bash
grep -n '出阵\|Deck\|StartingLibrary\|RewardPool' tools/balance/Program.cs tools/trace/Program.cs
```

- [ ] **Step 2: 逐处改成卡池语义**

把手写的出阵卡组换成「已解锁卡池」(仿真画像里那份字表原样保留,只改变量名与注释),
`StartingLibrary` 的调用补上 `graph` 与 `GameRandom`(工装用固定种子,保证仿真可复现)。

⚠ 项目 memory `hardcoded_char_lists_go_stale` 与 `balance_harness_profiles_were_bogus`:
工装里的手写字表是幽灵字重灾区,改完顺手核一遍每个字都还在 chars.json 里 ——
幽灵字不会让任何测试变红。

- [ ] **Step 3: 跑工装**

```bash
cd tools/balance && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet run --nologo 2>&1 | tail -30
```

Expected: 能跑完不崩。数值会变(战利品曲线换了、起手随机了),这是预期的 —— 记下新读数供后续平衡参考,别当成 bug 去修。

- [ ] **Step 4: 更新 CLAUDE.md**

若「规则的唯一来源」或别的小节提到出阵/配方拍板与出阵的关系,改述为卡池。
新增一条到该节:

```markdown
- 起手抽卡与战利品共用一条稀有度权重:`MetaRules.RarityWeights`(2026-09-06 起,
  出阵已废止)。起手 = 五行各一张加权抽 + 最高档保底一张,不去重。
```

- [ ] **Step 5: 最终全量验证**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ -q
```

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "chore(tools): 工装与 CLAUDE.md 跟进卡池抽卡"
```

---

## 交付后需要人眼验收的

离线编译只证明「能编过」,不证明「接上了」。Presentation 没有自动化测试,下面几条只能在 Unity 里看:

1. **收集页侧栏**卡池概览渲染正常,各档字数与百分比对得上,没有稀有度档名印成英文枚举名。
2. **收集页网格**拖拽已经拆掉,列表仍能正常上下滚(原先竖向手势要靠 `DragToDeck` 转发给
   `ScrollRect`,拖拽删掉后这条转发链也没了 —— 确认没把 `ScrollRect` 一起弄坏)。
3. **主界面角色栏**卡池摘要显示正常。
4. **登塔起手** 6 张:五系齐全,第 6 张是当前最高档,重复的那张在字库里占两个卡位。
5. **真机字形**:新文案没有豆腐块。
6. **教程**:按 spec 第五节,这一条**预期是坏的**,交给用户后续处理。
