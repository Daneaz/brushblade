# 开场走调度 · 同速优先级反转 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 战斗开场走调度器(玩家不再有免费先手),同速时行动优先级改为「玩家 > 召唤物 > 敌方」,并把开场那几拍的过程完整播给玩家看。

**Architecture:** Core 侧四项改动必须**一起落地**(实测:单独任一项都会大面积红,一起才自洽);构造函数末尾用推进循环替代 `StartTurn()`,`StartTurn` 由 `BeginPlayerTurn` 提供 —— 这是 97 个测试构造点一个都不用改的关键。表现层靠 Core 记下的 `OpeningSteps` 逐拍回放开场。

**Tech Stack:** Unity 6000.5.2f1 / C# 9(LangVersion 锁 9,**不可用 C# 10+ 语法**)/ uGUI(非 TMP)/ NUnit

**Spec:** `docs/superpowers/specs/2026-08-17-开场走调度与同速优先级反转-design.md`

## Global Constraints

- **Core / Data 禁止引用 UnityEngine**(asmdef `noEngineReferences: true`)。
- **Core 全程整数**,不许 float/double 中间值 —— Unity Mono 与 .NET 8 中间精度不同,配取整会整级翻车(项目已因此翻车过)。浮点只允许在 Presentation。
- **C# LangVersion 锁 9**。
- **禁用 emoji** —— 字体子集补不出来,上线渲染成空框。
- **玩家可见文字必须能进字体子集**(`subset_fonts.py` 扫 `.cs` 字符串字面量,不扫注释)。本次不新增玩家可见文字。
- **测试断言禁用 `Is.AnyOf` / `Is.All.AnyOf`**(Unity 自带 NUnit 没有,工装绿≠编辑器绿)。多选一用 `Is.EqualTo(a).Or.EqualTo(b)`。
- **测试定位仓库根只用 `TestContext.CurrentContext.TestDirectory`**,禁用 `AppContext.BaseDirectory`。
- **测试代码禁止直接引用 Newtonsoft**。
- **改完 Presentation 必须跑离线编译**(`tools/prescompile`),只看 `error CS`,`warning MSB3245` 忽略。
- 提交信息用 conventional commits(中文正文)。
- 当前分支 `fix/opening-goes-through-scheduler`,spec 已在 `a9fe6a0`。

## 铁律:不许迁就断言改数字

本计划会让 19 条既有测试变红。**每一条都必须先推演出新的期望值、再跑测试验证推演** ——
不许跑一遍看实际值然后填进去。

理由(spec §5.8 的实例):`Speed200_ActsTwicePerTurn` 断言「速度 200 的敌人一回合动 2 次」。
它红了,但**断言是对的**,错的是 helper 的构造顺序。如果直接把 2 改成 1,就把
「速度决定出手频率」这个设计意图的验证悄悄丢掉了。

**如果实际值与你的推演不符,停下来查为什么**,把结论写进报告。

---

## 文件结构

| 文件 | 责任 | 任务 |
|---|---|---|
| `Core/EnemyDef.cs` | `EnemyDef.Speed` 配置通道;`EnemyState` 从 def 取速度 | 1 |
| `Core/BattleEngine.cs` | priority 重排、构造函数推进循环、`OpeningStep`/`OpeningSteps`、召唤物上场即满格、两处注释改写 | 2 |
| `Core/TurnScheduler.cs` | 一处「方向不要再调回去」注释改写 | 2 |
| `Tests/TurnSchedulerTests.cs` | 4 个 priority helper + 3 条断言重算 | 2 |
| `Tests/BattleEngineTests.cs` | 8 条速度节奏测试改用 `EnemyDef.Speed` | 2 |
| `Tests/AtbTimingTests.cs` | 4 条 `LastAdvanceTicks_*` 重新设计 + 新增开场用例 | 2、3 |
| `Tests/RunEngineTests.cs` | `CarriedSummons_CountTowardCap` 改成反映新行为 | 2 |
| `Presentation/BattleView.cs` | `OpeningRoutine` 回放 + 两处注释改写 | 4 |

**依赖:** 1(独立)→ 2(用 1 的通道修 8 条速度测试)→ 3(守新口径)→ 4(表现层)→ 5(收尾)

---

### Task 1: `EnemyDef.Speed` 配置通道

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EnemyDef.cs:74-85`(`EnemyDef` 构造)、`:301`(`EnemyState` 构造)
- Test: `Brushblade/Assets/_Project/Tests/AtbTimingTests.cs`

**Interfaces:**
- Consumes: 无
- Produces:
  - `EnemyDef.Speed` → `int`(配置的基础速度;`0` = 未配置)
  - `EnemyDef(string id, Element element, int maxHp, int attack, EnemyAbility ability = EnemyAbility.None, IReadOnlyList<BossPhaseDef> phases = null, int defense = 0, int speed = 0)` — **`speed` 加在末尾**
  - `EnemyState.Speed` 初始值改为「`def.Speed > 0` 时取它,否则 100」

> 为什么需要它:见 spec §5.8。Task 2 让构造函数在开场就跑推进,而 8 条速度测试是
> 「先构造、后改 `Enemies[0].Speed`」—— 速度改得太晚,开场那一拍敌人以默认 100 参与。
> 修法只能把速度移到构造前,而现在没有这个通道。
>
> **本任务不改任何现有数值**:默认 `speed = 0` 回落 100,`enemies.json` 一个字不动。

- [ ] **Step 1: 写失败测试**

追加到 `Brushblade/Assets/_Project/Tests/AtbTimingTests.cs` 类内末尾。`Engine` / `Dummy` 是该文件已有的 helper。

```csharp
        // ===== EnemyDef.Speed 配置通道(2026-08-17,spec §5.8)=====

        [Test]
        public void EnemyDefSpeed_FlowsIntoEnemyState()
        {
            var fast = new EnemyDef("疾", Element.Heart, 999, 0, speed: 200);
            var engine = Engine(new[] { fast });

            Assert.That(engine.Enemies[0].Speed, Is.EqualTo(200));
        }

        [Test]
        public void EnemyDefSpeed_Unset_FallsBackToBaseline()
        {
            // 不配 speed 的怪仍是基准 100 —— 全部既有字表都走这条路径,数值一个不能变
            var engine = Engine(new[] { Dummy() });

            Assert.That(engine.Enemies[0].Speed, Is.EqualTo(100));
        }
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "FullyQualifiedName~EnemyDefSpeed"
```

Expected: 编译失败 —— `EnemyDef` 没有名为 `speed` 的参数

- [ ] **Step 3: 给 `EnemyDef` 加 `Speed`**

`Core/EnemyDef.cs`,在 `Defense` 属性下面加:

```csharp
        /// <summary>配置的基础速度(2026-08-17,spec §5.8)。`0` = 未配置,由
        /// <see cref="EnemyState"/> 回落到基准 100 —— 眼下**全部字怪都走回落**,
        /// `enemies.json` 里没有 speed 字段,本次也不加。
        ///
        /// 打开这个通道是为了让速度能在**构造之前**定下来:自 2026-08-17 起
        /// `BattleEngine` 构造函数就会跑开场推进,而「先构造、后改 Enemies[0].Speed」
        /// 的写法会让敌人以默认 100 参与开场那一拍(spec §5.8 的实例)。</summary>
        public int Speed { get; }
```

构造函数签名与赋值(`speed` 加在**末尾**,既有调用点一个不受影响):

```csharp
        public EnemyDef(string id, Element element, int maxHp, int attack,
            EnemyAbility ability = EnemyAbility.None, IReadOnlyList<BossPhaseDef> phases = null,
            int defense = 0, int speed = 0)
        {
            Id = id;
            Element = element;
            MaxHp = maxHp;
            Attack = attack;
            Ability = ability;
            Phases = phases ?? System.Array.Empty<BossPhaseDef>();
            Defense = defense;
            Speed = speed;
        }
```

- [ ] **Step 4: 让 `EnemyState` 从 def 取速度**

`Core/EnemyDef.cs` 的 `internal EnemyState(EnemyDef def, int phaseJitterPercent, GameRandom random)`(约 `:301`),在 `Def = def;` **下面**加一行:

```csharp
            // 配置速度优先,未配置(0)回落基准 100(2026-08-17,spec §5.8)。
            // Speed 保留 setter:快照恢复路径显式赋值,减速走 SpeedModifier 状态不碰这里。
            if (def.Speed > 0) Speed = def.Speed;
```

> ⚠ 用 `if (def.Speed > 0) Speed = ...` 而不是无条件赋值 —— `Speed` 的字段初始化器
> 已经是 `= 100`,无条件赋值会把未配置的怪打成 0。

- [ ] **Step 5: 跑测试确认通过**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "FullyQualifiedName~EnemyDefSpeed"
```

Expected: 2 passed

- [ ] **Step 6: 跑全量,确认零回归**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: **1064 passed**(既有 1062 + 新增 2),Failed 0。本任务是纯追加,一条都不该红。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/EnemyDef.cs \
        Brushblade/Assets/_Project/Tests/AtbTimingTests.cs
git commit -m "feat(core): 打开 EnemyDef.Speed 配置通道

默认 0 → 回落基准 100,enemies.json 一个字不动,现有数值一个不变。

打开它是为了让敌人速度能在**构造之前**定下来:下一步要让 BattleEngine 构造函数
跑开场推进,而既有测试的「先构造、后改 Enemies[0].Speed」会让敌人以默认 100
参与开场那一拍 —— 没有这个通道,那 8 条速度节奏测试只能迁就断言改数字,
把「速度 200 = 出手两倍」的验证丢掉。见 spec §5.8。"
```

---

### Task 2: Core 四项改动 + 19 条测试处置

**Files:**
- Modify: `Core/BattleEngine.cs` — `BuildSlots` priority、构造函数、`OpeningStep`/`OpeningSteps`、召唤物上场即满格、`:717-723` 与 `:1499-1502` 两处注释
- Modify: `Core/TurnScheduler.cs:34-37` — 一处注释
- Modify: `Tests/TurnSchedulerTests.cs` — 4 个 helper + 3 条断言
- Modify: `Tests/BattleEngineTests.cs` — 8 条速度节奏测试
- Modify: `Tests/AtbTimingTests.cs` — 4 条 `LastAdvanceTicks_*`
- Modify: `Tests/RunEngineTests.cs` — `CarriedSummons_CountTowardCap`

**Interfaces:**
- Consumes: `EnemyDef(..., int speed = 0)`(Task 1)
- Produces:
  - `BattleEngine.OpeningStep` — `readonly struct`,字段 `Actor`(`ActorRef`)、`Ticks`(`int`)、`PlayerMeter`(`int`)、`SummonMeters`(`IReadOnlyList<int>`)、`EnemyMeters`(`IReadOnlyList<int>`)、`Events`(`IReadOnlyList<BattleEvent>`)
  - `BattleEngine.OpeningSteps` → `IReadOnlyList<OpeningStep>`(按发生顺序;**不进快照**)

> **这四项必须在同一个提交里落地。** 实测(spec §2.2):只去掉免费先手红 537 条、
> 只反转 priority 红 199 条、两个一起红 32 条、再加召唤物满格红 16 条。
> 中间任何一步单独提交都是大片红的状态。

- [ ] **Step 1: priority 重排**

`Core/BattleEngine.cs` 的 `BuildSlots`:

```csharp
            var slots = new List<SchedulerSlot>
            {
                new(ActorRef.Player, EffectivePlayerSpeed, PlayerActionMeter, 0),
            };
```

召唤物那行 `0` → `1`:

```csharp
                slots.Add(new SchedulerSlot(new ActorRef(ActorKind.Summon, s),
                    _summons[s].Speed, _summons[s].ActionMeter, 1));
```

敌人那行 `1 : 2` → `2 : 3`:

```csharp
                int priority = enemy.Def.Ability == EnemyAbility.Buff ? 2 : 3;
```

- [ ] **Step 2: 改写 `BuildSlots` 的方向警告注释**

把 `BuildSlots` 上方那段以「⚠ 优先级(并列时的排序主键,小者先)是**召唤物 0 → Buff 敌 1 → 其余敌 2 → 玩家 3**」开头、到「没有任何特例」结束的整段替换为:

```csharp
        /// ⚠ 优先级(并列时的排序主键,小者先)是**玩家 0 → 召唤物 1 → Buff 敌 2 → 其余敌 3**
        /// ——玩家排**最先**(2026-08-17 用户拍板,推翻 2026-08-15 的反向口径)。
        ///
        /// 2026-08-15 那次把玩家定成最后,并留下「方向不要再调回去」的警告,理由是
        /// 「玩家优先会让它每次推进都抢在敌人前面把行动权收回去」。**那个诊断把病根归错了**:
        /// 病根是「玩家优先 + 构造函数给的免费先手」这个**组合**,不是玩家优先本身。
        /// 实测(spec §2.2,各跑一次全量 1062 条):
        ///   只反转 priority(保留免费先手)→ 红 199 条,确实复现了那个现象
        ///   只去掉免费先手(保留玩家最后)→ 红 537 条(AP=0、字库空)
        ///   两个一起                      → 红 32 条,且序列变成干净的「玩家 → 召唤 → 敌」
        /// 当初三种「开局记账」(创建时先手 / 玩家记负债 / 消费一拍)全是在抵消那次免费先手;
        /// 把免费先手删掉(见构造函数),它们一个都不需要。
        ///
        /// 这里只管**同速并列**。速度不同时先比谁先攒满(TicksUntilAnyFull),
        /// 所以敌人速度高于玩家时它先动 —— 那是速度该有的表达力,旧的免费先手把它压平了。
```

- [ ] **Step 3: 改写 `TurnScheduler.cs` 的同款警告**

`Core/TurnScheduler.cs:34-37`,把 `SchedulerSlot` 上方以「⚠ **玩家排最后**」开头的整段替换为:

```csharp
    /// ⚠ **玩家排最先**(2026-08-17,推翻 2026-08-15 的反向口径):同速并列时
    /// 玩家 0 → 召唤物 1 → Buff 敌 2 → 其余敌 3。旧口径让玩家排最后,并警告「方向不要
    /// 再调回去」—— 那个诊断把病根归错了:病根是「玩家优先 + 构造函数给的免费先手」
    /// 这个组合。免费先手已随本次改造删除,玩家优先因此不再需要任何记账补偿。
    /// 完整推演与实测数字见 BattleEngine.BuildSlots 的注释与
    /// docs/superpowers/specs/2026-08-17-开场走调度与同速优先级反转-design.md §二。
```

- [ ] **Step 4: 加 `OpeningStep` 结构与 `OpeningSteps` 属性**

`Core/BattleEngine.cs`,在 `BattlePhase` 枚举**之后**、`BattleEngine` 类之前加结构:

```csharp
    /// <summary>开场推进的一拍(2026-08-17)。构造函数在开场就把推进跑完了,表现层靠这些
    /// 数据把过程逐拍播出来 —— 否则玩家只会看到「进战斗即已打完」(spec §5.7)。
    /// 字段与表现层每播一拍所需的东西一一对应:谁动、跨了几拍(定条动画时长)、
    /// 推进后全场计量器(条的终点)、这一拍产生的事件(交给 Juice 播)。</summary>
    public readonly struct OpeningStep
    {
        public ActorRef Actor { get; }
        public int Ticks { get; }
        public int PlayerMeter { get; }
        public IReadOnlyList<int> SummonMeters { get; }
        public IReadOnlyList<int> EnemyMeters { get; }
        public IReadOnlyList<BattleEvent> Events { get; }

        public OpeningStep(ActorRef actor, int ticks, int playerMeter,
            IReadOnlyList<int> summonMeters, IReadOnlyList<int> enemyMeters,
            IReadOnlyList<BattleEvent> events)
        {
            Actor = actor;
            Ticks = ticks;
            PlayerMeter = playerMeter;
            SummonMeters = summonMeters;
            EnemyMeters = enemyMeters;
            Events = events;
        }
    }
```

在 `BattleEngine` 类内(`LastAdvanceTicks` 附近)加字段与属性:

```csharp
        private readonly List<OpeningStep> _openingSteps = new();

        /// <summary>开场每一拍的回放数据,按发生顺序(2026-08-17)。同速开局只有一条
        /// (玩家自己那一拍);携带满格召唤物或敌人更快时会有多条。
        /// **不进快照** —— 断点续爬恢复的是战斗中途,没有「开场」可回放。</summary>
        public IReadOnlyList<OpeningStep> OpeningSteps => _openingSteps;

        /// <summary>把当前这一拍的状态拷成回放数据。必须逐个拷值 ——
        /// `_events` 下一拍开头就被 Clear,存引用会拿到空列表。</summary>
        private OpeningStep CaptureOpeningStep()
        {
            var summons = new int[_summons.Count];
            for (int i = 0; i < summons.Length; i++) summons[i] = _summons[i].ActionMeter;
            var enemies = new int[_enemies.Count];
            for (int i = 0; i < enemies.Length; i++) enemies[i] = _enemies[i].ActionMeter;
            return new OpeningStep(LastActor, LastAdvanceTicks, PlayerActionMeter,
                summons, enemies, new List<BattleEvent>(_events));
        }
```

- [ ] **Step 5: 构造函数改成推进循环**

`Core/BattleEngine.cs` 构造函数末尾,把

```csharp
            Phase = BattlePhase.PlayerTurn;
            StartTurn();
```

替换为:

```csharp
            Phase = BattlePhase.PlayerTurn;
            // 开场走调度(2026-08-17):不再直接开玩家回合 —— 那是改造前 AP 制的遗留,
            // 等于给玩家一次免费先手,而全场计量器此刻都还是 0。现在全场从 0 攒,
            // 谁先满谁先动;轮到玩家时 AdvanceOnce 的玩家分支会跑
            // BeginPlayerTurn → StartTurn(AP / 发牌 / Turn+1),所以这里不必直接调 StartTurn。
            //
            // 每拍都记进 _openingSteps:AdvanceOnce 每次开头都 _events.Clear(),不记的话
            // 开场抢先行动的单位(携带的满格召唤物、以后的高速敌人)表现全丢,玩家只会
            // 看到「进战斗即已打完」。见 spec §5.7。
            //
            // ⚠ 这个循环可能让战斗在构造函数返回前就分出胜负(携带满格召唤物秒掉弱敌),
            // Phase 会是 Won —— 表现层必须兜住(spec §5.7)。
            bool more;
            do
            {
                more = AdvanceOnce();
                _openingSteps.Add(CaptureOpeningStep());
            } while (more);
```

- [ ] **Step 6: 召唤物上场即满格**

`Core/BattleEngine.cs` 的 `ApplyEffects` → `EffectKind.Summon` 分支,把 `new SummonState(...)` 上方那段以「新召唤物从 0 起攒计量器」开头的注释整段替换,并在 `newborn` 创建后补一行:

```csharp
                            // 新召唤物**上场即满格**(2026-08-17 用户拍板):召唤术的价值就在
                            // 「立刻有个肉盾并反击」,从 0 起攒会让它召出那一轮完全不动。
                            //
                            // 这是恢复 2026-08-15 删掉的那个头寸。当时删它的理由是「那是在给
                            // 反向的 tie-break 打补丁」—— 那句话在当时是对的(priority 刚被改成
                            // 「玩家最后」,召唤物排最先,从 0 起攒也能同回合出手)。2026-08-17
                            // 把方向调回「玩家最先」之后,玩家插到了召唤物前面,这个头寸就重新
                            // 成为必需:实测它让失败测试从 32 条降到 16 条(spec §2.3)。
                            // 它不是补丁,是「玩家优先」那一整套设计的组成部分。
                            var newborn = new SummonState(effect.SummonChar, attacker, value,
                                ScaleByAttack(MetaRules.ScaleByCardLevel(effect.SummonAttack, cardLevel)),
                                effect.Passive);
                            newborn.ActionMeter = TurnScheduler.Threshold;
```

> ⚠ 这一行要放在 `if (AliveSummons() < SummonCap)` **之前** —— 两条上场路径
> (`_summons.Add` 与原地顶替 `_summons[slot] = newborn`)都要覆盖。

- [ ] **Step 7: 跑测试,核对失败清单**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo 2>&1 | grep -iE "^\s*failed |^Failed!|^Passed!" | sed 's/^[[:space:]]*//' | sort -u
```

Expected: **16 条失败**,且清单与 spec §5.6 的表**逐条吻合**(此时还没改 `TurnSchedulerTests`
的 helper,所以那 3 条还没红)。

⚠ **如果清单不吻合**(多了、少了、或换了别的测试),说明前六步有出入 —— 停下来核对,
不要往下改测试。

- [ ] **Step 8: 改 4 个 priority helper**

`Tests/TurnSchedulerTests.cs:17-26`:

```csharp
        private static SchedulerSlot Player(int speed, int meter = 0) =>
            new(ActorRef.Player, speed, meter, 0);

        private static SchedulerSlot Enemy(int index, int speed, int meter = 0) =>
            new(new ActorRef(ActorKind.Enemy, index), speed, meter, 3);

        private static SchedulerSlot Summon(int index, int speed, int meter = 0) =>
            new(new ActorRef(ActorKind.Summon, index), speed, meter, 1);

        private static SchedulerSlot Buffer(int index, int speed, int meter = 0) =>
            new(new ActorRef(ActorKind.Enemy, index), speed, meter, 2);
```

同时把这四个 helper 上方那段「玩家排最后…方向不要再调回去」的注释改写成指向新口径
(可引用 `BuildSlots` 的注释,不必重复整段推演)。

- [ ] **Step 9: 重算 `TurnSchedulerTests` 的 3 条断言**

这三条是纯函数测试,推演直接可得:

`SameSpeed_AlternatesInPriorityOrder` —— `Player(100), Enemy(0, 100)`(速度都 100,meter 0):
推 1 拍 → 全场 100 → 玩家 priority 0 赢 → 玩家动(玩家 0、敌 100)→ 敌满格 → 敌动 →
全场 0 → 推 1 拍 → 玩家赢。序列 **玩家, 敌, 玩家, 敌**:

```csharp
        [Test]
        public void SameSpeed_AlternatesInPriorityOrder()
        {
            // 玩家排最先(2026-08-17,推翻 2026-08-15 的反向口径):同速并列时玩家赢,
            // 序列是「玩家 → 敌」交替。免费先手已随构造函数改造删除,所以玩家不会连动两次
            // ——那正是旧警告担心的现象,病根在免费先手而非本排序(见 BuildSlots 注释)。
            var slots = new List<SchedulerSlot> { Player(100), Enemy(0, 100) };

            var seq = Sequence(slots, 4);

            Assert.That(seq[0].Kind, Is.EqualTo(ActorKind.Player));
            Assert.That(seq[1].Kind, Is.EqualTo(ActorKind.Enemy));
            Assert.That(seq[2].Kind, Is.EqualTo(ActorKind.Player));
            Assert.That(seq[3].Kind, Is.EqualTo(ActorKind.Enemy));
        }
```

`TieBreak_SummonThenBufferThenOthersThenPlayer` —— 四个单位同速同 meter,按新 priority
玩家 0 → 召唤 1 → Buff 敌 2 → 普通敌 3。**方法名也要改**(它写的是旧顺序):

```csharp
        [Test]
        public void TieBreak_PlayerThenSummonThenBufferThenOthers()
        {
            // 全部同速同计量器 —— 纯考排序契约。这条锁死后,谁「顺手优化」调度器
            // 都会立刻变红。顺序:玩家 0 → 召唤物 1 → Buff 敌 2 → 其余敌 3(2026-08-17)
            var slots = new List<SchedulerSlot>
            {
                Enemy(0, 100), Buffer(1, 100), Summon(0, 100), Player(100),
            };

            var seq = Sequence(slots, 4);

            Assert.That(seq[0], Is.EqualTo(ActorRef.Player));
            Assert.That(seq[1], Is.EqualTo(new ActorRef(ActorKind.Summon, 0)));
            Assert.That(seq[2], Is.EqualTo(new ActorRef(ActorKind.Enemy, 1)), "Buff 敌先于普通敌");
            Assert.That(seq[3], Is.EqualTo(new ActorRef(ActorKind.Enemy, 0)));
        }
```

`AlreadyFull_ActsWithoutAdvancingTime` —— `Player(100, meter: 120), Enemy(0, 100, meter: 110)`
两者都已满格,按新 priority 玩家赢:

```csharp
        [Test]
        public void AlreadyFull_ActsWithoutAdvancingTime()
        {
            // 已经满格的不该再推进时间:否则同一 tick 内的第二个行动者会白拿一次累积。
            // 两者都已满格时按优先级判并列:玩家(0)先于敌人(3)——2026-08-17 反转
            var slots = new List<SchedulerSlot> { Player(100, meter: 120), Enemy(0, 100, meter: 110) };

            var step = TurnScheduler.Advance(slots);

            Assert.That(step.Actor.Kind, Is.EqualTo(ActorKind.Player));
            Assert.That(step.Meters[0], Is.EqualTo(20), "行动者(玩家)扣 100");
            Assert.That(step.Meters[1], Is.EqualTo(110), "其他人(敌人)原地不动");
        }
```

- [ ] **Step 10: 8 条速度节奏测试改用 `EnemyDef.Speed`**

这 8 条的**断言全部不改**,只把速度从「构造后赋值」移到「构造前配置」。

`Tests/BattleEngineTests.cs` 的 `HastedEngine`(约 `:1953`):

```csharp
        // 2026-08-17:速度改为在 EnemyDef 里配好再构造。构造函数现在会跑开场推进
        // (spec §5.2),先构造再改 Enemies[0].Speed 会让敌人以默认 100 参与开场那一拍,
        // 于是它少攒 100、这一回合少动一次 —— 断言没错,是构造顺序错(spec §5.8)。
        private static BattleEngine HastedEngine(int speed) =>
            new BattleEngine(Graph(), Config(), new[] { "灯" },
                Array.Empty<string>(), new[] { MetalBossWithSpeed(speed) }, 42);
```

`MetalBossWithSpeed` 按 `MetalBoss()` 的定义照抄一份、末尾加 `speed: speed`
(去读 `MetalBoss()` 怎么写的,把它的实参原样搬过来)。

`SlowedEngine` 与 `Slow_*` / `SpeedModifiers_StackAdditively` 那几条同理 ——
**逐条检查它有没有「先构造后改 `Speed`」**:
- 有 → 移到 `EnemyDef` 的 `speed:` 参数,断言不改
- 没有(走 `SpeedModifier` 状态,由 `Cast` 施加)→ 那是纯相位变化,按 Step 11 的方法重算

- [ ] **Step 11: 重算 Buff 敌 2 条 + `LastAdvanceTicks` 4 条**

这 6 条走的是**相位变化**(不是构造顺序),必须逐条推演。推演方法:

1. 写下构造完的状态:玩家 meter、各敌人 meter、各召唤物 meter
   (同速时构造完是「玩家 0,其余 100」—— 玩家在开场消费了一拍)
2. 逐次 `Advance`:先看有没有人已满格(`FirstFull`),没有才推进
   `ticks = min ceil((100 − meter) / speed)`
3. 满格者按 priority(玩家 0 / 召唤 1 / Buff 敌 2 / 其余敌 3)取最小

`LastAdvanceTicks_StartsAtZero` 的**前提已经消失** —— 构造函数现在就会推进一次,
`LastAdvanceTicks` 不再是 0。这条要**重新设计**而不是改数字:

```csharp
        [Test]
        public void LastAdvanceTicks_RecordsOpeningAdvance()
        {
            // 2026-08-17:构造函数现在就会跑开场推进,所以「战斗刚开始 LastAdvanceTicks 为 0」
            // 这个前提已经不成立。改为断言它记下了开场那一拍 —— 同速开局恰好 1 拍。
            var engine = Engine(new[] { Dummy() });

            Assert.That(engine.LastAdvanceTicks, Is.EqualTo(1));
        }
```

其余 3 条按同法推演后重写。**每条的推演过程都要写进报告。**

- [ ] **Step 12: `CarriedSummons_CountTowardCap` 改成反映新行为**

`Tests/RunEngineTests.cs:919` 附近。它现在红在 `Expected: SummonCapFull, But was: BattleOver`
—— 上一层带过来 6 只满格召唤物,构造函数里的开场推进让它们直接把新战斗的弱敌全秒了,
`Phase` 已经是 `Won`(spec §5.7)。

**这是正确的新行为**,不是缺陷。把那几行断言改成:先断言新战斗确实开局即胜
(`run.Battle.Phase == BattlePhase.Won`),再把「满编强阻断」那两条断言挪到**上一场**
(那时召唤物已满编而战斗还没结束)去验,或者拆成一条独立测试用不会被秒掉的敌人。

选哪种由你定,但报告里要说明:**「携带满格召唤物开局即胜」这个行为被哪条测试守住了**
—— 不能因为改了这条测试就让 §5.7 那个行为失去覆盖。

- [ ] **Step 13: 跑全量,必须全绿**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: **Failed 0**。总数应为 1064(Task 1 的 1064)± 你在 Step 12 拆分的条数。

- [ ] **Step 14: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs \
        Brushblade/Assets/_Project/Core/TurnScheduler.cs \
        Brushblade/Assets/_Project/Tests/TurnSchedulerTests.cs \
        Brushblade/Assets/_Project/Tests/BattleEngineTests.cs \
        Brushblade/Assets/_Project/Tests/AtbTimingTests.cs \
        Brushblade/Assets/_Project/Tests/RunEngineTests.cs
git commit -m "feat(core): 开场走调度、同速时玩家优先、召唤物上场即满格

四项一起落地 —— 实测单独任一项都会大面积红(spec §2.2):
  只反转 priority       → 红 199(玩家 EndTurn 后立刻抢回行动权)
  只去掉免费先手        → 红 537(AP=0、字库空)
  两个一起              → 红 32
  再加召唤物上场即满格  → 红 16

推翻 2026-08-15 的「方向不要再调回去」:那个诊断把病根归给「玩家优先」,实际
病根是「玩家优先 + 免费先手」的组合。免费先手删掉后,当初三种开局记账一个都不需要。

构造函数末尾用推进循环替代 StartTurn() —— StartTurn 由 BeginPlayerTurn 提供,
这是 97 个测试构造点一个都不用改的关键。同时记 OpeningSteps 供表现层回放开场。

召唤物上场即满格是恢复 2026-08-15 删掉的头寸:当时删它的理由在当时成立(召唤物
排最先),方向调回来后它重新成为必需。"
```

---

### Task 3: 新增测试守住新口径

**Files:**
- Modify: `Brushblade/Assets/_Project/Tests/AtbTimingTests.cs`

**Interfaces:**
- Consumes: `EnemyDef(..., int speed = 0)`(Task 1)、`BattleEngine.OpeningSteps`(Task 2)
- Produces: 无

> 这些测试守的是「口径能不能被后人误改回去」。没有它们,priority 被当成绝对顺序、
> 或开场推进被删掉,都不会变红。

- [ ] **Step 1: 写测试**

追加到 `Tests/AtbTimingTests.cs` 类内末尾:

```csharp
        // ===== 开场走调度(2026-08-17,spec 口径 2 / 7)=====

        [Test]
        public void Opening_SameSpeed_PlayerActsFirst()
        {
            // 同速开局:全场从 0 攒,同拍满格,玩家 priority 最小 → 玩家先动。
            // 构造完玩家计量器已被扣过 100(它真的行动了),而敌人停在满格等下一拍。
            var engine = Engine(new[] { Dummy() });

            Assert.That(engine.LastActor, Is.EqualTo(ActorRef.Player));
            Assert.That(engine.PlayerActionMeter, Is.EqualTo(0), "玩家攒满后消费了那一拍");
            Assert.That(engine.Enemies[0].ActionMeter, Is.EqualTo(TurnScheduler.Threshold));
        }

        [Test]
        public void Opening_EnemyFullInOneTickWhilePlayerIsNot_EnemyActsFirst()
        {
            // 口径 7:tie-break 只管同速。但「敌人先动」的条件不是「敌人更快」——
            // ticks 是**全场共用的最小值**,只要玩家一拍能攒满(speed >= 100),
            // 敌人再快也是同拍满格、tie-break 生效、玩家赢。
            // 真实条件是**玩家一拍攒不满而敌人能**:玩家 25 要 4 拍,敌人 400 只要 1 拍。
            // 没有这条,后人把 priority 当成绝对顺序也不会变红。
            var engine = Engine(new[] { new EnemyDef("疾", Element.Heart, 999, 0, speed: 400) },
                new BattleConfig { PlayerMaxHp = 999, PlayerSpeed = TurnScheduler.MinSpeed });

            Assert.That(engine.OpeningSteps[0].Actor.Kind, Is.EqualTo(ActorKind.Enemy));
        }

        [Test]
        public void Opening_FasterEnemyButPlayerFullInOneTick_PlayerStillActsFirst()
        {
            // 上一条的反面,也是最容易被误解的一条:敌人速度是玩家的 4 倍,**玩家仍先动** ——
            // 双方都 1 拍满格,落到 tie-break。速度差体现为之后敌人连动两次,不是抢第一拍。
            var engine = Engine(new[] { new EnemyDef("疾", Element.Heart, 999, 0, speed: 400) });

            Assert.That(engine.OpeningSteps[0].Actor, Is.EqualTo(ActorRef.Player));
        }

        [Test]
        public void Opening_SlowedPlayer_EnemyActsFirst()
        {
            // 与上面两条互补:敌人只是基准 100,单靠玩家慢(25 要 4 拍)就够让敌人抢到第一拍。
            // 这条走 PlayerSpeed 配置通道,证明「敌人先动」不需要敌人被配高速。
            var engine = Engine(new[] { Dummy() },
                new BattleConfig { PlayerMaxHp = 999, PlayerSpeed = TurnScheduler.MinSpeed });

            Assert.That(engine.OpeningSteps[0].Actor.Kind, Is.EqualTo(ActorKind.Enemy));
        }

        [Test]
        public void Opening_RecordsReplayDataForEveryStep()
        {
            // spec §5.7:构造函数把开场推进跑完了,表现层要靠 OpeningSteps 把过程演出来。
            // 每条都必须带齐回放所需的四样:谁动、跨几拍、推进后计量器、该拍事件。
            var engine = Engine(new[] { Dummy() });

            Assert.That(engine.OpeningSteps, Is.Not.Empty);
            var first = engine.OpeningSteps[0];
            Assert.That(first.Ticks, Is.GreaterThan(0), "开场必然跨了至少一拍");
            Assert.That(first.EnemyMeters.Count, Is.EqualTo(engine.Enemies.Count));
            Assert.That(first.Events, Is.Not.Empty, "每批至少带 ActorActed 段首标记");
        }

        [Test]
        public void Opening_SnapshotRestore_HasNoOpeningSteps()
        {
            // 口径 8:断点续爬恢复的是战斗中途,没有「开场」可回放 —— 表现层据此跳过回放
            var engine = Engine(new[] { Dummy() });
            var restored = BattleEngine.Restore(engine.Capture(), Graph(),
                new BattleConfig { PlayerMaxHp = 999 }, null);

            Assert.That(restored.OpeningSteps, Is.Empty);
        }
```

> ⚠ `BattleEngine.Restore` 的实参个数与类型**去 `Core/BattleEngine.cs:421` 附近核对**
> (`RunEngine.cs:184` 有一个真实调用点可以照抄)。如果签名不符就按实际的写,
> 并在报告里说明。

```csharp
        [Test]
        public void Summon_EntersFieldAtFullMeter()
        {
            // 口径 3:召唤术的价值在「立刻有个肉盾并反击」。这条守住那个头寸不被再删一次
            // ——它 2026-08-15 被删过,理由在当时成立(召唤物排最先),方向调回来后失效。
            // 用真实召唤路径(Cast 一个召唤字)而不是直接构造 SummonState。
        }
```

> ⚠ 上面这条只给了意图与注释。**召唤字的 id、`Graph()` 里要不要补这个字、
> `Cast` 的调用形式,你去看 `Tests/SummonPassiveTests.cs` 或 `BattleEngineTests.cs`
> 里现成的召唤测试怎么写,照那个模式补完。** 断言是
> `engine.Summons[0].ActionMeter == TurnScheduler.Threshold`。

- [ ] **Step 2: 跑测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q --filter "FullyQualifiedName~Opening_|FullyQualifiedName~Summon_EntersFieldAtFullMeter"
```

Expected: 全部 passed。**任何一条红都说明 Task 2 的实现与口径不符** —— 回去查 Task 2,
不要改这些断言来迁就。

- [ ] **Step 3: 跑全量 + 提交**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
git add Brushblade/Assets/_Project/Tests/AtbTimingTests.cs
git commit -m "test(core): 守住开场走调度与新 tie-break 的六条口径

同速玩家先动、敌人更快时它先动(两条通道:配置高速 / 玩家被减速)、
OpeningSteps 带齐回放数据、存档恢复无开场数据、召唤物上场即满格。

其中「敌人更快时它先动」这两条守的是「tie-break 只管同速」——
没有它们,后人把 priority 当成绝对顺序也不会变红。"
```

---

### Task 4: 表现层回放开场

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`

**Interfaces:**
- Consumes: `BattleEngine.OpeningSteps` / `OpeningStep`(Task 2)、既有的 `FillActionBars` / `DropActingBar` / `MeterSnapshot`
- Produces: 无

- [ ] **Step 1: 写 `OpeningRoutine`**

照 `AdvanceRoutine` 的循环体写,数据来源换成回放。放在 `AdvanceRoutine` 旁边:

```csharp
        /// <summary>回放开场那几拍(2026-08-17)。构造函数已经把开场推进跑完了(spec §5.2),
        /// 所以这里不推进任何状态,只把 Core 记下的 OpeningSteps 逐拍演出来 ——
        /// 否则玩家会看到「进战斗即已打完」(携带满格召唤物秒掉弱敌,spec §5.7)。
        ///
        /// 循环体与 AdvanceRoutine 一致,差别只在数据来源(回放 vs 现场推进)。
        /// pre 的串法:首拍从全 0 起(开场的定义),之后每拍的 pre 是上一拍的 post。</summary>
        private System.Collections.IEnumerator OpeningRoutine()
        {
            var steps = Battle.OpeningSteps;
            var pre = (player: 0, summons: new int[Battle.Summons.Count],
                enemies: new int[Battle.Enemies.Count]);
            var allDeaths = new System.Collections.Generic.List<int>();

            foreach (var step in steps)
            {
                var post = (player: step.PlayerMeter,
                    summons: ToArray(step.SummonMeters), enemies: ToArray(step.EnemyMeters));
                yield return FillActionBars(pre, post, step.Actor, step.Ticks);
                if (step.Events.Any(e => e.Kind != BattleEventKind.ActorActed))
                {
                    bool done = false;
                    var events = new System.Collections.Generic.List<BattleEvent>(step.Events);
                    _juice.Play(events, EnemyAnchor, SummonAnchor, () => done = true, OnImpact);
                    while (!done) yield return null;
                    yield return new WaitForSecondsRealtime(0.12f);
                }
                DropActingBar(step.Actor, post);
                pre = post;
            }

            Refresh();
            OnAnimDone(allDeaths);
        }

        /// <summary>IReadOnlyList&lt;int&gt; → int[],供 FillActionBars 的元组用。</summary>
        private static int[] ToArray(System.Collections.Generic.IReadOnlyList<int> source)
        {
            var result = new int[source.Count];
            for (int i = 0; i < result.Length; i++) result[i] = source[i];
            return result;
        }
```

> ⚠ `FillActionBars` / `DropActingBar` 的形参是具名元组
> `(int player, int[] summons, int[] enemies)`。上面的 `pre`/`post` 用了同款推断元组 ——
> 编译不过就显式写成那个类型。
>
> ⚠ 开场若打死了敌人,`allDeaths` 要收到那些下标再交给 `OnAnimDone`,否则
> `_dyingEnemies` 的着色残留会撞上下一场同下标的新敌人(`BattleView` 里那条既有的坑)。
> 死亡下标从 `step.Events` 里筛(`BattleEventKind` 里表示敌人死亡的那一种,
> 去 `DeathsThisAction()` 看它是怎么认的)。

- [ ] **Step 2: 在 `Refresh` 里接入**

照 `enteredReviving`(`BattleView.cs:542-553`)那个先例,在它**旁边**加:

```csharp
            // 开场回放(2026-08-17):进战斗后先把开场那几拍演出来,再放开输入。
            // 与 enteredReviving 同型:BeginAnim + 起协程 + return(本帧不走完整 switch,
            // OpeningRoutine 内部会自己 Refresh)。靠 _openingPlayedForBattle 守一次性,
            // 不必让 Core 记「播过没有」。
            if (_run.Phase == RunPhase.InBattle && !Animating
                && _openingPlayedForBattle != _run.BattleIndex
                && Battle.OpeningSteps.Count > 0)
            {
                _openingPlayedForBattle = _run.BattleIndex;
                BeginAnim();
                StartCoroutine(OpeningRoutine());
                return;
            }
```

字段(放在 `_lastBattleIndex` 附近):

```csharp
        // 这一场的开场回放播过没有(2026-08-17)。用 BattleIndex 而不是 bool:
        // 换一场战斗就自然重置,不必找地方清标记。−1 = 还没播过任何一场。
        private int _openingPlayedForBattle = -1;
```

> ⚠ 位置要在 `enteredReviving` 那个分支**之后** —— 复活流程的续跑优先级更高,
> 而且复活时 `OpeningSteps` 早就播过了(`_openingPlayedForBattle` 已等于当前 index)。

- [ ] **Step 3: 兜住 `Phase == Won`**

spec §5.7:构造函数可能返回一个已经打完的战斗。确认 `OpeningRoutine` 播完后
`Refresh()` 会走到「本场已分胜负,等待结算」那条 `case RunPhase.InBattle:` 分支
并正常出结算 —— 去读 `Refresh` 的 switch 确认,若走不到就补。

**报告里要说明你是怎么确认的。**

- [ ] **Step 4: 改写两处过时注释**

`BattleView.cs:520-522` 那段写着「满格未动的敌人(**调度优先级 1/2,高于玩家的 3**)本该
先补完它们那一拍」—— priority 已反转,这句成了谎言。改写成新口径
(玩家 0 / 召唤 1 / Buff 敌 2 / 其余敌 3),并说明复活续跑的理由不变。

顺带检查 `BattleView.cs` 里还有没有别处提到旧优先级或「玩家排最后」,一并改。

- [ ] **Step 5: 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

Expected: 无 `error CS`

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation/BattleView.cs
git commit -m "feat(ui): 回放开场那几拍,不让玩家看到「进战斗即已打完」

构造函数已经把开场推进跑完了(spec §5.2),所以 OpeningRoutine 不推进任何状态,
只把 Core 记下的 OpeningSteps 逐拍演出来。循环体与 AdvanceRoutine 一致,
差别只在数据来源;FillActionBars / DropActingBar 直接复用。

pre 的串法:首拍从全 0 起(开场的定义),之后每拍的 pre 是上一拍的 post。

顺带改掉 :520 那段注释 —— 它写着「调度优先级 1/2,高于玩家的 3」,priority
反转后已成谎言。"
```

---

### Task 5: 收尾验证

**Files:**
- Modify: `docs/superpowers/specs/2026-08-15-ATB回合制改造-design.md`

- [ ] **Step 1: 标注旧 spec 的 tie-break 口径被推翻**

在 `docs/superpowers/specs/2026-08-15-ATB回合制改造-design.md` 已有的废止标注**下面**追加:

```markdown
> ⚠ 2026-08-17 再次部分废止:tie-break 方向(「并列时玩家排最后」)与三处配套的
> 「方向不要再调回去」注释已被推翻 —— 那个诊断把病根归给「玩家优先」,实际病根是
> 「玩家优先 + 构造函数给的免费先手」这个组合。见
> `docs/superpowers/specs/2026-08-17-开场走调度与同速优先级反转-design.md` §二(附实测数字)。
> 本 spec 的其余部分(调度算法、时序归属、事件、`PlayerSpeed`)全部仍然有效。
```

- [ ] **Step 2: 跑全部自动化验证**

```bash
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ -q
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd ../prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

Expected: pytest 全绿、coretests Failed 0、prescompile 无 `error CS`

- [ ] **Step 3: 提交**

```bash
git add "docs/superpowers/specs/2026-08-15-ATB回合制改造-design.md"
git commit -m "docs(spec): 标注 ATB spec 的 tie-break 口径已被 2026-08-17 推翻"
```

- [ ] **Step 4: 交给用户 Play 验收**

**不可自动化,不可跳过。** 请用户在 Unity 里 Play,逐项确认:

1. **开场是否先看到全场条从 0% 涨到满、然后才轮到行动** —— 这是本次要解决的症状
2. 带着满格召唤物进新一层时,能否看到它们出手的完整表现(而不是瞬间胜利)
3. 同速局一轮下来是否是「玩家 → 召唤 → 敌」
4. 开场 0.5 秒的节奏是否合适(嫌拖改 `ActionBarBaseMs`)
5. 召唤字打出去,召唤物是否**当轮**就反击(口径 3 的可见证据)
6. 打出减速字之后,敌人的条是否明显涨得比玩家慢

---

## 自审记录

**Spec 覆盖:** 口径 1→T2S1、口径 2→T2S5、口径 3→T2S6、口径 4→T2S4 + T4、口径 5→T2S5(`BeginPlayerTurn` 由 `AdvanceOnce` 调,无需额外代码)、口径 6→T4S1(`step.Ticks` 传给 `FillActionBars`)、口径 7→T3S1 三条(含一条反面用例:敌人快 4 倍但玩家仍先动)、口径 8→T3S1 的 `Opening_SnapshotRestore_HasNoOpeningSteps`、口径 9→无需代码(不动 `:1315`);§5.1→T2S1、§5.2→T2S5、§5.3→T2S4、§5.4→T2S6、§5.5→T4、§5.6→T2S7-12、§5.7→T4S3、§5.8→T1;§六验证→T5。

**类型一致性:** `EnemyDef(..., int speed = 0)`(T1)在 T2S10 与 T3S1 使用;`OpeningStep` 六个字段(T2S4)在 T3S1 与 T4S1 使用;`OpeningSteps`(T2S4)在 T4S2 的进入条件使用;`FillActionBars` / `DropActingBar`(上一批已存在)在 T4S1 复用。

**已知未定项(刻意留给实现者,均附了查证入口):**
- `MetalBossWithSpeed` 的实参 —— 照 `MetalBoss()` 搬(T2S10)
- `BattleEngine.Restore` 的签名 —— 照 `RunEngine.cs:184` 搬(T3S1)
- `Summon_EntersFieldAtFullMeter` 的召唤字 id 与 `Cast` 形式 —— 照 `SummonPassiveTests.cs` 搬(T3S1)
- 死亡下标怎么从事件里筛 —— 照 `DeathsThisAction()`(T4S1)
- `CarriedSummons_CountTowardCap` 的重写方式 —— 二选一,由实现者定并说明覆盖去处(T2S12)
