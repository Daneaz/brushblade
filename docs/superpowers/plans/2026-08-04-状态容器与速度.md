# 统一状态容器与行动计量器 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把散在两个类里的 7 种战斗状态收进统一容器,并把「半速开关」升级为行动计量器。

**Architecture:** 新增 `StatusEffect`(有极性、有时限、有来源)与 `StatusBag`(封装去重/查询/递减),
`EnemyState` 与 `BattleEngine` 各持一个。状态逐批迁入,每批连同快照一起改,保证任何一个提交点上
558 条既有测试都是绿的。速度用「每回合累积 Speed 点、每满 100 行动一次」的计量器,现有 `Slow` 是其特例。

**Tech Stack:** C#(Core/Data 禁引 UnityEngine)、NUnit、Unity 6000.5.2f1。

## Global Constraints

- Core 与 Data **禁止引用 UnityEngine**;随机性一律走 `GameRandom`,禁用 `UnityEngine.Random`。
- 测试断言只用 Unity 版 NUnit 支持的 API:**禁用 `Is.AnyOf`/`Is.All.AnyOf`**,多选一用 `Is.EqualTo(a).Or.EqualTo(b)`,集合子集用 `Has.All.Matches<T>`。
- 测试代码**禁止引用 Newtonsoft**;只能用 Tests asmdef 放行的程序集(Core / Data)。
- 每个任务提交前跑:`cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
- 改动 Presentation 的任务**必须**加跑:`cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q`(只看 `error CS`)
- **测试基线 558 全绿。**
- ⚠️ **最重要的一条**:那 26 条 P0 机制测试(`Bleed_*` / `Freeze_*` / `Slow_*` / `HealOverTime_*` / `DamageReduction_*`)
  是「纯搬迁不改行为」的唯一凭据。**不允许为迁就重构而修改它们的断言** —— 断言变了就说明行为变了,
  那是缺陷不是适配。唯一例外是 Task 3 明确列出的 `Slow` 相关断言(速度模型下字段名变了,但**逐拍行为必须一致**)。
- 提交信息用 conventional commits,正文写中文。
- ⚠️ **只提交你自己改动的文件**。工作区有用户未完成的改动(`Brushblade/Assets/Settings/`、
  `ProjectSettings/ProjectSettings.asset`),**不要 `git add -A`**,不要碰它们。

---

### Task 1: StatusEffect 与 StatusBag(纯新增,零行为变更)

**Files:**
- Create: `Brushblade/Assets/_Project/Core/StatusEffect.cs`
- Test: `Brushblade/Assets/_Project/Tests/StatusBagTests.cs`(新建)

**Interfaces:**
- Consumes: 无
- Produces: `StatusKind` / `StatusPolarity` 枚举;`StatusEffect` 类;
  `StatusBag` 及其成员 `All` / `Has(StatusKind)` / `TotalMagnitude(StatusKind)` / `Find(StatusKind)` /
  `Apply(StatusEffect)` / `Remove(StatusKind)` / `RemoveAll(StatusPolarity)` / `TickTurns()` / `Clear()`

本任务**不碰任何既有代码**,只把新类型和容器建起来并测透。后续任务才逐批迁入。

- [ ] **Step 1: 写失败测试**

新建 `Brushblade/Assets/_Project/Tests/StatusBagTests.cs`:

```csharp
using Brushblade.Core;
using NUnit.Framework;

namespace Brushblade.Core.Tests
{
    /// <summary>统一状态容器(2026-08-04):P1~P3 的驱散/净化都要靠它枚举与分类。</summary>
    public class StatusBagTests
    {
        private static StatusEffect Burn(int stacks) => new()
        {
            Kind = StatusKind.Burn, Polarity = StatusPolarity.Debuff, Magnitude = stacks, TurnsLeft = -1,
        };

        private static StatusEffect Reduction(string sourceId, int percent) => new()
        {
            Kind = StatusKind.DamageReduction, Polarity = StatusPolarity.Buff,
            Magnitude = percent, TurnsLeft = -1, SourceId = sourceId,
        };

        [Test]
        public void Apply_SameKindAndSource_RefreshesInsteadOfStacking()
        {
            var bag = new StatusBag();
            bag.Apply(Reduction("铠", 20));
            bag.Apply(Reduction("铠", 20));   // 同字重复施放

            Assert.That(bag.All.Count, Is.EqualTo(1));
            Assert.That(bag.TotalMagnitude(StatusKind.DamageReduction), Is.EqualTo(20));
        }

        [Test]
        public void Apply_SameKindDifferentSource_BothKept()
        {
            var bag = new StatusBag();
            bag.Apply(Reduction("铠", 20));
            bag.Apply(Reduction("崟", 15));

            Assert.That(bag.All.Count, Is.EqualTo(2));
            Assert.That(bag.TotalMagnitude(StatusKind.DamageReduction), Is.EqualTo(35));
        }

        [Test]
        public void TickTurns_ExpiresAtZero_KeepsPersistent()
        {
            var bag = new StatusBag();
            bag.Apply(new StatusEffect
            {
                Kind = StatusKind.Bleed, Polarity = StatusPolarity.Debuff, Magnitude = 3, TurnsLeft = 1,
            });
            bag.Apply(Reduction("铠", 20)); // TurnsLeft = -1,段内持久

            bag.TickTurns();

            Assert.That(bag.Has(StatusKind.Bleed), Is.False);          // 到期移除
            Assert.That(bag.Has(StatusKind.DamageReduction), Is.True); // -1 不递减
        }

        [Test]
        public void RemoveAll_ByPolarity_OnlyTouchesThatSide()
        {
            var bag = new StatusBag();
            bag.Apply(Burn(3));
            bag.Apply(Reduction("铠", 20));

            Assert.That(bag.RemoveAll(StatusPolarity.Debuff), Is.EqualTo(1)); // 返回移除条数
            Assert.That(bag.Has(StatusKind.Burn), Is.False);
            Assert.That(bag.Has(StatusKind.DamageReduction), Is.True);
        }

        [Test]
        public void Find_ReturnsNullWhenAbsent()
        {
            var bag = new StatusBag();
            Assert.That(bag.Find(StatusKind.Freeze), Is.Null);
            Assert.That(bag.TotalMagnitude(StatusKind.Freeze), Is.EqualTo(0));
        }
    }
}
```

- [ ] **Step 2: 跑测试,确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:`error CS0246: The type or namespace name 'StatusBag' could not be found`。

- [ ] **Step 3: 实现类型与容器**

新建 `Brushblade/Assets/_Project/Core/StatusEffect.cs`:

```csharp
using System.Collections.Generic;

namespace Brushblade.Core
{
    /// <summary>状态种类(2026-08-04)。护盾不在此列——它是资源不是状态,
    /// 有独立吸伤顺序与跨段规则,驱散/净化本就不该碰它。</summary>
    public enum StatusKind
    {
        Burn,             // 灼烧层数
        Bleed,            // 流血:每回合固定伤害
        Freeze,           // 冻结:跳过行动
        SpeedModifier,    // 速度增减(点数,可正可负)
        HealOverTime,     // 持续治疗
        DamageReduction,  // 减伤百分比
        AttackBuff,       // 攻击加成
    }

    public enum StatusPolarity { Buff, Debuff }

    /// <summary>一条状态。Magnitude 按 Kind 解读:Burn=层数、Bleed/HealOverTime=每回合量、
    /// DamageReduction=百分比、SpeedModifier=速度点数、AttackBuff=攻击加成。</summary>
    public sealed class StatusEffect
    {
        public StatusKind Kind { get; set; }
        public StatusPolarity Polarity { get; set; }
        public int Magnitude { get; set; }
        public int TurnsLeft { get; set; }   // -1 = 段内持久,不随回合递减
        public string SourceId { get; set; } // 字 ID:同字去重、驱散追溯
        public bool TargetAll { get; set; }  // 仅 HealOverTime 用

        public StatusEffect Clone() => new()
        {
            Kind = Kind, Polarity = Polarity, Magnitude = Magnitude,
            TurnsLeft = TurnsLeft, SourceId = SourceId, TargetAll = TargetAll,
        };
    }

    /// <summary>单位身上的状态集合。封装「同字不叠只刷新」与按极性批量清除,
    /// 供 P1~P3 的 Dispel/Cleanse 直接使用。</summary>
    public sealed class StatusBag
    {
        private readonly List<StatusEffect> _list = new();

        public IReadOnlyList<StatusEffect> All => _list;

        public bool Has(StatusKind kind) => Find(kind) != null;

        public StatusEffect Find(StatusKind kind)
        {
            foreach (var e in _list)
                if (e.Kind == kind) return e;
            return null;
        }

        /// <summary>该种类的量值合计(减伤多源叠加、速度多条修正求和都靠它)。</summary>
        public int TotalMagnitude(StatusKind kind)
        {
            int sum = 0;
            foreach (var e in _list)
                if (e.Kind == kind) sum += e.Magnitude;
            return sum;
        }

        /// <summary>施加一条。同 Kind 且同 SourceId 视为同一来源,覆盖刷新而非叠加
        /// (口径来自 P0:同字减伤不叠加,重复施放只刷新)。SourceId 为 null 时按 Kind 去重。</summary>
        public void Apply(StatusEffect effect)
        {
            for (int i = 0; i < _list.Count; i++)
            {
                if (_list[i].Kind != effect.Kind) continue;
                if (_list[i].SourceId != effect.SourceId) continue;
                _list[i] = effect;
                return;
            }
            _list.Add(effect);
        }

        public void Remove(StatusKind kind) => _list.RemoveAll(e => e.Kind == kind);

        /// <summary>按极性批量清除,返回移除条数(驱散/净化用)。</summary>
        public int RemoveAll(StatusPolarity polarity)
        {
            int before = _list.Count;
            _list.RemoveAll(e => e.Polarity == polarity);
            return before - _list.Count;
        }

        public void Clear() => _list.Clear();

        /// <summary>回合数递减,归零即移除;TurnsLeft &lt; 0 表示段内持久,不受影响。</summary>
        public void TickTurns()
        {
            for (int i = _list.Count - 1; i >= 0; i--)
            {
                if (_list[i].TurnsLeft < 0) continue;
                _list[i].TurnsLeft -= 1;
                if (_list[i].TurnsLeft <= 0) _list.RemoveAt(i);
            }
        }

        /// <summary>深拷贝(快照恢复用):条目是引用对象,浅拷会让两个单位共享同一条状态。</summary>
        public void CopyFrom(IEnumerable<StatusEffect> source)
        {
            _list.Clear();
            foreach (var e in source) _list.Add(e.Clone());
        }
    }
}
```

- [ ] **Step 4: 跑测试**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:5 条新测试通过,既有 558 条不受影响(本任务没碰它们)。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/StatusEffect.cs Brushblade/Assets/_Project/Tests/StatusBagTests.cs
git commit -m "feat(core): 新增 StatusEffect 与 StatusBag——状态容器的地基

有极性、有时限、有来源的统一状态条目,封装「同字不叠只刷新」与按极性
批量清除。本次纯新增,不碰任何既有状态字段,迁移分批在后续任务进行。

CopyFrom 走深拷贝:条目是引用对象,浅拷会让快照恢复后两个单位共享同一条状态。"
```

---

### Task 2: 迁移敌人侧减益(Burn / Bleed / Freeze)

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EnemyDef.cs`(`EnemyState` 129-133 行一带)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(灼烧段 504-520、流血段 524-536、冻结判定 592/609-613、施加处 730-745 一带)
- Modify: `Brushblade/Assets/_Project/Core/RunSnapshot.cs`(`EnemySnapshot`)
- Test: `Brushblade/Assets/_Project/Tests/BattleEngineTests.cs`、`SnapshotRoundTripTests.cs`

**Interfaces:**
- Consumes: Task 1 的 `StatusBag` / `StatusEffect` / `StatusKind` / `StatusPolarity`
- Produces: `EnemyState.Statuses`(`StatusBag`);`EnemyState.Burn` / `Bleed` / `BleedTurns` / `FreezeTurns` 字段**删除**;
  `EnemySnapshot.Statuses`(`List<StatusEffect>`)

⚠️ **本任务不碰 `SlowTurns` / `SlowActs`** —— 那是 Task 3 的范围,它们与速度模型绑定。

- [ ] **Step 1: 先读懂现有语义,再动手**

三处结算的**现有行为必须逐字保持**:

| 状态 | 现有行为 |
|---|---|
| `Burn` | 回合末 `floor(层数 × _burnPerStack × 火对目标的克制系数)` 伤害,然后**层数 −1** |
| `Bleed` | 回合末固定 `Bleed` 点伤害(**不走生克**),`BleedTurns −1`,归零时 `Bleed` 也清零 |
| `Freeze` | `FreezeTurns > 0` 时该敌人跳过行动(蓄力/加攻/技能全跳),并 `FreezeTurns −= 1` |

注意 `Burn` 的递减是**层数**而非回合数,所以它迁移后 `TurnsLeft = -1`(段内持久),
靠结算段自己减 `Magnitude`;而 `Bleed`/`Freeze` 用 `TurnsLeft` 递减。
**不要**把 `Burn` 的层数塞进 `TurnsLeft` —— 那会让 `TickTurns()` 提前清掉它。

- [ ] **Step 2: 写失败测试**

加到 `BattleEngineTests.cs`(这几条钉住迁移后仍能按种类查询):

```csharp
[Test]
public void Statuses_BurnAndBleed_QueryableByKind()
{
    var engine = new BattleEngine(BleedGraph(), Config(), new[] { "锯" },
        Array.Empty<string>(), new[] { new EnemyDef("桩", Element.Metal, 500, 0) }, 42);
    engine.Cast("锯");

    var bag = engine.Enemies[0].Statuses;
    Assert.That(bag.Has(StatusKind.Bleed), Is.True);
    Assert.That(bag.TotalMagnitude(StatusKind.Bleed), Is.EqualTo(3));
    Assert.That(bag.Find(StatusKind.Bleed).Polarity, Is.EqualTo(StatusPolarity.Debuff));
}
```

- [ ] **Step 3: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:`'EnemyState' does not contain a definition for 'Statuses'`。

- [ ] **Step 4: 加容器,保留旧字段作为过渡投影**

`EnemyState` 加 `public StatusBag Statuses { get; } = new();`,
并把 `Burn` / `Bleed` / `BleedTurns` / `FreezeTurns` 四个字段**改为读写容器的计算属性**:

```csharp
        public StatusBag Statuses { get; } = new();

        // 过渡投影(2026-08-04):迁移期间保留旧名,内部转发到容器。
        // Step 6 把所有调用点改完后,这四个属性一并删除。
        internal int Burn
        {
            get => Statuses.TotalMagnitude(StatusKind.Burn);
            set
            {
                if (value <= 0) { Statuses.Remove(StatusKind.Burn); return; }
                Statuses.Apply(new StatusEffect
                {
                    Kind = StatusKind.Burn, Polarity = StatusPolarity.Debuff,
                    Magnitude = value, TurnsLeft = -1,
                });
            }
        }
```

⚠️ 这一步的**目的是让 558 条测试立刻恢复绿**,证明容器读写与旧字段等价。
`Bleed` / `BleedTurns` / `FreezeTurns` 照同样套路写(`Bleed` 与 `BleedTurns` 共用一条
`StatusKind.Bleed`:`Magnitude` = 每回合伤害,`TurnsLeft` = 剩余回合)。

- [ ] **Step 5: 跑全量,确认 558 仍绿**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:558 + 新增测试全绿。**若有红,说明投影语义与旧字段不等价,必须先修好再往下走。**

- [ ] **Step 6: 把结算段与施加处改为直接用容器,删除过渡投影**

`BattleEngine` 的灼烧段改为:

```csharp
            for (int i = 0; i < _enemies.Count; i++)
            {
                var enemy = _enemies[i];
                if (!enemy.Alive) continue;
                var burn = enemy.Statuses.Find(StatusKind.Burn);
                if (burn == null || burn.Magnitude <= 0) continue;
                // 灼烧属火(2026-08-03):只结算克制,不结算相生 —— 层数是平值
                int tick = (int)Math.Floor(burn.Magnitude * _burnPerStack
                    * WuxingResolver.KeMultiplier(Element.Fire, enemy.Element));
                enemy.Hp = Math.Max(0, enemy.Hp - tick);
                burn.Magnitude -= 1;
                if (burn.Magnitude <= 0) enemy.Statuses.Remove(StatusKind.Burn);
                _events.Add(new BattleEvent(BattleEventKind.BurnTick, i, tick));
                if (!enemy.Alive) ResolveDefeat(i); else CheckBossPhase(i);
            }
```

流血段与冻结判定照此改写(流血用 `TurnsLeft` 递减,冻结判定改为 `enemy.Statuses.Find(StatusKind.Freeze)`)。
改完删除 Step 4 那四个过渡属性。

- [ ] **Step 7: 快照迁移**

`EnemySnapshot` 删 `Burn` / `Bleed` / `BleedTurns` / `FreezeTurns`,加:

```csharp
        public List<StatusEffect> Statuses { get; set; } = new();
```

`EnemyState.Capture()` 侧写 `Statuses.All` 的**深拷贝**(`e.Clone()`),`Restore()` 侧用 `Statuses.CopyFrom(...)`。
⚠️ 浅拷会让恢复后的两个敌人共享同一条状态对象 —— spec 风险 3 点名了这条。

加一条往返测试到 `SnapshotRoundTripTests.cs`:

```csharp
[Test]
public void EnemyStatuses_SurviveRoundTrip_AndAreNotShared()
{
    var graph = Graph();
    var enemyDef = new EnemyDef("桩", Element.Metal, 500, 0);
    var engine = new BattleEngine(graph, Config(), new[] { "锯" },
        Array.Empty<string>(), new[] { enemyDef, enemyDef }, 42);
    engine.Cast("锯", 0); // 只给 0 号上流血

    var restored = BattleEngine.Restore(engine.Capture(), graph, Config(), null,
        new Dictionary<string, EnemyDef> { ["桩"] = enemyDef });

    Assert.That(restored.Enemies[0].Statuses.Has(StatusKind.Bleed), Is.True);
    Assert.That(restored.Enemies[1].Statuses.Has(StatusKind.Bleed), Is.False); // 没被别名共享
}
```

⚠️ `SnapshotRoundTripTests` 的 `Graph()` 需含「锯」;若没有,按该文件既有风格补进它自己的 `Graph()`。

- [ ] **Step 8: 跑全量并提交**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

```bash
git add Brushblade/Assets/_Project/Core/EnemyDef.cs Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Core/RunSnapshot.cs Brushblade/Assets/_Project/Tests/
git commit -m "refactor(core): 敌人侧 Burn/Bleed/Freeze 迁入状态容器

三个状态从 EnemyState 的独立字段迁为 StatusBag 条目。Burn 的递减是层数
而非回合数,故 TurnsLeft = -1 由结算段自减 Magnitude;Bleed/Freeze 走
TurnsLeft 递减。

快照 Capture/Restore 走深拷贝——条目是引用对象,浅拷会让恢复后的两个
敌人共享同一条状态。"
```

---

### Task 3: 行动计量器,Slow 迁为 SpeedModifier

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EnemyDef.cs`(`EnemyState`、`SummonState`)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(减速预处理 573-584、辅助段 586-602、主行动段 604-640、施加处 744-745)
- Modify: `Brushblade/Assets/_Project/Core/RunSnapshot.cs`(`EnemySnapshot`、`SummonSnapshot`)
- Test: `Brushblade/Assets/_Project/Tests/BattleEngineTests.cs`、`SnapshotRoundTripTests.cs`

**Interfaces:**
- Consumes: Task 1 的 `StatusBag`;Task 2 迁好的 `EnemyState.Statuses`
- Produces: `EnemyState.Speed` / `EnemyState.ActionMeter`、`SummonState.ActionMeter`;
  `EnemyState.SlowTurns` / `SlowActs` 字段**删除**;`EnemySnapshot.ActionMeter`、`SummonSnapshot.ActionMeter`

**这是本计划唯一包含行为变更的任务。**

- [ ] **Step 1: 写失败测试**

```csharp
// ---- 行动计量器(2026-08-04):Speed 每回合累积,每满 100 行动一次 ----

[Test]
public void Speed50_MatchesOldSlowRhythm_SkipActSkipAct()
{
    // 减速敌人:攻 5,玩家 50 血。逐拍验证「跳、动、跳、动」
    var engine = SlowedEngine();           // 见 Step 3 的辅助
    int hp0 = engine.PlayerHp;

    engine.EndTurn();
    Assert.That(engine.PlayerHp, Is.EqualTo(hp0), "第 1 回合:计量器 50,不足 100,跳过");
    engine.EndTurn();
    Assert.That(engine.PlayerHp, Is.LessThan(hp0), "第 2 回合:累到 100,行动");
}

[Test]
public void Speed200_ActsTwicePerTurn()
{
    var engine = HastedEngine(speed: 200);
    int hp0 = engine.PlayerHp;
    engine.EndTurn();
    Assert.That(hp0 - engine.PlayerHp, Is.EqualTo(10)); // 攻 5 × 2 次
}

[Test]
public void Speed300_CappedAtTwoActions_NoCarryOver()
{
    var engine = HastedEngine(speed: 300);
    int hp0 = engine.PlayerHp;
    engine.EndTurn();
    Assert.That(hp0 - engine.PlayerHp, Is.EqualTo(10));            // 封顶 2 次
    Assert.That(engine.Enemies[0].ActionMeter, Is.EqualTo(0));     // 余额清零,不留到下回合
}

[Test]
public void SpeedModifiers_StackAdditively()
{
    var engine = SlowedEngine();
    engine.Enemies[0].Statuses.Apply(new StatusEffect
    {
        Kind = StatusKind.SpeedModifier, Polarity = StatusPolarity.Debuff,
        Magnitude = -25, TurnsLeft = 3, SourceId = "凝",
    });
    // 基准 100 − 50 − 25 = 25 → 每 4 回合行动一次
    int hp0 = engine.PlayerHp;
    engine.EndTurn(); engine.EndTurn(); engine.EndTurn();
    Assert.That(engine.PlayerHp, Is.EqualTo(hp0), "前 3 回合累计 75,不足 100");
    engine.EndTurn();
    Assert.That(engine.PlayerHp, Is.LessThan(hp0), "第 4 回合到 100");
}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:`'EnemyState' does not contain a definition for 'ActionMeter'`。

- [ ] **Step 3: 加两个测试辅助**

加到 `BattleEngineTests.cs`(照该文件既有辅助的风格):

```csharp
/// <summary>挂了 −50 速度修正的敌人(等价于旧的半速 Slow)。</summary>
private static BattleEngine SlowedEngine()
{
    var engine = new BattleEngine(Graph(), Config(), new[] { "灯" },
        Array.Empty<string>(), new[] { MetalBoss() }, 42);
    engine.Enemies[0].Statuses.Apply(new StatusEffect
    {
        Kind = StatusKind.SpeedModifier, Polarity = StatusPolarity.Debuff,
        Magnitude = -50, TurnsLeft = 99, SourceId = "洼",
    });
    return engine;
}

/// <summary>指定基础速度的敌人(测加速与封顶)。</summary>
private static BattleEngine HastedEngine(int speed)
{
    var engine = new BattleEngine(Graph(), Config(), new[] { "灯" },
        Array.Empty<string>(), new[] { MetalBoss() }, 42);
    engine.Enemies[0].Speed = speed;
    return engine;
}
```

⚠️ `MetalBoss()` 是攻 5 的既有辅助。若 `Speed` 的 setter 是 `internal set`,
测试程序集与 Core 不在同一程序集、拿不到 —— 那就把 `Speed` 的 setter 放开为 `public set`,
并在注释里说明「测试需要直接设定基础速度」。**不要**为此加 `InternalsVisibleTo`
(Tests asmdef 是 `overrideReferences: true`,加了工装绿也不等于编辑器绿)。

- [ ] **Step 4: 加字段**

`EnemyState` 与 `SummonState` 各加:

```csharp
        /// <summary>基础速度(2026-08-04)。有效速度 = Speed + 所有 SpeedModifier 之和,下限 0。
        /// 基数用本字段而非常量 100:将来若有天生快/慢的字怪,写死 100 会让它们的修正算错。</summary>
        public int Speed { get; set; } = 100;

        /// <summary>行动计量器:回合末累积有效速度,每满 100 行动一次。</summary>
        public int ActionMeter { get; internal set; }
```

- [ ] **Step 5: 改行动循环**

删除 573-584 行的 `slowSkip[]` 预处理整段,换成计算每个敌人**本回合行动次数**:

```csharp
            // 行动计量器(2026-08-04):每回合累积有效速度,每满 100 行动一次。
            // 冻结期间不累积 —— 保持「节拍原地暂停」语义(旧 SlowActs 交替的等价物)。
            var actionCount = new int[_enemies.Count];
            for (int i = 0; i < _enemies.Count; i++)
            {
                var enemy = _enemies[i];
                if (!enemy.Alive || enemy.Statuses.Has(StatusKind.Freeze)) continue;
                int effective = Math.Max(0,
                    enemy.Speed + enemy.Statuses.TotalMagnitude(StatusKind.SpeedModifier));
                enemy.ActionMeter += effective;
                actionCount[i] = Math.Min(enemy.ActionMeter / ActionMeterThreshold, MaxActionsPerTurn);
                enemy.ActionMeter -= actionCount[i] * ActionMeterThreshold;
                if (enemy.ActionMeter >= ActionMeterThreshold) enemy.ActionMeter = 0; // 封顶后不留余额
            }
```

在 `BattleEngine` 加两个常量:

```csharp
        private const int ActionMeterThreshold = 100; // 计量器满值:攒够即行动一次
        private const int MaxActionsPerTurn = 2;      // 单回合行动次数封顶(口径 4)
```

辅助段(586-602)的 `if (slowSkip[i]) continue;` 改为 `if (actionCount[i] == 0) continue;`。
主行动段(604-640)把 `if (slowSkip[i]) continue;` 改为按次数循环——**把原有的单次行动体
整个包进 `for (int act = 0; act < actionCount[i]; act++)`**,并在每次行动前重新检查 `enemy.Alive`
(反伤可能在两次行动之间打死它)。

冻结的递减改为走 `Statuses.TickTurns()`(Step 6 统一处理),原先在主行动段里的
`enemy.FreezeTurns -= 1` 删除。

- [ ] **Step 5b: 召唤物反击也走计量器**

口径 6 是「敌人与召唤物走同一模型」—— 只给 `SummonState` 加字段却不用,那是死代码。

召唤物反击循环(555-566 行)现在是每个存活召唤物固定打一次。改为按计量器决定次数:

```csharp
            // 召唤物反击(木系,2026-07-19):前排树各打首个存活敌人,走生克。
            // 2026-08-04:与敌人同走行动计量器 —— 减速将来也能作用于我方召唤物。
            for (int s = 0; s < _summons.Count; s++)
            {
                var summon = _summons[s];
                if (!summon.Alive) continue;
                summon.ActionMeter += summon.Speed;
                int acts = Math.Min(summon.ActionMeter / ActionMeterThreshold, MaxActionsPerTurn);
                summon.ActionMeter -= acts * ActionMeterThreshold;
                if (summon.ActionMeter >= ActionMeterThreshold) summon.ActionMeter = 0;
                for (int act = 0; act < acts; act++)
                {
                    if (!summon.Alive) break;          // 反伤可能在两次出手之间打死它
                    int target = -1;
                    for (int i = 0; i < _enemies.Count; i++)
                        if (_enemies[i].Alive) { target = i; break; }
                    if (target < 0) break;
                    _events.Add(new BattleEvent(BattleEventKind.SummonAttack, target, summon.Attack, s));
                    DamageEnemy(target, summon.Attack, Array.Empty<Element>(), summon.Element);
                }
            }
```

⚠️ 默认 `Speed = 100` 时每回合恰好一次,**行为与改前完全一致** —— 现有的召唤物测试
(`SummonAttack` 相关)必须全绿,一条断言都不用改。若有红,说明计量器接错了。

⚠️ 原代码用 `if (target < 0) break;` 跳出**外层**循环(没有敌人了就整段停),
改写后内层的 `break` 只跳内层。这是有意的:外层继续遍历不会产生额外伤害(内层立刻 break),
但保持了 `ActionMeter` 对所有召唤物正确累积。

- [ ] **Step 6: 状态回合递减收口**

在 `EndTurn` 末尾、`StartTurn()` 之前,对每个敌人调一次 `enemy.Statuses.TickTurns()`,
并对玩家容器调一次(玩家容器是 Task 4 的事,本任务先只处理敌人)。

⚠️ 递减必须在**所有结算之后**,否则本回合刚施加的状态会立刻少一回合。
现有代码里 `BleedTurns -= 1` 是在流血结算内做的 —— 迁到统一递减后,
要确认流血的总跳数不变(施加时 3 回合 → 结算 3 次)。**这是最容易出错的一处,
`Bleed_ExpiresAfterThreeTurns` 那条测试就是守它的。**

- [ ] **Step 7: 施加处改写**

`Cast` 里 `EffectKind.Slow` 的分支(744-745 行)改为施加状态:

```csharp
                    case EffectKind.Slow:
                        _enemies[targetIndex].Statuses.Apply(new StatusEffect
                        {
                            Kind = StatusKind.SpeedModifier, Polarity = StatusPolarity.Debuff,
                            Magnitude = -50, TurnsLeft = value, SourceId = def.Id,
                        });
                        break;
```

`EffectKind.Slow` 的 `value` 语义仍是**持续回合数**,不变。

- [ ] **Step 8: 快照**

`EnemySnapshot` 删 `SlowTurns` / `SlowActs`,加 `public int ActionMeter { get; set; }`;
`SummonSnapshot` 加同名字段。`Capture`/`Restore` 两侧接上。

加往返测试:

```csharp
[Test]
public void ActionMeter_SurvivesRoundTrip_RhythmContinues()
{
    var graph = Graph();
    var enemyDef = new EnemyDef("枯", Element.Wood, 100, 5);
    var engine = new BattleEngine(graph, Config(), new[] { "灯" },
        Array.Empty<string>(), new[] { enemyDef }, 42);
    engine.Enemies[0].Statuses.Apply(new StatusEffect
    {
        Kind = StatusKind.SpeedModifier, Polarity = StatusPolarity.Debuff,
        Magnitude = -50, TurnsLeft = 99, SourceId = "洼",
    });
    engine.EndTurn(); // 计量器攒到 50,未行动

    var restored = BattleEngine.Restore(engine.Capture(), graph, Config(), null,
        new Dictionary<string, EnemyDef> { ["枯"] = enemyDef });

    Assert.That(restored.Enemies[0].ActionMeter, Is.EqualTo(50));
    int hp0 = restored.PlayerHp;
    restored.EndTurn();
    Assert.That(restored.PlayerHp, Is.LessThan(hp0), "续爬后下一回合就该行动,不从零重攒");
}
```

- [ ] **Step 9: 处理 Slow 相关的既有测试**

这几条测试的**断言值不许改**,但字段名要跟着迁移:
`Slow_EnemyActsEveryOtherTurn`、`Slow_PausesDuringFreeze_ThenResumesFromSamePoint`、
`Slow_SurvivesSnapshotRoundTrip_RhythmContinues`、`Buffer_SlowActTurn_StillBuffsOthers`、
`Buffer_SlowSkipTurn_DoesNotBuffOthers`。

把它们里对 `SlowTurns` / `SlowActs` 的读写换成对 `SpeedModifier` 状态与 `ActionMeter` 的读写,
**逐拍行为必须一致**。若发现某条无论如何都对不上,**停下来报告**,不要改断言 ——
那说明速度模型与旧 Slow 不等价,是设计问题而非适配问题。

- [ ] **Step 10: 跑全量并提交**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

```bash
git add Brushblade/Assets/_Project/Core/ Brushblade/Assets/_Project/Tests/
git commit -m "feat(core): 半速开关升级为行动计量器,Slow 迁为 SpeedModifier

每回合累积有效速度、每满 100 行动一次。旧的半速是 Speed 50 的特例,
逐拍等价;SlowTurns/SlowActs/slowSkip[] 三样一并删除。

新增能力:Speed 200 每回合两次、150 每两回合三次、25 每四回合一次。
单回合封顶 2 次且余额清零 —— 500 速一回合打 5 次既难看也难平衡。
冻结期间不累积,保持节拍原地暂停。"
```

---

### Task 4: 迁移玩家侧增益(HoT / DamageReduction)

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`_hots` 111、`_damageReductions` 127、减伤乘算 135、HoT 结算 540-553、施加处)
- Modify: `Brushblade/Assets/_Project/Core/RunSnapshot.cs`(`BattleSnapshot`、`RunSnapshot`、删 `HotSnapshot`)
- Modify: `Brushblade/Assets/_Project/Core/RunEngine.cs`(`_carriedDamageReductions` 7 处接线)
- Test: `Brushblade/Assets/_Project/Tests/BattleEngineTests.cs`、`SnapshotRoundTripTests.cs`、`RunEngineTests.cs`

**Interfaces:**
- Consumes: Task 1 的 `StatusBag`
- Produces: `BattleEngine.PlayerStatuses`(只读暴露);`_hots` / `_damageReductions` 字段**删除**;
  `BattleSnapshot.PlayerStatuses`;`RunSnapshot.CarriedStatuses`(替代 `CarriedDamageReductions`)

- [ ] **Step 1: 写失败测试**

```csharp
[Test]
public void PlayerStatuses_HotAndReduction_QueryableByPolarity()
{
    var engine = new BattleEngine(HealGraph(), Config(), new[] { "沐", "铠" },
        Array.Empty<string>(), new[] { WoodMinion() }, 42);
    engine.Cast("沐");
    engine.Cast("铠");

    var buffs = 0;
    foreach (var s in engine.PlayerStatuses.All)
        if (s.Polarity == StatusPolarity.Buff) buffs++;
    Assert.That(buffs, Is.EqualTo(2)); // HoT + 减伤
    Assert.That(engine.PlayerStatuses.TotalMagnitude(StatusKind.DamageReduction), Is.EqualTo(20));
}
```

`HealGraph()` 已存在于 `BattleEngineTests.cs:1320`。先读它确认 `沐`(HoT)与 `铠`(DamageReduction 20)
都在里面;若只有其中一个,按该文件既有风格把另一个补进 `HealGraph()`,**不要**新建一个 Graph 辅助。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:`'BattleEngine' does not contain a definition for 'PlayerStatuses'`。

- [ ] **Step 3: 迁移 DamageReduction**

`_damageReductions`(`Dictionary<字ID,百分比>`)删除,改用 `_playerStatuses`。
减伤乘算(135 行一带)改为遍历容器里 `Kind == DamageReduction` 的条目:

```csharp
        /// <summary>减伤总系数:各来源乘法叠加(同字不叠 —— 靠 SourceId 去重)。</summary>
        public float DamageReductionMultiplier
        {
            get
            {
                float multiplier = 1f;
                foreach (var s in _playerStatuses.All)
                    if (s.Kind == StatusKind.DamageReduction)
                        multiplier *= 1f - s.Magnitude / 100f;
                return multiplier;
            }
        }
```

`DamageReductions` 那个公开只读字典属性(260 行)改为暴露 `PlayerStatuses`。
**注意** `RunEngine` 与 Presentation 可能引用了它 —— grep 全仓,一并改;
若 Presentation 有引用,本任务要加跑 `prescompile`。

- [ ] **Step 4: 迁移 HoT**

`_hots`(`List<(Amount,Turns,All)>`)删除。结算段改为遍历容器里 `Kind == HealOverTime` 的条目,
`TargetAll` 决定群体还是单体。回合递减交给 Task 3 已经加好的 `TickTurns()`(玩家容器同样调一次)。

- [ ] **Step 5: 快照与跨战斗携带**

`BattleSnapshot` 删 `Hots` / `DamageReductions`,加 `PlayerStatuses`;`HotSnapshot` 类整个删除。
`RunSnapshot.CarriedDamageReductions` 改为 `CarriedStatuses`(`List<StatusEffect>`)。

`RunEngine` 侧 `_carriedDamageReductions` 的 **7 个接线点**(字段、构造、`Capture`、`Restore`、
公开属性、`AdvanceAfterBattle` 取值、`NewBattle` 注入)全部改为携带状态列表。
⚠️ 金汤 perk 的护盾 top-up 没有减伤类比,那一处不要动。

- [ ] **Step 6: 跑全量并提交**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

```bash
git add Brushblade/Assets/_Project/Core/ Brushblade/Assets/_Project/Tests/
git commit -m "refactor(core): 玩家侧 HoT 与减伤迁入状态容器

_hots 与 _damageReductions 两个字段删除,统一走 _playerStatuses。
减伤的「同字不叠、乘法叠加」语义不变——去重键从字典 key 变成
StatusEffect.SourceId。

RunSnapshot.CarriedDamageReductions 随之改为 CarriedStatuses,
RunEngine 的 7 个跨战斗接线点一并迁移。"
```

---

### Task 5: 加攻改为可驱散的 AttackBuff

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EnemyDef.cs`(`EnemyState.Attack`)
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(标点小妖 599、焦痕 881)
- Modify: `Brushblade/Assets/_Project/Core/RunSnapshot.cs`(`EnemySnapshot`)
- Test: `Brushblade/Assets/_Project/Tests/BattleEngineTests.cs`

**Interfaces:**
- Consumes: Task 1 的 `StatusBag`;Task 2 迁好的 `EnemyState.Statuses`
- Produces: `EnemyState.BaseAttack`;`EnemyState.Attack` 变为计算属性;`EnemySnapshot.BaseAttack`

- [ ] **Step 1: 写失败测试**

```csharp
[Test]
public void AttackBuff_RemovableAndRestoresBaseAttack()
{
    var engine = new BattleEngine(Graph(), Config(),
        new[] { "灯" }, Array.Empty<string>(), new[] { MetalBoss() }, 42);
    var enemy = engine.Enemies[0];
    int baseAttack = enemy.Attack;

    enemy.Statuses.Apply(new StatusEffect
    {
        Kind = StatusKind.AttackBuff, Polarity = StatusPolarity.Buff,
        Magnitude = 7, TurnsLeft = -1, SourceId = "点",
    });
    Assert.That(enemy.Attack, Is.EqualTo(baseAttack + 7));

    enemy.Statuses.RemoveAll(StatusPolarity.Buff);   // 驱散
    Assert.That(enemy.Attack, Is.EqualTo(baseAttack), "驱散后还原到基础攻击");
}

[Test]
public void RegrowGrowth_IsNotDispellable() // 口径 5:缺笔妖补全是形态变化,不是增益
{
    var engine = RegrowEngine();   // 见下方说明
    int before = engine.Enemies[0].Attack;
    engine.EndTurn();              // 触发一次补全成长
    int after = engine.Enemies[0].Attack;
    Assert.That(after, Is.GreaterThan(before));

    engine.Enemies[0].Statuses.RemoveAll(StatusPolarity.Buff);
    Assert.That(engine.Enemies[0].Attack, Is.EqualTo(after), "成长不该被驱散抹掉");
}
```

`RegrowEngine()` 需构造一个 `EnemyAbility.Regrow` 的敌人。`BattleEngineTests.cs` 里
既有的缺笔妖测试(grep `Regrow`)已经有构造方式,**直接复用它的写法**,不要另起一套。
现成的敌人辅助是 `MetalBoss(hp = 200)`(攻 5)与 `WoodMinion(hp = 12)`(攻 3),
但缺笔妖要 `EnemyAbility.Regrow`,得用 `EnemyDef` 的完整构造。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

预期:第一条断言 `Expected: baseAttack + 7  But was: baseAttack`(状态还没接进 `Attack`)。

- [ ] **Step 3: `Attack` 改为计算属性**

```csharp
        /// <summary>基础攻击(缺笔妖补全会直接抬高它 —— 那是形态变化不是增益,故不可驱散)。</summary>
        public int BaseAttack { get; internal set; }

        /// <summary>当前攻击 = 基础 + 所有可驱散的攻击增益。</summary>
        public int Attack => BaseAttack + Statuses.TotalMagnitude(StatusKind.AttackBuff);
```

原先所有 `enemy.Attack = X` / `enemy.Attack += X` 的写入点改为写 `BaseAttack`,
**除了**下面 Step 4 要改成增益的两处。

- [ ] **Step 4: 两处加攻改为增益**

标点小妖(599 行):

```csharp
                    other.Statuses.Apply(new StatusEffect
                    {
                        Kind = StatusKind.AttackBuff, Polarity = StatusPolarity.Buff,
                        Magnitude = enemy.Attack, TurnsLeft = -1, SourceId = enemy.Def.Id,
                    });
                    _events.Add(new BattleEvent(BattleEventKind.EnemyBuff, j, enemy.Attack));
```

⚠️ 现有语义是「加成本场累计、回合末不回滚」。用 `SourceId = enemy.Def.Id` 会让同一只小妖的
第二次加攻**覆盖**而非累加 —— 这与现状不符。要保持累计,`SourceId` 应带上回合数使每次唯一,
例如 `$"{enemy.Def.Id}#{Turn}"`。**`Buffer_*` 那两条测试会守住这个语义,不要改它们的断言。**

焦痕自燃(881 行)照此改写,`Magnitude = ScorchGain`。

缺笔妖(660、666 行)**保持写 `BaseAttack`**,不要改成增益。

- [ ] **Step 5: 快照**

`EnemySnapshot` 的 `Attack` 改名为 `BaseAttack`(增益部分已在 `Statuses` 里,重复存会导致恢复后翻倍)。
`Capture`/`Restore` 两侧同步。

⚠️ 这是**最容易出错的一处**:若 `Capture` 存的是计算后的 `Attack`,而 `Statuses` 里又存了增益,
恢复后攻击力会变成 `基础 + 增益 + 增益`。加一条往返测试钉住:

```csharp
[Test]
public void AttackBuff_SurvivesRoundTrip_WithoutDoubling()
{
    var graph = Graph();
    var enemyDef = new EnemyDef("枯", Element.Wood, 100, 5);
    var engine = new BattleEngine(graph, Config(), new[] { "灯" },
        Array.Empty<string>(), new[] { enemyDef }, 42);
    engine.Enemies[0].Statuses.Apply(new StatusEffect
    {
        Kind = StatusKind.AttackBuff, Polarity = StatusPolarity.Buff,
        Magnitude = 7, TurnsLeft = -1, SourceId = "点#1",
    });
    int expected = engine.Enemies[0].Attack; // 5 + 7 = 12

    var restored = BattleEngine.Restore(engine.Capture(), graph, Config(), null,
        new Dictionary<string, EnemyDef> { ["枯"] = enemyDef });

    Assert.That(restored.Enemies[0].Attack, Is.EqualTo(expected)); // 不是 19
}
```

- [ ] **Step 6: 跑全量并提交**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

```bash
git add Brushblade/Assets/_Project/Core/ Brushblade/Assets/_Project/Tests/
git commit -m "feat(core): 标点小妖与焦痕的加攻改为可驱散增益

Attack 变为 BaseAttack + 所有 AttackBuff 之和,驱散时移除条目即自动还原
——此前是 enemy.Attack += N 直接写死进字段,没留下「谁给的、加了多少」,
Dispel 将来无从下手。

缺笔妖的补全成长保持直接改 BaseAttack:那是形态变化不是增益,驱散它
在世界观上讲不通,且 ×2 是乘法、塞进加法增益要靠等效换算平白失真。
RegrowGrowth_IsNotDispellable 守住这条边界。"
```

---

## 收尾(控制器执行,不派给实施者)

- [ ] 确认 `EnemyState` / `BattleEngine` 里不再有 `Burn`/`Bleed`/`BleedTurns`/`FreezeTurns`/
  `SlowTurns`/`SlowActs`/`_hots`/`_damageReductions` 这些独立状态字段(spec 验收标准 4)。
- [ ] 确认那 26 条 P0 机制测试的**断言未被修改**(`git diff` 逐条核对),这是「行为不变」的唯一凭据。
- [ ] 让用户在 Unity EditMode 跑一次全绿(`ConfigLoaderTests` 被工装排除)。
- [ ] 走 `superpowers:finishing-a-development-branch` 收尾。
