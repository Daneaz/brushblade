# E-b1 属性载体 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让玩家攻击力(ATK)成为随角色等级成长的真实属性并进入伤害链路,且在基准值下与改造前逐字节等价。

**Architecture:** ATK 沿用生命属性今天已有的链路(`CharacterXp → CharacterLevel → MaxHpFor → BattleConfig`),新增 `MetaRules.AttackFor(level)`。伤害侧不改 `WuxingResolver.ResolveEffect`,而是在它**之前**用 `ScaleByAttack(v) = v * EffectiveAttack / 100` 把基础值缩放一次;`EffectiveAttack == 100` 时整数除恒等,现有测试一条断言都不用改。

**Tech Stack:** C# / Unity 6000.5.2f1 / NUnit(Unity 自带)/ 离线 dotnet 工装

**Spec:** `docs/superpowers/specs/2026-08-11-战斗属性模型-Eb1-属性载体-design.md`

## Global Constraints

- **Core 与 Data 禁止 `using UnityEngine`**(asmdef 已设 `noEngineReferences: true`)。本计划改的 `Core/Meta.cs`、`Core/BattleEngine.cs` 都在此约束下。
- **验收硬线:全部现有测试原封不动地绿,一条断言都不许改。** `coretests 792 passed` / `pytest 244 passed` / `prescompile 0 error CS`。若某条现有断言变红,是实现错了,**不是去改那条断言**。
- **`AttackBaseline = 100`**,`BattleConfig.PlayerAttack` 默认值必须等于它。
- **ATK 只放大「玩家造成的伤害」**:直接伤害、流血、召唤物攻击力、灼烧每层伤害。**不放大**护盾、治疗、`HealOverTime`、冻结/减速/致盲/沉默的回合数、驱散条数、免疫次数、复活数、`BurnPotency`、反弹百分比、荆的反伤。
- **不新建 `CombatStats` 容器**。生命今天就是 `BattleConfig.PlayerMaxHp` 平铺字段,ATK 跟随同一模式。
- **不写投机的预留代码**。「进阶角色特长」的口子靠伤害链路本来就带着的 `attacker` 属性,不加恒为 1.0 的乘数参数。
- **测试断言只用 Unity 版 NUnit 也支持的 API**:禁用 `Is.AnyOf` / `Is.All.AnyOf`。多选一用 `Is.EqualTo(a).Or.EqualTo(b)`。
- **测试代码禁止直接引用 Newtonsoft**。Tests asmdef 是 `overrideReferences: true` 且只放行 `nunit.framework.dll`;只能引用 Core / Data。
- **注释里的引号一律用 「」**,不要用 ASCII 双引号包中文 —— `tools/fonts/subset_fonts.py` 的 `code_chars()` 用裸正则扫 `"..."`,分不清代码与注释,中文会被打进出货字体。(Tests/ 目录被子集脚本跳过,但 Core/Presentation 不跳。)
- **提交信息用 conventional commits**(feat/fix/test/docs/chore + 范围)。

### ⚠ 用户正在并行开发 —— git 纪律

`git status` 里可能有用户未提交的改动。

- **绝对禁止 `git add -A` / `git add .` / `git commit -a`**
- **只 `git add` 你自己改的那几个文件**,逐个列路径
- ⚠ **绝对不要用 `git stash`** —— 会把用户未提交的改动一起收走。要对比干净状态用 `git show HEAD:<path>`
- 新建的 `.cs` 需要 Unity 生成 `.meta`,Unity 没开时不会生成 —— **不要手写 `.meta`,也不要 `git add` 未跟踪的 `.meta`**

### 验证命令

```bash
# Core/Data 单元测试(首选)
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q

# Presentation 离线编译(改完 Presentation 必跑)
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q

# 管线
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q

# 仿真工装(只有 Task 6 需要)
cd tools/balance && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet run --nologo
```

只看 `error CS`,`warning MSB3245` 是 Unity 程序集自带的无关引用,忽略。

---

## File Structure

| 文件 | 职责 | 任务 |
|---|---|---|
| `Brushblade/Assets/_Project/Core/Meta.cs` | `MetaRules.AttackFor(level)` —— 角色属性成长曲线,纯函数 | 1 |
| `Brushblade/Assets/_Project/Core/BattleEngine.cs` | `BattleConfig.AttackBaseline` 常量、`BattleConfig.PlayerAttack` 字段、`EffectiveAttack` 属性、`ScaleByAttack` 方法,以及四处调用点 | 2, 3, 4 |
| `Brushblade/Assets/_Project/Core/AssemblyInfo.cs` | **新建**。`InternalsVisibleTo("Brushblade.Core.Tests")`,让只为测试存在的入口不必挂到生产 API 面上 | 2 |
| `Brushblade/Assets/_Project/Tests/AttackStatTests.cs` | **新建**。ATK 的全部单元测试 | 2, 3, 4 |
| `Brushblade/Assets/_Project/Tests/MetaTests.cs` | 追加 `AttackFor` 曲线测试 | 1 |
| `Brushblade/Assets/_Project/Presentation/GameRoot.cs` | 把 `AttackFor(角色等级)` 注入 `BattleConfig.PlayerAttack` | 5 |
| `Brushblade/Assets/_Project/Presentation/MapView.cs` | 角色属性上屏(与现有「HP 上限」并排) | 5 |
| `tools/balance/Program.cs` | 三档 profile 喂 `AttackFor(level)` | 6 |

---

## Task 1: 角色攻击成长曲线

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Meta.cs`(在 `MaxHpFor` 之后,约 129 行)
- Test: `Brushblade/Assets/_Project/Tests/MetaTests.cs`

**Interfaces:**
- Consumes: 无
- Produces: `public static int MetaRules.AttackFor(int level)` —— 返回 `Math.Min(150, 100 + 2 * (level - 1))`。Task 5 与 Task 6 消费它。

- [ ] **Step 1: 写失败测试**

追加到 `Brushblade/Assets/_Project/Tests/MetaTests.cs`,放在 `ScaleByCardLevel_TenPercentPerLevel_Ceiled` 那条测试之后:

```csharp
        [TestCase(1, 100)]   // 1 级 = 基准,伤害与引入攻击力之前逐字节相同
        [TestCase(2, 102)]
        [TestCase(11, 120)]
        [TestCase(25, 148)]
        [TestCase(26, 150)]  // 与 MaxHpFor 同在 26 级触顶
        [TestCase(40, 150)]  // 封顶后不再涨
        public void AttackFor_GrowsTwoPerLevel_CapsAt150(int level, int expected)
        {
            Assert.That(MetaRules.AttackFor(level), Is.EqualTo(expected));
        }

        [Test]
        public void AttackFor_CapsAtSameLevelAsMaxHp()
        {
            // 两条角色属性曲线刻意同形同封顶级(19.2.1),口径一致才好记也好平衡。
            // 分开写死会在改了一条忘了另一条时静默漂移,这条守住它们的耦合。
            Assert.That(MetaRules.AttackFor(26), Is.EqualTo(150));
            Assert.That(MetaRules.MaxHpFor(26), Is.EqualTo(100));
            Assert.That(MetaRules.AttackFor(25), Is.LessThan(150));
            Assert.That(MetaRules.MaxHpFor(25), Is.LessThan(100));
        }
```

- [ ] **Step 2: 跑测试确认它红**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 编译失败,`error CS0117: 'MetaRules' does not contain a definition for 'AttackFor'`

- [ ] **Step 3: 实现**

在 `Brushblade/Assets/_Project/Core/Meta.cs` 的 `MaxHpFor` 那一行之后紧接着加:

```csharp
        /// <summary>攻击成长:100 + 2×(等级−1),上限 150(19.2.1 角色属性)。
        /// 与 <see cref="MaxHpFor"/> 同形同封顶级(26 级)——两条角色属性曲线口径一致。
        /// 基准 100:<c>伤害 = 值 × ATK ÷ 100</c>,1 级时恒等于引入攻击力之前。</summary>
        public static int AttackFor(int level) => Math.Min(150, 100 + 2 * (level - 1));
```

- [ ] **Step 4: 跑测试确认它绿**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: `Passed! - Failed: 0, Passed: 799`(原 792 + 6 个 `TestCase` + 1 条耦合测试)。
**关键是 `Failed: 0` 且没有任何原有测试变红**,总数以实际输出为准。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/Meta.cs Brushblade/Assets/_Project/Tests/MetaTests.cs
git commit -m "feat(core): 角色攻击成长曲线 AttackFor,与 MaxHpFor 同形同封顶级"
```

---

## Task 2: 属性载体与恒等缩放,接入直接伤害

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`
  - `BattleConfig`(约 27~30 行):加 `AttackBaseline` 常量与 `PlayerAttack` 字段
  - 常量区(约 118~127 行):无需改动,常量放在 `BattleConfig` 上
  - 字段区(约 139 行 `_playerStatuses` 附近):加 `EffectiveAttack` 属性与 `ScaleByAttack` 方法
  - `ApplyEffects` 的 `DamageSingle` 分支(约 846~854 行)与 `DamageAll` 分支(约 856~866 行)
- Create: `Brushblade/Assets/_Project/Core/AssemblyInfo.cs`(**新建**,InternalsVisibleTo)
- Test: `Brushblade/Assets/_Project/Tests/AttackStatTests.cs`(**新建**)

**Interfaces:**
- Consumes: 无(不依赖 Task 1;`AttackFor` 只在 Presentation 侧被调用)
- Produces:
  - `public const int BattleConfig.AttackBaseline = 100`
  - `public int BattleConfig.PlayerAttack { get; set; }`,默认 `AttackBaseline`
  - `public int BattleEngine.EffectiveAttack { get; }`
  - `private int BattleEngine.ScaleByAttack(int value)` —— Task 3、Task 4 调用它
  - `internal void BattleEngine.ApplyPlayerAttackBuff(int amount)` —— Task 3、Task 4 的测试调用它;靠新建的 `Core/AssemblyInfo.cs` 对 Tests 可见

**为什么这样切:** `ScaleByAttack` 是整个子项目的心脏,它的恒等性是唯一的验收硬线,单独一个评审门。同一任务里连负向覆盖(护盾/治疗/回合数不吃 ATK)一起做——「吃不吃 ATK」完全取决于哪些结算点调了它,正负两侧必须同一个人同一轮想清楚。

- [ ] **Step 1: 写失败测试**

新建 `Brushblade/Assets/_Project/Tests/AttackStatTests.cs`:

```csharp
using System;
using Brushblade.Core;
using NUnit.Framework;

namespace Brushblade.CoreTests
{
    /// <summary>玩家攻击力(19.2.1 角色属性)进入伤害链路。
    ///
    /// 测试字一律用 <see cref="Element.Heart"/> 且不给配方:心对全属性生克都是 1.0x,
    /// 又没有配方就不会触发相生 ×3 —— 于是断言里看到的数字就是 ATK 缩放本身,
    /// 不掺生克。用五行属性的字会让 ×1.5/×3 混进来,算错了也看不出是哪一层的错。</summary>
    public sealed class AttackStatTests
    {
        private static RecipeGraph Graph() => new(new[]
        {
            new CharDef("甲", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.DamageSingle, 20) }),
            new CharDef("乙", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.DamageAll, 10) }),
            new CharDef("丙", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.Shield, 7) }),
            new CharDef("丁", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.HealSelf, 9) }),
            new CharDef("戊", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.Freeze, 2) }),
        });

        private static EnemyDef Dummy(int hp = 500) => new("怔", Element.Heart, hp, 0);

        private static BattleEngine Battle(int attack, params string[] library) =>
            new(Graph(), new BattleConfig { PlayerAttack = attack, PlayerMaxHp = 100 },
                library, Array.Empty<string>(), new[] { Dummy() }, seed: 1);

        // ---- 恒等性硬线 ----

        [Test]
        public void BaselineAttack_LeavesDamageByteIdentical()
        {
            // 这条是整个子项目的地基:基准值下伤害必须与引入攻击力之前一模一样。
            // 它红了意味着 792 条现有断言里会有一批跟着红,实现方向就是错的。
            var engine = Battle(BattleConfig.AttackBaseline, "甲");
            engine.Cast("甲", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(500 - 20));
        }

        [Test]
        public void DefaultConfig_AttackIsBaseline()
        {
            Assert.That(new BattleConfig().PlayerAttack, Is.EqualTo(BattleConfig.AttackBaseline));
            Assert.That(BattleConfig.AttackBaseline, Is.EqualTo(100));
        }

        // ---- 直接伤害吃 ATK ----

        [Test]
        public void HigherAttack_ScalesSingleTargetDamage()
        {
            var engine = Battle(150, "甲");
            engine.Cast("甲", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(500 - 30), "20 × 150 ÷ 100 = 30");
        }

        [Test]
        public void HigherAttack_ScalesAoeDamage()
        {
            var engine = Battle(150, "乙");
            engine.Cast("乙");
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(500 - 15), "10 × 150 ÷ 100 = 15");
        }

        [Test]
        public void IntegerDivision_TruncatesSmallValues()
        {
            // 已知副作用(spec 第十节):整数除会吃掉低数值字的加成。
            // 20 × 102 ÷ 100 = 20(不是 20.4 也不是 21)。
            // 写成测试是为了让它成为**有意的行为**而不是某天被人当 bug「修」掉 ——
            // 真解法是 E-b5 抬高字表数值量级,不是在这里改成 ceil。
            var engine = Battle(102, "甲");
            engine.Cast("甲", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(500 - 20));
        }

        // ---- 局内增益 ----

        [Test]
        public void InCombatAttackBuff_AddsToConfigAttack()
        {
            var engine = Battle(BattleConfig.AttackBaseline, "甲");
            engine.ApplyPlayerAttackBuff(50);
            Assert.That(engine.EffectiveAttack, Is.EqualTo(150));
            engine.Cast("甲", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(500 - 30));
        }

        // ---- 负向:不该吃的没吃 ----

        [Test]
        public void HighAttack_DoesNotScaleShield()
        {
            // 只写正向断言的话,把护盾也乘上 ATK 不会有任何测试发现 ——
            // 子项目 D 的白名单方向性教训,同一类漏洞换了个形状。
            var engine = Battle(150, "丙");
            engine.Cast("丙");
            Assert.That(engine.PlayerShield, Is.EqualTo(7), "护盾不吃攻击力");
        }

        [Test]
        public void HighAttack_DoesNotScaleHeal()
        {
            // PlayerHp 是 { get; private set; },不能用对象初始化器设 ——
            // 起始血量只能走构造参数 startingHp
            var engine = new BattleEngine(Graph(),
                new BattleConfig { PlayerAttack = 150, PlayerMaxHp = 100 },
                new[] { "丁" }, Array.Empty<string>(), new[] { Dummy() }, seed: 1,
                startingHp: 50);
            engine.Cast("丁");
            Assert.That(engine.PlayerHp, Is.EqualTo(59), "治疗不吃攻击力");
        }

        [Test]
        public void HighAttack_DoesNotScaleFreezeTurns()
        {
            var engine = Battle(150, "戊");
            engine.Cast("戊", 0);
            var freeze = engine.Enemies[0].Statuses.Find(StatusKind.Freeze);
            Assert.That(freeze, Is.Not.Null);
            Assert.That(freeze.TurnsLeft, Is.EqualTo(2), "回合数不吃攻击力");
        }

        // ---- 快照 ----

        [Test]
        public void AttackBuff_SurvivesSnapshotRoundTrip()
        {
            var engine = Battle(BattleConfig.AttackBaseline, "甲");
            engine.ApplyPlayerAttackBuff(50);
            var defs = new System.Collections.Generic.Dictionary<string, EnemyDef> { ["怔"] = Dummy() };
            var restored = BattleEngine.Restore(engine.Capture(), Graph(),
                new BattleConfig { PlayerAttack = BattleConfig.AttackBaseline, PlayerMaxHp = 100 },
                null, defs);
            Assert.That(restored.EffectiveAttack, Is.EqualTo(150),
                "局内增益存在 PlayerStatuses 里,快照本来就在存 —— 零新增字段");
        }
    }
}
```

- [ ] **Step 2: 跑测试确认它红**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 编译失败 —— `BattleConfig.AttackBaseline`、`BattleConfig.PlayerAttack`、`BattleEngine.EffectiveAttack`、`BattleEngine.ApplyPlayerAttackBuff` 都不存在。

> ⚠ 新建的 `AttackStatTests.cs` 需要 Unity 生成 `.meta`。Unity 没开时不会生成 —— **不要手写,也不要 `git add` 未跟踪的 `.meta`**,`tools/coretests` 不需要它就能跑。

- [ ] **Step 3: 实现 —— BattleConfig 加常量与字段**

在 `Brushblade/Assets/_Project/Core/BattleEngine.cs` 的 `BattleConfig` 里,`PlayerMaxHp` 那一行**之后**加:

```csharp
        /// <summary>攻击力基准。<see cref="PlayerAttack"/> 等于此值时,
        /// 伤害与引入攻击力之前**逐字节相同** —— 这是 E-b1 的验收硬线。</summary>
        public const int AttackBaseline = 100;

        /// <summary>玩家攻击力(19.2.1 角色属性)。由 GameRoot 按
        /// <c>MetaRules.AttackFor(角色等级)</c> 注入;工装与测试不给就取基准值。</summary>
        public int PlayerAttack { get; set; } = AttackBaseline;
```

- [ ] **Step 3.5: 新建 `Core/AssemblyInfo.cs`,放开 internal 给测试**

下一步的 `ApplyPlayerAttackBuff` 是 `internal`(它只为测试存在,不该出现在生产 API 面上,
2026-08-11 用户裁定)。Tests 是独立 asmdef,默认看不见 internal,所以要放开。

新建 `Brushblade/Assets/_Project/Core/AssemblyInfo.cs`:

```csharp
using System.Runtime.CompilerServices;

// Tests 是独立 asmdef,默认看不见 Core 的 internal。放开是为了让「只为测试存在」的入口
// (BattleEngine.ApplyPlayerAttackBuff)不必挂到生产 API 面上。
// 程序集名取自 Tests/Brushblade.Core.Tests.asmdef 的 name 字段,改名要同步改这里。
[assembly: InternalsVisibleTo("Brushblade.Core.Tests")]
```

> ⚠⚠ **这条 attribute 只对 Unity 有意义,而 `tools/coretests` 永远验证不了它。**
>
> `tools/coretests/Brushblade.CoreTests.csproj` 用 `EnableDefaultCompileItems=false` +
> `<Compile Include>` 把 `Core/**`、`Data/**`、`Tests/**` 全部编进**同一个程序集**
> (`Brushblade.CoreTests`)—— 同程序集内 `internal` 本来就可见,加不加这条 attribute 都一样绿。
> 只有 Unity 里 `Brushblade.Core` 与 `Brushblade.Core.Tests` 才是两个 asmdef,才真的需要它。
>
> 这就是 CLAUDE.md 记的「工装绿≠编辑器绿」。**不要**把 Step 6 的绿当成这条 attribute 生效的证据。
> 唯一的证据是用户在 Unity Test Runner 里跑一次 —— 已写进收尾验收的交接清单。
>
> 因此:字符串必须**逐字**等于 `Brushblade/Assets/_Project/Tests/Brushblade.Core.Tests.asmdef`
> 里 `"name"` 字段的值,拼错了工装照样绿、编辑器才炸。当前值是 `Brushblade.Core.Tests`。
>
> ⚠ 这是项目里首次使用 `InternalsVisibleTo`,没有先例可抄。
> 新建的 `.cs` 需要 Unity 生成 `.meta` —— **不要手写,也不要 `git add` 未跟踪的 `.meta`**。

- [ ] **Step 4: 实现 —— BattleEngine 加属性与缩放方法**

在 `Brushblade/Assets/_Project/Core/BattleEngine.cs` 里 `_statusSerial` 字段之后、`DamageReductionMultiplier` 属性之前加:

```csharp
        /// <summary>本场生效的玩家攻击力 = 角色属性(config)+ 局内增益。
        /// 局内增益复用 <see cref="StatusKind.AttackBuff"/> —— 敌人侧的标点小妖加攻、
        /// 焦痕受击自燃早就在用同一个 Kind,不新增枚举值。
        /// 钳到 ≥0 与 <see cref="EnemyState.Attack"/> 同口径:负攻击力会打出负伤害,
        /// 等于给敌人回血,且全程无声。</summary>
        public int EffectiveAttack =>
            Math.Max(0, _config.PlayerAttack + _playerStatuses.TotalMagnitude(StatusKind.AttackBuff));

        /// <summary>按玩家攻击力缩放一个输出值。**整数除**:
        /// <c>EffectiveAttack == AttackBaseline</c> 时 <c>value * 100 / 100 == value</c>,逐字节恒等。
        ///
        /// 刻意不用 <c>ceil</c>:<c>ceil(7 × 1.02) = 8</c> 等于 +14%,低数值字反而超额收益,
        /// 方向是错的。低数值字在攻击成长前期没反应是已知副作用,
        /// 真解法是 E-b5 抬高字表数值量级(见 spec 第十节)。</summary>
        private int ScaleByAttack(int value) => value * EffectiveAttack / BattleConfig.AttackBaseline;

        /// <summary>给玩家挂一层攻击增益。E-b3 的 剡/战意 会走正规的效果分支,
        /// 在那之前这是局内改变攻击力的唯一入口,现阶段只有测试在用。
        ///
        /// internal 而非 public(2026-08-11 用户裁定):它只为测试存在,不该出现在生产 API 面上。
        /// 见同目录 AssemblyInfo.cs 的 InternalsVisibleTo。
        /// SourceId 每次唯一(同 HoT / 焦痕自燃的做法):Apply() 会按 SourceId 覆盖同源条目,
        /// 不给唯一 id 的话第二次挂增益会覆盖第一次而不是叠加。</summary>
        internal void ApplyPlayerAttackBuff(int amount)
        {
            _playerStatuses.Apply(new StatusEffect
            {
                Kind = StatusKind.AttackBuff, Polarity = StatusPolarity.Buff,
                Magnitude = amount, TurnsLeft = -1,
                SourceId = $"debug-atk#{_statusSerial++}",
            });
        }
```

- [ ] **Step 5: 实现 —— 接入直接伤害两处调用点**

`ApplyEffects` 的 `DamageSingle` 分支,把

```csharp
                            DamageEnemy(targetIndex,
                                ExecuteBonus(effect, targetIndex, BaseValue(effect, value, _enemies[targetIndex])),
                                recipeElements, attacker, effect.IgnoreArmor);
```

改成

```csharp
                            // ATK 缩放在最外层:先过卡等级 → 灼烧翻倍 → 残血加伤,最后整体乘攻击力,
                            // 再交给 DamageEnemy 过生克与减伤。放在里层会与那几个 ×2 的取整互相干扰
                            DamageEnemy(targetIndex,
                                ScaleByAttack(ExecuteBonus(effect, targetIndex,
                                    BaseValue(effect, value, _enemies[targetIndex]))),
                                recipeElements, attacker, effect.IgnoreArmor);
```

`DamageAll` 分支,把

```csharp
                            DamageEnemy(i,
                                ExecuteBonus(effect, i, BaseValue(effect, value, _enemies[i])),
                                recipeElements, attacker, effect.IgnoreArmor);
```

改成

```csharp
                            DamageEnemy(i,
                                ScaleByAttack(ExecuteBonus(effect, i, BaseValue(effect, value, _enemies[i]))),
                                recipeElements, attacker, effect.IgnoreArmor);
```

- [ ] **Step 6: 跑测试确认全绿**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: `Failed: 0`。**若任何原有测试变红,是实现错了,不要改那条断言** —— 回到 Step 3~5 找恒等性在哪里破了。

- [ ] **Step 7: 变异检查(项目标准)**

逐个做,做完**务必用 `git checkout -- <path>` 还原**(⚠ 不要用 `git stash`):

| 变异 | 期望 |
|---|---|
| 把 `ScaleByAttack` 改成 `=> value`(恒等) | `HigherAttack_ScalesSingleTargetDamage` 与 `..AoeDamage` 变红 |
| 给 `Shield` 分支的 `value` 套上 `ScaleByAttack` | `HighAttack_DoesNotScaleShield` 变红 |
| 给 `Freeze` 分支的 `TurnsLeft` 套上 `ScaleByAttack` | `HighAttack_DoesNotScaleFreezeTurns` 变红 |
| 把 `EffectiveAttack` 里的 `+ _playerStatuses.TotalMagnitude(...)` 删掉 | `InCombatAttackBuff_AddsToConfigAttack` 与 `AttackBuff_SurvivesSnapshotRoundTrip` 变红 |

任何一条**没有**变红,说明该断言没有辨别力,补测试而不是放过。把四条的结果写进报告。

- [ ] **Step 8: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs \
  Brushblade/Assets/_Project/Core/AssemblyInfo.cs \
  Brushblade/Assets/_Project/Tests/AttackStatTests.cs
git commit -m "feat(core): 玩家攻击力进伤害链路,基准 100 时逐字节恒等"
```

---

## Task 3: 流血与召唤物攻击接入

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`
  - `ApplyEffects` 的 `Bleed` 分支(约 875~884 行)
  - `ApplyEffects` 的 `Summon` 分支(约 1069~1089 行)
- Test: `Brushblade/Assets/_Project/Tests/AttackStatTests.cs`

**Interfaces:**
- Consumes: `private int BattleEngine.ScaleByAttack(int value)`(Task 2)
- Produces: 无新公开接口

**关键约束:** 这两处都是**出牌时快照**——`StatusEffect.Magnitude` 与 `SummonState.Attack` 本来就是施加/创建时定死的,所以套上 `ScaleByAttack` 天然就是快照语义,零新增字段。必须同时断言「事后抬 ATK,已在场的召唤物攻击力不变」,否则实现成实时读也没人发现。

- [ ] **Step 1: 写失败测试**

追加到 `AttackStatTests.cs` 的 `Graph()` 里两个字(放在 `戊` 之后):

```csharp
            new CharDef("己", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.Bleed, 4) }),
            new CharDef("庚", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.Summon, 20, summonCount: 1,
                    summonAttack: 6, summonChar: "木") }),
```

追加测试(放在类的末尾、快照那条之前):

```csharp
        // ---- 流血:施加时吃 ATK ----

        [Test]
        public void HigherAttack_ScalesBleedAtApplyTime()
        {
            var engine = Battle(150, "己");
            engine.Cast("己", 0);
            var bleed = engine.Enemies[0].Statuses.Find(StatusKind.Bleed);
            Assert.That(bleed, Is.Not.Null);
            Assert.That(bleed.Magnitude, Is.EqualTo(6), "4 × 150 ÷ 100 = 6");
        }

        [Test]
        public void Bleed_DoesNotRetroactivelyScale()
        {
            // 出牌时快照:挂上之后再抬攻击力,已挂的流血不变。
            // 没有这条,把 ScaleByAttack 挪到流血结算处(实时读)也不会有测试红。
            var engine = Battle(BattleConfig.AttackBaseline, "己");
            engine.Cast("己", 0);
            engine.ApplyPlayerAttackBuff(100);
            var bleed = engine.Enemies[0].Statuses.Find(StatusKind.Bleed);
            Assert.That(bleed.Magnitude, Is.EqualTo(4), "已挂的流血不回溯");
        }

        // ---- 召唤物:创建时吃 ATK ----

        [Test]
        public void HigherAttack_ScalesSummonAttackAtBirth()
        {
            var engine = Battle(150, "庚");
            engine.Cast("庚");
            Assert.That(engine.Summons.Count, Is.EqualTo(1));
            Assert.That(engine.Summons[0].Attack, Is.EqualTo(9), "6 × 150 ÷ 100 = 9");
        }

        [Test]
        public void SummonHp_DoesNotScaleWithAttack()
        {
            // 召唤物的**血量**是防御资源,不吃攻击力;只有它的攻击力吃。
            var engine = Battle(150, "庚");
            engine.Cast("庚");
            Assert.That(engine.Summons[0].MaxHp, Is.EqualTo(20), "召唤物血量不吃攻击力");
        }

        [Test]
        public void ExistingSummon_DoesNotRetroactivelyScale()
        {
            var engine = Battle(BattleConfig.AttackBaseline, "庚");
            engine.Cast("庚");
            engine.ApplyPlayerAttackBuff(100);
            Assert.That(engine.Summons[0].Attack, Is.EqualTo(6), "已在场的召唤物不回溯");
        }
```

- [ ] **Step 2: 跑测试确认它红**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: `HigherAttack_ScalesBleedAtApplyTime` 期望 6 实得 4;`HigherAttack_ScalesSummonAttackAtBirth` 期望 9 实得 6。另外两条(不回溯)此时已经是绿的 —— 它们守的是**将来别改坏**,现在绿是正常的。

- [ ] **Step 3: 实现 —— 流血**

`Bleed` 分支里,把

```csharp
                                Magnitude = value, TurnsLeft = 3,   // 固定 3 回合
```

改成

```csharp
                                // 出牌时吃攻击力:Magnitude 本来就是施加时定死的,套上即为快照语义
                                Magnitude = ScaleByAttack(value), TurnsLeft = 3,   // 固定 3 回合
```

- [ ] **Step 4: 实现 —— 召唤物攻击力**

`Summon` 分支里,把

```csharp
                            var newborn = new SummonState(effect.SummonChar, attacker, value,
                                MetaRules.ScaleByCardLevel(effect.SummonAttack, cardLevel),
                                effect.Passive);
```

改成

```csharp
                            // 召唤时吃攻击力:只作用于攻击力,血量(value)是防御资源不吃。
                            // SummonState.Attack 本来就是创建时常量,套上即为快照语义 ——
                            // 之后再抬攻击力,已在场的这只不变
                            var newborn = new SummonState(effect.SummonChar, attacker, value,
                                ScaleByAttack(MetaRules.ScaleByCardLevel(effect.SummonAttack, cardLevel)),
                                effect.Passive);
```

- [ ] **Step 5: 跑测试确认全绿**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: `Failed: 0`,且没有任何原有测试变红。

- [ ] **Step 6: 变异检查**

做完用 `git checkout -- <path>` 还原(⚠ 不要 `git stash`):

| 变异 | 期望 |
|---|---|
| 流血的 `ScaleByAttack(value)` 改回 `value` | `HigherAttack_ScalesBleedAtApplyTime` 变红 |
| 召唤物的 `ScaleByAttack(...)` 改回原样 | `HigherAttack_ScalesSummonAttackAtBirth` 变红 |
| 把召唤物的 `value`(血量)也套上 `ScaleByAttack` | `SummonHp_DoesNotScaleWithAttack` 变红 |

结果写进报告。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/AttackStatTests.cs
git commit -m "feat(core): 流血与召唤物攻击力吃攻击力,出牌/召唤时快照"
```

---

## Task 4: 灼烧与引爆接入(回溯口径)

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`
  - `SettleBurnOn`(约 1120~1140 行,`int tick = ...` 那一句)
  - `Detonate`(约 1155~1170 行,`int damage = ...` 那一句)
- Test: `Brushblade/Assets/_Project/Tests/AttackStatTests.cs`

**Interfaces:**
- Consumes: `private int BattleEngine.ScaleByAttack(int value)`(Task 2)—— **但这一处不用它**,见下
- Produces: 无新公开接口

**关键约束(与 Task 3 相反):** 灼烧的每层伤害不存在状态上,它是 `_burnPerStack` 这个全场全局标量,而且**今天就已经回溯生效**(`炽`/`BurnPotency` 一抬,场上已挂的层全部跟着变强)。所以灼烧**结算时读当前 ATK**,是回溯的 —— 沿用 `炽` 已确立的口径,不是新的不一致。

这一处**不能**复用 `ScaleByAttack`:那个方法是整数除,而灼烧本来就是一条浮点式子(`Math.Floor(层数 × 系数 × 生克)`),要把 ATK 比值插进同一个浮点表达式里,晚截断,才能保住恒等。

- [ ] **Step 1: 写失败测试**

追加到 `AttackStatTests.cs`(放在召唤那几条之后)。先在 `Graph()` 里加两个字:

```csharp
            new CharDef("辛", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.BurnSingle, 3) }),
            new CharDef("壬", Element.Heart,
                effects: new[] { new EffectDef(EffectKind.Detonate, 0) }),
```

再加测试:

```csharp
        // ---- 灼烧:结算时读,回溯生效(与炽同款口径)----

        [Test]
        public void BaselineAttack_LeavesBurnTickIdentical()
        {
            // 恒等性硬线在灼烧这条链上的对应物。
            // 3 层 × 每层 2 = 6,与引入攻击力之前一致
            var engine = Battle(BattleConfig.AttackBaseline, "辛");
            engine.Cast("辛", 0);
            engine.EndTurn();
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(500 - 6));
        }

        [Test]
        public void HigherAttack_ScalesBurnTick()
        {
            var engine = Battle(150, "辛");
            engine.Cast("辛", 0);
            engine.EndTurn();
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(500 - 9), "floor(3 × 2 × 1.5) = 9");
        }

        [Test]
        public void Burn_RetroactivelyScalesWithAttack()
        {
            // 与流血/召唤物**相反**:灼烧回溯。先挂满层再抬攻击力,已挂的层照样变强。
            // 这不是不一致,是沿用炽/BurnPotency 已确立的口径 —— 每层伤害本来就是
            // _burnPerStack 这个全局标量,从来不是出牌时冻结的量。
            var engine = Battle(BattleConfig.AttackBaseline, "辛");
            engine.Cast("辛", 0);
            engine.ApplyPlayerAttackBuff(50);
            engine.EndTurn();
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(500 - 9), "已挂的层吃新攻击力");
        }

        [Test]
        public void BurnStackCount_DoesNotScaleWithAttack()
        {
            // 只放大每层伤害,不放大层数(spec 第三节)。
            // 层数被放大的话总伤害会按 N(N+1)/2 平方级膨胀,那是另一回事。
            var engine = Battle(150, "辛");
            engine.Cast("辛", 0);
            Assert.That(engine.Enemies[0].Statuses.TotalMagnitude(StatusKind.Burn),
                Is.EqualTo(3), "层数不吃攻击力");
        }

        [Test]
        public void HigherAttack_ScalesDetonate()
        {
            // 引爆 = N(N+1)/2 × 每层伤害,同口径吃攻击力。
            // 3 层:3×4/2 = 6 → 6 × 2 = 12 → ×1.5 = 18
            var engine = Battle(150, "辛", "壬");
            engine.Cast("辛", 0);
            engine.Cast("壬", 0);
            Assert.That(engine.Enemies[0].Hp, Is.EqualTo(500 - 18));
        }
```

- [ ] **Step 2: 跑测试确认它红**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: `HigherAttack_ScalesBurnTick` 期望 9 实得 6;`Burn_RetroactivelyScalesWithAttack` 期望 9 实得 6;`HigherAttack_ScalesDetonate` 期望 18 实得 12。恒等与层数那两条应当已经绿。

- [ ] **Step 3: 实现 —— 灼烧结算**

`SettleBurnOn` 里,把

```csharp
            int tick = (int)Math.Floor(burn.Magnitude * _burnPerStack
                * WuxingResolver.KeMultiplier(Element.Fire, enemy.Element));
```

改成

```csharp
            // 攻击力**结算时读**,回溯生效 —— 与炽/BurnPotency 同口径:每层伤害从来不是
            // 出牌时冻结的量,它是 _burnPerStack 这个全局标量。层数(Magnitude)不吃攻击力。
            // 不复用 ScaleByAttack:那是整数除(早截断),这里要插进既有的浮点式子里晚截断,
            // 才能在基准值下保住逐字节恒等
            int tick = (int)Math.Floor(burn.Magnitude * _burnPerStack
                * (EffectiveAttack / (double)BattleConfig.AttackBaseline)
                * WuxingResolver.KeMultiplier(Element.Fire, enemy.Element));
```

- [ ] **Step 4: 实现 —— 引爆**

`Detonate` 里,把

```csharp
            int damage = (int)Math.Floor(stacks * (stacks + 1) / 2.0 * _burnPerStack
                * WuxingResolver.KeMultiplier(Element.Fire, enemy.Element));
```

改成

```csharp
            // 与 SettleBurnOn 同口径吃攻击力:引爆是把剩余层数一次性兑现,
            // 每层伤害用的是同一个量,不能只有一边吃
            int damage = (int)Math.Floor(stacks * (stacks + 1) / 2.0 * _burnPerStack
                * (EffectiveAttack / (double)BattleConfig.AttackBaseline)
                * WuxingResolver.KeMultiplier(Element.Fire, enemy.Element));
```

- [ ] **Step 5: 跑测试确认全绿**

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: `Failed: 0`。灼烧相关的原有测试(`BurnVariantTests`、`BattleEngineTests` 里的灼烧段)必须全绿 —— 它们用默认 config,`EffectiveAttack == 100`,比值恰为 1.0。

- [ ] **Step 6: 变异检查**

做完用 `git checkout -- <path>` 还原(⚠ 不要 `git stash`):

| 变异 | 期望 |
|---|---|
| `SettleBurnOn` 的 ATK 比值删掉 | `HigherAttack_ScalesBurnTick` 与 `Burn_RetroactivelyScalesWithAttack` 变红 |
| `Detonate` 的 ATK 比值删掉 | `HigherAttack_ScalesDetonate` 变红 |
| 把 `ApplyBurn` 的层数套上 `ScaleByAttack` | `BurnStackCount_DoesNotScaleWithAttack` 变红 |
| 把 `(double)` 去掉写成整数除 `EffectiveAttack / BattleConfig.AttackBaseline` | 基准值下 `100/100 == 1` 仍恒等,但 ATK 150 时 `150/100 == 1` → `HigherAttack_ScalesBurnTick` 变红 |

最后一条尤其要做:整数除会把比值直接截成 1,是个很容易写出来又很难看出来的错。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/AttackStatTests.cs
git commit -m "feat(core): 灼烧与引爆吃攻击力,结算时读沿用炽的回溯口径"
```

---

## Task 5: Presentation 注入与属性上屏

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/GameRoot.cs`(约 191~196 行,`maxHp` 计算与 `battleConfig` 构造处)
- Modify: `Brushblade/Assets/_Project/Presentation/MapView.cs`(约 94~95 行,角色信息栏)

**Interfaces:**
- Consumes: `MetaRules.AttackFor(int level)`(Task 1)、`BattleConfig.PlayerAttack`(Task 2)
- Produces: 无

**为什么单独一个任务:** Presentation 不在 `tools/coretests` 的覆盖范围内,验证手段是离线编译而不是单元测试,失败模式也不同(编译错会一路漏到用户打开 Unity 才炸,已发生过两次)。

- [ ] **Step 1: 注入 PlayerAttack**

在 `Brushblade/Assets/_Project/Presentation/GameRoot.cs` 里,把

```csharp
            int maxHp = MetaRules.MaxHpFor(MetaRules.CharacterLevel(_meta.CharacterXp))
                + PerkRules.HpBonus(_meta);
            var battleConfig = new BattleConfig
            {
                DropTable = _campaign.DropTable,
                PlayerMaxHp = maxHp,
```

改成

```csharp
            int characterLevel = MetaRules.CharacterLevel(_meta.CharacterXp);
            int maxHp = MetaRules.MaxHpFor(characterLevel) + PerkRules.HpBonus(_meta);
            var battleConfig = new BattleConfig
            {
                DropTable = _campaign.DropTable,
                PlayerMaxHp = maxHp,
                // 攻击力与生命同为角色属性(19.2.1),同一条链注入。
                // 「被动技能加攻击」的口子将来在这里 + 一个 Bonus 项,与 HpBonus 并排
                PlayerAttack = MetaRules.AttackFor(characterLevel),
```

> 这段原本对 `MetaRules.CharacterLevel(_meta.CharacterXp)` 求值一次只用于 `maxHp`;
> 提成局部变量 `characterLevel` 是因为现在有两个消费者。**不要**改动同文件约 150 行处
> 那个 `int level = ...`(那是另一个方法里的),两处互不相干。

- [ ] **Step 2: 属性上屏**

在 `Brushblade/Assets/_Project/Presentation/MapView.cs` 里,把

```csharp
            Ui.ThemedLabel(header.transform,
                $"经验 {_meta.CharacterXp}    HP 上限 {MetaRules.MaxHpFor(level) + PerkRules.HpBonus(_meta)}", 20, Theme.TextDim);
```

改成

```csharp
            Ui.ThemedLabel(header.transform,
                $"经验 {_meta.CharacterXp}    HP 上限 {MetaRules.MaxHpFor(level) + PerkRules.HpBonus(_meta)}"
                + $"    攻击 {MetaRules.AttackFor(level)}", 20, Theme.TextDim);
```

> 「攻击」两个字是新增的中文字面量,`tools/fonts/subset_fonts.py` 会扫到并打进字体子集。
> 「攻」「击」都是常用字,Noto 必有,不会出空框。Step 4 会跑一次子集脚本确认。

- [ ] **Step 3: 离线编译**

```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
```

Expected: `Build succeeded`,零 `error CS`。

- [ ] **Step 4: 重跑字体子集并判断要不要提交**

```bash
python3 tools/fonts/subset_fonts.py
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
```

⚠ **字体 `.ttf` 变成 modified 不代表内容变了。** 用 cmap 判断,不要看文件是否 modified:

```bash
python3 - <<'PY'
from fontTools.ttLib import TTFont
import subprocess, pathlib
p = pathlib.Path("/tmp/head-sans.ttf")
p.write_bytes(subprocess.run(
    ["git","show","HEAD:Brushblade/Assets/_Project/Presentation/Fonts/Resources/NotoSansSC-Subset.ttf"],
    capture_output=True).stdout)
old = set(TTFont(p, lazy=True).getBestCmap())
new = set(TTFont("Brushblade/Assets/_Project/Presentation/Fonts/Resources/NotoSansSC-Subset.ttf",
                 lazy=True).getBestCmap())
print("字符集相同:", old == new)
print("新增:", "".join(chr(c) for c in sorted(new - old)) or "无")
print("移除:", "".join(chr(c) for c in sorted(old - new)) or "无")
PY
```

- 字符集**相同** → `git checkout -- Brushblade/Assets/_Project/Presentation/Fonts/Resources/` 还原,不提交字体
- 字符集**新增了「攻」「击」** → 连同 `tools/fonts/charset.txt` 与两个 `.ttf` 一起提交

- [ ] **Step 5: 提交**

按 Step 4 的判断决定要不要带字体文件:

```bash
git add Brushblade/Assets/_Project/Presentation/GameRoot.cs Brushblade/Assets/_Project/Presentation/MapView.cs
# 若字符集有变,再加:
#   git add tools/fonts/charset.txt \
#     Brushblade/Assets/_Project/Presentation/Fonts/Resources/NotoSansSC-Subset.ttf \
#     Brushblade/Assets/_Project/Presentation/Fonts/Resources/NotoSerifSC-Subset.ttf
git commit -m "feat(presentation): 注入角色攻击力并在角色信息栏上屏"
```

---

## Task 6: 仿真工装接入攻击力

**Files:**
- Modify: `tools/balance/Program.cs`(`Profile` 类约 53~61 行;三档 profile 约 36~44 行;`battleConfig` 构造约 101~106 行)

**Interfaces:**
- Consumes: `MetaRules.AttackFor(int level)`(Task 1)、`BattleConfig.PlayerAttack`(Task 2)
- Produces: 无

**为什么必须做:** 不接的话 E-b5 重平衡会瞎。这是有前科的 —— E-a 时工装的 `UnlockedChars` 锁在 9 个火系字上,新字根本进不了池,「P50 没变」被误当成「没有影响」,复核才发现工装压根看不见新字。

- [ ] **Step 1: Profile 加字段**

把

```csharp
        private sealed class Profile
        {
            public string Name;
            public IReadOnlyList<string> Library;
            public Dictionary<string, int> CardLevels;
            public int MaxHp;
            public Profile(string name, IReadOnlyList<string> library, Dictionary<string, int> cardLevels, int maxHp)
            { Name = name; Library = library; CardLevels = cardLevels; MaxHp = maxHp; }
        }
```

改成

```csharp
        private sealed class Profile
        {
            public string Name;
            public IReadOnlyList<string> Library;
            public Dictionary<string, int> CardLevels;
            public int MaxHp;
            public int Attack;
            public Profile(string name, IReadOnlyList<string> library, Dictionary<string, int> cardLevels,
                int maxHp, int attack)
            { Name = name; Library = library; CardLevels = cardLevels; MaxHp = maxHp; Attack = attack; }
        }
```

- [ ] **Step 2: 三档 profile 喂攻击力**

把

```csharp
                new Profile("新手(灯,1级,HP50)", new[] { "灯" },
                    new Dictionary<string, int>(), MetaRules.MaxHpFor(1)),
                new Profile("小成长(灼炎烧灯,卡3级,HP54)", new[] { "灼", "炎", "烧", "灯" },
                    FireCards.ToDictionary(c => c, _ => 3), MetaRules.MaxHpFor(3)),
                new Profile("养成(焚炽灼燚,卡5级,HP68)", new[] { "焚", "炽", "灼", "燚" },
                    FireCards.ToDictionary(c => c, _ => 5), MetaRules.MaxHpFor(10)),
```

改成

```csharp
                // 攻击力与生命取同一个角色等级(2026-08-11 E-b1):画像的等级此前只体现在血量上,
                // 攻击力恒为基准 —— 那会让 E-b5 重平衡看不见攻击成长这一整条轴
                new Profile("新手(灯,1级,HP50,ATK100)", new[] { "灯" },
                    new Dictionary<string, int>(), MetaRules.MaxHpFor(1), MetaRules.AttackFor(1)),
                new Profile("小成长(灼炎烧灯,卡3级,HP54,ATK104)", new[] { "灼", "炎", "烧", "灯" },
                    FireCards.ToDictionary(c => c, _ => 3), MetaRules.MaxHpFor(3), MetaRules.AttackFor(3)),
                new Profile("养成(焚炽灼燚,卡5级,HP68,ATK118)", new[] { "焚", "炽", "灼", "燚" },
                    FireCards.ToDictionary(c => c, _ => 5), MetaRules.MaxHpFor(10), MetaRules.AttackFor(10)),
```

> 第三档原本就是「HP 取 10 级、卡取 5 级」的混搭(名字写 HP68 = `MaxHpFor(10)`),
> 攻击力跟随同一个 10 级,保持画像内部一致。名字里的 ATK 数字是
> `AttackFor(1)=100 / AttackFor(3)=104 / AttackFor(10)=118`,与实现对得上。

- [ ] **Step 3: 注入 BattleConfig**

把

```csharp
                var battleConfig = new BattleConfig
                {
                    DropTable = campaign.DropTable, PlayerMaxHp = profile.MaxHp,
                    UnlockedChars = FireCards,
                };
```

改成

```csharp
                var battleConfig = new BattleConfig
                {
                    DropTable = campaign.DropTable, PlayerMaxHp = profile.MaxHp,
                    PlayerAttack = profile.Attack,
                    UnlockedChars = FireCards,
                };
```

- [ ] **Step 4: 跑工装,确认它真的看见了攻击力**

```bash
cd tools/balance && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet run --nologo
```

Expected:
- 编译通过、跑完三档
- **1 级画像的死亡深度与改造前完全一致**(`AttackFor(1) == 100 == 基准` → 恒等)
- **3 级与 10 级画像的死亡深度应当变深**(攻击力 104 / 118)

⚠ 若 3 级与 10 级也**完全没变**,不要当成「影响很小」就放过 —— 那更可能是 `PlayerAttack` 根本没接进去。回去检查 Step 3。「没有变化」与「看不见」在仿真数据里长得一模一样,这正是 E-a 踩过的坑。

把三档改造前后的死亡深度数字写进报告。

- [ ] **Step 5: 提交**

```bash
git add tools/balance/Program.cs
git commit -m "chore(balance): 仿真画像接入角色攻击力,否则 E-b5 重平衡看不见这条轴"
```

---

## 收尾验收

全部任务完成后,在合并前跑一遍三套:

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd ../prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q
cd ../.. && python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
```

**验收硬线**:`Failed: 0`,且**原有 792 条测试里没有任何一条被修改过**。用这条命令自查:

```bash
git diff main --stat -- Brushblade/Assets/_Project/Tests/
```

Expected:只有 `AttackStatTests.cs`(新建)与 `MetaTests.cs`(纯追加)出现。若其他测试文件有改动,说明恒等性破了而有人去改断言迁就它 —— 那是本计划最需要防的失败模式。

**留给用户在 Unity 里做的**:
1. 为新建的 `AttackStatTests.cs` 与 `Core/AssemblyInfo.cs` 生成 `.meta` 并提交
2. **跑一次 EditMode Test Runner** —— 这是  是否生效的**唯一**证据:
    把 Core 与 Tests 编进同一个程序集,那条 attribute 在工装里加不加都绿,
   拼错了也发现不了。编辑器里若报  不可访问,就是它没生效
3. 在跑图界面看一眼「攻击 100」有没有正常上屏、有没有挤到墨锭图标
