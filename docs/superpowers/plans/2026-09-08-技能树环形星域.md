# 技能树环形星域 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把已合并的三页签规则网格技能树改造成一张可平移缩放的环形星域画布,并加入 3 个跨树节点(效果 = 保底值 + 已投资量 × 增量)。

**Architecture:** Core 侧只加三个字段(`Prereq` / `Scaling` / `BaseValue`)与两个查询(`PrereqMet` / `ScaleCountOf`),40 个既有节点走的代码路径逐字不变;Presentation 侧 `PerkView` 整份重写成极坐标画布,`PerkNodeSheet` 从居中弹窗改成右侧推出。

**Tech Stack:** Unity 6000.5.2f1 / C# / uGUI(无 TMP,走 `Ui` 静态构建器)/ Python(图标与字体子集管线)

**Spec:** `docs/superpowers/specs/2026-09-08-技能树环形星域-design.md`
**上游规格(仍有效,管 40 个节点的内容):** `docs/superpowers/specs/2026-09-07-技能树重构-design.md`

---

## Global Constraints

这些是全项目硬线,**每个 Task 的验收都隐含包含本节**。违反任何一条的实现一律打回。

1. **Core 与 Data 禁止引用 UnityEngine**。`Perk.cs` / `Meta.cs` 里不许出现 `using UnityEngine`、`Mathf`、`Vector2`,取最小值用 `System.Math.Min`。
2. **随机性一律走 Core 内带种子的 RNG**。本计划不新增任何随机,若你觉得需要,那是理解错了。
3. **玩家可见中文文案走字符串表**(`Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`,读取走 `Strings.T(key, args)`),禁止硬编码。
4. **⚠ 新增任何玩家可见中文后必须重跑字体子集**:`python3 tools/fonts/subset_fonts.py`,然后 `python3 -m pytest tools/fonts/tests/ -q`。游戏打包的是子集字体,新字不在里面就在真机上是豆腐块,而离线编译与 Core 单测**都发现不了**。本次已知缺字:**「济」「融」**(见 Task 5)。
5. **⚠ 图标是产物不能手写**。加一枚要同时改三处:`tools/icons/build_icons.py` 的 `ICONS`、`tools/icons/tests/test_icons.py` 的 `EXPECTED`、`Brushblade/Assets/_Project/Presentation/UI/Icons.cs` 的 `Glyphs`。缺任一处的后果不是编译错,是上线渲染成「?」。
6. **测试断言 API 限制(工装绿 ≠ 编辑器绿)**:
   - 禁用 `Is.AnyOf` / `Is.All.AnyOf`(Unity 自带 NUnit 没有)。多选一写 `Is.EqualTo(a).Or.EqualTo(b)`。
   - `Does.Not.Contain(x)` **只在 x 是 string 时**能用。集合判包含一律写 `Assert.That(list.Contains(x), Is.True, "…")`。
   - 定位仓库根**只能用** `TestContext.CurrentContext.TestDirectory`,禁用 `AppContext.BaseDirectory`。
   - 测试**禁止直接引用 Newtonsoft**(`JsonConvert` 等),Tests asmdef 只放行 `nunit.framework.dll`。要读配置走 `Data.ConfigLoader`。
   - 测试只能引用 Tests asmdef references 列出的程序集(Core / Data)。
7. **恒等性硬线**:没点任何跨树节点时,`PerkRules.Bonus()` / `CanUnlock()` / `MetaRules.BuildBattleConfig()` 的结果与改前**逐字节相同**。既有 1680 条测试里,**只有 `PerkTests.Nodes_AreFortyAcrossThreeTrees` 一条允许改**(表的形状真变了),其余任何一条变红都是 bug。
8. **提交信息用 conventional commits**(`feat` / `fix` / `docs` / `chore` + 范围),中文正文,结尾加:
   ```
   Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
   ```
9. **只提交你这个 Task 改的文件**。仓库里可能有别人未完成的改动,`git add -A` 会把它们一起带走。每步的 `git add` 都已在计划里逐个列出文件名,照抄,别用 `-A`。
10. **别用 `git stash`**:worktree 与主检出共享同一个 stash 栈,`pop` 会弹出别人的改动。要对比改动前后用 `git show HEAD:<path>`。

### 验证命令(绝对路径,复制即用)

```bash
# Core/Data 单元测试(首选;毫秒级,不依赖编辑器锁)
cd /Users/eugenewu/code/game/tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

```bash
# Presentation 离线编译(改完 Presentation 必跑;coretests 盖不到这一层)
cd /Users/eugenewu/code/game/tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

```bash
# Python 管线(三个目录都要跑 —— 漏掉 tools/icons/ 会让「手写一张 PNG」全绿通过)
cd /Users/eugenewu/code/game && python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ -q
```

⚠ 离线编译只看 `error CS`;`warning MSB3245` 是 Unity 程序集自带的无关引用,忽略。
⚠ 在 git worktree 里跑 prescompile **必须**带上面那个 `-p:ProjectAsm=...`(`Brushblade/Library/` 不入 git,新 worktree 里不存在,不借主检出的会 CS0006 找不到 UI/TMP)。

---

## File Structure

| 文件 | 动作 | 职责 |
|---|---|---|
| `Brushblade/Assets/_Project/Core/Perk.cs` | 修改 | 加 `PerkTree.Cross`、`PerkScaling`、`PerkRequirement`;`PerkNodeDef` 加 `Prereq`/`Scaling`/`BaseValue`;抽出 `PrereqMet`;`Bonus` 加缩放;新增 `ScaleCountOf`;表加 3 个跨树节点 |
| `Brushblade/Assets/_Project/Tests/PerkTests.cs` | 修改 | 只改 `Nodes_AreFortyAcrossThreeTrees` 与 `TotalCost_MatchesTheSpecBudget` 两条(表形状与总价变了) |
| `Brushblade/Assets/_Project/Tests/CrossNodeTests.cs` | 新建 | 跨树节点的前置谓词 |
| `Brushblade/Assets/_Project/Tests/PerkScalingTests.cs` | 新建 | 保底值 + 三种缩放口径 |
| `tools/icons/build_icons.py` | 修改 | 新增 9 枚图标的 SVG 片段 |
| `tools/icons/tests/test_icons.py` | 修改 | `EXPECTED` 加 9 个 key |
| `Brushblade/Assets/_Project/Presentation/UI/Icons.cs` | 修改 | `Glyphs` 加 9 个兜底汉字 |
| `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json` | 修改 | 3 个跨树节点的名字/描述/详解 + 缩放拆分显示文案 + 画布导航文案 |
| `Brushblade/Assets/_Project/Presentation/PerkNodeIcons.cs` | 新建 | 节点 → 图标 key 的映射(单一职责,`PerkView` 与 `PerkNodeSheet` 共用) |
| `Brushblade/Assets/_Project/Presentation/PerkLayout.cs` | 新建 | 极坐标布局的纯计算(节点 → 画布坐标),不碰 GameObject —— 可被单独读懂与验算 |
| `Brushblade/Assets/_Project/Presentation/PerkView.cs` | **整份重写** | 可平移缩放的极坐标画布 + 跳转锚点 + 缩略图 |
| `Brushblade/Assets/_Project/Presentation/UI/Theme.cs` | 修改 | `PerkBranchColor` 补三个跨树枝(不补会三个同色) |
| `Brushblade/Assets/_Project/Presentation/PerkNodeSheet.cs` | 修改 | 居中弹窗 → 右侧推出;跨树双前置显示;效果分量拆分显示 |

---

## Task 1: Core —— 三个字段、前置分叉、缩放求值

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Perk.cs`
- Test: `Brushblade/Assets/_Project/Tests/PerkScalingTests.cs`(新建)

**Interfaces:**
- Consumes: 无(第一个 Task)
- Produces:
  - `enum PerkTree { Wuxing, Passive, Mechanic, Cross }`
  - `enum PerkScaling { None, PerDeepWuxingNode, PerMechanicNode, PerDeepElement }`
  - `sealed class PerkRequirement { PerkTree Tree; int MinDepth; int Count; }`,构造 `PerkRequirement(PerkTree tree, int minDepth, int count = 1)`
  - `PerkNodeDef` 新增只读属性:`IReadOnlyList<PerkRequirement> Prereq`、`PerkScaling Scaling`、`int BaseValue`
  - `PerkNodeDef` 新增构造重载(**旧构造保留且行为不变**,40 个既有节点继续用它)
  - `public static bool PerkRules.PrereqMet(MetaState meta, PerkNodeDef def)`
  - `public static int PerkRules.ScaleCountOf(MetaState meta, PerkNodeDef def)` —— 非缩放节点恒返回 1
  - `public static int PerkRules.CountOwned(MetaState meta, PerkTree tree, int minDepth)`

本 Task **不往表里加节点**(那是 Task 2)。做完这一步,43,600 的总价与 40 的节点数都还没变,既有 1680 条测试应当**一条不红**。

- [ ] **Step 1: 写失败测试**

新建 `Brushblade/Assets/_Project/Tests/PerkScalingTests.cs`:

```csharp
using System.Collections.Generic;
using Brushblade.Core;
using NUnit.Framework;

namespace Brushblade.Core.Tests
{
    /// <summary>缩放节点的求值(spec 2026-09-08 §5)。
    ///
    /// 本文件**不依赖表里真有跨树节点** —— 它自己造 PerkNodeDef 来测求值规则,
    /// 这样 Task 1 的求值逻辑与 Task 2 的入表数值可以各自变红/变绿,
    /// 出问题时一眼分得清是「公式错了」还是「数值配错了」。</summary>
    public class PerkScalingTests
    {
        private static MetaState MetaWith(params string[] perks)
        {
            var meta = new MetaState { CharacterXp = 0 };
            foreach (var p in perks) meta.UnlockedPerks.Add(p);
            return meta;
        }

        // ---- CountOwned:遍历口径 ----

        [Test]
        public void CountOwned_CountsNodesAtOrDeeperThanMinDepth()
        {
            // 金脉点满 4 层 → L3/L4 两个够深
            var meta = MetaWith("metal_1", "metal_2", "metal_3", "metal_4");
            Assert.That(PerkRules.CountOwned(meta, PerkTree.Wuxing, 3), Is.EqualTo(2),
                "PerDeepWuxingNode 数的是**节点**:一系点满 4 层算 2(L3、L4 各一个)");
            Assert.That(PerkRules.CountOwned(meta, PerkTree.Wuxing, 1), Is.EqualTo(4));
        }

        [Test]
        public void CountOwned_IsIndifferentToInsertionOrder()
        {
            var forward = MetaWith("metal_1", "metal_2", "metal_3");
            var backward = MetaWith("metal_3", "metal_2", "metal_1");
            Assert.That(PerkRules.CountOwned(backward, PerkTree.Wuxing, 3),
                Is.EqualTo(PerkRules.CountOwned(forward, PerkTree.Wuxing, 3)),
                "必须遍历固定顺序的 Nodes,不是 meta.UnlockedPerks");
        }

        [Test]
        public void CountOwned_IgnoresUnknownIds()
        {
            // 改表后的旧档里会留着已删除节点的 id;索引器会抛,整个存档读不出来
            var meta = MetaWith("metal_3", "a_node_that_no_longer_exists");
            Assert.That(PerkRules.CountOwned(meta, PerkTree.Wuxing, 3), Is.EqualTo(1));
        }

        // ---- ScaleCountOf:三种口径 ----

        private static PerkNodeDef Scaler(PerkEffect effect, PerkScaling scaling,
            int baseValue, int perCount) =>
            new PerkNodeDef("t_scaler", PerkTree.Cross, "tscale", null,
                depth: 1, unlockLevel: 1, inkCost: 0, effect: effect,
                value: perCount, baseValue: baseValue, scaling: scaling,
                prereq: new List<PerkRequirement>());

        [Test]
        public void PerDeepWuxingNode_CountsOnlyDepthThreeAndDeeper()
        {
            var meta = MetaWith("metal_1", "metal_2", "wood_1", "wood_2");
            var def = Scaler(PerkEffect.AttackPercent, PerkScaling.PerDeepWuxingNode, 8, 3);
            Assert.That(PerkRules.ScaleCountOf(meta, def), Is.EqualTo(0),
                "L1/L2 是供给不是强化,不计入");
        }

        [Test]
        public void PerDeepElement_CountsElementsNotNodes()
        {
            var meta = MetaWith("metal_1", "metal_2", "metal_3", "metal_4");
            var def = Scaler(PerkEffect.DrawRolls, PerkScaling.PerDeepElement, 1, 1);
            Assert.That(PerkRules.ScaleCountOf(meta, def), Is.EqualTo(1),
                "一系点满 4 层只算 1 个系(与 PerDeepWuxingNode 的 2 刻意不同)");
        }

        [Test]
        public void PerDeepElement_ClampsAtTwo()
        {
            var meta = MetaWith(
                "metal_1", "metal_2", "metal_3",
                "wood_1", "wood_2", "wood_3",
                "water_1", "water_2", "water_3",
                "fire_1", "fire_2", "fire_3",
                "earth_1", "earth_2", "earth_3");
            var def = Scaler(PerkEffect.DrawRolls, PerkScaling.PerDeepElement, 1, 1);
            Assert.That(PerkRules.ScaleCountOf(meta, def), Is.EqualTo(2),
                "上限 2 夹在 ScaleCount 里,不是夹在调用点");
        }

        [Test]
        public void PerMechanicNode_CountsEveryMechanicNode()
        {
            var meta = MetaWith("lore_1", "lore_2", "qi_1");
            var def = Scaler(PerkEffect.CritChance, PerkScaling.PerMechanicNode, 6, 3);
            Assert.That(PerkRules.ScaleCountOf(meta, def), Is.EqualTo(3), "机制树只有两层,全算");
        }

        [Test]
        public void ScaleCountOf_IsOneForNonScalingNodes()
        {
            var meta = MetaWith("power_1");
            Assert.That(PerkRules.ScaleCountOf(meta, PerkRules.Get("power_1")), Is.EqualTo(1),
                "非缩放节点恒 1 —— 详情面板拿它算分量时不用分支");
        }

        // ---- 恒等性硬线 ----

        [Test]
        public void ExistingNodes_AreAllUnscaledWithZeroBaseValue()
        {
            foreach (var def in PerkRules.Nodes)
            {
                if (def.Tree == PerkTree.Cross) continue;
                Assert.That(def.Scaling, Is.EqualTo(PerkScaling.None), $"{def.Id} 不该是缩放节点");
                Assert.That(def.BaseValue, Is.EqualTo(0), $"{def.Id} 的 BaseValue 必须是 0");
                Assert.That(def.Prereq, Is.Null, $"{def.Id} 必须走隐式同枝前置");
            }
        }

        [Test]
        public void UnscaledBonus_IsBitIdenticalToPlainSum()
        {
            var meta = MetaWith("power_1", "power_2", "power_3");
            Assert.That(PerkRules.Bonus(meta, PerkEffect.AttackPercent), Is.EqualTo(30),
                "5 + 10 + 15 —— Scaling == None 时走的还是 sum += def.Value");
        }
    }
}
```

⚠ 这里刻意用了**新构造重载**(带 `baseValue` / `scaling` / `prereq` 命名实参)。Step 3 要把它加出来。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /Users/eugenewu/code/game/tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 编译失败 —— `PerkTree.Cross` / `PerkScaling` / `PerkRequirement` / `PerkRules.CountOwned` / `ScaleCountOf` 都不存在。

- [ ] **Step 3: 改 `Perk.cs`**

**3a.** `PerkTree` 加第四个值:

```csharp
    /// <summary>三棵技能树(spec 2026-09-07)+ 跨树节点(spec 2026-09-08 §3.0)。
    ///
    /// ⚠ 「机制树」= 改玩法交互逻辑(字库/起手/稀有度/AP),「被动树」= 被动数值强化
    /// (生命/攻击/暴击/护甲)。用户 2026-09-07 明确对调过一次命名,勿按字面直觉互换。
    ///
    /// Cross 不是第四棵树,是**不属于任何一棵树**的三个咬合节点。它们的 Depth 恒为 1、
    /// Branch 各自独立,前置走 PerkNodeDef.Prereq 的显式谓词而不是同枝推导。</summary>
    public enum PerkTree { Wuxing, Passive, Mechanic, Cross }
```

**3b.** 新增 `PerkScaling` 与 `PerkRequirement`(放在 `PerkNodeDef` 之前):

```csharp
    /// <summary>效果值的缩放方式(spec 2026-09-08 §5)。None = 值就是 Value(全部 40 个普通节点)。
    /// 其余三种:值 = BaseValue + Value × 计数,计数由本枚举决定。
    ///
    /// ⚠ PerDeepWuxingNode 数**节点**,PerDeepElement 数**系** —— 两者都叫 "deep" 但口径不同:
    /// 一系点满 4 层,前者算 2(L3、L4 各一个),后者算 1。Node / Element 这对后缀是刻意的,
    /// 别在别处简写掉。</summary>
    public enum PerkScaling
    {
        None,
        PerDeepWuxingNode,
        PerMechanicNode,
        PerDeepElement,
    }

    /// <summary>跨树节点的一条前置谓词(spec 2026-09-08 §4.1)。
    ///
    /// 是**谓词**而不是「某个具体节点的 id」—— 跨树节点要的是「任一五行 L3」,不是「金脉 L3」。
    /// 写成 id 列表就等于把它绑死在某一枝上,那正是 §3.1 要避免的「第四棵树的固定路径」。</summary>
    public sealed class PerkRequirement
    {
        public PerkTree Tree { get; }
        /// <summary>至少点到第几层(1 起)。</summary>
        public int MinDepth { get; }
        /// <summary>至少几个。</summary>
        public int Count { get; }

        public PerkRequirement(PerkTree tree, int minDepth, int count = 1)
        {
            Tree = tree; MinDepth = minDepth; Count = count;
        }
    }
```

**3c.** `PerkNodeDef` 加三个属性与新构造。**旧构造保留**,委托到新构造并填缺省值 —— 40 个既有节点的建表代码一行不动:

```csharp
        public PerkEffect Effect { get; }
        public int Value { get; }

        /// <summary>缩放节点的保底值(spec 2026-09-08 §3.2);Scaling == None 时恒为 0。
        ///
        /// 存在的理由不是「避免 +0」(前置本就保证计数 ≥ 1),而是「纯缩放下的入门档
        /// 配不上定价」—— 相济 1,200 墨锭只换 +2% 攻击,同价的力 L3 给 +15%。</summary>
        public int BaseValue { get; }

        public PerkScaling Scaling { get; }

        /// <summary>显式前置谓词;**null = 走「同枝 Depth−1」的隐式推导**(40 个普通节点)。
        /// 空列表 ≠ null:空列表表示「显式声明了没有前置」。</summary>
        public IReadOnlyList<PerkRequirement> Prereq { get; }

        public PerkNodeDef(string id, PerkTree tree, string branch, Element? element,
            int depth, int unlockLevel, int inkCost, PerkEffect effect, int value)
            : this(id, tree, branch, element, depth, unlockLevel, inkCost, effect, value,
                   baseValue: 0, scaling: PerkScaling.None, prereq: null)
        {
        }

        public PerkNodeDef(string id, PerkTree tree, string branch, Element? element,
            int depth, int unlockLevel, int inkCost, PerkEffect effect, int value,
            int baseValue, PerkScaling scaling, IReadOnlyList<PerkRequirement> prereq)
        {
            Id = id; Tree = tree; Branch = branch; Element = element;
            Depth = depth; UnlockLevel = unlockLevel; InkCost = inkCost;
            Effect = effect; Value = value;
            BaseValue = baseValue; Scaling = scaling; Prereq = prereq;
        }
```

**3d.** 在 `PerkRules` 里,把前置判定从 `CanUnlock` **抽成 public**,再让 `CanUnlock` 调它。

⚠ 抽 public 不是为了好看:`Presentation/PerkView.StateOf` 里**另有一份**同样的
`def.Depth > 1 && !IsUnlocked($"{branch}_{depth-1}")` 判定。跨树节点 `Depth == 1`,
那份会把「前置没满足的跨树节点」显示成可解锁。Task 3 会把它改成调这里。

把原来的 `PrerequisiteOf` 换成:

```csharp
        /// <summary>同枝上一层的 id;第一层返回 null。仅用于隐式前置的节点。</summary>
        private static string PrerequisiteOf(PerkNodeDef def) =>
            def.Depth <= 1 ? null : $"{def.Branch}_{def.Depth - 1}";

        /// <summary>该节点的前置是否已满足(spec 2026-09-08 §4.1)。
        ///
        /// public 而非 private:<c>Presentation.PerkView.StateOf</c> 要用**同一份**判据把
        /// 「点不了」拆成理由显示。此前那边抄了一份同枝推导,跨树节点(Depth == 1)会被它
        /// 误判成可解锁 —— 同一份逻辑两条路径,是这一层最常见的静默 bug。</summary>
        public static bool PrereqMet(MetaState meta, PerkNodeDef def)
        {
            if (def.Prereq == null)
            {
                var prereq = PrerequisiteOf(def);
                return prereq == null || IsUnlocked(meta, prereq);
            }
            foreach (var req in def.Prereq)
                if (CountOwned(meta, req.Tree, req.MinDepth) < req.Count) return false;
            return true;
        }

        /// <summary>已点亮的、属于该树且 Depth ≥ minDepth 的节点数。
        ///
        /// ⚠ 遍历固定顺序的 <see cref="Nodes"/> 而不是 <c>meta.UnlockedPerks</c> ——
        /// 后者的顺序取决于玩家点技能的先后。这里只是计数、顺序不影响结果,但与
        /// <c>MetaRules.GuaranteedLootElements</c> 保持同一条遍历习惯,免得下一个人重新推一遍。
        /// 顺带白拿一条:未知 id(改表后的旧档)自动被忽略,不会抛。</summary>
        public static int CountOwned(MetaState meta, PerkTree tree, int minDepth)
        {
            int n = 0;
            foreach (var def in Nodes)
                if (def.Tree == tree && def.Depth >= minDepth && IsUnlocked(meta, def.Id))
                    n++;
            return n;
        }

        /// <summary>点到 L3 或更深的五行**系**数(一系点满 4 层也只算 1)。
        ///
        /// ⚠ 遍历 <see cref="Nodes"/> 去重收集元素,而**不是** <c>Enum.GetValues(typeof(Element))</c> ——
        /// 后者的顺序由枚举声明决定,和表无关;这里要的是「表里真有的五行枝」。
        /// (`MetaRules.StartingElements` 是 private,够不着;它自己也是这个固定顺序。)</summary>
        private static int CountDeepElements(MetaState meta)
        {
            var counted = new List<Element>();
            foreach (var def in Nodes)
            {
                if (def.Tree != PerkTree.Wuxing || def.Element is not { } element) continue;
                if (def.Depth < 3 || !IsUnlocked(meta, def.Id)) continue;
                if (counted.Contains(element)) continue;   // 数系不数节点
                counted.Add(element);
            }
            return counted.Count;
        }
```

⚠ 别改成 `Enum.GetValues` 或 `HashSet` —— 前者顺序与表无关,后者的遍历顺序不保证稳定。
这里虽然只是计数、顺序不影响结果,但与 `MetaRules.GuaranteedLootElements` 保持同一条
遍历习惯,能免掉下一个人重新推一遍。

**3e.** `CanUnlock` 改成调 `PrereqMet`:

```csharp
        public static bool CanUnlock(MetaState meta, string id)
        {
            var def = Get(id);
            if (IsUnlocked(meta, id)) return false;                                   // 已点
            if (!PrereqMet(meta, def)) return false;                                  // 前置未满足
            if (MetaRules.CharacterLevel(meta.CharacterXp) < def.UnlockLevel) return false; // 等级不足
            return meta.Ink >= def.InkCost;                                            // 墨锭足够
        }
```

**3f.** `Bonus` 加缩放,并新增 `ScaleCountOf`:

```csharp
        /// <summary>已点节点里该效果的值之和(不分元素)。
        ///
        /// ⚠ Scaling == None 那一支与改前**逐字相同**(sum += def.Value)—— 恒等性硬线
        /// 就靠这一行:40 个普通节点走的还是原来那条路。</summary>
        public static int Bonus(MetaState meta, PerkEffect effect)
        {
            int sum = 0;
            foreach (var id in meta.UnlockedPerks)
                if (ById.TryGetValue(id, out var def) && def.Effect == effect)
                    sum += def.Scaling == PerkScaling.None
                        ? def.Value
                        : def.BaseValue + def.Value * ScaleCountOf(meta, def);
            return sum;
        }

        /// <summary>缩放节点的计数;非缩放节点恒返回 1。
        ///
        /// public 而非 private:详情面板要把「基础 +8% / 已点亮 2 个 → +6%」拆成两行显示,
        /// 得拿到分量。**别让 Presentation 自己重算一遍计数** —— 那就是同一份逻辑两条路径。</summary>
        public static int ScaleCountOf(MetaState meta, PerkNodeDef def) => def.Scaling switch
        {
            // 已点亮的五行 L3/L4 **节点**个数(只数深层,L1/L2 是供给不是强化)
            PerkScaling.PerDeepWuxingNode => CountOwned(meta, PerkTree.Wuxing, 3),
            // 已点亮的机制树节点个数(该树只有两层,全算)
            PerkScaling.PerMechanicNode => CountOwned(meta, PerkTree.Mechanic, 1),
            // 点到 L3 或更深的五行**系**数,夹上限 2。
            // ⚠ 上限夹在这里而不是调用点:起手抽取次数有四个来源(基础 1 + 慧眼 +
            // 该系五行 L1 + 博采),夹在调用点会漏掉其中几条。
            PerkScaling.PerDeepElement => System.Math.Min(2, CountDeepElements(meta)),
            _ => 1,
        };
```

⚠ `System.Math.Min` 而不是 `Mathf.Min` —— Core 禁止引用 UnityEngine。

- [ ] **Step 4: 跑测试确认通过**

```bash
cd /Users/eugenewu/code/game/tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: PASS。**既有 1680 条一条不红**(本 Task 没动表)。新增 11 条。

- [ ] **Step 5: 跑离线编译**

```bash
cd /Users/eugenewu/code/game/tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

Expected: 0 个 `error CS`。`PerkTree` 加了枚举值,`PerkView`/`PerkNodeSheet` 里若有不带 `default` 的 `switch` 会在这里炸出来 —— 炸了就补 `default` 分支,别改判据。

- [ ] **Step 6: 提交**

```bash
cd /Users/eugenewu/code/game && git add Brushblade/Assets/_Project/Core/Perk.cs Brushblade/Assets/_Project/Tests/PerkScalingTests.cs Brushblade/Assets/_Project/Tests/PerkScalingTests.cs.meta && git commit -m "$(cat <<'EOF'
feat(core): 技能节点支持显式前置谓词与效果缩放

PerkTree 加 Cross;PerkNodeDef 加 Prereq/Scaling/BaseValue 三个字段与新构造
(旧构造保留并委托,40 个既有节点建表代码一行不动)。前置判定从 CanUnlock 抽成
public PrereqMet —— Presentation.PerkView.StateOf 另有一份同枝推导,跨树节点
Depth==1 会被它误判成可解锁,下一步要改去调这一份。

恒等性:Scaling == None 那一支与改前逐字相同,既有测试一条不红。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"```

⚠ `.meta` 由 Unity 在下次打开工程时生成。若此刻 `PerkScalingTests.cs.meta` 还不存在,
`git add` 那一项会报错 —— 去掉它只提交 `.cs`,并在提交信息里不必提及。

---

## Task 2: 三个跨树节点入表

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/Perk.cs`
- Modify: `Brushblade/Assets/_Project/Tests/PerkTests.cs`(**只改两条断言**)
- Test: `Brushblade/Assets/_Project/Tests/CrossNodeTests.cs`(新建)

**Interfaces:**
- Consumes: Task 1 的 `PerkTree.Cross` / `PerkScaling` / `PerkRequirement` / `PerkNodeDef` 新构造 / `PerkRules.PrereqMet` / `CountOwned` / `ScaleCountOf`
- Produces: 表里多出三个节点,id 为 `cross_vigor` / `cross_edge` / `cross_draw`,`Branch` 为 `"xvigor"` / `"xedge"` / `"xdraw"`,`Tree = PerkTree.Cross`,`Depth = 1`

数值(spec §3 表 + §3.2 表 + §6 表):

| id | 名字 | Branch | 效果 | BaseValue | Value | Scaling | 前置 | UnlockLevel | InkCost |
|---|---|---|---|---|---|---|---|---|---|
| `cross_vigor` | 相济 | `xvigor` | `AttackPercent` | 8 | 3 | `PerDeepWuxingNode` | 五行 ≥L3 ×1 + 被动 ≥L2 ×1 | 16 | 1200 |
| `cross_edge` | 融会 | `xedge` | `CritChance` | 6 | 3 | `PerMechanicNode` | 被动 ≥L2 ×1 + 机制 ≥L1 ×1 | 8 | 900 |
| `cross_draw` | 博采 | `xdraw` | `DrawRolls` | 1 | 1 | `PerDeepElement` | 机制 ≥L1 ×1 + 五行 ≥L2 ×1 | 10 | 900 |

**全树总价 43,600 → 46,600。**

- [ ] **Step 1: 写失败测试**

新建 `Brushblade/Assets/_Project/Tests/CrossNodeTests.cs`:

```csharp
using Brushblade.Core;
using NUnit.Framework;

namespace Brushblade.Core.Tests
{
    /// <summary>三个跨树节点(spec 2026-09-08 §3)。
    ///
    /// 与 PerkScalingTests 的分工:那边测**求值公式**(自造 def),这边测**表里配的数值**
    /// 与**前置谓词**。出问题时一眼分得清是公式错了还是数配错了。</summary>
    public class CrossNodeTests
    {
        /// <summary>等级拉满、墨锭管够 —— 本文件测的是**前置与缩放**,
        /// 等级/墨锭那两道闸由 PerkTests 守着,这里不该被它们干扰。
        /// CharacterXp = 100000 是既有测试统一的「全等级开放」写法(PerkTests 里到处是它),
        /// 照抄那一份,别自己算等级换算。</summary>
        private static MetaState MetaWith(params string[] perks)
        {
            var meta = new MetaState { CharacterXp = 100000, Ink = 100000 };
            foreach (var p in perks) meta.UnlockedPerks.Add(p);
            return meta;
        }

        // ---- 表的形状 ----

        [Test]
        public void ThreeCrossNodes_AreInTheTable()
        {
            foreach (var id in new[] { "cross_vigor", "cross_edge", "cross_draw" })
            {
                var def = PerkRules.Get(id);
                Assert.That(def.Tree, Is.EqualTo(PerkTree.Cross), $"{id} 该属于 Cross");
                Assert.That(def.Depth, Is.EqualTo(1), $"{id} 的 Depth 恒为 1");
                Assert.That(def.Element.HasValue, Is.False, $"{id} 不绑元素");
                Assert.That(def.Prereq, Is.Not.Null, $"{id} 必须带显式前置");
                Assert.That(def.Prereq.Count, Is.EqualTo(2), $"{id} 要两侧各一个前置");
                Assert.That(def.Scaling, Is.Not.EqualTo(PerkScaling.None), $"{id} 是缩放节点");
            }
        }

        [Test]
        public void CrossNodes_CarryTheSpecValues()
        {
            var vigor = PerkRules.Get("cross_vigor");
            Assert.That(vigor.Effect, Is.EqualTo(PerkEffect.AttackPercent));
            Assert.That(vigor.BaseValue, Is.EqualTo(8));
            Assert.That(vigor.Value, Is.EqualTo(3));
            Assert.That(vigor.Scaling, Is.EqualTo(PerkScaling.PerDeepWuxingNode));
            Assert.That(vigor.UnlockLevel, Is.EqualTo(16));
            Assert.That(vigor.InkCost, Is.EqualTo(1200));

            var edge = PerkRules.Get("cross_edge");
            Assert.That(edge.Effect, Is.EqualTo(PerkEffect.CritChance));
            Assert.That(edge.BaseValue, Is.EqualTo(6));
            Assert.That(edge.Value, Is.EqualTo(3));
            Assert.That(edge.Scaling, Is.EqualTo(PerkScaling.PerMechanicNode));
            Assert.That(edge.UnlockLevel, Is.EqualTo(8));
            Assert.That(edge.InkCost, Is.EqualTo(900));

            var draw = PerkRules.Get("cross_draw");
            Assert.That(draw.Effect, Is.EqualTo(PerkEffect.DrawRolls));
            Assert.That(draw.BaseValue, Is.EqualTo(1));
            Assert.That(draw.Value, Is.EqualTo(1));
            Assert.That(draw.Scaling, Is.EqualTo(PerkScaling.PerDeepElement));
            Assert.That(draw.UnlockLevel, Is.EqualTo(10));
            Assert.That(draw.InkCost, Is.EqualTo(900));
        }

        // ---- 前置:两侧都要 ----

        [Test]
        public void Xvigor_NeedsBothSides()
        {
            var def = PerkRules.Get("cross_vigor");

            var onlyWuxing = MetaWith("metal_1", "metal_2", "metal_3");
            Assert.That(PerkRules.PrereqMet(onlyWuxing, def), Is.False, "缺被动侧");

            var onlyPassive = MetaWith("power_1", "power_2");
            Assert.That(PerkRules.PrereqMet(onlyPassive, def), Is.False, "缺五行侧");

            var both = MetaWith("metal_1", "metal_2", "metal_3", "power_1", "power_2");
            Assert.That(PerkRules.PrereqMet(both, def), Is.True, "两侧齐了");
        }

        [Test]
        public void Xvigor_PrereqIsAPredicateNotASpecificBranch()
        {
            var def = PerkRules.Get("cross_vigor");
            var viaMetal = MetaWith("metal_1", "metal_2", "metal_3", "power_1", "power_2");
            var viaFire = MetaWith("fire_1", "fire_2", "fire_3", "guard_1", "guard_2");
            Assert.That(PerkRules.PrereqMet(viaMetal, def), Is.True);
            Assert.That(PerkRules.PrereqMet(viaFire, def), Is.True,
                "「五行任一 L3」不是「金脉 L3」;被动侧同理");
        }

        [Test]
        public void Xedge_NeedsPassiveDepthTwoAndAnyMechanic()
        {
            var def = PerkRules.Get("cross_edge");
            Assert.That(PerkRules.PrereqMet(MetaWith("edge_1", "lore_1"), def), Is.False,
                "被动只到 L1 不够");
            Assert.That(PerkRules.PrereqMet(MetaWith("edge_1", "edge_2"), def), Is.False,
                "缺机制侧");
            Assert.That(PerkRules.PrereqMet(MetaWith("edge_1", "edge_2", "lore_1"), def), Is.True);
        }

        [Test]
        public void Xdraw_NeedsMechanicL1AndWuxingL2()
        {
            var def = PerkRules.Get("cross_draw");
            Assert.That(PerkRules.PrereqMet(MetaWith("qi_1", "water_1"), def), Is.False,
                "五行只到 L1 不够");
            Assert.That(PerkRules.PrereqMet(MetaWith("water_1", "water_2"), def), Is.False,
                "缺机制侧");
            Assert.That(PerkRules.PrereqMet(MetaWith("qi_1", "water_1", "water_2"), def), Is.True);
        }

        [Test]
        public void PrereqMet_IsIndifferentToUnlockOrder()
        {
            var def = PerkRules.Get("cross_vigor");
            var forward = MetaWith("metal_1", "metal_2", "metal_3", "power_1", "power_2");
            var backward = MetaWith("power_2", "power_1", "metal_3", "metal_2", "metal_1");
            Assert.That(PerkRules.PrereqMet(backward, def),
                Is.EqualTo(PerkRules.PrereqMet(forward, def)));
        }

        // ---- 保底值真的在起作用 ----

        [Test]
        public void Xdraw_PaysTheBaseValueWhenNoElementIsDeep()
        {
            // 前置只要五行 L2,缩放却数 L3 —— 这是唯一真会落在基础档的节点(spec §3.4)
            var meta = MetaWith("qi_1", "water_1", "water_2", "cross_draw");
            Assert.That(PerkRules.ScaleCountOf(meta, PerkRules.Get("cross_draw")), Is.EqualTo(0));
            Assert.That(PerkRules.Bonus(meta, PerkEffect.DrawRolls), Is.EqualTo(1),
                "缩放计数为 0 时给保底 1,不是 0");
        }

        [Test]
        public void BaseValue_IsAddedOnceNotPerCount()
        {
            var meta = MetaWith(
                "metal_1", "metal_2", "metal_3", "metal_4",   // 深层 ×2
                "power_1", "power_2",
                "cross_vigor");
            Assert.That(PerkRules.ScaleCountOf(meta, PerkRules.Get("cross_vigor")), Is.EqualTo(2));
            // 力 L1+L2 = 5 + 10 = 15;相济 = 8 + 3×2 = 14
            Assert.That(PerkRules.Bonus(meta, PerkEffect.AttackPercent), Is.EqualTo(29),
                "8 + 3×2 = 14,不是 (8+3)×2 = 22");
        }

        [Test]
        public void Xedge_ScalesWithMechanicNodes()
        {
            var meta = MetaWith("edge_1", "edge_2", "lore_1", "lore_2", "qi_1", "cross_edge");
            // 锋 L1+L2 = 5 + 10 = 15;融会 = 6 + 3×3 = 15
            Assert.That(PerkRules.Bonus(meta, PerkEffect.CritChance), Is.EqualTo(30));
        }

        // ---- 与既有守卫的衔接 ----

        [Test]
        public void CrossNodes_UnlockByLevelTwentyTwo()
        {
            foreach (var id in new[] { "cross_vigor", "cross_edge", "cross_draw" })
                Assert.That(PerkRules.Get(id).UnlockLevel, Is.LessThanOrEqualTo(22),
                    $"{id} 的门槛不该超过全开等级");
        }

        /// <summary>烟雾测试:三个跨树节点在两侧前置都齐了时,真的点得亮。
        ///
        /// ⚠ 这条守不住「Prereq 被误改回 null」的回归 —— 它只断言 True 分支,而那个回归下
        /// 这里依然是 True。跨树节点 Depth 恒为 1,Prereq 一旦变 null,<c>PrerequisiteOf</c>
        /// 会因 <c>Depth &lt;= 1</c> 短路直接返回 null(根本走不到 "xvigor_0" 这种不存在的 id),
        /// <c>PrereqMet</c> 的 <c>prereq == null</c> 分支随即恒真 —— 后果是前置被整个绕过、
        /// 节点随时可点(过度放行),而不是永远点不亮。真正兜住这个回归的是断了 False 分支的
        /// <see cref="Xvigor_NeedsBothSides"/>、<see cref="Xedge_NeedsPassiveDepthTwoAndAnyMechanic"/>、
        /// <see cref="Xdraw_NeedsMechanicL1AndWuxingL2"/> 那三条 —— 改表时别把它们删了只留这条当哨兵。</summary>
        [Test]
        public void CrossNodes_AreReachableAtAll()
        {
            var meta = MetaWith(
                "metal_1", "metal_2", "metal_3",
                "power_1", "power_2",
                "lore_1",
                "water_1", "water_2");
            Assert.That(PerkRules.CanUnlock(meta, "cross_vigor"), Is.True);
            Assert.That(PerkRules.CanUnlock(meta, "cross_edge"), Is.True);
            Assert.That(PerkRules.CanUnlock(meta, "cross_draw"), Is.True);
        }
    }
}
```

⚠ `MetaRules.XpForLevel(level)` —— 若这个方法不存在或签名不同,去 `Meta.cs` 找现成的
「等级 → 经验」换算(既有测试里一定有人用过),**照抄那一份**,别自己乘系数。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /Users/eugenewu/code/game/tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: `PerkRules.Get("cross_vigor")` 抛 `KeyNotFoundException`,新测试全红。
**同时** `PerkScalingTests.ExistingNodes_AreAllUnscaledWithZeroBaseValue` 仍应绿(它跳过 Cross)。

- [ ] **Step 3: 入表**

在 `Perk.cs` 的 `BuildNodes()` 里,机制树那一段之后、`return list;` 之前加:

```csharp
            // ---- 跨树节点:3 个(spec 2026-09-08 §3)----
            // 各在两棵树的扇区交界上,要两侧各一个前置才开。效果是**缩放器**:
            // 值 = BaseValue + Value × 已投资量。它放大你已有的投资,而不要求特定组合 ——
            // 写死「金脉 L3 + 锋枝 L2 = 某个具体加成」会把它变成第四棵树的固定路径。
            //
            // BaseValue 的存在理由不是「避免 +0」(前置本就保证计数 ≥ 1),而是
            // 「纯缩放下的入门档配不上定价」:相济 1,200 墨只换 +2%,同价的力 L3 给 +15%。
            // 保底值按「下限对标同价位的被动节点」定(spec §3.2)。
            list.Add(new PerkNodeDef("cross_vigor", PerkTree.Cross, "xvigor", null,
                depth: 1, unlockLevel: 16, inkCost: 1200,
                effect: PerkEffect.AttackPercent, value: 3,
                baseValue: 8, scaling: PerkScaling.PerDeepWuxingNode,
                prereq: new[]
                {
                    new PerkRequirement(PerkTree.Wuxing, minDepth: 3),
                    new PerkRequirement(PerkTree.Passive, minDepth: 2),
                }));
            list.Add(new PerkNodeDef("cross_edge", PerkTree.Cross, "xedge", null,
                depth: 1, unlockLevel: 8, inkCost: 900,
                effect: PerkEffect.CritChance, value: 3,
                baseValue: 6, scaling: PerkScaling.PerMechanicNode,
                prereq: new[]
                {
                    new PerkRequirement(PerkTree.Passive, minDepth: 2),
                    new PerkRequirement(PerkTree.Mechanic, minDepth: 1),
                }));
            // ⚠ 前置只要五行 L2,缩放却数 L3 —— 这个错位是刻意的(spec §3.4):
            // L2 口径下 1,800 墨就能吃满 +3 起手抽取,会变成预算内的标配;
            // 而前置若也提到 L3,门槛就撞上相济的 Lv16,三个跨树节点的等级梯度塌掉。
            // 保底 1 正好兜住「刚点亮时缩放计数为 0」这一档。
            list.Add(new PerkNodeDef("cross_draw", PerkTree.Cross, "xdraw", null,
                depth: 1, unlockLevel: 10, inkCost: 900,
                effect: PerkEffect.DrawRolls, value: 1,
                baseValue: 1, scaling: PerkScaling.PerDeepElement,
                prereq: new[]
                {
                    new PerkRequirement(PerkTree.Mechanic, minDepth: 1),
                    new PerkRequirement(PerkTree.Wuxing, minDepth: 2),
                }));
```

- [ ] **Step 4: 改两条既有断言**

`Brushblade/Assets/_Project/Tests/PerkTests.cs`。**只改这两条**,别顺手动别的。

`Nodes_AreFortyAcrossThreeTrees` → 改名并改断言:

```csharp
        [Test]
        public void Nodes_AreFortyThreeAcrossFourCategories()
        {
            Assert.That(PerkRules.Nodes.Count, Is.EqualTo(43));
            int wuxing = 0, passive = 0, mechanic = 0, cross = 0;
            foreach (var def in PerkRules.Nodes)
            {
                if (def.Tree == PerkTree.Wuxing) wuxing++;
                else if (def.Tree == PerkTree.Passive) passive++;
                else if (def.Tree == PerkTree.Mechanic) mechanic++;
                else cross++;
            }
            Assert.That(wuxing, Is.EqualTo(20));
            Assert.That(passive, Is.EqualTo(12));
            Assert.That(mechanic, Is.EqualTo(8));
            Assert.That(cross, Is.EqualTo(3), "跨树节点不属于任何一棵树(spec 2026-09-08 §3.0)");
        }
```

`TotalCost_MatchesTheSpecBudget` 的期望值 `43600` → `46600`,并更新注释里的 spec 引用:

```csharp
            Assert.That(total, Is.EqualTo(46600), "全树总价(spec 2026-09-08 §6)");
```

- [ ] **Step 5: 检查其余既有断言有没有被跨树节点误伤**

跑一次全量,**逐条读红的**。已知会被波及的两类,按下面处理:

- `OnlyWuxingNodes_CarryAnElement` —— 断言 `def.Element.HasValue == (def.Tree == PerkTree.Wuxing)`。
  跨树节点 `Element` 为 null、`Tree` 为 Cross,**这条自然成立,不该红**。红了说明你给跨树节点绑了元素,改数据不改断言。
- `EveryBranch_IsAContiguousChainFromDepthOne` —— 跨树节点各自单独一枝、只有 Depth 1,
  **是合法的连续链,不该红**。红了说明三个跨树节点共用了 Branch,改成各自独立的 `xvigor`/`xedge`/`xdraw`。

若红的是别的断言,**停下来报告**,不要改断言 —— Global Constraint 7 说了只有那一条允许改。

```bash
cd /Users/eugenewu/code/game/tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿。

- [ ] **Step 6: 跑离线编译**

```bash
cd /Users/eugenewu/code/game/tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

Expected: 0 个 `error CS`。

⚠ 此刻 `PerkView` 会把三个跨树节点**漏掉不画**(它按 `Tabs` 三棵树遍历),`BranchName` 会
把 `"xvigor"` 原样返回。**这是预期的中间态**,Task 3~7 会补上;别在这一步临时糊一下。

- [ ] **Step 7: 提交**

```bash
cd /Users/eugenewu/code/game && git add Brushblade/Assets/_Project/Core/Perk.cs Brushblade/Assets/_Project/Tests/PerkTests.cs Brushblade/Assets/_Project/Tests/CrossNodeTests.cs && git commit -m "$(cat <<'EOF'
feat(core): 三个跨树节点入表(相济/融会/博采)

各在两棵树的扇区交界上,要两侧各一个前置才开;效果是缩放器,
值 = BaseValue + Value × 已投资量。全树总价 43,600 → 46,600。

改了两条既有断言:Nodes_AreFortyAcrossThreeTrees 改成
Nodes_AreFortyThreeAcrossFourCategories(表的形状真的变了),
TotalCost 期望值跟着改。除这两条外既有测试一条不红。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: `StateOf` 接上 `PrereqMet`

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/PerkView.cs`(**只改 `StateOf` 一个方法**)
- Modify: `Brushblade/Assets/_Project/Presentation/PerkNodeSheet.cs`(`BranchMaxDepth` 兜底)

**Interfaces:**
- Consumes: `PerkRules.PrereqMet(meta, def)`(Task 1)
- Produces: 无新接口

这个 Task 很小,但**单独成一个** —— 它修的是 CLAUDE.md 记的那类静默 bug(同一份逻辑两条路径),
放进 Task 6 的整份重写里会被淹掉,而它的正确性可以独立被审。

**病灶**:`PerkView.StateOf` 自己抄了一份同枝前置推导:

```csharp
if (def.Depth > 1 && !PerkRules.IsUnlocked(meta, $"{def.Branch}_{def.Depth - 1}"))
    return NodeState.GatedPrereq;
```

跨树节点 `Depth == 1`,这一行**恒不成立** → 前置一个都没满足的相济会被显示成
「可解锁」,玩家点下去 `PerkRules.TryUnlock` 拒绝,界面毫无反应。

- [ ] **Step 1: 改 `StateOf`**

```csharp
        /// <summary>判据顺序与 <see cref="PerkRules.CanUnlock"/> 内部完全一致(已点 → 前置 →
        /// 等级 → 墨锭),只是把「不能点」拆成三种理由分别显示——同源判据,不是另一套规则。
        ///
        /// ⚠ 前置判定走 <see cref="PerkRules.PrereqMet"/> 而**不是**在这里重抄一份同枝推导:
        /// 跨树节点的 Depth 恒为 1,抄来的 `def.Depth > 1 && …` 那行对它恒不成立,
        /// 会把前置一个都没满足的相济显示成「可解锁」,点下去毫无反应(2026-09-08)。</summary>
        internal static NodeState StateOf(MetaState meta, PerkNodeDef def, int charLevel)
        {
            if (PerkRules.IsUnlocked(meta, def.Id)) return NodeState.Owned;
            if (!PerkRules.PrereqMet(meta, def)) return NodeState.GatedPrereq;
            if (charLevel < def.UnlockLevel) return NodeState.GatedLevel;
            if (meta.Ink < def.InkCost) return NodeState.PoorInk;
            return NodeState.CanUnlock;
        }
```

- [ ] **Step 2: `grep` 一遍还有没有第三份同枝推导**

```bash
cd /Users/eugenewu/code/game && grep -rn '_\{def\.Depth\|Depth - 1\|Depth-1' Brushblade/Assets/_Project/Presentation/ Brushblade/Assets/_Project/Core/
```

Expected: 只剩 `Core/Perk.cs` 的 `PrerequisiteOf` 一处。若 `PerkNodeSheet.BuildChain` 里也有,
那是**画前置链**用的(不是判定),留着,但要加兜底 —— 见 Step 3。

- [ ] **Step 3: `PerkNodeSheet` 的前置链兜底**

`BuildChain` 假定「同枝从 Depth 1 排到 `BranchMaxDepth`」。跨树节点单独一枝、只有一个节点,
链会画成一个孤零零的圈。本 Task **不做**跨树节点的双前置显示(那是 Task 7),
只保证它**不崩**:在 `BuildChain` 开头加一句提前返回。

```csharp
        private static void BuildChain(Transform parent, MetaState meta, PerkNodeDef def)
        {
            // 跨树节点的前置是「任一五行 L3 + 任一被动 L2」这样的谓词,不是同枝直链 ——
            // 这里的逐层圆点画法表达不了它。完整的双前置显示见 Task 7;在那之前先不画,
            // 免得渲染成一个没有意义的孤立圆点。
            if (def.Tree == PerkTree.Cross) return;
            ...
```

- [ ] **Step 4: 跑离线编译**

```bash
cd /Users/eugenewu/code/game/tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

Expected: 0 个 `error CS`。

- [ ] **Step 5: 跑 Core 测试确认没回归**

```bash
cd /Users/eugenewu/code/game/tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿(本 Task 只动 Presentation,Core 测试不该有任何变化)。

- [ ] **Step 6: 提交**

```bash
cd /Users/eugenewu/code/game && git add Brushblade/Assets/_Project/Presentation/PerkView.cs Brushblade/Assets/_Project/Presentation/PerkNodeSheet.cs && git commit -m "$(cat <<'EOF'
fix(presentation): 节点五态的前置判定改走 PerkRules.PrereqMet

PerkView.StateOf 此前自己抄了一份「同枝 Depth−1」推导。跨树节点 Depth 恒为 1,
那一行对它恒不成立 —— 前置一个都没满足的相济会显示成「可解锁」,点下去毫无反应。
同一份逻辑两条路径,是这一层最常见的静默 bug。

PerkNodeSheet.BuildChain 对跨树节点先提前返回(它的谓词前置画不成同枝直链),
完整的双前置显示留给后续。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 9 枚新图标(三处对账)

**Files:**
- Modify: `tools/icons/build_icons.py`
- Modify: `tools/icons/tests/test_icons.py`
- Modify: `Brushblade/Assets/_Project/Presentation/UI/Icons.cs`
- Create: `Brushblade/Assets/_Project/Presentation/UI/Resources/icon_perk_*.png`(**由脚本生成,不许手写**)+ 对应 `.meta`
- Create: `tools/icons/svg/icon_perk_*.svg`(脚本同时刷新,仓库里那份也在对账)

**Interfaces:**
- Consumes: 无
- Produces: 9 个新图标 key —— `perk_draw` / `perk_loot` / `perk_amplify` / `perk_wellspring` / `perk_heft` / `perk_hp` / `perk_library` / `perk_hand` / `perk_ap`

**⚠ 前置:** 需要 `rsvg-convert`(`brew install librsvg`)。没有它 `build_icons.py` 会失败 —— 先跑一次确认可用,失败就停下来报告,别手写 PNG 绕过管线(2026-09-04 栽过一次)。

**⚠ 15 枚图标覆盖 43 个节点**,其中 6 枚是**复用既有的**(`morale` / `speed` / `burn` / `attack` / `crit` / `defense`),路径一个字节都不改。只新增 9 枚。

- [ ] **Step 1: 在 `test_icons.py` 的 `EXPECTED` 里加 9 个 key(先让测试红)**

```python
    # 技能树节点图标 9 枚(2026-09-08,spec 技能树环形星域 §7.2)。
    # 15 枚撑起 43 个节点:这 9 枚 + 复用既有的 morale/speed/burn/attack/crit/defense。
    # 五行 L1/L2/L3 三层**跨五系共用同一枚** —— 图形表达「效果类型」,颜色表达「哪一系」,
    # 正是 build_icons.ICONS 开头那条「图形一律白色,底色由 C# 上色」的约定。
    "perk_draw", "perk_loot", "perk_amplify", "perk_wellspring", "perk_heft",
    "perk_hp", "perk_library", "perk_hand", "perk_ap",
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /Users/eugenewu/code/game && python3 -m pytest tools/icons/tests/ -q
```

Expected: `test_icon_set_matches_spec` FAIL(`EXPECTED` 比 `build_icons.ICONS` 多 9 个),
`test_csharp_fallback_covers_every_icon` 也 FAIL。

- [ ] **Step 3: 在 `build_icons.py` 的 `ICONS` 末尾加 9 枚 SVG 片段**

坐标**逐字节照抄设计稿**(64×64、内容留 8px 边距、`stroke-width 6` 圆头圆角)。
稿里用 `currentColor`,这里换成 `#fff`(即用现成的 `STROKE` / `FILL` 常量)。
另需一个细描边常量 —— 若 `build_icons.py` 里还没有,在 `FILL` 之后加:

```python
# 细描边:技能树图标的构件比状态图标多(两张牌、四个格),6 的线宽在 46px 节点上糊成一坨
STROKE4 = 'fill="none" stroke="#fff" stroke-width="4.5" stroke-linecap="round" stroke-linejoin="round"'
```

`ICONS` 末尾追加:

```python
    # ---- 技能树节点 9 枚(2026-09-08)----
    # 抽取次数 +1:两张错位的签,前面那张顶端尖出来 = 抽两次取更好的
    "perk_draw": (
        f'<path {STROKE4} d="M14 22h16v34H14z"/>'
        f'<path {STROKE} d="M36 18h14v38H36z"/>'
        f'<path {FILL} d="M43 6l7 11H36z"/>'
    ),
    # 战利品保底:一排候选,中间那个实心 = 必有一个是你要的
    "perk_loot": (
        f'<circle cx="14" cy="40" r="6" {STROKE4}/>'
        f'<circle cx="32" cy="40" r="9" {FILL}/>'
        f'<circle cx="50" cy="40" r="6" {STROKE4}/>'
        f'<path {STROKE4} d="M32 10v12"/>'
    ),
    # 效果放大:实心核 + 两道外扩的弧
    "perk_amplify": (
        f'<circle cx="32" cy="32" r="9" {FILL}/>'
        f'<path {STROKE4} d="M46 18a20 20 0 0 1 0 28M18 46a20 20 0 0 1 0-28"/>'
    ),
    # 泉:承水的凹弧 + 向上涌的三点
    "perk_wellspring": (
        f'<path {STROKE} d="M12 38a20 20 0 0 0 40 0"/>'
        f'<path {STROKE} d="M32 30V10M22 24l4-8M42 24l-4-8"/>'
    ),
    # 厚:自下而上收窄的三层 = 堆叠的厚度
    "perk_heft": f'<path {STROKE} d="M10 50h44M15 36h34M21 22h22"/>',
    # 生命上限:容器 + 内部液面 + 顶上的加号
    "perk_hp": (
        f'<path {STROKE} d="M16 22v24a16 16 0 0 0 32 0V22"/>'
        f'<path {STROKE4} d="M17 38h30"/>'
        f'<path {STROKE4} d="M32 8v10M27 13h10"/>'
    ),
    # 字库容量:四格 + 一个虚位(右下角空着 = 还能再放)
    "perk_library": (
        f'<path {STROKE4} d="M12 12h18v18H12zM34 12h18v18H34zM12 34h18v18H12z"/>'
        f'<path {STROKE4} stroke-dasharray="5 5" d="M34 34h18v18H34z"/>'
    ),
    # 起手张数:扇形展开的三张牌
    "perk_hand": (
        f'<path {STROKE4} d="M26 54V20h12v34" transform="rotate(-22 32 54)"/>'
        f'<path {STROKE4} d="M26 54V20h12v34" transform="rotate(22 32 54)"/>'
        f'<path {STROKE} d="M25 52V16h14v36z"/>'
    ),
    # 行动点:三格槽,填了两格
    "perk_ap": (
        f'<path {STROKE4} d="M10 24h44v16H10z"/>'
        f'<path {FILL} d="M14 28h10v8H14zM27 28h10v8H27z"/>'
    ),
```

⚠ **跨树节点不需要第 10 枚新图标**:它们用「外圈虚线环 + 内部放它所放大的那个效果的图标」
—— 相济用 `attack`、融会用 `crit`、博采用 `perk_draw`。虚线环由 Presentation 侧画(Task 6),
不进图标管线。

- [ ] **Step 4: 在 `Icons.cs` 的 `Glyphs` 里加 9 个兜底汉字**

```csharp
            // 技能树节点 9 枚(2026-09-08)。PNG 取不到时回落成汉字徽章。
            // ⚠ 这几个字必须在字体子集里 —— subset_fonts.py 扫 .cs 的字符串字面量,
            // 写在这里就自动收进去,但**改完必须重跑一次** subset_fonts.py。
            { "perk_draw", "抽" },
            { "perk_loot", "获" },
            { "perk_amplify", "增" },
            { "perk_wellspring", "泉" },
            { "perk_heft", "厚" },
            { "perk_hp", "命" },
            { "perk_library", "库" },
            { "perk_hand", "手" },
            { "perk_ap", "点" },
```

- [ ] **Step 5: 生成 PNG**

```bash
cd /Users/eugenewu/code/game && python3 tools/icons/build_icons.py
```

Expected: 输出 9 个新 `icon_perk_*.png`,并刷新 `tools/icons/svg/` 下对应的 `.svg`。

- [ ] **Step 6: 补 `.meta`**

`.meta` **脚本不生成**。从同目录别的 `icon_*.png.meta` 复制,并把 `guid:` 换成新的
(32 位十六进制,每个文件都必须不同 —— 重复 guid 会让 Unity 静默丢掉其中一个)。

```bash
cd /Users/eugenewu/code/game/Brushblade/Assets/_Project/Presentation/UI/Resources && \
for k in perk_draw perk_loot perk_amplify perk_wellspring perk_heft perk_hp perk_library perk_hand perk_ap; do
  sed "s/^guid: .*/guid: $(python3 -c 'import uuid;print(uuid.uuid4().hex)')/" icon_attack.png.meta > "icon_$k.png.meta"
done && ls icon_perk_*.png.meta && grep -h '^guid:' icon_perk_*.png.meta | sort | uniq -d```

Expected: 列出 9 个 `.meta`,`uniq -d` **无输出**(没有重复 guid)。

- [ ] **Step 7: 跑图标测试确认通过**

```bash
cd /Users/eugenewu/code/game && python3 -m pytest tools/icons/tests/ -q
```

Expected: PASS。三处对账全绿(`ICONS` == `EXPECTED` == `Icons.Glyphs`)。

- [ ] **Step 8: 重跑字体子集**

新增了 9 个兜底汉字(抽/获/增/泉/厚/命/库/手/点),必须重跑:

```bash
cd /Users/eugenewu/code/game && python3 tools/fonts/subset_fonts.py && python3 -m pytest tools/fonts/tests/ -q
```

Expected: PASS。

⚠ **光看 diff 大小说明不了变没变,要比 cmap**:

```bash
cd /Users/eugenewu/code/game && python3 - <<'PY'
from fontTools.ttLib import TTFont
for f in ("Brushblade/Assets/_Project/Presentation/UI/Resources/NotoSansSC-Subset.ttf",):
    pass
import glob
for f in glob.glob('Brushblade/Assets/**/*Subset.ttf', recursive=True):
    cmap = set(TTFont(f, lazy=True).getBestCmap())
    miss = [c for c in '抽获增泉厚命库手点' if ord(c) not in cmap]
    print(f.split('/')[-1], 'missing:', ''.join(miss) or '(none)')
PY
```

Expected: 两个字体都是 `missing: (none)`。

- [ ] **Step 9: 跑全量 pytest**

```bash
cd /Users/eugenewu/code/game && python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ -q
```

Expected: 全绿。**三个目录都要跑** —— 漏掉 `tools/icons/` 会让「手写一张 PNG」全绿通过。

- [ ] **Step 10: 跑离线编译**

```bash
cd /Users/eugenewu/code/game/tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

Expected: 0 个 `error CS`。

- [ ] **Step 11: 提交**

```bash
cd /Users/eugenewu/code/game && git add tools/icons/build_icons.py tools/icons/tests/test_icons.py tools/icons/svg Brushblade/Assets/_Project/Presentation/UI/Icons.cs Brushblade/Assets/_Project/Presentation/UI/Resources && git commit -m "$(cat <<'EOF'
feat(icons,fonts): 技能树节点图标 9 枚

三处对账同改:build_icons.ICONS / test_icons.EXPECTED / Icons.cs 的 Glyphs。
15 枚撑起 43 个节点 —— 这 9 枚 + 复用既有的 morale/speed/burn/attack/crit/defense;
五行 L1/L2/L3 三层跨五系共用同一枚,图形表达效果类型、颜色表达哪一系。

跨树节点不占新图标:外圈虚线环 + 内部放它所放大的那个效果的图标。

兜底汉字新增 9 个,已重跑字体子集。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 字符串表

**Files:**
- Modify: `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`

**Interfaces:**
- Consumes: 无
- Produces: 下列 key,供 Task 6/7 用

**⚠ 「济」「融」两字不在当前字体子集里。** 本 Task 末尾必须重跑 `subset_fonts.py`。

**⚠ 「融会」不叫「贯通」**:既有节点 `qi_2`(一气 L2)已经叫「贯通」了。别改 `qi_2`。

- [ ] **Step 1: 加 key**

在 `strings.zh-CN.json` 里加(位置跟着既有 `perk.*` 那一段,保持分组):

```json
  "perk.tree.cross": "跨树",

  "perk.branch.xvigor": "相济",
  "perk.branch.xedge": "融会",
  "perk.branch.xdraw": "博采",

  "perk.node.cross_vigor.name": "相济",
  "perk.node.cross_edge.name": "融会",
  "perk.node.cross_draw.name": "博采",

  "perk.node.cross_vigor.desc": "攻击 +{base}%,每点亮一个五行第三/四层节点再 +{value}%",
  "perk.node.cross_edge.desc": "暴击率 +{base},每点亮一个机制树节点再 +{value}",
  "perk.node.cross_draw.desc": "起手抽取次数 +{base},每有一系点到第三层再 +{value}(最多 +2)",

  "perk.detail.cross_vigor": "五行与被动的咬合点。它不给固定加成,而是放大你已经投下去的五行深度——每点亮一个第三层或第四层的五行节点,攻击再涨一档。专精两系走到底拿得最多,广种薄收拿不到。",
  "perk.detail.cross_edge": "被动与机制的咬合点。机制树每一个已点亮的节点都让暴击率再涨一档——它奖励的是「你改了多少条玩法规则」,而不是某一条具体规则。",
  "perk.detail.cross_draw": "机制与五行的咬合点。每有一系点到第三层,起手每一格就多抽一次、取档位最高的那张,最多多抽两次。只要一系走得够深就有,不挑是哪一系。",

  "perk.detail.section.scaling": "数值构成",
  "perk.detail.scaling.base": "基础 +{value}",
  "perk.detail.scaling.count.deep_wuxing": "已点亮 {count} 个五行第三/四层 → +{value}",
  "perk.detail.scaling.count.mechanic": "已点亮 {count} 个机制节点 → +{value}",
  "perk.detail.scaling.count.deep_element": "已有 {count} 系点到第三层 → +{value}",
  "perk.detail.scaling.hint.deep_wuxing": "还没有五行节点点到第三层。把某一系走深,这一条就会跟着涨。",
  "perk.detail.scaling.hint.mechanic": "还没有点亮机制树节点。",
  "perk.detail.scaling.hint.deep_element": "还没有哪一系点到第三层。再深一层就 +1。",
  "perk.detail.scaling.total": "合计 +{value}",

  "perk.detail.section.cross_prereq": "跨树前置",
  "perk.detail.cross_req.wuxing": "五行任一枝点到第{depth}层",
  "perk.detail.cross_req.passive": "被动任一枝点到第{depth}层",
  "perk.detail.cross_req.mechanic": "机制任一枝点到第{depth}层",
  "perk.detail.cross_req.met": "已满足({count}/{need})",
  "perk.detail.cross_req.gap": "还差 {gap} 个({count}/{need})",

  "perk.view.jump.wuxing": "五行",
  "perk.view.jump.passive": "被动",
  "perk.view.jump.mechanic": "机制",
  "perk.view.jump.cross": "跨树",
  "perk.view.root.wuxing": "五行",
  "perk.view.root.passive": "被动",
  "perk.view.root.mechanic": "机制",
  "perk.view.hub": "斗",
  "perk.view.zoom_hint": "双指缩放 · 拖动平移"
```

⚠ **`perk.detail.crit_chance` 那条既有文案说「合计封顶 100%」** —— 融会给的是百分点,
和锋枝一起结算,这句话仍然成立,**不用改**。

⚠ **别改 `perk.node.qi_2.name`**(「贯通」),那是既有节点,与跨树的「融会」是两回事。

- [ ] **Step 2: 确认 JSON 仍可解析**

```bash
cd /Users/eugenewu/code/game && python3 -c "
import json,io
d=json.load(io.open('Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json',encoding='utf-8'))
need=['perk.node.cross_vigor.name','perk.detail.scaling.base','perk.detail.cross_req.wuxing','perk.view.jump.cross']
for k in need: assert k in d, k
print('ok', len([k for k in d if k.startswith('perk.')]), 'perk keys')
"
```

Expected: `ok 16x perk keys`(比改前多 30 余个)。

- [ ] **Step 3: 重跑字体子集**

```bash
cd /Users/eugenewu/code/game && python3 tools/fonts/subset_fonts.py && python3 -m pytest tools/fonts/tests/ -q
```

- [ ] **Step 4: 比 cmap 确认「济」「融」进去了**

```bash
cd /Users/eugenewu/code/game && python3 - <<'PY'
from fontTools.ttLib import TTFont
import glob
for f in glob.glob('Brushblade/Assets/**/*Subset.ttf', recursive=True):
    cmap = set(TTFont(f, lazy=True).getBestCmap())
    miss = [c for c in '济融相会博采咬合档涨挑' if ord(c) not in cmap]
    print(f.split('/')[-1], 'missing:', ''.join(miss) or '(none)')
PY
```

Expected: 两个字体都是 `missing: (none)`。**有输出缺字就是没跑成**,回 Step 3。

⚠ `subset_fonts.py` 扫的是**字符串表 JSON 与 .cs 字面量**。上面那串检查字只覆盖了少数
新词;真正的守卫是 `pytest tools/fonts/tests/`,它对着完整来源清单核。**两个都要过**。

- [ ] **Step 5: 提交**

```bash
cd /Users/eugenewu/code/game && git add Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json Brushblade/Assets/_Project/Presentation/UI/Resources && git commit -m "$(cat <<'EOF'
feat(strings): 跨树节点与缩放数值构成的文案

三个跨树节点的名字/描述/详解,数值构成的拆分显示(基础 / 计数 / 合计 / 计数为 0 的
提示),跨树双前置的满足度,以及画布导航的跳转锚点文案。

「融会」不叫「贯通」—— 既有节点 qi_2(一气 L2)已经叫贯通了。
「济」「融」两字原不在子集里,已重跑 subset_fonts.py。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: `PerkView` 重写 —— 极坐标画布

**风险最高的一批。** 整份重写、无自动化测试,只有离线编译 + 人眼。

**Files:**
- Create: `Brushblade/Assets/_Project/Presentation/PerkLayout.cs`
- Create: `Brushblade/Assets/_Project/Presentation/PerkNodeIcons.cs`
- Modify: `Brushblade/Assets/_Project/Presentation/PerkView.cs`(整份重写 Build 部分,保留 `NodeState` / `StateOf` / `BranchColor` / `BranchSoft` / `BranchName` / `TreeName` 这几个 internal 成员的**签名不变** —— `PerkNodeSheet` 依赖它们)

**Interfaces:**
- Consumes: `PerkRules.Nodes` / `StateOf`(Task 3)/ `Icons.Get`(Task 4)/ Task 5 的字符串 key
- Produces:
  - `PerkLayout.Place(PerkNodeDef def) → (float x, float y)` —— 画布坐标,原点左上,画布 1120×1120
  - `PerkLayout.CanvasSize`(1120)、`PerkLayout.Center`(560)
  - `PerkLayout.RootPlace(PerkTree tree) → (float x, float y)` —— 三个树根标记
  - `PerkNodeIcons.KeyFor(PerkNodeDef def) → string`
  - `PerkView` 的 internal 成员签名**一个不变**

### 布局规格(spec §2.1 / §2.2 / §2.3)

角度是**屏幕角**,y 向下,0° = 正右,顺时针为正;270° = 正上。

| 树 | 角度区间 | 枝数 × 层数 |
|---|---|---|
| 五行 | 180° ~ 360° | 5 × 4 |
| 被动 | 0° ~ 108° | 4 × 3 |
| 机制 | 108° ~ 180° | 4 × 2 |

半径:中心「斗」44,三个树根 100,五行第一圈 190、被动 200、机制 280,各树最外层 **475**(共用),跨树节点 **530**。

跨树节点角度:相济 0°、融会 108°、博采 180°。

- [ ] **Step 1: 写 `PerkLayout.cs`**

```csharp
using System;
using System.Collections.Generic;
using Brushblade.Core;
using UnityEngine;

namespace Brushblade.Presentation
{
    /// <summary>技能树环形星域的**纯布局计算**(spec 2026-09-08 §2)。不碰 GameObject ——
    /// 单独一个文件是为了这份坐标推导能被独立读懂和验算,不用在 500 行的建 UI 代码里找。
    ///
    /// 坐标系:画布左上为原点,y 向下(与 uGUI 的 anchoredPosition 相反,取用处要翻 y)。
    /// 角度:屏幕角,0° = 正右,顺时针为正,270° = 正上。
    ///
    /// ⚠ **三棵树的第一圈不共用一个半径**(层数少的树起点更远),最外层才共用 475。
    /// 这条是从一次失败里得来的:初稿让所有枝的 L1 共用半径 52,那一圈要挤 13 个节点,
    /// 枝间距只剩 32px(节点直径 20),机制树压到 16px —— 直接重叠。</summary>
    public static class PerkLayout
    {
        public const float CanvasSize = 1120f;
        public const float Center = CanvasSize / 2f;

        public const float HubRadius = 44f;
        public const float RootRadius = 100f;
        public const float OuterRadius = 475f;
        public const float CrossRadius = 530f;

        /// <summary>节点直径。spec §2.3:最紧处枝距 88px,直径 52 留 36px 余量,
        /// 节点里放得下两个字的名字。</summary>
        public const float NodeDiameter = 52f;

        private readonly struct Sector
        {
            public readonly float FromDeg, ToDeg, FirstRadius;
            public Sector(float from, float to, float firstRadius)
            {
                FromDeg = from; ToDeg = to; FirstRadius = firstRadius;
            }
            public float MidDeg => (FromDeg + ToDeg) / 2f;
        }

        private static Sector SectorOf(PerkTree tree) => tree switch
        {
            PerkTree.Wuxing => new Sector(180f, 360f, 190f),
            PerkTree.Passive => new Sector(0f, 108f, 200f),
            PerkTree.Mechanic => new Sector(108f, 180f, 280f),
            _ => new Sector(0f, 360f, CrossRadius),   // Cross 不走扇区,见 Place
        };

        /// <summary>某树的枝列表,顺序取自 <see cref="PerkRules.Nodes"/> 里各枝首次出现的顺序 ——
        /// 不在这里另写一份枝名列表,表变了(加枝/调顺序)这里自动跟着变。</summary>
        private static List<string> BranchesOf(PerkTree tree)
        {
            var list = new List<string>();
            foreach (var def in PerkRules.Nodes)
                if (def.Tree == tree && !list.Contains(def.Branch))
                    list.Add(def.Branch);
            return list;
        }

        private static int MaxDepthOf(PerkTree tree)
        {
            int max = 0;
            foreach (var def in PerkRules.Nodes)
                if (def.Tree == tree) max = Math.Max(max, def.Depth);
            return max;
        }

        /// <summary>跨树节点的角度:各在两棵树的扇区交界上。</summary>
        private static float CrossAngle(string branch) => branch switch
        {
            "xvigor" => 0f,     // 五行 ↔ 被动
            "xedge" => 108f,    // 被动 ↔ 机制
            "xdraw" => 180f,    // 机制 ↔ 五行
            _ => 0f,
        };

        public static Vector2 Place(PerkNodeDef def)
        {
            if (def.Tree == PerkTree.Cross)
                return Polar(CrossAngle(def.Branch), CrossRadius);

            var sector = SectorOf(def.Tree);
            var branches = BranchesOf(def.Tree);
            int i = branches.IndexOf(def.Branch);
            int n = branches.Count;

            // 枝在扇区内**均分**:第 i 枝落在 (i + 0.5)/n 处,两端各留半格 ——
            // 直接按 i/(n-1) 铺满会让首尾两枝贴到相邻扇区的边界上。
            float angle = sector.FromDeg + (sector.ToDeg - sector.FromDeg) * (i + 0.5f) / n;

            int maxDepth = MaxDepthOf(def.Tree);
            float radius = maxDepth <= 1
                ? sector.FirstRadius
                : sector.FirstRadius
                  + (OuterRadius - sector.FirstRadius) * (def.Depth - 1) / (maxDepth - 1);

            return Polar(angle, radius);
        }

        /// <summary>三个树根标记(纯分区标记,不是可解锁节点 —— spec §2.4)。</summary>
        public static Vector2 RootPlace(PerkTree tree) =>
            Polar(SectorOf(tree).MidDeg, RootRadius);

        public static Vector2 HubPlace() => new Vector2(Center, Center);

        /// <summary>某树扇区的中心方向 + 最外层半径,给跳转锚点用(把视口平移到那儿)。</summary>
        public static Vector2 JumpTarget(PerkTree tree) => tree == PerkTree.Cross
            ? new Vector2(Center, Center)   // 三个跨树节点散在三边,跳到中心才看得全
            : Polar(SectorOf(tree).MidDeg, (SectorOf(tree).FirstRadius + OuterRadius) / 2f);

        private static Vector2 Polar(float degrees, float radius)
        {
            float rad = degrees * Mathf.Deg2Rad;
            return new Vector2(Center + radius * Mathf.Cos(rad),
                               Center + radius * Mathf.Sin(rad));
        }
    }
}
```

- [ ] **Step 2: 写 `PerkNodeIcons.cs`**

```csharp
using Brushblade.Core;

namespace Brushblade.Presentation
{
    /// <summary>节点 → 图标 key(spec 2026-09-08 §7)。15 枚撑起 43 个节点。
    ///
    /// **五行 L1/L2/L3 三层跨五系共用同一枚** —— 图形表达「效果类型」,颜色表达「哪一系」。
    /// 这正是 build_icons.py 开头写的约定:「图形一律白色,底色由 C# 侧上色 —— 一张图跨底色复用」。
    /// 五个系的 L1 长得一样但五种颜色,玩家一眼看出「这是五个系各自的供给节点」。
    ///
    /// 跨树节点用它**所放大的那个效果**的图标(外圈虚线环由 PerkView 画,不进图标管线)——
    /// 图标本身就说明了它放大什么。
    ///
    /// 单独一个文件:PerkView 与 PerkNodeSheet 都要用同一份映射,放哪一边都会变成
    /// 「另一边去 reach 进来」。</summary>
    public static class PerkNodeIcons
    {
        public static string KeyFor(PerkNodeDef def)
        {
            if (def.Tree == PerkTree.Cross)
                return def.Branch switch
                {
                    "xvigor" => "attack",
                    "xedge" => "crit",
                    "xdraw" => "perk_draw",
                    _ => "perk_amplify",
                };

            if (def.Tree == PerkTree.Wuxing)
                return def.Depth switch
                {
                    1 => "perk_draw",       // 该系起手格抽取次数 +1
                    2 => "perk_loot",       // 战利品保底 1 张该系
                    3 => "perk_amplify",    // 该系字效果值 +15%
                    _ => def.Branch switch  // L4:各系专属天花板,各不相同
                    {
                        "metal" => "morale",
                        "wood" => "speed",
                        "water" => "perk_wellspring",
                        "fire" => "burn",
                        "earth" => "perk_heft",
                        _ => "perk_amplify",
                    },
                };

            // 被动树与机制树:一枝一枚,层间共用
            return def.Branch switch
            {
                "vigor" => "perk_hp",
                "power" => "attack",
                "edge" => "crit",
                "guard" => "defense",
                "lore" => "perk_library",
                "wide" => "perk_hand",
                "insight" => "perk_draw",
                "qi" => "perk_ap",
                _ => "perk_amplify",
            };
        }
    }
}
```

- [ ] **Step 3: 重写 `PerkView` 的建 UI 部分**

保留不动:`NodeState` 枚举、`StateOf`、`BranchColor`、`BranchSoft`、`BranchName`、`TreeName` ——
**签名一个不变**,`PerkNodeSheet` 依赖它们。`BranchName` 要补三个跨树枝:

```csharp
            "xvigor" => Strings.T("perk.branch.xvigor"),
            "xedge" => Strings.T("perk.branch.xedge"),
            "xdraw" => Strings.T("perk.branch.xdraw"),
```

`TreeName` 补一个分支:

```csharp
            PerkTree.Cross => Strings.T("perk.tree.cross"),
```

`Theme.PerkBranchColor` 的 `switch` 对未知枝兜底成 `TextMain` —— 三个跨树节点会**全是同一个墨色**。
在 `Brushblade/Assets/_Project/Presentation/UI/Theme.cs:251` 的 `PerkBranchColor` 里补三条,
各取它所放大的那棵树的代表色(相济放大五行 → 取五行侧的暖色,融会放大机制 → 取机制侧,
博采跨机制与五行 → 取一个与两侧都不撞的色):

```csharp
            // 跨树节点(2026-09-08)。不补这三条会全部兜底成 TextMain,三个节点同色 ——
            // 不是编译错,是上线看不出区别。
            "xvigor" => Cinnabar,
            "xedge" => Gold,
            "xdraw" => Jade,
```

⚠ 上面三个是**占位取色**,取自 `Theme` 已有的常量。若与相邻扇区的枝色撞了(相济挨着
被动「元」枝的 `Cinnabar`),在人眼验收时换一个 —— 但**必须是 `Theme` 里已有的常量**,
别在这个 Task 里新造颜色。

删掉:`Tabs` 数组、`BuildTabs`、`BuildGateRail`、`BuildGrid`、`BuildLegend`、`DashedPanel` 的
**网格用法**(虚线画法本身留着,跨树节点的外环要用)、`BranchesOf` / `MaxDepthOf` / `GateLevel`
(搬进了 `PerkLayout`)。

新的骨架:

```csharp
        // 视口内的画布缩放范围。捏合到底(MinZoom)时 43 个节点一屏看完,
        // 此时**不画图标与名字**(spec §8.2)—— 52px 的节点缩到 20px,画什么都是糊的。
        private const float MinZoom = 0.36f;
        private const float MaxZoom = 1.15f;
        private const float DetailZoom = 0.55f;   // 低于此值只画色块

        private RectTransform _viewport;   // 裁剪框
        private RectTransform _canvas;     // 1120×1120 的内容层,平移缩放作用在它上面
        private float _zoom = 0.7f;

        private void Build()
        {
            Ui.Clear(transform);
            Ui.Stretch((RectTransform)transform);

            var card = Ui.CardPanel(transform, "Panel");
            Ui.Anchor((RectTransform)card.transform,
                new Vector2(0.06f, 0.05f), new Vector2(0.94f, 0.95f), Vector2.zero, Vector2.zero);

            BuildCanvasViewport(card.transform);   // 先铺画布,顶栏浮在它之上
            BuildTopBar(card.transform);
            BuildJumpAnchors(card.transform);
            BuildMinimap(card.transform);
        }
```

⚠ **顶栏浮在画布之上,不是并排** —— 底下垫一道宣纸色渐隐(`linear-gradient(#F6F1E7 32%, transparent)`,
高 76),免得节点滑到标题后面糊成一团(spec §8.4)。uGUI 没有渐变,用一张
`Theme` 里现成的渐隐 sprite;没有就用两层半透明 `Image` 叠(0.9 / 0.45 两档),
**别去 `Theme` 里新造资源** —— 那是另一个 Task 的范围。

画布与手势:

```csharp
        private void BuildCanvasViewport(Transform parent)
        {
            var viewport = Ui.Panel(parent, "Viewport");
            Ui.Stretch((RectTransform)viewport.transform);
            viewport.AddComponent<RectMask2D>();
            _viewport = (RectTransform)viewport.transform;

            var canvas = Ui.Panel(viewport.transform, "Canvas");
            _canvas = (RectTransform)canvas.transform;
            _canvas.anchorMin = _canvas.anchorMax = new Vector2(0.5f, 0.5f);
            _canvas.pivot = new Vector2(0.5f, 0.5f);
            _canvas.sizeDelta = new Vector2(PerkLayout.CanvasSize, PerkLayout.CanvasSize);
            _canvas.localScale = Vector3.one * _zoom;

            // 拖拽平移 + 双指缩放。挂在 viewport 上而不是 canvas 上 ——
            // 挂在 canvas 上时,拖到边缘会因为 canvas 本身移出视口而丢失后续事件。
            var pan = viewport.AddComponent<PerkCanvasPan>();
            pan.Init(_viewport, _canvas, MinZoom, MaxZoom, OnZoomChanged);

            BuildConnectors(canvas.transform);   // 连线在下
            BuildHub(canvas.transform);
            BuildRoots(canvas.transform);
            int charLevel = MetaRules.CharacterLevel(_meta.CharacterXp);
            foreach (var def in PerkRules.Nodes)
                BuildNode(canvas.transform, def, charLevel);   // 节点在上
        }
```

`PerkCanvasPan` 是一个新的小 `MonoBehaviour`,实现 `IDragHandler` / `IBeginDragHandler`,
写在 `PerkView.cs` 同文件里(它只服务这一个视图,单独开文件是过度拆分)。

⚠ **拖拽与滚动共用一根手指**:此处画布是唯一可拖的东西,没有嵌套 ScrollRect,
不需要转发手势 —— 但**别顺手把画布塞进一个 ScrollRect**,那会立刻撞上这个老问题。

⚠ **拖拽期间不许重绘**:`OnDrag` 只改 `_canvas.anchoredPosition`,不重建任何 GameObject。
缩放跨过 `DetailZoom` 阈值时才切一次「画/不画图标名字」,并且**只切一次**(用一个
`_detailShown` 布尔挡住重复调用),不是每帧判断。

坐标换算(画布 y 向下 → uGUI y 向上):

```csharp
        /// <summary>PerkLayout 的画布坐标(左上原点,y 向下)→ canvas 的 anchoredPosition
        /// (中心原点,y 向上)。两处翻 y —— 漏翻的症状是整棵树上下颠倒,一眼能看出来。</summary>
        private static Vector2 ToAnchored(Vector2 canvasPoint) => new Vector2(
            canvasPoint.x - PerkLayout.Center,
            PerkLayout.Center - canvasPoint.y);
```

节点绘制沿用既有的五态画法(`Owned` / `CanUnlock` / `PoorInk` / `GatedPrereq` / `GatedLevel`),
把原来的「列内 LayoutElement」换成绝对定位:

```csharp
        private void BuildNode(Transform parent, PerkNodeDef def, int charLevel)
        {
            var state = StateOf(_meta, def, charLevel);
            // …… 五态画法照搬既有 switch,得到 cell / clickTarget / face ……

            var rect = (RectTransform)cell.transform;
            rect.anchorMin = rect.anchorMax = new Vector2(0.5f, 0.5f);
            rect.pivot = new Vector2(0.5f, 0.5f);
            rect.sizeDelta = new Vector2(PerkLayout.NodeDiameter, PerkLayout.NodeDiameter);
            rect.anchoredPosition = ToAnchored(PerkLayout.Place(def));

            // 跨树节点:外圈虚线环(表示跨)+ 内部放它所放大的那个效果的图标
            if (def.Tree == PerkTree.Cross) AddCrossRing(cell.transform, BranchColor(def));

            AddIcon(face.transform, PerkNodeIcons.KeyFor(def), state);
            AddName(face.transform, Strings.T($"perk.node.{def.Id}.name"), state);

            // 点任意状态的节点一律先开详情(解锁动作在弹窗里)——
            // 43 个节点、单次不可撤销地花几百到几千墨锭,卡面误触代价太高
            clickTarget.AddComponent<Button>().onClick.AddListener(
                () => PerkNodeSheet.Show(transform.root, _meta, def, OnNodeChanged));
        }
```

连线:每个节点连到它的前置。**普通节点连同枝上一层**(Depth 1 连到本树根),
**跨树节点连到两棵树的树根**(不是连到某个具体节点 —— 它的前置是谓词,连到具体节点是撒谎)。

```csharp
        private void BuildConnectors(Transform parent)
        {
            foreach (var def in PerkRules.Nodes)
            {
                var to = PerkLayout.Place(def);
                if (def.Tree == PerkTree.Cross)
                {
                    // 前置是谓词(「任一五行 L3」),连到两棵树的**树根**而不是某个具体节点
                    foreach (var req in def.Prereq)
                        DrawLine(parent, PerkLayout.RootPlace(req.Tree), to, dashed: true);
                    continue;
                }
                var from = def.Depth <= 1
                    ? PerkLayout.RootPlace(def.Tree)
                    : PerkLayout.Place(PerkRules.Get($"{def.Branch}_{def.Depth - 1}"));
                DrawLine(parent, from, to, dashed: false);
            }
        }
```

跳转锚点(左下四个胶囊)——**不是页签**,画布始终是同一张:

```csharp
        /// <summary>左下四个胶囊 = 跳转锚点,点一下把视口平移过去。
        ///
        /// ⚠ **不是页签**。跨树节点本来就跨在两棵树的边界上,切页会把它切碎 ——
        /// 那正是这次重做要消灭的东西。</summary>
        private void BuildJumpAnchors(Transform parent)
        {
            var row = Ui.Row(parent, "Jump", 8);
            Ui.Anchor((RectTransform)row.transform, new Vector2(0f, 0f), new Vector2(0f, 0f),
                new Vector2(20f, 20f), new Vector2(20f, 20f));
            foreach (var tree in new[] { PerkTree.Wuxing, PerkTree.Passive,
                                         PerkTree.Mechanic, PerkTree.Cross })
            {
                var t = tree;
                Ui.PillButton(row.transform, Strings.T($"perk.view.jump.{JumpKey(t)}"),
                    () => JumpTo(t), Theme.PanelPaper, Theme.TextMain, 16, new Vector2(72, 38));
            }
        }

        private void JumpTo(PerkTree tree)
        {
            // 视口居中到目标点:canvas 反向平移 目标点×zoom
            var target = ToAnchored(PerkLayout.JumpTarget(tree));
            _canvas.anchoredPosition = -target * _zoom;
        }
```

缩略图(右下圆形,红框标当前视口):

```csharp
        /// <summary>右下圆形缩略图 = 全局位置。只画 43 个圆点 + 一个视口框,不画图标名字 ——
        /// 它回答的是「我在哪」,不是「这里有什么」。</summary>
        private void BuildMinimap(Transform parent) { /* …… */ }
```

- [ ] **Step 4: 跑离线编译**

```bash
cd /Users/eugenewu/code/game/tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

Expected: 0 个 `error CS`。

- [ ] **Step 5: `grep` 一遍共享状态的所有读取点**

CLAUDE.md 记的第二类静默缺陷:「给共享的交互状态赋予新含义时,要回头查它所有的读取点」。
本 Task 引入的共享状态是 `_zoom` 与 `_canvas.anchoredPosition`,读取点有三处
(手势、跳转锚点、缩略图视口框)。

```bash
cd /Users/eugenewu/code/game && grep -n '_zoom\|_canvas\.anchoredPosition\|_detailShown' Brushblade/Assets/_Project/Presentation/PerkView.cs
```

逐处确认:跳转改了 `anchoredPosition` 后,**缩略图的视口框必须跟着动**;
缩放改了 `_zoom` 后,**`JumpTo` 用的还是新 `_zoom`**。这两条对不上的症状是
「点跳转后缩略图红框还停在原处」和「缩放后再跳转跳偏」,离线编译一个都抓不到。

- [ ] **Step 6: 跑 Core 测试确认没回归**

```bash
cd /Users/eugenewu/code/game/tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿。

- [ ] **Step 7: 提交**

```bash
cd /Users/eugenewu/code/game && git add Brushblade/Assets/_Project/Presentation/PerkView.cs Brushblade/Assets/_Project/Presentation/PerkLayout.cs Brushblade/Assets/_Project/Presentation/PerkNodeIcons.cs Brushblade/Assets/_Project/Presentation/UI/Theme.cs && git commit -m "$(cat <<'EOF'
feat(presentation): 技能树改成可平移缩放的环形星域画布

三页签规则网格 → 一张 1120×1120 的极坐标画布。屏幕只是视口,这个前提是
拥挤问题的真正解药 —— 在它之前所有布局努力都在和 350px 的垂直空间搏斗。

左下四个胶囊是**跳转锚点不是页签**:跨树节点跨在两棵树的边界上,切页会把它切碎。
连线上,跨树节点连到两棵树的**树根**而不是某个具体节点 —— 它的前置是谓词,
连到具体节点是撒谎。

布局计算抽成 PerkLayout(纯函数,不碰 GameObject),节点图标映射抽成 PerkNodeIcons
(PerkView 与 PerkNodeSheet 共用)。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: `PerkNodeSheet` 改推出式 + 跨树双前置 + 数值构成

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/PerkNodeSheet.cs`

**Interfaces:**
- Consumes: `PerkRules.ScaleCountOf` / `CountOwned`(Task 1)、`PerkNodeIcons.KeyFor`(Task 6)、Task 5 的字符串 key
- Produces: 无新接口(`Show` 签名不变)

- [ ] **Step 1: 面板从居中弹窗改成右侧推出**

宽 396(spec §8.3),盖在画布上,高度铺满。**画布能滑之后,常驻面板就是白占视野** ——
所以是推出不是常驻。

把 `Ui.Sheet(root, name, SheetW, SheetH, …)` 换成右侧贴边的锚定:

```csharp
        private const float SheetW = 396f;   // spec §8.3(原 620 的居中弹窗)

        // 右侧贴边、上下铺满:anchorMin/Max 都取右边缘
        Ui.Anchor((RectTransform)sheet.transform,
            new Vector2(1f, 0f), new Vector2(1f, 1f),
            new Vector2(-SheetW, 0f), new Vector2(0f, 0f));
```

⚠ 遮罩层(点空白关闭)保留 —— 它现在只盖住左侧画布区,不盖面板本身。

- [ ] **Step 2: 数值构成的拆分显示(spec §5.1)**

新增一个 section,**只对缩放节点显示**:

```csharp
        /// <summary>数值构成:把「基础 + 计数 × 增量」拆成三行(spec 2026-09-08 §5.1)。
        ///
        /// 不能只印合计值 —— 玩家看到「攻击 +14%」无从判断该不该再投。
        ///
        /// ⚠ 计数走 <see cref="PerkRules.ScaleCountOf"/>,**别在这一层重算一遍** ——
        /// 重算就是同一份逻辑两条路径,是这一层最常见的静默 bug。</summary>
        private static void BuildScalingBreakdown(Transform parent, MetaState meta, PerkNodeDef def)
        {
            if (def.Scaling == PerkScaling.None) return;

            SectionLabel(parent, Strings.T("perk.detail.section.scaling"));

            int count = PerkRules.ScaleCountOf(meta, def);
            int scaled = def.Value * count;

            BuildParagraph(parent, Strings.T("perk.detail.scaling.base", ("value", def.BaseValue)),
                16, Theme.TextMain);

            string suffix = ScalingKeySuffix(def.Scaling);
            if (count > 0)
                BuildParagraph(parent,
                    Strings.T($"perk.detail.scaling.count.{suffix}",
                        ("count", count), ("value", scaled)),
                    16, Theme.TextMain);
            else
                // 博采是唯一真会落在这一档的节点:前置只要五行 L2,缩放却数 L3(spec §3.4)。
                // 印「+0」会被当成 bug,要说清「再深一层就涨」。
                BuildParagraph(parent, Strings.T($"perk.detail.scaling.hint.{suffix}"),
                    15, Theme.TextDim);

            BuildParagraph(parent,
                Strings.T("perk.detail.scaling.total", ("value", def.BaseValue + scaled)),
                17, Theme.TextMain);
        }

        private static string ScalingKeySuffix(PerkScaling scaling) => scaling switch
        {
            PerkScaling.PerDeepWuxingNode => "deep_wuxing",
            PerkScaling.PerMechanicNode => "mechanic",
            PerkScaling.PerDeepElement => "deep_element",
            _ => "deep_wuxing",
        };
```

- [ ] **Step 3: 跨树节点的双前置显示**

替掉 Task 3 里那句提前返回:

```csharp
        private static void BuildChain(Transform parent, MetaState meta, PerkNodeDef def)
        {
            if (def.Tree == PerkTree.Cross) { BuildCrossPrereq(parent, meta, def); return; }
            // …… 既有的同枝直链画法不变 ……
        }

        /// <summary>跨树节点的前置是**谓词**(「五行任一枝点到第 3 层」),不是同枝直链。
        /// 逐条列出并标满足度 —— 玩家要能看出「差哪一侧、还差几个」。</summary>
        private static void BuildCrossPrereq(Transform parent, MetaState meta, PerkNodeDef def)
        {
            SectionLabel(parent, Strings.T("perk.detail.section.cross_prereq"));
            foreach (var req in def.Prereq)
            {
                int have = PerkRules.CountOwned(meta, req.Tree, req.MinDepth);
                bool met = have >= req.Count;
                string label = Strings.T($"perk.detail.cross_req.{TreeKey(req.Tree)}",
                    ("depth", req.MinDepth));
                string status = met
                    ? Strings.T("perk.detail.cross_req.met", ("count", have), ("need", req.Count))
                    : Strings.T("perk.detail.cross_req.gap",
                        ("gap", req.Count - have), ("count", have), ("need", req.Count));
                BuildRequirementBox(parent, label, status, met);
            }
        }

        private static string TreeKey(PerkTree tree) => tree switch
        {
            PerkTree.Wuxing => "wuxing",
            PerkTree.Passive => "passive",
            PerkTree.Mechanic => "mechanic",
            _ => "wuxing",
        };
```

⚠ 复用既有的 `BuildRequirementBox(parent, label, status, met)` —— 它是「解锁条件」那两栏
(角色等级 / 墨锭)用的同一个画法。跨树前置在语义上就是第三、第四条解锁条件,
用同一个画法是对的,别另画一套。

- [ ] **Step 4: 头部与详解文案接上跨树节点**

- 面包屑:跨树节点没有「第 N 层」。`perk.detail.breadcrumb` 是
  `"{tree} · {branch} · 第{depth}层"`,对跨树节点改成只印 `{tree} · {branch}`
  (加一个 `perk.detail.breadcrumb.cross` key,值 `"{tree} · {branch}"`,Task 5 若漏了在此补上)。
- `WatermarkChar(def)`:跨树节点取名字第一个字(相/融/博)。
- 详解段落:`perk.detail.{effect}` 那一套是按 `PerkEffect` 取的,跨树节点会取到
  `perk.detail.attack_percent`(讲的是被动树的攻击加成),**不对**。跨树节点按 **id** 取:
  `perk.detail.cross_vigor` / `cross_edge` / `cross_draw`。加一个前置判断:

```csharp
            string explainKey = def.Tree == PerkTree.Cross
                ? $"perk.detail.{def.Id}"
                : $"perk.detail.{EffectKey(def.Effect)}";
```

- [ ] **Step 5: 底部操作钮确认覆盖跨树节点**

`FooterButtonText(state, def)` 是按 `NodeState` 取文案的,跨树节点走同一套五态
(Task 3 已把前置判定接到 `PrereqMet`),**应当自动正确**。但
`perk.node.badge.gated_prereq` 的文案是「需先点上一层」,对跨树节点是错的。
加一个分支:

```csharp
            NodeState.GatedPrereq => def.Tree == PerkTree.Cross
                ? Strings.T("perk.node.badge.gated_cross")   // 「需两侧前置」
                : Strings.T("perk.node.badge.gated_prereq"),
```

并在字符串表补 `"perk.node.badge.gated_cross": "需两侧前置"`。

⚠ 这个 key Task 5 没列到,**在这里补**,并**重跑一次字体子集**(「侧」字确认在子集里)。

- [ ] **Step 6: 跑离线编译**

```bash
cd /Users/eugenewu/code/game/tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

Expected: 0 个 `error CS`。

- [ ] **Step 7: 重跑字体子集 + 全量 pytest**

```bash
cd /Users/eugenewu/code/game && python3 tools/fonts/subset_fonts.py && python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ -q
```

Expected: 全绿。

- [ ] **Step 8: 跑 Core 测试**

```bash
cd /Users/eugenewu/code/game/tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

Expected: 全绿。

- [ ] **Step 9: 提交**

```bash
cd /Users/eugenewu/code/game && git add Brushblade/Assets/_Project/Presentation/PerkNodeSheet.cs Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json Brushblade/Assets/_Project/Presentation/UI/Resources && git commit -m "$(cat <<'EOF'
feat(presentation): 节点详情改右侧推出,补跨树双前置与数值构成

画布能滑之后常驻面板就是白占视野,所以是推出不是常驻(宽 396,盖在画布上)。

数值构成拆成「基础 / 已点亮 N 个 → +M / 合计」三行 —— 只印合计值,玩家无从
判断该不该再投。计数走 PerkRules.ScaleCountOf,不在这一层重算。

跨树前置逐条列出并标满足度,复用「解锁条件」那两栏的同一个画法 ——
它在语义上就是第三、第四条解锁条件。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## 收尾:合并与人眼验收

- [ ] **全量验证三件套**

```bash
cd /Users/eugenewu/code/game/tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
```

```bash
cd /Users/eugenewu/code/game/tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```

```bash
cd /Users/eugenewu/code/game && python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ tools/icons/tests/ -q
```

- [ ] **合并回 main**

⚠ **先退出 worktree**:main 正被主检出 checkout 着,从 worktree 往它 push 会被 git 拒
(`branch is currently checked out`),`cd` / `-C` 指向主检出也会被会话隔离拦下。
用 `ExitWorktree` 回到主检出再 merge —— 那是唯一一条通路。

- [ ] **人眼验收清单**(Presentation 没有自动化测试,下面这些全绿的测试一条都抓不到)

Unity EditMode 集成验证(编辑器开着时会因项目锁失败,让用户在 Test Runner 里跑):

```bash
cd /Users/eugenewu/code/game && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath Brushblade -runTests -testPlatform EditMode -testResults /tmp/results.xml -logFile /tmp/unity_test.log
```

然后进游戏逐条看:

1. **43 个节点全在画布上**,一个不漏、一个不叠。捏合到底能一屏看完。
2. **五态在极坐标下都认得出**:已点亮 / 可解锁(主色外发光)/ 墨锭不足 / 前置未点(虚线)/ 等级未到(纯灰)。
3. **跨树节点的外圈虚线环**画出来了,且连到的是**两棵树的树根**而不是某个具体节点。
4. **前置没满足的跨树节点显示成「前置未点」而不是「可解锁」**(Task 3 修的那个静默 bug —— 这是本次最容易复发的一条)。
5. **跳转锚点**四个都能跳过去;跳完**缩略图红框跟着动**。
6. **缩放后再跳转不跳偏**(`JumpTo` 用的是新 `_zoom`)。
7. **详情面板从右侧推出**,不遮住被点的那个节点太多。
8. **数值构成三行都印出来了**;点一个刚满足前置的博采,第二行印的是提示语而不是「+0」。
9. **节点名字在 52px 的圆里放得下**(两个字);低于 `DetailZoom` 时不画图标与名字。
10. **图标不是「?」**(三处对账全接上了);兜底汉字不是豆腐块(字体子集跑过了)。
11. 拖动画布时**不卡**(拖拽期间没有重绘)。

---

## Self-Review 记录

**Spec 覆盖**:§2 拓扑 → Task 6(`PerkLayout`);§3 三个跨树节点 → Task 2;§3.2 保底值 → Task 1(字段)+ Task 2(数值)+ Task 7(显示);§3.3 融会改名 → Task 5;§3.4 博采口径 → Task 2;§4 前置模型 → Task 1 + Task 3;§5 效果缩放 → Task 1;§5.1 拆分显示 → Task 7;§6 门槛定价 → Task 2;§7 图标 → Task 4;§8 UI → Task 6 + Task 7;§10 测试 → Task 1/2 的测试文件 + 收尾的人眼清单。**无遗漏**。

**计划外新增的一条**:spec 没写 `PerkView.StateOf` 里那份重复的前置推导。它是读代码时发现的
真实缺陷(跨树节点 `Depth == 1` 会被误判成可解锁),已独立成 Task 3。

**类型一致性**:`PerkRules.CountOwned(meta, tree, minDepth)` / `ScaleCountOf(meta, def)` /
`PrereqMet(meta, def)` 三个签名在 Task 1 定义,Task 2/3/6/7 的调用与之逐字一致;
`PerkLayout.Place` 返回 `Vector2`(Task 6 定义并自用);`PerkNodeIcons.KeyFor(def)` 返回 `string`。
