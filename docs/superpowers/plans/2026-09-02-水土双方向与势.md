# 水土双方向与「势 / 水势」实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让水系 15 张、土系 13 张实体字各有「攻击」与「治疗/加盾」两个互斥方向,并给土/水各接一台资源引擎(势/水势),同时补齐五行护甲怪 —— 解决试玩中土系几乎不带、水系只带控制字的失衡。

**Architecture:** 三步走。**步 1「接线」**只加机制不动数值,靠 `ScaleByBaseAttack` 在基准 ATK=100 时恒等、势 0 层时 +0%,做到既有 1000+ 测试全绿、Tests 纯追加。**步 2「配值」**重配 28 张水/土字与 10 只护甲怪,断言随之重算。**步 3「文案」**补字符串表并重跑字体子集。

**Tech Stack:** Unity 6000.5.2f1 / C# / 四层 asmdef(`Presentation → {Core, Data, Platform}`,`Data → Core`)。Core 与 Data 禁引用 UnityEngine。测试用 NUnit,跑在 `tools/coretests` 的 dotnet 工装上。

**Spec:** `docs/superpowers/specs/2026-09-02-水土双方向与势-design.md`

## Global Constraints

这些约束适用于**每一个** task,不再逐条重复:

- **工作目录**:`/Users/eugenewu/code/game/.claude/worktrees/earth-water-dual-direction`。所有命令从这里跑,**不要 `cd` 回主检出**,不要切分支。
- **Core / Data 禁止引用 UnityEngine**(asmdef 已设 `noEngineReferences: true`)。
- **随机性一律走 Core 内带种子的 `GameRandom`**,禁用 `UnityEngine.Random`。
- **玩家可见 UI 文案走字符串表**(`Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`),禁止硬编码中文。
- **测试断言只用 Unity 版 NUnit 也支持的 API**:
  - 禁用 `Is.AnyOf` / `Is.All.AnyOf`。多选一写 `Is.EqualTo(a).Or.EqualTo(b)`。
  - `Does.Not.Contain(x)` **只在 x 是 string 时可用**。集合判包含一律写成 `Assert.That(list.Contains(x), Is.True, "…")`。
  - 集合子集用 `Has.All.Matches<T>`。
- **测试里定位仓库根只能用 `TestContext.CurrentContext.TestDirectory`**,禁用 `AppContext.BaseDirectory`(后者在 Unity Test Runner 下指向编辑器安装目录)。
- **测试代码禁止直接引用 Newtonsoft**(`JsonConvert` 等)。要测序列化走 `Data.SaveSerializer` / `Data.ConfigLoader` 这些真实入口。测试只能用 Tests asmdef references 列出的程序集(Core / Data)。
- **浮点比较夹噪声到 4 位小数**,别配 `Ceiling`(Unity Mono 与 .NET 8 的中间精度不同)。
- **提交信息用 conventional commits**(`feat/fix/docs/chore` + 范围),中文正文。每个 commit 结尾加:
  ```
  Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
  ```
- **只提交你这个 task 改的文件**。用 `git add <具体路径>`,**禁止 `git add -A` 或 `git add .`** —— 工作区里可能有别人未完成的改动。

### 验证命令(逐字照抄)

```bash
# Core/Data 单测 —— 每个 task 都要跑
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q

# Presentation 离线编译 —— 只有改了 Presentation/ 的 task 要跑
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies

# 管线 + 字体 —— 只有 Task 14 要跑
ln -s /Users/eugenewu/code/game/tools/fonts/raw tools/fonts/raw   # 若 tools/fonts/raw 不存在才建
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
```

只看 `error CS`。`warning MSB3245` 是 Unity 程序集自带的无关引用,忽略。

### 恒等性硬线(步 1 专属)

Task 1–7 做完后,**既有测试必须全绿,新测试纯追加**。任何一条既有断言变红都说明接线接错了,**不是"预期内的重平衡"** —— 停下来查,别改断言迁就实现。

---

## File Structure

| 文件 | 职责 | 涉及 Task |
|---|---|---|
| `Brushblade/Assets/_Project/Core/StatusEffect.cs` | `StatusKind` 追加 `Momentum` / `WaterPower` | 1 |
| `Brushblade/Assets/_Project/Core/EffectDef.cs` | `EffectKind` 追加 `SpendMomentum` / `SpendWaterPower` | 4 |
| `Brushblade/Assets/_Project/Core/BattleEngine.cs` | 势/水势累积与被动、引爆结算、`ScaleByBaseAttack`、快照往返 | 2,4,5 |
| `Brushblade/Assets/_Project/Core/RunSnapshot.cs` | `BattleSnapshot` / `RunSnapshot` 新增余数字段 | 3 |
| `Brushblade/Assets/_Project/Core/RunEngine.cs` | 携带态白名单 + 余数跨战斗 | 3 |
| `Brushblade/Assets/_Project/Core/WuxingResolver.cs` | 删 `ShengMultiplier`,`ResolveEffect` 去参数 | 6 |
| `Brushblade/Assets/_Project/Core/EnemyDef.cs` | 新增 `MinDepth` | 8 |
| `Brushblade/Assets/_Project/Core/Campaign.cs` | `Scale` 传递 `MinDepth` | 8 |
| `Brushblade/Assets/_Project/Core/Endless.cs` | `WithinDepth` 深度闸 | 8 |
| `Brushblade/Assets/_Project/Data/ConfigLoader.cs` | 解析 `minDepth`,新 `EffectKind` 自动走枚举名 | 8 |
| `Brushblade/Assets/_Project/Presentation/BattleView.cs` | 方向选择器状态 `_directionPicking` | 7 |
| `Brushblade/Assets/StreamingAssets/config/chars.json` | 28 张水/土字双方向 + 火系 2 个数 + 相生写死 3 张 | 6,10,11,12 |
| `Brushblade/Assets/StreamingAssets/config/enemies.json` | 10 只护甲怪 | 9 |
| `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json` | 方向按钮 2 条 + 怪名 | 14 |
| `Brushblade/Assets/_Project/Tests/MomentumTests.cs` | **新建**:势/水势全部测试 | 2,3,4,5 |
| `Brushblade/Assets/_Project/Tests/DualDirectionTests.cs` | **新建**:双方向锚点一致性 | 10,11 |
| `Brushblade/Assets/_Project/Tests/ArmoredEnemyTests.cs` | **新建**:护甲怪覆盖与 MinDepth | 8,9 |
| `tools/balance/Program.cs` | `Power()` 记分 + 水土画像 | 13 |
| `docs/design/wuxing-reference.md` | 相生规则删除 | 6 |
| `CLAUDE.md` | 相生描述删除 | 6 |

## 并发分组

**同一个 worktree,并行 agent 共用一个 HEAD** —— 写冲突是唯一判据,任务编号不是。

| 组 | Task | 能否并行 | 理由 |
|---|---|---|---|
| A | 1 → 2 → 4 → 5 | **串行** | 全部改 `BattleEngine.cs`,热点冲突 |
| B | 3 | 在 2 之后 | 改 `RunSnapshot.cs` + `RunEngine.cs` + `BattleEngine.cs` 快照段 |
| C | 6 | 在 5 之后 | 改 `WuxingResolver.cs` + `BattleEngine.cs` 全部调用点 |
| D | 7 | **可与 A/B/C 并行** | 只改 `Presentation/BattleView.cs`,与 Core 无写冲突 |
| E | 8 → 9 | 串行,在 D 之后 | `EnemyDef/Campaign/Endless/ConfigLoader` + `enemies.json` |
| F | 10 → 11 → 12 | 串行,在 6 之后 | 全部改 `chars.json` 同一文件 |
| G | 13 | 在 F 之后 | 只改 `tools/balance/Program.cs` |
| H | 14 | 最后 | 依赖 9 的怪名与 7 的按钮文案 |

---

## 步 1 · 接线(恒等性必须保住)

### Task 1: StatusKind 追加势与水势

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/StatusEffect.cs`
- Test: `Brushblade/Assets/_Project/Tests/CritStatTests.cs:492`

**Interfaces:**
- Consumes: 无
- Produces: `StatusKind.Momentum`(序号 21)、`StatusKind.WaterPower`(序号 22)

- [ ] **Step 1: 写失败测试**

在 `CritStatTests.cs` 的 `StatusKindOrdinals_AreLockedForSaveCompatibility` 方法末尾(`Assert.That((int)StatusKind.DodgeBuff, Is.EqualTo(20), ...)` 那行之后)追加:

```csharp
            // 2026-09-02(水土双方向):势与水势。追加在末尾,DodgeBuff(20) 之后。
            Assert.That((int)StatusKind.Momentum, Is.EqualTo(21), "新值必须追加在末尾");
            Assert.That((int)StatusKind.WaterPower, Is.EqualTo(22), "新值必须追加在末尾");
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 编译失败,`error CS0117: 'StatusKind' does not contain a definition for 'Momentum'`

- [ ] **Step 3: 实现**

在 `StatusEffect.cs` 的 `StatusKind` 枚举末尾(`DodgeBuff` 那一项之后)追加:

```csharp
        Momentum,         // 势(2026-09-02,土):Magnitude = **层数**,每层 +5% 伤害,上限 10。
                          // 来源是**获得护盾的量**(不是挨打),每攒够 MaxHp/10 涨一层。
                          // 与 Morale 同型:Magnitude 是层数不是加成值,进 EffectiveAttack 的
                          // 百分比乘区;混进 AttackBuff 会既丢层数上限又让 +1 层被当成 +1 攻击。
        WaterPower,       // 水势(2026-09-02,水):Magnitude = **层数**,每层 +10% 治疗,上限 10。
                          // 来源是治疗的**名义值**(不是实际回血)——满血溢出照样攒,
                          // 这正是「满血奶自己不亏」那条诉求的落点。
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 全绿。**既有测试一条都不该变红** —— 枚举追加在末尾不影响任何现有行为。

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/StatusEffect.cs Brushblade/Assets/_Project/Tests/CritStatTests.cs
git commit -m "$(cat <<'EOF'
feat(core): StatusKind 追加 Momentum(21) 与 WaterPower(22)

追加在末尾,序号由 StatusKindOrdinals_AreLockedForSaveCompatibility 锁住。
两者的 Magnitude 都是层数(与 Morale 同型),不是加成值。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: 势/水势的累积与被动增幅

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`
- Test: `Brushblade/Assets/_Project/Tests/MomentumTests.cs`(新建)

**Interfaces:**
- Consumes: `StatusKind.Momentum` / `StatusKind.WaterPower`(Task 1)
- Produces:
  - `BattleEngine.MomentumStacks` → `int`(公开只读,给 UI 与测试)
  - `BattleEngine.WaterPowerStacks` → `int`
  - `BattleEngine.ShieldAccum` → `int`(余数,给 Task 3 的快照)
  - `BattleEngine.HealAccum` → `int`
  - `private void GainMomentum(int shieldAmount)`
  - `private void GainWaterPower(int healAmount)`
  - `const int MomentumPercentPerStack = 5`
  - `const int WaterPowerPercentPerStack = 10`
  - `const int MaxResourceStacks = 10`

- [ ] **Step 1: 写失败测试**

新建 `Brushblade/Assets/_Project/Tests/MomentumTests.cs`。先照抄本仓库既有测试的 using 与夹具风格 —— 打开 `Brushblade/Assets/_Project/Tests/CritStatTests.cs` 看它怎么造 `BattleEngine`(构造参数、`RecipeGraph`、`BattleConfig`),照同一套写。夹具要求:`PlayerMaxHp = 500`、`PlayerAttack = 100`(基准,保证恒等)。

```csharp
        [Test]
        public void Shield_AtThreshold_GainsOneMomentumStack()
        {
            // MaxHp 500 → 阈值 50。加 50 盾 = 1 层。
            var battle = NewBattle(maxHp: 500);
            battle.GainMomentumForTest(50);
            Assert.That(battle.MomentumStacks, Is.EqualTo(1));
            Assert.That(battle.ShieldAccum, Is.EqualTo(0), "整除时余数归零");
        }

        [Test]
        public void Shield_BelowThreshold_KeepsRemainderAndNoStack()
        {
            var battle = NewBattle(maxHp: 500);
            battle.GainMomentumForTest(30);
            Assert.That(battle.MomentumStacks, Is.EqualTo(0));
            Assert.That(battle.ShieldAccum, Is.EqualTo(30), "不足一层的量要留着,下次接着攒");
        }

        [Test]
        public void Shield_AccumulatesAcrossCalls()
        {
            var battle = NewBattle(maxHp: 500);
            battle.GainMomentumForTest(30);
            battle.GainMomentumForTest(30);   // 合计 60 = 1 层 + 余 10
            Assert.That(battle.MomentumStacks, Is.EqualTo(1));
            Assert.That(battle.ShieldAccum, Is.EqualTo(10));
        }

        [Test]
        public void Momentum_CapsAtTenStacks_AndStopsAccumulatingRemainder()
        {
            var battle = NewBattle(maxHp: 500);
            battle.GainMomentumForTest(50 * 12);   // 够 12 层
            Assert.That(battle.MomentumStacks, Is.EqualTo(10), "上限 10 层");
            Assert.That(battle.ShieldAccum, Is.EqualTo(0),
                "满层后余数也不再攒 —— 否则掉层时会瞬间跳回满层");
        }

        [Test]
        public void Momentum_AddsFivePercentDamagePerStack()
        {
            var battle = NewBattle(maxHp: 500);   // PlayerAttack = 100 基准
            int baseline = battle.EffectiveAttack;
            Assert.That(baseline, Is.EqualTo(100));
            battle.GainMomentumForTest(50 * 10);  // 满 10 层
            Assert.That(battle.MomentumStacks, Is.EqualTo(10));
            Assert.That(battle.EffectiveAttack, Is.EqualTo(150), "10 层 = +50%,与战意同顶");
        }

        [Test]
        public void Heal_UsesNominalValue_SoOverhealStillGainsWaterPower()
        {
            // 这条是整套改动的核心诉求:满血时治疗一分不亏。
            var battle = NewBattle(maxHp: 500);   // 满血
            Assert.That(battle.PlayerHp, Is.EqualTo(500));
            battle.GainWaterPowerForTest(100);    // 名义治疗 100,实际回血 0
            Assert.That(battle.PlayerHp, Is.EqualTo(500), "满血不会超上限");
            Assert.That(battle.WaterPowerStacks, Is.EqualTo(2), "溢出的治疗照样攒水势");
        }

        [Test]
        public void WaterPower_CapsAtTenStacks()
        {
            var battle = NewBattle(maxHp: 500);
            battle.GainWaterPowerForTest(50 * 12);
            Assert.That(battle.WaterPowerStacks, Is.EqualTo(10));
        }
```

⚠ `GainMomentumForTest` / `GainWaterPowerForTest` 是 Step 3 要加的 `internal` 测试钩子。Tests asmdef 若没开 `InternalsVisibleTo`,就把这两个方法做成 `public`,并在 XML 注释里写明「仅供测试与引擎内部调用」。**先去 `Brushblade/Assets/_Project/Core/AssemblyInfo.cs` 看有没有 `InternalsVisibleTo`,按实际情况选**。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 编译失败,找不到 `MomentumStacks` / `GainMomentumForTest`

- [ ] **Step 3: 实现**

在 `BattleEngine.cs` 里,`_burnPerStack` 那批私有字段旁(约 `:351`)加:

```csharp
        private int _shieldAccum;           // 势的余数:不足一层的护盾量(2026-09-02)
        private int _healAccum;             // 水势的余数:不足一层的治疗名义值
```

在 `MoralePercentPerStack` 常量旁加:

```csharp
        /// <summary>势每层的伤害加成(百分点,2026-09-02)。5 × 10 层 = +50%,
        /// 与战意的 10 × 5 层 = +50% **同顶** —— 两条乘性轴一高一低会让堆盾直接压过战意。</summary>
        public const int MomentumPercentPerStack = 5;

        /// <summary>水势每层的治疗加成(百分点,2026-09-02)。</summary>
        public const int WaterPowerPercentPerStack = 10;

        /// <summary>势与水势的层数上限(2026-09-02)。</summary>
        public const int MaxResourceStacks = 10;
```

公开只读访问器,放在 `PlayerShield` 那批旁边(约 `:656`):

```csharp
        public int MomentumStacks => _playerStatuses.TotalMagnitude(StatusKind.Momentum);
        public int WaterPowerStacks => _playerStatuses.TotalMagnitude(StatusKind.WaterPower);
        public int ShieldAccum => _shieldAccum;
        public int HealAccum => _healAccum;
```

阈值与累积:

```csharp
        /// <summary>攒一层势/水势需要的量 = 玩家生命上限的十分之一(2026-09-02)。
        ///
        /// 用百分比而不是固定值:固定 100 点在早期(垒 50 盾)攒不出一层、在深层
        /// (㙓 630 盾)一次给 6 层。百分比口径自动跟着角色成长走。
        /// 下钳 1:MaxHp &lt; 10 时整数除会得 0,那会让 while 循环永不终止。</summary>
        private int ResourceThreshold => Math.Max(1, _config.PlayerMaxHp / 10);

        /// <summary>获得护盾时攒势(2026-09-02)。<paramref name="shieldAmount"/> 是
        /// **获得量**,不是实际吸伤量 —— 势衡量的是"你堆了多少防御",不是"你挨了多少打"。
        /// 满层后余数也不再攒:否则掉一层会立刻被余数补回,层数形同不掉。</summary>
        public void GainMomentumForTest(int shieldAmount) => GainMomentum(shieldAmount);

        private void GainMomentum(int shieldAmount)
        {
            GainStacks(shieldAmount, StatusKind.Momentum, "势", ref _shieldAccum);
        }

        /// <summary>治疗时攒水势(2026-09-02)。<paramref name="healAmount"/> 是
        /// **名义值**,不是实际回血量 —— 满血时治疗溢出照样攒,这是「满血奶自己不亏」的落点。</summary>
        public void GainWaterPowerForTest(int healAmount) => GainWaterPower(healAmount);

        private void GainWaterPower(int healAmount)
        {
            GainStacks(healAmount, StatusKind.WaterPower, "水势", ref _healAccum);
        }

        /// <summary>势与水势共用的攒层逻辑(2026-09-02)。两者只在 Kind、来源标识与
        /// 余数字段上不同,规则一字不差 —— 写两份迟早分叉。</summary>
        private void GainStacks(int amount, StatusKind kind, string sourceId, ref int accum)
        {
            if (amount <= 0) return;
            var existing = _playerStatuses.Find(kind);
            int stacks = existing?.Magnitude ?? 0;
            if (stacks >= MaxResourceStacks) return;   // 满层:连余数都不攒

            accum += amount;
            int threshold = ResourceThreshold;
            while (accum >= threshold && stacks < MaxResourceStacks)
            {
                accum -= threshold;
                stacks++;
            }
            if (stacks >= MaxResourceStacks) accum = 0; // 攒到顶,余数清掉

            _playerStatuses.Apply(new StatusEffect
            {
                Kind = kind,
                Polarity = StatusPolarity.Buff,
                Magnitude = stacks,
                TurnsLeft = -1,        // 持久,不随回合递减
                SourceId = sourceId,   // 单一来源:Apply() 走覆盖刷新而非叠加
            });
        }
```

势进 `EffectiveAttack` 的百分比乘区。把现有的 `percent` 那一行(约 `:397`)改成:

```csharp
                int percent = 100
                    + _playerStatuses.TotalMagnitude(StatusKind.Morale) * MoralePercentPerStack
                    // 势(2026-09-02):与战意同一个百分比乘区相加。「先加后乘」的既有顺序不动 ——
                    // Empower / AttackBuff 是加点,战意与势是乘比例。
                    + _playerStatuses.TotalMagnitude(StatusKind.Momentum) * MomentumPercentPerStack;
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 新测试全绿。**既有测试一条都不该变红** —— 势 0 层时 `percent` 仍是 `100 + 0 = 100`。

- [ ] **Step 5: 接进护盾与治疗的施加点**

在 `BattleEngine.cs` 的 `case EffectKind.Shield:` 分支里,`_events.Add(new BattleEvent(BattleEventKind.Shield, allySlot, shield));` **之前**加:

```csharp
                        // 攒势(2026-09-02):按获得量算,加给谁都一样 ——
                        // 给召唤物的盾同样是"你堆了防御"。
                        GainMomentum(shield);
```

在 `case EffectKind.HealSelf:` 分支里,`HealAlly(allySlot, heal);` **之前**加:

```csharp
                        GainWaterPower(heal);   // 名义值:满血溢出照样攒(2026-09-02)
```

在 `case EffectKind.HealAll:` 分支里,把那一行改成:

```csharp
                    case EffectKind.HealAll:
                    {
                        int healAll = WuxingResolver.ResolveEffect(value, recipeElements, attacker);
                        GainWaterPower(healAll);
                        HealPlayerAndSummons(healAll);
                        break;
                    }
```

在 `case EffectKind.HealOverTime:` 分支里,`_playerStatuses.Apply(...)` **之前**加(HoT 按**总量**攒,即每回合量 × 回合数):

```csharp
                        // HoT 按总量攒水势(2026-09-02):它承诺的治疗总量就是
                        // Magnitude × Turns,分几回合兑现不改变承诺量。
                        GainWaterPower(WuxingResolver.ResolveEffect(value, recipeElements, attacker)
                            * Math.Max(1, effect.Turns));
```

- [ ] **Step 6: 补接入点测试**

在 `MomentumTests.cs` 追加(用真实 `chars.json` 的字施放,走 `battle.Cast`):

```csharp
        [Test]
        public void Cast_ShieldChar_GainsMomentum()
        {
            // 圭 = 护盾 200(卡 1 级)。MaxHp 500 → 阈值 50 → 4 层。
            var battle = NewBattleWithChar("圭", maxHp: 500);
            battle.Cast("圭", -1);
            Assert.That(battle.MomentumStacks, Is.EqualTo(4));
        }

        [Test]
        public void Cast_HealChar_GainsWaterPower()
        {
            // 沝 = 治疗 160(卡 1 级)。阈值 50 → 3 层 + 余 10。
            var battle = NewBattleWithChar("沝", maxHp: 500);
            battle.Cast("沝", 0);
            Assert.That(battle.WaterPowerStacks, Is.EqualTo(3));
            Assert.That(battle.HealAccum, Is.EqualTo(10));
        }
```

⚠ 这两条读的是**当前**字表数值(圭 200 / 沝 160)。Task 10/11 重配后要跟着更新 —— 在测试上方写一条注释标明这个耦合。

- [ ] **Step 7: 跑全量确认**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 全绿,既有测试零变红。

- [ ] **Step 8: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/MomentumTests.cs
git commit -m "$(cat <<'EOF'
feat(battle): 势与水势的累积与被动增幅

阈值 = MaxHp/10(百分比口径,跟着角色成长走),上限 10 层,满层连余数都不攒。
势每层 +5% 伤害进 EffectiveAttack 的百分比乘区,与战意同顶(+50%)。
水势按治疗**名义值**累积 —— 满血溢出照样攒,这是「满血奶自己不亏」的落点。

势 0 层时 percent 仍是 100,既有断言逐字节恒等。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: 快照与跨战斗延续

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/RunSnapshot.cs`
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(`Capture` / `Restore` / 构造函数)
- Modify: `Brushblade/Assets/_Project/Core/RunEngine.cs`
- Test: `Brushblade/Assets/_Project/Tests/MomentumTests.cs`

**Interfaces:**
- Consumes: `BattleEngine.ShieldAccum` / `HealAccum`(Task 2)
- Produces: `BattleSnapshot.ShieldAccum` / `HealAccum`;`RunSnapshot.CarriedShieldAccum` / `CarriedHealAccum`

- [ ] **Step 1: 写失败测试**

```csharp
        [Test]
        public void Snapshot_RoundTrip_PreservesStacksAndRemainder()
        {
            var battle = NewBattle(maxHp: 500);
            battle.GainMomentumForTest(130);      // 2 层 + 余 30
            battle.GainWaterPowerForTest(70);     // 1 层 + 余 20
            var snapshot = battle.Capture();
            var restored = NewBattleFromSnapshot(snapshot, maxHp: 500);

            Assert.That(restored.MomentumStacks, Is.EqualTo(2));
            Assert.That(restored.ShieldAccum, Is.EqualTo(30), "余数漏存是静默的:续爬会丢半层");
            Assert.That(restored.WaterPowerStacks, Is.EqualTo(1));
            Assert.That(restored.HealAccum, Is.EqualTo(20));
        }

        [Test]
        public void CarriedStatuses_IncludeMomentumAndWaterPower()
        {
            // 护盾本来就整场爬塔延续(_shieldNormal),势必须跟它同步,
            // 否则每场重新攒,而护盾还留着 —— 两者会一直对不上。
            var run = NewRunAfterWinningWithMomentum();
            Assert.That(run.CarriedStatuses.Count(s => s.Kind == StatusKind.Momentum), Is.EqualTo(1));
            Assert.That(run.CarriedStatuses.Count(s => s.Kind == StatusKind.WaterPower), Is.EqualTo(1));
        }
```

`NewBattleFromSnapshot` / `NewRunAfterWinningWithMomentum` 是夹具方法。照 `Brushblade/Assets/_Project/Tests/` 里既有的快照往返测试(搜 `Capture()` 找)写,别自己发明第二套造法。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 编译失败,`BattleSnapshot` 没有 `ShieldAccum`

- [ ] **Step 3: 实现 —— BattleSnapshot**

在 `RunSnapshot.cs` 的 `BattleSnapshot` 类里,`MoraleGraceTurn` 之后追加:

```csharp
        /// <summary>势的余数:不足一层的护盾量(2026-09-02)。
        /// ⚠ 漏存是**静默**的 —— 续爬后余数归零,玩家丢掉最多一层势,而没有任何
        /// 现有测试看得见(全部测试都建新对象,没有一条读旧快照字节)。</summary>
        public int ShieldAccum { get; set; }

        /// <summary>水势的余数:不足一层的治疗名义值(2026-09-02)。同上,漏存静默。</summary>
        public int HealAccum { get; set; }
```

- [ ] **Step 4: 实现 —— BattleEngine 往返**

在 `Capture()` 里,`BurnPerStack = _burnPerStack,` 那行旁加:

```csharp
                ShieldAccum = _shieldAccum,
                HealAccum = _healAccum,
```

在 `Restore` 那段(约 `:623`,`_burnPerStack = snapshot.BurnPerStack,` 旁)加:

```csharp
                _shieldAccum = snapshot.ShieldAccum,
                _healAccum = snapshot.HealAccum,
```

在 `BattleEngine` 构造函数上追加两个参数(接在 `startingPersistShield` 之后,**给默认值 0**,这样既有的 1000+ 处调用一个都不用改 —— 这是恒等性的关键):

```csharp
            int startingShieldAccum = 0, int startingHealAccum = 0,
```

并在构造体里赋值:

```csharp
            _shieldAccum = startingShieldAccum;
            _healAccum = startingHealAccum;
```

- [ ] **Step 5: 实现 —— RunSnapshot 与 RunEngine**

在 `RunSnapshot` 类里(`CarriedPersistShield` 旁)加:

```csharp
        public int CarriedShieldAccum { get; set; }
        public int CarriedHealAccum { get; set; }
```

在 `RunEngine.cs` 加私有字段(`_carriedPersistShield` 旁,约 `:70`):

```csharp
        private int _carriedShieldAccum;
        private int _carriedHealAccum;
```

`Capture()` 里(`CarriedPersistShield = _carriedPersistShield,` 旁)加:

```csharp
                CarriedShieldAccum = _carriedShieldAccum,
                CarriedHealAccum = _carriedHealAccum,
```

`Restore` 那段(约 `:184`)加:

```csharp
                _carriedShieldAccum = snapshot.CarriedShieldAccum,
                _carriedHealAccum = snapshot.CarriedHealAccum,
```

战后捕获那段(约 `:534`,`_carriedPersistShield = Battle.ShieldPersist;` 之后)加:

```csharp
            _carriedShieldAccum = Battle.ShieldAccum;
            _carriedHealAccum = Battle.HealAccum;
```

**携带态白名单**(约 `:539`)—— 把

```csharp
                .Where(s => s.Kind == StatusKind.DefenseBuff)
```

改成

```csharp
                // 势/水势跟护盾同步跨战斗(2026-09-02):护盾本来就整场爬塔延续
                // (_shieldNormal),势不跟着延续的话每场重攒,而护盾还留着 —— 两者永远对不上。
                .Where(s => s.Kind == StatusKind.DefenseBuff
                    || s.Kind == StatusKind.Momentum
                    || s.Kind == StatusKind.WaterPower)
```

最后,`NewBattle(...)` 那处(约 `:762`)把两个余数传进 `BattleEngine` 构造:

```csharp
                _carriedNormalShield, _carriedPersistShield, _carriedShieldAccum, _carriedHealAccum,
                _carriedSummons, _carriedStatuses);
```

⚠ **参数位置要与 Step 4 加的构造参数顺序一致**。加完先编译,别靠猜。

- [ ] **Step 6: 跑测试确认通过**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 全绿,既有测试零变红(新参数都有默认值)。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/_Project/Core/RunSnapshot.cs Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Core/RunEngine.cs Brushblade/Assets/_Project/Tests/MomentumTests.cs
git commit -m "$(cat <<'EOF'
feat(core): 势/水势进快照并跨战斗延续

余数(ShieldAccum/HealAccum)进 BattleSnapshot 与 RunSnapshot —— 漏存是静默的,
续爬会丢最多一层。携带态白名单从只有 DefenseBuff 扩到含 Momentum/WaterPower,
与护盾整场延续同步。

新构造参数一律给默认值 0,既有 1000+ 处调用零改动。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: 引爆(SpendMomentum / SpendWaterPower)

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EffectDef.cs`
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`
- Test: `Brushblade/Assets/_Project/Tests/MomentumTests.cs`

**Interfaces:**
- Consumes: `MomentumStacks` / `WaterPowerStacks`(Task 2)
- Produces: `EffectKind.SpendMomentum`、`EffectKind.SpendWaterPower`

- [ ] **Step 1: 写失败测试**

```csharp
        [Test]
        public void SpendMomentum_DealsStacksTimesValueToAll_AndClearsStacks()
        {
            var battle = NewBattleWithTwoEnemies(maxHp: 500);
            battle.GainMomentumForTest(50 * 4);   // 4 层
            int hp0 = battle.Enemies[0].Hp, hp1 = battle.Enemies[1].Hp;

            battle.CastEffectForTest(new EffectDef(EffectKind.SpendMomentum, 60));

            // 4 层 × 60 = 240,过 ScaleByAttack(基准 100 → ×1)与相克
            Assert.That(hp0 - battle.Enemies[0].Hp, Is.GreaterThan(0));
            Assert.That(hp1 - battle.Enemies[1].Hp, Is.GreaterThan(0), "是全体效果");
            Assert.That(battle.MomentumStacks, Is.EqualTo(0), "引爆清空全部层数");
        }

        [Test]
        public void SpendMomentum_AtZeroStacks_IsNoOp_ButSiblingEffectsStillFire()
        {
            // 崩 = 全体伤害 + 发势。0 层时 AOE 那一半仍该打出来 ——
            // 「0 层就整张字拒出」会把 AOE 一起吞掉。
            var battle = NewBattleWithTwoEnemies(maxHp: 500);
            Assert.That(battle.MomentumStacks, Is.EqualTo(0));
            int before = battle.Enemies[0].Hp;

            battle.CastCharForTest("崩");

            Assert.That(battle.Enemies[0].Hp, Is.LessThan(before), "AOE 那一半照常生效");
            Assert.That(battle.MomentumStacks, Is.EqualTo(0));
        }

        [Test]
        public void SpendWaterPower_DealsStacksTimesValueToAll_AndClearsStacks()
        {
            var battle = NewBattleWithTwoEnemies(maxHp: 500);
            battle.GainWaterPowerForTest(50 * 5);   // 5 层
            battle.CastEffectForTest(new EffectDef(EffectKind.SpendWaterPower, 80));
            Assert.That(battle.WaterPowerStacks, Is.EqualTo(0));
        }

        [Test]
        public void SpendMomentum_DoesNotNeedTarget()
        {
            // 全体效果,与全体驱散(淡)、全体引爆(炸)同处理
            var def = new CharDef("测", Element.Earth,
                effects: new[] { new EffectDef(EffectKind.SpendMomentum, 60) });
            Assert.That(BattleEngine.NeedsTarget(def), Is.False);
        }
```

`CastEffectForTest` / `CastCharForTest` / `NewBattleWithTwoEnemies` 是夹具方法,照既有测试的造法写。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 编译失败,`EffectKind` 没有 `SpendMomentum`

- [ ] **Step 3: 实现 —— EffectKind**

在 `EffectDef.cs` 的 `EffectKind` 枚举末尾追加(`EffectKind` **不进存档**,`ConfigLoader` 按枚举**名字**解析,所以位置自由):

```csharp
        SpendMomentum,   // 发势(2026-09-02,土):清空全部势,对全体造成 层数 × Value 伤害。
                         // 0 层时空转 —— 不发事件、零伤害,但**同字的其他效果照常生效**
                         // (崩 = 全体伤害 + 发势,0 层时 AOE 那一半不能被吞掉)。
        SpendWaterPower, // 泻(2026-09-02,水):清空全部水势,对全体造成 层数 × Value 伤害。
                         // 规则与 SpendMomentum 一字不差,只是读另一个资源。
```

- [ ] **Step 4: 实现 —— 结算分支**

在 `ApplyEffects` 的 `switch` 里加两个 case(放在 `case EffectKind.Detonate:` 附近,与其他全体效果做邻居):

```csharp
                    case EffectKind.SpendMomentum:
                        SpendResource(StatusKind.Momentum, value, attacker, recipeElements);
                        break;
                    case EffectKind.SpendWaterPower:
                        SpendResource(StatusKind.WaterPower, value, attacker, recipeElements);
                        break;
```

并加实现方法(放在 `SettleBurnOn` 附近):

```csharp
        /// <summary>引爆一条资源(2026-09-02):清空层数,对全体存活敌人造成
        /// <c>层数 × perStack</c> 伤害。势与水势共用这一份 —— 两者规则一字不差。
        ///
        /// 0 层时**直接返回**:不发事件、不造成伤害,但调用方(ApplyEffects 的 foreach)
        /// 会继续走同一张字的其他效果 —— 崩 的 AOE 那一半不能被吞掉。
        ///
        /// 走 DamageEnemy 而不是自己扣血:相克、护甲、暴击、反噬、分裂那一整套
        /// 都在那条链路上,绕过去就得抄一遍。</summary>
        private void SpendResource(StatusKind kind, int perStack, Element attacker,
            IReadOnlyList<Element> recipeElements)
        {
            int stacks = _playerStatuses.TotalMagnitude(kind);
            if (stacks <= 0 || perStack <= 0) return;

            _playerStatuses.Remove(kind);

            int damage = ScaleByAttack(stacks * perStack);
            // 取 Count 快照:分裂(叠字怪)会在循环里往 _enemies 追加,
            // 新生成的克隆不该被同一发引爆再打一次(与 DamageAll 同口径)。
            int count = _enemies.Count;
            for (int i = 0; i < count; i++)
                if (_enemies[i].Alive)
                    DamageEnemy(i, damage, recipeElements, attacker, crit: RollCrit());
        }
```

⚠ `DamageEnemy` 的确切签名去 `BattleEngine.cs` 里 `case EffectKind.DamageAll:` 那段抄 —— 照它怎么调就怎么调,别自己拼参数。

- [ ] **Step 5: 跑测试确认通过**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 除 `SpendMomentum_AtZeroStacks_...` 那条外全绿 —— 那条要等 Task 11 给 `崩` 配上发势才能通过。**先把它标 `[Ignore("等 Task 11 给崩配发势")]`,Task 11 里取消 Ignore。**

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/_Project/Core/EffectDef.cs Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/MomentumTests.cs
git commit -m "$(cat <<'EOF'
feat(battle): 引爆 SpendMomentum / SpendWaterPower

清空层数,对全体造成 层数 x Value 伤害,走 DamageEnemy 复用相克/护甲/暴击那条链路。
0 层时空转但不吞同字的其他效果 —— 崩 是「全体伤害 + 发势」的复合字。
两者都是全体效果,不进 NeedsTarget。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: 护盾/治疗接上角色攻击成长

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`
- Test: `Brushblade/Assets/_Project/Tests/MomentumTests.cs`

**Interfaces:**
- Consumes: 无
- Produces: `private int ScaleByBaseAttack(int value)`

- [ ] **Step 1: 写失败测试**

```csharp
        [Test]
        public void Shield_ScalesWithCharacterAttack()
        {
            // 圭 = 护盾 200。ATK 150(26 级)→ 300。
            var battle = NewBattleWithChar("圭", maxHp: 500, playerAttack: 150);
            battle.Cast("圭", -1);
            Assert.That(battle.PlayerShield, Is.EqualTo(300));
        }

        [Test]
        public void Shield_AtBaselineAttack_IsIdentical()
        {
            // 恒等性硬线:ATK = 100 时一分不差
            var battle = NewBattleWithChar("圭", maxHp: 500, playerAttack: 100);
            battle.Cast("圭", -1);
            Assert.That(battle.PlayerShield, Is.EqualTo(200));
        }

        [Test]
        public void Shield_IgnoresMomentumAndMorale_NoFeedbackLoop()
        {
            // 这条是正反馈环的哨兵,删了会悄悄退化回去:
            // 若护盾读 EffectiveAttack,就成了 堆盾 → 涨势 → 势放大护盾 → 涨更多势。
            var battle = NewBattleWithChar("圭", maxHp: 500, playerAttack: 100);
            battle.GainMomentumForTest(50 * 10);            // 满 10 层势 = EffectiveAttack 150
            battle.ApplyMoraleForTest(5);                   // 满 5 层战意
            Assert.That(battle.EffectiveAttack, Is.GreaterThan(100), "伤害侧确实被放大了");

            battle.Cast("圭", -1);
            // 圭 加盾前已有的势带来的盾不算:这里断言的是这一次施放的增量
            Assert.That(battle.PlayerShield, Is.EqualTo(200),
                "护盾只认 config.PlayerAttack,不吃势也不吃战意");
        }
```

`ApplyMoraleForTest` 若没有现成钩子,就用真实的战意字(`战` / `刺`)施放来堆层,或加一个与 `GainMomentumForTest` 同型的 `internal` 钩子。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: `Shield_ScalesWithCharacterAttack` 失败(实际 200,期望 300)

- [ ] **Step 3: 实现**

在 `ScaleByAttack` 旁加:

```csharp
        /// <summary>按**角色等级的**攻击力缩放一个防御向输出(护盾/治疗,2026-09-02)。
        ///
        /// ⚠ 读的是 <c>_config.PlayerAttack</c>,**不是 <see cref="EffectiveAttack"/>**。
        /// 用后者会造出一个正反馈环:势进 EffectiveAttack 的百分比乘区 → 护盾吃
        /// EffectiveAttack → 堆盾涨势 → 势放大护盾 → 涨更多势。10 层上限能兜住不爆炸,
        /// 但「战意(连续出字的节奏奖励)放大护盾」「势放大自己的来源」两条语义都荒谬。
        ///
        /// 要表达的是「笔力越深,写什么都更重」这一层等级成长,局内增益不在内。
        /// 基准值下逐字节恒等:<c>v * 100 / 100 == v</c>(E-b1 立的硬线)。</summary>
        private int ScaleByBaseAttack(int value) =>
            value * _config.PlayerAttack / BattleConfig.AttackBaseline;
```

然后四处套用:

`case EffectKind.Shield:` 里把

```csharp
                        int shield = WuxingResolver.ResolveEffect(value, recipeElements, attacker);
```

改成

```csharp
                        int shield = ScaleByBaseAttack(
                            WuxingResolver.ResolveEffect(value, recipeElements, attacker));
```

`case EffectKind.HealSelf:` 里同样把 `int heal = WuxingResolver.ResolveEffect(...)` 包一层 `ScaleByBaseAttack(...)`。

`case EffectKind.HealAll:` 里把 Task 2 加的 `int healAll = ...` 包一层。

`case EffectKind.HealOverTime:` —— 这一支有**两个不同的量**,别混:`Magnitude` 是**每回合量**,攒水势用的是**总量**(每回合量 × 回合数)。抽一个局部变量,两处都从它算:

```csharp
                    case EffectKind.HealOverTime:
                    {
                        int perTurn = ScaleByBaseAttack(
                            WuxingResolver.ResolveEffect(value, recipeElements, attacker));
                        // 按**总量**攒水势:HoT 承诺的治疗总量就是 每回合量 × 回合数,
                        // 分几回合兑现不改变承诺量。
                        GainWaterPower(perTurn * Math.Max(1, effect.Turns));
                        _playerStatuses.Apply(new StatusEffect
                        {
                            Kind = StatusKind.HealOverTime, Polarity = StatusPolarity.Buff,
                            Magnitude = perTurn,          // 每回合量
                            TurnsLeft = effect.Turns, TargetAll = effect.TargetAll,
                            TargetSlot = allySlot,
                            SourceId = $"{def.Id}#{_statusSerial++}",
                        });
                        break;
                    }
```

⚠ Task 6 会把 `WuxingResolver.ResolveEffect(value, recipeElements, attacker)` 改成 `WuxingResolver.ResolveEffect(value)`。这里先按当前签名写,Task 6 统一改。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 全绿。**既有测试零变红** —— 夹具的 `PlayerAttack` 缺省是 `AttackBaseline = 100`。

若有既有断言变红,**先查那条测试的夹具是不是显式注入了非 100 的 ATK**,再决定是改夹具还是改断言。**不要为了让测试变绿而回退实现。**

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/_Project/Tests/MomentumTests.cs
git commit -m "$(cat <<'EOF'
feat(battle): 护盾/治疗接上角色攻击成长(ScaleByBaseAttack)

读 config.PlayerAttack 而非 EffectiveAttack —— 后者会造出正反馈环
(堆盾→涨势→势放大护盾→涨更多势)。表达的是等级成长,局内增益不在内。
基准 ATK=100 时逐字节恒等。

带一条哨兵测试:满势满战意下护盾产出一分不变。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: 取消相生

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/WuxingResolver.cs`
- Modify: `Brushblade/Assets/_Project/Core/BattleEngine.cs`(全部 `ResolveEffect` 调用点)
- Modify: `Brushblade/Assets/StreamingAssets/config/chars.json`(焚 / 蒸 / 刲 三张)
- Modify: `docs/design/wuxing-reference.md`
- Modify: `CLAUDE.md`
- Test: `Brushblade/Assets/_Project/Tests/WuxingResolverTests.cs`

**Interfaces:**
- Consumes: 无
- Produces: `WuxingResolver.ResolveEffect(int baseValue, Element attacker, Element defender)`(去掉 `recipeElements` 参数)、`WuxingResolver.ResolveEffect(int baseValue)`(无对抗目标版,现在等于恒等函数)

- [ ] **Step 1: 写失败测试**

在 `WuxingResolverTests.cs` 里,**删掉**所有 `ShengMultiplier` 的测试,并追加:

```csharp
        [Test]
        public void ResolveEffect_NoLongerAppliesSheng()
        {
            // 相生 x3 已取消(2026-09-02 用户拍板):全表只有 4 张字吃得到,
            // 是条空转规则。基础值直接写成实战值,配置值 = 实战值。
            // 木生火,但这里不该再有任何倍率
            Assert.That(WuxingResolver.ResolveEffect(100, Element.Fire, Element.Heart),
                Is.EqualTo(100));
        }

        [Test]
        public void ResolveEffect_StillAppliesKe()
        {
            // 相克保留:火克金 x1.5,火被水克 x0.5
            Assert.That(WuxingResolver.ResolveEffect(100, Element.Fire, Element.Metal),
                Is.EqualTo(150));
            Assert.That(WuxingResolver.ResolveEffect(100, Element.Fire, Element.Water),
                Is.EqualTo(50));
        }
```

再新建一条钉住"等值改写"的测试(放在 `Brushblade/Assets/_Project/Tests/` 下,读真实 `chars.json`):

```csharp
        [Test]
        public void ShengRemoval_PreservesCombatValuesOfTheFourAffectedChars()
        {
            // 取消相生前这 4 张字靠 x3 达到的实战值,取消后由基础值直接表达。
            // 沏 是例外:它在水系重配范围内,由 DualDirectionTests 覆盖。
            var graph = LoadRealGraph();
            Assert.That(graph.Get("焚").Effects.First(e => e.Kind == EffectKind.DamageAll).Value,
                Is.EqualTo(120), "原 40 x3");
            Assert.That(graph.Get("蒸").Effects.First(e => e.Kind == EffectKind.DamageSingle).Value,
                Is.EqualTo(135), "原 45 x3");
            Assert.That(graph.Get("刲").Effects.First(e => e.Kind == EffectKind.DamageSingle).Value,
                Is.EqualTo(450), "原 150 x3");
        }
```

`LoadRealGraph()` 用 `TestContext.CurrentContext.TestDirectory` 往上找仓库根,**禁用 `AppContext.BaseDirectory`**。照既有读真实字表的测试(搜 `chars.json` 找)抄那段路径解析。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 编译失败或断言失败

- [ ] **Step 3: 实现 —— WuxingResolver**

把 `WuxingResolver.cs` 改成:

```csharp
using System;

namespace Brushblade.Core
{
    /// <summary>五行相克结算。规则唯一来源:docs/design/wuxing-reference.md。
    ///
    /// ⚠ **相生 ×3 已于 2026-09-02 取消**(用户拍板)。全表 74 张字里只有 4 张吃得到
    /// (焚/蒸/沏/刲,土 0/13、木 0/14),是条空转规则:最大的乘区对两个系完全不可达。
    /// 那 4 张字的基础值已直接写成 ×3 后的实战值,战斗结果一分不差。
    /// 收益是配置值 = 实战值 —— 读字表不用再心算。</summary>
    public static class WuxingResolver
    {
        // 相克环:木克土,土克水,水克火,火克金,金克木(心不在环内)
        private static readonly System.Collections.Generic.Dictionary<Element, Element> Ke = new()
        {
            { Element.Wood, Element.Earth },
            { Element.Earth, Element.Water },
            { Element.Water, Element.Fire },
            { Element.Fire, Element.Metal },
            { Element.Metal, Element.Wood },
        };

        /// <summary>相克倍率:克制 1.5,被克 0.5,其余(含心)1.0。</summary>
        public static float KeMultiplier(Element attacker, Element defender)
        {
            if (Ke.TryGetValue(attacker, out var victim) && victim == defender)
                return 1.5f;
            if (Ke.TryGetValue(defender, out var counter) && counter == attacker)
                return 0.5f;
            return 1.0f;
        }

        /// <summary>效果结算:floor(基础值 × 相克)。</summary>
        public static int ResolveEffect(int baseValue, Element attacker, Element defender) =>
            (int)Math.Floor(baseValue * KeMultiplier(attacker, defender));

        /// <summary>无对抗目标的效果结算(护盾/治疗等)。相生取消后这是恒等函数,
        /// 保留它只为让调用点读起来仍然「过了一道五行结算」,将来要加别的规则有落点。</summary>
        public static int ResolveEffect(int baseValue) => baseValue;
    }
}
```

- [ ] **Step 4: 实现 —— 更新全部调用点**

Run: `grep -n "ResolveEffect\|ShengMultiplier\|RecipeElements" Brushblade/Assets/_Project/Core/BattleEngine.cs`

逐个改:
- `ResolveEffect(value, recipeElements, attacker, defender)` → `ResolveEffect(value, attacker, defender)`
- `ResolveEffect(value, recipeElements, attacker)` → `ResolveEffect(value)`
- **`SpendResource`(Task 4 加的)的签名要跟着去掉 `recipeElements` 参数**,改成:
  ```csharp
  private void SpendResource(StatusKind kind, int perStack, Element attacker)
  ```
  内部的 `DamageEnemy(i, damage, recipeElements, attacker, crit: RollCrit())` 也跟着去掉那个参数 —— 照 `case EffectKind.DamageAll:` 改完后的调用形态抄。两个 `case` 的调用点同步改成 `SpendResource(StatusKind.Momentum, value, attacker)`。
- `ApplyEffects` 开头的 `var recipeElements = _graph.RecipeElements(def.Id);` —— 若改完后**再无任何使用者**就删掉它。**别留下没人读的参数或局部变量**(那是本次改动自己造出来的孤儿,按项目规范要清掉)。

⚠ 编译器会把漏改的地方全部报出来,跟着 `error CS` 一个个修即可。

也要检查 `RecipeGraph.RecipeElements` 是否还有别的调用者:
Run: `grep -rn "RecipeElements" Brushblade/Assets/_Project/ tools/`
若只剩定义没有调用,**保留定义不删** —— 它是 `RecipeGraph` 的公开 API,不是本次改动造出的孤儿。

- [ ] **Step 5: 实现 —— 字表三张字**

改 `Brushblade/Assets/StreamingAssets/config/chars.json`:

| 字 | 找到这一项 | 改成 |
|---|---|---|
| 焚 | `"kind": "DamageAll", "value": 40` | `"value": 120` |
| 蒸 | `"kind": "DamageSingle", "value": 45` | `"value": 135` |
| 刲 | `"kind": "DamageSingle", "value": 150` | `"value": 450` |

⚠ `沏` **这一步不动**(基础值 70)—— 它归 Task 10 按水系锚点配成 180。

- [ ] **Step 6: 更新文档**

`docs/design/wuxing-reference.md`:删掉相生环与 ×3 规则的全部内容,在文首加一条变更说明:

```markdown
> **v0.8 变更(2026-09-02)**:**相生 ×3 已取消**。全表 74 张字里只有 4 张吃得到
> (焚/蒸/沏/刲,土 0/13、木 0/14),最大的乘区对两个系完全不可达。那 4 张字的
> 基础值已直接写成 ×3 后的实战值,战斗结果一分不差。本文档现在只描述相克。
```

`CLAUDE.md`:把「五行生克(相克 ×1.5 / 相生 ×3 去重不叠乘 / 心中立)」改成「五行相克(相克 ×1.5 / 被克 ×0.5 / 心中立;**相生 ×3 已于 2026-09-02 取消**)」。

- [ ] **Step 7: 跑测试确认通过**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`

Expected: 全绿。

⚠ **这一步很可能有既有断言变红**,因为它们写死了相生后的值。逐条判断:
- 断言的是**实战结果**(如"焚打 120")→ 应当仍然绿。红了说明等值改写算错了,查数。
- 断言的是**中间量**(如 `ShengMultiplier(...) == 3`)→ 删掉,规则没了。
- 断言的是**基础值**(如 `graph.Get("焚").Effects[0].Value == 40`)→ 改成新的 120,并在断言上写一句注释说明这是相生取消后的等值改写。

- [ ] **Step 8: 提交**

```bash
git add Brushblade/Assets/_Project/Core/WuxingResolver.cs Brushblade/Assets/_Project/Core/BattleEngine.cs Brushblade/Assets/StreamingAssets/config/chars.json Brushblade/Assets/_Project/Tests/ docs/design/wuxing-reference.md CLAUDE.md
git commit -m "$(cat <<'EOF'
refactor(core): 取消相生 x3,保留相克

全表 74 张字只有 4 张吃得到相生(土 0/13、木 0/14)——最大的乘区对两个系
完全不可达,是条空转规则。焚/蒸/刲 的基础值直接写成 x3 后的实战值
(40→120 / 45→135 / 150→450),战斗结果一分不差;沏 归水系重配。

收益:少一条规则、少一个乘区、配置值 = 实战值。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: 方向选择器(可与 Task 1–6 并行)

**Files:**
- Modify: `Brushblade/Assets/_Project/Presentation/BattleView.cs`
- Modify: `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`

**Interfaces:**
- Consumes: `CharDef.AttackEffects`(已存在)、`BattleEngine.NeedsTarget(def, attackMode)`、`NeedsAllyTarget(def, attackMode)`(已存在)
- Produces: `BattleView._directionPicking`(私有状态)

⚠ **Presentation 没有自动化测试**,验收靠离线编译 + 人工确认。改完**必跑** prescompile。

- [ ] **Step 1: 加字符串表两条**

在 `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json` 里,`battle.btn.cast` 附近加:

```json
  "battle.btn.direction_attack": "攻",
  "battle.btn.direction_support": "护",
  "battle.hint.pick_direction": "「{charId}」:打人还是护己?",
```

⚠ 动作按钮的文案**一律单字**(2026-08-21 用户拍板:竖栏里按钮排一行,长标签会把整行挤换行)。所以是「攻」/「护」而不是「攻击」/「治疗」。

⚠ 「攻」「护」两个字若不在字体子集里,真机上会是豆腐块 —— Task 14 会统一重跑子集。

- [ ] **Step 2: 加状态字段**

在 `BattleView.cs` 的 `_allyTargeting` 字段旁(约 `:99`)加:

```csharp
        // 方向选择态(2026-09-02):水/土 双方向字选中后先问「攻」还是「护」,
        // 选完再进各自的目标态。与 _targeting / _allyTargeting 同构,
        // 只是这一态还没决定要打谁,只决定走哪套效果。
        private bool _directionPicking;
```

- [ ] **Step 3: 在 OnCastPressed 前面拦一道**

把 `OnCastPressed(CharDef def)`(约 `:3748`)改成先判方向:

```csharp
        private void OnCastPressed(CharDef def)
        {
            // 双方向字(2026-09-02):有 AttackEffects 就先问方向。
            // AttackEffects 为空的字(火/金/木全部)一路直下,行为与改造前完全一致。
            if (def.AttackEffects.Count > 0 && !_directionPicking)
            {
                _directionPicking = true;
                _message = Strings.T("battle.hint.pick_direction", ("charId", def.Id));
                Refresh();
                return;
            }
            _directionPicking = false;
            CastInDirection(def, attackMode: false);
        }

        /// <summary>选定方向后的出字流程(2026-09-02)。<paramref name="attackMode"/>
        /// true = 攻击面(走 AttackEffects),false = 治疗/加盾面(走 Effects)。
        ///
        /// 这就是原来 OnCastPressed 的整个函数体,只是把写死的 attackMode: false
        /// 变成参数 —— 免选判据、友方目标判据、最终 BeginCast 三处全部跟着这个参数走,
        /// 否则会出现「选了攻击方向,却按治疗面判断要不要选目标」这种错位。</summary>
        private void CastInDirection(CharDef def, bool attackMode)
        {
            if (BattleEngine.NeedsTarget(def, attackMode) && LegalTargetCount(def, attackMode) > 1)
            {
                _targeting = true;
                _pendingAttackMode = attackMode;
                _message = Strings.T("battle.hint.cast_pick_enemy_target", ("charId", def.Id));
                Refresh();
                return;
            }
            if (BattleEngine.NeedsAllyTarget(def, attackMode) && Battle.AliveSummonCount > 0)
            {
                _pendingAttackMode = attackMode;
                EnterAllyTargeting(def, enemyTarget: -1);
                Refresh();
                return;
            }
            BeginCast(def.Id, -1, attackMode: attackMode, libraryIndex: _selectedIndex);
        }
```

新增一个字段记住待定的方向(放在 `_directionPicking` 旁):

```csharp
        // 进目标选择态时记住选的是哪个方向(2026-09-02):点敌人/点友方那两条回调
        // 隔了一次用户交互才回来,不带着这个值会退回 attackMode: false。
        private bool _pendingAttackMode;
```

- [ ] **Step 4: 让点目标那两条回调用上待定方向**

Run: `grep -n "attackMode: false" Brushblade/Assets/_Project/Presentation/BattleView.cs`

把**点敌人**那条(约 `:3920`,`BeginCast(_selectedChar, index, attackMode: false, ...)`)和**点友方**那条(约 `:1860`)改成传 `_pendingAttackMode`。

⚠ 拖放路径(`AttachDragToAttack` 里的 `attackMode: true`)**一个字都不要改** —— 拖到敌人本来就等于选了攻击方向。

`CancelSelection()` 里加两行复位:

```csharp
            _directionPicking = false;
            _pendingAttackMode = false;
```

- [ ] **Step 5: 画方向按钮**

在 `DrawActions` 里,`if (_allyTargeting) { ... return; }` 之后、画「出」按钮之前插入:

```csharp
            // 方向选择(2026-09-02):水/土 双方向字选中后画「攻」「护」两个钮,
            // 点完各自进对应的目标态。与下面那排动作钮同尺寸,免得手指点位跳。
            if (_directionPicking)
            {
                Ui.RoundButton(_actionRow, Strings.T("battle.btn.direction_attack"),
                    () => CastInDirection(def, attackMode: true),
                    Theme.Cinnabar, Color.white, 17, new Vector2(76, 52));
                Ui.RoundButton(_actionRow, Strings.T("battle.btn.direction_support"),
                    () => CastInDirection(def, attackMode: false),
                    Theme.ElementColor(def.Element), Color.white, 17, new Vector2(76, 52));
                return;
            }
```

⚠ `Ui.RoundButton` 的确切签名照同文件里既有那句「出」按钮抄(`Ui.RoundButton(_actionRow, Strings.T("battle.btn.cast"), () => OnCastPressed(def), Theme.Cinnabar, Color.white, 17, new Vector2(76, 52));`),别自己拼参数。

- [ ] **Step 6: 离线编译**

Run:
```bash
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
```
Expected: 零 `error CS`

- [ ] **Step 7: 跑字符串表检查**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`

Expected: 全绿。⚠ 字符串表有孤儿 key 检查,而**它只认字面量** —— 上面三条 key 都是直接写在 `Strings.T("…")` 里的字面量,能被扫到。

- [ ] **Step 8: 提交**

```bash
git add Brushblade/Assets/_Project/Presentation/BattleView.cs Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json
git commit -m "$(cat <<'EOF'
feat(ui): 双方向字的方向选择器

选中有 AttackEffects 的字后先画「攻」「护」两个钮,选完再进各自的目标态。
把 OnCastPressed 的函数体抽成 CastInDirection(def, attackMode),免选判据、
友方目标判据、BeginCast 三处全跟着方向参数走。

AttackEffects 为空的字(火/金/木全部)一路直下,行为与改造前完全一致;
拖放路径一个字不改 —— 拖到敌人本来就等于选了攻击方向。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## 步 2 · 配值

### Task 8: EnemyDef.MinDepth 与深度闸

**Files:**
- Modify: `Brushblade/Assets/_Project/Core/EnemyDef.cs`
- Modify: `Brushblade/Assets/_Project/Core/Campaign.cs`
- Modify: `Brushblade/Assets/_Project/Core/Endless.cs`
- Modify: `Brushblade/Assets/_Project/Data/ConfigLoader.cs`
- Test: `Brushblade/Assets/_Project/Tests/ArmoredEnemyTests.cs`(新建)

**Interfaces:**
- Consumes: 无
- Produces: `EnemyDef.MinDepth` → `int`;`Endless.WithinDepth(pool, depth)`

- [ ] **Step 1: 写失败测试**

```csharp
        [Test]
        public void MinDepth_FiltersEnemiesOutOfEarlyFloors()
        {
            var config = LoadRealEndlessConfig();
            // 1-5 层不出任何带甲怪
            for (int depth = 1; depth <= 5; depth++)
            {
                var segment = Endless.BuildSegment(config, depth, seed: 12345);
                foreach (var floor in segment.Encounters)
                    foreach (var enemy in floor)
                        Assert.That(enemy.Defense, Is.EqualTo(0),
                            $"第 {depth} 层不该有带甲怪:{enemy.Id}");
            }
        }

        [Test]
        public void MinDepth_IsPreservedThroughScale()
        {
            // Scale 会重建 EnemyDef —— 漏传 MinDepth 不会报错,只会静默失效
            var def = new EnemyDef("测", Element.Earth, 100, 10, minDepth: 6);
            var scaled = CampaignConfig.Scale(def, 2.0f);
            Assert.That(scaled.MinDepth, Is.EqualTo(6));
        }

        [Test]
        public void FirstTowerSegment_StillUsesTheThreeTutorialEnemies()
        {
            // 首塔前 3 层取 Bands[0].EnemyPool 的下标 0/1/2。
            // 新怪必须追加在池尾,否则会挤掉引导用的那三只。
            var config = LoadRealEndlessConfig();
            var segment = Endless.BuildFirstTowerSegment(config, seed: 1);
            Assert.That(segment.Encounters[0][0].Id, Is.EqualTo("错字鬼"));
            Assert.That(segment.Encounters[0][0].Defense, Is.EqualTo(0));
        }
```

⚠ `Endless.BuildSegment` 与 `LoadRealEndlessConfig` 的确切签名去既有测试里找(`grep -rn "BuildSegment" Brushblade/Assets/_Project/Tests/`),照抄。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 编译失败,`EnemyDef` 没有 `minDepth` 参数

- [ ] **Step 3: 实现 —— EnemyDef**

在 `EnemyDef` 类里加属性(与 `Defense` 并排):

```csharp
        /// <summary>最早出现层(2026-09-02);0 = 不限。
        ///
        /// 低阶护甲怪配 6:前 5 层是新手教学区,带甲怪会让还没有破甲手段的玩家直接卡死,
        /// 而层段(band)整段共用一个 enemyPool,表达不了段内的深度差。</summary>
        public int MinDepth { get; }
```

构造函数追加参数(**放在最后,给默认值 0**,这样既有调用零改动):

```csharp
            AttackFocus focus = AttackFocus.Default, int columnSpan = 1, int minDepth = 0)
```

并赋值:`MinDepth = minDepth;`

- [ ] **Step 4: 实现 —— Campaign.Scale 传递**

⚠ **这一步漏了不会报错,只会静默失效。** 在 `Campaign.cs:90` 的 `return new EnemyDef(...)` 末尾补上 `enemy.MinDepth`:

```csharp
            return new EnemyDef(enemy.Id, enemy.Element,
                Scaled(enemy.MaxHp, scale), Scaled(enemy.Attack, scale),
                enemy.Ability, phases, ScaledDefense(enemy.Defense, scale), enemy.Speed,
                enemy.Row, enemy.Range, enemy.Focus, enemy.ColumnSpan, enemy.MinDepth);
```

- [ ] **Step 5: 实现 —— Endless 深度闸**

在 `Endless.cs` 的 `WithoutArmor` 方法旁加:

```csharp
        /// <summary>摘掉还没到出场层的候选(2026-09-02);摘完为空时原样返回。
        /// 与 <see cref="WithoutArmor"/> 同型 —— 抽不出来比放一只早到的更糟。</summary>
        private static IReadOnlyList<EnemyDef> WithinDepth(IReadOnlyList<EnemyDef> pool, int depth)
        {
            var ok = new List<EnemyDef>(pool.Count);
            foreach (var enemy in pool)
                if (depth >= enemy.MinDepth) ok.Add(enemy);
            return ok.Count > 0 ? ok : pool;
        }
```

在 `BuildSegment` 的编成循环里,**在 `nonSupport` / `frontOpeners` 计算之前**先收窄整个池子:

```csharp
            // 深度闸排在最前(2026-09-02):先按层数收窄候选,再走既有的
            // 「首位前排 / 辅助不单独成场 / 带甲去重」三道闸。顺序反过来的话
            // frontOpeners 与 nonSupport 会是按未过滤的池子算出来的。
            var depthPool = WithinDepth(band.EnemyPool, depth);
```

然后把该方法里后续所有用 `band.EnemyPool` 的地方改成 `depthPool`(至少三处:`nonSupport` 的 foreach、`else pool = band.EnemyPool;`、以及任何别的引用)。

Run: `grep -n "band.EnemyPool" Brushblade/Assets/_Project/Core/Endless.cs` 逐个核对。

- [ ] **Step 6: 实现 —— ConfigLoader**

在 `EnemyDto` 里加:

```csharp
            public int MinDepth { get; set; }  // 最早出现层(2026-09-02);缺省 0 = 不限
```

在构造 `EnemyDef` 的那处把 `dto.MinDepth` 传进去。Run: `grep -n "new EnemyDef" Brushblade/Assets/_Project/Data/ConfigLoader.cs` 找到位置。

- [ ] **Step 7: 跑测试确认通过**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 全绿(第一条测试此时会通过,因为还没有任何低阶护甲怪进字林池)

- [ ] **Step 8: 提交**

```bash
git add Brushblade/Assets/_Project/Core/EnemyDef.cs Brushblade/Assets/_Project/Core/Campaign.cs Brushblade/Assets/_Project/Core/Endless.cs Brushblade/Assets/_Project/Data/ConfigLoader.cs Brushblade/Assets/_Project/Tests/ArmoredEnemyTests.cs
git commit -m "$(cat <<'EOF'
feat(core): EnemyDef.MinDepth 与 Endless 深度闸

层段整段共用一个 enemyPool,表达不了段内深度差(要"字林 1-10 里 6-10 才出带甲怪")。
新增 MinDepth 字段 + WithinDepth 闸,与既有 WithoutArmor 同型。
闸排在最前:顺序反了会让 frontOpeners/nonSupport 按未过滤的池子算。

CampaignConfig.Scale 一并传递 MinDepth —— 漏传不报错、只静默失效。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: 护甲怪 10 只

**Files:**
- Modify: `Brushblade/Assets/StreamingAssets/config/enemies.json`
- Test: `Brushblade/Assets/_Project/Tests/ArmoredEnemyTests.cs`

**Interfaces:**
- Consumes: `EnemyDef.MinDepth`(Task 8)
- Produces: 7 只新怪(枯笔/版牍/火漆/窑变/砚台/铜钤/宿墨)

- [ ] **Step 1: 写失败测试**

```csharp
        [Test]
        public void ArmoredEnemies_CoverAllFiveElementsAtBothTiers()
        {
            var campaign = LoadRealCampaign();
            var armored = campaign.Enemies.Where(e => e.Defense > 0).ToList();
            foreach (var element in new[] { Element.Wood, Element.Fire, Element.Earth,
                                            Element.Metal, Element.Water })
            {
                var low = armored.Where(e => e.Element == element && e.Defense < 30).ToList();
                var high = armored.Where(e => e.Element == element && e.Defense >= 30).ToList();
                Assert.That(low.Count, Is.GreaterThanOrEqualTo(1), $"{element} 缺低阶护甲怪");
                Assert.That(high.Count, Is.GreaterThanOrEqualTo(1), $"{element} 缺高阶护甲怪");
            }
        }

        [Test]
        public void LowTierArmoredEnemies_HaveMinDepthSix()
        {
            var campaign = LoadRealCampaign();
            foreach (var id in new[] { "枯笔", "火漆", "砚台", "铜钤", "墨渍" })
            {
                var enemy = campaign.Enemies.First(e => e.Id == id);
                Assert.That(enemy.MinDepth, Is.GreaterThanOrEqualTo(6),
                    $"{id} 是低阶护甲怪,不该在 1-5 层出现");
            }
        }

        [Test]
        public void EveryFloor_HasAtMostOneArmoredEnemy()
        {
            // WithoutArmor 闸(既有)在补齐 10 只后仍须成立 ——
            // 点数护甲对 AOE 有 N 倍惩罚,带甲成群会把 AOE 流派打废。
            var config = LoadRealEndlessConfig();
            for (int depth = 1; depth <= 60; depth++)
            {
                var segment = Endless.BuildSegment(config, depth, seed: depth * 7919);
                foreach (var floor in segment.Encounters)
                    Assert.That(floor.Count(e => e.Defense > 0), Is.LessThanOrEqualTo(1),
                        $"第 {depth} 层出现了多只带甲怪");
            }
        }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: `ArmoredEnemies_CoverAllFiveElementsAtBothTiers` 失败(木/火缺护甲怪)

- [ ] **Step 3: 加 7 只新怪**

在 `enemies.json` 的 `enemies` 数组**末尾**追加(⚠ 必须追加在末尾:首塔前 3 层取 `Bands[0].EnemyPool` 的下标 0/1/2):

```json
    { "id": "枯笔", "element": "Wood",  "maxHp": 150, "attack": 30, "defense": 18, "minDepth": 6 },
    { "id": "版牍", "element": "Wood",  "maxHp": 260, "attack": 40, "defense": 45 },
    { "id": "火漆", "element": "Fire",  "maxHp": 140, "attack": 35, "defense": 18, "minDepth": 6 },
    { "id": "窑变", "element": "Fire",  "maxHp": 220, "attack": 50, "defense": 42 },
    { "id": "砚台", "element": "Earth", "maxHp": 200, "attack": 22, "defense": 22, "minDepth": 6 },
    { "id": "铜钤", "element": "Metal", "maxHp": 130, "attack": 40, "defense": 20, "minDepth": 6 },
    { "id": "宿墨", "element": "Water", "maxHp": 240, "attack": 38, "defense": 45 }
```

- [ ] **Step 4: 改现有两只**

- `铁画` 的 `defense` 从 `25` 改成 `40`(高阶位要配得上)
- `墨渍` 加 `"minDepth": 6`

- [ ] **Step 5: 加进层段池**

在 `endless.bands` 里:

- **字林**(`fromDepth: 1`)的 `enemyPool` **末尾**追加 5 只低阶:`"枯笔", "火漆", "砚台", "铜钤", "墨渍"`
- **词渊**(11)的 `enemyPool` 末尾追加:`"枯笔", "火漆", "砚台", "铜钤"`(墨渍已在)
- **文山**(26)与**墨海**(51)的 `enemyPool` 末尾追加全部 9 只(5 低阶 + 4 新高阶:`"版牍", "窑变", "宿墨"`;镇纸/铁画已在)

- [ ] **Step 6: 跑测试确认通过**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 全绿。

⚠ 若 `EveryFloor_HasAtMostOneArmoredEnemy` 变红,说明 `WithoutArmor` 闸在带甲密度提高后不够用了 —— **不要放宽这条断言**,去查 `BuildSegment` 里闸的位置对不对(它必须在每一位次的候选池上都生效)。

- [ ] **Step 7: 提交**

```bash
git add Brushblade/Assets/StreamingAssets/config/enemies.json Brushblade/Assets/_Project/Tests/ArmoredEnemyTests.cs
git commit -m "$(cat <<'EOF'
feat(content): 五行护甲怪补齐到 5 系 x 低/高阶

新增 7 只(枯笔/版牍/火漆/窑变/砚台/铜钤/宿墨),复用墨渍/镇纸/铁画;
铁画 DEF 25→40 配得上高阶位。低阶 5 只配 minDepth 6 —— 1-5 层是教学区。
全部追加在 enemies 数组与 enemyPool 末尾,不挤掉首塔引导用的那三只。

破甲/穿透那 6 张字此前只对 3/21 只敌人有意义,补齐后该轴才算活。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 10: 水系 15 张双方向

**Files:**
- Modify: `Brushblade/Assets/StreamingAssets/config/chars.json`
- Test: `Brushblade/Assets/_Project/Tests/DualDirectionTests.cs`(新建)

**Interfaces:**
- Consumes: `EffectKind.SpendWaterPower`(Task 4)
- Produces: 15 张水系字的 `attackEffects`

- [ ] **Step 1: 写失败测试**

```csharp
        private static readonly string[] WaterChars =
        {
            "溃", "冻", "海", "冷", "浴", "湮", "沏", "澡",
            "沝", "冰", "沐", "淼", "淡", "淋", "㵘",
        };

        [Test]
        public void EveryWaterChar_HasBothDirections()
        {
            var graph = LoadRealGraph();
            foreach (var id in WaterChars)
            {
                var def = graph.Get(id);
                Assert.That(def.Effects.Count, Is.GreaterThan(0), $"{id} 缺治疗面");
                Assert.That(def.AttackEffects.Count, Is.GreaterThan(0), $"{id} 缺攻击面");
            }
        }

        [Test]
        public void EveryWaterChar_HasHealOnSupportSide()
        {
            var graph = LoadRealGraph();
            var healKinds = new[] { EffectKind.HealSelf, EffectKind.HealAll, EffectKind.HealOverTime };
            foreach (var id in WaterChars)
            {
                var def = graph.Get(id);
                bool heals = def.Effects.Any(e => healKinds.Contains(e.Kind));
                Assert.That(heals, Is.True, $"{id} 的治疗面没有治疗效果");
            }
        }

        [Test]
        public void WaterCharValues_MatchRarityAnchors()
        {
            // 锚点表(spec §4.1):带附加特性的面 x0.7,纯效果取满值。
            var graph = LoadRealGraph();
            Assert.That(HealValueOf(graph, "沝"), Is.EqualTo(340), "金档满值");
            Assert.That(HealValueOf(graph, "㵘"), Is.EqualTo(540), "红档满值");
            Assert.That(HealValueOf(graph, "浴"), Is.EqualTo(77), "蓝档 110 x0.7(带净化)");
            Assert.That(HealValueOf(graph, "沏"), Is.EqualTo(180), "紫档 150 x1.2(相生取消后的补偿)");
        }
```

`HealValueOf` 是取该字治疗面第一条治疗效果 `Value` 的辅助方法。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: `EveryWaterChar_HasBothDirections` 失败(`attackEffects` 全表 0 处配置)

- [ ] **Step 3: 改字表**

⚠ **口径**:`effects` = **治疗面**(双击选「护」),`attackEffects` = **攻击面**(双击选「攻」或拖到敌人)。

逐字改 `chars.json`(照抄下表,一个数都别自己发挥):

```json
{ "id": "溃", "rarity": "White", "element": "Water", "recipe": ["氵", "贵"],
  "effects": [{ "kind": "HealSelf", "value": 45 }],
  "attackEffects": [
    { "kind": "DamageSingle", "value": 32, "shape": "Chain", "shots": 3, "shapePercent": 50 },
    { "kind": "ArmorBreak", "value": 10 }] }

{ "id": "冻", "rarity": "White", "element": "Water", "recipe": ["冫", "东"],
  "effects": [{ "kind": "HealSelf", "value": 45 }],
  "attackEffects": [
    { "kind": "Freeze", "value": 1 },
    { "kind": "DamageSingle", "value": 32 }] }

{ "id": "海", "rarity": "Green", "element": "Water", "recipe": ["氵", "每"],
  "effects": [{ "kind": "HealSelf", "value": 70 }],
  "attackEffects": [{ "kind": "DamageAll", "value": 50 }] }

{ "id": "冷", "rarity": "Blue", "element": "Water", "recipe": ["冫", "令"],
  "effects": [{ "kind": "HealSelf", "value": 110 }],
  "attackEffects": [
    { "kind": "Slow", "value": 2 },
    { "kind": "DamageSingle", "value": 77 }] }

{ "id": "浴", "rarity": "Blue", "element": "Water", "recipe": ["氵", "谷"],
  "effects": [
    { "kind": "HealSelf", "value": 77 },
    { "kind": "Cleanse", "value": 0 }],
  "attackEffects": [
    { "kind": "Revive", "value": 1 },
    { "kind": "DamageSingle", "value": 77 }] }

{ "id": "湮", "rarity": "Purple", "element": "Water", "recipe": ["氵", "垔"],
  "effects": [{ "kind": "HealSelf", "value": 150 }],
  "attackEffects": [
    { "kind": "DamageSingle", "value": 105 },
    { "kind": "Dispel", "value": -1 }] }

{ "id": "沏", "rarity": "Purple", "element": "Water", "recipe": ["氵", "切"],
  "effects": [{ "kind": "HealSelf", "value": 180 }],
  "attackEffects": [{ "kind": "DamageSingle", "value": 180 }] }

{ "id": "澡", "rarity": "Purple", "element": "Water", "recipe": ["氵", "喿"],
  "effects": [
    { "kind": "HealSelf", "value": 105 },
    { "kind": "Cleanse", "value": 0 }],
  "attackEffects": [
    { "kind": "DamageSingle", "value": 105 },
    { "kind": "Cleanse", "value": 0 }] }

{ "id": "沝", "rarity": "Gold", "element": "Water", "recipe": ["水", "水"],
  "effects": [{ "kind": "HealSelf", "value": 340 }],
  "attackEffects": [{ "kind": "DamageSingle", "value": 340 }] }

{ "id": "冰", "rarity": "Gold", "element": "Water", "recipe": ["冫", "水"],
  "effects": [{ "kind": "HealSelf", "value": 340 }],
  "attackEffects": [
    { "kind": "DamageSingle", "value": 238, "doubleVs": "Controlled" },
    { "kind": "Freeze", "value": 1 }] }

{ "id": "沐", "rarity": "Gold", "element": "Water", "recipe": ["氵", "木"],
  "effects": [{ "kind": "HealOverTime", "value": 113, "turns": 3 }],
  "attackEffects": [{ "kind": "DamageSingle", "value": 340 }] }

{ "id": "淼", "rarity": "Orange", "element": "Water", "recipe": ["水", "沝"],
  "effects": [
    { "kind": "HealSelf", "value": 430 },
    { "kind": "SpendWaterPower", "value": 50 }],
  "attackEffects": [{ "kind": "DamageAll", "value": 190 }] }

{ "id": "淡", "rarity": "Orange", "element": "Water", "recipe": ["氵", "炎"],
  "effects": [{ "kind": "HealSelf", "value": 430 }],
  "attackEffects": [
    { "kind": "DamageAll", "value": 133 },
    { "kind": "Dispel", "value": 1, "targetAll": true }] }

{ "id": "淋", "rarity": "Orange", "element": "Water", "recipe": ["氵", "林"],
  "effects": [{ "kind": "HealAll", "value": 430 }],
  "attackEffects": [{ "kind": "DamageAll", "value": 190 }] }

{ "id": "㵘", "rarity": "Red", "element": "Water", "recipe": ["水", "淼"],
  "effects": [
    { "kind": "HealSelf", "value": 540 },
    { "kind": "SpendWaterPower", "value": 80 }],
  "attackEffects": [{ "kind": "DamageAll", "value": 250 }] }
```

⚠ 保留每个条目里原有的 `pinyin` / `gloss` 等字段,别删。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`

Expected: 新测试全绿。

⚠ **既有断言会变红**,这是步 2 的预期。逐条改:
- `MomentumTests.Cast_HealChar_GainsWaterPower` 用了 沝 的旧值 160 → 改成 340(阈值 50 → 6 层 + 余 40)
- 任何写死水系字数值的测试 → 按新表改,并在断言旁注明"2026-09-02 双方向重配"

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/StreamingAssets/config/chars.json Brushblade/Assets/_Project/Tests/
git commit -m "$(cat <<'EOF'
feat(content): 水系 15 张改纯二选一双方向

effects = 治疗面,attackEffects = 攻击面,两面互斥。数值按稀有度锚点表配
(带附加特性 x0.7,纯效果满值),护盾/治疗与单体伤害 1:1。
淼/㵘 的治疗面挂「泻」(SpendWaterPower),攒与泄落在同一张字上。

沏 定 180(紫档 150 x1.2)——相生取消后它保留一点独特性的方式。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 11: 土系 13 张双方向

**Files:**
- Modify: `Brushblade/Assets/StreamingAssets/config/chars.json`
- Test: `Brushblade/Assets/_Project/Tests/DualDirectionTests.cs`
- Test: `Brushblade/Assets/_Project/Tests/MomentumTests.cs`(取消 Task 4 的 Ignore)

**Interfaces:**
- Consumes: `EffectKind.SpendMomentum`(Task 4)
- Produces: 13 张土系字的 `attackEffects`

- [ ] **Step 1: 写失败测试**

```csharp
        private static readonly string[] EarthChars =
        {
            "碉", "砸", "碾", "垒", "壁", "崩", "堡", "碎", "塔", "圭", "杜", "垚", "㙓",
        };

        [Test]
        public void EveryEarthChar_HasBothDirections()
        {
            var graph = LoadRealGraph();
            foreach (var id in EarthChars)
            {
                var def = graph.Get(id);
                Assert.That(def.Effects.Count, Is.GreaterThan(0), $"{id} 缺加盾面");
                Assert.That(def.AttackEffects.Count, Is.GreaterThan(0), $"{id} 缺攻击面");
            }
        }

        [Test]
        public void EveryEarthChar_HasShieldOnSupportSide()
        {
            var graph = LoadRealGraph();
            foreach (var id in EarthChars)
            {
                bool shields = graph.Get(id).Effects.Any(e => e.Kind == EffectKind.Shield);
                Assert.That(shields, Is.True, $"{id} 的加盾面没有护盾效果");
            }
        }

        [Test]
        public void EarthCharValues_MatchRarityAnchors()
        {
            var graph = LoadRealGraph();
            Assert.That(ShieldValueOf(graph, "圭"), Is.EqualTo(340), "金档满值");
            Assert.That(ShieldValueOf(graph, "㙓"), Is.EqualTo(540), "红档满值");
            Assert.That(ShieldValueOf(graph, "杜"), Is.EqualTo(238), "金档 340 x0.7(带免疫)");
        }

        [Test]
        public void MomentumDetonators_AreOnGreenAndRed()
        {
            // 引爆每系两张载体(中档 + 红档):只挂红档五系四叠字的话,
            // 大部分玩家一局都摸不到这个机制。
            var graph = LoadRealGraph();
            Assert.That(graph.Get("崩").AttackEffects.Any(e => e.Kind == EffectKind.SpendMomentum),
                Is.True, "崩(绿)是前期就能拿到的引爆载体");
            Assert.That(graph.Get("㙓").AttackEffects.Any(e => e.Kind == EffectKind.SpendMomentum),
                Is.True);
        }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: `EveryEarthChar_HasBothDirections` 失败

- [ ] **Step 3: 改字表**

```json
{ "id": "碉", "rarity": "White", "element": "Earth", "recipe": ["石", "周"],
  "effects": [{ "kind": "Shield", "value": 45 }],
  "attackEffects": [{ "kind": "Summon", "value": 120, "count": 1, "attack": 0,
                      "summonChar": "碉", "passive": { "thorns": 50 } }] }

{ "id": "砸", "rarity": "White", "element": "Earth", "recipe": ["石", "匝"],
  "effects": [{ "kind": "Shield", "value": 45 }],
  "attackEffects": [{ "kind": "DamageSingle", "value": 45,
                      "shape": "Cleave", "shapePercent": 50 }] }

{ "id": "碾", "rarity": "White", "element": "Earth", "recipe": ["石", "展"],
  "effects": [{ "kind": "Shield", "value": 45 }],
  "attackEffects": [{ "kind": "DamageSingle", "value": 45,
                      "shape": "Sweep", "shapePercent": 60 }] }

{ "id": "垒", "rarity": "Green", "element": "Earth", "recipe": ["厽", "土"],
  "effects": [{ "kind": "Shield", "value": 70 }],
  "attackEffects": [{ "kind": "DamageSingle", "value": 70 }] }

{ "id": "壁", "rarity": "Green", "element": "Earth", "recipe": ["辟", "土"],
  "effects": [
    { "kind": "Shield", "value": 49 },
    { "kind": "Reflect", "value": 30, "turns": 2 }],
  "attackEffects": [
    { "kind": "DamageSingle", "value": 49 },
    { "kind": "Reflect", "value": 30, "turns": 2 }] }

{ "id": "崩", "rarity": "Green", "element": "Earth", "recipe": ["山", "朋"],
  "effects": [{ "kind": "Shield", "value": 70 }],
  "attackEffects": [
    { "kind": "DamageAll", "value": 50 },
    { "kind": "SpendMomentum", "value": 30 }] }

{ "id": "堡", "rarity": "Blue", "element": "Earth", "recipe": ["保", "土"],
  "effects": [{ "kind": "Shield", "value": 110 }],
  "attackEffects": [{ "kind": "Summon", "value": 240, "count": 1, "attack": 0,
                      "summonChar": "堡", "passive": { "thorns": 50, "taunt": true } }] }

{ "id": "碎", "rarity": "Blue", "element": "Earth", "recipe": ["石", "卒"],
  "effects": [{ "kind": "Shield", "value": 110 }],
  "attackEffects": [
    { "kind": "DamageSingle", "value": 77 },
    { "kind": "ArmorBreak", "value": 20 }] }

{ "id": "塔", "rarity": "Purple", "element": "Earth", "recipe": ["土", "荅"],
  "effects": [{ "kind": "Shield", "value": 150 }],
  "attackEffects": [{ "kind": "Summon", "value": 110, "count": 2, "attack": 50,
                      "summonChar": "塔" }] }

{ "id": "圭", "rarity": "Gold", "element": "Earth", "recipe": ["土", "土"],
  "effects": [{ "kind": "Shield", "value": 340 }],
  "attackEffects": [{ "kind": "DamageSingle", "value": 340 }] }

{ "id": "杜", "rarity": "Gold", "element": "Earth", "recipe": ["木", "土"],
  "effects": [
    { "kind": "Shield", "value": 238 },
    { "kind": "Immunity", "value": 2 }],
  "attackEffects": [
    { "kind": "DamageSingle", "value": 238 },
    { "kind": "Immunity", "value": 1 }] }

{ "id": "垚", "rarity": "Orange", "element": "Earth", "recipe": ["土", "圭"],
  "effects": [{ "kind": "Shield", "value": 430 }],
  "attackEffects": [{ "kind": "DamageSingle", "value": 301, "doubleVs": "ArmorBroken" }] }

{ "id": "㙓", "rarity": "Red", "element": "Earth", "recipe": ["土", "垚"],
  "effects": [{ "kind": "Shield", "value": 540, "persistOnce": true }],
  "attackEffects": [
    { "kind": "DamageAll", "value": 250 },
    { "kind": "SpendMomentum", "value": 60 }] }
```

- [ ] **Step 4: 取消 Task 4 的 Ignore**

在 `MomentumTests.cs` 里删掉 `SpendMomentum_AtZeroStacks_IsNoOp_ButSiblingEffectsStillFire` 上的 `[Ignore(...)]` 特性 —— `崩` 现在有发势了。

- [ ] **Step 5: 跑测试确认通过**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`

Expected: 全绿。既有断言里写死土系数值的(如 `圭` 200)按新表改。⚠ `MomentumTests.Cast_ShieldChar_GainsMomentum` 用了 圭 的旧值 200 → 改成 340(阈值 50 → 6 层 + 余 40)。

- [ ] **Step 6: 提交**

```bash
git add Brushblade/Assets/StreamingAssets/config/chars.json Brushblade/Assets/_Project/Tests/
git commit -m "$(cat <<'EOF'
feat(content): 土系 13 张改纯二选一双方向

effects = 加盾面,attackEffects = 攻击面。召唤字(碉/堡/塔)攻击面保留原召唤数值
——召唤的数值体系是另一套(血/攻/被动),不适用伤害锚点。
崩(绿)与 㙓(红)的攻击面挂「发势」,中档载体让前期就能玩到引爆。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 12: 火系两个数字

**Files:**
- Modify: `Brushblade/Assets/StreamingAssets/config/chars.json`
- Test: `Brushblade/Assets/_Project/Tests/DualDirectionTests.cs`

- [ ] **Step 1: 写失败测试**

```csharp
        [Test]
        public void FireOrangeAndRed_HaveCorrectTierOrdering()
        {
            // 修档位倒挂:燚(红) 的 AOE 100 曾低于 焱(橙) 的 120。
            var graph = LoadRealGraph();
            int yan = graph.Get("焱").Effects.First(e => e.Kind == EffectKind.DamageAll).Value;
            int yi = graph.Get("燚").Effects.First(e => e.Kind == EffectKind.DamageAll).Value;
            int fen = graph.Get("焚").Effects.First(e => e.Kind == EffectKind.DamageAll).Value;
            Assert.That(yan, Is.EqualTo(120));
            Assert.That(fen, Is.EqualTo(120), "相生取消后的等值改写");
            Assert.That(yi, Is.EqualTo(180), "红档 AOE 锚点 250 x0.7(带灼烧)");
            Assert.That(yi, Is.GreaterThan(yan), "红档必须强于橙档");

            // 焚 与 焱 数值相同,靠灼烧层数区分(焚 需要 林+火 跨系配方,更难合)
            int fenBurn = graph.Get("焚").Effects.First(e => e.Kind == EffectKind.BurnAll).Value;
            int yanBurn = graph.Get("焱").Effects.First(e => e.Kind == EffectKind.BurnAll).Value;
            Assert.That(fenBurn, Is.EqualTo(4));
            Assert.That(yanBurn, Is.EqualTo(3));
        }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 失败,燚 是 100、焚的 BurnAll 是 3

- [ ] **Step 3: 改字表**

- `燚` 的 `DamageAll` 从 `100` 改成 `180`
- `焚` 的 `BurnAll` 从 `3` 改成 `4`

⚠ `焚` 的 `DamageAll` 应该已经在 Task 6 里从 40 改成 120 了。确认一下。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q`
Expected: 全绿

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/StreamingAssets/config/chars.json Brushblade/Assets/_Project/Tests/DualDirectionTests.cs
git commit -m "$(cat <<'EOF'
fix(content): 修火系橙/红档位倒挂

燚(红) AOE 100→180 ——原本低于 焱(橙) 的 120,红档反而更弱。
180 = 红档 AOE 锚点 250 x0.7(带灼烧附加),与 㵘/㙓 的红档 AOE 250 自洽。
焚 灼烧 3→4:相生取消后它与 焱 数值完全相同,而 焚 需要 林+火 跨系配方、
严格更难合,多给一层做区分。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 13: 仿真工装记分

**Files:**
- Modify: `tools/balance/Program.cs`

⚠ **这个 task 不是可选项。** `Power()` 对 `Freeze/Slow/ArmorBreak` 记 0 分,而**记 0 分的字机器人永远不会出,那与「没把它加进出阵表」完全等价** —— 注释里写着这坑已踩过两次(焰、灯)。不改的话跑仿真读数不会有任何反应,而那会被误读成"这次改动没效果"。

- [ ] **Step 1: 给新 EffectKind 记分**

在 `Power()` 的 `switch` 里追加:

```csharp
                    // 势/水势的引爆(2026-09-02):按满层折算 —— 机器人不模拟攒层过程,
                    // 给个中位估值让它至少会去出这张字。系数是多少不重要,**是不是 0 才重要**。
                    case EffectKind.SpendMomentum: sum += e.Value * 5; break;
                    case EffectKind.SpendWaterPower: sum += e.Value * 5; break;
```

同时把 `Shield` 与 `HealSelf` 的记分从 `e.Value / 2` 抬到 `e.Value`(它们现在攒势/水势,不再是纯亏 tempo):

```csharp
                    // 2026-09-02:护盾/治疗改记全额 —— 它们现在攒势/水势,
                    // 折半记分是「防御没有进攻价值」那个旧模型的残留。
                    case EffectKind.Shield: sum += e.Value; break;
                    case EffectKind.HealSelf: sum += e.Value; break;
                    case EffectKind.HealAll: sum += e.Value; break;
```

- [ ] **Step 2: 加水土画像**

在 `profiles` 数组里追加两档(照既有 `Profile` 构造抄):

```csharp
                // 2026-09-02 双方向对照组:不加这两档就没有任何观测点能看见水/土的改动。
                new Profile("水系双方向(冻沝淼㵘,卡5级,10级)", new[] { "冻", "沝", "淼", "㵘" },
                    WaterCards.ToDictionary(c => c, _ => 5), level: 10),
                new Profile("土系双方向(垒圭垚㙓,卡5级,10级)", new[] { "垒", "圭", "垚", "㙓" },
                    EarthCards.ToDictionary(c => c, _ => 5), level: 10),
```

并在 `FireCards` 旁加两张出阵表:

```csharp
        /// <summary>水系出阵表(2026-09-02 双方向对照组)。15 张实体字全列 ——
        /// 漏掉的字机器人摸不到(回合掉字与合成都锁 UnlockedChars),那一档观测点就是空的。</summary>
        private static readonly string[] WaterCards =
            { "溃", "冻", "海", "冷", "浴", "湮", "沏", "澡", "沝", "冰", "沐", "淼", "淡", "淋", "㵘" };

        /// <summary>土系出阵表(2026-09-02)。13 张实体字全列。</summary>
        private static readonly string[] EarthCards =
            { "碉", "砸", "碾", "垒", "壁", "崩", "堡", "碎", "塔", "圭", "杜", "垚", "㙓" };
```

- [ ] **Step 3: 跑仿真**

Run:
```bash
cd tools/balance && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet run --nologo
```

Expected: 跑完输出各画像的 P50 深度。**记录下水系/土系两档的读数**,与火系对照 —— 这是本次改动唯一的量化验收。

⚠ 机器人不会用方向选择器(它走 `Cast(id, target)`,默认 `attackMode: false` = 治疗/加盾面),所以这两档量到的是**防御面 + 势/水势**的价值,不是双方向的完整价值。这是仪器的已知局限,**读数时要说明,别当成双方向的总评**。

- [ ] **Step 4: 提交**

```bash
git add tools/balance/Program.cs
git commit -m "$(cat <<'EOF'
chore(balance): 仿真工装补势/水势记分与水土画像

Power() 对新 EffectKind 记 0 分等于机器人永远不出这些字(焰/灯 踩过两次)。
护盾/治疗从记半额抬到全额——它们现在攒势/水势,折半是旧模型的残留。
补两档水/土双方向画像当对照组,否则改完跑仿真读数毫无反应。

已知局限:机器人默认走 attackMode:false,量到的是防御面而非双方向全貌。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## 步 3 · 文案

### Task 14: 字符串表与字体子集

**Files:**
- Modify: `Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`
- Modify: `Brushblade/Assets/.../fonts/*`(由脚本生成)

- [ ] **Step 1: 确认怪名进了字符串表**

7 只新怪的名字(枯笔/版牍/火漆/窑变/砚台/铜钤/宿墨)是否需要进字符串表,取决于项目怎么显示怪名。

Run: `grep -n "错字鬼\|缺笔妖" Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json`

- 若既有怪名**在字符串表里** → 照同样的 key 格式给 7 只新怪各加一条
- 若既有怪名**只在 enemies.json 里**(id 直接当显示名)→ 不用加,跳到 Step 2

- [ ] **Step 2: 重跑字体子集**

⚠ **这一步不能跳过。** 游戏打包的是**子集字体**(只含实际用到的字形),新字不在里面就会在真机上显示成空白/豆腐块 —— 而离线编译和 Core 单测**都发现不了**。

本次新增的字形至少有:`攻` `护` 以及 7 个怪名里的 `枯` `笔` `版` `牍` `漆` `窑` `变` `砚` `台` `铜` `钤` `宿` `墨`。

Run:
```bash
python3 tools/fonts/subset_fonts.py
```

- [ ] **Step 3: 跑字体测试**

Run:
```bash
# tools/fonts/raw 不入 git,worktree 里没有,只能软链
ln -s /Users/eugenewu/code/game/tools/fonts/raw tools/fonts/raw
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
```
Expected: 全绿

- [ ] **Step 4: 核对 cmap(不是看 diff 大小)**

⚠ 重新生成必然产生时间戳 churn,**光看 diff 大小说明不了变没变**。要真的确认新字进了子集,比 cmap:

```bash
python3 -c "
from fontTools.ttLib import TTFont
import glob
for path in glob.glob('Brushblade/Assets/**/*.ttf', recursive=True) + glob.glob('Brushblade/Assets/**/*.otf', recursive=True):
    cmap = TTFont(path).getBestCmap()
    missing = [c for c in '攻护枯笔版牍漆窑变砚台铜钤宿墨' if ord(c) not in cmap]
    print(path, '缺:', ''.join(missing) if missing else '无')
"
```
Expected: 每个字体文件都输出「缺: 无」。**有任何一个缺字就回 Step 2 重跑,别往下走。**

- [ ] **Step 5: 提交**

```bash
git add Brushblade/Assets/StreamingAssets/config/strings.zh-CN.json Brushblade/Assets/
git commit -m "$(cat <<'EOF'
chore(fonts): 重跑字体子集,纳入方向按钮与护甲怪新字

新增字形:攻 护 枯 笔 版 牍 漆 窑 变 砚 台 铜 钤 宿 墨。
子集不含新字会在真机上显示成豆腐块,而离线编译与 Core 单测都发现不了。
已比对 cmap 确认全部入表(不是看 diff 大小)。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## 收尾验证

全部 task 做完后跑一遍完整验证:

```bash
cd tools/coretests && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet test --nologo -v q
cd tools/prescompile && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet build --nologo -v q -p:ProjectAsm=/Users/eugenewu/code/game/Brushblade/Library/ScriptAssemblies
python3 -m pytest tools/pipeline/tests/ tools/fonts/tests/ -q
cd tools/balance && /Applications/Unity/Hub/Editor/6000.5.2f1/Unity.app/Contents/Resources/Scripting/DotNetSdk/dotnet run --nologo
```

三绿 + 仿真读数记录下来,然后把分支名交给用户合并。**不要自己动 main。**
